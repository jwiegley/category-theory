# Key Files and Concepts

The per-development index of this library's headline developments: one
bullet per landed development, grouped thematically under five headings
(Theory Core, Structures, Constructions, Concrete Instances, Applied
Programming — the grouping is by subject, not by directory: `Structure/Topos.v`
sits under Theory Core), each recording what was delivered, at what strength
(`eq_refl`, `≈`, `Cat` against `StrictCat`), what was measured and by what
criterion (universe binders and constraint blocks, axiom closure, dependency
closure, `Fail` probes and their kinds), and what was NOT delivered. Until
2026-09-09 this was the "Key Files and Concepts" section of CLAUDE.md; it was
moved here with every bullet unchanged because CLAUDE.md is loaded into every
session and had grown past a megabyte. Any `.v` header, commit message, plan
document or issue checkbox written before that date that refers to the
CLAUDE.md Key Files index, to a bullet or figure in it ("the CLAUDE.md
bullet", what "CLAUDE.md records" or "carries"), or to a count of occurrences
in CLAUDE.md, refers to the entries or the count in this file.
Cross-references inside bullets to a bullet "above" or "below" are relative
to the section order here, which is the original order.

Conventions. One bullet per development, appended under the heading its
sibling developments already sit under. Every figure in a bullet is a
measurement with its criterion stated; a claim that cannot be re-measured from
the sentence does not belong here. Read a file's bullet before editing the
file (`grep -n 'File.v' docs/INDEX.md` on the basename, since some bullets
spell paths relative to their section, then read that line): it records the
traps already met. About a third of the `.v` files head a bullet of their own
(just over 300 of 926 on 2026-09-09, counting distinct bold heads), so
absence is not a signal. An `Edit` that inserts a bullet has twice eaten the
heading that followed it, so check `grep -c '^- \*\*' docs/INDEX.md` and
`grep -c '^## ' docs/INDEX.md` before and after inserting; `make index-check`
(run by lefthook and CI) fails if the five `## ` headings are not all present
in order. Companion documents: docs/AXIOMS.md (the axiom audit),
docs/INHABITATION.md (which flagship results have a concrete in-tree
witness), docs/SIZE.md (size and foundations), docs/NOTATION.md.

## Theory Core
- **Theory/Category.v**: Defines categories with setoid hom-sets
- **Theory/Functor.v**: Functors preserve equivalence — with the §I.3/§I.5 closure and reflection results: `Full_Compose`/`Faithful_Compose`, and `faithful_reflects_monic`/`_epic` (a faithful functor reflects both cancellation properties), from which the concrete-category corollary in Theory/Concrete/Morphisms.v follows. Subcategory inclusions are faithful generically (`Incl_Faithful`, Construction/Subcategory.v); the converse "full inclusion ⟹ full subcategory" needs the selected morphisms to be closed under `≈`, which the `Subcategory` record does not supply and which Construction/Subcategory/FullConverse.v proves NECESSARY by counterexample
- **Theory/Shapes.v**: functors out of the walking shapes (Mac Lane §I.3) — objects ≃ functors `_1 ⟶ C` (`Point`/`point_of`), arrows ≃ functors `_2 ⟶ C` (`Walk`/`arrow_of`), composable pairs ≃ functors `_3 ⟶ C` (`Functor_of_Pair`), each an explicit construction pair with both round trips rather than a bijection class, the `_1` and `_2` ones landing at the strict `Functor_StrictEq_Setoid`. The category-level comparisons `One_Fun_iso : [_1, C] ≅[Cat] C` and `Two_Fun_Arrow : [_2, C] ≅[Cat] Arrow C` — both at `C : Category@{o Set Set}`, forced on the `_2` side because `Fun` identifies source and target hom universes while `_2`'s homs live in `Set` — close the gap `Construction/Arrow.v` previously disclosed as documentation-level — read their strength precisely: `≅[Cat]` is EQUIVALENCE, `Arrow_Fun_Arrow_strict` is strict on one side only, and the `StrictCat` isomorphism is explicitly not delivered (the object component would be a Leibniz equality of functor records, which `fmap_id` alone does not give). Seven Sketches instantiations: the three functors `_2 ⟶ _3` of Example 3.36 exhibited as named morphisms and proved pairwise non-equivalent, the six-functor count of Exercise 3.37 DERIVED by transporting the morphism count of `3` rather than enumerated, and Exercise 3.45's "functors from 1 to Sets are sets" turned from comment prose into a theorem. Note the name: the pre-existing `Instance/Shapes.v` is unrelated (its `Shape` indexes tries)
- **Theory/Diagram.v**: Diagrams and commutativity — a diagram in `C` of shape `G` is a quiver homomorphism `Diagram G C := QuiverHomomorphism G (QuiverOfCat C)`, path denotation `dpath` composes edge labels along a `tlist` path, and `Commutative D` quantifies over EVERY pair of vertices and EVERY pair of parallel paths of arbitrary length (Mac Lane §I.1, no shape privileged — unlike the tree's figure-fixing statements `Construction/Sq.v`'s `dsq`, `Structure/Cone.v`'s `cone_coherence`, and `Theory/Morphisms/Stability.v`'s `is_pullback_commutes`, the last two of which are recovered as instances). The bridge to the free category runs both ways with both round trips proved — `FunctorOfDiagram`/`DiagramOfFunctor`, `functor_of_diagram_of_functor` in StrictCat's hom-setoid and `diagram_of_functor_of_diagram` in `QuiverCategory` — and under it `Commutative` coincides with the Seven Sketches diagram-as-functor predicate `CommutativeShape` (`commutative_iff_shape`, `shape_iff_commutative`); respectfulness and naturality of the two maps are disclosed as NOT established, so the two maps are mutually inverse up to `≈` pointwise — weaker than a bijection of the underlying types (the round trips are up to `≈`, not `=`) and weaker than one of the setoid quotients (without respectfulness the maps do not descend), and in particular not a hom-setoid isomorphism. Walking square and triangle with the general predicate proved EQUIVALENT to the single two-composite equation (`square_commutative_iff`, `triangle_commutative_iff`), the naturality square as such a diagram (`naturality_square_commutative_iff`), Riehl's Lemma 1.6.20 for arbitrary path length (`faithful_reflects_commutative`), and the Solver-style term denotation `dterm` over `Construction/Free/Quiver.v`'s `Mor` with `dterm_dpath`/`commutative_dterm`. Concrete axiom-free witnesses in `Coq` escape BOTH vacuity traps, each escape proved: the shape genuinely has two distinct parallel paths (`sq_via_B_neq_via_C`, in Theory/Diagram.v) and the target is provably not thin (`coq_parallel_not_equiv`), so `coq_square_commutative` is not commuting by collapse — and `coq_square_not_commutative` is a provably NON-commuting diagram over the SAME quiver into the SAME category (Theory/Diagram/Examples.v). The empty path is not inert: `loop_commutative_iff` proves that over the walking loop `Commutative` is EXACTLY "the drawn endomorphism is `id`", with concrete `Coq` witnesses both ways (`coq_loop_negb_not_commutative`, `coq_loop_id_commutative`); the header is careful about what this separates — `dsq` quantifies over arbitrary OBJECTS, so `dsq X X X X e id id id` reduces to `e ≈ id` and CAN state the loop condition, the real distinction being that `Commutative` FORCES it from the shape alone (the empty path is always available as a competitor) while `dsq` must be instantiated to it, and `cone_coherence`/`is_pullback_commutes` are record FIELDS that cannot be freely instantiated at all. The two walking shapes are acyclic, proved by a rank argument (`sq_endo_paths_are_nil`, `tri_endo_paths_are_nil`) rather than inferred from empty self-edge sets — which would not suffice, a directed cycle of length ≥ 2 having no self-edge. The Seven Sketches footnote packaging (commutes ⟺ factors through the preorder reflection) IS proved, in both directions (`commutative_shape_factors`, `factors_commutative_shape`, `factors_commutative_shape'`, `commutative_factors`), and packaged as the single biconditional `commutes_iff_factors`, with the forward factorization an equality of FUNCTORS in StrictCat's hom-setoid (`shape_factor_functor`), over **Construction/PreorderReflection.v** — the preorder reflection as the quotient of a category by the TOTAL hom-congruence, `PreorderReflect C := Quotient C (TotalRel C)` with `preorder_reflect_thin` and the identity-on-objects `Reflect`, plus the universal property `ThinLift`/`ThinLift_proj`/`ThinLift_unique` showing every functor into a thin category factors through `Reflect` uniquely — so it is THE universal thin quotient, which is what earns the word "reflection" rather than an nLab pointer. This is NOT the nLab's *posetal* reflection, which identifies OBJECTS by mutual relatedness; `preorder_reflect_obj` proves objects are untouched definitionally. It needed no new machinery (`Construction/Quotient.v` already has the hom-congruence quotient and `QuotientLift`; all FOUR `HomCongruence` fields are immediate — reflexivity is the derived `cong_refl`, not a field), and it supplies work item 1 of issue #803's SEVEN, the other six remaining open; #803's plan supplies the module name and `PreorderReflect`, the other names being this file's own
- **Theory/Natural/Transformation.v**: Natural transformations — the arrows-only (object-free) presentation is Theory/Natural/Transformation/Arrows.v (Mac Lane §I.4 exercise 5): `ArrowTransform` indexes a morphism `S c ~> T c'` by each arrow `f : c ~> c'` under two splice laws, with the round trip to `Transform` (components `τ_arr id`) both ways, the four uniqueness statements, and the correspondence packaged as an `Isomorphism` in `Sets` between the two setoids. Two results worth knowing: respectfulness of the family in its arrow argument is DERIVABLE from the two laws rather than extra data (each law supplies one leg of the detour through `f ∘ id`), and the two splice laws are genuinely INDEPENDENT — `dom_family`/`cod_family`, indexed by the WALKING ARROW `_2` with the parallel-pair category as target (the target is what supplies the two non-equivalent arrows that separate; the index is where the domain and codomain of a non-identity arrow come apart), satisfy one law and refute the other, so neither field can be dropped. A closing section settles the companion question with countermodels over a hom-setoid that genuinely quotients: NEITHER law alone implies respectfulness (`left_law_alone_gives_no_respect`, `right_law_alone_gives_no_respect`), so the derivation's use of both laws is essential rather than an artifact of the proof. This is unlike `naturality`/`naturality_sym` in the parent file, which are one equation in two orientations
- **Theory/Adjunction.v**: The most important concept - adjunctions
- **Adjunction/Conjugate.v**: Mac Lane §IV.7 — conjugate natural transformations, the mate OPERATOR upgraded to the mate RELATION. `Conjugate A A' σ τ` IS the hom-set square `⌊k ∘ σ x⌋' ≈ τ a ∘ ⌊k⌋` quantified over every transposable `k`, not its value at one distinguished argument, with the inverse-transpose reading `ConjugateFrom` proved equivalent (`conjugate_iff_from`); `conjugate_characterizations` proves four pasting forms all equivalent to it — the two mate formulas `τ = U'ε ∘ U'σU ∘ η'U` and `σ = ε'F ∘ F'τF ∘ F'η`, which are what `mate`/`mate_inv` compute, and the unit and counit equations `τF ∘ η = U'σ ∘ η'` and `ε ∘ σU = ε' ∘ F'τ`, the counit equation having had no in-tree statement at all and the unit equation only the degenerate `F = F'` form of Theory/Bicategory/Adjunction.v's `mate_charac`/`mate_unit_compat`. Each transformation of left adjoints has one and only one conjugate and dually (`conjugate_unique_right`/`conjugate_unique_left`, by evaluating the square at the universal arrows ε and id), packaged as `conjugate_bijection : (F' ⟹ F) ≊ (U ⟹ U')` in Sets — the ordinary-category form of Theory/Bicategory/Mates.v's `mate_iso`, developed over arbitrary C and D in a seven-module cone with no bicategorical machinery (the Cat route stays available at any size, at the cost of Instance/Cat and the bicategory chain). Riehl §4.3(iv) rides it: `conjugate_invertible_iff` (σ pointwise invertible ⟺ its conjugate is) is proved from the square alone, using neither transformation's naturality, so Mates.v's descope ledger 10 was untouched by that file — `conjugate_id`/`conjugate_compose` are that ledger entry's identity-bounding-cell shadow, not its discharge, and the VERTICAL half of what `Instance/Adj/Bicategory.v` later completes horizontally, narrowing the note without discharging it. The bridge is Instance/Cat/Bicategory/Conjugate.v: padding transforms (`F' ◯ Id ⟹ Id ◯ F` is not `F' ⟹ F` definitionally, the two functor records carrying different `fmap_respects`/`fmap_id`/`fmap_comp` fields, which are data here and which record eta cannot identify), `Cat_conj_mate_agrees`/`Cat_conj_mate_inv_agrees` identifying `conj_mate`/`conj_mate_inv` with `mate`/`mate_inv` at identity bounding cells through the transparent `Cat_Adjunction_BicatAdjunction` rather than the Qed-opaque `Cat_BicatAdjunction_Adjunction_iff`, `conjugate_bijection_is_mate_iso`, equating the forward leg of `conjugate_bijection` with `mate_iso`'s forward leg conjugated by the two padding isomorphisms, the `Cat_mate_inv_unfold_raw` the tree lacked, and the boundary witness `mate_of_iso_not_invertible` — an invertible 2-cell whose mate over the non-identity bounding functor `Erase _2` is not invertible
- **Adjunction/Map.v**: Mac Lane §IV.7 definition 1 and Proposition 1 (book p. 99) + Riehl §4.2 Exercise 4.2.v (printed p. 143) — **maps of adjunctions, delivered in BOTH the strict and the `≈` reading over ONE engine that needs neither**. The engine is `Section MapCompare`: an arbitrary pair of comparison ISOMORPHISM families `al : ∀ x, K (F x) ≅ F' (L x)` and `be : ∀ a, L (U a) ≅ U' (K a)`, over which `MapAdjHom`/`MapAdjUnit`/`MapAdjCounit` are stated and the three-way equivalence is proved ONCE (`map_adj_hom_iff_unit`, `map_adj_hom_iff_counit`, `map_adj_unit_iff_counit`). **The naturality hypotheses are EXPLICIT ARGUMENTS, so each passage's TYPE records what it consumes** — measured, not asserted: `map_hom_to_unit` and `map_hom_to_counit` consume NEITHER square's naturality, `map_unit_to_hom` only the RIGHT, `map_counit_to_hom` only the LEFT. **BOTH READINGS ARE INSTANTIATIONS OF THAT ENGINE, AND THEY ARE NOT PARALLEL CONSTRUCTIONS**: the strict `Record AdjSquares` (Leibniz object equations `sq_left`/`sq_right` plus their naturality, with the hom-set condition written through `id_cast`) and the `≈` `Record WeakAdjSquares` (fields `wsq_left : wsq_K ◯ F ≈ F' ◯ wsq_L`), related by **`strict_hom_is_weak_hom : SquaresHom A A' S = WeakSquaresHom (WeakAdjSquares_of_AdjSquares S)` at LEIBNIZ EQUALITY OF TYPES by `reflexivity`**, so `WeakMapOfAdjunctions_of_MapOfAdjunctions` carries `map_hom` across unchanged with no proof. `SquaresHom` is quantified over EVERY `x : D`, `a : C` and `k : F x ~> a` — the issue's explicit reviewer check — and `map_of_adjunctions_unit_iff_counit` and `MapOfAdjunctions` are its two pinned names. **THE ISSUE IS INTERNALLY INCONSISTENT ON THE MODELLING DECISION AND BOTH ITS HALVES ARE DELIVERED, SO NOTHING OF IT IS REFUTED — what IS refuted is the COORDINATOR'S OBJECTION, and the file says which.** Its Work item 1 asks for "the two strict commutation equations for the functor squares" and its Work item 4 calls the notion "the strict special case", while its modelling note directs "Deliver the `≈` version (which is the usable one)"; both readings are delivered and the decision is disclosed, which is what its Definition of Done actually asks. Strict is primary here on source fidelity, both books stating the definition strictly (Riehl's exercise is titled "**strict** morphism of adjunctions", writes `KF = F′H`, and joins her display with `═`; Mac Lane's display (3) uses a double bar `‖`), so strict is primary here and the `≈` version rides alongside. The coordinator's brief had claimed the `≈` condition "does not even typecheck without transport"; the OBSERVATION is right about the naive spelling (pinned as the probe's TYPING negative 1) but the CONCLUSION is wrong — a `Functor_Setoid` equality supplies exactly the isomorphism family the engine wants, and inserting `from (al x)`/`to (be a)` makes the condition well typed with NO transport at all. **The identity-bounding-functor case IS `Adjunction/Conjugate.v`, ON THE NOSE**: `map_adj_hom_is_conjugate : MapAdjHom A A' Id[C] Id[D] al be = Conjugate A A' conj_sigma conj_tau` by `reflexivity`, again Leibniz equality of TYPES, and **no padding transformation is needed** — `Id`'s object and arrow actions are the literal identities, so the `F' ◯ Id` trap `Instance/Cat/Bicategory/Conjugate.v` records is bicategorical and does not arise on this route. **SCOPE THAT IDENTIFICATION EXACTLY**: a map of adjunctions supplies INVERTIBLE comparison data while `Conjugate σ τ` demands no invertibility of either transformation, so what is recovered is a conjugate pair whose two transformations are POINTWISE INVERTIBLE, and not every conjugate pair arises this way — pinned as a TYPING negative; the converse, that every pointwise-invertible conjugate pair does arise, is NOT built (no constant in the file takes an `IsIsomorphism`-family hypothesis), so only the one inclusion is machine-checked (a bare `Transform` does not inhabit the iso family), with `Conjugate.v`'s own `conjugate_invertible_iff` recording that the two invertibility conditions imply each other under the square. **The issue's Current-state section is stale in what it OMITS rather than in what it asserts**: its clause "no lemma writes either the unit condition or the counit condition" is TRUE as scoped, since `ConjugateUnit` and `ConjugateCounit` quantify over no `K` or `L` at all and Mac Lane's conditions `Lη = η'L` and `ε'K = Kε` are about a bounding functor — what the issue misses is that `Adjunction/Conjugate.v` exists at all, naming `Theory/Bicategory/Mates.v` as "the nearest relative" though Conjugate.v is the SAME section of the SAME book and is exactly this file's identity-bounding-functor case; and `Instance/Adj.v`'s own caveat block already names `Conjugate` as Mac Lane's §IV.7 hom-set square; that file is UNTOUCHED and its proposed hom retyping is a declined follow-on. Mac Lane's literal displays are `squares_unit_fused`/`squares_counit_fused` (`Lη = η'L`, `Kε = ε'K` with the whole identification on one side). Identity and composition exist (`AdjSquares_id`, `AdjSquares_compose`, `MapOfAdjunctions_id`, `MapOfAdjunctions_compose`, with `eq_refl` readbacks for both object actions), but **NO associativity or unit law is stated and the unit law is REFUTED at `eq_refl` and pinned** — `AdjSquares_compose` builds `sq_K M ◯ Id[C]`, not `sq_K M` — so there is no category or 2-category of adjunctions here; stating a law up to `≈` would first need a setoid on maps, which is not built. By-product, measured absent by TYPE rather than by name: **`AdjunctionProduct : F ⊣ U → G ⊣ V → (F ∏⟶ G) ⊣ (U ∏⟶ V)`** — no term in the tree has a type of that shape, and the only name pairing "Product" with "Adjunction" is `Diagonal_Product_Adjunction`, a different construction. Two `id_cast` lemmas absent from `Construction/Quotient.v` are stated here and disclosed as belonging beside their donors (`id_cast_sym_trans`, `fmap_id_cast_sym`). **The squares are written in `id_cast` form rather than through `Functor_StrictEq_Setoid` on a MEASURED ground**: requiring `Theory/Skeleton.v`, where `strict_equiv_of_id_cast_nat` lives, takes the closure from **21 to 32** modules; the PROBE pays that once and proves the record's fields ARE that constant's arguments by application alone, with no tactic and no transport. Non-vacuity is honest about its two halves: `SwapMap` is built over an ARBITRARY adjunction (its product with itself) and both square equations are `eq_refl`, but its hom obligation closes by identity-cancellation because `Swap` and `_ ∏⟶ _` are both spelled with `fst`/`snd` — a computation, not a chase — while the UNCONDITIONAL separation `mapadj_two_swap_not_identity` instantiates at `MapAdjTwo`, a two-object category whose hom-setoid is trivially true, so that separation is OBJECT-LEVEL only and the file says so. **126/126 constants closed under the global context** — 118 `Parameter`/`Definition` entries from `Print Module` (66 `Parameter` + 52 `Definition`), of which **26** are `Program`-obligation heads and **14** are the four records' field projections, neither visible to a source sweep; 26 + 14 added to the 78 source-declared heads is exactly 118, which checks better than either number alone (and note 26 matches neither the 13 `Next Obligation` blocks nor the 11 `Program Definition`s in the source) plus the four records and their four `Build_*`, which `Print Module` lists only after a `:=`; the gate's `closed` count rose by exactly 126. **ZERO `Defined` tokens in either file** (53 `Qed` in the library): nothing is load-bearing because nothing exists to flip, and all fourteen `reflexivity`-closed statements compare TYPES or actions built from plain `Definition`s, record literals and transparent `Program Definition` bodies, never through a `Qed` proof. Universes measured off BOTH binder and block: `AdjSquares` is over `C : Category@{u u0 u0}`, `D : Category@{u1 u0 u0}`, `C' : Category@{u2 u3 u3}`, `D' : Category@{u4 u3 u3}` — hom identified with proof, AND C's hom level with D's, by REUSING the level variable in the BINDER — while its block carries only the bound `u0 <= u3` and NO equation, all four object universes free; the engine's theorems read the OTHER way, `map_adj_unit_iff_counit`'s binder keeping the hom levels apart while its BLOCK carries `u0 = u2` and `u6 = u8`. **`Set` occurs ZERO times in any binder or block across all 126.** `Test/ProbeMapAdj393.v` carries **9 `Fail`s = 1 instrument + 8 negatives of THREE kinds** (3 TYPING, 2 CONVERSION, 3 FORMABILITY), each stripped ONE AT A TIME and compiled alone with its whole error read; **`Isomorphism` and `Functor` are shown NOT to be donors**, both accepted at exactly the levels where `AdjSquares`, `id_cast` and `Adjunction` are each refused alone, the last two being independent donors (neither command mentions the other). Rename-simulated 8/8, and every constant a negative names also appears outside a `Fail`. `make todo` grows by 10, ALL in the probe, the library contributing ZERO. One real collision was found and renamed before landing (`TwoPt` is `Instance/Top/Wedge.v`, hence `MapAdjTwo`). A brief-level correction worth carrying: Riehl's letters are NOT a straight swap of Mac Lane's — read off her diagram, her `K : D → D'` runs between the codomains of the left adjoints and so IS Mac Lane's `K`, while her `H : C → C'` is his `L`. NOT delivered: the Mates identification is NOT machine-checked and is not asserted; no converse `WeakMapOfAdjunctions → MapOfAdjunctions`; no transport of a map along an equivalence and no relation to `Adjunction/Compose.v`; nothing about when `K` or `L` is full, faithful or an equivalence; and nothing registered as an `Instance`
- **Adjunction/Parameter.v**: Mac Lane §IV.7 Theorem 3 with its remark and Exercise 2 (book pp. 101–102, `maclane:IV.7:thm3`/`remark1`/`ex2`) + §IX.4 Ex 2 (`maclane:IX.4:ex2`) + Riehl §4.4 Proposition 4.4.6 and Exercise 4.4.ii (`riehl:4.4:prop6`, `riehl:4.4:exii`) — **adjunctions with a parameter: the right adjoints of a bifunctor's partial functors extend UNIQUELY to a bifunctor contravariant in the parameter, with the bijection natural in all three variables**. `Record ParametrizedAdjunction (F : X ∏ P ⟶ A)` carries `pa_right : P → (A ⟶ X)` and `pa_adj (p) : Partial_l F p ⊣ pa_right p`, and **`parametrized_right_adjoint_bifunctor : P^op ∏ A ⟶ X`** is Mac Lane's `G` (both names are the issue's pinned ones, delivered verbatim). **THE FUNCTORIALITY IS CONSUMED FROM #394, NOT RECOMPUTED, WHICH IS WHAT THE THEOREM ACTUALLY SAYS**: `pa_param_mate` is `conj_mate` and nothing else (`pa_param_mate_is_conj_mate`, `eq_refl`), `pa_param_mate_id` ends `apply conj_mate_id` and `pa_param_mate_comp` ends `apply conj_mate_compose` — both of those donors being themselves proved by `apply conj_mate_uniq` — so Mac Lane's "the uniqueness of the choice of conjugates shows for `h'h` that `G(h'h,−) = G(h,−) ∘ G(h',−)`, so that `G(−,a)` is a functor and `G` a bifunctor" is discharged BY NAME; a donor-use census over the file gives `conj_mate_uniq` 5, `conj_mate` 4, `conj_mate_respects` 3, `conj_mate_id` 3, `conj_mate_compose` 3, `conjugate_iff_from` 2, `conjugate_unique_right` 1, `conjugate_conj_mate` 1, and no conjugacy is re-derived anywhere. **`pa_square_is_Conjugate` closes at `eq_refl` ON THE TYPES**, so his "must be chosen as the conjugate to `F(−,h)`" is true by construction rather than by argument, with `pa_square_from_is_ConjugateFrom` the inverse-transpose twin and `pa_square_iff_from` a `:=` of `conjugate_iff_from`. UNIQUENESS is delivered at Mac Lane's own quantifier — `pa_assignment_unique` ranges over an arbitrary ASSIGNMENT `∀ p p' (h : p' ~> p), G p ⟹ G p'` whose square commutes and shows it agrees with the mate everywhere, carried up to the bifunctor by `pa_bifunctor_fmap_determined`, with `pa_param_mate_universal : ∃! τ, pa_square h τ`; **no bare `∃!` over a TYPE of bifunctors is stated**, since that would need a Leibniz equation on `fobj` and hence a transport. **THE DUAL IS READ OFF THE PRINTED PAGE, AND THE PAGE SAYS SOMETHING OTHER THAN THE OBVIOUS GUESS**: Mac Lane writes "given a bifunctor `G : P^op × A → X` where each `G(p,−)` has a **right** adjoint `F(−,p)`", so the hypothesis makes `G`'s partial functors LEFT adjoints and `F` comes out COVARIANT in the parameter — not the same adjunction read backwards — and `parametrized_left_adjoint_bifunctor : X ∏ P ⟶ A` contains no `op` at all, the whole `copa_*` mirror going through `conj_mate` at swapped arguments with `copa_param_mate_comp` covariant. The `Swap` route is BUILT rather than argued about (`copa_swap_route`), and measured to produce a bifunctor `P ∏ X ⟶ A` — the arguments in the OPPOSITE ORDER to the book — so the direct route is the one taken. **THE CURRYING INSTANCE PROVES PARAMETER NATURALITY RATHER THAN ASSUMING IT, which is the issue's second reviewer check and is unstatable against the class**: `Structure/Cartesian/Closed.v`'s `Closed` asks only for `exp_iso` plus the beta law, with NO naturality field in any variable, so `curry_natural_param` is delivered as a STANDALONE lemma in plain `curry`/`second`/`eval` vocabulary for that file's users, with `uncurry_natural_param`, `curry_natural_param_from_mate` and `_routes_agree` beside it. `curry_ParametrizedAdjunction` cannot reuse `Curry_Adjunction` by ascription and the obstruction is EXACT: `Partial_l ×(C) p` and `Prod_Functor p` agree on OBJECTS on the nose (`curry_prod_functor_obj`) but their arrow actions differ by one `id_left` — `bimap f id = (f ∘ exl) △ (id ∘ exr)` against `first f = (f ∘ exl) △ exr` — so a field copy is used, and it is rejected DISCRIMINATINGLY, getting past `adj` (which mentions `fobj` alone, and `fobj` converts) and stopping at `to_adj_nat_l`, the first field mentioning `fmap[F] g`. Five `eq_refl` readbacks pin the instance: `pa_right` IS `Exp_Functor`, the counit IS `eval`, `pa_to` IS `curry`, `pa_from` IS `uncurry`. **EXERCISE 2'S ANSWER TURNS OUT TO BE #394's `ConjugateUnit`**: `pa_unit_dinatural` is ONE application of `conjugate_to_unit` at §C's conjugate pair, and the packaged form **`pa_unit_Wedge x : Wedge (UnitIntegrand x)`** inhabits `Structure/Wedge.v`'s OWN class with apex `x` and the units as legs (`_obj`/`_map` at `eq_refl`), the wedge obligation's two normalisations closing by `reflexivity` so that clearing three identity residues leaves exactly the elementary equation; `pa_counit_extranatural` is the counit's mirror from `conjugate_to_counit`. **`Theory/Dinatural.v`'s `Dinatural` does NOT fit, for three measured reasons and not by preference**: the source here is constant, which is what `Structure/Wedge.v`'s own header says a wedge is for; and even given a constant bifunctor — five lines, ZERO obligations, compiled out of tree — the dinaturality hexagon is NOT the wedge condition at `eq_refl`, the two being interderivable by two `id_right`s and a `symmetry`, both shipped as passing probe controls. That module is consequently not required (measured: not in the closure; it would cost 1). Riehl 4.4.6(iii) is **`mutually_right_adjoint : AdjointOnTheRight mr_left mr_right`** in `Adjunction/Right.v`'s OWN class — a RECORD LITERAL with no tactic — where `mr_left := Partial_l G c : P^op ⟶ X` and `mr_right := Partial_l H c : X^op ⟶ P` make `aor` read `P(p, H(x,c)) ≅ X(x, G(p,c))`; the mirror hypothesis needs no new record (`Partial_l (F ◯ Swap) x` and `Partial_r F x` agree in BOTH data fields on the nose), and only TWO of the four naturality fields are proved directly, the other two obtained by conjugating those with the isomorphism. Riehl Exercise 4.4.ii IS that proposition and is recorded as not separate work. §G's `ParametrizedAdjunctionTransform` and `parametrized_adjunction_iff_transform` give §IX.4 Ex 2's unit/counit presentation, with both parametrized triangles explicitly NOT claimed new (each a bare `exact` of `counit_fmap_unit`/`fmap_counit_unit` at a fixed `p`, re-exposed so the display is visible with `p` written out). **THE ISSUE'S PRIOR ART IS STALE IN THREE PLACES AND THE HEADER SAYS SO**: `LeftAdjointFunctorFromUniversalArrows` is in `Theory/Universal/Arrow.v`, not where the issue points; "searches for parametrised, parameterised or two-variable adjunctions return nothing" is true only of DECLARATION HEADS, the bare words occurring as prose in 39 `.v` files; and work item 6's premise is stale in both halves — **`ModTensor : RMod R ∏ RMod R ⟶ RMod R` already exists at `Instance/Mod/Monoidal.v` with no commutativity hypothesis**, and `Instance/Mod/Closed.v` supplies `RMod_SymMonClosed`, so the module tensor-hom instance is declined on MARGINAL closure cost (+26, 68 → 94) rather than for want of the categories. **179/179 constants closed under the global context with ZERO `Axioms:` lines**, counted from `Print Module` under a stated criterion — entries sit at exactly 5-space indent, giving 99 `Definition` + 74 `Parameter` + 3 `Record` = 176 distinct, plus the 3 `Build_*` the printer lists only inline after a `:=` — where **the module head AND all three `Record` heads WRAP**, so a line-anchored sweep both harvests a spurious `Parameter` and misses every record name; the `.glob` adds nothing and misses 17 `Program` obligations. All 179 are in the `make print-assumptions` gate, so the closure is permanent rather than a one-time measurement. Zero tree-wide collisions. **Universes measured off BOTH binder and block, and here the BINDER is the informative one**: all 179 are over `Category@{u u0 u0}` — hom identified with proof by REUSING the level variable — with NO block equation saying so, while 155 carry the block equations `u0 = u4` and friends and **24 carry none at all** (the whole §E `curry_*` family, which lives over one ambient category); **across all 179, NO equation touches any OBJECT universe**, and there is no word-bounded `Set` anywhere. The identification has two INDEPENDENT donors, `Product` and `Adjunction`, each rejected alone under `Constraint ch < cp` while `@Functor Cu Cu` is ACCEPTED there — **and `Partial_l` is NOT one of them, a trap this file's own probe caught**: its type forces the product to be formed first, so a probe aimed at it fails at its ARGUMENT and measures nothing (the `MonoidObject` trap of #340). `Test/ProbeParameter396.v` carries **21 `Fail` commands = 1 instrument check + 20 negatives of THREE kinds** — 12 CONVERSION, 2 TYPING, 6 FORMABILITY — each stripped ONE AT A TIME, compiled alone and classified by reading its whole error, with 120 passing `Check` controls; guard coverage is complete and the rename simulation is **12/12 with zero vacuous guards**, every break landing on a control line and none inside a `Fail`. `make todo` grows by **34, ALL of them in the probe** (21 `Fail` commands plus prose); the library file contributes ZERO, so the DoD box is not met and is disclosed rather than glossed — dropping the probe would leave twenty measured refutations guarding nothing. NOT delivered: no `Cowedge` instance for the counit (`pa_counit_extranatural` is elementary only, and packaging it needs the opposite of both the integrand and `A`); no end or coend, so neither wedge is shown universal; no naturality in `c` of §H's isomorphism and no two-variable-adjunction object; no converse to §H; no `AdjointOnTheLeft` reading; no concrete instance of §F, §G or §H at a named category, the currying instance being the file's only witness and not run through them; no whole-record round trip for §G, so `parametrized_adjunction_iff_transform` is a biconditional and not an isomorphism of types; the module tensor-hom is a NOTE, measured reachable and deliberately not shipped; and nothing is registered as an `Instance`
- **Adjunction/Choice.v**: Mac Lane §IV.7 Exercise 3 (book p. 102, `maclane:IV.7:ex3`) — **the functors with a right adjoint form a full subcategory S of `[D, C]`, and choosing one right adjoint per object makes `R : S^op ⟶ [C, D]` a functor with `Rσ` the conjugate of `σ`**. "Has a right adjoint" is DATA — `HasRightAdjoint F := ∃ U, F ⊣ U`, a `sigT` — so Mac Lane's "choosing one RF for each F" IS the membership witness and no choice principle appears; `LeftAdjSub C D : Subcategory ([D, C])` cuts it with the terminal `shom` (the `CRng_Sub`/`TorsionFree_Sub` idiom; the two prior subcategories OF A FUNCTOR CATEGORY, `ReprSubcat` and `Instance/Ab/ModFunctor.v`'s `AbFunAdd_sub`, are the closer analogues, the latter written in the identical five-line `@Build_Subcategory` form), S is `LeftAdjCat C D := Sub ([D, C]) (LeftAdjSub C D)`, FULL by construction, whose objects ARE `Instance/Adj.v`'s `AdjObj C D` at `eq_refl` (`leftadj_obj`). **`RightAdjointFunctor C D : (LeftAdjCat C D)^op ⟶ [C, D]`** takes the opposite LITERALLY as its source (the `AdjForgetRight` idiom) with `fmap := conj_mate`, its three laws `Adjunction/Conjugate.v`'s `conj_mate_respects`/`conj_mate_id`/`conj_mate_compose` under a file-wide `Obligation Tactic := intros` (the default tactic closes NONE, measured); `raf_id_is_conj_mate_id` and `raf_fmap_variance` read the conjugate and the variance back at `eq_refl`. **S IS THE CATEGORY OF ADJUNCTIONS, AT ISOMORPHISM STRENGTH**: `SubToAdj : LeftAdjCat C D ⟶ Adj C D` is Full (spending `conj_mate_uniq`), Faithful and essentially surjective — `SubToAdj_Equivalence` through `FF_ESO_Equivalence` — and beyond that **`SubAdj_strict_iso : @Isomorphism StrictCat (LeftAdjCat C D) (Adj C D)`**, an isomorphism of categories with `AdjToSub` as inverse, objects round-tripping at `eq_refl` and arrows by `conj_mate_uniq`/`conj_pair_law` (my own brief predicted this would FAIL; a scout compiled it). R is therefore #395's `AdjForgetRight` transported — `RAF_via := AdjForgetRight C D ◯ Opposite_Functor (SubToAdj C D)` agrees with it on BOTH actions at `eq_refl` (`raf_obj_via`/`raf_map_via`) while the whole records are refuted (three rebuilt law fields) and pinned, likewise `LI_via` against `Incl`. **THE CHOICE IS INERT TWICE**: two right adjoints of one F are isomorphic in S (`choice_iso_in_Sub` via `Full_membership_iso`, its legs `eq_refl`, its inverse law `≈`-only — the composite of the two mates of the identity is `nat_id` only after `conj_mate_compose`, an identity cancellation and `conj_mate_id`, refuted strict and pinned); and a re-choice `RightAdjChoice C D := ∀ F, HasRightAdjoint F → HasRightAdjoint F` gives `RightAdjointFunctor_ch` with **`choice_independence : RightAdjointFunctor_ch C D ch ≈ RightAdjointFunctor_ch C D ch'`** for ANY two choices, the canonical `ch_canonical := fun _ h => h` returning the OBJECT's three projections at `eq_refl` (the functor at that choice is NOT compared with R at any strength). Non-vacuity over the walking arrow: `TwoConst TwoX ⊣ TwoConst TwoY` through `Build_Adjunction'` (the file's ONE `Defined`, measured NOT load-bearing by flipping it alone) gives an object of `LeftAdjCat _2 _2` with R-value `TwoConst TwoY` at `eq_refl`, the two constant functors proved distinct, the object proved not an identity, and `TwoConst TwoY` proved to have NO right adjoint — S is a PROPER subcategory. **Universes measured off BOTH binder and block over all 79: NOT ONE block carries an equation** (184 `<`, 1529 `<=`); the identification sits entirely in the BINDER — `C : Category@{o1 h h}`, `D : Category@{o2 h h}`, all 127 `Category@{o h p}` instances h = p, every one of the 63 two-category binders filling all four hom-and-proof slots with ONE level, both object universes free. hom = proof has FOUR independent donors (`Opposite`, `Subcategory`, `Fun`, `Adjunction`, each rejected alone at levels declared apart with `x ~> y`, `id[x]` and functors both ways accepted, so `Functor` is NOT a donor); the hom-level collapse has TWO (`Fun` alone; functors in BOTH directions). Word-bounded `Set` in 13 blocks and 7 types, ALL in the `_2` section and ALL strict lower bounds — 9 genuine `Set < u` on the constant's own levels (`Instance/Two.v`'s `TwoHom : … → Set`), 4 only the stdlib `Set < Basics.flip.u*`. **79/79 closed under the global context** (56 heads + 23 `Program` obligations, all in the gate FULLY QUALIFIED), zero collisions, closure 29 excluding self (marginals: `Construction/Subcategory`, `Functor/Opposite`, `Instance/Adj/Forgetful`, `Instance/StrictCat`, `Instance/Two`, `Theory/Equivalence/FullFaithful` 1 each; `Instance/Sets` 0 yet REQUIRED — without it the file stops at `two_const_hom_iso` with "The reference Sets was not found"); 15 `eq_refl` readbacks. `Test/ProbeChoice397.v` mirrors the target's eighteen `Require`s and carries **17 `Fail`s = 1 instrument + 16 negatives of THREE kinds told apart by the error TEXT** — 7 CONVERSION, 1 TYPING (the covariant ascription `LeftAdjCat C D ⟶ [C, D] := RightAdjointFunctor C D`, a plain has-type mismatch with no `cannot unify` and no universe clause, beside the accepted contravariant one), 8 FORMABILITY — each stripped one at a time and compiled alone, rename-simulated 10/10 with every break on a `Check` line. **A `Fail` FALSE-PASS SHAPE MEASURED ON THIS VERY STATEMENT**: `Fail Program Definition` on the covariant ascription reports "The command has not failed!" — `Program` ACCEPTS it and defers the variance into the obligation `(LeftAdjCat C D)^op = LeftAdjCat C D` — so the negative is a plain `Fail Definition` and the variance is pinned by `eq_refl` readbacks, never through `Program`. `make todo` grows by 36, all in the probe (17 `Fail` commands + 19 prose lines), the library file contributing ZERO. NOT delivered: `Theory/Bicategory/Mates.v`'s ledger 10 is NOT discharged (the identification is at identity bounding cells) and that file was untouched by this issue (it is narrowed, not discharged, by `Instance/Adj/Bicategory.v`); no dual on the subcategory of functors with a LEFT adjoint; no repleteness or wideness of `LeftAdjSub`; no identification of S's image in `[D, C]`; nothing registered as an exported `Instance` except `SubToAdj_Faithful` and `SubToAdj_Full` (`SubToAdj_ESO` is `#[local]`, a chosen preimage must not resolve)
- **Adjunction/Square.v**, **Instance/Cat/Bicategory/Square.v**: Mac Lane §IV.7 Exercises 4 (Kelly) and 5 (Palmquist) (book p. 103, `maclane:IV.7:ex4`, `maclane:IV.7:ex5`) + Riehl §4.3 Exercise 4.3.v (`riehl:4.3:exv`) — **adjoint squares as a hom-set condition over EVERY transposable arrow, the mate as its characterisation, Palmquist's bijection, and the two pasting laws of mates**. **THE CONDITION IS OVER BARE COMPONENT FAMILIES, AND THAT IS WHAT MAKES THE FILE AN UPGRADE OF #393 AND #394 RATHER THAN A PARALLEL CONSTRUCTION**: `AdjointSquare A A' K L sigma tau := ∀ x a (k : F x ~> a), ⌊ fmap[K] k ∘ sigma x ⌋² ≈ tau a ∘ fmap[L] ⌊ k ⌋` (Mac Lane's X, A, G, H are D, C, U, L) is, at `K = L = Id`, LITERALLY `Adjunction/Conjugate.v`'s `Conjugate` at Leibniz equality of TYPES by `eq_refl`, and at invertible comparison families literally `Adjunction/Map.v`'s `MapAdjHom` — likewise its unit and counit forms against `ConjugateUnit`/`ConjugateCounit` and `MapAdjUnit`/`MapAdjCounit`, and the mate operators `sq_mate`/`sq_mate_inv` (defined BY THE TRANSPOSE, as `conj_mate`/`conj_mate_inv` are) against `conj_mate`/`conj_mate_inv`, the last two at Leibniz equality of COMPONENTS rather than of types — ten `eq_refl`s in all; Map.v's own passages are NOT reusable (their types demand an `Isomorphism` family — a TYPING refusal), so the four legs are rebuilt with Map.v's proofs verbatim. The ledger records what each passage consumes in its TYPE: hom → unit/counit/mate and `sq_hom_iff_from` (an addition Map.v lacks) need NO naturality; `adjoint_square_iff_mate : SigmaNat sigma → (AdjointSquare ↔ ∀ a, tau a ≈ sq_mate sigma a)` needs σ's; its `_inv` twin needs τ's. Transform wrappers `AdjointSquareT`/`SqMate`/`SqMateInv` (components `eq_refl`) and `square_bijection : @Isomorphism Sets sq_dom sq_cod` use Conjugate.v's inline `{| carrier := … |}` idiom (`NatSetoid` would cost +13 modules). **Palmquist**: `palm_to`/`palm_from` with both round trips, `palmquist_bijection : @Isomorphism Sets palm_dom palm_cod` and `palmquist_nat_bijection : (F' ◯ L ◯ U ⟹ K) ≊ (L ⟹ U' ◯ K ◯ F)`, the generic `precomp_bijection`/`postcomp_bijection`, and two factorisations — through the mate (`palm_to_via_mate`, NO associator, because the mate ABSORBS one pre/post pair on the nose: `sq_mate_is_post_of_pre`/`sq_mate_inv_is_pre_of_post` at `eq_refl`) and through the two factors (`palm_two_factor_agrees` componentwise; `palm_two_factor_spliced` via `fun_comp_assoc`, whose component `fun_comp_assoc_component` pins as `fmap[U'] id`, NOT `id`); `(F' ◯ L) ◯ U` and `F' ◯ (L ◯ U)` are different functor RECORDS with agreeing actions (`bracket_fobj_left`/`bracket_fmap_left`), so whole-Transform statements are bracketing-sensitive where componentwise ones are not. **Pasting (Riehl 4.3.v)**: `adjoint_square_paste_h`/`sq_mate_paste_h` and, over `Adjunction_Compose`, `adjoint_square_paste_v`/`sq_mate_paste_v` — the pasted pair satisfies the square (a short chase) and the mate law follows BY UNIQUENESS (`SqMate_uniq`); the vertical pasting never touches the composite's unit because its transposes are definitional (`AB_to_strict`, `eq_refl`; the unit is `≈`-only, `AB_unit_upto`). The bridge file gives `sq_mate_is_mate : SqMate A A' K L sg ≈ mate SqBA SqBA' sg` and `sq_mate_inv_is_mate_inv` (nine `fmap _ id` residues cleared, the `Cat_mate_unfold_raw` move; no padding at genuine bounding functors) and the issue's literal item 2, `adjoint_square_iff_Cat_mate`; it is a separate file because `Instance/Cat/Bicategory/Adjunction` shadows `Theory/Adjunction.v`'s `unit`/`counit`. **Universes off BOTH binder and block over all 162 constants**: hom = proof in EVERY `Category` binder of all 162 with no block equation (TWO independent donors, `Adjunction` and `Compose`, each refused alone at `ch < cp` where functors both ways and a Transform are accepted); THIRTY-SEVEN constants — all of section (A) plus the five Map.v readbacks, not the ten a first draft of the header named — carry only `u0 = u2`, `u6 = u8` and keep the two adjunctions' hom levels APART (bounds only, `u0 <= u6`), while every Transform-typed FOUR-category constant collapses all four (`u0 = u2 = u6 = u8`), donor `Compose` — the whole condition, both families, `sq_mate` and `adjoint_square_iff_mate` accepted at hom levels declared apart, `F' ◯ L`/`K ◯ F`/`L ◯ U`/`U' ◯ K` refused; horizontal pasting identifies the three columns' hom levels, vertical six categories', the bridge the four OBJECT universes too; no word-bounded `Set` anywhere. 156 + 6 constants closed under the global context (116 heads + 40 `Program` obligations; 6), all gated fully qualified; closure 25 (`Compose` 3, `Map` 3, the rest 0) and 34; zero `Defined.` in either file; zero collisions (`AB`/`AB'` occur elsewhere only as prose). `Test/ProbeSquare398.v` carries **25 refutation commands = 1 instrument + 24 negatives of THREE kinds** (12 CONVERSION, 3 TYPING, 9 FORMABILITY), each stripped ONE AT A TIME and compiled alone with the whole error read, every constant a negative names under a `Check` outside every `Fail`, rename-simulated 14/14; three ≈-only identifications (`palm_two_factor_spliced`, `sq_mate_pasting`, `sq_mate_inv_pasting`) have their strict forms measured to fail out of tree and are pinned nowhere; `make todo` grows by 40, all in the probe, the library files contributing zero. Two of its findings are folded into the header: `Compose` is a SECOND independent donor of hom = proof (`Uq ◯ Fq` refused with no adjunction in the command, so `Adjunction` is sufficient but not necessary), and `bracket_transform_type_right` is an `X = X` after parsing (`◯` associates left), recording the parser's convention rather than a bracketing fact. A second measurer re-ran every count, closure, citation and universe class of both headers; four over-broad sentences were corrected before landing (the book's second hom-set arrow carries a SUBSCRIPT star, postcomposition; the two-equation profile is thirty-seven constants' not ten's; the four-equation collapse is scoped to (C) and the four-category constants of (D); the cross-pair bounds are bounds-only rather than an exact enumeration). NOT delivered: no category or double category of adjunction squares (no identity square, laws, interchange or setoid), so the mate is not exhibited AS A FUNCTOR and Mates.v's ledger 10 was left untouched by this issue (narrowed later by `Instance/Adj/Bicategory.v`, which consumes this file's vertical pasting as its route-(b) cross-check); no `sq_mate_inv` pasting; no relation to `conjugate_compose`; no naturality in the bounding functors; no concrete witness; nothing registered as an `Instance`. The issue's "Current state" is stale on three counts (Map.v already carries the square at invertible families; Map.v and Conjugate.v already carry unit/counit forms; `conjugate_bijection` is the setoid-iso precedent), and its Riehl `rg` command returns 18 substring hits of `conj_mate_compose` as written, none under `-w`
- **Instance/Adj.v** (RETYPED), **Instance/Adj/Forgetful.v**: Mac Lane §IV.7 construction 1 (book p. 101) — **the category of adjunctions, whose arrows are now the CONJUGATE PAIRS Mac Lane actually specifies, and its two forgetful functors**. `AdjObj C D` is the triple, `Record ConjPair` carries `conj_left : adjobj_left x ⟹ adjobj_left y`, `conj_right : adjobj_right y ⟹ adjobj_right x` and the square `conj_pair_law` as a FIELD, and `Adj C D` takes it as its hom. **THE ONE DESIGN DECISION IS THE VARIANCE, AND IT IS EXPRESSED BY THE ORDER OF TWO ARGUMENTS**: `Adjunction/Conjugate.v` declares `Conjugate A A' (σ : F' ⟹ F) (τ : U ⟹ U')` — σ BACKWARD, τ forward — whereas Mac Lane's p. 101 display puts **σ forward on the left adjoints and τ backward on the right adjoints**, so the hom `x ⟶ y` is **`Conjugate (adjobj_adj y) (adjobj_adj x)`, the two adjunction arguments SWAPPED**; nothing in the type checker objects to the other order, it merely builds a different and wrong category, which is why `Test/ProbeAdjCat395.v` pins it as a TYPING negative (`The term "s" has type "F' ⟹ F" while it is expected to have type "F ⟹ F'"`, no `cannot unify`, no universe clause) against the accepted swapped form as its control. **A CONSEQUENCE THAT MAKES THE WIRING FREE**: because the hom runs `Conjugate (adj y) (adj x)`, the three adjunction arguments of `conjugate_compose` line up as codomain, middle, domain, so it fits `compose f g` with NO rearrangement. **THE ISSUE'S FIRST WORK ITEM WAS ALREADY IN TREE AND THE FILE SAYS SO**: it asks for the composition-of-conjugate-pairs lemma, calling it "the substantive lemma the current file avoids", but `Adjunction/Conjugate.v`'s `conjugate_id` and `conjugate_compose` — whose conclusion `Conjugate A1 A3 (σ ∙ σ') (τ' ∙ τ)` already composes the second component BACKWARD, which IS the composition equation of p. 101, `⟨σ'·σ, τ·τ'⟩` — the label (8) sits on the two-arrow chain and the equation is the unnumbered running text below it — landed with #394; both are CONSUMED, neither re-proved. **THE CATEGORY LAWS NO LONGER DISCHARGE TRIVIALLY, WHICH IS THE ISSUE'S REVIEWER CHECK, AND THE SCOPE OF THAT IS STATED HONESTLY**: `id` and `compose` do not typecheck without a conjugacy proof, so `Program` raises two obligations that did not exist before — `Adj_obligation_1 : Conjugate (adjobj_adj x) (adjobj_adj x) nat_id nat_id` and `Adj_obligation_2 : Conjugate (adjobj_adj z) (adjobj_adj x) (nat_compose (conj_left f) (conj_left g)) (nat_compose (conj_right g) (conj_right f))`, the second with the τ's composed backward — and they are discharged by `exact conjugate_id` / `exact conjugate_compose`; that `cat`, `program_simpl`, `simpl; intros; cat` and `auto` all leave both goals open was **measured out of tree against an instrument control, and is NOT pinned**. The OTHER five obligations (`compose_respects` and the four category laws) are still componentwise equations that close by ordinary automation, exactly as before, and the header says so rather than claiming all seven became hard. Four `eq_refl` readbacks pin the data (`adj_id_sigma`, `adj_id_tau`, `adj_compose_sigma`, and `adj_compose_tau`, whose statement `conj_right (f ∘ g) = conj_right g ∙ conj_right f` is display (8) at Leibniz equality), reducing through `ConjPair`'s primitive projections whatever the opacity of the third field; `conj_pair_right_unique`/`conj_pair_left_unique` record that either component determines the other. **THE `CAVEAT` IS DISCHARGED, NOT EDITED**, and the old coarse category is **NOT kept** — `Instance/Adj.v`'s block (thirteen lines longer than the coordinator's brief claimed) said the hom was the bare product setoid `([D,C] × [C,D])`, that "every category obligation discharges trivially", and that retyping was future work; all three are now false of the file. **Retyping was safe and that was measured BY USE, not by name**: before this change NO `.v` file anywhere Required `Category.Instance.Adj` (the new sibling is the first), while a bare `rg '\bAdj\b'` returns EIGHT files whose every hit outside this one is a local hypothesis of that name — `Adjunction/LeftInverse.v`, `Adjunction/Pare.v`, `Instance/Proset/Galois.v`, `Test/ProbeLeftInverse376.v`, four of them section variables, hence glob-recorded as `var` rather than as declarations, the rest `intros`-bound or prose — or prose; a full `make` after the change rebuilt 22 files with no error — **read that number in the right place**, since 20 of the 22 are reverse dependents of the COMMENT-ONLY edit to `Instance/Adjoints.v`, while the retyping itself reaches at most the two files added here, and **before this change** `Instance/Adj.v` had no downstream dependents at all (it now has exactly two, both added here). `Instance/Adj/Forgetful.v` supplies Mac Lane's two displayed functors: **`AdjForgetLeft C D : Adj C D ⟶ [D, C]`** onto the left adjoints and **`AdjForgetRight C D : (Adj C D)^op ⟶ [C, D]`** onto the right ones, **the contravariance expressed by taking `Construction/Opposite.v`'s opposite LITERALLY as the source** — an arrow of `(Adj C D)^op` from `x` to `y` IS a `ConjPair y x`, whose `conj_right` field already has the needed type, so the arrow action is a bare projection with no transport and nothing inserted; `AdjForgetRight` ascribed covariantly is refused, pinned as the probe's second TYPING negative. **BEYOND THE ISSUE'S DEFINITION OF DONE, AND FLAGGED AS SUCH**: both functors are proved **Full and Faithful** — faithfulness is `conj_pair_right_unique`, fullness supplies the mate as the chosen preimage — which is what makes the word "forgetful" precise here, since neither functor loses information; it is a clean excision if the files are ever wanted minimal. `Instance/Adjoints.v` is corrected **in comments only, its record untouched**: its header had said "[c]hoosing the left adjoint as the 'forward' direction is an arbitrary but harmless convention", whereas `adj_morphism` declares `free_functor : D ⟶ C` and `forgetful_functor : C ⟶ D` with `hom := @adj_morphism`, so an arrow `C ~> D` carries its **RIGHT** adjoint forward — measured, `forgetful_functor f : X ⟶ Y` typechecks and `free_functor f : X ⟶ Y` is rejected — and the new text says which of Mac Lane's two orientations it realizes: p. 101 displays BOTH, `A^X ← A^(adj)X` reading off the left adjoints covariantly and `[A^(adj)X]^op → X^A` the right ones on the opposite, and `Adjoints` matches the SECOND while `Adj C D` takes the FIRST. **43/43 constants closed under the global context** (25 in `Instance/Adj.v` + 18 in `Instance/Adj/Forgetful.v`, by `Print Module` flattened — it wraps the module head, and a filename containing a dot makes `coqc` refuse the module identifier outright); **ZERO `Defined` tokens** in either file, so nothing is load-bearing, the four readbacks reducing through transparent record literals while the opaque obligations sit only in the third field. **Zero name collisions**, swept over every `def`/`prf`/`var`/`proj`/`constr`/`inst`/`rec`/`ind` record in the tree — **28,435** under that criterion, and the criterion has to be stated, since four defensible readings of the same `.glob` set span 23,733 to 99,863 — instrument-checked at `Full` = 3 and `Monoid` = 5; every one of the 43 names has exactly one record, its own, and `Adj` has **five** records in all, its own `def` plus the four `var`s above. Closure: `Instance/Adj.v` **18 → 19**, the single added module being `Adjunction/Conjugate.vo` itself, everything it needs having been reachable already through `Theory/Adjunction`; `Instance/Adj/Forgetful.v` is 21. Universes read off BOTH binder and block: `Adj` is over `Category@{u7 u9 u9}` and `Category@{u8 u9 u9}` — hom identified with proof and C's hom level with D's, by reusing the level variable in the BINDER — while **not one of the five headline constraint blocks contains a universe equation at all**, only bounds, with both object universes free of each other and **zero word-bounded `Set`**; the identification is inherited from `AdjObj` and **no isolating probe was run, so it is NOT attributed to a single donor**. `Test/ProbeAdjCat395.v` carries **3 `Fail`s = 1 instrument + 2 negatives, both TYPING**, each stripped and compiled alone with exactly one `Error` read whole, every constant they name also appearing outside a `Fail`. **A NAME DIVERGENCE, DISCLOSED**: the issue's Verification block pins `Print Assumptions Adj_Forget_Left.`, a name that occurs NOWHERE in the tree; the delivered names are `AdjForgetLeft` and `AdjForgetRight`. NOT delivered: **the cut is not shown STRICT** — no pair of transformations is exhibited that fails to be conjugate, so nothing here proves the new category is genuinely coarser than the old product one; no essential surjectivity for either functor and no identification of `AdjForgetLeft`'s image with the left adjoints that have a right adjoint, hence no equivalence with any subcategory of `[D, C]`; no concrete witness at a named pair of categories, so nothing is instantiated; and, IN THIS FILE, no 2-categorical content — the 2-categorical structure on `Adj` has since been built in `Instance/Adj/Bicategory.v`, whose hom-categories are these `Adj C D` on the nose, while nothing here relates `Adj C D` to `Theory/Bicategory/Mates.v`, to `Instance/Cat/Bicategory/Conjugate.v`, or to `Adjunction/Map.v`'s maps of adjunctions, whose identity-bounding-functor case IS this hom
- **Instance/Adj/Bicategory.v**: Mac Lane §IV.8 Theorem 2, Exercise 1 and the closing remark (book p. 104, `maclane:IV.8:thm2`, `maclane:IV.8:ex1`, `maclane:IV.8:remark1`) — **horizontal composition of conjugate pairs is conjugate for the composite adjunctions, it is a bifunctor `Adj(A,D) × Adj(X,A) → Adj(X,D)`, and categories, adjunctions and conjugate pairs assemble into a BICATEGORY `Adj_Bicategory` whose hom-categories are `Instance/Adj.v`'s `Adj C D` on the nose**. `conjugate_hcompose` (the issue's pinned name) is Theorem 2 in `Adjunction/Conjugate.v`'s vocabulary: given `Conjugate A A' s t` and `Conjugate B B' sb tb`, the Godement products `nat_hcompose sb s` and `nat_hcompose t tb` are conjugate for `Adjunction_Compose A B` and `Adjunction_Compose A' B'` — six rewrites, Mac Lane's hom-set diagram read left to right — and BOTH reviewer checks are met by construction rather than by comparison: the components ARE `Theory/Natural/Transformation.v`'s `nat_hcompose` and the composites ARE `Adjunction/Compose.v`'s `Adjunction_Compose`, pinned at `eq_refl` by `hcomp_left_is_nat_hcompose`, `hcomp_right_is_nat_hcompose` and `hcomp_obj_adj`. The proof needs `Instance/Sets` IMPORTED (Instance/Adj.v does not import it and `Require` is not `Export`) or every setoid rewrite dies on an unresolved `ProperProxy … Sets.morphism` — measured on the shipped file: with that `Require Import` deleted it stops at `conjugate_hcompose`'s first `rewrite`, though no `Sets` token occurs in the code below the requires and the module costs the closure nothing, so what the line buys is instance visibility — worth knowing for any consumer of `Adj`. **A SECOND, INDEPENDENT PROOF CONSUMES #398**: `conjugate_hcompose_via_square` is `Adjunction/Square.v`'s `adjoint_square_paste_v` at identity bounding functors after two paddings (`conj_padL`/`conj_padR`, the same construction as `Instance/Cat/Bicategory/Conjugate.v`'s `Cat_conj_padL`/`Cat_conj_padR`, their obligation scripts differing only by the leading `intros` that file's local `idtac` obligation tactic forces, restated because requiring that file costs +9 modules and drags in `Adjunction/Natural/Transformation`'s `unit`/`counit` shadowing): `routeb_hyp` transfers the hypothesis at a Leibniz equality of TYPES, `routeb_sigma` shows the σ leg of the vertical paste IS `nat_hcompose` at `eq_refl`, and `routeb_tau` shows the τ leg only at `≈` — `paste_v_tau` uses the OTHER factorisation of the Godement square, one `naturality` away — so Theorem 2 IS #398's vertical pasting at identity bounding functors up to two paddings per side and one naturality on the right leg; the two proof terms are not compared. **THE BIFUNCTOR `Adj_Hcompose : (Adj y z ∏ Adj x y) ⟶ Adj x z`** (fst the OUTER 1-cell, as `Cat_Hcompose`; `Adj_Hcompose_shape` pins the type of display (1)) over `adjobj_hcompose` and `conj_pair_hcompose`, whose one obligation is one `exact` of `conjugate_hcompose`; its `fmap_comp` — the middle-four interchange — is discharged ON EACH LEG by `nat_hcompose_interchange`, a plain `Definition` whose body is `Cat_Hcompose`'s own `fmap_comp` (`Instance/Cat/Bicategory.v`) with no tactic at all: Cat's interchange CONSUMED, not restated, at a closure cost of exactly one module. **EXERCISE 1 IS DERIVED, NOT REPROVED**: `conj_interchange` is one `exact` of `@fmap_comp _ _ Adj_Hcompose …`, and `conj_interchange_left`/`_right`/`_sigma`/`_tau` are `fst`/`snd` of it at explicit arguments; `apply fmap_comp` does not close the goal (the hom-setoid of `Adj` is a conjunction and the goal displays unfolded) and a bare pair `(m, n)` does not elaborate at a product-category hom without its three object arguments — both measured. **THE UNITOR DICTIONARY IS CROSSED ONCE MORE THAN CAT'S**: `Instance/Fun.v` names `nat_λ F : F ◯ Id ≅ F` and `nat_ρ F : Id ◯ F ≅ F` against convention and `Cat_Bicategory` sets `hunit_left := nat_ρ`; here `Adj_hunit_left` uses `nat_λ` on the left adjoints and `nat_ρ` on the right adjoints, because `adjobj_hcompose (AdjIdObj y) f` has left adjoint `adjobj_left f ◯ Id[y]` and right adjoint `Id[y] ◯ adjobj_right f` (four `eq_refl` readbacks), and `Adj_hassoc` cannot be one `iso_sym (nat_α f g h)`: its two legs take `nat_α`'s three arguments in OPPOSITE orders and both are the `to` direction. The five coherence laws are stated in `Adj`'s setoid (each a pair of `≈`) through `adj_hcomp2`; four close by `split; simpl; intros; cat`, and `Adj_hassoc_natural` needs the same extra `rewrite !fmap_id, fmap_comp` as `Cat_hassoc_natural`. Everything from `Adj_Hcompose_shape` on runs under `#[local] Obligation Tactic := program_simpl`, NOT the tree default `cat_simpl` (`Lib/Tactics.v`), and that is load-bearing — with `cat_simpl` there the file stops inside `Adj_hunit_left`'s obligations with `No obligations remaining` (measured). **`Adj_Bicategory` is built with the RAW `Build_Bicategory`**, feeding `Adj`'s own ten projections so record eta survives (`Build_Bicategory'` churns exactly as `Instance/Cat/Bicategory.v` predicts, re-measured), with `Adj_bicat_is_Adj : bicat Adj_Bicategory C D = Adj C D`, `Adj_hcompose_readback`, `Adj_bi1id_readback`, `Adj_hcomp2_readback` and two BRIDGES to Cat at `eq_refl` — the two legs of a horizontal composite of 2-cells here ARE `Cat_Bicategory`'s `hcomp2` on the underlying adjoints (`Adj_hcomp2_left_is_Cat_hcomp2`, its two arguments swapped, the contravariance of `adjobj_left` made visible, and `Adj_hcomp2_right_is_Cat_hcomp2`); a `Definition`, not an `Instance`, on `Cat_Bicategory`'s precedent. **STRICT OR WEAK: measured, WEAK, for a TYPING reason** — `Adjunction_Compose (Adjunction_Compose A B) Cc` and `Adjunction_Compose A (Adjunction_Compose B Cc)` do not share a type (`H ◯ (G ◯ F) ⊣ U ◯ V ◯ W` against `H ◯ G ◯ F ⊣ U ◯ (V ◯ W)`), so associativity is NOT STATABLE (a plain has-type mismatch with both sides' implicits written out) and both unit laws are refused the same way; one level down the functor equations are refused at CONVERSION with `fobj`/`fmap` agreeing (the three law fields of `Compose`, as `Adjunction/Pare.v` records), and at the `AdjObj` level the unit and associativity of `adjobj_hcompose` are refused at conversion. `Theory/TwoCategory.v`'s cast-carrying `TwoCategory` with `tcat := Adjoints` was MEASURED and NOT taken: `Adjoints` composes by `adj_comp`, and `adjunction (mA ∘ mB) = Adjunction_Compose (adjunction mA) (adjunction mB)` is a CONVERSION refusal at one and the same type (so that route would state the composite's conjugacy for `adj_comp` and fail the reviewer check), all three casts are genuinely needed (the 2-cell types at the two bracketings and at the unit padding are measured NON-CONVERTIBLE at `eq_refl`; no inequality is proved), and `Adjoints`' arrows are RECORDS where `AdjObj` is a `sigT` — the bridge `adjobj_of_morphism`/`morphism_of_adjobj` round-trips at `eq_refl` on the record (`adjoints_round_record`) and not on the `sigT`; the "strictly more work" estimate is an argument from those measurements plus the field counts (25 against 21), not a compiled comparison. **The orientation dictionary is disclosed rather than assumed**: the tree's `Adj C D` is the book's `Adj(D, C)`, a 1-cell runs in the direction of the RIGHT adjoint (`Instance/Adjoints.v`'s orientation), σ is forward on the left adjoints and τ backward on the right adjoints as on the page, and Mac Lane's product `Adj(A,D) × Adj(X,A)` lists its factors in the OPPOSITE order to `Adj_Hcompose`'s (his first factor is outer along the LEFT adjoints, the tree's along the RIGHT), a listing FORCED by `Bicategory`'s `hcompose` field type. **Universes measured off BOTH binder and block, and the two halves of the identification sit in DIFFERENT places**: hom = proof in every binder with no block equation stating it; the categories' hom levels are identified in the BINDER for the `Adj`-level constants (`adjobj_hcompose` at `Category@{u3 u9 u9}`/`@{u8 u9 u9}`/`@{u2 u9 u9}`, block `u9 < u0` and bounds) but in the BLOCK for the five section constants — `conjugate_hcompose` in `Section HorizontalCompose`, and `conjugate_hcompose_via_square`, `routeb_hyp`, `routeb_sigma`, `routeb_tau` in `Section RouteB`, the two sections' `Context` lines character-identical — (`u0 = u2`, `u0 = u4`, forced by those sections' functors in both directions and handed to every constant inside by section discharge, which is why `routeb_hyp`, binding only two categories, carries `u0 = u4` — measured out of tree, a functors-only section giving a trivial `x = x` example both equations — and NOT by `Adjunction_Compose`, which accounts only for the extra `u5 = u8` the two Theorem-2 constants carry; the other 62 blocks carry no equation), a count the Guard corrected from two and an attribution the fess audit corrected — a prediction of my brief that the identification would sit entirely in the binder was HALF WRONG and the header says so; object universes bounded, never identified (nine constants, those mentioning `Adj_Bicategory` or `Adjoints`, carry a strict `<` below the 0-cell universe, the rest `<=` only); `Adj_Bicategory@{…} : Bicategory@{u u0 u1 u0 u1}` is EXACTLY `Cat_Bicategory`'s instantiation shape with `Category` one level up; zero word-bounded `Set`. The binder identification has several donors each sufficient alone — `Adjunction` and `Compose` pinned in the probe, `Functor` and `Transform` shown NOT to be donors — so `conj_padL`, whose type mentions no `Instance/Adj.v` constant, gets it from `Compose`. `Test/ProbeAdjBicat399.v` carries **27 `Fail`s = 1 instrument + 26 negatives of THREE kinds told apart by the error text** — 11 CONVERSION, 4 TYPING (the associativity and both unit laws of `Adjunction_Compose`, not statable at all, and the σ leg as a whole transformation) and 11 FORMABILITY (`Adjunction` and `Compose` each a donor alone; `AdjObj`, `Adj`, `ConjPair`, `nat_hcompose`, `Cat_Hcompose`, `adjobj_hcompose`, `Adj_Hcompose` inheriting; functors in both directions identifying the two hom levels) — each stripped ONE AT A TIME and compiled alone with its whole error read, guard coverage measured mechanically (every constant a negative names is `Check`ed outside every `Fail`), rename-simulated 15/15 with every break on a `Check` line; two corrections to expectation are recorded there: the `Build_Bicategory'` churn is CONVERSION (its message carries `cannot unify`), and the `ConjPair` negative refuses at its `AdjObj` argument (the #340 trap), so whether `ConjPair` identifies anything of its own is UNKNOWN. `make todo` grows by 41, ALL in the probe (27 `Fail` commands + 14 prose lines), the library file contributing ZERO. **67/67 constants closed under the global context** (47 heads + 20 `Program` obligations, all in the gate FULLY QUALIFIED; `Print Module` wraps the module head, so a whitespace-flattened sweep harvests the spurious name `Bicategory` and reports 68), zero `Defined` (33 `Qed` in code, so nothing is load-bearing), zero collisions over all 67 names, closure 29 excluding self (`Adjunction/Square` 4, `Instance/Adj` 1, `Instance/Cat/Bicategory` 1, every other `Require` 0; 24 without the two extras paid deliberately). `Theory/Bicategory/Mates.v`'s descope note for ledger entry 10 is NARROWED, not discharged — its Cat-level content in ordinary vocabulary is now #398's two pasting laws plus this bifunctor, while the double category of adjunctions and the statement in an ARBITRARY bicategory stay open — and the sentences in `Adjunction/Conjugate.v`, `Adjunction/Choice.v`, `Adjunction/Square.v`, `Instance/Cat/Bicategory/Square.v`, `Instance/Adj.v` and `Instance/Adj/Forgetful.v` that said Mates.v was untouched, or that `Adj C D` carried no 2-categorical structure, are corrected in the same commit, with the 17 citation sites (21 line numbers; the builder's fifteen counted sites, two of which carry two numbers) those edits shifted repaired in `.v` files (two of them — a bare continuation in `Instance/Cat/Bicategory/Square.v` and `Instance/Cat/Bicategory/Conjugate.v` — found by the Guard after the builder's exact-path sweep missed them), five line numbers in CLAUDE.md and one in the qa plan doc. The issue's "Instance/Adj.v … no conjugacy condition on its arrows" has been stale since #395. NOT delivered: no `TwoCategory` instance; no double category of adjunctions and no pasting functoriality of mates in an arbitrary bicategory; no comparison of the two Theorem-2 proof terms; no naturality in C, D or E and no functoriality of `Adj_Hcompose` in its category arguments; no concrete witness at a named pair of categories; no pseudofunctor out of `Adj_Bicategory`; nothing registered as an `Instance`
- **Theory/Monad.v**: Monads as endofunctors with structure
- **Theory/EckmannHilton.v**: The abstract Eckmann–Hilton argument (Mac Lane §II.5 Ex 5; Eckmann–Hilton 1962) over a BARE setoid — imports `Category.Lib` only, the principle being pre-categorical: two `Proper` binary operations with SEPARATE two-sided units satisfying the interchange law have coinciding units (`eh_units` — the strictly-more-general piece, since the in-tree instance never needed it), coinciding operations (`eh_ops`), and the single operation commutative and associative (`eh_comm`/`eh_assoc`, with `eh_g_comm`/`eh_g_assoc` on the other side), bundled as the pinned `eckmann_hilton` (a right-associated `∧`-prod — `≈` is Type-valued, so no `and`); all hypotheses discharge as explicit positional arguments (`Local Set Default Proof Using "All"`, the Theory/Category/Monoid.v precedent, since they appear only in proofs). Structure/Semiadditive.v's convolution trio `conv_conv_pr`/`conv_comm`/`conv_assoc` is REFACTORED to route through it with statements byte-identical — `conv_interchange` and the four `conv_*_zero_*` lemmas survive unchanged as the instantiation's inputs, both units being `zero_mor`. Other in-tree instances of the principle, cited in the header: Instance/Cat/TwoCategory.v's `NatBase_centre` (eq_refl form — 2-cells on an identity 1-cell in the strict 2-category Cat, Mac Lane §II.5 Ex 8's centre) and the essay mentions in Structure/Monoid.v and Structure/Group.v
- **Theory/Centre.v**: The centre of a category (Mac Lane §II.5 Ex 8; Riehl §1.7 Ex 1.7.iv, her exercise being this verbatim) — `centre_monoid := hom_monoid ([C, C]) Id[C]`, the natural endotransformations of the identity functor as a `MonObject` by PURE Deloop.v reuse (zero new monoid obligations; four eq_refl Examples pin carrier/unit/op), with **`centre_commutative`** proved from a SINGLE naturality square: `fmap[Id]` is definitionally the identity, so α's naturality at the morphism `transform[β] x` IS the commutation `α_x ∘ β_x ≈ β_x ∘ α_x` (`centre_commutes_at`, two tactics; only α's naturality is consumed). The Eckmann–Hilton route (`centre_commutative_EH` via Theory/EckmannHilton.v's `eh_comm` — this file is that theorem's third consumer) is delivered as a disclosed CONSISTENCY EXHIBIT, not an independent derivation, with the honesty MACHINE-CHECKED rather than asserted: `centre_hcompose_is_vcomp` shows the Godement composite has the same components as vertical composition at Id by `eq_refl` (so the "two operations" are one operation and its argument swap — stated componentwise deliberately, `nat_hcompose` landing at `Id ◯ Id` being a transport question the header declines to paper over with a unitor), and `centre_interchange_forces_commutative` derives commutativity from the interchange SHAPE alone by instantiating at identities (so interchange is not a weaker input). Computations: `centre_Sets_trivial`(`_nat`/`_subsingleton`) — the centre of Sets is trivial, by probing naturality at the constant map from the singleton setoid, constructive, universe-clean after fixing a found Set-pinning (`@{o so}` binders on the probe); `centre_Coq_trivial` likewise (`=` there, elements of Coq not morphisms). Header carries the three-way DISTINCTION with kinds — this centre (commutative MONOID) vs the premonoidal centre (`Centre`, Structure/Binoidal/Central.v — a CATEGORY of central morphisms, which is why that name was left untaken here) vs the Drinfeld centre (Structure/Monoidal/Drinfeld.v — a BRAIDED MONOIDAL CATEGORY) — and the Grp/Ab identifications as explicitly-labelled remarks (fg-Ab = (ℤ,·) needs a torsion argument the tree lacks). 35/35 constants closed under the global context
- **Monad/Strong.v**: Strong monads — a monad whose functor carries a tensorial strength compatible with η and μ (Kock/Moggi)
- **Comonad/Core.v**: Covariant comonad API over the one-line `Comonad := @Monad (C^op) (M^op)` — extract/duplicate/extend as definitional op-reads; co-Kleisli category (Comonad/CoKleisli.v), co-Eilenberg-Moore coalgebras (Comonad/Coalgebra.v), costrength (Comonad/Strong.v), and the adjunction-induced comonad with co-resolutions by duality (Comonad/Duality.v); Env/Store/Traced instances in Instance/Coq/Comonad/. Monad resolutions: the Kleisli and Eilenberg-Moore free/forgetful adjunctions in Monad/Kleisli/Adjunction.v and Monad/Eilenberg/Moore/Adjunction.v
- **Monad/Monadicity/Beck.v**: Monadicity — the Eilenberg-Moore comparison functor with both triangle theorems and the `Monadic` predicate (Monad/Comparison.v, over a transparent `Adjunction_Induced_Monad`); split coequalizers with absoluteness and reflexive pairs (Structure/Coequalizer/{Split,Reflexive}.v); the Beck engine room — canonical split forks, `EM_Forget` reflects isos and creates U-split coequalizers (Monad/Monadicity/BeckObjects.v); crude monadicity fully proven (Monad/Monadicity/Crude.v), whose first topos-theoretic consumer is Structure/Topos/Monadic.v's `power_object_monadic` — Paré's theorem, which CONSUMES `crude_monadicity` rather than reproving any monadicity theorem, and which does not need Beck's precise form because the crude hypotheses are directly dischargeable — the image pair being SPLIT (not merely U-split) makes `pow_PreservesReflexiveCoequalizers` immediate and, by an ARGUMENT that is not compiled, would also let Beck's precise theorem apply, a route not taken because creation would cost the comparison isomorphism and the reflection clause on top of the same data; Beck's precise theorem with conservativity derived from creation and the `monadic_creates` converse at `EM_Forget` (Monad/Monadicity/Beck.v); the Dubuc adjoint-triangle lifting theorem (Monad/Lifting.v); identity-monad witness (Monad/Monadicity/Examples.v)
- **Theory/Algebra/Rig.v**: Rigs (semirings) — Seven Sketches §5.3.1 Definition 5.36 clause for clause (multiplication deliberately NOT commutative) over a setoid carrier, following Instance/CMon.v's naming: `RigObject`/`RigHom`/the category `Rig`, with forgetful functors to `CMon` (additive half) and to `Mon Sets` (multiplicative half as an internal monoid in (Sets, ∏)). The one-object reading is a theorem both ways: `DeloopRig` equips the delooping of the multiplicative monoid with a `Preadditive` instance (the four rig clauses ARE the Preadditive fields on one object), `EndRig` reads a rig off the endomorphism hom of any object of any LOCALLY PROPOSITIONAL preadditive category — an earlier revision of this bullet said "any preadditive category", and measured on 2026-09-17 after the PR "algebraic carriers are sets" a `RigObject` carries `rig_prop`, so `EndRig` takes `{LP : LocallyPropositional C}` and the packaged theorem below now reads `… * (∀ C, LocallyPropositional C → Preadditive C → obj → RigObject) * …`; those two constants are the only statements in the file that changed, the four `eq_refl` round-trip examples and `EndRig_DeloopRig` being unchanged and the class discharged at a delooping by `DeloopRig_LocallyPropositional` (`Theory/Algebra/Rig.v`) — and the rig-side round trip is definitional on all four data fields, packaged as the identity-carrier iso `EndRig_DeloopRig` (full record equality is blocked only by proof-carrying law fields; the category-side composite is deferred exactly as Construction/Deloop.v defers its §I.3 dictionary) — all packaged as `rig_iff_one_object_preadditive`. Instances: `Nat_Rig` (Example 5.37), `Bool_Rig` (Example 5.38, (false, ∨, true, ∧)); `RingObject` = rig + additive inverses (Example 5.42, mirroring Additive-over-Preadditive) with negation-preservation a THEOREM (`RigHom_neg`, by uniqueness of inverses — ring homs are just rig homs) and the axiom-free witness `Int_Ring` over stdlib Z (chosen over the suggested ℝ to keep the instance layer axiom-clean). ℕ is initial (`Rig_Initial`, Seven Sketches Ex 6.7: the unique hom is the n-fold sum of 1). Theory/Algebra/Rig/Connections.v records, by `eq_refl`, that Monad/Graded.v's `AndGrade` is the multiplicative half of `Bool_Rig` and that `Nat_Rig`'s operations are the object actions of skeletal FinSet's coproduct/product — the decategorified shadow of Example 5.37
- **Theory/Metacategory/General.v**: Arrows-only metacategories over an arbitrary arrow type — Mac Lane §§I.1/I.8's presentation with axiom (iii) stated as the CONJUNCTION Mac Lane wrote (both sibling files encode it as an implication and say so in `jww` notes; their form is vacuously true). Identity uniqueness (`mident_unique_src`/`_tgt`), and the separation result `weak_identity_not_unique`: a four-arrow countermodel refuting identity uniqueness under axiom (i)'s weak one-sided form — the axiom-(iii) conjunction is carried identically by both records, so the separation is about axiom (i), and the guarded conjunctive presentation as a whole is a correction, not a restyling. Both passages: `Category_from_Metacategory` (unconditional) and `ToArrows : ∀ C, ObjUIP-style hypothesis → Metacategory` (bundling homs into one arrow sort transports along object equalities, the campaign's recurring honest hypothesis), with the comparison `ToArrows_Functor` proved Faithful + Full + EssentiallySurjective. `id_unique`/`id_unique_left`/`id_unique_right` land at `Category` level (Theory/Category.v, Riehl §1.1). The sibling files' headers are repaired to state what they actually prove — including the machine-checked fact that their unguarded `identity` field leaves every example metacategory objectless (`Three` is the empty category) and that the naive `->`→`/\` fix would make their records uninhabitable — in ArrowsOnly.v the arrow type must first be cut to the table's support, while in Metacategory.v even that is insufficient (its unguarded `identity` field admits no identities at all, support-restricted or not) and the guards must be restored first; the `jww` TODOs stay, with these diagnoses. `MetaRoundTrip`/`MetaRoundTrip_Equivalence` form the metacategory-side composite under the analogous UIP hypothesis; no metacategory-level equivalence notion is defined, so no comparison with `M` is stated at that level. ETAC, the elementary first-order language itself, is deep-embedded in Theory/Metacategory/ETAC.v (Mac Lane §§II.1-II.2 + Awodey §3.1): two-sorted de Bruijn syntax over dom/cod/identity/composite/equality atoms, Type-valued satisfaction `holds` over arrow BUNDLES `Arr C` (endpoint comparisons Leibniz on objects — the object sort's own relation, disclosed; hom-setoid `≈` exactly at the arrow equations, cast along sigma-packaged endpoint equalities with the `acast` kit), the syntactic dual with `dual_invol`, `closed_below`/`sentence` with environment-independence of sentences, the semantic duality lemma `holds_dual` (C ⊨ Σ* iff C^op ⊨ Σ under the `op_arr` environment transport — the op-transport of each arrow-equation satisfaction proved on literal-pair bundles reached by the propositional `arr_eta`, where every destruct is variable-to-variable), and `duality_principle : valid p → valid (dual p)` (instantiate at C^op; no double-op unwinding). The functor extension (remark 2) is scoped to the bundle-level `arr_fmap_op` commutation with the deferral disclosed (validity is not isomorphism-invariant — object atoms are strict, stated in the header). The §IV.3 dualization-table rows land beside it: the inverse-pair quartet (`op_Retraction_of_Section` etc., Theory/Morphisms/Duality.v) and `Full_op`/`Faithful_op` with converses for an ARBITRARY functor (Functor/Opposite.v — previously reachable only through the equivalence detour)
- **Theory/Category/Monoid.v**: Mac Lane's §I.2 remark that the set-based definition of a category IS a monoid for the composable-pairs product over its object set — the germ of internal categories, formalized at the set level (the general internal-category theory over pullbacks is explicitly out of scope). Two-sorted graphs `Graph` (objects strict, arrows a setoid) with the fibred product `Composable` and its UMP; `SpanMonoid G` packages unit + multiplication with boundary and monoid laws up to `≈`. Direction one is unconditional: `Category_from_SpanMonoid : SpanMonoid G → Category`. Direction two, `Category_SpanMonoid : ∀ C, HomRigid C → SpanMonoid (Category_Graph C)`, carries an explicit `HomRigid` hypothesis (transport of a morphism along object-equality proofs is proof-independent; implied by `ObjUIP`, axiom-free via Hedberg when object equality is decidable) — the obstruction is real (consumed by `arrow_mul_respects` and `arrow_mul_assoc`: two proofs of one composability equation differ by a loop at the *intermediate* object, which the outer setoid freedom cannot repair) and provably unavoidable: `arrow_mul_respects_forces_UIP` shows discharging it uniformly entails UIP for every type, via an `option A` countermodel whose `None` has provably contractible loops by encode-decode. In Mac Lane's set-based reading the question does not arise. Round trips: objects definitional both ways by `eq_refl`, and the Category-side comparison assembles into `Category_monoid_iso : C ≅[Cat] Category_from_SpanMonoid (Category_SpanMonoid C rigid)` — in this library the same strength as an equivalence of categories (Cat's hom-setoid is `Functor_Setoid`), and the strongest packaging available, with the genuinely extra content in the construction: both functors identity-on-objects, one hom leg definitional
- **Theory/OGraph.v**: Mac Lane §II.7 display (3) — categories ARE monoids in O-graphs, with the monoidal structure built in FULL rather than approximated. Over a fixed node type O: `OGraph O` (an edge family with per-pair setoids), identity-on-nodes morphisms (`OGraphHom` carries NO node action — that is what "identity-on-objects" means here), the category `OGrph O`, the trivial O-graph `OGraph_unit` (edges `x = y`), and the composable-pairs product `OGraph_prod` over a primitive-projection record `Composable` (cmid/cleft/cright). **`OGrph_Monoidal` is a real `Monoidal (OGrph O)`** — genuine tensor BIFUNCTOR, both unitors and the associator as isomorphisms, all six naturality fields, and BOTH `ograph_triangle` and `ograph_pentagon` proved, nothing descoped — so **`category_is_monoid_in_OGrph`** is stated against Theory/Algebra/Monoid.v's own `Monoid` class (`OMonoid A := @Monoid (OGrph O) (OGrph_Monoidal O) A`) rather than a bespoke lookalike, and Mac Lane's display (3) IS `mu_assoc`/`mu_unit_left`/`mu_unit_right`. **NO UIP** — not as an axiom, not as a hypothesis: composable pairs are identified by a middle-node equality plus transported `≈`, and every consumer ELIMINATES that equality rather than comparing two proofs of it. The escape is localized to a SETOID CHOICE, and localized as a THEOREM: the trivial O-graph compares its edge-proofs by LEIBNIZ equality, so the left unitor's respectfulness gets `p = p'` handed to it, and `coarse_respectfulness_entails_UIP` (audit-adopted, axiom-free) shows that over the coarsened setoid — all proofs identified, which looks harmless — respectfulness of that very map ENTAILS `NodeUIP O`, not merely resembles it. Correspondingly `I_O` may carry MORE than one loop at a node when O lacks UIP and is still a unit — the product's setoid quotients by exactly that freedom — and "exactly one arrow per node" is NOT claimed. Round trips measured strict-first: the O-graph and the multiplication's underlying map return by `eq_refl` (primitive-projection eta on `Composable` is what buys the second), the unit only pointwise (`eq` has no eta rule, stated as the reason); on the category side obj/hom/homset/id/compose are five separate `eq_refl` Examples, packaged as `ocat_roundtrip_iso : C ≅[Cat] CategoryOfOMonoid (MonoidOfCat C)` with both functors identity on objects AND morphisms — Leibniz equality of the two `Category` records is NOT claimed (law fields are rebuilt), the boundary Rig.v's `EndRig_DeloopRig` also reports. Orientation pinned by computation, not prose: `ocat_mu_is_compose`/`ocat_eta_is_id` are `eq_refl`, so the multiplication IS this library's `compose` (the other orientation would give the opposite category). Witness at `O := poly_unit` recovering Construction/Deloop.v's `Deloop M` fieldwise by `eq_refl`, with `ograph_nat_compose_2_3` computing 2·3 = 5 in (ℕ,+). Universes measured per constant with causes attributed: `OGraph_prod` carries exactly `o <= h`, which is the construction's honest content (a composable pair literally contains a node inside an edge); `OGrph`'s hom=proof identification is `Structure/Monoidal.v`'s own class shape, NOT a restriction on which categories can be read as graphs (`OGraphOfCat` takes an arbitrary `Category@{o h p}`); the definitions carry the Quiver.v/Category.v explicit-binder discipline (the development reports having needed it against minimization; the counterfactual is not machine-checkable and is not claimed as measured). The packaged `category_is_monoid_in_OGrph` is DEGENERATE in its universes — its constraint clause collapses each half to one level, because the round-trip Examples do not carry the binders the definitions do — so the general result lives in the components `MonoidOfCat`/`CategoryOfOMonoid` (only `o <= h`, `h <= p`), a known gap recorded in-file rather than papered over. The Quiver bridge `OGrph_Quiver` is faithful under an EXPLICIT `NodeUIP` hypothesis (the quiver hom-equivalence compares up to an arbitrary family of node self-equalities), with the unconditional question left open rather than asserted. Contrast recorded in-header with Theory/Category/Monoid.v's §I.2 germ, which DOES need `HomRigid`: there a graph is two-sorted so reading a category bundles all homs into one sort and transports along object equalities; here edges stay INDEXED by endpoints, nothing is bundled, and no rigidity is taken. Theory/Metacategory/ArrowsOnly.v's composable-pairs TABLE is unrelated content, said so explicitly. 109/109 constants closed under the global context, no `Program` anywhere in the file
- **Functor/Bifunctor/Partial.v**: Mac Lane §II.3 Propositions 1 and 2, completed (the necessity directions were Functor/Bifunctor.v's bimap calculus) — `Partial_l`/`Partial_r` bundling a bifunctor's partial functors; Awodey §7.6's raw-data smart constructor `Build_Bifunctor` (single object map + two separately functorial arrow actions + the interchange law — no object-agreement equations, hence no transports, and no universe pinning; contrast the `Cat_Closed` currying route) with `Build_Bifunctor_l/r` computing the partials back, `Build_Bifunctor_unique` the uniqueness half up to `Functor_Setoid` (transparent, `eq_iso` cast components observable), and `bifunctor_iff_partial` the full iff over the bundled `RawLaws`; Mac Lane's family form `Bifunctor_from_partial` (functors `L c`/`M b` with Leibniz object agreement, the commutation condition stated on L's objects under `cast2` — the price of "agreeing on objects", paid once); `NaturalIn1`/`NaturalIn2` (naturality in one variable, maclane:II.3:def3) with `transform_natural_in1/2` (necessity, at `(f, id)`/`(id, g)`) `Transform_from_partial` (sufficiency: componentwise naturality assembles over the product through the interchange factorization), both round-trip composites, `NaturalIn1_partials` (the predicate IS partial-functor naturality by `eq_refl`, with the per-c `Transform_partial_l`), and `transform_iff_natural_in_each` — Proposition 2 as the iff
- **Theory/Kan/Extension.v**: Universal property of Kan extensions — global (`RightKan`/`LeftKan` as adjoints of the restriction functor `Induced`) and local (`LocalRightKan`/`LocalLeftKan`). The pointwise (co)limit formula (Mac Lane §X.3 Theorem 1 + dual; Riehl Theorem 6.2.1 both handednesses) is Structure/Limit/Kan/Pointwise.v: given limits of `X ◯ comma_proj2` over each `=(b) ↓ F`, the assignment `R b := Lim` extends to a functor by comma reindexing, the counit is the limit-cone leg at `(a, id[F a])` (definitionally — recorded by `eq_refl` acceptance tests), and `⟨R, ε⟩` inhabits `LocalRightKan F X` (`Pointwise_LocalRightKan`); dually colimits over `F ↓ =(b)` yield `Pointwise_LocalLeftKan`, built covariantly through Preservation.v's cocone accessors so no tactic proof manipulates an opposite category (the cocone-building wrappers alone name the op, `Cocone` being a cone over the opposite diagram); the coend/end half of the bridge stays open, per docs/INHABITATION.md both constructions are conditionals with no in-tree premise witness. Engine room: legs of a limit cone are jointly monic (`ran_jointly_monic`, via the probe cone) and transport along `(id, id)` comma morphisms between ≈-equal components (`ran_leg_transport` — comma objects carry their morphism as data, so ≈-equal components are distinct objects connected by a genuine morphism); every functor law and naturality is "compose with the legs and compute" through these two, and the reindexing functors are the minimal constant-domain case (the general calculus is issue #1021, not blocking)
- **Functor/Hom/Yoneda/Iso.v**: Riehl §2.3's representably isomorphic objects — the composite the tree never took: `RepresentablyIsomorphic` (both variances, Riehl's orientations), the biconditionals `iso_iff_representably_iso`/`iso_iff_corepresentably_iso` by instantiating `FullyFaithful` at the Yoneda embeddings (with `CoYoneda_Faithful`/`CoYoneda_Full` supplied by name — typeclass search does not unfold `Curried_CoHom`), and Proposition 2.3.1's CREATES strength as a bijection of data: `representable_iso_setoid : (x ≅ y) ≊ ([Hom ─,x] ≅ [Hom ─,y])` over `iso_setoid`, forward the embedding's action, backward `eval_iso` — whose components are Riehl's explicit pair α_x(id)/(α⁻¹)_y(id) (`eval_iso_to`/`_from`) and which is itself `repr_pair_iso` at C^op against the identity representation, zero new proof content. `repr_pair_iso` (two representations of one functor have isomorphic representing objects, by evaluation at the identity) is the construction Structure/UniversalProperty.v's `univ_property_unique` had inlined since its beginning; that proof is now a two-line `exact` of it, statement unchanged. `representably_iso_variances_agree` closes the circle; Functor/Hom/Yoneda.v's header now cites the theorems it previously asserted in prose
- **Functor/Hom/Yoneda/Natural.v**: Mac Lane §III.2 Lemma 2 — the Yoneda bijection upgraded from a FAMILY of isomorphisms to an isomorphism of BIFUNCTORS, `yoneda_natural : YoNat C ≅ YoEval C` in `[[C, Sets] ∏ C, Sets]`, which is what `Functor/Hom/Yoneda.v` had asserted in PROSE ONLY since it was written (that header is corrected in the same commit — Riehl §E.1's checkbox, the only one of the issue's three header-defect items that its own QA correction leaves standing). **The two bifunctors cost very different amounts, and the file says so rather than presenting them as a pair.** `YoNat` is a PURE ASSEMBLY of in-tree parts — `Hom ([C, Sets]) ◯ (Opposite_Functor (Curried_Hom C) ∏⟶ Id) ◯ Swap`, a plain `Definition` with NO obligations, so all three functor laws are INHERITED and its object action is `eq_refl`-equal to the hand-written record. `YoEval` genuinely had to be built: the tree's only evaluation is the uncurried identity buried in `Cat_Closed`'s `exp_iso` (`Instance/Cat/Cartesian/Closed.v` describes it and **names no constant for it** — generic `eval`s do exist elsewhere, at `Structure/Cartesian/Closed.v` and `Structure/Monoidal/Closed.v`, but instantiating the former at `Cat` is a universe inconsistency), and it uses the OTHER factorization `fmap[G] g ∘ τ_A` — `yo_eval_map_alt` proves the two agree and the header states plainly that neither order is canonical. **The isomorphism is the existing one assembled, not a parallel construction**: `yoneda_natural_to_component` and `yoneda_natural_from_component` are `eq_refl` at whole-`SetoidMorphism`-record Leibniz equality against `Covariant_Yoneda_Lemma`, in BOTH legs. **THE DONOR UNIVERSE RESTRICTION BITES, AND THE PARTITION IS MEASURED RATHER THAN ASSUMED** — `Covariant_Yoneda_Lemma@{u u0}` identifies object, hom and proof universes, and the boundary runs EXACTLY where it is consumed — **a claim an audit initially refuted and which is now true because the code was fixed, not because it was reworded**: `YoEvalPre`, `YoNatPre`, `yo_nat_pre_obj` and `yo_eval_pre_obj` were pinned at `Category@{u0 u0 u0}` WITHOUT consuming the donor, by universe minimization at their unannotated top-level binders, and explicit binders free all four. So `YoEval`, `YoNat`, `YoEvalAt` and the four `*Pre` constants are FREE (`Category@{u u0 u0}`), while `yoneda_natural`, `yoneda_natural_pre` and `Yoneda_Lemma_derived` display `Category@{u0 u0 u0}` outright. **`YoEvalAt_Representable` is the trap: its binder displays the free `Category@{u u0 u0}` while its CONSTRAINT SET contains `u = u0`** — so reading the binder alone gets it wrong, and only the constraint block settles it. Guarded by four FORMABILITY negatives (`Fail Check` at a section declaring `Constraint uo < uh`) against four positive controls, one of them an identity isomorphism in the very functor category the theorem is stated in, so the rejection is attributable to the donor and not to the `Isomorphism`-in-`[X,Sets]` packaging; the restriction is NOT routed around (unlike #311 and #315, which took the Yoneda-free `ue_representation`) and is NOT claimed unavoidable. **The contravariant orientation is literal instantiation and needs NO `op_invol`, contrary to the issue's own premise**: `Curried_CoHom C` IS `Curried_Hom C^op` by definition and `@Presheaves C Sets` IS `@Copresheaves C^op Sets`, so `yoneda_natural_pre := yoneda_natural (C^op)` is conversion alone — and it is not needed for the REVERSE derivation either, an audit having corrected that counterfactual: `(C^op)^op = C` holds by `eq_refl` here (`Construction/Opposite.v`), so `op_invol` is on neither path. The same move closes the pointwise gap the issue records: `Yoneda_Lemma_derived C F A := Covariant_Yoneda_Lemma (C^op) F A` typechecks against `Presheaves [Hom ─,A] F ≅ F A` with no coercion, so the two long-separate `Program Instance`s are related at last — though `Yoneda_Lemma` itself is NOT replaced and is not shown redundant, and the four strict attempts to identify the two records (whole `Isomorphism`, both legs, and the `from`-value, which is a whole `Transform` whose `naturality` fields are different opaque constants) are REFUTED and pinned, agreement holding one level further in at the components. `YoEvalAt` with `YoEvalAt_Representable` is Riehl Remark 2.2.7's representability half (`ev_c` represented by `[Hom c,─]`); the universal-element packaging Riehl phrases with `id_c` is disclosed as not delivered. Eight negatives in all, and the file keeps the KINDS distinct — four formability (universe) and four conversion, the latter alone using `Fail Definition … := eq_refl`. Non-vacuity over `Deloop Nat_Plus`: the square at ⟨α₂, 3⟩ applied to α₅ computes to 10 on both sides through visibly different intermediates (0→3→8→10 against 0→5→8→10), with the two endpoints and both α-readings machine-checked by `eq_refl` and both components proved non-identity; the intermediates themselves are shown in the file's prose rather than pinned. A drafted header claim was corrected before landing: no downstream consumer needs NATURALITY — though the cases differ, and an audit separated them: `Structure/UniversalProperty.v` does consume `Yoneda_Lemma` and proves its results from the pointwise bijection alone, whereas `Yoneda_Full` and `Yoneda_Faithful` consume nothing from Yoneda.v and cannot, living in the UPSTREAM `Functor/Hom.v` and inlining evaluation at the identity directly — which together is precisely why the binaturality clause could go unformalized for so long
- **Functor/Representable.v** (extended), **Functor/Representable/Functorial.v**: Mac Lane §III.2 Exercise 1 — representations are FUNCTORIAL in natural transformations. For `Representable` K and K′ and any `τ : K ⟹ K′` there is a UNIQUE `h : repr_obj K' ~> repr_obj K` compatible with both representations (`repr_induced`, `repr_induced_compatible`, `repr_induced_unique`, packaged as `repr_induced_universal : ∃! h, ReprCompatible R R' τ h`), with `repr_induced_id`/`repr_induced_comp` making the assignment functorial and the τ = id case falling out as `repr_unique_iso` — whose `repr_unique_iso_universal` carries the uniqueness clause a bare `≅` does not. **The variance is not a choice**: `Yoneda_Embedding'` relates `hom d c` to `[Hom c,─] ⟹ [Hom d,─]`, so the induced arrow runs BACKWARDS between representing objects. **THE HEADLINE IS A UNIVERSE RESULT, AND IT IS THE OPPOSITE OF WHAT THE ISSUE'S SUGGESTED ROUTE WOULD HAVE GIVEN.** `Yoneda_Embedding'`, `Yoneda_Full` and `Yoneda_Faithful` are all over `Category@{u u u}` — object, hom and proof identified — while the `Representable` class itself is over `Category@{o h h}` with the object universe FREE, so routing the theorem through the hom-bijection as suggested would have pinned it to a strictly narrower class of categories than the class it is about. It is not routed that way: `repr_induced` and `repr_induced_universal` carry **no `o = h` and not even `o <= h`** in their constraint blocks (measured there, not read off the binder — a binder can display free while the constraints identify, as `repr_pair_iso_from_is_induced` in the cross-link section does). **And the pin on the three `Yoneda_*` constants is PROVED to be an artifact rather than inherent**: both proof scripts were re-derived verbatim inside a section declaring `Constraint uo < uh`, and both succeed — those re-derivations are positive controls 7 and 8 of the probe, so the claim is guarded rather than measured. The repair to `Functor/Hom.v` is deliberately NOT made here (wide blast radius) and is left as the disclosed deferral. The two routes are nevertheless proved to agree where both are formable: `repr_induced_is_yoneda_transpose` shows the issue's suggested transpose and the arrow named directly are **the same term** by `eq_refl`, not merely equivalent arrows. **The `IsUniversalProperty` ↔ `Representable` cross-link was a CONSTRUCTION, not a citation**: no passage existed in either direction anywhere (only two files mentioned both class names, and in both the other is prose), so both are built — `Representable_of_UnivProperty` and `UnivProperty_of_Representable` — with the honest note that the latter is a tautology, `IsUniversalProperty` being representability with the predicate left free, and with **no claim that they are mutually inverse**, which they are not, being not even composable in general (one needs a point of the predicate, the other replaces it). `univ_property_iso_from_is_induced` identifies the backward leg of `univ_property_unique_up_to_unique_iso`'s isomorphism as `repr_induced … nat_id`. The optional functor packaging IS delivered and is not thin: `ReprCat` is `Construction/Subcategory.v`'s `Sub` at the `Representable` predicate (six lines, since the predicate is already Type-valued on objects of `[C, Sets]`), and `ReprObjFunctor C : ReprCat C ⟶ C^op` — opposite because `repr_induced_comp` reverses composition — is proved **FULL and FAITHFUL**, fullness being Mac Lane's correspondence run backwards and faithfulness the compatibility condition probed through ψ's invertibility; essential surjectivity and any equivalence with `C^op` are explicitly NOT delivered. Two strict attempts REFUTED and pinned with their causes diagnosed: `repr_pair_iso`'s leg is not `repr_induced … nat_id` on the nose because `nat_id`'s component is `fmap[F] id` rather than `id`, and the `YoEvalAt` witness is only `≈` componentwise because Yoneda's backward leg leaves a `∘ id` — over (ℕ,+) an `n + 0`. Eight negatives in all, six FORMABILITY and two CONVERSION, kept lexically apart, against twelve positive controls. Non-vacuity over `Deloop Nat_Plus` at both the tautological representation and the in-tree `YoEvalAt_Representable`, with a scope point the exercise does not anticipate: **at DISTINCT representing objects "the induced h is not an identity" is not even well-typed** (`repr_induced` runs `repr_obj R' ~> repr_obj R`, so `≈ id` elaborates only when the two coincide), so such a witness forces the two REPRESENTING OBJECTS to coincide — **not**, as an earlier draft of this bullet said, a one-object category: an audit refuted that with a compiled counterexample over `Coq`, which has at least two objects, using `Hom_Representable bool` on both sides. A one-object category is merely the cheapest way to arrange the coincidence, which is why the witness is over `Deloop Nat_Plus`. What replaces the claim in general is `ReprObj_Faithful`. `Hom_Representable` (the tautological representation of a hom-functor) is deliberately a plain `Definition` rather than an `Instance` so typeclass resolution is unperturbed, and `Structure/Cartesian/Closed/Adjunction.v`'s header is amended in the same commit — prose only — to record the new `Hom_Representable` inhabitant and to point at the two new passages. Read that amendment precisely: the old text never claimed the two classes were UNRELATED (it said `IsUniversalProperty` lives "under a different class" and noted the analogy that it too requires a natural iso out of a hom-functor), and its inhabitation sentence was already past-tense and scoped, so the edit is a clarification rather than a correction of something false
- **Functor/Hom/Induced.v**: Mac Lane §II.5 Ex 7 — the arrow functions of `T : A ⟶ D` as ONE natural transformation `hom_action T : Hom A ⟹ Hom D ◯ (T^op ∏⟶ T)` (component `f ↦ fmap[T] f`, the two naturality fields each `now rewrite !fmap_comp`; the component family a plain Definition with `proper_morphism` supplied inline, so the bridge `hom_action_component` holds at Leibniz `=` by `reflexivity`). The novelty is measured precisely: every other pullback of a hom-bifunctor along a PRODUCT OF TWO FUNCTORS in tree carries `Id` in one slot (Repr_left/Repr_right, Adjunction/Hom.v; `CoHom_Alt := Hom C ◯ Swap` pulls back along Swap, not a `∏⟶`) — here the SAME non-identity functor occupies BOTH slots, which is what keeps the index category fixed at `A^op ∏ A` (`F ∏⟶ F` itself already occurs in the monoidal-functor comparison, named in the header). Componentwise readings through `transform[hom_action]`: `Faithful T ↔ hom_action_injective` and `Full T ↔ hom_action_surjective` (↔ is Lib's Type-valued iffT; the surjectivity is preimage-as-DATA mirroring `Full`'s bare `prefmap` section, no functoriality asserted), four named directions with the biconditionals transparent pairings so projection returns the named lemma by conversion; `hom_action_Id`/`hom_action_Compose` eq_refl acceptance Examples; all 15 constants (13 named plus `hom_action`'s two Program obligations) closed under the global context.
- **Functor/Hom/Limit.v**: Mac Lane §III.4 p. 70 (`maclane:III.4:remark3`) — the covariant hom-functor preserves limits, delivered at CONE level: **`hom_ContinuousFunctor c : ContinuousFunctor (HomFrom c)`** with the per-diagram `hom_PreservesLimitCone`, where `HomFrom c := [Hom c,─]`. **The issue asks for the apex-only `PreservesLimit`; that is the WEAKER reading and is delivered only as the derived `hom_PreservesLimit`/`hom_PreservesAllLimits`** — `Structure/Limit/Preservation.v`'s own header states `ContinuousFunctor` "is what the word means in Mac Lane §V.4" and that apex-only is a CONSEQUENCE, so the cone-level statement is the faithful one. No separation between them is proved, so "strictly weaker" is not claimed. What the cone level pins and the apex-only reading does NOT name at all (scope the first: `hom_preserved_leg` is an instance of a fact true of an ARBITRARY functor, so it measures what cone-level preservation pins in general; `hom_preserved_mediator` IS specific to this file's `hom_med`): `hom_preserved_leg` (the image cone's legs ARE `fmap[HomFrom c]` of the originals, `eq_refl`) and `hom_preserved_mediator` (the produced mediator IS `hom_med`, `eq_refl`), with `hom_med_commutes`/`hom_med_unique`. **NO `Sets_Complete` IS USED, AND THAT IS MEASURED RATHER THAN ASSERTED**: the transitive dependency closure is 52 modules (my `coqdep` iteration first said 53 by counting `./Lib.v` and `Lib.v` as two — the file header's 52 was right) containing neither `Instance/Sets/Complete.v` nor `Instance/Sets/Products.v` — possible because `PreservesLimitCone` quantifies over CONES, so each element of a competing apex gives a fibre cone in `C` and the mediator is `s ↦` its factorization; the issue's suggested compatible-families route would have imported completeness and its universe pins for nothing. Mac Lane's own product form is stated explicitly and at an ARBITRARY index type: `hom_IsIndexedProduct` and `hom_IsCartesianProduct`, both apex-pinned. **Those two are proved DIRECTLY and are NOT derived from the headline, and the reason is a DONOR ANNOTATION DEFECT rather than a structural obstruction**: `IsLimitCone` identifies the shape's hom-and-proof universe with the ambient category's, while **`DiscreteCat_Functor`'s UNANNOTATED declaration** (`Instance/Discrete.v`) instantiates `DiscreteCat@{u Set Set}` — so routing the corollaries through the discrete presentation pins `C` to `Category@{o Set Set}`, and stated directly they carry no `Set` at all. **Attribute that to the FUNCTOR, not the shape**: `DiscreteCat@{o h p} (A : Type@{o}) : Category@{o h p}` has hom and proof FREE and `DiscreteCat@{Set uh uh} bool` elaborates under `Constraint Set < uh` — an earlier revision of this bullet said "`DiscreteCat`'s is `Set`", which is false. An audit went further and compiled a re-annotated discrete-diagram functor that LIFTS the blocking step, so the pin is of the `Build_Quiver_Standard_Eq` minimization family (#300's erratum) and is NOT claimed unavoidable; what remains unestablished is whether the WHOLE derivation closes (the tree has `limit_is_indexed_product` but not its converse, and `family_cone` is itself unannotated). The cost stands: the corollaries are not exhibited as instances of the headline, and no bridge is invented. The contravariant twin is pure instantiation — `HomTo c` IS `@HomFrom (C^op) c` by `eq_refl` (`hom_to_is_op_hom_from`), so `cohom_ContinuousFunctor`, `cohom_colimit_to_limit : IsColimitCocone N → IsLimitCone (FCone (HomTo c) N)` and the two dual product corollaries are all `:=` with no tactic; note `Check` DISPLAYS `cohom_IsCartesianProduct`'s hypothesis with the category suppressed, so it reads as a product where the source says `@IsCartesianProduct (C^op)` — a coproduct in `C` — flagged in-file as a display hazard. Non-vacuity at both levels over `Coq`: `coq_hom_product` with three COMPUTING `eq_refl` Examples on `Coq(unit, bool × nat)`, and the cone-level `coq_hom_limit_cone`/`coq_hom_bool_nat_limit` applying the headline to the in-tree `Limit` from `Cartesian_Limit` — that one does NOT compute (`Cartesian_Limit` is `Qed`) and carries the `Set` pin, both disclosed. By-product: `Structure/Cartesian.v` has `Cartesian` and `IsCartesianProduct` but NO passage between them (only `product_of_pullback` builds the record, from a pullback), so a six-line `cartesian_IsCartesianProduct` is declared HERE, not registered as an `Instance` and not moved into that file. **42/42** constants closed under the global context — and the delegate's 42 was RIGHT while my 39 was wrong: my glob sweep matched only `def|ind|constr` and so dropped 6 `prf` and 3 `inst` entries, the three missing names being the `#[local] Instance`s `hom_med_respects`, `hom_iprod_med_respects` and `hom_fork_respects`. The glob alone carries 41 declarations and `Print Module` lists exactly 42, so the method this bullet states could never have yielded 39. `Test/ProbeHomLimit331.v` pins the boundary with 2 negatives of DIFFERENT KINDS kept lexically apart — one TYPING (`HomFrom`'s implicit `{C : Category}` is undeterminable from a bare `unit : Type` — NOT, as an earlier revision said, that `FCone`'s category arguments cannot be implicit: they can, `coq_hom_limit_cone` writes them so, and an object ascription repairs it) and one FORMABILITY (the `Set` pin above) — plus an instrument check, each stripped and its kind confirmed, rename-simulated at 7/7. NOT delivered: no converse or characterisation, no separation of cone-level from apex-only, no cocontinuity of `HomFrom`, no reflection or creation, no instantiation of `cone_comparison` here (performed since #428 by Functor/Hom/Continuous.v's `remark2_comparison_iso`), no continuity of `Curried_Hom C` itself, no naturality in `c` (the equation is #428's `remark2_natural`; no statement in `[C^op, Sets]`), no relation to RAPL or Yoneda, no nullary case, and no concrete witness for the dual corollaries
- **Functor/Hom/Continuous.v**: Mac Lane §V.4 Theorem 1 and Remarks 1-3 (book pp. 116-117, `maclane:V.4:thm1`, `maclane:V.4:remark1-3`) + Awodey §5.5 Proposition 5.27 / Corollary 5.29 and §7.2's transport remark + Riehl §3.5 (3.5.1), Exercise 3.5.i, Theorems 3.5.2 and 3.5.6 — **the ISOMORPHISM form of hom-functor continuity, `lim C(c, F−) ≅ C(c, lim F)` natural in c, with its dual, its product and coproduct instances, the Riehl (3.5.1) round trip, the end/limit bridge, and transport of preservation along a natural isomorphism of functors**. Every theorem is a transport or an instantiation of a landed one. **Stale premises**: Theorem 1 and Remark 3 at CONE level were ALREADY LANDED by #331 (Functor/Hom/Limit.v `hom_ContinuousFunctor`, `cohom_ContinuousFunctor`, `cohom_colimit_to_limit`), so the issue's "no preservation witness for the hom-functor" is false and its two pinned names are supplied as aliases `hom_preserves_limits` / `cohom_carries_colimits_to_limits`; "`Curried_CoHom` has no consumer beyond Yoneda" false (`HomTo` consumes it); "no `Cocartesian` companion file" false (Adjunction/Diagonal/Coproduct.v); the Instance/Ens.v header has no universe caveat to discharge; TRUE gaps: Remark 2 as a `Sets` isomorphism, the product/coproduct instances as isomorphisms (not UMP records), Awodey's transport along an isomorphism of FUNCTORS (Theory/Equivalence/Limit.v's `isalimit_transport` handles isomorphisms of DIAGRAMS only), and all three Riehl §3.5 items. **Transport** (A): `fun_equiv_whisker`, `transport_isalimit`/`_cone`/`_coneiso`/`_islimitcone`, **`PreservesLimitCone_transport : PreservesLimitCone K F → PreservesLimitCone K G`** for `F ≈ G`, `ContinuousFunctor_transport`. **Riehl (3.5.1), no limit assumed** (B): `cone_of_nat_mor`/`nat_of_cone_mor`, the one-line round trips `nat_cone_nat`/`cone_nat_cone`, **`cone_nat_iso : Nat(Δ1, HomDiagram X F) ≅[Sets] Cone(X, F)`** independent of `wl_iso`, naturality in X `cone_of_nat_natural`/`nat_of_cone_natural` (both `reflexivity`), and the tuple presentation `limtuple_cone_iso : Sets_limit_obj (HomDiagram X F) ≅[Sets] Cone(X, F)`. **End/limit bridge** (C): `homend_functor := HomDiagram X F ◯ Snd`, `end_limtuple_iso`, `end_cone`, `end_coneiso`, `end_IsLimitCone`, **`end_Limit : Limit (HomDiagram X F)`** with the end as apex — the one missing link the issue isolates. **Remark 2** (D), over `L : Limit F`: `homlim_cone`/`homlim_islimit` (Instance/Sets/Complete.v's limit of the hom-diagram), `homimg_islimit` (#331 at the limit cone), `remark2_coneiso` (a `ConeIso`), **`remark2_iso c : Sets_limit_obj (HomFrom c ◯ F) ≅[Sets] C(c, lim F)`** with `remark2_iso_legs`/`remark2_iso_from`, `homlim_reindex`, and **naturality in c as the equation `remark2_natural`** (by `limit_med_eq`); Riehl's own direction `remark2_comparison` with **`remark2_comparison_iso`** — the `comparison_iso_of_PreservesLimitCone` instantiation Functor/Hom/Limit.v recorded as not performed. **Remark 3** (E): `remark3_iso : Sets_limit_obj (HomTo c ◯ F^op) ≅[Sets] C(colim F, c)`, `remark3_natural` (a bare `:=` and a one-line `exact`) at `(J^op, C^op, F^op)` (`Colimit F` IS `Limit F^op`, `HomTo c` IS `HomFrom c` on `C^op`). **Product/coproduct instances as isomorphisms** (F): **`hom_iprod_iso : C(c, ∏ aᵢ) ≅[Sets] ∏ᵢ C(c, aᵢ)`**, **`cohom_icoprod_iso : C(∐ aⱼ, c) ≅[Sets] ∏ⱼ C(aⱼ, c)`** by `iprod_unique_iso` against #331's UMP records and `Sets_IsIndexedProduct`, with `_to` readbacks and naturality in c. **The issue's names and Remark 1** (G): the aliases `hom_preserves_limits`/`cohom_carries_colimits_to_limits`, `representable_iso_ContinuousFunctor : HomFrom c ≈ G → ContinuousFunctor G` (Awodey §7.2 as stated), `HomFrom_at@{o h s +} : C ⟶ Sets@{h s}`, `hom_continuous_at`. **Universes**: Remark 1 is delivered only as polymorphism over C — `Sets@{o so} : Category@{so o o}` (Instance/Sets.v) instantiates `SetoidObject@{o o}`, so every set in `HomFrom c`'s image has carrier AND relation at C's hom level, and the free parameter of `HomFrom_at`/`hom_continuous_at` is only the level of the universe holding Sets' objects (an earlier revision called it the relation level and the remark half true); the hom-functor of a FIXED C cannot be read into a larger universe of sets (probe N2: `HomFrom cw : Cw ⟶ Sets@{hbig sbig}` refused under `h1 < hbig`, "Cannot enforce h1 = hbig", beside the accepted `Sets@{h1 sbig}`); 40 of the first 50 blocks carry `u0 = u2` (the 39 shape-indexed constants other than `cohom_carries_colimits_to_limits`, whose binder already shares the hom variable, plus `ContinuousFunctor_transport`, where as in all of section A it reads C's hom = D's hom; five transports add `u0 = u7`), eight of the ten constants taking `L` add the limit's level (`u0 = u3`), object levels free; **SMALLNESS**: 23 constants carry `u <= u2` — J's OBJECT level at or below C's hom = Sets' carrier — every one forming `Sets_Limit`/`Sets_limit_cone` of the hom-diagram or `[J, Sets]`; the other 17 shape-indexed constants and #331's theorem carry none (probe N3: under `h < jo`, `HomFrom cw ◯ F` and `hom_PreservesLimitCone cw F` accepted, `Sets_Limit (HomFrom cw ◯ F)` refused "Cannot enforce h = … because h < jo <= …") — Riehl's large-diagram footnote is exactly what the bound excludes; the four product/coproduct constants carry `u0 = u1` (index universe = C's hom, from `Sets_iprod_obj@{u0 u0 u0 u0 u0}`), a collapse not a `Set` pin; the two aliases written bare do NOT minimize (measured against their donors; unlike #425's corollaries); no word-bounded `Set`, no `JMeq`/`EqdepFacts`/`eq_rect_r`; section C's eight constants and their seven obligations carry stdlib `prod_rect` bounds from the product shape. **70 constants closed under the global context, zero `Axioms:` lines** — 51 `.glob` heads (38 `def`, 13 `prf`) plus 19 `Program` obligations (`Obligation Tactic := idtac` locally; every obligation written out); the gate carries the 51 heads; two `Defined`, `fun_equiv_whisker` LOAD-BEARING (flipped to `Qed`, `transport_coneiso_legs`'s `change … with` stops), `end_coneiso` not; 32 `Qed` tokens; closure 73 excluding self (Theory/Equivalence/Limit.v 7 at the margin, kept for `isalimit_transport` against ~35 duplicated lines; Functor/Hom/Limit.v 2, Instance/Sets/Complete.v 2, Instance/Sets/End.v 1, Structure/Limit/Product/Finite.v 1, Structure/Limit/Weighted.v 1, 21 `Require`s 0); no name collision beyond the `remark2_natural` mention this commit adds to Functor/Hom/Limit.v's header. `Test/ProbeHomContinuous428.v` mirrors the `Require` list and carries **4 refutation commands = 1 instrument + N1 CONVERSION (`fmap[HomFrom c ◯ F] g = fmap[HomDiagram c F] g` refused while the objectwise and elementwise forms convert — the opaque-`Program`-obligation trap, so Remark 2 is stated over the composite and the Riehl items over `HomDiagram`, with no bridge) + N2 UNIVERSE + N3 UNIVERSE**, each stripped one at a time in a copy of the whole file beside its accepted controls; five `eq_refl` readbacks; guard coverage 24/21 with three exhaustive exceptions; rename-simulated 12/12, every first break on a positive line; `make todo` grows by those 4 lines only (2233 → 2237 over master 1de68608), so the issue's "adds no new hits" box is not met as written (disclosed). Two sentences of Functor/Hom/Limit.v's NOT DELIVERED block repointed line-neutrally (its bullet above says the same). NOT delivered: Remark 2 as an isomorphism of presheaves in `[C^op, Sets]` (the equation form only, as `wlim_natural`); Riehl's large-diagram footnote; a Remark 2 over `HomDiagram` or any bridge between the two presentations; continuity of `Curried_Hom C` itself and the rest of Functor/Hom/Limit.v's list; Awodey §7.2's `Grp` consequence (#411); Remark 1 as a lift; no edit to Functor/Hom.v, Structure/Limit/Weighted.v, Instance/Sets/Complete.v, Instance/Sets/End.v, Theory/Equivalence/Limit.v or Instance/Ens.v.
- **Functor/Construction/Postcompose.v**: Mac Lane §III.2 Ex 4 + remark 1 — naturality is unchanged by enlarging the codomain. `Postcompose J : [D,E] ⟶ [D,E']` for an ARBITRARY `J : E ⟶ E'`, whose `fmap` IS `Theory/Natural/Transformation.v`'s left whiskering `J ⊳ θ` on the nose (both actions `eq_refl`; `Instance/Fun.v` carries the whisker LEMMAS, not the operation); `Postcompose_Faithful` (3 lines), `Postcompose_Full` under `Full J + Faithful J`, and the headline `postcompose_hom_iso : @Isomorphism Sets (NatSetoid K L) (NatSetoid (J◯K) (J◯L))` with the readable `postcompose_nat_bijection : (K ⟹ L) ≊ (J◯K ⟹ J◯L)`, `nat_setoid_is_Transform_Setoid` recording by `eq_refl` that the two packagings are one statement. **Faithfulness is spent TWICE and the second time is the non-obvious one**: `Full`'s `prefmap` is a bare section with NO functoriality demanded, so naturality of the recovered family is a THEOREM (`fmap_inj` + `fmap_comp` + `fmap_sur`, the `Structure/Groupoid/Basepoint.v` `deloop_ff_moniso` pattern) — and `prefmap` is not assumed to respect `≈` either, so the backward leg being a `SetoidMorphism` at all is a second theorem. **The issue's premise that no postcomposition functor exists in tree is FALSE and the file says so**: `Partial_r (@Cat_Hcompose D E E') J` already is one, and it is built here as `PostcomposeViaHcompose` and COMPARED — object actions `eq_refl`, arrow actions REFUTED at Leibniz because `bimap F id g` feeds an identity 2-cell into the Godement product and `nat_id`'s component is `fmap[J] id` rather than `id` (`postcompose_via_hcompose_component` pins the cause on the nose), agreement at `≈` via `postcompose_via_hcompose_equiv` with `iso_id` components; the direct build is chosen so that unit does not sit inside every naturality computation, and `Theory/Kan/Extension.v`'s `Induced` is cited as the PREcomposition mirror. **THE THEOREM DOES NOT REACH MAC LANE'S OWN MOTIVATING INSTANCE, and that is measured rather than glossed**: `Instance/Sets/Powerset.v`'s `Sets_Lift : Sets@{o so} ⟶ Sets@{so sso}` exists, but `Postcompose Sets_Lift` is unformable — and one need not even reach the functor category to meet the wall, since already `Sets_Lift ◯ K` is rejected, `Compose` wanting one shared hom level while `Sets`' own declaration forces `o < so`; read that precisely, since it says `Compose` ALONE suffices and NOT that the functor category is innocent — `Fun` independently identifies its source and target hom levels, so the two `Sets` levels could not index one functor category either. Both refusals are pinned as formability negatives and no route around them is offered. Universes measured in the constraint blocks: all six hom/proof levels of D, E, E' COLLAPSE to one, attributed to `Compose` (declared over three categories sharing one hom-and-proof level) with `Fun`'s own `u0 = u2` a second, independent cause — the three OBJECT universes stay free. Sharpness is proved, not assumed: `postcompose_full_needs_full` exhibits `DiscToTwo : Two_Discrete ⟶ _2`, faithful and provably not full, whose `Postcompose` is provably not full, while `postcompose_disc_Faithful` shows faithfulness alone does transfer; whether faithfulness of J is NECESSARY for fullness of `Postcompose J` is explicitly left open — no proof, no counterexample. Full-subcategory instantiation over `Incl_Faithful`/`Full_Implies_Full_Functor`, witnessed at `FinSets_Incl` (Mac Lane's own §I.3 example, so no new subcategory) with non-degeneracy proved three ways including a preimage exhibited DOWNSTAIRS in `Sets` rather than obtained by whiskering, and the disclosure that over the shape `_1` naturality is degenerate so the witness exercises the hom-setoids and fullness only. Nine negatives (5 conversion, 4 formability, kept lexically apart) against 12 controls; two of the conversion negatives are DONOR OPACITY rather than mathematics — `Full_Implies_Full_Functor` is a `Qed` lemma, so no component of that `prefmap` reduces. Two engineering findings recorded in-file: `Construction/Subcategory.v` exports its own `Full` (first argument a `Category`), handled by the `Functor/Diagonal.v` mid-file-`Require` idiom plus the `Instance/Field/Frac.v` qualified spelling; and writing an indexed-inductive arrow action inline in a `Program Definition` routes the branches through a dependent eliminator whose `eq`/`JMeq` obligations no ordinary case analysis discharges — elaborate the object and arrow actions as plain `Definition`s first. 63/63 constants closed under the global context
- **Theory/Concrete.v**: Concrete categories (Mac Lane §I.7; Riehl §1.6) — `Concrete C := { underlying : C ⟶ Sets; underlying_faithful : Faithful underlying }`, with "arrows are actual functions" as the definitional unfolding (`concrete_fun`, `concrete_arrow_eq`, `concrete_arrow_iff`); Awodey's generalized-element refinement as `Separator`/`WellPointedCategory`, with the round trip proved where it actually holds — `Separator t` ⟺ `Faithful (Hom(t,−))` — rather than between `Separator` and `Concrete` at large. `Sets` is concrete twice over in Theory/Concrete.v itself (identity and global elements) and is well pointed; the roster in Instance/Concrete.v is `Coq`, `CMon` (supplying the faithfulness Instance/CMon.v only asserts in a comment), and `Rel`. **A folklore correction, proved:** the common quotation "Rel admits no faithful functor to Sets" is FALSE (the target is that folklore reading, not Mac Lane's own sentence, which already scopes the claim to the evident forgetful functor) — `Rel_Concrete` concretizes it by the direct-image functor. What is true is the precise negative: Rel's arrows are not functions (`Rel_hom_is_not_a_function`) and no candidate whose value at `1` is a subsingleton can be faithful (`Rel_subsingleton_not_Faithful`), which covers the evident forgetful functor. Freyd's Toph non-concreteness is a disclosed deferral. Riehl's multi-sorted Example 1.6.19 is Construction/Free/Quiver/Concrete.v: the vertex and arrow functors each proved NOT faithful with explicit parallel witnesses, and the disjoint-union functor faithful — the latter under node-UIP, giving `Concrete SetQuiverCat` rather than `Concrete QuiverCategory`, because the in-tree hom-equivalence is a sigma carrying one chosen node family while faithfulness yields per-edge endpoint equalities that nothing ties together; and the hypothesis is proved NECESSARY — `faithful_QuiverElements_implies_UIP` shows unrestricted faithfulness would give UIP, so concreteness of `QuiverCategory` via this functor is unreachable axiom-free; whether some other functor concretizes it is left open
- **Theory/Equivalence.v**: Equivalence of categories — quasi-inverse class with the Full+Faithful+essentially-surjective characterization (Equivalence/FullFaithful.v), adjoint equivalences (Equivalence/Adjoint.v), preservation/reflection/creation of limits and transport of adjunctions and monoidal structure along equivalences (Equivalence/{Limit,Adjunction,Monoidal,Bundled}.v); RAPL/LAPC in Adjunction/Continuity.v over the preservation vocabulary of Structure/Limit/Preservation.v; adjunction composition in Adjunction/Compose.v
- **Theory/Skeleton.v**: Skeletons and skeletal categories (Mac Lane §IV.4, Riehl §1.5) — the `Skeletal` predicate (isomorphic objects are equal) and "A is a skeleton of C" packaged as DATA: a full subcategory with a chosen representative and a uniqueness clause stated at the level of `Sub`'s objects, so no choice principle is consumed and no existence claim is made (that claim is equivalent to AC, per Theory/Equivalence.v). Derived: `skeleton_is_skeletal`, and `skeleton_inclusion_is_equivalence` through the FF+ESO donor with the reflector reducing (`skel_reflect_obj` by `reflexivity`, `skel_reflect_incl_obj` on the nose at objects; `skeleton_remark` packages Mac Lane's `Id ≅ K ◯ T`). Exercise 1 is proved at STRICT strength with NO rigidity hypothesis: `skeletal_equivalence_is_isomorphism` turns any equivalence between two skeletal categories into `A ≅[StrictCat] B`, and `skeletons_are_isomorphic` and `skeletons_isomorphic_iff_equivalent` follow by composing equivalences. `≅[StrictCat]` is forced — `Cat`'s hom-setoid IS natural isomorphism (Instance/Cat.v), so an isomorphism in `Cat` between skeletons would only re-say that both are equivalent to the ambient category. The crux is that the two evident comparison functors are not mutually strict inverses (their composite conjugates by an automorphism); the fix is the HoTT half-adjoint correction of the counit at the level of object types (`half_adjoint_triangle`), which makes the offending loop `eq_refl` and replaces the object-UIP hypothesis the naive route needs. That the carrier-level weakening does not suffice is itself a theorem: `skeleton0_skeletal_forces_UIP` shows that deriving `Skeletal (Sub C (s0_sub S))` uniformly from the carrier-level record `Skeleton0` would entail UIP for every type, by a free-loop-space countermodel over `DiscreteCat` (the pattern of `arrow_mul_respects_forces_UIP`), while `skeleton0_is_skeletal_carrier` keeps the carrier half. Riehl's non-functoriality remark gets its positive clause, `skel_comparison`/`skel_comparison_natural`, with the pseudofunctor explicitly deferred (its object assignment IS the AC-equivalent existence claim). Separation (Theory/Skeleton/Separation.v): `skeletality_is_not_equivalence_invariant` separates `1` from `Indiscrete bool` — the core file's `Skeletal_StrictCat_invariant` supplying the other half of Awodey §7.10 Ex 11 — and `Indiscrete_bool_skeleton_is_One` exhibits the skeleton of a non-skeletal category on the nose. Instances: `FinSet_Skeletal` by a self-contained `Fin.t` pigeonhole (Instance/FinSet/Skeleton.v), upgrading Instance/FinSet.v's long-standing prose; and `Proset_Skeletal_iff_Antisymmetric` — partial orders are exactly the skeletal preorders (Instance/Proset/Skeletal.v, Fong–Spivak §1.2.2), with `skeleton_of_proset_antisymmetric` for Riehl's "the skeleton of a preorder is a poset" at the carrier level.
- **Construction/Subcategory/Dense.v**: Mac Lane §IV.4 Proposition 2 (book p. 94, `maclane:IV.4:prop2`) — **an isomorphism-dense full subcategory is reflective**: `IsoDense S := ∀ c, { a : Sub C S & Incl C S a ≅ c }` (Mac Lane's "every c ∈ C is isomorphic (in C) to some object of A" with the object CHOSEN, the reading `EssentiallySurjective` already takes — `iso_dense_ESO` is the repackaging, a plain `Definition` with zero obligations; the `EssentiallySurjective` round trip closes at `eq_refl` on the WHOLE record while the `IsoDense` one closes on the object component only, stdlib `sigT` lacking eta, pinned), `dense_incl_equivalence : EquivalenceOfCategories (Incl C S)`, the adjoint equivalence in BOTH handednesses (`dense_incl_adjoint_equivalence` with the insertion on the LEFT, and `dense_adj : dense_reflector ⊣ Incl C S` with it on the RIGHT — Mac Lane's ⟨T, K; η, ε⟩ under the §IV.1 convention that the first-listed functor is the left adjoint), and **`dense_full_subcategory_reflective : Full C S → IsoDense S → Reflective S`** (the issue's pinned name) as `Construction/Reflective.v`'s own record through `Build_Reflective`, all three fields reading back at `eq_refl`. **THE FILE PROVES NO NEW EQUATION — every constant is a `:=` with no tactic, and there is no `Qed` and no `Defined` anywhere in it**: fullness of the inclusion is `Full_Implies_Full_Functor`, faithfulness `Incl_Faithful` (`Construction/Subcategory.v`, LITERALLY in the equivalence's proof term — the reviewer check, visible under `Set Printing Implicit`), the equivalence `FF_ESO_Equivalence`, the adjoint equivalence `Equivalence_to_AdjointEquivalence`; the ONE design decision is the HANDEDNESS — `Equivalence_to_AdjointEquivalence` makes the equivalence's OWN functor the left adjoint, so at `Incl` it yields `Incl ⊣ T`, the wrong way round for a reflection, and `AdjointEquivalence_swap_adjunction` (`Theory/Equivalence/Adjoint.v`, the shape `Theory/Equivalence/Creation.v` already uses) supplies `T ⊣ Incl`. The reflector's object action IS the chosen representative at `eq_refl` (`dense_reflector_obj`), because `FF_ESO_Equivalence` is `Defined` and `ff_eso_inverse`'s `fobj` is `eso_obj`. **THE ISSUE'S "Current state" IS STALE ON FOUR CLAIMS**, all measured at `62de3fb3`: "there is no generic `Faithful (Incl C S)` instance" is FALSE — `Incl_Faithful` exists and is consumed by `Theory/Skeleton.v` among others, so work item 1 was done before this issue began; the issue's donor citations have drifted, for `Incl`, `Full`, `Full_Implies_Full_Functor` and for `EssentiallySurjective` in `Theory/Equivalence.v`; "the existing applications of `FF_ESO_Equivalence` are not subcategory inclusions" is STALE — eleven files apply it, fourteen mention it (the declaring file and two prose mentions make the difference), and `Theory/Skeleton.v` applies it to `skel_incl S`, the `Incl` of a `Sub` (`Instance/FinSet/Skeleton.v`'s `FinSet_Incl` is a hand-built `Program Definition`, not an `Incl C S`); and the skeleton corollary the issue defers "once `Theory/Skeleton.v` lands" is deliverable, that file having landed. **THE SPECIAL CASE PRECEDED THE GENERAL STATEMENT, AND THE GENERAL REFLECTOR AT A SKELETON IS THE OLD ONE ON THE NOSE**: `Theory/Skeleton.v` already carried `Incl_EssSurj`, `skeleton_inclusion_is_equivalence` and `skel_reflect`; here `skeleton_IsoDense` pairs `skel_rep` with `iso_sym` of the `Skeleton` record's own `skel_iso` field and nothing more, `skeleton_reflective Sk : Reflective (skel_sub Sk)` is the general theorem applied, and `skeleton_reflector_is_skel_reflect : reflector (skeleton_reflective Sk) = skel_reflect Sk` closes at `eq_refl` on the WHOLE functor record (the two `EssentiallySurjective` records agree fieldwise and the class has eta) — the brief had asked for that whole-record claim to be refuted and pinned, and it is TRUE instead, so no second reflector enters the tree. **MAC LANE'S "COUNIT THE IDENTITY" IS NOT DELIVERED, AND THE OBSTRUCTION IS A TYPING FACT RATHER THAN A MISSING PROOF**: his "for objects a ∈ A we can then choose a₀ = a" is a case distinction on membership made per object, whereas the hypothesis here is ONE uniform function, so the counit at `a` runs `dense_reflector (Incl a) ~> a` between two objects of `Sub C S` that need not coincide — `counit (dense_adj D) a = id[a]` is ILL-TYPED, pinned as a TYPING negative (a plain "has type … expected", no `cannot unify`) at a witness where the two endpoints are PROVABLY distinct: the full subcategory of `Indiscrete bool` on BOTH points with each point's chosen representative the OTHER point (`swap_dense`, `swap_objects_differ`). Delivered instead is the componentwise isomorphism `dense_counit_iso : IsIsomorphism (counit (dense_adj D) a)` (`adj_equiv_counit_iso` of the swapped adjoint equivalence, a `:=`), bundled as `dense_counit_Isomorphism` whose forward leg IS the counit at `eq_refl`; `reflective_counit_iso` (`Construction/Reflective.v`) states the same fact at record level and is cited, not restated, being `Qed` while producing data. Two further identifications are measured and NOT obtained, each with its cause: the counit as the fullness-lift of the chosen isomorphism at `≈` is true but the route through `equiv_adjunction_counit_at` (`Adjoint.v`) stops at `symmetry` on `Functor_Setoid` (`Theory/Functor.v`), whose `Equivalence` obligation is `Qed`, so the isomorphism family does not reduce (the strict form refuted and pinned; the `≈` form neither proved nor pinned); and the strictified variant under "the representative of an object already in the subcategory IS that object" would NOT suffice even where it holds, the chosen isomorphism being possibly a non-identity automorphism — Mac Lane's remark needs BOTH his choices, "a₀ = a" AND "η_{Ka} the identity", and `swap_not_self_dense` shows the first fails at the witness. Universes: all 26 constants over `C : Category@{u u0 u0}` in the BINDER, inherited from `Subcategory` (empty block; pinned as the one formability negative, the category's hom, identity and hom-setoid accepted at levels declared apart — `IsoDense` and `Reflective` cannot be tested apart from their `Subcategory` argument, the #340 trap); 21 blocks carry no equation and FIVE — `dense_full_subcategory_reflective` and its four `eq_refl` readbacks; the four skeleton constants package `Reflective` too and carry none — carry `u0 = u2`, attributed by `About` to `Reflective` instantiating `Subcategory`'s fourth universe at C's hom level where `Subcategory` itself leaves it free; zero word-bounded `Set` tokens in the library file (the witnesses' `Indiscrete@{u} : Category@{u Set Set}` pin is `Instance/Discrete/Reconstruct.v`'s, disclosed). **26/26 + 15/15 constants closed under the global context** (26 glob heads = `Print Module`; no `Program`, no records, so no `Build_*`), zero collisions over 45 names (instrument `Full`/`Monoid` = 3; `Theory/Skeleton.v`'s `Incl_Full`/`Incl_EssSurj` are EXPORTED after their section, so they are real hazards, and were avoided), closure 32 excluding the file (the `Theory/Skeleton` `Require` costing 7 of it). `Test/ProbeDense375.v`: 6 `Fail`s = 1 instrument + 5 negatives of THREE kinds (2 conversion, 2 typing, 1 formability), each stripped one at a time with its whole error read, rename-simulated 12/12 over the constants the `Fail`s name; witnesses `Indiscrete_bool_Skeleton` through `skeleton_reflective`, the reflection proved NOT inert at the non-representative point (`probe_skel_not_inert`, by `discriminate`), and the swap witness above — both over `Indiscrete bool`, hom and proof at the literal `Set`; `make todo` grows by 11, all in the probe. NOT delivered: the identity counit and the two identifications above; Exercise 4 of the same page (a left-adjoint-left-inverse of `G` makes `G` an isomorphism onto a reflective subcategory), a separate catalog item, cited; the converse (a reflective subcategory with a componentwise-invertible unit is iso-dense); naturality of the choice in any variable; uniqueness of the reflector; anything about `Coreflective`; the optional `Proset_Skeleton` witness; and nothing registered as an `Instance`, deliberately, `EssentiallySurjective` being a class through which a chosen preimage must not become globally resolvable
- **Theory/Morphisms.v**: The monic/epi calculus — `Monic`/`Epic`/`Bimorphic`, closure under composition (`monic_compose`/`epi_compose`) and the cancellation half (`epic_cancel : Epic (f ∘ g) → Epic f`, `monic_cancel : Monic (f ∘ g) → Monic g`; Riehl §1.2 Lemma 1.2.11, Awodey §2.9 Ex 2(b)/(c)). Cancellation is one-sided and the failure is witnessed, not asserted: `sets_epic_left_factor_only` / `sets_monic_right_factor_only` (Instance/Sets.v) refute both converses with a single pair of setoid maps through the singleton. `Bimorphic` is inhabited but strictly weaker than invertibility — `two_bimorphic_not_iso` (Instance/Two.v) exhibits the walking arrow's non-identity morphism as monic and epic with no inverse, so `2` is not balanced — in genuine contrast to `Sets`, which IS balanced in tree via `Sets_balanced` (`Instance/Sets.v`), assembled from `injectivity_is_monic` and `epic_implies_surjective` through `bijective_is_iso`. Riehl's dual-derivation discipline lives in Theory/Morphisms/Duality.v (`Monic_of_op_Epic` and the three siblings, then `monic_cancel_op`/`monic_compose_op` re-derived from the epi forms with no second argument); it cannot live in Morphisms.v itself, since `Construction/Opposite.v` requires `Theory/Isomorphism.v` which requires `Theory/Morphisms.v` — the header records the cycle. Riehl §1.2's wide subcategories are there too — `MonoSub`/`EpiSub` with their `Wide` witnesses, `EpiSub` transported from `MonoSub (C^op)` rather than reproved. Downstream, `sub_le_monic` (Theory/Subobject.v) is Awodey §5.1: a factorization between subobjects is itself monic. Mac Lane §I.5's split-morphism half rides the same file: `split_pair_idempotent : g ∘ h ≈ id → Idempotent (h ∘ g)` discharges what the `SplitIdempotent` class had asserted in header prose only (`split_idem_Idempotent`), and `RegularMorphism f := ∃ g, f ∘ g ∘ f ≈ f` — von Neumann regularity, §I.5 Ex 7 — sits under both one-sided notions: `regular_of_section`/`regular_of_retraction` down, `regular_epic_retraction`/`regular_monic_section` back up under a cancellation hypothesis, with both composites of a pseudoinverse idempotent (`regular_composites_idempotent`). Witnesses are DATA (`∃` is `sigT`), so those four are `Defined`, not `Qed`. Ex 7's Set half is delivered CONSTRUCTIVELY over FinSet and REFUTED as a blanket principle over `Sets`. `finset_regular` (Instance/FinSet/Regular.v) builds the pseudoinverse by an executable finite search over `Fin.t` (`fin_preimage`, reducing by `eq_refl` on closed input); its inhabited-domain hypothesis is SHARP — `finset_empty_to_one_not_regular` refutes regularity for the unique arrow 0 → 1 — yet costs nothing for splitting, since an epi out of the empty object forces an empty codomain and so `finset_every_epi_splits` holds for EVERY arrow of FinSet with no hypothesis at all. Over `Sets` the blanket statement is not a choice principle but a strictly stronger one, and Instance/Sets/Regular.v proves it axiom-free: `blanket_regularity_entails_LEM` derives `∀ P : Prop, P + (P → False)` from "every arrow with inhabited domain is regular", and `blanket_splitting_entails_LEM` derives the same from the weaker internal choice principle "every epi with inhabited domain splits", by a Diaconescu-shaped countermodel (bool coarsened by P) for which regularity is EXACTLY decidability (`sets_coarsen_regular_iff_dec`) and non-regularity is refutable outright (`sets_coarsen_not_regular_absurd`). Coq's own choice axioms do not decide propositions — `Stdlib.Logic.ClassicalChoice` gets excluded middle by re-exporting `Classical`, and it is EXTENSIONAL (setoid) choice that Carlström's decomposition prices at `EM + Ext + AC_int`, per `Stdlib.Logic.SetoidChoice` — so choice is what the CLASSICAL argument spends, not what the principle costs here. The implication between the two quantified principles is `blanket_regularity_entails_splitting`, a Coq statement rather than prose, and composing it with `blanket_splitting_entails_LEM` re-proves the regularity result. No `Axiom` anywhere. Non-vacuity is proved rather than gestured at: `finset_regular_not_split` is regular but neither a section, a retraction, nor an iso; `finset_split_pair_nontrivial` is a split pair whose other composite is provably not an identity; and `two_epic_not_regular` (Instance/Two.v) is an epi of the walking arrow that neither splits nor is regular — the in-tree stand-in for Mac Lane's Grp aside, this tree having no category of groups. (It has categories of algebras — `Models T C`, `OperadAlgebras`, `FAlg F`, `Algs`, `CMon` — but none instantiated at the group signature.) Both refutations of regularity work by an EMPTY reverse hom-set, and that shape is forced rather than convenient: `sets_coarsen_not_regular_absurd` exhibits an arrow of `Sets` with inhabited domain AND inhabited reverse hom-set whose non-regularity is refutable outright. Fong–Spivak §3.2.5 Ex 3.34 is Instance/Sets/Split.v: explicit maps between a two- and a three-element setoid, with `sets_split_mono_not_iso`/`sets_split_epi_not_iso` making the `IsoClass ⊆ SplitMonoClass`/`SplitEpiClass` inclusions of Theory/Morphisms/Classes.v strict — the tree's first `IsIsomorphism` refutations in `Sets`; the `Section`/`Retraction` refutations are elsewhere, `finset_shift3_not_section`/`finset_shift3_not_retraction` in Instance/FinSet/Regular.v and `TwoXY_not_section`/`TwoXY_not_retraction` in Instance/Two.v. `sets_two_card`/`sets_three_card` pin the two carriers' counts by exhaustiveness AND distinctness, the objects being discrete so that `=` is their `≈`
- **Theory/Morphisms/CokernelPair.v**, **Instance/Sets/CokernelPair.v**: Mac Lane §III.3 def 7 + §III.4 Ex 4 + Seven Sketches §7.2.1 Def 7.5 — the cokernel pair, and epimorphism CHARACTERIZED by a pushout square. `cokernel_pair f : IsPushout f f` under `HasPushouts` (the `kernel_pair` pattern of `Structure/Regular.v` dualized), the apex-pinned `IsCokernelPair`, and the headline biconditionals **`epic_iff_pushout_square`** (f is `Epic` iff the square on f with both legs `id` on the codomain is a pushout — Seven Sketches takes exactly this as the DEFINITION of epi) and `epic_iff_cokernel_pair_trivial`, with the dual **`monic_iff_pullback_square`** derived through `C^op`; the mono half of Seven Sketches 7.5 proper belongs to **#672** and is cross-referenced rather than claimed. **THE ENABLING PIECE IS A NEW PREDICATE, AND THE NAME COLLISION IS PROVED RATHER THAN ASSERTED**: `Structure/Pushout.v`'s `IsPushout f g := @Pullback (C^op) y z x f g` is the BUNDLED notion despite its `Is` prefix — it carries its apex as DATA through `Structure/Pullback.v`'s `Pull : C` field (in the `Record Pullback`) — so it cannot say that a GIVEN square is a pushout, which is what Mac Lane's characterization requires; `Fail Check (IsPushout f f y id id)` is pinned as a probe and fails with **`Illegal application (Non-functional construction)`**, a TYPING error rather than a universe one, against passing controls at both `IsPushoutSquare f f y id id` and `IsPushout f f`. Hence `IsPushoutSquare {x y z} f g P i1 i2 := @IsPullback (C^op) y z x f g P i1 i2` — the apex-pinned form, dualizing `Theory/Morphisms/Stability.v`'s `IsPullback`, whose own header already records why the bundled record could not assert it. Both field readings, the C-facing `Build_IsPushoutSquare`, and BOTH conversions with the bundled `IsPushout` are `:=` with no tactic, the bundled→pinned→bundled round trip closing at `eq_refl` on the WHOLE record. **A PRIOR-ART CORRECTION, AND THE REAL GAP IS SHARPER THAN THE ISSUE'S**: the issue asserts `rg 'cokernel.?pair|CokernelPair'` returns zero hits; it does not — six `.v` files match (25 files tree-wide), and THREE are genuine constructions with legs, each built to prove epi ⟹ surjective in its own category (`Instance/Sets.v`, `Instance/Top.v` including `Definition CokernelPair : TopSpace`, `Instance/Sets/Pointed.v`). What is actually absent is stronger than "never related to a pushout": **none of the three has a universal property at all** — each supplies apex, two legs and `u ∘ f ≈ v ∘ f` and stops — and the word "pushout" occurs nowhere in `Instance/Top.v` or `Instance/Sets/Pointed.v` and only once, unrelatedly, in `Instance/Sets.v`. **`Instance/Sets/CokernelPair.v` closes that for the `Sets` one at the STRONGEST available grade**: `sets_ck_IsCokernelPair` proves `Instance/Sets.v`'s OWN `CKSetoid`/`ck_left`/`ck_right` to BE a cokernel pair, those constants appearing literally as the apex and the two legs of the apex-pinned predicate and the commuting field being that file's own `ck_agree` reused — so this is an identification ON THE NOSE, not an "agrees with", not an isomorphism, and no comparison map appears anywhere in the statement; what is added is the universal property the construction never had. The two triangles hold POINTWISE at Leibniz `=` on the underlying functions (`sets_ck_med_inl`/`_inr` are `eq_refl` Examples) but only at `≈` as MORPHISMS, and the cause is diagnosed rather than left open — a control shows the two sides agree at `eq_refl` pointwise, so what differs is exactly the rebuilt `proper_morphism` certificate. Constructive throughout: `∃` is `sigT`, so the image witness is DATA and the one step with content (respectfulness at the gluing case) consumes no choice principle. Non-vacuity runs BOTH ways over that file's own witnesses — `collapse` is epic so its identity square IS a pushout, `pick_true` is not, with its two legs proved DISTINCT by hand — and `sets_pick_true_not_epic_via_ckp` re-derives `Instance/Sets.v`'s own `pick_true_not_epic` through the general theorem by a route sharing no proof text with it. Universes measured in the constraint blocks with the two levels declared SEPARATELY (an unannotated probe collapses them by minimization and reads as an identification): hom and proof ARE identified — `epic_iff_pushout_square@{u u0 u1}` is over `C : Category@{u u0 u0}` — but that is the DONORS' doing and is guarded, `Epic` ALONE being rejected at a section declaring `vh < vp` with `Check (x ~> y)` and `Check f` succeeding there as controls; the OBJECT universe is only BOUNDED (`u <= u1`), never identified, and nothing in these files adds to the identification. **The file placement departs from the issue's suggestion for a measured reason** (its own text permits "or a sibling"): `Structure/Pushout.v` Requires only Lib, Category, Isomorphism, Opposite and Pullback — none of `Theory/Morphisms.v`, `Stability.v` or `Duality.v`, all three of which this development needs — so extending it in place would push those three plus `Construction/Subcategory.v` (which `Duality.v` drags in) onto every downstream consumer of pushouts. Nine negatives against fourteen controls, the kinds genuinely distinct and kept lexically apart — one TYPING (the name-collision probe above), five FORMABILITY (universe, at `vh < vp`, one per donor: `Epic`, `Pullback`, `IsPullback`, `IsPushout`, and the inherited `IsPushoutSquare`), three CONVERSION — each stripped once and its failure kind read off the message. 61/61 gated constants closed under the global context, including the single `Program` obligation, which is gated by its fully-qualified name. NOT delivered IN THIS FILE, scoped: no functoriality of the cokernel pair on the arrow category — that is `Adjunction/CokernelPair.v` (#364), which CONSUMES this file's `cokernel_pair f : IsPushout f f` and its `ckp_*` accessors exactly as that issue's remediation note asks, so the division of labour is now the record rather than a gap; no relation to `RegularEpi` or `Structure/Regular.v`'s `regular_coeq`; no new `HasPushouts` instance; the `Top` and pointed-sets cokernel pairs are NOT identified with the API and nothing is claimed about whether they are pushouts; no general uniqueness-up-to-unique-iso for cokernel pairs (it would be `pullback_transport` read at `C^op`, not performed — `epic_iff_cokernel_pair_left_iso` gives an iso only in the epic case); and `CKSetoid f` is NOT identified with the chosen `Sets_HasPushouts` pushout, the two being a closed-form relation and a five-constructor inductive closure respectively, with no claim made either way
- **Theory/Bicategory.v**: Bicategories (weak 2-categories) — the full class over hom-categories `bicat x y`, with horizontal composition `hcompose` as a bifunctor, the Godement product `hcomp2 := fmap[hcompose]`, left/right unitors and associator as 2-isomorphisms with to-direction naturality, and the triangle/pentagon coherence laws (every field mirroring Structure/Monoidal.v under delooping: tensor↔hcompose, I↔bi1id), plus the smart constructor `Build_Bicategory'`; pseudofunctors with identity/composite and the associativity hexagon (Theory/Bicategory/Pseudofunctor.v); lax/oplax transformations and the pseudonatural mixin per Johnson–Yau (Theory/Bicategory/Lax.v); modifications and the category of lax transformations (Theory/Bicategory/Modification.v); adjunctions in a bicategory with both swallowtail triangles, uniqueness of adjoints up to invertible 2-cell (`adjoint_unique`), and the general Kelly unit coincidence `λ_I ≈ ρ_I` derived from pentagon+triangle (Theory/Bicategory/Adjunction.v); the mates correspondence `mate`/`mate_iso`/`mate_roundtrip_{left,right}` as a bijection of 2-cell setoids in Sets, factored through the precomp/postcomp hom-adjunctions with both round-trips from the zig-zag identities (Theory/Bicategory/Mates.v — functoriality under pasting is ledger 10, narrowed but not discharged: the identity-bounding-cell case in ordinary-CT vocabulary is Adjunction/Conjugate.v, the pasting of adjoint squares Adjunction/Square.v, and the bicategory of categories, adjunctions and conjugate pairs Instance/Adj/Bicategory.v); a monoidal category as a one-object bicategory by direct delooping (Theory/Bicategory/OneObject.v). Cat is the motivating bicategory with `bicat C D ≡ [C, D]` definitionally (Instance/Cat/Bicategory.v, raw `Build_Bicategory` so record-eta holds, reconciling Fun.v's reversed unitor names), where a bicategorical adjunction coincides with `F ∹ U` hence `F ⊣ U`, precomposition is the `⌊−⌋` transpose, and the general mate unfolds to the Kelly–Street formula `Cat_mate_unfold` (Instance/Cat/Bicategory/Adjunction.v)
- **Theory/TwoCategory.v**: Strict 2-categories and double categories (Mac Lane §II.5 Theorem 1 + defs 2-3, pp. 43-44; §XII.3 def 1; Riehl §1.7 Definition 1.7.8) — BOTH presentations. The GLOBULAR `Class TwoCategory` (§XII.3/Riehl field for field: `tcat` + parallel-pair 2-cells + vertical composition with `thom` the hom-category as a construction + `thcomp`/`thcomp_id`/`tinterchange` + whiskering with Mac Lane's display-(3) factorizations and `tgodement_square`) carries THREE BOUNDARY IDENTIFICATIONS AS DATA (`tassoc_cast`/`tunitl_cast`/`tunitr_cast` with identity-behavior laws) — because `(H ◯ G) ◯ F` and `H ◯ (G ◯ F)` are NOT convertible in this library (measured: the law fields are distinct opaque terms and `≈` is crelation-valued), so no naive on-the-nose class hosts Cat; a general ≈-cast is deliberately NOT a field (its proof-irrelevance at Cat is exactly object-UIP). The ARROWS-ONLY half is Mac Lane's own §II.5 wording via the Metacategory precedent REUSED: `MetaComp` (Metacategory data over a fixed setoid, bridges both ways), `StrictDoubleCategory` (one cell setoid, two `MetaComp`s, partial-operation interchange per his remark 2), `StrictTwoCategory` (def 3 verbatim: horizontal identities are vertical identities), with Theorem 1's "two different categories" literal (`dvert_Category`/`dhoriz_Category` through `Category_from_Metacategory`). **The presentation passage is a THEOREM under an explicit pack** (`StrictBase`, the ToArrows idiom one dimension up): obj_uip + hom_uip + the 1-cell layer STRICT as Leibniz equations + the three casts-are-transports compatibilities — the strictness group is load-bearing (without it the bundled horizontal unit law is false: `thcomp α (tid2 id)` lands at `(f∘id, g∘id)` with no Leibniz path — Mac Lane's defs presuppose a strict 1-cell layer, the globular class was engineered for the non-strict Cat); `TwoCategory_to_Strict` bundles via `TwoArr`/`twoarr_crush` (Arr one dimension up) with def 3 PROVED (`TwoArr_hident_is_vident`, by uniqueness of composites), comparison lemma family (bundling faithful/surjective, compositions read back as `tvcomp`/`thcomp`), SATISFIABILITY witnessed (`NatPlus_StrictBase` — the chaotic 2-category on `Deloop Nat_Plus`, UIP by axiom-free `UIP_dec`, non-collapse proved) AND the pack's non-inhabitability at Cat disclosed as UNDERIVABILITY, not a refutation theorem (no Leibniz path for `sb_assoc` is derivable there — the same limitation `ToArrows` carries; the identity type is not proved empty); converse ledgered with its two-dimensional GERM proved (`dvert_unit_hcomp` from one `dinterchange` instantiation — the composite of vertical units acts as a unit on the given composite, one instance; full `mcident`-hood not claimed), the residue scoped as the interlocked sub-setoid constructions plus that upgrade. Mac Lane's NEGATIVE example proved: `NatSq_Double` (commuting squares over `Deloop Nat_Plus`) is a `StrictDoubleCategory` that is NOT a 2-category (`NatSq_not_a_two_category`), non-degenerately (`NatSq_vid_is_not_hid`)
- **Instance/Cat/TwoCategory.v**: Mac Lane §II.5 Theorem 1's witness — **`Cat_TwoCategory : TwoCategory`** with tcat := StrictCat, 2-cells := `Transform`, vertical := `nat_compose` ([C,D]'s own laws by name), horizontal := `nat_hcompose`, and interchange REUSED: `Cat_tinterchange` is literally `Cat_Hcompose`'s `fmap_comp` obligation applied (Instance/Cat/Bicategory.v), not re-proved; the boundary identifications pass the `transform` field through unchanged (recorded by eq_refl — re-bracketing identifies the 2-CELL types since `Transform` mentions only fobj/fmap). The weak/strict reconciliation: every coherence cell of `Cat_Bicategory` has IDENTITY components (`Cat_bicat_hassoc_id` + five siblings, the `_is_nat_*` identifications by eq_refl). Sq tie-in: the negative example's cells ARE `Sq (Deloop Nat_Plus)`'s squares definitionally, `Sq_hid` supplying the horizontal-identity square Theory/DoubleCategory.v's SCOPE note omits. Concrete witness: `NatBase_centre` (§II.5 Ex 8, the centre of the delooping) with BOTH composites computing to m+n by eq_refl — the Eckmann-Hilton collapse (Ex 5) as a computation
- **Theory/DoubleCategory.v**: Pseudo double categories — strict vertical category, weak horizontal composition; squares `dsq h u v k` form setoids with the boundary-coercion calculus `dsq_coerce` (groupoid laws + derived lemma pack — the Phase-10 displayed `dtransport` pattern re-applied), strict vertical laws stated through coercion, horizontal pasting with `dinterchange`, and invertible GLOBULAR `dassoc`/`dunit_left`/`dunit_right` (sigma packaging, named accessors) under triangle+pentagon coherence at square level; header discloses the coherence-only scope (horizontal identity squares on verticals, unit-interchange, and coherence naturality are not required — both models satisfy them) and the associator orientation (opposite to Bicategory's `hassoc`). Companions and conjoints with the vertical zigzag in its standard representability form (`companion_name`/`companion_eval` transposition bijection) and uniqueness up to canonical invertible globular square under the `DoubleCoerceComp` mixin (coercion-vs-composition interchange, mirroring Displayed's `dtransport_comp_l/r`, underivable from the class) in Theory/DoubleCategory/Companion.v. Models: the commuting-squares double category `Sq C` where `dsq h u v k := (k ∘ u ≈ v ∘ h)` is a proposition, every morphism is its own companion, and conjoints exist EXACTLY for isomorphisms (`Sq_companion`, `Sq_conjoint`, `Sq_conjoint_iso` — a plan erratum corrected to the standard characterization; Construction/Sq.v); and the cospans double category over chosen pushouts with genuinely proof-relevant squares, unitors/associator from the pushout UMP, and the FULL pentagon proved via the single pasting principle `pushout_jointly_epic` (Construction/Cospan/Double.v)
- **Structure/Topos.v**: Topos theory — subobjects as a setoid whose equivalence IS the quotient of monos, with the factorization preorder and equivalence-iff-mutual-factorization (Theory/Subobject.v); the `Sub : C^op ⟶ Sets` functor by chosen-pullback reindexing, functorial via the Phase-8 pasting toolkit (Theory/Subobject/Functor.v); `SubobjectClassifier` (Ω, monic `truth`, `char` with the classifying `IsPullback` square and uniqueness; `char_respects` derived as a lemma rather than required as a redundant field) with the classification theorem `classifier_classifies : SubObj x ≅ (x ~> Ω)` in Sets (Structure/SubobjectClassifier.v); `ElementaryTopos` carrying finite limits explicitly (terminal+products+pullbacks — a redundancy that is now a CHOICE rather than a gap, the reduction and its converse being proved in Structure/Pullback/Reduction.v; the class keeps its components explicit so an instance may supply whichever it computes with), whose equalizers are now DERIVED for every topos as Structure/Topos/Monadic.v's `topos_HasEqualizers`, with finite completeness stated there at generator level as `topos_finitely_complete` and finite COLIMITS at Structure/Topos/Colimits.v's `topos_has_finite_colimits` (Paré, via the monadic power-object functor) — still no finite-shape induction anywhere, so a `Limit`-shaped "all finite limits" is claimed nowhere + the real Cartesian/Closed.v closure + a classifier, with derived power objects `Pow a := Ω ^ a` and the relations iso `SubObj (a × b) ≅ (a ~> Pow b)` (Structure/Topos.v). The computable witness: skeletal FinSet with positional product codecs (`fin_pair`/`fin_unpair`, Instance/FinSet/Product.v), enumeration exponential codecs (`fin_tabulate`/`fin_apply`, pointwise round trip — funext-free by design, Instance/FinSet/Closed.v), counted pullbacks + the decidable classifier `Ω := 2` with monos-are-injections (Instance/FinSet/Classifier.v), assembled as `FinSet_Topos` whose sanity Examples compute by `eq_refl` (`Pow 2 = 4`; Instance/FinSet/Topos.v). The Sets story is CROSS-UNIVERSE theorems at that file, and — since #404 — ALSO a conditional instance (`Instance/Sets/Topos.v`'s `Sets_Topos`, on `Untruncate`/`DecImage`/`IEM`; `classifier_classifies` and `relations_iso` stay refused there, so this file's own witness column is unchanged), the cross-universe route being what that file proves (the truth-value setoid lives one level up; `PropSetoid` under `iffT`, `char_setoid`, `sets_char_pullback` via a piecewise `Setoid_Lift` that provably avoids collapsing levels, one-level `sets_char_subobject` via `Sets_Image` (Instance/Sets/Image.v), `sets_char_unique`; Instance/Sets/Classifier.v — upgrading Instance/Sets.v's size-obstruction note) — with the qualification that a genuine one-level `SubobjectClassifier Sets` instance DOES exist CONDITIONALLY in Instance/Sets/Classifier/OneLevel.v (under `Untruncate` or `DecImage`, neither inhabited axiom-free in tree), whose characteristic map is the `Powerset_squash`-truncation of `char_setoid` at `eq_refl`, so the two files are one construction at two levels rather than a gap and a workaround; what remains unstatable at `Sets` is `classifier_classifies` itself, whose `u <= u0` puts objects at or below homs — a bound that a PRESHEAF category CAN satisfy, so at `[C^op, Sets]` the theorem is formable at an abstract classifier, refused at Instance/Fun/Classifier.v's minimized `Fun_Classifier`, and applies to that file's re-annotated `Fun_Classifier_small`, where it is `fun_classifier_classifies` (#403). Sheaves as the full subcategory of presheaves on the existing Sheaf predicate with full/faithful inclusion and repleteness `sheaf_iso_closed` — the header discloses precisely that the inherited predicate is per-leg and vacuous beyond subsingleton fibres (donor erratum; matching-family re-founding deferred with sheafification, ledger 1; Theory/Sheaf/Category.v)
- **Structure/Topos/Power.v**, **Structure/Topos/Monadic.v**, **Structure/Topos/Colimits.v**, **Theory/Equivalence/Pullback.v**: Mac Lane §IV.10 remark 2 (book p. 107, `maclane:IV.10:remark2`) + Appendix §App.1 (book p. 290, `maclane:App.1:remark2`) + Awodey §8.8 (printed p. 210, `awodey:8.8:remark-topos-properties`) + Seven Sketches §7.2.1 (printed p. 225, `7sketches:7.2.1:remark-topos-properties`) — **every elementary topos has finite colimits, by Paré's route: the power-object functor is self-adjoint on the right and MONADIC**, delivered as `power_object_monadic : Monadic PowF` and `topos_has_finite_colimits : @Initial C * @Cocartesian C * @HasCoequalizers C * @HasPushouts C` (both the issue's pinned names, verbatim), with the appended Seven Sketches checkbox as `topos_finitely_complete`. **THREE BOOKS STATE IT IN FOUR PLACES AND NONE PROVES IT** — Mac Lane's whole treatment is "The axioms for a topos have many useful consequences. For examples, every topos has all finite colimits", Awodey refers the reader to Mac Lane–Moerdijk, Seven Sketches says "we will not prove it in this book" — and Mac Lane–Moerdijk was NOT available, so the argument is RECONSTRUCTED and CHECKED BY COMPILATION, with the reconstruction disclosed as such in three headers. **STEP TWO CONSUMES NO SUBOBJECT CLASSIFIER, ONLY THE OBJECT Ω, AND THAT IS A UNIVERSE RESULT RATHER THAN A CONVENIENCE** — the issue routes the transposition `E(a,Ω^b) ≅ E(b,Ω^a)` through `Structure/Topos.v`'s `relations_iso` and hence through subobjects of `a × b`; the map is instead `Structure/Cartesian/Closed.v`'s pre-existing `flip f := curry (uncurry f ∘ swap)`, whose missing INVOLUTION lemma `flip_flip` is four rewrites (a whole-tree search returns only the list-reversal hypothesis `flip_involutive` of `Lib/TList.v` and `Lib/NETList.v` and `Theory/Morphisms.v`'s `flip_invol` about an `Involutive` endomorphism, none of them this `flip`; by shape, `flip_flip` is the only `flip (flip f)` statement in the tree), and the FOUR `AdjointOnTheRight` naturality fields collapse to TWO lemmas because `to` and `from` are one map (`to_aor_nat_a` and `from_aor_nat_x` are both `flip_comp`, `to_aor_nat_x` and `from_aor_nat_a` both `flip_pow_comp`) — while **packaging subobjects as an OBJECT of `Sets` IDENTIFIES C's object universe with its hom universe**, measured: `pow_relations` and `pow_aor_is_relations` (the fidelity checks that DO run through `relations_iso`, delivered at `≈` with the `eq_refl` form refuted) carry the block equation `u = u0` while `PowF`, `pow_aor`, `pow_self_adjoint`, `pow_adjunction`, `power_object_monadic` and `topos_has_finite_colimits` carry NO block equation at all, and at a topos whose objects are declared strictly BELOW its homs `relations_iso` and `pow_relations` are refused while `Pow`, `PowF`, `pow_aor`, the adjunction, the monadicity and the headline are all accepted (`SubObj` itself accepted too, so the identification is the `Sets`-packaging's and not the subobject record's). **THE SECOND UNIVERSE MEASUREMENT DECIDES THE WHOLE OF STEP 4, AND IT IS WHY NO `Limit` APPEARS**: `Terminal_Limit` (`Structure/Limit/Terminal.v`) and `Cartesian_Limit` (`Structure/Limit/Cartesian.v`) are stated over `Category@{u Set Set}`, pinning the ambient hom AND proof universes to the literal `Set`, so the `Limit` route would have confined the theorem to Set-homed toposes — both refusals pinned at a category whose hom-and-proof level is declared strictly above `Set`, with `Terminal_transport`, `crude_monadicity`, `Cartesian_of_HasPullbacks_Terminal`, `HasPullbacks_transport` AND `topos_has_finite_colimits` itself accepted at exactly those levels; measured, no `Cone`/`Limit`/`Colimit`/`Cocomplete` token occurs outside a comment in any of the four files, the sole exception being the module path `Category.Structure.Limit.Preservation` in one `Require` (it supplies `ReflectsIsos` and no limit vocabulary). **THE BECK MACHINERY IS CONSUMED, NOT REPROVED** — `Monad/Monadicity/Crude.v`'s `crude_monadicity` applied to three discharged hypotheses: `op_HasReflexiveCoequalizers` is three applications of pre-existing reductions (`HasEqualizers_of_HasPullbacks_Terminal` → `HasCoequalizers_of_HasEqualizers_op` at `C^op` → `HasCoequalizers_HasReflexiveCoequalizers`, the middle one typechecking because `(C^op)^op = C` by reflexivity); `PowF_ReflectsIsos` through `PowF_Faithful` (from `flip_pow_comp` at the singleton, using step two and NOT the adjunction machinery) and `topos_balanced`; and `pow_PreservesReflexiveCoequalizers`, the hard half, where the P-images of a co-reflexive pair and its equalizer form a genuine **SPLIT** coequalizer whose four `Structure/Coequalizer/Split.v` fields are discharged by `pow_fmap_comp`+`fork_eq`, `pow_ex_mono` twice, and `pow_bc` — Beck's precise theorem (`CreatesUSplitCoequalizers`) would ALSO apply since the pair is split rather than merely P-split, and is not taken because creation needs the comparison isomorphism and the reflection clause on top of the same data. **THE DIRECT IMAGE NEEDS NO IMAGE FACTORIZATION**, which is why the hard step is tractable: `ex_mono m M := curry (char_sub (sub_push (second m) (second_Monic m M) (pow_mem u)))` exists because `second m ∘ (membership mono)` is already a mono, so epi–mono factorization (Seven Sketches' clause 4) is neither built nor needed. **THE SCOUT'S PLAN FOR `pow_bc` WAS SIMPLIFIED BY MEASUREMENT**: its item (vii), an `ex_mono` composition law, is NOT needed — `pow_precompose` on one side and `curry_comp_l` on the other reduce both transposes to characteristic maps, `char_reindex` turns those into subobjects, and TWO applications of `bc_mono` (over `second_pullback` of `coreflexive_equalizer_pullback`, and over `IsPullback_sym` of the new hypothesis-free `first_second_pullback`) meet exactly at `mem_pow_square`. Step 4 is elementary throughout: `EM_Terminal` (the terminal object with action `one`, every law `one_unique`) and `EM_HasPullbacks` (the carrier pullback with the unique action making both projections algebra maps, both algebra laws checked leg by leg through `is_pullback_jointly_monic`, new in its general binary form — `fpb_jointly_monic` and `wide_pullback_jointly_monic` are the pointwise and wide analogues already in tree, beside `ran_jointly_monic` and `iprod_jointly_monic`) transported by `Terminal_transport` and the NEW `HasPullbacks_transport`, then `@Initial C` IS `@Terminal (C^op)`, `@Cocartesian C` IS `@Cartesian (C^op)`, and `HasCoequalizers_of_HasEqualizers_op`/`HasPushouts_of_HasPullbacks_op` close the rest. **Theory/Equivalence/Pullback.v is general and topos-free**: `IsPullback_transport` (the apex-pinned core: a pullback in C of the quasi-inverse image of a D-cospan becomes, after F and the counit corrections, a pullback in D) plus `HasPullbacks_transport`, spending counit naturality and invertibility for existence and `equivalence_unit_conjugate` plus `Equivalence_Inverse_Faithful` for uniqueness, with **NO triangle identity used** — an `EquivalenceOfCategories` carries two natural isomorphisms and no coherence between them, so none is available and the argument is arranged to avoid needing one; measured with the five new files excluded, a whole-tree search for the six `*_transport` names returns 8 lines, all in `Theory/Equivalence/Terminal.v`, naming `Terminal_transport`, `Initial_transport` and their two object readbacks — a NAME search: other structures ARE carried across an equivalence under other names (`Complete_equivalence_invariant`, `Cocomplete_equivalence_invariant`, `Transported_Monoidal`, `Transported_Adjunction`), so what is new is the pullback case and not the pattern. **WHAT WAS ABSENT, RE-MEASURED WITH THE NEW FILES EXCLUDED**: 119 case-insensitive hits for "balanced" across 18 files and **not one of those files mentions `ElementaryTopos`**; the name `RegularMono` occurs on exactly one line outside the five files, in `Adjunction/CokernelPair.v`, which says in terms there is no such class (the only other `.v` mentions of a regular monomorphism are two more prose lines of that file and one comment in `Theory/Morphisms.v`); the 4 `singleton_map` hits are two declarations and two uses in `Instance/Sets/Powerset.v`; `PowFunctor`, `PowF`, `Pow_Functor`, `pow_reflects_isos`, `pow_monadic`, `power_object` zero files each. **NON-VACUITY AT FinSet, AT THE STRENGTH THAT HOLDS AND NOT ONE GRADE HIGHER**: the FinSet section lives in `Test/ProbeToposColimits405.v` for a MEASURED reason (requiring `Instance/FinSet/Topos.v` costs Colimits.v +6 modules, 92 → 98 excluding self, above the +5 allowed), and delivers **`FinSet_derived_HasCoequalizers : HasCoequalizers FinSet`, the FIRST such inhabitant the tree has ever had** — no `.v` file declares a constant of that type, and the only two other `.v` hits for that phrase are prose notes in `Instance/Sets/Coequalizer.v` and `Instance/Sets/Coequalizer/Interconnect.v` recording its ABSENCE — plus agreement of the derived initial object, binary coproduct and pushout with FinSet's computable ones **up to `≅`**, the `eq_refl` form refused for the initial object and that one refusal pinned as a CONVERSION negative (the coproduct and pushout readbacks are delivered at `≅` only and no `eq_refl` form of them is attempted), its cause NOT isolated: the derived object is the quasi-inverse `Crude_Inverse` at `EM_Terminal`'s object (`topos_Initial_obj`, `eq_refl`), i.e. the reflexive coequalizer `Monad/Monadicity/Crude.v` chooses through `op_HasReflexiveCoequalizers` and `topos_HasEqualizers`, a chain that never consumes `PowF_ReflectsIsos` (`Crude_Inverse` takes no `ReflectsIsos` argument; `refl` enters Crude.v only at `crude_theta_iso` and one `Proof using`), and no experiment names the constant that blocks the reduction — a first draft blamed `PowF_ReflectsIsos` and `topos_balanced`, which the Guard refuted, the coproduct half through a `cartesian_unique_iso` built in the probe because **no product-uniqueness lemma of the shape `product_obj _ ≅ product_obj _` exists anywhere in the tree** (`Structure/Cartesian.v` carries only `ump_products`) — a correction to the plan — while `initial_unique` and `pullback_unique` DO exist and are consumed. **Universes measured off BOTH binder and block over all 112 constants**: every one of the 117 `Category@{…}` binder occurrences identifies hom with proof, inherited and not introduced here — at the 103 of the 106 topos-facing constants whose type carries an `ElementaryTopos` argument from `ElementaryTopos`, at the other three — `IsIso_of_op`, `op_IsIso_of` and `sub_push`, transparent `Definition`s that `Set Default Proof Using "All"` does not reach — from `Opposite` for the two `IsIso` permutations (`C^op` alone is refused at hom < proof while `IsIsomorphism` alone is accepted) and from `Monic`/`SubObj` for `sub_push`, each measured, and at the six topos-free `Theory/Equivalence/Pullback.v` constants from `IsPullback`/`HasPullbacks`, each of the three refused ALONE at hom and proof levels declared strictly apart with the hom-set and the identity accepted as controls (three FORMABILITY negatives in the probe; `Monic`, `Cartesian`, `Terminal`, `SubObj`, `Closed` and `SubobjectClassifier` are refused the same way, measured and not pinned, so read the donors as sufficient rather than independent), and `is_pullback_jointly_monic@{u u0}` is the binder/block trap in miniature, its constraint block LITERALLY EMPTY while its binder reads `Category@{u u0 u0}`; **ZERO word-bounded `Set` tokens anywhere in the whole `About` dump**; the OBJECT universe is FREE in every headline (`PowF@{u u0 u1 u2} : ∀ {C : Category@{u u0 u0}}, ElementaryTopos@{u1 u u0} C → Functor`, block all `<`/`<=`); and exactly 25 of the 112 blocks carry an equation — the four `relations_iso` fidelity constants (`u = u0`, object = hom), SIXTEEN Eilenberg–Moore pullback constants (`u3 = u5`, `u3 = u6`, auxiliary monad levels that appear in no binder — the whole `EMPullback` section, `EM_Pullback` itself included) and the five `Theory/Equivalence/Pullback.v` constants (`u0 = u2`, the two categories' hom-and-proof levels, automatic at `C^op`/`C`). **112/112 constants closed under the global context, ZERO `Axioms:` lines** (Power 29, Monadic 38, Colimits 39, Pullback 6, counted on flattened `Print Module`; the `.glob` sees only 101, the 11 `Program` obligations being invisible to it; no `Record`/`Class`/`Inductive` in any of the four, so no unlisted `Build_*`), all 112 in the `make print-assumptions` gate FULLY QUALIFIED as a new block of 9533 bytes, taking the gate's `Closed` count from 5159 to 5271 with the `Axioms:` line count unchanged at 1. Zero DECLARATION collisions over 125 names — the 112 `Print Module` names plus the probe's 13 declaration heads that enter the environment; the five names its refutation commands declare, which never do, are swept too, for 130 in all, and a first count said 121 because four names were run together in the name file — (criterion: no `.v` outside the five new files declares any of them; the whole-word `rg -l` sweep behind that, instrument `Full` = 97 files and `Monoid` = 94, returns exactly one file for three of the names — `Structure/Topos.v`, whose rewritten header cites `topos_HasEqualizers`, `topos_finitely_complete` and `topos_has_finite_colimits` as prose pointers written by this same change), and no declared name equals any of the 186 module short names the gate now loads into one scope (184 before this change; `Monadic` and `Colimits` are new). Closures excluding self: Power 63 (`Adjunction/Right` costs 28 of it and dominates), `Theory/Equivalence/Pullback` 25, Monadic 87, Colimits 92, probe 104. **Seven `Defined` tokens, EXACTLY FOUR load-bearing by flipping each ALONE to `Qed`** — `pow_split_coequalizer` (whose `scoeq_obj` must reduce to `Pow k` for `pow_PreservesReflexiveCoequalizers` to typecheck), `em_pb_fst`, `em_pb_snd` and `EM_Pullback`; `PowF_Faithful` and the two `EM_Terminal` data obligations flip with nothing breaking and are kept `Defined` by the data convention only. Twenty `eq_refl` statements (Power 9, Colimits 10, Pullback 1, Monadic 0), among them that both legs of `relations_iso` reduce on the nose to `curry ∘ char_sub` and to pulling truth back along the untransposed map — which the plan did not expect — that both transposes of the adjunction ARE `flip`, that its unit and counit are one and the same `flip id`, and that the pushout IS the transported pullback read in `C^op` with no second construction interposed. `Test/ProbeToposColimits405.v` mirrors the union of the four `Require` lists (all 44, `Adjunction/Right` included) plus `Structure/Limit/{Terminal,Cartesian}`, `Structure/Cartesian/Closed/Adjunction`, `Instance/FinSet{,/Topos,/Pushout}` and `Structure/Topos/Colimits` itself, and carries **13 refutation commands = 1 instrument + 12 negatives of THREE kinds told apart by the error TEXT** — 7 FORMABILITY (`Terminal_Limit` and `Cartesian_Limit` above `Set`; `relations_iso` and `pow_relations` at object < hom; `ElementaryTopos`, `IsPullback` and `HasPullbacks` each alone at hom < proof, the hom-set and the identity accepted), 2 TYPING (`Partial_r (InternalHomFunctor C) Ω` ascribed at `C^op ⟶ C`, a plain has-type mismatch with no `cannot unify` and no universe clause; the swapped-handedness `PowF ⊣ Opposite_Functor PowF`, which is a well-formed TYPE and merely the wrong one), 3 CONVERSION (`Exp_Functor`'s object action is `(−)^Ω` and not `Ω^(−)`; the fidelity lemma at `eq_refl`; the FinSet initial readback at `eq_refl`) — each stripped ONE AT A TIME, compiled alone and its whole error read, guard coverage measured mechanically (50 identifiers inside refutation commands, 43 also outside, the 7 exceptions exhaustively the keyword `Fail`, the five `Fail`-declared names that never enter the environment, and the instrument's absent name), rename-simulated 5/5 over the target constants the negatives name (`PowF`, `pow_adjunction`, `pow_aor`, `pow_relations`, `topos_Initial`), each rename applied in a shadow tree that rebuilds all three library files and each breaking the probe at a `Check` or `Example` control line, never inside a `Fail`. `make todo` grows by 13, ALL in the probe (2001 → 2014, counting the lines that begin `./`); the four library files contribute ZERO, so the issue's "adds no new hits" box is NOT met as written; no header records that, and this bullet, the commit message and the PR are where it is disclosed. Two engineering hazards are recorded where they bite: `Category.Functor.Opposite` opens `functor_scope` and `Category.Theory.Monad` carries it in by requiring that module, after which a bare `C^op` on a CATEGORY parses as `Opposite_Functor C` and dies naming neither culprit (the four files that require either of them reopen `category_scope` after their `Require` block; `Theory/Equivalence/Pullback.v` requires neither and needs no guard), and a `{| t_alg := … |}` record literal needs `Category.Monad.Algebra` REQUIRED EXPLICITLY, requiring only `Category.Monad.Eilenberg.Moore` giving "t_alg: Not a projection"; a third is new here — `Program` and `simpl` unfold `fmap[PowF]` to its `curry` normal form, after which neither `fmap_comp` nor `pow_ex_mono` matches, so `pow_split_coequalizer` is built with `unshelve refine` rather than `Program`; `EM_Pullback` is too, for the different reason its own comment gives — `Program`'s obligation numbering would leave the competing cone and its witness unnamed. §D's `IsPullback_sym`, `second_pullback`, `first_second_pullback` and `bc_mono` use no classifier (the first mentions only `IsPullback`, the next two only the cartesian structure, `bc_mono` only chosen pullbacks and `SubObj`) yet all four carry an `ElementaryTopos C` argument, because Monadic.v's section-wide `Set Default Proof Using "All"` discharges the whole context into every `Qed`-closed lemma; they belong beside `pullback_paste` in `Theory/Morphisms/Stability.v` (`bc_mono` beside `sub_reindex` in `Theory/Subobject/Functor.v`), are kept in Monadic.v to avoid a wide rebuild, and the header records the debt and its cause. NOT delivered: no `Cocomplete` and no infinite colimits; no `Limit`/`Colimit`-shaped statement and no finite-shape induction, so BOTH the colimit theorem and `topos_finitely_complete` are at GENERATOR level and a `Limit K`-for-every-finite-shape claim is still made nowhere in this library; no image factorization and no direct image along a general arrow; no Beck's-precise-theorem route; no naturality of any identification, and none of the fidelity lemma in `a` or `b`; no `eq_refl` FinSet readback (measured, not assumed); no second concrete topos; nothing about slices of a topos (Awodey's other half) nor about epi–mono factorization (Seven Sketches' clause 4); no statement that F PRESERVES pullbacks in `Theory/Equivalence/Pullback.v`, and no equalizer, product or general-limit transport, nor a dual; and no constant of the three `Structure/Topos/` files is exported as a resolvable colimit — the delivered constants are plain `Definition`s (with `Example`s, `Lemma`s and one `Program Definition` beside them), and the three `Instance` declarations are `#[local] Program Instance flip_respects` (Power.v) and the `#[export]` `PowF_Faithful` and `PowF_ReflectsIsos` (Monadic.v), properties of one named functor, plus a `#[local] Existing Instance pow_monad` inside Colimits.v's section; the two exported ones are `#[export]` so that `Faithful PowF` and `ReflectsIsos PowF` stay resolvable downstream (measured: both resolve, and `pow_monad` does not leak) — NOT for the `print-assumptions` gate, which reaches a `#[local] Instance` by qualified name just as well: the same block gates the `#[local] flip_respects`, Closed, and `PowF_Faithful` declared `#[local]` still compiles Monadic.v, Colimits.v and the probe and is still gate-reachable (measured)
- **Structure/SubobjectClassifier/Natural.v**: Awodey §5.3 Example 5.14(v) — the classification correspondence upgraded from a per-object bijection to a NATURAL isomorphism, `Sub_classifier_natural : @Isomorphism ([C^op, Sets]) (Sub C HP) (Curried_CoHom C Ω)` for an ARBITRARY `SubobjectClassifier`. Awodey's point is explicitly that naturality is strictly stronger than an object-wise bijection, so this is not a repackaging of `classifier_classifies` (`Structure/SubobjectClassifier.v`) — but it IS an upgrade rather than a parallel construction, and that is machine-checked: both components ARE `classifier_classifies`'s legs by `eq_refl`. The square's content is `char_reindex : char_sub (sub_reindex f s) ≈ char_sub s ∘ f`, proved from the two existing round trips plus `sub_reindex_comp` (`Theory/Subobject/Functor.v`) — which IS the pullback pasting lemma, the argument Awodey runs. Payoff the per-object statement could not give: **`Sub_Representable`** — the subobject presheaf is REPRESENTABLE, represented by Ω. That naturality is strictly stronger is NOT reproved here (it is a fact about families, and the tree already carries witnesses — `Instance/Ab/Character/NonNatural.v`, `Instance/FdVect/NonNatural.v` — cited rather than restated)
- **Theory/Universal/Element.v**: Mac Lane §III.1 def 2 + remark 3 — UNIVERSAL ELEMENTS as a first-class structure, replacing the anonymous sigma `{ x : F c & IsIsomorphism (from (Yoneda_Lemma C F c) x) }` that `Structure/UniversalProperty.v`'s `representability_by_yoneda` had buried inside one proposition. `UniversalElement H` (bundled) and `AUniversalElement H r` (object as parameter) carry Mac Lane's elementary unique-factorization form, with setoids comparing the underlying element. Deliverable 2 is stated at the strength it actually has: **an isomorphism of setoids in `Sets`** (`universal_element_yoneda`), not a biconditional and not mutually inverse maps up to `≈` — and it is free of transport because `ue_yoneda_obj` is `representability_by_yoneda`'s source TERM FOR TERM (the same `#[local] exists_setoid` constant, reachable by qualified name and re-registered locally; `rby_agrees` checks the match by ascription). Had a copy been re-declared instead, the two `SetoidObject`s would not have been convertible and a hand-built comparison iso would have been needed — worth knowing for anyone else composing with that proposition. The equivalence's grades are measured, not assumed: the mate's ACTION is definitional (`ue_mate_at` by `reflexivity`, so Mac Lane's `(H k) e` and the Yoneda mate's component are the same term), the passage OUT of the Yoneda encoding costs 2 obligations, and the passage IN costs 5 (the inverse must be built as a natural transformation, and each naturality square is a uniqueness argument through `fmap_comp`) — so the cheap direction is out, not in. Round trips survive on the ELEMENT by `eq_refl` both ways; the whole record does not (factorization data is rebuilt), pinned as a `Fail`. Mac Lane's remark 3 is ONE theorem, `universal_element_arrow_subsumption`, carrying both halves — (a) universal elements of `H` are universal arrows from the one-point setoid `SetsOne`, and (b) `⟨r,u⟩` is a universal arrow from `c` to `S` iff it is a universal element of `d ↦ Hom(c, S d)` — with `subsumption_composite` the round trip that exists only once both halves do. Half (b) is much the cheaper: the two records' field types are CONVERTIBLE (`fmap[[Hom c,─] ◯ S] k u` IS `fmap[S] k ∘ u`), so both passages are `:=` with no tactic and both round trips are `eq_refl` on the WHOLE record; half (a) reaches only `≈` on the arrow side, `global_element` rebuilding the map from its value at `ttt`. **A DONOR UNIVERSE RESTRICTION drove the design and is disclosed rather than worked around silently**: `Yoneda_Lemma@{u u0}` and `representability_by_yoneda@{u u0}` are stated over `C : Category@{u0 u0 u0}` — object, hom AND proof universes IDENTIFIED — so neither can be instantiated at `Instance/Coq/Nat.v`'s `Endos`, whose objects sit strictly above its homs, and the Nat witness would not have existed by that route. A hand-built Yoneda-free route (`ue_transform`/`ue_representation`/`AUniversalElement_of_repr` over the single computation `repr_eval`) carries every accessor and both `Representable` passages instead, with `routes_agree_elem` (`eq_refl`) and `routes_agree_repr` (`≈`) showing the choice costs nothing where both typecheck; three `Fail` probes pin the restriction as a genuine universe inconsistency, each against a positive control at the same arguments. The restriction is the DONOR's, not introduced here, and is NOT shown unavoidable. Payoff: `nat_UniversalElement` recovers `Instance/Coq/Nat.v`'s hand-checked universal element through the general class — `ue_obj = NatSucc` and `ue_elem = O` both by `eq_refl`, at the very category the Yoneda route cannot reach — and — read the attribution precisely — the statement that generalizes it is **`ue_of_repr_elem`**, over the DIRECT route with the object universe free. `universal_element_of_representation_at` does NOT: it goes through Yoneda, inherits the constraint `u = u0`, and is provably not instantiable at `Endos`, so it is a second instance of the restriction rather than an escape from it. Theory/Universal/Element/Elements.v adds Riehl 2.4.8 (`nat_Elements_Initial`: a universal element is an initial object of `Construction/Elements.v`'s category of elements), recorded on the data rather than as a setoid iso since `Initial` carries a chosen `one` field with no in-tree setoid on it. Deferred and disclosed: no dual (couniversal element / terminal object of elements); no functoriality of `UniversalElement`, no comparison with `Elements_proj`; **`Elements Endos_Forget ≃ FAlg NatF` is NOT built**, so Nat.v's `repr_initial` is neither re-derived nor made redundant — the two remain independent, and Nat.v's own stale "the tree does not carry" clause is corrected in the same commit. Prior art disclosed rather than implied away: `Structure/UniversalProperty/Universal/Arrow.v`'s `UniversalArrowIsUniversalProperty` already builds, for `AUniversalArrow c U a`, an `IsUniversalProperty` whose representing functor is the same composite `HomAfter` names — so half (b)'s CONTENT existed in the representability form, and what is new is the elementary statement and the direct passage. Two engineering findings worth carrying: a `Sets`-morphism out of the CONCRETE `SetsOne` whose `proper_morphism` is left to INSTANCE RESOLUTION — whether by `Program`'s obligation tactic or by writing `reflexive_proper` out by hand — resolves `Reflexive` at `SetsOne` and thereby pins `Sets`'s carrier universe to `Set`, making the whole of 3(a) unformable; writing the pointwise term `fun a b _ => reflexivity x` avoids the resolution and keeps it polymorphic. **It is NOT `Program`** (the same definition with a variable source stays free under `Program`), **NOT "supply the field as a term"** (`reflexive_proper _` is such a term and is pinned — only that particular pointwise term works), and **NOT `Universe Minimization ToSet`** (identical with the flag on and off). The exposure is therefore this narrow shape rather than a tree-wide hazard — a conclusion carried by the not-`Program` negative above, not by sampling; an audit sweep of 88 `Program` constants across seven `Instance/Sets*`/`Instance/Top*` modules independently found zero others pinned, which is corroboration on 7 of ~566 files and could not establish it alone, and `AUniversalElement`'s universal field takes `(d : D) (x : H d)` EXPLICITLY where `AUniversalArrow` has them implicit — deliberate, since every use site supplies both
- **Theory/Universal/Arrow/Dual.v**: Mac Lane §III.1 def 3 — COUNIVERSAL arrows, the counit-side mirror of `Theory/Universal/Arrow.v`, at the path #347 is scheduled to import rather than rebuild. The issue asked for two things that turn out to be INCOMPATIBLE at `eq_refl` strength, and the file MEASURES the incompatibility rather than quietly picking one (measured, not *proved* — a `Fail Check` records that conversion rejects the identification at the current definitions; no `≠` theorem is stated): a definitional instantiation of the primal class at the opposite categories ranges over `1 ∏ D^op`, while a terminal object of `F ↓ =(c)` ranges over `D ∏ 1`, and `Product` of categories is not symmetric on the nose. **The mismatch is NOT on the terminal/initial axis** — `terminal_is_initial_op` records by `eq_refl` that `Terminal (F ↓ =(c))` IS `Initial ((F ↓ =(c))^op)`, so the `Structure/Initial.v` idiom costs nothing here; what fails to line up is specifically WHICH comma category. The op-instantiation is taken as the DEFINITION (`CouniversalArrow c F := @UniversalArrow (C^op) (D^op) c (F^op)`, `Existing Class`, the Comonad/Core.v idiom) and the terminal reading is delivered as a proved bridge BOTH ways (`couniversal_arrow_terminal`/`couniversal_arrow_of_terminal`, the couniversal object chosen by `eq_refl`). **The covariance is real and costs nothing**: `coarrow_obj`, `coarrow`, `ump_couniversal_arrows` and `couniversal_arrow_from_UMP` are all supplied by `:=` with NO tactic, so a consumer writes `coarrow U` and `ump_couniversal_arrows U h` and never sees an `op` — the accessor discipline the issue actually asked for, verified by writing the consumer uses independently. `RightAdjointFunctorFromCouniversalArrows` and `AdjunctionFromCouniversalArrows` complete the assembly, the latter inhabiting `F ⊣ R` on the nose. Measured negatives, each pinned in Test/ProbeCouniversal.v as a `Fail` against a positive control (6 probes / 14 controls, instrument sanity-checked, each stripped and confirmed a genuine `cannot unify` or typing error): the op-side `arrow_initial` is NOT `Terminal (F ↓ =(c))`, the two comma categories are not Leibniz-equal, `CouniversalArrow c F ≠ UniversalArrow c F`, and the counit is `≈` but not `eq_refl` to `coarrow` (blocked by an `fmap[F] id` residue in the transpose, NOT by the `abstract`-opacity one might expect — the object action DOES reduce, `right_adjoint_obj` being `eq_refl`). **`Cocomma` does not help** and that is measured, not assumed: `Cocomma F =(c)` is neither the comma the op-side inhabits nor `(F ↓ =(c))^op` on the nose, both negatives pinned. NOTATION GUARD, with the halves separated (an earlier draft of this bullet overstated it): three scopes declare `_ ^op` (category/functor/adjunction) and requiring `Adjunction.Opposite` makes the adjunction one win, but the `Open Scope category_scope` is DEFENSIVE, not required — `Bind Scope category_scope with Category` rescues every `C^op` in the file, all of which sit in argument or ascription positions, and deleting the line leaves it compiling clean (measured). The hazard is real in general (a bare `Definition d := C^op.` after that Require block does fail) but this file has no such occurrence. What IS forced is the other half: `F^op` is rejected under either scope order, so every opposite functor is written `Opposite_Functor F` by name (same family as Instance/Rng/Mod.v's note). Non-vacuity is the deliberate mirror of `Adjunction/GAFT/Examples.v` (Theory/Universal/Arrow/Dual/Examples.v): the product with its projections as a couniversal arrow out of Δ, the terminal reading exercised at it, then the assembly run end to end (`Δ ⊣ R`) with `right_adjoint_iso` against `Diagonal_Product_Adjunction` showing `R ≈ ×(C)`, closing at `Sets` — and the strength runs OPPOSITE to the primal example, GAFT's functor being `Qed`-opaque where this one reduces (`fobj[R] p = fst p × snd p` by `eq_refl`). By-product beyond the issue: Arrow.v carries both encodings but never related them; the passage is added here both ways (on arrow and object, NOT as a bijection or setoid iso — disclosed). Deferred: no notation (the primal's `c ⟿ F` is section-local and does not export, so there is no spelling to mirror); the §IV.1 Thm 2 converse is #347's; no dual of `UniversalArrowIsUniversalProperty`
- **Adjunction/Determination.v**: Mac Lane §IV.1 Theorem 2 (`maclane:IV.1:thm2`) and Awodey §9.2 Corollary 9.5 (`awodey:9.2:cor5`) — an adjunction is determined by its counit and by couniversal arrows, supplying the leg the tree could not cite. **THE ISSUE'S "Current state" IS STALE and the file says so**: it claims a whole-tree search for `couniversal` "returns nothing" and that `Theory/Universal/Arrow.v` "has no dual", whereas #302's `Theory/Universal/Arrow/Dual.v` already supplies `CouniversalArrow`, `coarrow`, `ump_couniversal_arrows`, `RightAdjointFunctorFromCouniversalArrows` and `AdjunctionFromCouniversalArrows` — all CONSUMED here, none re-derived, exactly as that issue's own QA correction directs ("consume, do not rebuild… this issue's obligation is only the covariant re-exposure and the round trip"). **THE DISTINCTION THAT DEFINES THE FILE IS A DIRECTION, AND IT IS EASY TO MISS**: `Dual.v`'s `counit_couniversal` takes a couniversal family as DATA and describes the adjunction that file BUILDS — every `⊣` in Dual.v concerns `RightAdjointFunctorFromCouniversalArrows`, so it never starts from an arbitrary adjunction. What was open, and is delivered here, is the CONVERSE: from an **arbitrary** `F ⊣ U`, **`adj_counit_couniversal`** exhibits each counit component as a `CouniversalArrow` **PACKAGED AS THE CLASS**, which is the issue's own bar ("not merely that the equational form `adj_univ_impl` holds"). **THE CONVERSE DIRECTION IS STRICTLY BETTER BEHAVED THAN THE DONOR'S, AND THAT IS MEASURED**: the ARROW readback `coarrow (adj_counit_couniversal c) = counit` holds at `eq_refl`, where `Dual.v`'s own `counit_couniversal` reaches only `≈` — because building the couniversal arrow FROM the counit makes the counit definitionally its arrow, whereas recovering the counit from a given family leaves an `fmap[F] id` residue. **The OBJECT readback is NOT evidence of that and an earlier revision wrongly cited it as such**: it is `eq_refl` on both sides (`Dual.v`'s `right_adjoint_obj` is `reflexivity`) and on this file's own other direction too, so it is parity. The object readbacks are load-bearing all the same — `couniversal_of_counit_med` and `adjunction_of_counit_unit` only TYPECHECK because they are definitional. **The one `≈`-only step is the MEDIATOR, and its cause DISCRIMINATES rather than blaming opacity in general**: `coarrow`/`coarrow_obj` reduce because they are transparent projections of the transparent `couniversal_arrow_from_UMP`, while the mediator is read out of `ump_couniversal_arrows`, whose primal donor `ump_universal_arrows` (`Theory/Universal/Arrow.v`) is closed with `Qed` — controls and negative sit on the SAME couniversal arrow. **Read that at its true strength**: it establishes the failure is not a property of the arrow in general, but does NOT by itself isolate the `Qed`, since `coarrow`/`coarrow_obj` never route through `ump_*` and would reduce under any competing hypothesis. An isolating experiment against a transparent clone of the donor was run during review and DOES confirm the attribution — and shows the `≈` could be strengthened by making the donor transparent — but is NOT shipped, and changing `Theory/Universal/Arrow.v` is out of this issue's scope. The round trip is delivered BOTH ways: `counit_couniversal_family` → `right_adjoint_of_counit`/`adjunction_of_counit` with `right_adjoint_of_counit_iso : right_adjoint_of_counit ≈ U` and the transpose/untranspose/unit readings, and back through `couniversal_of_counit_iso`. Mac Lane's FIRST presentation is given its OWN RECORD — `UnitPresentation` (a left adjoint, a right adjoint, and a unit whose every component is universal) with passages both ways (`unit_presentation_of_adjunction`/`adjunction_of_unit_presentation`); the issue records that the tree previously conflated it with the third. **That is a DICTIONARY, not a separation** — nothing proves the two presentations differ — and **unlike the counit side it carries NO round trip**: nothing states that building an adjunction from a recovered presentation returns the original. The two transpose readings are `eq_refl` BY CONSTRUCTION, since `adjunction_of_unit_presentation` defines its transposes as exactly those terms, so they pin a definition rather than recovering anything. **33/33 constants closed under the global context** (`Print Module`; an earlier revision said 31 by missing `UnitPresentation` and `Build_UnitPresentation`, because `Print Module` WRAPPED the `Record` line and a line-anchored pattern skipped it — the same wrapping hazard the tree records for universe instances. `Print Module` renders the 17 opaque constants as `Parameter`, a display convention and not an axiom). `Test/ProbeDetermination347.v` pins the one measured boundary from OUTSIDE the target, with the discriminating controls beside it, an instrument check and probe-vacuity **4/4** — a figure that was FALSE when first claimed: `unique_obj`, `to` and the `adj[_]` notation appeared only INSIDE the `Fail`, making the guard inert at a true 2/4, and the control that names all three outside it was added for that reason. **A PROBE-HYGIENE EPISODE IS RECORDED IN THAT FILE RATHER THAN QUIETLY FIXED**: the negative passed through THREE states before it certified anything — with `Theory/Isomorphism` missing it failed on "reference `to` not found", with `Instance/Sets` missing it failed on "Illegal application" because the `SetoidMorphism` coercion was out of scope, and only with the target's FULL import list mirrored does it fail on the intended `cannot unify`. NOT delivered: no naturality of the couniversal family in `c`; no comparison with `Theory/Universal/Element.v`'s universal-element route; no dual of the `UnitPresentation` packaging (a counit presentation as a separate record); and nothing is claimed about whether the mediator's `≈` could be strengthened by making the donor transparent — that is a change to `Theory/Universal/Arrow.v` and is not attempted
- **Adjunction/Representability.v**, **Theory/Universal/Arrow.v** (extended): Mac Lane §IV.1 Corollary 2 + Exercise 1 (book pp. 85-86) + Awodey §9.6 + Riehl §4.4 Prop 4.4.4 — **a functor has a left adjoint exactly when each `Hom_C(c, G−)` is representable**, as a genuine biconditional over an ARBITRARY `G`: **`adjunction_iff_pointwise_representable : (∀ c, Representable (HomAfter G c)) ↔ { L : C ⟶ D & L ⊣ G }`** (the issue's own pinned name), with the dual `coadjunction_iff_pointwise_representable` for Exercise 1's other orientation. **THE ENABLING LEMMA IS A PACKAGING EQUIVALENCE, AND IT IS THE MISSING TWIN OF ONE THAT ALREADY EXISTED**: `Theory/Universal/Arrow.v` has carried BOTH encodings since it was written — the comma-initial `UniversalArrow` (which `AdjunctionFromUniversalArrows` consumes) and the object-as-parameter `AUniversalArrow` (which the representability side arrives at, via `Theory/Universal/Element.v`'s `AUniversalArrow_of_hom`) — each with its own mediator kit and its own uniqueness theorem, and NOTHING converted between them, so Mac Lane's chain could not be run end to end. `ua_of_aua`/`aua_of_ua` close that, and the tree said so itself: `Theory/Universal/Arrow/Dual.v`'s own comment reads "Theory/Universal/Arrow.v carries both encodings but never relates them", that file having supplied the COUNIVERSAL passage (`ACouniversalArrow_of_CouniversalArrow` and its inverse) — **so the dual was composable before the primal was**, and what is added here is the mirror, not an invention. **BOTH ROUND TRIPS RETURN EVERY OBSERVABLE ON THE NOSE**: five `eq_refl` readbacks (`ua_of_aua_obj`, `ua_of_aua_arrow`, `aua_ua_round_arrow`, `ua_aua_round_obj`, `ua_aua_round_arrow`), with `aua_ua_round : ≈` at `AUniversalArrowEquiv`; only the comma `Initial` RECORD is rebuilt, refuted at `eq_refl` and pinned. **STRENGTHS MEASURED STRICT-FIRST, and the object agreement Mac Lane's phrase actually asks for holds DEFINITIONALLY**: `left_adjoint_of_representable_obj` — the left adjoint built from a family of representations agrees ON OBJECTS with the chosen representing objects — is `eq_refl`, as are `adj_unit_universal_obj`/`_arrow` (the universal object is `F c`, the universal arrow IS `unit`), `adj_representable_obj`, `representable_roundtrip_obj`, and both dual object readbacks. **THE ONE `≈`-ONLY STEP HAS ITS CAUSE EXHIBITED RATHER THAN DESCRIBED**: recovering the unit leaves an `fmap[G] id`, and `representable_roundtrip_arrow_residue`/`adjunction_of_representable_unit_residue` state that term LITERALLY and close by `eq_refl`, so the residue is displayed, not guessed — the probe pairs the refuted strict form against that residue as its control, which is what makes the attribution discriminate. **THE DUAL ORIENTATION COSTS NOTHING AND THAT IS MEASURED, NOT ASSERTED**: `CoHomBefore F a` IS `HomAfter (Opposite_Functor F) a`, so `couniversal_of_corepresentable` is the PRIMAL constant applied at the opposite categories — `:=` with no tactic, no transport and no coercion — and the forward dual leg is `adj_representable` at `Opposite_Adjunction`. **RIEHL'S UNIQUENESS CLAUSE IS DELIVERED IN THREE GRADES THAT ARE KEPT APART**: `unit_square_forces_fmap` (**the unit square has AT MOST ONE solution** up to `≈` — read the hypotheses exactly: it takes BOTH that `fmap[L] f` satisfies the square and that `g` does, so it is SYMMETRIC in the two, which is why the proof is the uniqueness field used twice; it is **NOT** the stronger "any `g` satisfying the square is `fmap[L] f`", which is false for an arbitrary `L` and family, and an earlier revision of this bullet stated it that way. The instance `left_adjoint_of_representable_fmap_unique` is sound because it discharges the extra hypothesis from the proved `left_adjoint_of_representable_natural`); `adjunction_of_natural_universal` (a functor carrying a NATURAL family of universal arrows is itself left adjoint to `G`, on the nose rather than up to comparison); and `natural_universal_left_adjoint_iso`/`left_adjoint_of_representable_iso` via the in-tree `left_adjoint_iso`, **explicitly labelled the WEAKER statement and explicitly NOT claimed to be Riehl's clause**, since it says nothing about the object assignment. **THE ISSUE'S "Current state" IS STALE — the seventh consecutive time — AND TWO OF ITS CLAIMS ARE FALSE, MEASURED**: it says `Functor/Representable.v`'s `Representable` "has no consumers anywhere in the tree (no file even imports it)" when **25 files on master Require it** (27 with these two) and inhabitants of the class are declared in some fifteen files, one of them `Curry_Representable` (`Structure/Cartesian/Closed/Adjunction.v`) IN an adjunction file — an earlier revision of this bullet said "26 files across 11 declaration sites", and BOTH figures were artifacts: 26 matched neither endpoint, and 11 came from a one-line regex that misses a signature spanning two lines; and it says `rg 'couniversal|RightAdjointFunctor|TerminalArrow'` returns **0 hits** when that command — case-SENSITIVE, as the issue writes it — returns **6 files on master** (7 with this file); the figure 8 holds only under `-i`, which the quoted command lacks, including `Theory/Universal/Arrow/Dual.v` and `Adjunction/Determination.v`. Consequently Riehl's "construct the RIGHT adjoint" checkbox was ALREADY largely discharged by #302's `RightAdjointFunctorFromCouniversalArrows`/`AdjunctionFromCouniversalArrows`, which are CONSUMED here, not rebuilt. **Yoneda is off the CONSTANT chain, not off the module closure, and the difference is stated**: every representability passage goes through Element.v's Yoneda-FREE `ue_representation`/`AUniversalElement_of_repr` route and none touches `Yoneda_Lemma`, but `Functor/Hom/Yoneda` IS in the transitive closure via `Theory/Universal/Element.v`. **The universes ARE measured rather than disclaimed**: the delivered constants are over `C : Category@{u u0 u0}` and `D : Category@{u1 u2 u2}` — object universes FREE of hom, which is exactly what the Yoneda-free route buys, `Yoneda_Lemma@{u u0}` being over `Category@{u0 u0 u0}` with all three identified — while the blocks DO carry one identification, `u0 = u2`, collapsing C's and D's hom-and-proof universes, in `adjunction_iff_pointwise_representable`, `left_adjoint_of_representable` and `adj_representable` (`ua_of_aua` carries no equation at all); its CAUSE is NOT attributed, no isolating experiment having been run, and it is not claimed unavoidable. `HomAfter` had exactly two prior occurrences in tree (its own module and one probe file). **50/50 constants closed under the global context** (42 in Representability.v by `Print Module`, against 36 source declarations — the 6-way gap is `abstract` SUBPROOFS a glob sweep cannot see — the file contains no `Program` at all, and the six are `adjunction_of_natural_universal_subproof{,0..4}` — plus the 8 new ones in `Theory/Universal/Arrow.v`), of which the `make print-assumptions` gate carries 18 permanently. `Test/ProbeRepresentability348.v` pins the two boundaries from OUTSIDE both targets with 2 CONVERSION negatives against 6 controls plus a scope-free instrument check, each negative stripped and its failure kind read off the error. NOT delivered: no equation or isomorphism between two functor RECORDS pinning the object assignment (it would need a transport in the type of `fmap`, so the strong reading exists only in the two forms above); no naturality of the family of representations in `c`; no concrete witness instantiating either biconditional at a named category; no uniqueness statement for the representations themselves; and no universe measurement
- **Adjunction/Representability/Sets.v**: Mac Lane §V.6 Theorem 3 and Definition 3 (book p. 122) with §V.8 Exercise 1 (p. 131) and Riehl 4.7.14 — **a `Sets`-valued functor on a complete category that preserves limits and has a solution set is REPRESENTABLE, and conversely: `representability_theorem` and the biconditional `representability_iff : (PreservesImageLimit K * ElementSolutionSet K) ↔ Representable K`**, over `Complete C`. Definition 3 is the book's ELEMENT-wise solution set, `ElementSolutionSet` (a `Type` of indices, an object and an element of `K` at each, and a factorization of every element of every `K c`); `sols_of_esols`/`esols_of_sols` pass between it and Adjunction/GAFT.v's hom-shaped `SolutionSet K SetsOne`, keeping index, objects and elements on the nose (four `eq_refl` readbacks), the two records being inter-derivable but NOT convertible (probe N1). The bridge the issue asks to "prove as a reusable lemma" — `(1 ~{Sets}~> X) ≅ carrier X` — already existed as `global_elements_iso` (Theory/Universal/Element.v, which also carries its natural form) and is not re-proved. The route to Theorem 3 is: a representation is a universal element, hence an initial object of Construction/Elements.v's `=(1) ↓ K`, which GAFT's machinery builds from a solution set — a step that was **sealed inside `GAFT`'s own `Qed`** and is exported here as **Adjunction/GAFT.v's `comma_initial_of_sols`**, appended at that file's end so every line above it stays put (eleven external citations point into it), with `GAFT_via_comma_initial` re-deriving GAFT from it as a cross-check that it is the same step (the theorem is NOT rewritten to use it). The converse leg is Functor/Hom/Continuous.v's `representable_iso_ContinuousFunctor` (`continuous_of_representable`) — the #428 constant the issue names neither of. **§V.8 Exercise 1**: forward, `HomAfter K 1` IS `Hom(1,−) ◯ K` on the nose (readback) but NOT `K` (probe N2), so `homafter_one_iso` transports #348's `adj_representable` onto `K` and `representable_of_left_adjoint` returns the left adjoint at the singleton as its representing object (`eq_refl`); `saft_representable` is the same passage after SAFT (Riehl 4.7.14). (CORRECTION, #451: the same passage over `Adjunction/SAFT.v`'s premises, which are not Riehl's. Its `cover` premise is refuted at `K := Id[Sets]` by `Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd`, at every `Sets@{o _}` with `Set < o` — a functor that Riehl's hypotheses cover and that `Sets_Id_repr` represents — so `saft_representable` never applies there.) (Since #453, as the file's header marks it: superseded, and kept. `Adjunction/SAFT/Characterization/Corollaries.v`'s `continuous_Set_functor_representable` is the same passage over the hypotheses of Mac Lane's §V.8 Corollary — `Complete`, `PreservesImageLimit`, a `Cogenerator` and #451's `WellPowered`, with no covering datum — and `continuous_Set_functor_representable_iff` is the criterion as a biconditional; `Adjunction/SAFT/Characterization/Examples.v` applies the first at `Id[Sets]` and at `HomFrom A` under `Untruncate` (`sets_id_repr_saft`, `sets_hom_repr_saft`). `saft_representable` and its statement are unchanged.) The CONVERSE is conditional and the reason is measured, not asserted: every copower in tree (Structure/Limit/Power.v from #321, with #366's Structure/Limit/Power/Adjunction.v) is indexed by a bare `Type` and yields plain functions out of the index, where a `Sets`-valued adjunction needs setoid morphisms respecting `≈` (probe N5 pins the mismatch), so it is `left_adjoint_of_representable_Sets` over `HasSetsCopowers`, stated exactly as #348's `adjunction_iff_pointwise_representable` consumes it — which makes the hypothesis, by that same biconditional, "the represented functor has a left adjoint", disclosed rather than hidden; the hypothesis is also stronger than what is used (`cop` is applied only at the representing object), and no inhabitant of it is built by any route, nor is one shown impossible. **Witnesses**, both with every premise discharged in tree: the points functor `Sets_points := Hom(1,−)` on `Sets` (`Sets_points_repr` by Theorem 3, its element-wise solution set the singleton at `1` with the identity as element, its preservation `hom_ContinuousFunctor`; `Sets_points_iso` compares the produced object with `1`), and the identity functor twice — `Sets_Id_repr` by Theorem 3 from Adjunction/GAFT/Sets.v's own solution set, and `Sets_Id_repr_of_adjoint` by Exercise 1 from `GAFT_at_Sets_Id`, whose representing object reads back at `eq_refl` as that adjoint at the singleton. **Stale premises**: the issue's "Current state" is FALSE in five substantive claims — the global-points bridge exists, the singleton instantiation exists (Element.v), 26 constants across 19 files conclude `Representable` at the parent commit, and 33 files `Require` it, one of the 26 being an adjunction-sourced instance (Representability.v) — counted by testing whether each head's conclusion begins with `Representable`, comments stripped, the category of elements of a `Sets`-valued functor exists with its comma presentation and proved comparison (Construction/Elements.v), and #366's copowers landed — and six of its citations are stale (`Representable` is in Functor/Representable.v, cited five times; UniversalProperty.v; Instance/Sets.v, where the object wanted is `SetsOne` in Construction/Elements.v; Continuity.v, whose cited site is header prose; Comma/Limit.v; WeaklyInitial.v); the `Sets_global_points` of its Verification block exists nowhere and is not created. **Universes**: Definition 3 is UNPINNED (`ElementSolutionSet@{u u0 u1 u2}` over `C : Category@{u u0 u0}`, one strict constraint, no `Set`); everything downstream of the comma-initial step inherits GAFT's pin of hom AND proof to `Set`, which is why the theorem is stated at TOP LEVEL — probe N4 pins the refusal at a category with those levels apart ("Cannot enforce sp = sh because sh < sp"); the witnesses land at `Sets@{Set u}`, as GAFT/Sets.v's header already records. **54 constants closed under the global context, zero `Axioms:` lines** — 38 `.glob` heads (32 `def`, 1 `prf`, 4 `proj`, 1 `rec`) + 14 `Program` obligations (6 of `homafter_one_iso` by its local tactic, 8 of `homafter_whisker` by hand) + GAFT.v's 2 new heads, all carried by the gate; four `Defined` here, each LOAD-BEARING under a flip (each stops its own first component readback: `sols_of_esols_index`, `esols_of_sols_index`, `representability_iff_fst`, `Sets_points_esol_obj`), and eight `Qed` (all `homafter_whisker`'s obligations); GAFT's `comma_initial_of_sols` is `Defined` as data, matching `wif_of_sols`, but its flip is NOT load-bearing and that is disclosed rather than claimed; closure 99 excluding self (Functor/Hom/Continuous.v 10 at the margin, GAFT/Sets.v 2, Representability.v 2, Comma/Creation.v 2, SAFT.v 1, and 22 of the 27 `Require`s at margin 0, none droppable) — a satellite rather than an extension of Adjunction/Representability.v precisely because those imports would take the parent's closure from 39 excluding self to 98 for every downstream consumer of #348's biconditional; (CORRECTION, #451: `Adjunction/SAFT.v` now requires `Theory/Subobject.v`, which no other import here reaches. By iterated `coqdep` and by `.Makefile.coq.d`, which agree and reproduce 99, 98, 39 and the five nonzero margins above when that one edge is dropped, the closure is 100 excluding self, `SAFT.v` costs 2 at the margin, the other four margins are unchanged, and the parent with this file's imports would be 99; the parent's own 39 is unchanged.) zero name collisions across the 38 names. `Test/ProbeRepresentability437.v` mirrors the `Require` list and carries **6 refutation commands = 1 instrument + N1-N2 CONVERSION + N3, N5 TYPING + N4 UNIVERSE**, each stripped one at a time in a copy of the whole file beside its accepted control; seven `eq_refl` readbacks; guard coverage 34/24 with ten exhaustive exceptions (three keywords, three bound variables, the four refuted names); rename-simulated 14 library names, every first break on a positive line; `make todo` grows by those 6 lines only (2283 → 2289 over #435's tip), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: a setoid-indexed copower, hence no unconditional converse to Exercise 1, and no inhabitant of `HasSetsCopowers`; the rewriting of `GAFT` itself to use the exported step; GAFT as a biconditional; representability at any target but `Sets`; a witness at any category but `Sets` (CORRECTION, #454: false since #449 and again since #454 — `Instance/Mod/TensorAFT.v`'s `tensor_via_AFT_of_esols` and `bal_tensor_via_AFT_of_esols` apply `representability_theorem` at `RMod R` and at `Ab`, and `Instance/Mod/Watts/Unconditional.v`'s `RModop_continuous_representable` applies it at `(RMod R)^op` with its completeness and solution-set premises discharged for every continuous K, and at K := the hom functor by `RModop_hom_representable_obj`; the file's header carries the same correction); naturality of `homafter_one_iso` in `K` or functoriality of anything here; no edit to Functor/Representable.v, Theory/Universal/Element.v, Construction/Elements.v, Functor/Hom/Continuous.v, Adjunction/SAFT.v or Adjunction/Representability.v, and none to Adjunction/GAFT.v above its last line. Instance/Sets/Complete.v and Adjunction/GAFT/Sets.v ARE edited, one sentence each and line-count-preserving: both said `SAFT` was never applied, which `saft_representable` falsifies, and docs/INHABITATION.md's `SAFT` row is corrected with them (every citation into those two files points below the corrected lines and still lands).
- **Adjunction/Additive.v**: Mac Lane §IV.1 Theorem 3 (book p. 85) + Riehl §4.6 Corollary 4.6.9 — **an adjoint of an additive functor is additive, and the adjunction bijection is an isomorphism of ABELIAN GROUPS rather than merely of sets**. Mac Lane's conclusion is a PAIR of claims and both halves are delivered. **THE CLASS IS CONSUMED, NOT BUILT, AND THE ISSUE'S PREMISE IS FALSE**: the catalog issue states the tree has no notion of an additive functor and that searches for `AdditiveFunctor` return nothing — it is declared at `Structure/AbCategory.v` with preservation of `padd` as its ONLY field, and that file already proves `fmap_pzero` and `fmap_abneg` (which discharges the negation clause the issue's own QA correction anticipated might still be owed) plus the closure instances `Id_AdditiveFunctor` and `Compose_AdditiveFunctor`; the issue searched `Structure/Preadditive.v`, `Structure/Additive.v` and `Structure/Semiadditive.v` and never `AbCategory.v`. Nothing here redefines any of them. **THE TRANSPOSES ARE ADDITIVE BECAUSE `U` IS AND COMPOSITION IS BILINEAR, WHICH IS MAC LANE'S OWN SENTENCE**: `to_adj_padd` (with `to_adj_pzero`/`to_adj_abneg`) runs through the in-tree `to_adj_unit`, which recovers `⌊f⌋ ≈ fmap[U] f ∘ η`, then `fmap_padd` for `U`, then `compose_padd_right`; the mirrors `from_adj_padd` and siblings go through `from_adj_counit` and `compose_padd_left` and need `F` additive instead. **THEOREM 3 IS DELIVERED AS AN INHABITANT OF THE CLASS**, not a bare equation — `left_adjoint_additive : F ⊣ U → AdditiveFunctor U → AdditiveFunctor F` — so it composes with `Compose_AdditiveFunctor`; the proof compares transposes (both sides transpose to `padd (η ∘ f) (η ∘ g)`) and concludes by `adj_to_inj`, injectivity of `⌊−⌋` from `to_adj_comp_law`. **THE HOM-GROUP ISOMORPHISM LANDS IN THE TREE'S OWN CATEGORY OF ABELIAN GROUPS**: `hom_ab` reads each hom-setoid of an Ab-category that is `LocallyPropositional` as an `Instance/Ab.v` `AbObject` (all four data fields the enrichment's own, by `eq_refl`) and `adj_hom_ab_iso` exhibits the adjunction bijection as an `@Isomorphism Ab` whose two legs ARE `⌊−⌋` and `⌈−⌉` (`eq_refl`) and whose round trips are `to_adj_comp_law`/`from_adj_comp_law` verbatim — a real record, not a bespoke lookalike. An earlier revision of this bullet stated both of those unconditionally; measured on 2026-09-17 after the PR "algebraic carriers are sets", an `AbObject` carries `cmon_prop` and an arbitrary category's hom-setoid supplies no `Prop` mirror of its `≈`, so TEN constants here take `{LP : LocallyPropositional C}` — `hom_ab`, `hom_ab_carrier`, `hom_ab_plus`, `hom_ab_zero`, `hom_ab_neg`, and, on BOTH categories, `adj_to_ab`, `adj_from_ab`, `adj_hom_ab_iso`, `adj_hom_ab_iso_to`, `adj_hom_ab_iso_from`. So read the opening boldface at its two different strengths: the FIRST half, that an adjoint of an additive functor is additive, is unconditional and unchanged; the SECOND, that the bijection is an isomorphism of ABELIAN GROUPS, now holds for locally propositional Ab-categories. Measured, not assumed: nothing else in the file changed statement, and the nine module and bimodule objects built on `hom_ab` lost nothing, `Ab` and `RMod R` discharging the class by `Ab_LocallyPropositional`/`RMod_LocallyPropositional`. **THE DUAL NEEDED A CONSTRUCTION THE TREE DID NOT HAVE, and that absence was measured by SHAPE rather than by name**: nothing anywhere inhabits `Preadditive (C^op)` or `AbEnriched (C^op)` — the only hit outside this file is `Structure/Preadditive.v`'s PROSE remark that the laws are self-dual, which is a sentence and not a term — so `Preadditive_op`/`AbEnriched_op` are built here (every field the original with `compose_padd_left`/`_right` exchanged, since composition in `C^op` is `C`'s flipped), with `AdditiveFunctor_op`/`_unop` carrying additivity across `Opposite_Functor` both ways and `right_adjoint_additive` reading off Theorem 3 at `Opposite_Adjunction`. **RIEHL'S COROLLARY 4.6.9 HALF LANDS, AND THE HYPOTHESIS-FREEDOM IS VERIFIED AT THE SIGNATURE**: `left_adjoint_additive_of_biproducts` takes the two Ab-enrichments, two `ZeroObject`s, the adjunction and `HasBiproducts D` and produces `AdditiveFunctor F` with **NO additivity hypothesis on either adjoint** — the biproduct bridge the issue names as the reusable missing lemma, via `biproduct_of_coproduct` and the image biproduct `adj_image_biproduct` (object and both structure maps the `F`-images on the nose). Read its scope: the biproducts of the source are an EXPLICIT hypothesis rather than derived from completeness; the Ab-ENRICHMENT of both categories is likewise ASSUMED, where Riehl's point is that in a semiadditive setting it comes for free — so what is derived is the additivity of the adjoint, not the enrichment; and only the left-adjoint orientation is delivered; `Adjunction/Continuity.v`'s `right_adjoint_preserves_limits` and `left_adjoint_preserves_colimits` cover the corollary's EXACTNESS half for all shapes — but NOTHING here connects this file's elementary coproduct preservation to them, so no bridge between the elementary and `Colimit`-shaped readings is claimed (an earlier revision of this bullet said "already subsume", overstating, and cited two line numbers that are a comment line and a section heading). **TWELVE strict readbacks hold and are shipped as `Example`s** — an earlier revision said thirteen and counted `left_adjoint_additive_fmap`, which was a SYNTACTIC TAUTOLOGY (`fmap[F]` IS notation for `@fmap _ _ F _ _`, so the statement was `t = t`, never mentioning the constant it claimed to measure and compiling at an arbitrary functor with no adjunction); it is REMOVED rather than repaired, there being nothing to read back — `AdditiveFunctor` is a structure ON a functor, not a bundled one; the three equations measured strict and REJECTED (`to_adj_padd`, `left_adjoint_fmap_padd`, `padd_copair_diag`) are `≈`-only because `padd` is an abstract class field so neither side reduces — the file's own header flagged that nothing in it would notice if a later change made one definitional, and `Test/ProbeAdditive349.v` closes that gap, pinning all three as CONVERSION negatives beside `≈` controls, each stripped and confirmed a genuine `cannot unify`. **Universes measured off BOTH binder and block, and the `⊣` identification has AT LEAST THREE INDEPENDENT DONORS — an earlier revision of this bullet attributed it to ONE and called the attribution DISCRIMINATING, which was FALSE**: every constant is over `Category@{u u0 u0}` (hom = proof, from `AbEnriched`/`Preadditive`/`ZeroObject`/`Biproduct` — though not only those, `adj_to_inj` mentioning none of the four and carrying it via `Adjunction`), and every result mentioning `⊣` additionally carries `u0 = u2`. Measured under `Constraint bh < ah` (which satisfies the bound `bh <= ah` while violating the equation), THREE donors are each rejected ALONE: `AdditiveFunctor` — with one functor in the command and no adjunction, its binder reusing a single level for both categories while its own block is EMPTY, and both cited statements take an `AdditiveFunctor U` hypothesis so this donor alone suffices; the mere presence of functors in BOTH directions, `Functor` forcing source-hom ≤ target-hom; and `Adjunction`. The control previously cited, `fmap_padd_of_preserved_coproduct`, IS accepted at those levels but removes all three donors at once (one functor, `Preadditive` for `AbEnriched`, no `AdditiveFunctor`), so it is a control for "one functor" and discriminates nothing — the exact failure this index records twice under "siblings tested before attributing". `Test/ProbeAdditive349.v` now pins all three as FORMABILITY negatives beside that control, each stripped and confirmed a genuine universe inconsistency naming the declared levels — and the error text separates them, `AdditiveFunctor` and `Adjunction` failing on an EQUATION and `Functor` on a BOUND. `hom_ab` is the sharpest binder/block trap here, its block LITERALLY EMPTY while its binder reads `Category@{u0 u u}`. None claimed unavoidable. **48/48 constants closed under the global context** (`Print Module` = source declarations = 48), of which the gate carries 17. The counts coincide not because the file avoids `Program` — it has two `Program Definition`s, `Preadditive_op` and `AbEnriched_op`, forced because a plain `Definition` rejects the swapped-endpoint `padd` field — but because they generate ZERO obligations. A name collision was found and renamed away before commit — `coprod_ext` clashed with `Construction/Cospan/Bridging.v`, latent today since that file is not in the gate's scope but exactly the hazard that makes `Print Assumptions` audit the wrong constant. NOT delivered: no characterization of additive functors as the biproduct-preserving ones (Mac Lane §VIII.2.4, its own catalog item); no additive-adjunction CLASS and hence no category of such; no naturality for `adj_hom_ab_iso`, so it is a family of group isomorphisms and not an isomorphism of Ab-valued bifunctors; no enriched reading, so nothing says an adjunction between Ab-categories is an Ab-ENRICHED adjunction; and no concrete instantiation at a named pair of Ab-categories
- **Adjunction/Pare.v**: Mac Lane §IV.1 Exercise 4 (book p. 86), attributed to **Paré** — **`G` has a left adjoint exactly when a whiskered idempotent splits**. Given `G : A ⟶ X`, `K : X ⟶ A`, `ε : K ◯ G ⟹ Id`, `ρ : Id ⟹ G ◯ K` and the SINGLE triangle `Gε ∘ ρG ≈ id[G]` (bundled as `PareData`), `pare_idem : K ⟹ K` is idempotent (`pare_idem_Idempotent`), and **`pare_left_adjoint_iff_splits : PareLeftAdjoint ↔ PareSplits`** is the biconditional both ways. **THE BICONDITIONAL IS NOT VACUOUS BY CONSTRUCTION, AND THAT TURNS ON ONE CLAUSE**: `PareSplits` is `∃ F S, split_idem S ≈ pare_idem` — a splitting OF THAT IDEMPOTENT, not of some idempotent, which every category trivially has. **THE UNIT AND COUNIT ARE LITERALLY THE ONES THE EXERCISE PRESCRIBES**, which is the reviewer's bar and the thing easiest to quietly substitute: `pare_unit := (G ⊳ r) ∙ ρ` and `pare_counit := ε ∙ (s ⊲ G)`, with `pare_unit_at`/`pare_counit_at` pinning the components as `fmap[G] (r x) ∘ rho x` and `eps a ∘ s (G a)` by `eq_refl`, and `pare_Adjunction_unit`/`_counit` recording that the produced `F ∹ G` carries exactly those. **THE FILE'S SHARPEST FINDING IS THAT MAC LANE'S OWN COMPOSITES ARE NOT FORMABLE AS WRITTEN, AND THE OBSTRUCTION IS LOCATED RATHER THAN ASSERTED**: `Compose` is not associative on the nose and `Id` is not a strict unit for it, so `εK ∘ Kρ` lands at `K ◯ Id[X] ⟹ Id[A] ◯ K` and `Gε ∘ ρG` at `Id[X] ◯ G ⟹ G ◯ Id[A]` — **three** identifications are missing, associativity and BOTH unitors, so repairing the middle would still leave the endpoints wrong. `Functor` has exactly five fields; `fobj` and `fmap` agree at `eq_refl` on both sides of associativity and of each unitor (four `Example`s); the whole records do not; so the difference is confined to the remaining three LAW fields, which `Theory/Functor.v`'s printed body identifies as `Compose_obligation_1..3` and `Unset Transparent Obligations` makes opaque. The file therefore builds `pare_idem` as an explicit transformation rather than as the naive composite. **Seventeen `eq_refl` Examples**; the measured rejections the header disclosed as unguarded are now PINNED in `Test/ProbePare350.v` — **8 negatives, ALL ONE KIND (CONVERSION)**, each stripped and its failure read off the whole error. An earlier revision claimed "4 negatives of TWO KINDS" and labelled the composite one TYPING; applying the stated method refutes that — all report `has type … cannot unify` and the composite carries NO distinguishing marker, where this tree's genuine TYPING negatives are told apart by `cannot satisfy constraint` (`Instance/Cat/Pullback.v`) or `Illegal application` (`Theory/Morphisms/CokernelPair.v`); only the POSITION of the mismatch differs. **Its stated cause was also wrong**: it fails at the MIDDLE object (`cannot unify "K ◯ (G ◯ K)" and "K ◯ G ◯ K"`) — ASSOCIATIVITY, not the unitors, whose endpoints agree on both sides and are never reached — and the type ascription it carried was INERT, the error being byte-identical with and without it. **The localization is now MEASURED rather than inferred**: `Functor` has primitive projections WITH ETA (so record equality IS field equality), the `fobj`/`fmap` controls show two fields agree, and three further negatives fail each remaining LAW field individually. The round trip `pare_r P F Adj = r`, disclosed as measured but pinned nowhere, is now the eighth negative. **THE CAUCHY-COMPLETE COROLLARY IS A CONDITIONAL WITH NO IN-TREE PREMISE WITNESS, STATED RATHER THAN LEFT TO BE FOUND**: `pare_splits_of_CauchyComplete` is `split_of` at `pare_idem` (a `:=` with no tactic) and `pare_left_adjoint_of_CauchyComplete` is the biconditional applied to it, but the tree's only two `IdempotentsSplit` instances are `Karoubi_IdempotentsSplit` (`Construction/Karoubi/Universal.v`) and `Sets_IdempotentsSplit` (`Instance/Sets/Karoubi.v`), **neither a functor category**, so nothing discharges `CauchyComplete ([X, A])` for any `X`, `A` (measured: no `IdempotentsSplit ([_, _])` anywhere). **83/83 constants closed under the global context** — and the count needed care: `Print Module` lists 81 under a line-anchored sweep because it WRAPS the `Record` onto its own line, hiding `PareData` and `Build_PareData`; the 81 decompose as 63 source-declared (64 minus the wrapped record) + 13 `Program` obligations + 5 `PareData` field accessors, which no source regex sees. 64 declared names, ZERO tree-wide collisions. NOT delivered, disclosed in-header: no round trip in either direction at ANY strength (`pare_r P F Adj = r` is measured and rejected); no uniqueness of the left adjoint (`left_adjoint_iso` is in the hom-set presentation this file never enters); no hom-set `⊣` reading, the conversion in `Adjunction/Natural/Transformation/Universal.v` not being performed; **no `PareData` with a nontrivial idempotent, hence no non-degenerate exercise of the biconditional** (`pare_triv` is the identity witness); no `IdempotentsSplit` instance for any functor category; no dual; and no universe measurement
- **Adjunction/Right.v**: Mac Lane §IV.2 Definition 2 (book p. 89) + Riehl §4.4 — **a pair of CONTRAVARIANT functors each ADJOINT ON THE RIGHT**, and Mac Lane's warning that this is not adjointness on the left, DISCHARGED AS A THEOREM rather than repeated as prose. `AdjointOnTheRight S T` for `S : A^op ⟶ X`, `T : X^op ⟶ A` is the bijection `A(a, T x) ≅ X(x, S a)` in `Sets` with four naturality laws, and `AdjointOnTheLeft` is its mirror. **THE REVIEWER'S BAR — THAT THE CLASS IS NOT MERELY AN ABBREVIATION FOR THE ORDINARY ADJUNCTION — IS MET AND MEASURED, NOT ASSERTED**: stripped, the negative reports `The term "H" has type "T^op ⊣ S" while it is expected to have type "AdjointOnTheRight S T"`, a plain TYPING mismatch with NO `cannot unify` and no universe clause, in BOTH directions; the two are nevertheless INTERDERIVABLE, with `Adjunction_of_AdjointOnTheRight` and `AdjointOnTheRight_of_Adjunction` both ways and **all six round trips at `eq_refl` on the WHOLE RECORD** (both handednesses, both directions, plus both directions of the primed second orientation), so it is a genuine record equivalent to — and not definitionally equal to — the ordinary adjunction at the opposite categories. **BOTH ORIENTATIONS ARE DELIVERED AND NEITHER IS WRONG**: `T^op ⊣ S` is the reduction matching the DIRECTION of the book's `A(a, T x) ≅ X(x, S a)` and is taken as primary, while `S^op ⊣ T` matches its INVERSE and is shipped as the primed orientation with passages both ways — identified with `Opposite_Adjunction` of the first at `eq_refl` — so which is "the" reduction is a question of which way the bijection is read rather than a defect in either spelling. **THE WARNING IS REFUTED TWICE, WITH THE CHOICE OF WITNESS EXPLAINED RATHER THAN LEFT TO TASTE**: `right_does_not_imply_left` and `right_does_not_imply_left_in_Sets` each exhibit one pair having the right-hand bijection and provably no left-hand one — (i) an antitone Galois connection on a three-element chain, cheap and free of any `Set` in its constraint block (the carrier is declared `Inductive Chain3 : Set`, so the bare phrase "Set-free" would be loose), NON-DEGENERATE by proof (`chain3_s_not_monotone`/`chain3_t_not_monotone` show that in a thin category, where a functor IS a monotone map, NEITHER object map is that of any COVARIANT functor, so contravariance is genuinely exercised), and (ii) Mac Lane's OWN example, the contravariant power set over `Instance/Sets/Powerset/Universal.v`'s single-universe `Powerset_Prop_op`, where the bijection IS the flip of a relation (`powerset_aor_is_flip`, `eq_refl`) and all four naturality fields discharge by `reflexivity`. A third witness (a constant functor on the walking arrow) was built during scouting and is DELIBERATELY NOT SHIPPED, being degenerate — the same data is also a covariant functor, machine-checked out of tree — and would drag two literal `Set`s into the universe instance. **BOTH CLASSES ARE INHABITED and that is load-bearing**: an implication into an EMPTY class would be cheap to refute, so `Id_AdjointOnTheLeft` is shipped purely to rule that out, and the two real witnesses are non-degenerate on the other axis too (`chain3_S_neq_T`; `powerset_Omega_two_subsets` proving P(Ω) has two ≉-distinct elements, so the related hom-setoids are not trivially singletons). **RIEHL §4.4's UNIT/COUNIT PRESENTATIONS COST ALMOST NOTHING, AND THE REASON IS STRUCTURAL**: `MutuallyRightAdjoint` carries TWO UNITS and `MutuallyLeftAdjoint` TWO COUNITS, because under the reduction `runit` is literally the unit of `T^op ⊣ S` while `runit'` is its COUNIT — and a counit in `A^op` is a unit in `A` — so both ride the existing round trip in `Adjunction/Natural/Transformation/Universal.v` and **no triangle identity is re-proved here**, the only new machinery being four `Transform` repackagings, all `:=` with no tactic, each swapping `naturality` with `naturality_sym`. Riehl's remark that only the TWO-SIDED dualization collapses is stated in all three readings: two-sided on an ORDINARY adjunction returns one (`ordinary_two_sided_collapses`, the in-tree `Opposite_Adjunction` applied); ONE-sided dualization is what PRODUCES the contravariant notions; and two-sided on a CONTRAVARIANT pair does NOT collapse but **EXCHANGES** the two notions (`AdjointOnTheLeft_of_AdjointOnTheRight_op` and its inverse, both round trips `eq_refl`). **UNIVERSES MEASURED OFF BOTH BINDER AND BLOCK — AND READING ONE LINE OF A WRAPPED BLOCK GETS IT BACKWARDS**: `AdjointOnTheRight@{o1 h1 p1 o2 h2 p2 o3 so sh sp}` opens its block with the bound `h1 < so`, but the block CONTINUES and carries **seven EQUATIONS** (`h1 = p1`, `h1 = h2`, `h1 = p2`, `h2 = p2`, and `h1 = o3 = sh = sp`), so four of A's and X's six levels COLLAPSE to one — a draft of this bullet read only the first line and claimed the exact opposite. What IS free is the pair that matters: **both OBJECT universes `o1` and `o2` occur in no constraint at all, not even a bound**. The class and the theorems carry the SAME identifications; the theorems' blocks are bounds-only with the identifications sitting in the BINDER (`A : Category@{u0 u2 u2}`, `X : Category@{u1 u2 u2}`), so annotating buys PRESENTATION rather than strength, and at the theorems the object universes are BOUNDED rather than free. Since the refuted hypothesis is UNIVERSALLY quantified, the narrower quantifier makes the refutation STRONGER, not weaker. Each identification has **TWO INDEPENDENT DONORS**, probed apart: hom=proof is rejected at `Opposite` and, with no functor in the command at all, at `Sets`; A's-hom=X's-hom at the contravariant PAIR itself and again at `Sets`. None is claimed unavoidable. **25 `eq_refl` statements** (28 occurrences less the three inside a `Fail`). **134/134 constants closed under the global context** — 127 DISTINCT names from `Print Module` (which wraps `Record` heads onto their own line) plus the four `Build_*` and three `Chain3` constructors, which it lists after a `:=` where no keyword regex sees them; 30 of the 134 are `Program` obligations. Mind the duplicates: flattening that output makes a keyword regex report 134 OCCURRENCES for those 127 names (`runit` three times, five others twice), so counting occurrences and then adding the seven constructors double-counts to 141 — `Print Assumptions` takes NAMES, and all 134 distinct ones were queried. The `make print-assumptions` gate carries 19 permanently. **TEN negatives in the target**, of THREE KINDS kept lexically apart — 3 TYPING, 2 CONVERSION and 4 FORMABILITY, the tenth being a scope-free instrument check — each stripped once and its whole error read; `Test/ProbeRight358.v` restates the five name-bearing ones from OUTSIDE the target, since an in-file `Fail` renames in lockstep with the constant it guards and so cannot detect a rename. NOT delivered: no composition of such pairs and no transport along equivalences; no preservation results (a mutually-right pair carries colimits of A to limits of X — not stated); no connection to `Adjunction/GAFT.v`, to representability or to Yoneda, so nothing says WHEN a contravariant functor has a partner; and the hom-set and unit/counit presentations are interderivable but their round trips are NOT `eq_refl` (the classes are rebuilt), pinned as the two CONVERSION negatives — whose cause is VALUE-LEVEL RECONSTRUCTION (the trip returns `fmap[S] g ∘ to aor (id)`, a different VALUE) and NOT the donor opacity an earlier draft of the file header blamed, the discriminating experiment having since been run. Also NOT delivered: **no `iffT` packaging**, so the issue's pinned `adjoint_on_the_right_iff_op` exists nowhere — both directions of both orientations are four separate named passages instead, which is what makes the `eq_refl` round trips statable. `make todo` grows by 25 lines, all `Fail`-family probe lines of the kind the already-merged probes contribute, with zero TODO/FIXME/jww added.
- **Adjunction/GAFT.v**: The adjoint functor theorems — GAFT concluded through the in-tree proven universal-arrow assembly (`Theory/Universal/Arrow.v`'s `AdjunctionFromUniversalArrows`): `SolutionSet`, `GAFT_from_initials` (immediate from a family of comma-initial objects), and `GAFT` routed solution-set ⇒ weakly-initial family ⇒ initial object ⇒ adjoint. The weakly-initial crux is the product+equalizer-of-all-endomorphisms Freyd construction (`Theory/WeaklyInitial.v`: `initial_from_weakly_initial`, with the endomorphism-indexed product kept an explicit input so smallness stays caller-chosen); limit creation in the comma `=(d) ↓ U` is `Comma_Complete` (`Construction/Comma/Limit.v`) stated over the honest cone-level `PreservesImageLimit` — the in-tree apex-only `PreservesLimit` is genuinely insufficient (its legs are unconstrained), and every right adjoint satisfies the cone-level form (`right_adjoint_PreservesImageLimit`). SAFT is a GAFT corollary with the well-powered/cogenerator hypotheses packaged as data since the library has no smallness/image-factorization machinery (`Adjunction/SAFT.v`: `SubobjectIndex`, `Cogenerator`, `SAFT`; the separation⇒monic content internalized as `cogenerator_canonical_monic`). (CORRECTION, #451: two parts of that sentence no longer stand. "No smallness machinery" is stale: `Structure/WellPowered.v`'s `WellPowered` states well-poweredness, a small index for the subobjects of each object pinned at or below the hom universe with the exhaustiveness clause `wp_to_from`, and image factorizations are in tree too (`Theory/Subobject/Lattice.v`'s `HasImages_of_OFS`, #445). And `SubobjectIndex` does not package the well-powered hypothesis: it has no exhaustiveness clause, `Adjunction/SAFT.v`'s `empty_SubobjectIndex` inhabits it at every object, and `Adjunction/SAFT/WellPowered.v`'s `WellPowered_SubobjectIndex` goes one way only. `SAFT` also takes the covering datum `SubobjectCover`, which `Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd` refutes at the identity of `Sets`, so SAFT is a GAFT corollary over packaged data that the classical hypotheses do not supply; see the `Structure/WellPowered.v` bullet.) Since #452 the object-level half of SAFT, Mac Lane's special initial-object theorem, is proved over the same `Cogenerator` record and `cogen_prod` (`Adjunction/SAFT/InitialObject.v`, the bullet after the `Structure/WellPowered.v` one), with `SAFT` itself not re-shaped. (CORRECTION, #453: "SAFT is a GAFT corollary" above now describes the constant `SAFT` only. The special adjoint functor theorem over the classical hypotheses, with no covering datum, is `Adjunction/SAFT/Characterization.v`'s `SAFT_wellpowered`, which is NOT a GAFT corollary: it assembles the adjoint from comma-initial objects through the transparent `LeftAdjointFunctorFromUniversalArrows`, not through `GAFT_from_initials`, and its value at each object reads back at `eq_refl` (`SAFT_left_obj`); the GAFT route over the same hypotheses is `Adjunction/SAFT/Characterization/Cover.v`'s `SAFT_cover_wp`, whose covering datum is taken over a product depending on `d` and is proved. `SAFT` itself is still not re-shaped and is kept; see the `Adjunction/SAFT/Characterization.v` bullet.) Indexed products as discrete-diagram limits (`Structure/Limit/Product.v`: `iprod`/`iprod_proj`/`iprod_ump` over `Instance/Discrete.v`'s `DiscreteCat`); a GAFT integration witness reconstructing Δ ⊣ (×) with the reflector shown `≅ Δ` (`Adjunction/GAFT/Examples.v`); `GAFT` ITSELF is applied in `Adjunction/GAFT/Sets.v` — `GAFT_at_Sets_Id : { F : Sets ⟶ Sets & F ⊣ Id }`, the tree's first closed application of the solution-set theorem, all three premises discharged in-tree (`Sets_Complete`; `right_adjoint_PreservesImageLimit (@adj_id Sets)`; the one-member `Sets_Id_SolutionSet`). Labelled a TOY and PROVED to be one — `GAFT_at_Sets_Id_is_Id` shows the produced adjoint is `Id` again — so what is demonstrated is that the three premises are simultaneously inhabited, not that a new adjunction was found; the header discloses that `GAFT`'s `Qed`-frozen universe context pins both categories' hom and proof universes to `Set`, so the application lands at `Sets@{Set _}` (CORRECTION, #451: stale since 2026-09-17, as #451 also records in `Instance/Sets/Complete.v`. `Adjunction/GAFT/Sets.v`'s header now records the `Set` pin as a minimization artifact of `Instance/Discrete.v`'s unannotated `DiscreteCat_Functor`, lifted when that donor was annotated in the PR "algebraic carriers are sets"; re-read on 2026-09-23 under `Set Printing Universes`, `About GAFT_at_Sets_Id` gives `GAFT_at_Sets_Id@{u u0 u1 u2}` with `u0 < u` and no `Set` among its constraints, so the application is at the polymorphic `Sets@{u0 u}`). By-product: `Sets_HasEqualizers := Complete_HasEqualizers Sets_Complete`, **not the first and not the only inhabitant of `HasEqualizers Sets`, and that clause has now been corrected twice** — it read "the tree's only" and then "the only such LIBRARY-file" one, and both are false: measured, THREE library files declare one, this, `Adjunction/CokernelPair.v`'s `SetsEqualizers` and `Adjunction/Diagonal/Finite.v`'s `DiagSets_HasEqualizers`, with `Test/ProbeToposInstances404.v` deriving a fourth, CONDITIONAL one through `topos_HasEqualizers` at `Sets_Topos`; none of the four is compared with any other. Reflective/coreflective subcategories over `Construction/Subcategory.v` with the full-reflective counit-iso lemma (`Construction/Reflective.v`) and the idempotent-monad correspondence both ways plus the Eilenberg–Moore equivalence (`Construction/Reflective/Idempotent.v`: `IdempotentMonad`, reflective ⟺ idempotent monad, `EM ≃` the local subcategory); orthogonal-subcategory localization (`Construction/Localization.v`: `WLocal`, the full subcategory `C_W`, `reflector_inverts_W`) with the universal property up to natural iso (`Construction/Localization/Universal.v`: `localization_universal`, the orthogonal form — calculus of fractions descoped, ledger 15)
- **Theory/WeaklyInitial.v (the characterization), Theory/WeaklyInitial/Sets.v**: Mac Lane §V.6 Theorem 1 as a CHARACTERIZATION (book p. 120, `maclane:V.6:thm1`, with the Awodey §9.8 and Riehl §4.7 clauses) — **the missing direction `weakly_initial_of_initial : Initial C → WeaklyInitialFamily C` (the singleton family at the initial object, indexed by `poly_unit`, written directly so its universe constraint block is EMPTY; index, member and covering arrow read back at `eq_refl` as `poly_unit`, `initial_obj`, `zero`), and the biconditionals `initial_iff_weakly_initial_family (Ps : FreydProducts C) (E : HasEqualizers C) : Initial C ↔ WeaklyInitialFamily C` (the two Freyd products packaged per family, so smallness stays caller-chosen as in the original theorem; Type-valued `↔`, `Defined`, forward half `eq_refl`-equal to `weakly_initial_of_initial`) and `initial_iff_weakly_initial_family_complete (HC : Complete C) (E : HasEqualizers C)` (Awodey's reading: both products harvested from `Complete`, equalizers kept explicit because `Complete_HasEqualizers` lives downstream in Adjunction/GAFT.v; `Complete`'s first two levels are the family's index level, printing `Complete@{u1 u1 u1 u3}` on the biconditional, so the shape's object universe is identified with the family's index universe — the honest "small-complete", no size class; an earlier revision of this bullet quoted `Complete@{u0 u0 Set u2}`, whose third slot, the hom universe, was the literal `Set` inherited from the then-unannotated `DiscreteCat_Functor`, and re-measured on 2026-09-17 after that donor was annotated in the PR "algebraic carriers are sets" the same slot is a free `u1`, identified with the other two rather than pinned)**, appended below the existing development with everything above it left line-identical (two external citations point into it). Riehl's one-object form `WeaklyInitial c : Type := ∀ x, c ~> x` (Definition 4.7.4; the constant safely takes the module's name), `weakly_initial_obj_of_initial`, `wif_of_weakly_initial`, and the relation lemma `weakly_initial_iprod : WeaklyInitial (iprod (wif_obj W) P)` — the inline `wmap` of the original proof, now named; the original theorem stays `Qed`-opaque and is NOT re-proved through it. Weak initiality is strictly weaker than initiality, PROVED at Instance/Parallel.v's walking parallel pair (already in the closure): `Parallel_ParX_WeaklyInitial`, `Parallel_ParX_not_initial`, `Parallel_ParY_not_weakly_initial`. The solution-set vocabulary: `sols_of_wif : WeaklyInitialFamily (=(d) ↓ U) → SolutionSet U d`, the exact converse of `wif_of_sols` (same index, same members), with the special case `sols_of_comma_initial : Initial (=(d) ↓ U) → SolutionSet U d := sols_of_wif … (weakly_initial_of_initial I)`, both appended at the END of Adjunction/GAFT.v (eleven external citations point into it), indices and member at `eq_refl`; GAFT is not made a biconditional (SUPERSEDED: #436's `GAFT_iff` makes it one; this bullet's text is left as written and corrected here). **Witness** (`Theory/WeaklyInitial/Sets.v`, a leaf; closure 53, Adjunction/GAFT/Sets.v 15 at the margin): the full round trip at `Sets` — `Sets_initial_characterization`, `Sets_wif`, `Sets_initial_recovered`, `Sets_roundtrip_iso : initial_obj Sets_initial_recovered ≅ initial_obj Sets_Initial` — at `Sets@{Set u}` as GAFT/Sets.v discloses (CORRECTION, #452: no longer so; `About Sets_initial_recovered` reads `@Initial Sets@{u0 u}` with `u0 < u` and no `Set`, the pin having been lifted with GAFT/Sets.v's); the DERIVED singleton family's index and member at `eq_refl` (nothing about the recovered initial object reads back); docs/INHABITATION.md has the row beside GAFT's. **Stale premises**: every citation in the issue is stale, though not by a single offset (they match 00fc744b; 820201bc shifted the file in two hunks, +13 above the proof body and +19 inside it) — `initial_from_weakly_initial`, `WeaklyInitialFamily`, `endo_absorb` and the uniqueness chase's `assert` have all moved; every absence claim TRUE (`Build_WeaklyInitialFamily` has one use, GAFT.v; no constant named `WeaklyInitial` existed). (CORRECTION, #452: one use when that was measured; `Adjunction/SAFT/InitialObject.v`'s `wif_of_cogenerator` is a second, a grep for `Build_WeaklyInitialFamily` over the comment-stripped `.v` files now finding it there and in `Adjunction/GAFT.v` only.) Two FALSE header sentences corrected in place, line-count-preserving: WeaklyInitial.v ("keeps `Instance/Sets` off this file's closure" — it is in it, TWO hops now that Instance/Parallel.v is a direct `Require`, three before; the `Structure/Limit/Power.v` bullet's one-hop route from Power.v itself is correct and is left alone) and Wide.v (the `Set` pin attributed to `Terminal` and the equalizers — both have empty blocks; it is `iprod`'s, `DiscreteCat_Functor` fixing the shape at `DiscreteCat@{u Set Set}` and `Limit` identifying its hom universe with the ambient's). **Universes**: no `Set` on the Riehl-side constants (`WeaklyInitial@{u u0 u1}`, `wif_of_weakly_initial` and `weakly_initial_obj_of_initial` carry only the Π-type's two `<=`; `weakly_initial_of_initial` ALONE has an EMPTY block, where the composite through `wif_of_weakly_initial` would carry four binders and two constraints); everything consuming `iprod` is over `C : Category@{_ Set Set}`, the donor's pin; a universe refutation at `Cat` does NOT refuse (`Cat`'s hom universe instantiates at `Set`) and is recorded, not shipped. **33 constants closed under the global context, zero `Axioms:` lines** — 17 new heads in WeaklyInitial.v (15 `def`, 2 `prf`), 6 in Sets.v, 5 in GAFT.v, no obligations, the gate carrying all 33 including this file's original five, which it did not carry before; four `Defined`, each LOAD-BEARING (flipped one at a time in a copy of the whole file, the matching `eq_refl` readback stops); WeaklyInitial.v's closure 29 excluding self (28 before; Structure/Complete.v the one new module on the GAFT path, Instance/Parallel.v 0 at the margin); no droppable `Require` in Sets.v and both new ones needed here, though the file's PRE-EXISTING `Theory/Functor.v` and `Theory/Morphisms.v` lines are droppable and are kept so the cited lines do not move; zero collisions for the 28 new names. `Test/ProbeWeaklyInitial435.v` mirrors the `Require` list plus the satellite, Adjunction/GAFT.v and their supplies, binds its category per command, and carries **6 refutation commands = 1 instrument + N1 TYPING (object vs family) + N2-N3 CONVERSION (the index is `poly_unit` not `bool`; `Terminal` vs `Initial`, the `C^op` pivot) + N4-N5 TYPING (the equalizer supply is not the second product; `wif_cover` picks its own index — Riehl's jointly-weakly-initial distinction)**, each stripped one at a time in a copy of the whole file beside its accepted control; seven `eq_refl` readbacks; guard coverage 34/27 with seven exhaustive exceptions; rename-simulated 15 library names, every first break on a positive line; `make todo` grows by those 6 lines only (2277 → 2283 over #434's tip), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: a size class; any re-proof of the two `Qed` theorems; a wide-form biconditional or `HasWideEqualizers` instance; the `Complete`-only biconditional; a witness elsewhere than `Sets`; an `eq_refl` reading of the round trip beyond the family; `weakly_initial_of_wif`; GAFT as a biconditional; the left-adjoint ⇒ solution-set passage that would make it one — BOTH delivered since, by #436's `GAFT_iff` and `solution_set_of_adjunction`; no edit to Structure/Complete.v, Structure/Limit/Product.v, Instance/Parallel.v, Instance/Sets/Complete.v, Adjunction/GAFT/Sets.v, and none to Adjunction/GAFT.v above its last line.
- **Adjunction/GAFT.v (the characterization), Adjunction/GAFT/Sets.v**: Mac Lane §V.6 Theorem 2 (book p. 123) and Awodey §9.8 Theorem 28 — **GAFT as a BICONDITIONAL**: for `U` with complete domain, having a left adjoint is equivalent to preserving limits and satisfying the solution set condition. The sufficient direction was `GAFT` and the necessary one was half there — `right_adjoint_PreservesImageLimit` (`Construction/Comma/Limit.v`) has always supplied continuity — while NOTHING in the tree built a `SolutionSet U d` out of an adjunction, which is the whole substantive gap and is what `solution_set_of_adjunction` fills. It is a SINGLETON: index `poly_unit`, member `F d`, arrow the unit, all three read back at `eq_refl`, and every `h : d ~> U c` factors as `U (ε c ∘ fmap[F] h) ∘ η d` by the triangle identities. No completeness, no preservation, no smallness and no discrete-shape limit enter, so unlike `GAFT` this direction is FREE of the `Set` pin — the accepted control beside probe negative n6 states it at a category with `Set` strictly below its homs, where the biconditional itself is refused ("Cannot enforce Set = p436h"). It is NOT free of every identification: it is stated at `Category@{uo uh uh}`, hom and proof identified and the same level for both categories, which `Adjunction` and `SolutionSet` already carry and this work does not introduce. **Mac Lane's own route is shipped beside it and measured against it**: he exhibits no solution set, he observes that the unit is a universal arrow, hence comma-initial, hence a weakly initial family of one — `universal_arrow_of_adjunction` → `comma_initial_of_adjunction` → `solution_set_of_adjunction_via_comma`, the passage that actually consumes #435's `sols_of_comma_initial`. The chain COLLAPSES: the derived arrow is again the unit at `eq_refl`. The two routes agree on every data field and are NOT the same term (n1). `GAFT_iff` is `Defined`, and its reverse half is `GAFT` ITSELF rather than `GAFT_via_comma_initial` — `GAFT` is `Qed`, the two are not convertible, and building it from the latter stops the readbacks; `GAFT_iff_rev_is_GAFT` and, at `Sets`, `Sets_Id_has_left_is_GAFT_at_Sets_Id` pin that the biconditional's reverse half IS the application `Adjunction/GAFT/Sets.v` already had. The manufactured family at `adj_id` agrees with the hand-built `Sets_Id_SolutionSet` on index, member and arrow at `eq_refl` while NOT being the same record, the covering witnesses differing (n9). **This supersedes two of the #435 bullet's named not-delivereds** — "GAFT as a biconditional" and "the left-adjoint ⇒ solution-set passage that would make it one" — and `Theory/WeaklyInitial.v`'s "GAFT itself does not become a biconditional" is corrected in place, line-neutrally. Two stale citations are corrected the same way: `Adjunction/GAFT/Sets.v` pointed at the wrong site in `Comma/Limit.v` for a constant, and at the wrong one in `Instance/Adjoints.v` for `adj_id`, and the amendment to that file's three-premise paragraph is line-count-preserving, so `Sets_HasEqualizers` stays where nine sites cite it. Measured: 14 new heads in `Adjunction/GAFT.v`, 7 in `Adjunction/GAFT/Sets.v` and one in `Construction/Comma/Creation.v` = **22 constants Closed under the global context**, zero `Program` obligations, all gated; NO new `Require` in any of the three, so `Adjunction/GAFT.v`'s closure is unchanged at 42 (`GAFT/Sets.v` 52, probe 54), and nothing at or above `Adjunction/GAFT.v` moves; three `Defined` in the appended block, each load-bearing in the strongest sense — flipping any one to `Qed` stops the file itself; zero collisions over the 21 names and the probe's 12 (the only hits outside the declaring files are two prose mentions, `GAFT_iff` in the corrected `Theory/WeaklyInitial.v` sentence and `Sets_Id_has_left` in `Adjunction/GAFT.v`'s own header); renaming each of the 19 in the file that DECLARES it and nowhere else, 17 stop the probe with every first break on a positive line and none inside a refutation, and the other two (`solution_set_of_adjunction`, `GAFT_iff`) stop `Adjunction/GAFT/Sets.v` at the lines that consume them, so nothing survives a rename. `Test/ProbeGAFTCharacterization436.v`: 8 refutations = 1 instrument + n7 FORMABILITY (a `SolutionSet` has four fields and no uniqueness clause) + n1, n8, n3, n9 CONVERSION + n4 TYPING + n6 UNIVERSE, each stripped one at a time in a copy of the whole file with the error read, against paired controls; n7 is a reference-not-found, the instrument's own KIND, so the four accepted `Check`s enumerating the record's fields carry the other half of that claim. `make todo` grows by 8, all of them the probe's refutation lines (2296 → 2304), so the issue's "adds no new hits" box is not met as written — disclosed, as in #430–#438. The issue's instruction to route the proof through the comma-creation result is answered by `comma_Complete_via_creation` (`Construction/Comma/Creation.v`), which derives comma completeness from `comma_CreatesAllLimits` and `creates_limits_Complete` — Mac Lane's argument rather than `Construction/Comma/Limit.v`'s direct construction — at zero closure cost in that file. NOT delivered: no rewrite of `GAFT` ITSELF to consume it, which would displace the text eleven external citations point into and would put `Construction/Comma/Creation.vo` into `Adjunction/GAFT.vo`'s closure for no theorem gained, so **#438 remains a paper dependency of `GAFT`'s own proof**; no smallness or size class, `sol_index : Type` remaining caller-chosen; no re-proof of `GAFT` or `GAFT_from_initials`, both still `Qed`; no witness outside `Sets`, the only concrete instance being `Id : Sets ⟶ Sets` through `adj_id`; no `eq_refl` identification of the two routes' solution sets, nor of the manufactured family with `Sets_Id_SolutionSet` as records; and no universe refutation at `Cat`, which was tried and does NOT refuse
- **Adjunction/GAFT/Necessity.v**, **Instance/Ordinal/Large.v**: Mac Lane §V.6 (book pp. 122-123) with Awodey §9.8 Remark 9.30 — **the solution set condition is necessary, and the size discipline that makes it necessary is measured rather than described**. The abstract half is three facts about the constant functor at the singleton, `ConstOne C : C ⟶ Sets`: it is **continuous for every `C` with no hypothesis at all** (the proof ignores its is-a-limit-cone argument, so it proves the stronger statement that EVERY cone maps to a limit cone); it is **representable exactly when `C` has an initial object** (`ConstOne_representable_iff_initial`, both directions — `Hom(c,−) ≅ ConstOne` says every hom-set out of `c` is a singleton, which IS initiality, and `Initial C` is notation for `@Terminal (C^op)`); and therefore over any `C` that is complete-for-small-shapes with no initial object, `ConstOne C` is continuous and not representable, which is the sharpness of #437's `representability_theorem` and of GAFT itself. **The deep point is a universe measurement, and an earlier revision of the header got it backwards.** That revision said the library's `Complete` has "no smallness side condition", hence `@Complete C` already yields `@Initial C`, hence "complete with no initial object" is an EMPTY pair and the counterexample "cannot be stated over `@Complete`". Measured, that overstates a real fact into a false one. `Complete` DOES carry the side condition — the shape's object universe strictly below the ambient's, stated in terms at `Instance/Sets/Complete.v` — and `Complete_Initial` consumes the COLLAPSED instance, i.e. large-completeness. Applying it to each witness in the tree: `Sets_Complete`, `Grp_Complete`, `Ab_Complete` and `SmallOrd_op_Complete` are all **refused**; exactly one is **accepted**, the thin `Subsets_Complete` (`Instance/Powerset.v`). That is Freyd's collapse (`Structure/Complete/Freyd.v`) appearing as a universe constraint — a category with limits over shapes as large as itself has an initial object and is a preorder — so the four `Complete_*` constants are statements about large-completeness, inert on every large category in tree, and the file says so instead of leaving a reader to find out. **Which is exactly why the counterexample must live at small-completeness**, and `Instance/Ordinal/Large.v` is built to sit there. Its ordinals are the Aczel/W-type `osup : ∀ I : Type@{u}, (I → SmallOrd@{u}) → SmallOrd@{u}` with `SmallOrd@{u} : Type@{u+1}`, and the file delivers **the same library class inhabited at one universe instance and refuted at another over one and the same category**: `SmallOrd_op_Complete : Complete@{u u Set u0}` with `u < u0` against `SmallOrd_op_not_large_complete : @Complete SmallOrd_op_Proset → False` at the diagonal instance, with `SmallOrd_self_limit : Limit (Id[SmallOrd_Proset])` as the control proving the diagonal pairing is not uninhabitable in general (its `Limit@{u u1 u0 u1}` has slots 2 and 4 identified, the very pairing the refutation kills on the other side). **Two pieces of the plan were wrong and the construction corrected them**: `osup I f` is NOT the least upper bound of the `f i` — it is the sup of their SUCCESSORS, and `smallord_lub_not_zero` refutes the naive reading — so the LUB is the flattening `ojoin` over the dependent sum of index types, still at `Type@{u}`; and the `≤`/`<` mutual fixpoint the plan proposed cannot pass the guard checker, since `ole` recurses on its first argument and `olt` would recurse on its second, so `ole` is single-argument structural and `olt` a plain definition. **And the universe argument is not where it was expected**: no-greatest-element needs no universe reasoning at all (successors exist, so `SmallOrd_no_greatest` is three lines from `olt_irrefl`); what the level gap buys is the COMPATIBILITY of small-completeness with the absence of a top, recorded as `SmallOrd_no_small_cofinal_family` — no `Type@{u}`-indexed family is cofinal, so `ojoin` provably cannot be applied to the class of all ordinals. Measured: **70 constants gated and all "Closed under the global context"** (7815 → 7885) — 29 `.glob` heads + 1 `Program` obligation for `Necessity.v`, 40 + 0 for `Large.v`. The Solovay half is a stated HYPOTHESIS, not an axiom: complete Boolean algebras are absent from the tree (the single tree-wide hit for solovay/complete boolean/CABA is a prose citation of a DIFFERENT Solovay result at `Instance/FdVect/NoRightAdjoint.v`), so the premise "no free complete Boolean algebra on a countable set exists" is a hypothesis of the theorem and the set-theoretic input is visible as such. Traps: `I` is the Prelude's constructor of `True`, so `match x with osup I f => …` reads `I` as a pattern variable shadowing it; and the three `Universes o h.` blocks in `Necessity.v` are KEPT but are **not** load-bearing — removing all three still compiles, because `Lib.v` sets `Unset Universe Minimization ToSet` project-wide, and an earlier header revision crediting them is corrected. Not delivered: `SmallOrd_Proset` is NOT shown to fail `Small` — the largeness here is a universe-LEVEL fact and refuting `Small` needs non-resizability, a separate argument close to Burali-Forti — so the Awodey hidden checkbox is met in the vocabulary of universe instances rather than of `Theory/Size.v`; no antisymmetry, trichotomy, well-foundedness or ordinal arithmetic, `ole` being claimed only as a preorder; no comparison with `Instance/Omega.v`'s ω or `Instance/Ordinal.v`'s finite ordinals; and no refutation probe under `Test/`, so the two universe-instance boundaries are measured but not guarded.
- **Construction/Reflective/Limit.v**: Mac Lane §IV.3 Exercise 7 (book p. 92, `maclane:IV.3:ex7`) + Riehl §4.6 Proposition 4.6.14(i) and §5.6 Corollary 5.6.6 (printed pp. 171/211, `riehl:4.6:prop14`, `riehl:5.6:cor6`) — **the inclusion of a full reflective subcategory CREATES limits**: `reflective_CreatesLimit R K : CreatesLimit K (Incl C S)` against `Structure/Limit/Creation.v`'s class, quantified as `reflective_CreatesAllLimits`, with Mac Lane's inheritance statement the one-line consequence `reflective_inherits_limits : Limit (Incl C S ◯ K) → Limit K` (the issue's pinned name), Riehl's "in particular" as `reflective_Complete : @Complete C → @Complete (Sub C S)`, and the hypothesis-free `reflective_Incl_PreservesLimitCone` (a right adjoint preserves limiting CONES) — the reviewer's cone-level vocabulary throughout, the apex-only `PreservesLimit` never appearing. **THE ISSUE'S APPENDED CHECKBOX IS STALE ON ITS PREMISE**: it says the tree has no general limit-creation predicate and that a reusable class is the real deliverable, whereas `Structure/Limit/Creation.v` already carries `CreatesLimit` with `creates_limit_lift`, `creates_limits_Complete` and `CreatesLimit_compose`, consumed by six files outside its declaring one (`rg -l 'CreatesLimit\b'` at `35511442` returns seven, the seventh being `Structure/Limit/Creation.v` itself); the class is CONSUMED and no lookalike built — the checkbox's DEMAND, creation rather than inheritance, is what is delivered. The issue's donor lines had drifted as well (`ff_reflects_limit` is `Theory/Equivalence/Limit.v`, `right_adjoint_preserves_limits` `Adjunction/Continuity.v`, `equivalence_creates_limits` `Theory/Equivalence/Limit.v`; the builder's first draft cited a USE site instead, and it was corrected before landing). **THE LIFT COSTS NOTHING, AND THAT IS A MEASUREMENT**: `Adjunction/Continuity.v`'s `rapl_transposed_cone` sits in a section whose `Context (L : Limit G)` it never consumes, so its discharged type is `F ⊣ U → Cone (U ◯ G) → Cone G` with NO limit argument — at the reflection adjunction it IS the created lift, apex `reflector R L` and legs the inverse transposes, both at `eq_refl`. **The only proof content is the comparison isomorphism `Incl (reflector L) ≅ L`** for a limiting cone with apex `L`: forward leg the limit mediator, whose `unique_property` IS the `ConeIso` leg condition (closed by `exact`), backward leg the unit; one inverse law spends the limit's uniqueness clause at the cone against itself, the other spends FULLNESS — the one place it is spent by hand: `prefmap` lifts `η ∘ to` into the subcategory and injectivity of `⌊−⌋` (restated locally in four tactic lines rather than requiring `Adjunction/Additive.v`'s `adj_to_inj`, outside the closure) forces it to `id`. `reflective_counit_iso` does NOT shorten it, structurally (it concerns objects OF the subcategory and `L` is not one) and, independently, because it is `Qed` while producing data (pinned). Reflection is `ff_reflect_ump` in exactly `Theory/Equivalence/Creation.v`'s packaging with `Incl_Faithful` and `Full_Implies_Full_Functor`; the record is a plain literal, term-mode because a tactic proof would demand a `Proof using` clause naming the section's `R`. **Riehl's monadic route (5.3.3 + 5.6.5) is MEASURED and not taken**: `Reflective_IdempotentMonad`, `em_strict_lift`, equivalence creation and `CreatesLimit_compose` all exist, but `Idempotent_EM_Equivalence` is at `MLocal_Subcategory` (the objects where the unit is invertible), not at the given `S`, and neither `Sub C S ≃ Sub C MLocal_Subcategory` nor a `CreatesLimit` transport along `≈` of functors exists (zero hits) — the bridge is not built. The colimit half, 4.6.14(ii), is #434's (`Construction/Reflective/Colimit.v`, landed: `reflective_colimit`, `reflective_Cocomplete` — this bullet said OPEN until then) and is scoped out with the pointer; its ingredients are `lapc_is_acolimit` (`Continuity.v`) and Theory/Equivalence/Colimit.v's transports, which an earlier revision of this clause did not name (the clause is not transcribed into the file because one of its words is a `make todo` pattern hit — the header says so). Strengths strict-first: six `eq_refl` Examples (lift apex and legs, both comparison legs, the created cone and apex); the two routes to `PreservesLimitCone K (Incl C S)` — `creation_preserves_limit` at this record and `right_adjoint_PreservesLimitCone` — agree on mediators at `≈` only (`reflective_two_routes_agree`, one uniqueness appeal), refuted at `eq_refl` both as terms and as mediators and pinned, with no single cause named. STRICT creation (`StrictlyCreatesLimit`) is not reachable BY THIS CONSTRUCTION — `StrictLift` demands a Leibniz apex equality where the construction has an isomorphism — and is not attempted; it is pinned as a TYPING negative. Universes: all 24 constants over `C : Category@{u u0 u0}` in the BINDER with no block equation saying so, `Subcategory@{u u0 u1 u2}` alone forcing it with an EMPTY block (pinned; the category's hom, identity and hom-setoid accepted as controls); the shape-indexed constants carry `u0 = u2`, `u0 = u8`, `u0 = u10` (C's hom-and-proof, J's, the `shom` universe and K's target hom collapse to one) plus `u4 = u11`, `u5 = u9`, each relating a level of `Reflective` to one of `Sub` (`Reflective`'s second to `Sub`'s auxiliary seventh, which `Incl` carries as its sixth; `Reflective`'s third to the object universe of `Sub C S`); the J-with-C collapse is the cone vocabulary's — `Cone`/`ACone`, `Ju ⟶ Vu` and `vertex_obj` accepted while `cone_leg` and `IsLimitCone` are each rejected alone (pinned; the `Structure/Limit/Initial.v` donor family) and `IsALimit`, `Limit`, `ConeIso` and `FCone` likewise (measured out of tree, NOT pinned — the two pinned are a sample of the family, not an enumeration); zero `Set` tokens anywhere. **24/24 + 8/8 constants closed under the global context** (24 glob heads = `Print Module`, no `Program`, no records, so no `Build_*`), zero collisions over 36 names, closure 36 (transitive in-project deps EXCLUDING self; `Theory/Equivalence/Limit` contributes 7 of it, `Creation` 3, `Reflective` 1, every other `Require` 0). `Test/ProbeReflectiveLimit373.v`: 9 `Fail`s = 1 instrument + 8 negatives of THREE kinds (3 conversion, 2 typing, 3 formability), each stripped one at a time with its whole error read, rename-simulated 18/18 — the sixteen first enumerated plus `Limit` and `to`, both named inside a `Fail` and guarded outside, an audit having recounted the denominator — with one vacuous guard found and closed during construction (`cone_leg`); non-vacuity at `TorsionFree_Reflective` with the empty diagram's limit over `Ab_Terminal` built by hand and `Defined` (`Terminal_Limit` elaborates at `Ab` but is `Qed`, so its apex would not reduce), the created apex reading back as `AbModTorsion` of the terminal group at `eq_refl` — read at its strength: the witness shows the assembly runs end to end and reduces, and is DEGENERATE on two axes, the shape being empty (every leg condition is exercised at no index; `IsLimitCone` at shape `0` is terminality) and the terminal group being itself torsion-free (so the reflection is inert up to isomorphism), while `torsion_reflector_obj` holds at an ARBITRARY object, so the carrier readback pins the reflector's definitional object action and not that the reflection did anything; `make todo` grows by 13, all in the probe. NOT delivered: a non-degenerate witness (the only one is at the empty shape and at an object already in the subcategory); strict creation; the colimit half; the monadic bridge; naturality of the comparison in `K`; anything about the reflector's own limits; a completeness witness for `Ab` (so `reflective_Complete` at the witness is a `Check`ed conditional); nothing registered as an `Instance`
- **Construction/Reflective/Colimit.v**: Mac Lane §V.5 Exercise 3 (book p. 120, `maclane:V.5:ex3`), the colimit half of Riehl's Proposition 4.6.14 beside Construction/Reflective/Limit.v's limit half — **colimits in a full reflective subcategory are computed by reflecting the ambient colimit: `reflective_colimit : Colimit (Incl C S ◯ K) → Colimit K` with apex `reflector R (colimit_apex L)` and injections `fmap[reflector R] (colimit_inj L x) ∘ rc_unit_hom (K x)` (the issue's formula, both at `eq_refl`), the cocone-level `reflective_colimit_cocone`, and `reflective_Cocomplete : @Cocomplete C → @Cocomplete (Sub C S)`** (the issue's `reflective_subcategory_cocomplete` is an alias). Six tactic-mode proofs, 17 tactic lines, nine of them the diagram isomorphism's, the headline a term (an earlier revision said "six tactic lines"): the reflector, a left adjoint, carries the ambient colimit to one of `reflector ◯ (Incl ◯ K)` (Adjunction/Continuity.v `lapc_is_acolimit`), the counit isomorphism whiskered along `K` is a diagram isomorphism `reflective_diagram_iso : reflector ◯ (Incl ◯ K) ≈ K`, and Theory/Equivalence/Colimit.v's `isacolimit_transport` moves the colimit across it with the apex untouched. Construction/Reflective.v's `reflective_counit_iso` is `Qed` and is consumed as data, so it is restated transparently (`rc_counit_iso`, inverse leg `rc_unit_hom`) — fullness spent exactly once there, faithfulness not used at all (the limit half needs both); `reflective_reflector_Cocontinuous` for free. **Witness**: `reflective_terminal_shape_colimit` — over any shape with a terminal object the ambient colimit exists in any category (Structure/Limit/Initial.v `terminal_Colimit`), so no cocompleteness hypothesis — instantiated in the probe at `TorsionFree_Reflective` over `Ordinal 2` (two objects and a non-identity arrow; #373's witness is at the empty shape), apex at `eq_refl`; kept out of the library because Instance/Ab/TorsionFree.v costs 33 modules of closure (an earlier revision said 22, the cost with Instance/Ab already present) and would make Construction/ import Instance/Ab; no ambient of an in-tree reflection is shown cocomplete, so `reflective_Cocomplete` at a concrete reflection stays conditional (docs/INHABITATION.md). **Stale premises** (six): `Cocomplete` has three inhabitants (Instance/Sets/Cocomplete.v, Instance/Powerset.v, Instance/Proset/Limit.v — the count when this bullet was written; since #450 three more hypothesis-free ones exist at algebraic categories, `Grp_Cocomplete_via_GAFT`, `Rng_Cocomplete_via_GAFT` and `SVariety_Cocomplete_via_GAFT`, measured by `grep -rn --include='*.v' '^Definition [A-Za-z]*_Cocomplete_via_GAFT'`, three hits (CORRECTION, #454: four hits since #454, the fourth `Instance/Mod/Colimit.v`'s `RMod_Cocomplete_via_GAFT`, measured by the same grep on the #454 tree; `RMod R` is not the ambient of an in-tree reflection either, the `.v` files naming both `RMod` and `eflective` being `Instance/Mod/Limit.v` and `Instance/FdVect.v`, each in a comment citing a `Construction/Reflective/` file) (and since #453 `subsets_saft_cocomplete` and `indiscrete_saft_cocomplete` in `Adjunction/SAFT/Characterization/Examples.v`, by the special adjoint functor theorem at the diagonal), none of them the ambient of an in-tree reflection, so `reflective_Cocomplete` at a concrete reflection is still conditional) and ten files binding it as a hypothesis (grep in binder position), none in GAFT; both "0 hits" greps now hit Limit.v's scope-out paragraph; `equivalence_creates_colimits` is in Theory/Equivalence/Limit.v, not where the issue puts it; the colimit-creation vocabulary exists at Structure/Limit/Creation.v (unused here — a reflective inclusion does not create colimits, probe N4); the site the issue cites in Continuity.v is a comment, with the constants below it; the Reflective.v citation lands on that file's record. **Universes**: no `Set`; every head carries `u0 = u2` (`Subcategory` identifies the ambient's hom and proof levels, Limit.v) and `u0 < u4`; the eight heads of the shape section carry Limit.v's five block equations; the two `Cocomplete`s of the headline share three of four levels, no annotation needed; caps `Basics.compose`/`ID`/`Projections` on every head from `radj`. **20 constants closed under the global context, zero `Axioms:` lines** — 20 `.glob` heads (16 `def`, 4 `prf`), no obligations; the gate carries them; two `Defined`, both LOAD-BEARING (`rc_unit_hom` stops `rc_iso_to_from`, `reflective_diagram_iso` stops the injection readback); four `Qed`; closure 45 excluding self (Structure/Limit/Initial.v 9 at the margin, Theory/Equivalence/Colimit.v 2, Construction/Reflective.v 1, Structure/Complete.v 1, the other twelve `Require`s 0; Limit.v's is 36; the `HasColimitsOfShape` form was left out at +31 against the file as shipped — an earlier revision said +35, measured before the witness's `Require`); zero name collisions. `Test/ProbeReflectiveColimit434.v` mirrors the `Require` list plus Structure/Limit/Creation.v and the witness's three modules and carries **5 refutation commands = 1 instrument + N1 TYPING (the reflected apex is not the ambient apex, `obj[Sub C S]` against `obj[C]`) + N2 CONVERSION (`Cocomplete (Sub C S) = Cocomplete C`) + N3-N4 TYPING (not `PreservesColimitCocone K (Incl C S)`, not `CreatesColimit K (Incl C S)`)**, each stripped one at a time in a copy of the whole file beside its accepted control; six `eq_refl` readbacks; guard coverage 20/16 with four exhaustive exceptions; rename-simulated 9 library names, every first break on a positive line; `make todo` grows by those 5 lines only (2272 → 2277 over #433's tip), so the issue's "adds no new hits" box is not met as written (disclosed). Limit.v's header paragraph on the colimit half and its bullet's "OPEN" clause are corrected in place. NOT delivered: creation of colimits by the inclusion or Riehl §4.6 ex xi's counterexample (a follow-on candidate, surfaced in the PR); naturality of the diagram isomorphism in `K`; `HasColimitsOfShape`; any `Cocomplete` at an ambient with a reflection; strict creation, the monadic route, `Coreflective`; no `Instance`; no edit to Construction/Reflective.v, Adjunction/Continuity.v, Theory/Equivalence/Colimit.v, Structure/Limit/Initial.v or Structure/Complete.v.
- **Adjunction/LeftInverse.v**: Mac Lane §IV.4's left-adjoint-left-inverse (book p. 94, `maclane:IV.4:def-left-adjoint-left-inverse`) and Exercise 4 (book p. 95, `maclane:IV.4:ex4`) — **`LeftAdjointLeftInverse G`**, a left adjoint `F ⊣ G` whose counit is the identity, and the three-way characterization **`lali_characterization`**. **THE DEFINITION CANNOT BE THE EQUATION THE ISSUE SUGGESTS, AND THAT IS A TYPING FACT**: the counit at `a` runs `F (G a) ~> a` (`Theory/Adjunction.v`) while `id[a]` runs `a ~> a`, so "counit ≈ id" — and with it the issue's "≈ nat_id is the usable form" — does not TYPECHECK unless `F (G a)` and `a` coincide at Leibniz equality, pinned in `Test/ProbeLeftInverse376.v` as a TYPING negative (a plain "has type … expected", no "cannot unify", no universe clause). The record therefore carries `lali_obj a : F (G a) = a` as DATA and states `lali_counit a : counit a ≈ id_cast (lali_obj a)` over `Construction/Quotient.v`'s transport kit; the strict-functor-equality phrasing (`F ◯ G` at `Theory/Functor.v`'s `Functor_StrictEq_Setoid`) is the same data rearranged and is not performed. Clause (b) is `LeftAdjointFFInjective` — a left adjoint, `Full`, `Faithful` and **`InjectiveOnObjects G := ∀ a a', G a = G a' → a = a'`**, a condition on `fobj` and not essential injectivity (the reviewer check), new here since the tree's only prior statements of that shape are `GrpAt_Incl_injective_on_objects` (`Instance/Grp/TwoFunctors.v`) and `slice_arrow_reflect`/`coslice_arrow_reflect` (`Instance/Cat/Pullback.v`, under that section's `ObjUIP C`), each for one concrete functor and none a predicate, with closure under identity, composition and right-hand-factor cancellation (an injective `G ◯ F` has an injective `F`) as three `:=` terms; clause (c) is `ReflectiveIsoPresentation` — a `Subcategory`, a `Reflective` on it (`Construction/Reflective.v`, whose `reflective_full` field is the fullness clause), `ri_iso : A ≅[StrictCat] Sub X ri_sub`, and `ri_factor`, Mac Lane's `G = KH` at STRICT functor equality — `StrictCat` forced, since `≅[Cat]` is EQUIVALENCE and would not pin the object assignment clause (a) is about. **THE EQUIVALENCE IS A THREE-LEG CYCLE WHOSE THIRD LEG IS CONDITIONAL, AND THE FILE SAYS SO IN ITS TYPE**: `lali_implies_ffi` ((a)⇒(b)) and `ffi_implies_ri` ((b)⇒(c)) are direct and unconditional with NO UIP anywhere ((b)⇒(c)'s object round trip closes by `destruct` of the membership equation), while `ri_lali_implies_lali` ((c)⇒(a)) additionally takes `LeftAdjointLeftInverse (Incl X (ri_sub Pres))` — the insertion's reflector must fix the subcategory ON THE NOSE, which is clause (a) restated for the insertion — Mac Lane's own step on p. 94 ("For objects a in A ⊂ C we can then choose a₀ = a = Ka and η_{Ka} the identity"), but STRONGER than his: a left-adjoint-left-inverse of `Incl X S` forces `(x; p) = (x; q)` for any two membership proofs over one object (`eq_trans (eq_sym (lali_obj L (x;p))) (lali_obj L (x;q))`, `Incl`'s object action being `` `1 ``), proof-irrelevance of membership that a set-theoretic subcategory has for free and this library's proof-relevant `sobj` does not; a bare `Reflective` yields only the ISOMORPHISM `reflective_counit_iso` (`Construction/Reflective.v`), the identity form being ill-typed there too (the second TYPING negative), and #375's `dense_full_subcategory_reflective` is exactly such a (c)-shaped witness with an iso counit, so the issue's second example is NOT an (a)-instance and no bridge is built. The hypothesis is proved NON-VACUOUS rather than argued: `lali_image_insertion` inhabits it for every presentation arising from clause (a) — Mac Lane's own closing sentence, that the insertion has a left-adjoint-left-inverse — and `lali_cycle` runs (a)⇒(c)⇒(a) with it discharged (`lali_cycle P = P` is refuted at `eq_refl` and pinned, the adjunction being rebuilt through the image subcategory). No unconditional (c)⇒(a) or (c)⇒(b) is claimed, AND BOTH ARE REFUTABLE — an audit compiled the countermodel out of tree, axiom-free: the subcategory of `1` with `sobj := fun _ => bool`, whose insertion is a `ReflectiveIsoPresentation` (constant reflector at `(ttt; true)`, `ri_iso` the identity) with neither a left-adjoint-left-inverse nor an injective-on-objects left adjoint, `true = false` by `discriminate`; it is NOT shipped (no new mathematics after audit), and the gap it exposes — the formalized clause (c) is strictly weaker than Mac Lane's because `Subcategory` membership is proof-relevant, not a missing case distinction, which a decider would not repair — is what the third leg's hypothesis pays for. (b)⇒(a) is argued, not refuted. The route is ASSEMBLY, not re-derivation: (a)⇒(b) directly from the transposition corollaries (fullness by `prefmap h := ⌈h⌉ ∘ id_cast (eq_sym (lali_obj a))`), the one-line `right_adjoint_fully_faithful_iff_counit_iso` alternative measured and DECLINED because requiring `Adjunction/FullFaithful.v` takes the transitive in-project closure from 36 modules to 95 (that file's own is 87; each excluding the measured file); (b)⇒(c) builds the image subcategory `LaliImageSub` (membership `{a & G a = x}`, every ambient morphism retained), `ImageTo`/`ImageFrom` (`Theory/Skeleton.v`'s `Finv` pattern), `ImageIso : A ≅[StrictCat] Sub X LaliImageSub` with both round trips at strict functor equality, and the reflection adjunction through **`strict_equivalence`** — a `StrictCat` isomorphism read as an `EquivalenceOfCategories` whose cells are `id_cast_iso`es BY CONSTRUCTION, which is what makes the counit computable through `equiv_adjunction_counit_at` with no half-adjoint correction (`Theory/Skeleton.v`'s `adjointified` never appears) — then `Adjunction_Compose` and `rs_adj` (an adjunction transported along a strict equality of its right adjoint); (c)⇒(a) is the same three steps in the other order, `lali_along_strict_iso` then `lali_along_right_strict`. By-products stated for reuse: `strict_id_cast_nat`, the missing inverse of `Theory/Skeleton.v`'s `strict_equiv_of_id_cast_nat`, and `strict_equivalence`. Examples: `Id_LALI` (degenerate by design) and **`terminal_LALI`, general** — for ANY category with a terminal object the functor `1 ⟶ C` picking it has `Erase C` as a left-adjoint-left-inverse — instantiated at the walking arrow as `two_LALI`, where `two_lali_moves_TwoX` proves by `discriminate` that `G ◯ F` carries `TwoX` to `TwoY`, so the example is not an isomorphism of categories in disguise. Two donors are rebuilt for measured reasons: `PointAt` duplicates `Theory/Shapes.v`'s `Point`, whose source is pinned at `_1@{_ Set Set}` (rejected at homs declared above `Set` with "Cannot enforce Set = h" while `PointAt` is accepted — pinned; the donor's minimization, `_1`'s three levels being free, not claimed unavoidable), and `TwoY_Terminal` duplicates `Instance/Two/Monoidal.v`'s `Two_Terminal`, whose module would cost 16 modules (36 → 52, measured). Strengths strict-first: sixteen `eq_refl` identifications (fifteen `Example`s plus `image_from_to_obj`); `image_to_from_strict` and `image_incl_strict` MUST be `Defined`, their object families being read back through `` `1 `` in `ii_counit` — with `Qed` it does not close, which is how the need was found and which an audit re-measured one flip at a time; `image_from_to_strict` and `image_factor` are `Defined` for uniformity only, both compiling as `Qed` (measured). Universes measured off BOTH binder and block: `InjectiveOnObjects@{ao ah ap xo xh xp}` is FREE (six binders, no equation, only `Functor`'s bounds), its closure constants inheriting `Id`'s `h = p` and `Compose`'s single hom-and-proof level; `LeftAdjointLeftInverse@{u u0 u1 u2 u3}` is over `A : Category@{u2 u3 u3}`, `X : Category@{u u3 u3}` — ONE level for four slots in the BINDER while the block carries no equation — with THREE donors none of which the record tests in isolation — `Functor` in the REVERSE direction suffices alone (`Au ⟶ Xu` accepted, `Xu ⟶ Au` rejected at hom levels declared apart, both pinned; `lali_left : X ⟶ A` is a field), `Adjunction`'s own block carries `h1 = p1`, `h1 = h2`, `h1 = p2`, and `Functor_StrictEq_Setoid` is declared at one hom level for source and target, the last alone probed in isolation; an audit corrected a draft that called two of them independent on the strength of the `Au ⟶ Xu` control, which discriminates nothing; the packaged `lali_characterization` further identifies the two OBJECT universes (`A X : Category@{u6 u13 u13}`), clause (c)'s doing through `≅[StrictCat]` and guarded as the contrast — at object levels declared apart clauses (a) and (b) are formable while `@Isomorphism StrictCat` and `ffi_implies_ri` are not — with `lali_along_strict_iso`/`lali_image_insertion` keeping the object universes apart (their blocks carry only the inherited `u0 = u2`); exactly five of the 118 constants carry `Set` (`TwoY_Terminal`, `two_LALI` and three readbacks, from `Instance/Two.v`'s `TwoHom : … → Set`), the other 113 `Set`-free. **118/118 constants closed under the global context** — 91 glob heads (63 `def`, 12 `prf`, 13 `proj`, 3 `rec`) plus 24 `Program` obligations and the 3 `Build_*` constructors, all queried fully qualified out of tree; zero collisions over 125 names, instrument-checked; closure 36 (the probe's 44); nothing registered as an `Instance`; no `Fail` in the library file. `Test/ProbeLeftInverse376.v` (318 lines): 14 `Fail`s = 1 instrument + 13 negatives of THREE kinds (3 TYPING, 2 CONVERSION, 8 FORMABILITY), each stripped one at a time with the whole error read and its line confirmed, every target constant a negative names guarded by a control (6/6); `make todo` grows by 18, all in the probe, zero TODO/FIXME/jww. NOT delivered: the countermodel refuting the unconditional (c)⇒(a) and (c)⇒(b) (measured out of tree, not shipped) and the unrefuted (b)⇒(a); `iffT` packaging; uniqueness of the left-adjoint-left-inverse; the dual Exercise 3 (left-adjoint-RIGHT-inverse, #377) and Exercise 5; any bridge to `Construction/Subcategory/Dense.v`; and the strict-functor-equality rephrasing of the definition
- **Theory/Equivalence/Strict.v**: Mac Lane §IV.4 Exercise 3 (book p. 95, `maclane:IV.4:ex3`) — **a full, faithful, surjective-on-objects functor has an adjoint equivalence whose UNIT is the identity, its left adjoint a left-adjoint-RIGHT-inverse**: `ff_surjective_adjoint_equivalence : Full S → Faithful S → SurjectiveOnObjects S → AdjointEquivalence (ff_surjective_left surj) S` (the issue's pinned name), with `ff_surjective_LARI` the record it carries. **SURJECTIVITY IS DATA, exactly as `Adjunction/LeftInverse.v`'s injectivity is a condition on `fobj`**: `SurjectiveOnObjects S := ∀ c, { a & S a = c }` — a chosen preimage WITH its object equation, no choice principle anywhere (`∃` is `sigT`) — universe-FREE with six binders and, measured, character for character the five bounds `InjectiveOnObjects` carries and no equation; its closure constants `_Id`/`_Compose`/`_cancel` mirror the injective ones with the same donors' identifications (`Id`'s `cp = ch`, `Compose`'s single hom-and-proof level in the BINDER), and the cancellation lemma runs the OTHER way (the injective one recovers the INNER factor of `G ◯ F`, this one the OUTER). `surjective_ESO` repackages it as `Theory/Equivalence.v`'s `EssentiallySurjective` (the issue cites, stale), the generic form of `Construction/Grothendieck/RoundTrip.v`'s `RT_EssSurj`, the section reading back at `eq_refl` and both legs of the witnessing iso being `id` at `eq_refl` at an `eq_refl` witness (the whole iso RECORD is not `iso_id`, pinned). **THE DESIGN CRUX IS #376's ONE VARIANCE OVER**: `unit c : c ~> S (T c)` and `id[c]` share a hom-set only when `S (T c) = c` at Leibniz equality, so `LeftAdjointRightInverse S` carries `lari_obj c : S (lari_left c) = c` as DATA and `lari_unit c : unit c ≈ id_cast (eq_sym (lari_obj c))`; `unit c ≈ id[c]` is pinned as a TYPING negative. **THE COUNIT IS AN ISOMORPHISM AND NOT AN IDENTITY, AND THAT IS MAC LANE'S OWN ⟨T, S; 1, ε⟩**: `T (S a)` is the CHOSEN preimage of `S a`, which need not be `a` — `counit a ≈ id[a]` is ill-typed at an abstract `A` (TYPING negative) and `ff_surjective_counit_iso` is what holds; the non-degenerate witness `Erase (Indiscrete bool)` — full, faithful, surjective on objects and NOT injective on objects — has `IndT ttt = true` at `eq_refl` and `indiscrete_counit_endpoints_differ : IndT (S false) = false → False`, so the counit there genuinely connects two distinct objects. Read the probe's recorded trap: at that very witness `counit false ≈ id[false]` IS well-typed and TRUE (proved as the probe control `p377_indiscrete_counit_is_id`), `Indiscrete`'s hom family ignoring its endpoints, so the typing obstruction is stated at an abstract `A` and not at the witness. **WHICH ROUTE COMPUTES IS MEASURED, AND IT IS THE DIRECT ONE**: `ff_surj_adj` is built by `Build_Adjunction'` (`Theory/Adjunction.v`) from the hom-set isomorphism (forward `f ↦ fmap[S] f ∘ id_cast⁻¹`, backward `g ↦ prefmap (g ∘ id_cast)`), so the unit reduces — `ff_surj_to`, `ff_surj_from`, `ff_surj_left_obj`, `ff_surj_left_map`, `ff_surj_counit_unfold` (the counit IS `prefmap (id ∘ id_cast …)`), `ff_surj_lari_left`, `ff_surj_lari_adj`, `ff_surj_equiv_adj` all at `eq_refl`; the ONE `≈`-only step is the unit itself, whose residue `fmap[S] id ∘ id_cast (eq_sym …)` is exhibited at Leibniz equality by `ff_surj_unit_residue` (`unit` is `⌊id⌋`), the strict form refuted and pinned. The alternative route `surjective_ESO` → `FF_ESO_Equivalence` (`FullFaithful.v`) → `Equivalence_to_AdjointEquivalence` (`Adjoint.v`) → `AdjointEquivalence_swap` — the one `Construction/Subcategory/Dense.v` takes — is BUILT as `ff_surj_eso_adjoint_equivalence` and its unit does not reduce at all: the pinned script stops at `equiv_adj_to EquivalenceOfCategories_sym id`, the `symmetry` on `Functor_Setoid` (`Theory/Functor.v`) whose `Equivalence` obligation is `Qed` at — the #375 prediction, now named and pinned, with the same script closing on the direct route as its control; that chain is confirmed constant by constant, though no experiment removes that one step and shows the term then reducing, so it is not claimed to be the sole blocker. The two LEFT ADJOINTS agree on both actions at `eq_refl` and at `Functor_StrictEq_Setoid` with every object component `eq_refl` (`ff_surj_eso_inverse_obj`/`_map`/`_strict`; the whole records refuted, three rebuilt law fields), while the two ADJUNCTIONS do not even share a TYPE (a TYPING negative, not conversion). **`Defined` IS LOAD-BEARING ONCE, MEASURED BY FLIPPING**: `ff_surj_adj` closed with `Qed` stops `ff_surjective_LARI`'s unit obligation with `Found no subterm matching "fmap[?Functor] id{?C}"`; `erase_adj` flipped compiles, and is `Defined` for uniformity. **THE NATURAL CONVERSE IS FALSE, AND THE BRIEF THAT ASKED FOR IT WAS WRONG BY A VARIANCE ERROR**: a left-adjoint-right-inverse of `S` makes the LEFT adjoint full and faithful (`lari_left_Full`, `lari_left_Faithful`, packaged with `lari_surjective` as `lari_left_ff_surjective`) — an invertible UNIT makes `T` fully faithful, and only an invertible COUNIT would make `S` so, which the record does not demand (both halves are already theorems in tree, `Adjunction/FullFaithful.v`'s `left_adjoint_fully_faithful_iff_unit_iso` and `right_adjoint_fully_faithful_iff_counit_iso`, cited not consumed: `lari_left_Full`/`lari_left_Faithful` follow from the first in one line, compiled out of tree, and are re-derived here because requiring that file raises the closure from 38 to 97) — and both clauses about `S` are REFUTED through one engine, `erase_LARI : Initial A → LeftAdjointRightInverse (erase_pt A)` (the left adjoint constant at the initial object): `lari_does_not_imply_Full` at `erase_pt _2` (there is no arrow `TwoY ~> TwoX`, `Instance/Two.v`, while there is one between their images; `Two_Initial` is new), `lari_does_not_imply_Faithful` at `erase_pt Sets` (the identity and the constant `true` on the two-element setoid have equal images), and `lari_does_not_imply_AdjointEquivalence` at the same witness (the counit at that setoid is the unique arrow out of the empty setoid, whose inverse would carry `true` into `False`) — each packaged over a sigma binding the functor ONCE, since two occurrences of a universe-polymorphic constant in one statement are two instances. Only the surjectivity clause transfers, so no `lari_characterization` exists, and the hypothesis "S has a left adjoint" the naive converse would add is REDUNDANT, the left adjoint being constructed. `Sets` is chosen over `Coq` for the two refutations on a measurement — both go through verbatim over either, `Instance/Sets` is already in the closure through `Theory/Adjunction`, and `Instance/Coq` would have added 23 modules for two witnesses (**38** against 61, transitive in-project `.vo` deps excluding self). **Universes measured off BOTH binder and block**: the headline constants carry the one block equation `u0 = u2` (the two categories' hom levels) with both OBJECT universes free and no `Set`, hom identified with proof in the BINDER (`Category@{u u0 u0}`, `Category@{u1 u2 u2}`) with no block equation saying so; `LeftAdjointRightInverse@{u u0 u1 u2 u3}` reads both categories at hom level `u3` in the binder while its block carries only `u3 < u0` and bounds, so the block alone reports no identification; `surjective_ESO`'s identification is `EssentiallySurjective`'s. TWO independent donors are pinned rather than attributed: the hom-level identification is forced by functors in BOTH directions before any adjunction is formed (`Au ⟶ Xu` accepted, `Xu ⟶ Au` rejected at `uh < vh` — the #376 lesson), and hom = proof separately by `Adjunction` (`F ⊣ G` rejected at `wh < wp` while the category, its homs and `Bu ⟶ Bu` are accepted). The WITNESS block is pinned: `erase_left` fixes `_1`'s hom `poly_unit` at `Set` and everything below reads `A : Category@{_ Set Set}` — two repairs measured and both failed (`Unset Universe Minimization ToSet` is INERT here, the printed instances identical with and without it, so no such flag is set; explicit binders on `erase_left` are rejected, its `Program` obligations needing universes outside the declared list) — not claimed unavoidable, and costing the three concrete witnesses nothing. **105/105 constants closed under the global context** — 80 glob heads (63 `def`, 12 `prf`, 4 `proj`, 1 `rec`) plus the 24 `Program` obligations the glob cannot see plus `Build_LeftAdjointRightInverse`, which neither source lists — of which the gate carries the 80 heads; zero collisions over 89 names after ONE real collision was renamed away before landing (`poly_unit_eq` is `Instance/StrictCat/Terminal.v`'s, for the identical statement, and the gate loads both into one scope — hence `poly_unit_all_eq`). `Test/ProbeStrict377.v` mirrors the target's `Require` list and carries **13 `Fail`s = 1 instrument + 12 negatives of THREE kinds told apart by the error TEXT** — 3 TYPING (the two identity readings and the two-adjunctions comparison, each a plain `has type … expected` with no `cannot unify`), 5 CONVERSION (the strict unit on both routes, the whole functor record, the stuck alternative-route script, the iso record), 4 FORMABILITY (the two donors above, each with the record's own rejection beside it — negatives 10 and 12 — which measure the donor rather than the record and say so) — each stripped ONE AT A TIME with the whole error read, every constant a negative names also in a control, rename-simulated 22/22; `make todo` grows by 13, all in the probe, zero TODO/FIXME/jww. NOT delivered: no dual (a right-adjoint-left-inverse, or the coreflective reading of `T`'s image); no comparison of `LeftAdjointRightInverse` with `LeftAdjointLeftInverse` beyond naming the pair; no uniqueness of the left adjoint; no `StrictCat` statement and no `S ◯ T = Id[C]`; no identification of the two routes' ADJUNCTIONS at any strength; no criterion for when a left-adjoint-right-inverse upgrades to an adjoint equivalence; no `Reflective`/`Coreflective`-style packaging; and Exercises 1, 2 and 5 of the same page are separate catalog items, cited not delivered
- **Adjunction/Diagonal/Connected.v**: Mac Lane §IV.4 Exercise 5 (book p. 95, `maclane:IV.4:ex5`) — **over a connected shape the colimit functor is a left-adjoint-left-inverse of the diagonal — but only once the colimits of CONSTANT diagrams have been CHOSEN to be the constants, which is a hypothesis this library cannot discharge from a bare choice of colimits, and the file says exactly where the line falls**. UNCONDITIONAL, over an arbitrary `ConnectedNonempty J` and an arbitrary `L : HasColimitsOfShape J C`: `diagonal_counit_iso` — the counit of `ColimitFunctor L ⊣ Δ[J]` (`Adjunction/Diagonal/Limit.v`'s `Colimit_Diagonal_Adjunction`) is an isomorphism at every constant diagram, its inverse EXHIBITED as the colimit injection at the chosen point (`diagonal_counit_inverse`), with the ZIG-ZAG spent in exactly one lemma per side, `colim_const_inj_agree` (all injections of a constant colimit agree, by `Structure/Limit/Constant.v`'s `leg_zigzag`) and its limit mirror `lim_const_leg_agree` — the POINT `cn_obj K` is used more widely, the exhibited inverse being the injection there — and no essential-uniqueness machinery; `colim_delta_nat` (naturality, needing NO connectedness); `colim_diagonal_iso_Id : ColimitFunctor L ◯ Δ[J] ≈ Id[C]` in `Functor_Setoid`, its component family the counit isomorphism at `eq_refl`; `diagonal_FFI : LeftAdjointFFInjective Δ[J]` — Exercise 4's clause (b), injectivity on objects by `f_equal` at the chosen point — and `diagonal_RI := ffi_implies_ri diagonal_FFI`, clause (c): Δ IS an isomorphism of `C` onto a full reflective subcategory of `[J, C]`; plus `diagonal_fully_faithful_of_counit`, a second route to fullness and faithfulness through `Adjunction/FullFaithful.v`'s biconditional (the two `Full` records NOT compared). **THE DESIGN CRUX IS #376's THIRD-LEG OBSTRUCTION AT THE DIAGONAL**: `LeftAdjointLeftInverse Δ[J]` demands `L (Δ c) = c` at LEIBNIZ equality, the chosen colimit of `Δ c` is only ISOMORPHIC to `c`, and Mac Lane's "can be CHOSEN" re-chooses the colimit of each constant diagram — a per-object case distinction on membership in the image of Δ, `{ c & Δ[J](c) = F }` over functor RECORDS, which a choice function `∀ F, Colimit F` cannot make. So clause (a) is delivered CONDITIONALLY, the hypothesis in the TYPE: `Record ConstantColimits J C` (a family `cc_colim`, `cc_obj c : colim_obj cc_colim Δ[J](c) = c`, `cc_inj c j : colim_inj … ≈ id_cast (eq_sym (cc_obj c))` — a statement about the CHOSEN DATA, checkable at a candidate) and **`colimit_is_lali_of_diagonal (j0 : J) (P : ConstantColimits J C) : LeftAdjointLeftInverse Δ[J]`** (the issue's pinned name), its functor `ColimitFunctor (cc_colim P)` and object equation `cc_obj P` at `eq_refl`, the counit clause DERIVED (`constant_colimits_counit`) from the injection clause at ONE object — so, read exactly, the headline needs a POINT of the shape and no zig-zag once the record is in hand, connectedness being what the unconditional half spends and what makes the record's demand reasonable (section G refutes it over a disconnected shape). `colim_diagonal_strict_Id` is the same comparison at `Functor_StrictEq_Setoid`, which the unconditional half cannot reach (its object clause is refuted at an arbitrary choice and pinned). **THE HYPOTHESIS IS INHABITED, NOT VACUOUSLY**: `terminal_ConstantColimits` — whenever the SHAPE has a terminal object, for EVERY `C`, through `Structure/Limit/Initial.v`'s `terminal_Colimit`, its two PROOF fields at `eq_refl`/`reflexivity` (the third is the chosen family itself), with `terminal_shape_connected` (such a shape meets the exercise's own hypothesis); witnesses `ordinal_lali` (`Ordinal (S n)`, non-degenerate at `n := 1`, `ordinal_two_not_degenerate`) and `two_lali` (`_2`, through `Instance/Two/Monoidal.v`'s pre-existing `Two_Terminal`). DISCLOSED: over such a shape the colimit functor IS evaluation at the terminal object on OBJECTS (`colimit_is_EvalAt`, `eq_refl`; the arrow actions are two different mediator terms, refuted at `eq_refl` and pinned, no `≈` form stated), so these are the inhabitants where Mac Lane's choice is FORCED rather than made; no inhabitant at a connected shape WITHOUT a terminal object is exhibited, and none is claimed. **THE OBVIOUS UNCONDITIONAL CANDIDATE IS REFUTED AT A SHAPE MEETING EVERY HYPOTHESIS**: `EvalAt j : [J, C] ⟶ C` (NEW at the time — since #424, Instance/Fun/Eval.v carries the general `Eval`, whose two data fields agree with this one's at `eq_refl` under this annotation; `EvalAt` is retained, the alias decision surfaced — measured by TYPE, the tree's only evaluation-shaped functors out of a functor category being `YoEvalAt` and `One_Eval`, the latter this one's `J := _1` special case; its explicit universe binders are LOAD-BEARING, the unannotated body identifying the shape's object universe with its hom universe) retracts Δ ON THE NOSE (`EvalAt_retracts`, `eq_refl`) but is NOT left adjoint to Δ: `eval_not_left_adjoint` refutes `EvalAt TwoX ⊣ Δ[_2]` over `Coq` by uniqueness of left adjoints against the terminal choice, at the diagram `TwoGap` carrying `Empty_set` to `bool` — `_2` being connected and inhabited, which is what makes the refutation informative. **THE DUAL NEEDS NO NEW RECORD**: `Theory/Equivalence/Strict.v`'s `LeftAdjointRightInverse S` at `S := LimitFunctor` IS, field for field, "Δ ⊣ Lim, `Lim (Δ c) = c`, unit the cast" — `diagonal_unit_limit_iso` (UNCONDITIONAL, the mirror), `Record ConstantLimits`, `diagonal_is_lari_of_limit`, inhabited whenever the shape has an INITIAL object (`initial_ConstantLimits` via `initial_Limit`; witnesses `one_ConstantLimits` at `_1` and the infinite `omega_lari` at `Omega`); `RightAdjointRightInverse` has zero occurrences tree-wide and none is introduced. **THE REGRESSION (work item 4) HOLDS FOR EVERY CHOICE OF COLIMITS**: over `Two_Discrete` with ambient `Coq`, `two_discrete_counit_not_iso` (through the #367 biconditional against `Structure/Limit/Constant.v`'s `Diagonal_Two_Discrete_not_Full`, so no property of the chosen colimits is used), `two_discrete_no_ffi`, `two_discrete_no_lali`, `two_discrete_no_constant_colimits`, with `two_discrete_objects_distinct` showing the shape is inhabited — so what goes is connectedness, not the point. **THE ISSUE'S "Current state" IS STALE ON EVERY DONOR IT NAMES** (the diagonal adjunction, the constant-diagram results, the connectedness vocabulary and the left-inverse record all landed since), and all are CONSUMED, none rebuilt; two of my own brief's citations were wrong and are corrected in the header (they are the ones for `LeftAdjointLeftInverse` and `terminal_ConnectedNonempty`). Universes measured off BOTH binder and block: every general constant is over `J : Category@{jo jh jh}`, `C : Category@{co ch ch}` (hom = proof in the BINDER, no such equation in any block) with the block equation `jh = ch` and no `Set`; `diagonal_FFI`/`diagonal_RI` carry one equation more, identifying `ConnectedNonempty`'s own universe with the shape's hom level — `Diagonal_Full`'s instantiation, which `diagonal_counit_iso` (consuming the same record through `leg_zigzag`) does not share; `ConstantColimits`/`ConstantLimits` bind ONE level for all four hom/proof slots in the BINDER with no block equation, the binder-versus-block trap again; each identification has three DONORS sufficient alone in a command, probed with `Ju ⟶ Cu` ACCEPTED at the declared levels (so `Functor` is not a cause) — `jh = ch` by `Fun`, `HasColimitsOfShape` and `Diagonal`; hom = proof by `LeftAdjointLeftInverse`, `HasColimitsOfShape` and `Diagonal` — but read those as donors and NOT as independent causes, an audit having measured that `Diagonal` (typed `C ⟶ [J, C]`) cannot be tested apart from `Fun`, that `LeftAdjointLeftInverse` cannot be tested apart from `Adjunction` (itself rejected at hom < proof with no left-inverse in the command), and that `HasColimitsOfShape` fails through `Limit` (`Cone F` elaborates at hom levels declared apart while `Limit F` does not), so the underlying causes are `Fun`, `Limit` and `Adjunction`, the named constants their proxies; `ConstantColimits` is rejected too but says nothing of its own, `HasColimitsOfShape` being one of its fields (the #340 `MonoidObject` trap); exactly EIGHT of 79 constants carry `Set`, all in the `_2`/`Two_Discrete` witness blocks, inherited from those shapes' `Set`-declared hom families. **79/79 constants closed under the global context** — 70 glob heads (48 `def`, 14 `prf`, 6 `proj`, 2 `rec`) plus the 7 `Program` obligations the glob cannot see plus `Build_ConstantColimits`/`Build_ConstantLimits`, which neither source lists — of which the gate carries the 70 heads; zero collisions over 89 names; closure **137** (transitive in-project `.vo` deps excluding self), measured per `Require`: `Adjunction/FullFaithful.v` 20 (paid for the regression's counit-level statement and the #367 cross-check rather than reproving that biconditional), `Structure/Limit/Constant.v` 6, three others 2 each, every other `Require` zero. Both `Defined`s (`colim_diagonal_iso_Id`, `colim_diagonal_strict_Id`) are LOAD-BEARING, measured by flipping each to `Qed` — each alone breaks its `eq_refl` readback; every other proof is `:=` or `Qed`. `Test/ProbeConnected378.v` mirrors the target's `Require` list (plus `Structure/Limit/Unique.v`) and carries **10 `Fail`s = 1 instrument + 9 negatives of THREE kinds told apart by the error text**: 3 CONVERSION (the unconditional apex clause `colim_obj L Δ[J](c) = c`; the counit against `Structure/Limit/Unique.v`'s `colimit_unique_iso` leg, the same morphism up to `≈` as two different mediator terms; the arrow action of `colimit_is_EvalAt`, which an audit found claimed at `eq_refl` in a draft header and refuted), 2 TYPING (the unconditional injection clause `colim_inj L Δ[J](c) j = id[c]` — NOT WELL-TYPED at an arbitrary choice, the two sides having different codomains until the apex equation is supplied, sharper than the conversion rejection a draft predicted; and the arity rejection that the headline does not accept a bare `HasColimitsOfShape` in place of the record), 4 FORMABILITY (the three `jh = ch` donors and `LeftAdjointLeftInverse` for hom = proof) — each stripped ONE AT A TIME with the whole error read, every one of the target's 70 heads under a `Check` control, rename-simulated 13/13 (two guards found vacuous on the first pass and closed); `make todo` grows by 15, all in the probe, zero TODO/FIXME/jww (one prose hit, the word "admitting", was found in the target and reworded). NOT delivered: no unconditional `LeftAdjointLeftInverse Δ[J]` and no impossibility theorem for it; no inhabitant of `ConstantColimits` at a connected shape lacking a terminal object and no proof that none exists; no converse to `terminal_ConstantColimits`; no monad or comonad; no naturality of any identification in `J` or `C`; nothing registered as an `Instance` (a chosen colimit must not become globally resolvable); no relation to `AbsoluteLimit`; no cone-level preservation; Exercises 1 and 2 of the page are other catalog items
- **Theory/Equivalence/Adjoint/Compose.v**: Mac Lane §IV.4 Exercise 2(b) (book p. 95, `maclane:IV.4:ex2`) — **the composite of two adjoint equivalences is an adjoint equivalence, with its unit and counit COMPARED to the constituents' rather than merely shown to exist**: `AdjointEquivalence_Compose A B : AdjointEquivalence (F' ◯ F) (U ◯ U')` (the issue's pinned name), built DIRECTLY on `Adjunction/Compose.v`'s `Adjunction_Compose` — its adjunction field IS that composite at `eq_refl` (`AdjointEquivalence_Compose_adjunction`), its two invertibility clauses the named `adjoint_equivalence_compose_unit_iso`/`_counit_iso` at `eq_refl`. Clause (a) of the exercise is `Theory/Equivalence/Bundled.v`'s pre-existing `EquivalenceOfCategories_Compose` (its two cells, and the bundled `Equivalence_trans`), consumed and not rebuilt; the module is a NEW sibling of `Theory/Equivalence/Adjoint.v` because the issue's suggested placement is an existing file, mirroring how `Adjunction/Compose.v` stands to `Theory/Adjunction.v`. **THE COMPARISON LEMMAS COME IN TWO GRADES THAT ARE KEPT APART**: Mac Lane's whiskering formulas `AdjointEquivalence_Compose_unit : unit ≈ fmap[U] (unit' (F x)) ∘ unit x` and `_counit : counit ≈ counit' y ∘ fmap[F'] (counit (U' y))` hold at `≈` only — supplied by `:=` with no tactic, they are their donors `Adjunction_Compose_unit`/`_counit` applied, and those are `Qed` corollaries proved from `to_adj_unit`/`from_adj_counit`, a rewriting step and not a conversion, so the grade is the donors' (both strict forms refuted and pinned, negatives 4–5); whereas `AdjointEquivalence_Compose_unit_transpose` and `_counit_transpose` hold at `eq_refl` — the composite's unit IS A's forward transpose of B's unit, and its counit IS B's inverse transpose of A's counit — with the two-sided inverses read back at `eq_refl` too (`_unit_inverse`: `η⁻¹ ∘ fmap[U] η'⁻¹`; `_counit_inverse`: `fmap[F'] ε⁻¹ ∘ ε'⁻¹`). That strict form is what the direct route buys. **THE ISSUE'S OWN ROUTE IS BUILT AND MEASURED, NOT DECLINED BY ASSERTION**: `AdjointEquivalence_Compose_via_equivalence := Equivalence_to_AdjointEquivalence (EquivalenceOfCategories_Compose (AdjointEquivalence_to_Equivalence A) (AdjointEquivalence_to_Equivalence B))`, and its premise checks out — the quasi-inverse of the composite equivalence IS `U ◯ U'` on the nose (`_via_quasi_inverse`, `eq_refl`, `quasi_inverse` of a `Build_EquivalenceOfCategories` being its second argument definitionally), so the constant elaborates at exactly the composite's type with no transport; but it is a DIFFERENT adjoint equivalence: whole record, underlying adjunction and unit components are each refuted at `eq_refl` (negatives 1–3) while the direct route's unit reduces as the control. A REASON is measured and labelled a reason, not an isolation: the two cells of `EquivalenceOfCategories_Compose` are `Qed`, so the refined transposes sit behind an opaque constant — yet an out-of-tree experiment rebuilding both cells `Defined` still stops one constructor further in, at the transposing isomorphism itself, so the `Qed` cells are NOT shown to be the sole blocker. Nothing is claimed about `≈`-agreement of the two routes in either direction (an adjunction structure on a fixed adjoint pair need not be unique; no proof, no counterexample). `AdjointEquivalence_Id C` is over `Adjunction_Id` (`Adjunction/Compose.v`, `Instance/Adjoints.v`'s `adj_id` reused): adjunction at `eq_refl`, unit and counit `≈ id` from `Adjunction_Id_unit`/`_counit`, both two-sided inverses `= id` at `eq_refl`. The INVERSE is `Theory/Equivalence/Adjoint.v`'s existing `AdjointEquivalence_swap`, CITED (`AdjointEquivalence_swap_readback` reads its adjunction field back as at `eq_refl`) and not rebuilt — its unit does not reduce, per the `Theory/Equivalence/Strict.v` measurement of that same chain, and a direct reducing swap is not attempted. **THE GROUPOID LAWS COME OUT FINER THAN "NOT STATABLE"**: at the CLASS, associativity and both identity laws are ILL-TYPED — `Compose` of functors is not associative on the nose and `Id` is not a strict unit for it, the `Adjunction/Pare.v` measurement — pinned as TYPING negatives 6–7 (plain `has type … expected`, no `cannot unify`, no universe clause), and no transport is invented; but the two bracketings have definitionally equal OBJECT actions, so the units and counits inhabit convertible types, and there the laws hold ON THE NOSE — six `eq_refl` Examples (`_assoc_unit`/`_assoc_counit`, `_id_left_unit`/`_counit`, `_id_right_unit`/`_counit`). An invertibility calculus for the PREDICATE `IsIsomorphism` rides along — `IsIso_id`, `IsIso_along` (transport along `≈`), `IsIso_comp`, `IsIso_fmap` — universe-FREE with LOAD-BEARING explicit binders (`IsIso_along@{o h p}`'s block is `Category`'s own `h <= p` and nothing else; `IsIso_fmap` keeps its two categories' six levels apart; written unannotated the same bodies minimize to identify hom with proof and are then rejected where the annotated forms are accepted, the probe's controls); `IsIso_fmap` DUPLICATES `Construction/Reflective/Idempotent.v`'s `fmap_IsIsomorphism`, restated on a measurement (that module's closure is 33) with the same statement up to the explicitness of `f` and the universe annotation, and the other three have no in-tree counterpart by NAME — nor by SHAPE: a sweep for `IsIsomorphism (_ ∘ _)`, `IsIsomorphism id` and any respectfulness of `IsIsomorphism` in `≈` finds nothing outside this file, the only `IsIsomorphism (fmap[_] _)` heads being the `fmap_IsIsomorphism` family and hypotheses. **NON-VACUITY IS NOT DEGENERATE ON THE AXIS THAT MATTERS, AND THE AXIS IT IS DEGENERATE ON IS DISCLOSED**: `indiscrete_square` composes `AdjointEquivalence_swap` of `Theory/Equivalence/Strict.v`'s `indiscrete_adjoint_equivalence` with that equivalence itself, an adjoint equivalence of `Indiscrete bool` with itself whose functor `IndT ◯ Erase (Indiscrete bool)` MOVES an object (`false ↦ true` at `eq_refl`, `indiscrete_square_not_id` by `discriminate`), so it is not an identity adjoint equivalence and does not even have the TYPE of one (TYPING negative 8); but `Indiscrete`'s hom family ignores its endpoints (`Instance/Discrete/Reconstruct.v` declares `hom := fun _ _ => unit`, the stdlib `unit : Set`, which is also where the witness block's `Set` comes from), every hom-set there being a singleton, so the MORPHISM-level statement "the unit at `false` IS the identity" is well typed and TRUE, at `eq_refl` and not merely at `≈`, and a morphism-level non-degeneracy statement is unavailable — the trap `Theory/Equivalence/Strict.v` records for its own counit; the object-level statement is the honest one and the only one made. **Universes measured off BOTH binder and block**: every adjunction-facing constant is over `Category@{u u0 u0}` in the BINDER with no such equation in any block; the composite carries `u0 = u2`, `u0 = u4`, `u2 = u4` (the three categories' hom-and-proof levels collapsed to one) and `u5 = u7`, all three OBJECT universes free; `AdjointEquivalence_Id` carries no equation at all; the witness block carries `Set < u3` through `Strict.v` from `Instance/One.v` and `Instance/Discrete/Reconstruct.v`. Donors probed with controls at the very same levels (negatives 9–13): **the IDENTITY FUNCTOR `Id` forces hom = proof ON ITS OWN** — easy to miss, and it means a probe writing `Id[Cu]` into an `Adjunction` measures `Id` and not `Adjunction`; the discriminating control is an arbitrary endofunctor `Cu ⟶ Cu`, which IS accepted, so `Functor` is not a donor — `Adjunction` is a donor independently (probed at two arbitrary endofunctors so no `Id` occurs in the command), `AdjointEquivalence` is a third that CANNOT be tested apart from `Adjunction` (its first field is `F ⊣ U`), `AdjointEquivalence_Id` has both in reach and isolates neither, and the composite's hom collapse is forced by functors in BOTH directions before any adjunction is formed (`Au ⟶ Bu` accepted, `Bu ⟶ Au` rejected under `ah < bh`). None claimed unavoidable. **PRIOR ART MEASURED WITH THE CRITERION STATED, because the count moves with it**: the bare word `AdjointEquivalence` occurs in FIVE files at the base commit INCLUDING its declaring one (`Adjunction/FullFaithful.v` prose, `Structure/Monoidal/Dual.v`, `Construction/Subcategory/Dense.v` and two inhabitants in `Theory/Equivalence/Strict.v`), the substring in EIGHT (`Theory/Equivalence/Adjunction.v` prose, `Creation.v` and `Limit.v` consuming `AdjointEquivalence_swap_adjunction` without naming the class); under either criterion none composes two and none exhibits an identity one, so the issue's "three files" is stale both ways, and the strings `AdjointEquivalence_Compose`, `AdjointEquivalence_Id` and `AdjointEquivalence Id` occur nowhere else. **46/46 constants closed under the global context** — 40 glob heads (39 `def`, 1 `prf`; no `inst`/`proj`/`rec`) plus the 6 `Program` obligations of `IsIso_id`/`_comp`/`_fmap`; no `Record`/`Class`/`Inductive`, so no `Build_*`; zero collisions over the 52 heads-plus-probe names; closure 39 excluding self (26 without the witness section's three requires — all 13 arrive with `Theory.Equivalence.Strict`, `Instance.One` and `Instance.Discrete.Reconstruct` costing nothing on top and being required only for the names `Erase` and `Indiscrete`; no single general-theory require costs anything alone; donors 22/20/20), probe 40; four `Defined` lines (the obligations of `IsIso_comp`/`IsIso_fmap`), NONE load-bearing — all four flip to `Qed` with every `eq_refl` readback intact, measured — the one `Qed` being `indiscrete_square_not_id`, which produces no data. `Test/ProbeAdjointCompose379.v` mirrors the target's `Require` list and carries **14 `Fail`s = 1 instrument + 13 negatives of THREE kinds** (5 CONVERSION, 3 TYPING, 5 FORMABILITY), each stripped one at a time with its whole error read, every constant a negative names guarded outside a `Fail` — including the identity functor `Id`, which the builder's cut named only INSIDE negative 9 so that renaming it left that negative silently green, found by audit and closed with a top-level `Check @Id.` that now breaks the probe under the rename — rename-simulated 6/6 over the target constants the negatives name; `make todo` grows by 16, all in the probe (14 `Fail` commands + 2 prose lines), zero in the library, zero TODO/FIXME/jww. NOT delivered: no direct reducing swap; no `≈` comparison of the two routes; no uniqueness of the composite; no category of categories and adjoint equivalences, so the groupoid laws are not packaged as a structure; no transport of an adjoint equivalence along a natural isomorphism of either adjoint; no 2-categorical reading and no relation to `Theory/Bicategory/Adjunction.v`; and no witness at three genuinely distinct categories
- **Adjunction/Diagonal/Coproduct.v**: the coproduct bifunctor `+(C) : C ∏ C ⟶ C` (dual to Functor/Product/Internal.v's `×(C)`) and the covariant adjunction `+(C) ⊣ Diagonal_Product C` — stated in `C` with no residual `^op`, mirroring the transposes of Adjunction/Diagonal/Product.v — together with named unit and counit (`coproduct_unit := (inl, inr)`, `coproduct_counit := id ▽ id`) each identified with the generic `unit`/`counit` of Theory/Adjunction.v, the counit identification being a definitional check
- **Adjunction/FunctorCategory.v**: Mac Lane §V.5 construction 1 and Remark 1 (book p. 119, `maclane:V.5:construction1`, `maclane:V.5:remark1`) + Riehl §4.3 Proposition 4.3.6 with Exercises 4.3.ii/iii (`riehl:4.3:prop6`, `riehl:4.3:exii`, `riehl:4.3:exiii`) — **an adjunction `F ⊣ G` lifts to functor categories both ways: `postcompose_adjunction : postcompose F ⊣ postcompose G` and `precompose_adjunction : precompose G ⊣ precompose F`, and Mac Lane's square of adjoint pairs gives `Lim_commutes_right_adjoint : LimitFunctor LX ◯ postcompose G ≈ G ◯ LimitFunctor LA` by ONE application of `right_adjoint_iso`** (uniqueness of right adjoints, not a re-proof). **Stale premises**: "`F ◯ −` is not defined anywhere" FALSE — `postcompose` IS Functor/Construction/Postcompose.v's `Postcompose` at a fixed shape and `precompose` IS Theory/Kan/Extension.v's `Induced` (both at `eq_refl`, `postcompose_obj`/`_component`, `precompose_obj`/`_component`; neither is a new functor, work item 1 was #318's); "`Δ ⊣ Lim` only binary" STALE since #353 (Adjunction/Diagonal/Limit.v `HasLimitsOfShape`, `LimitFunctor`, `Diagonal_Limit_Adjunction`, `lim_counit` — the counit IS the limiting cone); work item 3's square `F^J ∘ Δ = Δ ∘ F` is NOT an equality of functor records (`fmap[F] id` against `id`, probe N1) but the identity-component `square_iso : postcompose F ◯ Δ ≅[Fun] Δ ◯ F`. The CONSTRUCTED unit and counit are the whiskered originals ON THE NOSE (`postcompose_unit_component`, `postcompose_counit_component` at `eq_refl`; their naturality is `Adjunction_to_Transform`'s two `Transform` fields, Adjunction/Natural/Transformation/Universal.v, nothing re-derived), while the delivered adjunction's own `unit`/`counit` accessors carry a `fmap_id` residue and agree at `≈` only (`postcompose_adjunction_unit`/`_counit`, `precompose_adjunction_unit`/`_counit`; the `eq_refl` form is refused, probe N5 — an earlier revision glossed the constructed readbacks as the reviewer's condition on the nose); the precomposition half has components `fmap[S] (η c)` / `fmap[T] (ε d)` and reverses direction (probe N3) — the mirror construction written out, nothing by duality. The remark's readings: `Lim_commutes_nat_iso` (`≅[Fun]`) and the pointwise `lim_of_right_adjoint T : lim_obj LX (G ◯ T) ≅ G (lim_obj LA T)`; the routes `left_route`/`right_route` by `Adjunction_Compose`, `left_route'` by `adjunction_along_left_iso`. **The cone-level clause** `right_adjoint_carries_lim_counit T : IsLimitCone (FCone G (limit_cone (LA T)))` (legs = the `Δ ⊣ Lim` counit's components, `carried_leg_is_counit` at `≈`) is Adjunction/Continuity.v's DIRECT RAPL (an earlier revision pointed elsewhere in that file), NOT derived from the isomorphism: `right_adjoint_iso` is `Qed`, so the remark's components cannot be identified with the canonical comparison — Mac Lane's remark is delivered as the isomorphism it states, and the limit preservation it restates comes from the existing theorem (disclosed in the header). **Relocation**: `adjunction_along_left_iso : F ≈ F' → F' ⊣ G → F ⊣ G` lived only in Instance/Mod/Bimodule.v (#401), whose header said it "belongs beside Theory/Adjunction.v"; requiring Bimodule.v here would add 44 files (an earlier revision said 48), so the `AdjunctionAlongIso` section is moved verbatim (still `Defined`) to the end of Theory/Adjunction.v — it needs a section-local `Obligation Tactic := idtac` (Bimodule.v's file-wide setting; under Lib's global `cat_simpl` the target file stalls) — with Bimodule.v's figures corrected in place (427 constants, 213 `Qed`, five `Defined`, 82 `eq_refl`; 440/220/six/83 before), its gate entries requalified, a pointer comment at Bimodule.v and one repointed citation (the Instance/InnerProduct/Galois.v bullet). **Universes**: every block identifies the hom levels of the shape and both categories (`u0 = u2`, `u0 = u4`; the two naturality lemmas carry `u2 = u4` alone) — Instance/Fun.v's `Fun` pin plus `Postcompose`'s/`Induced`'s own, bare = annotated, nothing minimizes, no `Set`; caps by `About` on the donors: `prod_rect` first carried by `square_at`'s own obligations (inherited by `square_iso`, `square_equiv`), `eq_rect_r` from `Diagonal_Limit_Adjunction` (into `left_route`, `right_route`) and `lim_counit_is_limit_leg` (into `carried_leg_is_counit`), `Logic_lemmas.equality` + `prod_rect` from `adjunction_along_left_iso` into `left_route'` and the three remark constants; `right_adjoint_iso` carries none. **71 constants closed under the global context, zero `Axioms:` lines** — 39 `.glob` heads (32 `def`, 7 `prf`) plus 32 `Program` obligations; the gate carries the 39 heads, the issue's two names among them; NO `Defined` (every construction a `:=` or `Program` record; 40 `Qed` = 32 obligations + 8 lemmas, none hiding anything that computes — the eight `eq_refl` readbacks reduce through `:=` definitions alone); closure 74 excluding self (Adjunction/Diagonal/Limit.v 31 at the margin, Functor/Construction/Postcompose.v 8, Adjunction/Compose.v 2, Adjunction/Continuity.v 2, Universal.v 1, fourteen `Require`s 0); `postcompose`/`precompose` occur as WORDS in the prose of 3/8 files, never as declarations, the other 37 names 0. `Test/ProbeFunctorCategory431.v` mirrors the `Require` list and carries **6 refutation commands = 1 instrument + N1 and N5 CONVERSION (the square as functor records; the adjunction's own `unit` accessor at `eq_refl`) + N2-N3 TYPING (each induced adjunction has one direction) + N4 UNIVERSE (`[Jw, Xw]` at `jh < xh`: "Cannot enforce xh = jh because jh < xh" — `Fun`'s pin, not this file's)**, each stripped one at a time in a copy of the whole file beside its accepted controls; six `eq_refl` readbacks; guard coverage 29/25 with four exhaustive exceptions; rename-simulated over the seven library names the refutations use (`postcompose`, `precompose`, `postcompose_adjunction`, `precompose_adjunction`, `Diagonal`, `eq_refl`, `transform`; module-path components and section binders excluded), every first break on a positive line (an earlier revision said 11/11 over an unnamed set); `make todo` grows by those 6 lines only (2255 → 2261 over master 1be50678; the relocation adds none), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: a concrete instance (every result is conditional on an abstract `F ⊣ G`; docs/INHABITATION.md not touched); the passage from the remark's isomorphism to limit preservation; a transparent uniqueness-of-adjoints; the right-hand transport `G ≈ G' → F ⊣ G → F ⊣ G'`; (co)continuity of `postcompose`/`precompose`; Riehl's Exercise 4.3.iii hom-set route as a separate constant; no edit to Functor/Construction/Postcompose.v, Theory/Kan/Extension.v, Adjunction/Diagonal/Limit.v or Adjunction/Compose.v.
- **Theory/Lawvere.v**: Lawvere theories — the `LawvereTheory` class mirroring the PROP class's relaxed shape (`law_of_nat` naming function + propositional strictness equalities on OBJECTS; bijectivity deliberately not required, consumers state reachability), with `law_pow`/`law_pow_one`; the skeletal base `FinSetOp_Lawvere` on `FinSet^op` whose strictness fields compute by `eq_refl` even at open variables (Instance/FinSet/Lawvere.v); models as Cartesian+Terminal-preserving functors with the model category `Models T C` a full subcategory of the functor category, pack/unpack mutually inverse (Theory/Lawvere/Model.v); Sets-models with the underlying-set functor `ev1` and `ev1_Faithful` under an explicit reachability hypothesis — the honest price of the relaxed class, trivially witnessed on the base by `FinSetOp_reach` (Theory/Lawvere/Sets.v); the finitary-monad connection hypothesis-scoped over a given left adjoint `L ⊣ ev1` — `Lawvere_Monad` via the transparent induced monad, `Lawvere_EM_Comparison`, and `Lawvere_crude_monadicity` re-exposing the crude-monadicity hypotheses verbatim (full equivalence ledger 2; Theory/Lawvere/Monad.v); the PROP-spine bridge — cartesian monoidal/braided/symmetric structure via the CC_* instances and the interpretation tower `Lawvere_PROP_interp` (+Monoidal/Strict/Braided/Symmetric) from the FreePROP universal property under a disclosed strictness hypothesis pack (`coh := eq_refl` on skeletal instances, remaining equalities by nat induction), with the Fox-theorem pointer joining the cartesian and copy/discard spines (Theory/Lawvere/PROP.v)
- **Theory/Multicategory.v**: Symmetric multicategories with zipper single-slot composition — `mcomp {Γ₁ Γ₂ Δ b c} : mhom (Γ₁++b::Γ₂) c → mhom Δ b → mhom (Γ₁++Δ++Γ₂) c` (no heterogeneous-list telescopes), boundary casts `mcast` with the groupoid pack and any-proof law variants, unit laws through a named splice-equation kit, both associativity shapes (nested + disjoint over their boundary equations); the symmetric action `msym` indexed by the Type-valued `tperm` witness (plan erratum: stdlib `Permutation` is Prop with no elimination into Type; `tperm_Permutation` bridges back) with a disclosed SCOPE note (witness-indexed action; symmetric-group descent and slot-crossing equivariance deferred — every in-tree instance is expected to satisfy both, though neither is proven in-tree). Multifunctors with the heterogeneous setoid (Theory/Multicategory/Functor.v); the representable multicategory of a symmetric monoidal category via `tensor_list`/`tfold` folds and a braid-built `perm_arrow`, developed once over an object realization `ob : A → C` — `RepresentableMulticategory` threads UIP-on-object-lists (Grothendieck/Strict precedent) and `ColouredPROP_Multicategory` derives it from the colour decider by axiom-free Hedberg (Theory/Multicategory/Representable.v); the endomorphism operad `pow`/`EndOperad` with the genuine fork-algebra braid action and nat-level UIP normalization (Theory/Multicategory/Endomorphism.v); operads as one-object multicategories with `ohom` accessors and the two-sided presentation round trip (Theory/Multicategory/Operad.v); operad algebras `OperadAlgebra := MultiFunctor` into `EndOperad` with the convenience constructor (+ `alg_act_Build` sanity), the category `OperadAlgebras`, and the Comm example — algebras of the terminal operad in Sets are commutative monoids, both directions, connecting Instance/CMon.v (Theory/Multicategory/Algebra.v); an earlier revision of this bullet stated the forward direction unconditionally, and measured on 2026-09-17 after the PR "algebraic carriers are sets" a `CMonObject` carries `cmon_prop`, which a bare `X : Sets` does not supply, so `Comm_algebra_to_CMon` takes a `PropEquiv (is_setoid X)` and `Comm_algebra_CMon`'s left component reads `∀ (X : Sets), PropEquiv (is_setoid X) → OperadAlgebra Comm X → CMonObject`, the reverse direction `CMon_to_Comm_algebra` being unchanged since a `CMonObject`'s carrier supplies its own
- **Functor/Product/Fixed.v**: Mac Lane §I.4's fixed-factor product — for any cartesian `C` and fixed `H`, the functor `H × −` (`fixed_product_functor`, object map `G ↦ H × G`, `fmap := second`, laws exactly `second_id`/`second_comp`), and for `f : H ~> K` the transformation `H × − ⟹ K × −` with components `first f` (`fixed_product_transform`, whose `naturality` is `symmetry (first_second f g)` and whose `naturality_sym` is `first_second f g`, in that orientation), packaged up to the currying `fixed_product_curried : C ⟶ [C, C]`. The binoidal comparison is stated as proof, and it runs OPPOSITE to the obvious guess: this tree spells the product of morphisms `split` = `bimap[×(C)]`, and the composite `Monoidal_Binoidal ∘ Cartesian_Monoidal` — which the tree already reaches for a cartesian category *with a terminal object* — hits Mac Lane's `f × id` and `id × g` ON THE NOSE (`alt_is_inj_left`/`alt_is_inj_right`, by `reflexivity`), whereas this file's `first`/`second` match only up to `≈` (`fixed_product_bimap`, `fixed_product_transform_bimap`, `fixed_product_inj_right_fmap`). The components are `first f` because issue #240 prescribes them verbatim; `alt_transform` carries the `split f id` spelling alongside, related by `alt_transform_agrees`. The SOLE reason for the terminal-free `Cartesian_Binoidal` is the hypothesis count — `Class Cartesian` carries no terminal object — and its `inj_right`/`inj_left` corollaries are instances of `FromAFunctor_ToAFunctor` (Theory/Functor.v), true of every functor, recording only that the packaging round-trips. Non-degeneracy is proven, not assumed: `fixed_product_transform_faithful` (distinct `f` give distinct transformations, no terminal object needed) and `fixed_product_transform_reflects_id`. Instantiated at `Grp` over the pre-existing `Grp_Cartesian` (`Grp_fixed_product`, `Grp_fixed_product_transform`) with a non-degenerate witness: the zero endomorphism of Z/2, proved to have no left inverse and hence `Grp_Z2_zero_not_iso : IsIsomorphism Grp_Z2_zero → False` (the morphism-level predicate — the object-level `Isomorphism Z2 Z2 → False` would be FALSE, by `iso_id`), to induce a transformation that is not `nat_id`, and to have a component that moves `(true, true)` and is not monic
- **Theory/Size.v**: Size and foundations vocabulary (Mac Lane §I.6, Awodey 1.11/1.12, Riehl 1.1.6/1.1.7) — `LocallySmall` and `Small`, predicates the tree had never declared (`Theory/Lawvere/Sets.v` records the gap). Both are relative to levels. ALL of `Small`'s content is in its strict `uo < o, uh < h`; `Test/Size.v` writes out the trivial discharge and shows it REJECTED, in two separate commands because the combined one is refused inside its hom sub-term and so says nothing about `uo < o` on its own. `LocallySmall` is deliberately NON-strict, so `locally_small_ambient` witnesses it for EVERY category — Riehl 1.1.7's "each is locally small" as a lemma, recording that `Class Category`'s `homset` field builds it in; the converse "not small" half is NOT statable, its negation being a metatheoretic claim. Objects resize up to **`ObjEq`**, a level-polymorphic identity type declared in-file — NOT stdlib `=`, which is pinned to a global universe on 8.19/8.20 and cannot be mentioned under an explicit universe-constraint clause; homs resize up to a genuine setoid isomorphism (both directions respect `≈` — `ls_from_resp` is a field because it is not derivable, and without it the structure is only a section/retraction pair). Riehl 1.1.6's unindexed packaging is `ArrowQuiver`/`ArrowQuiverOfCat`/`TotalMor` (no reflexive quiver existed in tree; the tree's `Quiver` is indexed with no identity selection), where the dom/cod retractions hold by `obj_refl` — a book axiom made definitional rather than proved; both packagings are supplied but their EQUIVALENCE is not established. **Erratum corrected**: `Check (Cat : obj[Cat])` SUCCEEDS — universe polymorphism instantiates the two occurrences at different levels, so it is the Cat/CAT tower (Riehl §1.3) rather than self-membership, and the pinned-instance form `obj[Cat@{a b c d e}] := Cat@{a b c d e}` is what is actually rejected; the `Sets` case is an ordinary type error (`obj[Sets]` is `SetoidObject`), not a universe inconsistency. Full mapping in **docs/SIZE.md**; the Awodey §8.3 Yoneda size statements are documented there but NOT formalized.
- **Lib/Setoid/Propositional.v**, **Instance/Sets/Propositional.v**, **Test/ProbePropEquiv.v**: **a setoid whose equality is logically a PROPOSITION — the property, its transports, and the reason it is a property rather than a redefinition** (the PR "algebraic carriers are sets", 2026-09-17, its first phase). `Class PropEquiv@{u p} {A : Type@{u}} (S : Setoid@{u p} A) := { pequiv : A → A → Prop; pequiv_to : ∀ x y, pequiv x y → x ≈ y; pequiv_from : ∀ x y, x ≈ y → pequiv x y }` (`Lib/Setoid/Propositional.v`), measured by `About` under `Set Printing Universes` at `PropEquiv@{u p} : Setoid@{u p} A → Type@{max(Set+1,u,p)}` — AT the carrier universe for every `Setoid@{u u}`, which is the entire point: a `Prop`-valued relation on a carrier in `Type@{u}` is itself a type in `Type@{u}` while a `Type`-valued one lands in `Type@{u+1}`. That arithmetic is stated once and authoritatively in the size note in the header of `Structure/Complete.v`; this file is its implementation, and any disagreement is to be resolved in that note's favour. **`≈` IS NOT CHANGED, ANYWHERE**: `Class Setoid`'s `equiv` stays a `crelation` (`Lib/Setoid.v`), because in `Cat` an `F ≈ G` is a family of isomorphisms (`Theory/Functor.v`) and truncating it would discard data — `Test/ProbePropEquiv.v` pins that `Functor_Setoid` has no such `Prop` mirror, that a GENERIC `X : SetoidObject` supplies none either, and records why there is no `LocallyPropositional Sets`. **The three fields are FUNCTIONS, so no `Prop` is eliminated to build one**: `pequiv_to`/`pequiv_from` convert both ways, and the single elimination the property buys is `pequiv_elim_inhabited : inhabited (x ≈ y) → x ≈ y`, which is what makes every propositional truncation in the PR reversible at a propositional carrier. Constructor and instances: `PropEquiv_of_relation` for a setoid whose `equiv` IS a named `Prop` relation — the sort ascription `(x ≈ y : Prop)` is REFUSED even when `≈` is `eq`, since Coq judges the sort of `equiv x y` from the declared type of `equiv` (a negative with its own control) — together with instances for every setoid constructor of `Lib/Setoid.v`: `eq`, `unit`, `Fin`, `nat`, the pointwise function setoid, product, sum, option, list. `Instance/Sets/Propositional.v` transports the property through `Sets`: `PropEquivObj`, the hom-setoid pointwise into the target (`hom_PropEquiv`), binary products, indexed products (`iprod_PropEquiv`) and the vertex of `Instance/Sets/Complete.v`'s limit (`limit_PropEquiv`), each with an `eq_refl` readback that `pequiv` is the pointwise relation; and it declares `Class LocallyPropositional C := ∀ x y, PropEquiv (@homset C x y)` with the two generic builders `LocallyPropositional_of_relation` and `LocallyPropositional_of_eq`. **Measured on 2026-09-17**, criteria stated: 66 `.v` files mention `PropEquiv` (`grep -rl --include='*.v' PropEquiv .`); NINE constants inhabit `LocallyPropositional` (`grep -rn --include='*.v' -E '(Program )?(Definition|Instance) [A-Za-z_0-9]*_?LocallyPropositional' .`) — `Grp`, `Ab`, `CMon`, `RMod R`, a delooped rig, `Matr R`, the two generic builders, and the one the reverse direction of `Construction/Enriched/Ab.v` PRODUCES; and every constant of both files is "Closed under the global context" and gated. **SCOPE, deliberately narrow**: `PropEquiv` is a property of a SETOID and never of `Class Setoid`; internal algebra in an arbitrary category (the `Theory/Algebra/` files) keeps the ambient `≈` and carries no such field; and where an arbitrary category's hom-setoid must be read as an algebraic object, `LocallyPropositional` is taken as a HYPOTHESIS — it is never a theorem about `Sets` or about `Cat`, and neither is one
- **Theory/Subobject/Lattice.v**: Mac Lane §V.7 Definition 2 (book p. 126, `maclane:V.7:def2`), Riehl 2nd ed. Definition 4.7.9 (p. 177, `riehl:4.7:def9`) and Fong–Spivak §1.2.1 (p. 8, `7sketches:1.2.1:def-setops`/`:ex11`), issue #445 — **the lattice of subobjects as ORDER STRUCTURE on `Theory/Subobject.v`'s factorization preorder `sub_le`: `sub_meet` (the chosen pullback of the two monos, mapped in through the first) below both factors with the greatest-lower-bound property `sub_meet_is_glb` (the issue's pinned name) and `sub_meet_IsMeet`; `sub_wide_intersection S j0 W` over a `WidePullback` of the family's monos with Riehl's three clauses (`wide_pullback_proj_monic` = the legs are monic, the `sub_is_monic` field = the induced map is monic, `sub_wide_intersection_IsIntersection` = maximal among subobjects contained in every member) and `sub_wide_intersection_index_irrelevant`; `sub_join_via`/`sub_join` as the IMAGE of the copairing out of the coproduct of the domains with `sub_join_via_is_lub`/`sub_join_is_lub`; the two ends `sub_top` (the identity) and `sub_bot` (the arrow out of the initial object, under `Monic zero`); and the order vocabulary `IsIntersection`/`IsUnion` (glb/lub of a `J`-indexed family) with uniqueness and ≈-transport — 85 constants, every one `Print Assumptions`-closed** (85 `.glob` heads, the file using no `Program`, so `strings … .vo` returns no obligation; gate 8014 → 8198 = +184 across this file and its two witnesses). **Every lattice law is at ≈ and the bullet must not be read as more**: `SubObj x` is a preorder whose setoid identifies mutually-factoring subobjects, so `sub_meet_comm`/`sub_meet_assoc`/`sub_meet_idem`/`sub_meet_top`, their four join mirrors under `HasImages`, and the absorption pair `sub_meet_absorb : sub_meet u (sub_join u v) ≈ u`/`sub_join_absorb` are a lattice ON THAT SETOID and not a poset — `sub_le_antisym` is `sub_equiv_iff_mutual`, and the two "u ≤ v iff" pairs are FOUR lemmas (`sub_meet_of_le`/`sub_le_of_meet`, `sub_join_of_le`/`sub_le_of_join`) because `sub_le` is Type-valued; `Test/ProbeSubobjectLattice445.v`'s N2 pins `sub_meet_comm` REFUSED at Leibniz `=` (TYPING, no `cannot unify` clause). **`sub_meet u v` IS `sub_compose u (sub_reindex (sub_mono u) v)` at `eq_refl`**, the same record with the same three fields (`Theory/Subobject/Functor.v`) — measured, and pinned as the probe's `p445_meet_is_compose_reindex`; the Require of Functor.v is not taken by Lattice.v as a matter of NAMESPACE, not cost — an audit measured that `Instance/Sets.v` is ALREADY in Lattice.v's 48-file closure through `Structure/BiCCC.v`, `Structure/Regular/Factorization.v`, `Structure/Limit/Coproduct.v` and `Structure/Cartesian/Closed.v`, so requiring Functor.v would cost exactly ONE file, and a first draft's "it pulls `Instance/Sets`" was false. **The empty index is the design crux and is disclosed rather than smoothed over**: `IsWidePullback` carries no leg to the codomain, so the empty wide pullback is the TERMINAL object (`Structure/Pullback/Wide.v`, converse), which is not the top subobject — hence the `j0 : J` argument (probe N3: over `Empty_set` the hole `?j0` cannot be filled, FORMABILITY), the index is immaterial at the STRONGEST reading (same domain, witness `iso_id`, only the triangle discharged by `wide_pullback_commutes`), Riehl's limit-over-the-cospan-shape reading agrees at every nonempty index, and at the empty one the intersection is `sub_top` (`IsIntersection_empty`) while the union dual needs a least subobject (`IsUnion_empty_of_least`, instantiated as `IsUnion_empty_bot`). `sub_meet_is_wide_intersection` relates the two constructions in three lines through `IsIntersection_unique` rather than through `Wide.v`'s `binary_wide_pullback`, for a measured CONVERSION obstruction pinned as probe N5: `two_fam (sub_dom u) (sub_dom v)` and `fun b => sub_dom (sub_pair u v b)` convert at the two literals but not as functions of `b`. The total form `sub_intersection` lives under `HasWidePullbacks`. Images come from factorization systems: `ImageOf_of_OFS`/`HasImages_of_OFS` over `Structure/Factorization.v`'s `OFS E MonoClass`, minimality being `Theory/Orthogonality.v`'s diagonal read out of `ortho_lift` (no transport, `Theory/Morphisms/Classes.v` defining `MonoClass` as `fun _ _ f => Monic f`; probe N6 refuses an `(E, EpiClass)` system, CONVERSION), and `Regular_HasImages` applies it to `Structure/Regular/Factorization.v`'s `Regular_OFS` — CONDITIONAL, no `Regular` instance existing tree-wide (`Instance/Sets/Pullback.v`). The bottom: `zero_monic_of_strict` derives `Monic zero` from strictness of the initial object, `biccc_zero_monic` discharges it through `Structure/BiCCC.v`'s `initial_strict` under `Cartesian`+`Closed`+`Initial` only (a `Cocartesian` context was declared, found unused by `Check`, and removed; the requirement cost was measured at exactly TWO files, `Structure/BiCCC.v` and `Structure/Distributive.v`, the WHOLE file's closure going 37 (stub) → 48 (final) by iterated `coqdep`, +11 of which BiCCC's share is those two), `sub_meet_bot`, and Fong–Spivak's unit law ∅ ∪ X ≅ X as `sub_join_via_bot` (image handed in; first proved in the Sets witness during the parallel build and lifted here at integration) with `sub_join_bot` its `HasImages` corollary. **Universes, measured by `About`**: every constant binds `Category@{u u0 u0}`, hom identified with proof — INHERITED from `Theory/Subobject.v`'s `SubObj` record itself (`SubObj@{u u0} : ∀ {C : Category@{u u0 u0}}, …`) and `sub_le`'s block `h = p`, the first carrier in dependency order, so it belongs to that file and not to this one; no `Set` pin anywhere. 50 `Defined` / 2 `Qed` before the lift of `sub_join_via_bot` (52/2 after; a first draft said 51/53); flipping ALL of them to `Qed` recompiles green — Lattice.v, both witnesses AND the probe, `p445_meet_is_compose_reindex` included, since `sub_meet` is a `:=` definition the flip never touches — so NO in-tree consumer needs the transparency (an audit refuted a first draft's "a choice for the witnesses"); it is kept because `sub_le` and `≈` on `SubObj` are data. **NOT DELIVERED, and the header says each in terms**: no distributivity and no Heyting/Boolean structure, complement, implication or negation; no antisymmetry at Leibniz equality; no `HasPullbacks`/`HasWidePullbacks`/`HasImages`/`Regular` instance in THIS file (the witnesses are `Instance/Sets/SubobjectLattice.v` and `Instance/FinSet/Subobject.v`); no `Limit` presentation — `IsIntersection` is a glb in the preorder, not a cone over a wide-cospan shape, and `Wide.v` builds none either; no indexed join without an indexed coproduct handed in; that reindexing preserves binary meets is NOT stated (a 63-line proof was written, measured against the ~40-line budget for a satellite, and dropped); the SAFT hook (`Adjunction/SAFT.v`) is issue #448. (CORRECTION, #451: there is no such hook. `SAFT`'s premises are completeness, cone-level preservation, a cogenerating family, the index `SubobjectIndex` and the covering datum `SubobjectCover`, with no intersection hypothesis among them, and `SubobjectIndex` is not well-poweredness (`empty_SubobjectIndex` inhabits it at every object). The §V.8 consumer of these intersections is `Structure/WellPowered.v`, whose `complete_intersection` reads `sub_wide_intersection` over `Structure/Pullback/Wide/Complete.v`'s wide pullback from completeness; #448 used them in `Adjunction/SpanningArrow.v`'s `spanning_sub`.) (CORRECTION, #452: `Structure/WellPowered.v` is no longer the only §V.8 consumer. `Adjunction/SAFT/InitialObject.v`'s `special_initial_object`, Mac Lane §V.8 Theorem 1, takes its hypothesis as this file's `IsIntersection` of all subobjects of the cogenerator product, reaches `sub_wide_intersection` through `Structure/WellPowered.v`'s `complete_intersection` in its well-powered forms, and is handed this file's `sub_bot` at `Sets_zero_monic` by `Instance/Sets/SpecialInitial.v` through `intersection_all_of_least`.) (Since #453 `Adjunction/SAFT/Characterization.v`'s `least_mono`/`least_sub`, Mac Lane §V.8 Theorem 2, apply `sub_wide_intersection` to the wide pullback its hypothesis `HW` supplies.) The probe carries 12 refutations = 1 instrument + 11 negatives (3 INSTANCE, 1 TYPING, 1 FORMABILITY, 6 CONVERSION; the three INSTANCE negatives and the FORMABILITY one are `Definition`s, measured to be refused where the bare `Check` form is ACCEPTED with an open evar — the trap that would have made a `Fail Check` vacuously green; a first draft of this bullet miscounted them as 4 INSTANCE / 5 CONVERSION), each stripped alone in a copy of the whole file; `make todo` grows by exactly those 12, the three development files adding ZERO
- **Theory/Subobject/Quotient.v**: Mac Lane §V.7 Definition 3 (book p. 126, PDF pp. 135–136, `maclane:V.7:def3`), issue #446 — **quotient objects as subobjects of the opposite category, with the whole #445 lattice TRANSPORTED and not re-proved: `QuotObj x := @SubObj (C^op) x` with the covariant accessors `quot_cod`/`quot_epi`/`quot_is_epic`/`mk_quot`, the factorization preorder `quot_le`, the meet `quot_meet` as the PUSHOUT of the two epis (`HasPushouts C` through `Structure/Pushout.v`'s `HasPullbacks_op_of_HasPushouts`) with `quot_meet_is_glb`/`quot_meet_IsMeet` and the four laws, the join `quot_join` as the COIMAGE of the pairing `⟨quot_epi q, quot_epi r⟩` into `quot_cod q × quot_cod r` (`Cartesian C` + `HasCoimages C`) with `quot_join_is_lub`/`quot_join_IsJoin`, the absorption pair, the two ends `quot_top` (the identity quotient) and `quot_bot` (the terminal object, under `Epic (one : x ~> 1)`), and the wide meet `quot_wide_meet S j0 W` over a `WidePullback` IN `C^op` — 68 constants, every one `Print Assumptions`-closed** (68 `.glob` heads; the file uses no `Program`, so `strings … .vo` returns no obligation; gate 8198 → 8333 = +135 across this file and its three witnesses (an earlier revision of this bullet said 8329 / +131, before the Sets witness's four bottom-object constants); `make todo` 2383 → 2397 (+14, exactly the two probes' `Fail` lines, the four development files adding zero), so the issue's "adds no new hits" box is not met as written (disclosed); closure 51 files by `.Makefile.coq.d` NOT counting the file itself (52 with it), exactly THREE more than Lattice.v's 48 on the same convention — Lattice.v itself, `Theory/Morphisms/Duality.v` and the `Construction/Subcategory.v` it pulls — and the four `Structure/` Requires the interface stub lacked cost ZERO, being already inside). **Every lemma of the order theory is an instantiation at `C^op` written with `:=`**; the only constants with proof content of their own are `quot_equiv_iff_iso`, its one-directional corollary `quot_equiv_of_cod_iso` (first proved in the Grp witness during the parallel build and lifted here at integration), `mk_quot_respects` and the two four-field isomorphism permutations `Iso_of_op_Iso`/`op_Iso_of_Iso`, so 3 `Defined` / 0 `Qed` in the whole file and flipping all to `Qed` recompiles green. **Two readings are definitional and pinned as `eq_refl`, a third is REFUSED and is therefore a lemma**: `QuotObj_op_is_SubObj : @QuotObj (C^op) x = @SubObj C x` and `quot_le_unfold : quot_le q r = { k : quot_cod r ~> quot_cod q & k ∘ quot_epi r ≈ quot_epi q }` both hold at Leibniz `=` with no tactic, because two definitions in `Construction/Opposite.v` flip hom and composition DEFINITIONALLY; but the setoid does NOT unfold covariantly — `Theory/Subobject.v`'s witness read at `C^op` is an isomorphism IN `C^op`, so only `quot_equiv_unfold` (the `@Isomorphism (C^op)` spelling with the composition annotated `∘[C]`) is an `eq_refl`, the naive `{ i : quot_cod q ≅ quot_cod r & to i ∘ quot_epi q ≈ quot_epi r }` being refused with `cannot unify` (`Test/ProbeQuotObj446.v` N3), and the covariant characterization is `quot_equiv_iff_iso` at ≈/`iffT` with the orientation measured. **The order direction is the crux and the header states it in terms**: `quot_le q r` says q's epi factors through r's, so q is the COARSER quotient and the order is the REVERSE of inclusion of kernels (probe N4 refuses the swapped mediator, CONVERSION); `quot_meet` (the pushout, G/(NM) for groups) is the GREATEST LOWER BOUND for `quot_le` — the finest quotient that factors through both — and is NOT named a join, though a reader who orders quotients by the size of the codomain sees it as one; `quot_join` is reserved for the least upper bound, the coimage, G/(N ∩ M). An audit found the first draft's gloss of this bound inverted in four places ("the finest quotient that both q and r factor through" — the header, the comment before `quot_meet` written by the integrator's own stub, the comment before `quot_meet_is_glb`, and the Sets witness's comment on `Sets_Q_total_below`); the header, the `quot_meet` comment and the Sets comment carry the correction in place, the `quot_meet_is_glb` comment was rewritten to point at the header (an earlier revision of this sentence said all four carry a note). The issue's claim that there is "no lemma identifying `Monic` in `C^op` with `Epic`" was STALE and is cited as such: `Theory/Morphisms/Duality.v` carries all four bridges, `monic_op_iff_epic` pairs the two used here and `epic_of_monic_op`/`monic_op_of_epic` name its projections (probe N6 refuses the direction swap); the issue's `Epic` at `Theory/Morphisms.v` is at in tree. `@Cocartesian C` being NOTATION for `@Cartesian (C^op)` (`Structure/Cocartesian.v`), a plain `Cartesian C` is accepted for the `Cocartesian (C^op)` that `sub_join` consumes — MEASURED — which is why the join is stated with PRODUCTS of C; `HasCoimages C` is likewise notation for `@HasImages (C^op)` so that instance resolution still fires (probe N7 reports the missing instance under that name). `quot_bot`'s hypothesis `Epic (one : x ~> 1)` is left standing here as OBJECT-DEPENDENT, not rare — in `Sets` it holds at every inhabited object and not at the empty setoid; an audit refuted a first draft's "in most categories the arrow to the terminal object is not epic" by discharging it on `bool`, and both halves are now LANDED in `Instance/Sets/QuotObj.v` (`Sets_one_epic_of_inhabited`, hence `Sets_quot_bot`, and the refutation `Sets_one_not_epic_empty`; a second draft of the header said the discharge "lives with the witnesses" before any had been landed, which the integration audit caught) — and `one_epic_of_costrict`, the formal dual of `zero_monic_of_strict`, is labelled near-vacuous (its hypothesis collapses any pointed category). **Universes, measured by `About`**: every constant binds `Category@{u u0 u0}`, hom identified with proof, INHERITED from `Theory/Subobject.v` and not re-attributed; `quot_meet`'s `prod_rect` bounds and `quot_equiv_iff_iso`'s strict `u0 < u1` are likewise inherited, the latter already carried by `SubObj_Setoid`, `sub_equiv_iff_mutual` and `sub_le_antisym`; no `Set` pin. **NOT DELIVERED, each said in the header**: no concrete category in THIS file (the witnesses are `Instance/Sets/QuotObj.v`, `Instance/Grp/QuotObj.v`, `Instance/FinSet/QuotObj.v`); NO wide-pushout vocabulary is added, `Structure/Pullback/Wide.v` having none, so the wide meet takes the `C^op` spelling; `sub_intersection`, `sub_wide_join_via`, the degenerate-index lemmas, `sub_meet_is_wide_intersection` and the OFS route to images are NOT transported (each a one-line `:=` away, none written); no distributivity and no Heyting or Boolean structure; no antisymmetry at Leibniz equality, every law holding at ≈ and the two "q ≤ r iff" pairs being four lemmas; no `quot_epi_respects` (the epis of equivalent quotients live over different codomains, and the statement relating them IS `quot_equiv_iff_iso`); no functoriality and no reindexing. `Test/ProbeQuotObj446.v` carries 1 instrument + 6 negatives (4 CONVERSION, 2 INSTANCE — the INSTANCE ones as `Definition`s, the bare `Check` form being ACCEPTED with an open evar), each stripped alone in a copy of the whole file; with `Test/ProbeQuotObjWitnesses446.v`'s 7, `make todo` grows by exactly 14 and the four development files add ZERO
- **Structure/Generator.v** (+ `Structure/Generator/Dual.v`, `Structure/Generator/Concrete.v`): Mac Lane §V.7 Definition 4 (book p. 127, PDF p. 136, `maclane:V.7:def4`), Awodey §7.2 (p. 154, PDF p. 163, `awodey:7.2:def-generator`), Riehl 2nd ed. Definition 4.7.7 (p. 177, `riehl:4.7:def7`) and Epilogue §E.4 (p. 257, `riehl:E.4:def-separating-set`), issue #447 — **generating (separating) families and single separators, with the joint-faithfulness characterization proved in BOTH directions and the duality to `Adjunction/SAFT.v`'s `Cogenerator` discharged on the nose**: the family `Generator C` (index `Type`, objects, and the positive cancellation clause, shaped field for field after `Cogenerator`), Awodey's `IsSeparator c`, `JointlyFaithful`, the characterization terms `separator_faithful`/`faithful_separator` (`IsSeparator c ↔ Faithful [Hom c,─]`), `generator_jointly_faithful`/`jointly_faithful_generator` (`Generator C ↔ JointlyFaithful (fun j : gen_index G => [Hom gen_obj G j,─])`) and the issue's pinned `generator_iff_jointly_faithful` (the biconditional for an UNBUNDLED family `F : J -> C`, over the clause `Separates F`, Type-valued so a pair), the single-object/family pair `Generator_of_separator`/`separator_of_generator` with its `unit`-index round trip `separator_generator_roundtrip` stated as an EQUATION at `eq_refl` (a first draft stated only the type, which the hypothesis inhabits; an audit caught the vacuity — and the equation is what makes `separator_of_generator`'s `Defined` load-bearing: flipped to `Qed` the `eq_refl` is refused), its functor-level twin `jointly_faithful_faithful`/`faithful_jointly_faithful`, the transports `hom_test_iso`/`separator_iso`/`gen_family_mono`/`gen_iso`/`gen_extend`, and the free half of Riehl's contrapositive `gen_separates_contra`/`separator_contra` — 35 constants across the three files, every one `Print Assumptions`-closed (36 gated, counting the record constructor `Build_Generator`, which the `.glob` carries only as a `constr` reference; 24 + 8 + 3 `.glob` heads, and since none of the three files uses `Program`, `strings … .vo` returns NO obligation in any of them). **Every characterization is a term, not a tactic script, because the two sides have the same hypothesis once two definitions unfold**: `fmap[[Hom c,─]] f` is post-composition (`Functor/Hom.v`) and the `Sets` hom-setoid is pointwise (`Instance/Sets.v`'s `SetoidMorphism_equiv`), so `fmap[F j] f ≈ fmap[F j] g` IS `∀ k : gen_obj G j ~> x, f ∘ k ≈ g ∘ k`; what is not free is the packaging, the bare coercion `Faithful [Hom c,─] := H` being refused (TYPING, "has type "IsSeparator c" … expected … "Faithful (fobj[C] c)""), hence `Build_Faithful` one way and `fmap_inj` the other. **`Structure/Generator/Dual.v` is a separate file for a measured reason** — closure 19 (Generator.v, identical to the interface stub, no `Require` added) versus 46 (Dual.v, the extra being `Generator.vo` and SAFT's own 26), figures by iterated `coqdep` NOT counting the file itself (CORRECTION, #451: `Adjunction/SAFT.v` now also requires `Theory/Subobject.v`; by the same method Dual.v's closure is 47 and SAFT's own share 27, Generator.v's 19 is unchanged, and dropping that one edge reproduces 46 and 26) — and carries `gen_of_cog`/`cog_of_gen` with BOTH round trips at `eq_refl` (record eta with primitive projections plus function eta), `gen_op_of_cog`/`cog_of_gen_op` free from `C^op^op = C` (they must sit outside the section), and the dual characterization `cogenerator_jointly_faithful`/`jointly_faithful_cogenerator` over `Functor/Hom.v`'s `Curried_CoHom`, which **discharges the prose at `Adjunction/SAFT.v`** (that header is repointed LINE-NEUTRALLY, 1 insertion 1 deletion, giving up the sentence "This is the minimal data form of "cogenerating family"" so that `Record Cogenerator` stays put and `Theory/Concrete.v`'s citation stays true; no code touched). **The bridge needs the quantifiers exchanged and the category named**: `@cog_separates (C^op) G` applied without the swap is refused (CONVERSION, `cannot unify "x ~{ C }~> y" and "x ~{ C^op }~> y"`), and the `{| … |}` builder without an explicit category infers the record parameter as `C^op` from `gen_obj := cog_obj G` and then rejects the separation field ("The term "f" has type "x ~{ C^op }~> y" while it is expected to have type "y ~{ C^op }~> x"") — the record-builder trap the tree has met before. `Generator C = Cogenerator (C^op)` as TYPES is REFUSED at `eq_refl` (two distinct inductives carrying the same data), which is why the file carries a pair of maps and two `eq_refl` round trips instead of one equation — a stronger statement than an isomorphism. **The issue's "verified current state" was STALE and the correction is recorded rather than smoothed over**: `Theory/Concrete.v` has carried Awodey's single-object notion all along — `Class Separator`, `Separator_of_Faithful` (one half of the characterization), the other half inside `Concrete_of_Separator`'s `underlying_faithful` obligation, `WellPointedCategory`, and **the `Sets` witness the issue asks for, `Sets_Separator`, with `Sets_WellPointed` and the non-vacuity refutation `Sets_empty_not_Separator`** — so what is new here is the FAMILY notion, the two-way characterizations, the transports and the duality, not the single-object condition; `Structure/Generator/Concrete.v` carries the bridges `Separator_of_IsSeparator`/`IsSeparator_of_Separator` (and re-derives `separator_faithful` through `Concrete_of_Separator` as the check that the two developments agree), and is a THIRD file because `bool_setoid_object` is defined twice in the tree (`Instance/Sets.v`, `Theory/Concrete.v`) and requiring `Theory.Concrete` from `Structure/Generator.v` would shadow the `Sets` spelling downstream (cost measured: closure 21, +2 over Generator.v). The `Sets` witnesses themselves live in `Instance/Sets/Generator.v` (next bullet): a first draft of Concrete.v also read `Sets_Separator` through the bridge under the names `Sets_terminal_IsSeparator`/`Sets_terminal_Generator`, the second of which collided with the witness file's own, and integration kept the witness file's. **Riehl's `f ≠ g ⇒ ∃ h` form is NOT stated as the definition**: it is the contrapositive of the positive cancellation clause and neither direction is free with a Type-valued `≈`, so only the free half is landed (`gen_separates_contra`), and the header names `Structure/Complete/Freyd.v`'s `DecHom` as what a reader must supply. **Universes, measured by `About`**: `Generator@{u u0 u1} : Category@{u0 u1 u1} → Type@{max(u+1,u0,u1)}` with an EMPTY block, every constant binding `Category@{_ u0 u0}`; `JointlyFaithful` further identifies the two categories' hom and proof levels, the shape `Faithful` (`Theory/Functor.v`) already prints and the `Functor` record does not force; the `Sets`-mentioning characterizations carry `Instance/Sets.v`'s block (`u0 < u2` strict, `compose.u0/u1/u2`, `ID.u0`) together with `IsSeparator`'s and `Faithful`'s own ≤-bounds (a first draft said "exactly the Sets block", four bounds short, corrected in place); `hom_test_iso` and its three consumers carry `prod_rect.u0/u1/u2`, `gen_extend` adds `iso_id`'s strict `p < u` (`Theory/Isomorphism.v`), `separator_of_generator` carries `eq_rect_r`'s two bounds; the four duality bridges print EMPTY blocks, and no constant is pinned to `Set`. 1 `Defined` / 1 `Qed` in the three files, everything else a `:=` term; zero name collisions over the UNION of the two parallel halves (sweep over `.glob` with `find|xargs`, instrument-checked on `sub_le`; `gen_mono` was renamed to `gen_family_mono` because `Instance/Ab/DirectedColimit.v` has one, `Separator` was not introduced because `Theory/Concrete.v` has it, and the one cross-half collision, `Sets_terminal_Generator`, was resolved at integration); `make todo` 2397 → 2409, the +12 being exactly the two probes' `Fail` lines, so the issue's "adds no new hits" box is not met as written (disclosed). Boundaries in `Test/ProbeGenerator447.v` (N1 the unswapped bridge and N2 `Generator C = Cogenerator (C^op)`, both CONVERSION; N3 the wrong-variance DERIVATION — the wrong-variance STATEMENT typechecks, so the probe pins the derivation and a probe on the statement would be vacuous — N4 the bare coercion and N5 the implicit-category builder, all TYPING). **NOT DELIVERED, each said in the headers**: no `gen_collapse` — the classical remark that a generating family plus a coproduct gives the single generator `∐_j G_j` is not merely expensive but UNSOUND as stated, since arrows out of a coproduct are families of arrows out of the summands, so a summand with no arrow into `x` makes the hypothesis at `x` vacuous; the header carries the refutation in the topos `Set × Set` (`{(1, ∅), (∅, 1)}` separates, the coproduct `(1, 1)` does not, `Hom((1,1), (2, ∅))` being empty while `(2, ∅)` carries two distinct endomorphisms) as PROSE, not formalized. No `generator_iff_jointly_faithful` over a BUNDLED `Generator` (the pinned name is landed for an unbundled family, since a biconditional with `Generator C` would quantify the index on one side only); no dense, strong, extremal or regular generator and no claim that a separating family detects isos or epis; no Giraud clause; no smallness on `gen_index` (a bare `Type`, as `cog_index` is); no setoid on `Generator C`; no functoriality, adjunction or transport along a functor; no `Cogenerator` example (CORRECTION, #452: true of this development and no longer of the tree. `Instance/Sets/Cogenerator.v` builds cogenerators of `Sets`, `Sets_Cogenerator_untruncate` and `Sets_Cogenerator_IEM`, small and conditional on `Untruncate` and `IEM`, and the unconditional large `Sets_Cogenerator_large`; `Instance/Sets/SpecialInitial.v`'s `setsop_cog` is `cog_of_gen Sets_Generator`, a cogenerator of `Sets^op` and the bridge's first application to a concrete generator, by a grep for `cog_of_gen` over the comment-stripped `.v` files outside `Test/`; and `Adjunction/SAFT/InitialObject/Examples.v` builds cogenerators of the thin `Subsets X` and `Indiscrete`. `Structure/Generator/Dual.v`'s NOT DELIVERED carries the same correction; see the `Adjunction/SAFT/InitialObject.v` bullet) and no dual mirrors of the transport trio; and no statement that a separating FAMILY concretizes.
- **Adjunction/SpanningArrow.v**: Mac Lane §V.7 Definition 6, Lemma 2 and Remark 1 (book pp. 127–128, PDF pp. 136–137, `maclane:V.7:def6`, `maclane:V.7:lem2`, `maclane:V.7:remark1`), nLab *subobject* and *solution set condition*, issue #448 — **spanning arrows, and the SECOND structural route in tree from hypotheses to a `SolutionSet`**: `SubFactorsThrough G f m` (a lift `k` with `fmap[G] (sub_mono m) ∘ k ≈ f`, DATA), `Spanning G f := ∀ m : SubObj a, SubFactorsThrough G f m → sub_le sub_top m` (the issue's pinned `spanning_arrow` is an alias of it), the bespoke `PreservesWidePullbacks G`, and `SpanningArrowsOutOf G x`; then Mac Lane's Lemma in four named steps under `HasWidePullbacks A` and `PreservesWidePullbacks G` — `spanning_sub` (his intersection `b`, `sub_wide_intersection` over `FactoringFamily := { m : SubObj a & SubFactorsThrough G f m }`, nonempty at `factoring_top := (sub_top; factors_through_top G f)`), `spanning_factor` (his `f`, the mediator `unique_obj (wpull_ump spanning_wpull_preserved …)` that the PRESERVED wide pullback supplies to the cone of lifts `factoring_lifts_agree`), `spanning_factor_commutes` (`fmap[G] (sub_mono spanning_sub) ∘ spanning_factor ≈ f`) and `spanning_factor_spanning` — packaged as `factors_through_spanning` and delivered as `spanning_solution_set (x : X) : SolutionSet G x` with `sol_index := SpanningArrowsOutOf G x`, exactly the shape `Adjunction/GAFT.v` consumes, which `GAFT_from_spanning` does in one `exact`. **"Proper subobject" is never named, deliberately**: Mac Lane's definition is a negated existence, and with `≈` a Type-valued `crelation` and no antisymmetry on `SubObj` beyond `sub_le_antisym`, "strictly below the top" would yield nothing from its negation; the positive form `sub_le sub_top m` is strictly stronger constructively because it hands back the splitting `k` with `sub_mono n ∘ k ≈ id`, which is precisely what the spanning proof produces (the same move `Structure/Generator.v` makes for Riehl 4.7.7 and `Adjunction/SAFT.v`'s `Cogenerator` for "distinct arrows"); `sub_le_top_iso` turns it into `IsIsomorphism (sub_mono m)` and `spanning_forces_top` reads Mac Lane's own words back at ≈ — both first proved in the variety half and lifted here at integration. Measured: stated with the order FLIPPED, `∀ m, SubFactorsThrough G f m → sub_le m sub_top` is VACUOUS — `sub_top_greatest` (`Theory/Subobject/Lattice.v`) proves it for every `f`, so the probe for that boundary is a positive demonstration of vacuity (`p448_b1_vacuous`), not a `Fail`. **Every lattice fact is transported, none re-proved**: Mac Lane's minimality step is `inter_le` of `sub_wide_intersection_IsIntersection` (`Lattice.v`) at the composite subobject `sub_compose spanning_sub n` — `inter_greatest` is not needed and `sub_top_greatest` is used only by the lifted facts; the one inference made here is `apply (monic (Monic := sub_is_monic spanning_sub))` cancelling `sub_mono spanning_sub ∘ (sub_mono n ∘ k) ≈ sub_mono spanning_sub ∘ id`. **`PreservesWidePullbacks` is bespoke for a measured reason**: `Structure/Limit/Preservation.v`'s `PreservesLimit` is cone-level over a diagram functor and `Structure/Pullback/Wide.v` presents a wide pullback ELEMENTARILY without a wide-cospan shape category, so there is no diagram to feed it; the hypothesis quantifies over all index types but only over families of MONOS — intersections of subobjects, Mac Lane's exact hypothesis — and is consumed at exactly ONE family per arrow (a first draft took arbitrary families, strictly stronger than Mac Lane's; the verifier caught it and the monicity clause was added at integration). **The issue's "verified current state" was half stale and the correction is recorded rather than smoothed over**: it said `SolutionSet` "is manufactured only in `Adjunction/SAFT.v`"; measured (`grep -rn 'SolutionSet' --include='*.v' .`), the producers before this file are `Adjunction/GAFT.v` (twice, repackaging a weakly initial family or a comma initial object, and twice more from an adjunction that already exists), `Adjunction/SAFT.v`, `Adjunction/GAFT/Sets.v`, `Adjunction/Representability/Sets.v`, `Instance/Grp/FreeAFT.v` and `Instance/Rng/AFT.v` — the last three `solution_set_of_adjunction` applied to an adjunction the file already has. Re-measured on 2026-09-17 after the PR "algebraic carriers are sets": those last three citations are stale in line AND in kind. `Instance/Grp/FreeAFT.v` now produces TWO, the non-circular `Grp_Forget_solution_set_prop` and the kept circular `Grp_Forget_solution_set_from_adjunction`, and `Instance/Rng/AFT.v` likewise produces `Rng_Forget_Ab_solution_set_prop` and `Rng_Forget_solution_set_prop` beside its two `*_from_adjunction`; `Instance/Mod/TensorAFT.v` adds `tensor_esols_prop` and `bal_esols_prop`. The COUNTING SENTENCE above survives unchanged, because none of the new producers is a structural hypothesis on a category either: they are congruence indices at one functor, so this file is still the second structural route and SAFT's cogenerator-plus-well-poweredness the first. So the tree had exactly one route from STRUCTURAL hypotheses on the category, SAFT's cogenerator-plus-well-poweredness one — the others repackage a weakly initial family or a comma initial object, start from an adjunction that already exists, or are hand-built at one functor — and this is the second structural route, sharing no hypothesis with it. (CORRECTION, #451: "SAFT's cogenerator-plus-well-poweredness" mislabels that route. What `SAFT` consumes is a cogenerating family, the subobject index `SubobjectIndex`, which lacks well-poweredness's exhaustiveness clause and which `empty_SubobjectIndex` inhabits at every object, and the covering datum `SubobjectCover`, a datum at one functor rather than a structural hypothesis on the category, refuted at `Id[Sets]` by `Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd`; so SAFT's route is structural in its cogenerating family and its index but not in its covering datum. Well-poweredness proper is `Structure/WellPowered.v`'s `WellPowered`, which `Adjunction/SAFT/WellPowered.v` feeds to `SAFT` without discharging the covering datum. The COUNT does not survive the relabelling. By the criterion this bullet applies, under which a datum built at one functor is not a structural hypothesis — the ground on which the #450 addendum above set the congruence indices aside — SAFT's route does not qualify, since `SAFT_solution_set` consumes the covering datum at `U`; so "exactly one route from STRUCTURAL hypotheses on the category, SAFT's" and "the second structural route" do not stand, and this file's `spanning_solution_set`, from `HasWidePullbacks A`, a hypothesis on the category, and `PreservesWidePullbacks G`, a preservation property of the functor of the kind `GAFT`'s continuity hypothesis is, is the FIRST such route. Re-listed on 2026-09-23 by a grep for definitions whose type is a `SolutionSet`, over the `.v` files outside `Test/`, every other producer repackages a weakly initial family, a comma initial object or an element solution set, resizes a solution set, starts from an adjunction, is built at one functor, or, like `Instance/Mod/Spanning.v`'s `tensor_solution_set`, applies this one. (CORRECTION, #453: that survey no longer holds. `Adjunction/SAFT/Characterization/Cover.v`'s `saft_solution_set_at U comp cont G d W : SolutionSet U d` is a producer from `Complete C` and a `Cogenerator C`, hypotheses on the category, and `PreservesImageLimit U`, a preservation property of the functor, with a well-powering `W` of one product that `Structure/WellPowered.v`'s `wp_trivial` supplies at every object of every category, so `W` is not a per-functor datum; by this bullet's criterion it is a SECOND structural route, landed after `spanning_solution_set`, which stays the first.) The count would stand only if a per-functor covering datum were counted as structural. The file's own header counts by a different criterion, routes that do not presuppose the adjoint, under which SAFT's route still counts and its three families are unaffected.) **The stub's `FactorsThrough` collided** with `Instance/Sets/Pullback.v`'s same-concept definition for `Sets` (the builder's sweep caught it; no file required both), and was renamed `SubFactorsThrough` at integration. **Universes, measured by `About`**: `Spanning@{u u0 u1 u2 u3} : ∀ {A : Category@{u u0 u0}} {X : Category@{u1 u2 u2}} …`, A's hom identified with its proof universe by `SubObj` (`Theory/Subobject.v`, and anyway by minimization — a bare control definition reports the same); what this file adds is `u3 = u6` / `u4 = u7` on `spanning_factor` (one `FactoringFamily` indexes both hypotheses) and, on `spanning_solution_set` alone, `u0 = u2` — A's hom universe identified with X's, inherited from `SolutionSet@{u u0 u1 u2} : ∀ {C : Category@{u1 u2 u2}} {D : Category@{u0 u2 u2}}` at `Adjunction/GAFT.v`. `GAFT_from_spanning` is the file's ONLY `Set`-pinned constant, stated in its own section over `Category@{oA Set Set}`. An earlier revision of this bullet explained that pin as INHERITED: "`About GAFT` reports `∀ {C : Category@{u1 Set Set}} {D : Category@{u2 Set Set}}`, and the carrier is `initial_from_weakly_initial` (`Theory/WeaklyInitial.v`) — attributed by `About` on each of GAFT's five donors after a first draft of the in-file comment blamed `Instance/One.v`, `Instance/Parallel.v` and `Instance/Discrete.v`, all three FREE in h; applying the solution set to `GAFT` in an unannotated section is refused with "universe inconsistency: Cannot enforce Set = <A's hom universe>", because section Contexts bind rigid universes (probe B7)." The attribution chain was correct and ran one link further back than it said, to `DiscreteCat_Functor`, which `initial_from_weakly_initial` takes two limits over; that donor was annotated in place at `Instance/Discrete.v` in the PR "algebraic carriers are sets" (2026-09-17). Re-measured on 2026-09-17: `GAFT@{cobj dobj h u u0 u1}` is over `{C : Category@{cobj h h}} {D : Category@{dobj h h}}` with no `Set`, probe B7 turned over and is a positive control, and the very `exact` of `GAFT_from_spanning`, restated in a scratch section whose `A` and `X` have free hom universes, is ACCEPTED (`Complete@{hA hA hA oA}`, no `Set`). So the section's `Set` annotation is now a restriction the FILE imposes and no longer one it inherits; widening it is a change to a shipped statement and was deliberately left to its own commit. **THAT COMMIT LANDED, in the same PR, on 2026-09-17**, as a DELETION of the hand-written annotation (`Universes oA oX.` and the two `@{o Set Set}`), statement, `Proof using` and the one-line `exact` untouched: `GAFT_from_spanning` goes from `@{oA oX u u0 u1 u2 u3 u4}` — eight universes over `{A : Category@{oA Set Set}} {X : Category@{oX Set Set}}` with `Set = oA`, objects AND homs in `Set` — to `@{u u0 … u11}`, **thirteen universes over `{A : Category@{u u0 u0}} {X : Category@{u1 u2 u2}}` with no literal `Set` in the type and NOT ONE `Set` constraint in the block**, the only constant of the file besides `spanning_solution_set` with an empty `Set` set. So every sentence above that quotes the `Set` pin reads as history. The block's sharper content, which the elided quote in the commit message omits, is the three EQUALITIES `u0 = u2`, `u3 = u6`, `u4 = u7` — A's homs ARE X's homs — and that is what makes the surviving refusal legible: `GAFT_from_spanning` at `RMod R` is STILL refused, with a new message re-quoted from a stripped whole-file copy, "Cannot enforce u_hom = u_hom' because u_hom < u_obj <= u_hom'", i.e. the widened theorem wants objects at or below homs and `RMod R` has homs strictly below objects. The widening therefore removed an artifact and no wall. 31 constants, every one `Print Assumptions`-closed (from a scratch file carrying the target's exact Require list; no `Program`, so `strings … .vo` returns NO obligation, and `Print Module` cross-checks the 31 `.glob` heads exactly — it renders the `Qed`s as `Parameter`, which is display, not a declaration). 16 `:=` terms, 10 `Defined`, 5 `Qed`, the split following Lattice.v's own discipline (transparent wherever the result is `SubFactorsThrough`, `Spanning` or `sub_le`); flipping all 5 `Qed` to `Defined` still compiles, so opacity is nowhere forced, and both load-bearing `Defined`s are measured by what their `Qed` breaks — `` `1 (factors_through_top G f) = f `` and `sol_index (spanning_solution_set G GP x) = SpanningArrowsOutOf G x` (with `sol_obj`/`sol_arr`) each refused at `eq_refl`. Closure 66 by iterated `coqdep` NOT counting the file itself, and the three `Require`s kept only for `GAFT_from_spanning`'s statement (`Theory.Adjunction`, `Structure.Complete`, `Construction.Comma.Limit`) cost ZERO files — deleting those three lines from a copy leaves the closure at 66, `Adjunction.GAFT` already pulling each of them (an earlier revision of this bullet stated the same figure against the pre-integration stub, which is not in the commit; an audit called that unverifiable, and it is now measured from the file alone). Zero name collisions over the union of the two parallel halves after the rename (sweep with `find|xargs` over `.glob`, instrument-checked on `sub_le`). `make todo` 2409 → 2420, the +11 being exactly the two probes' `Fail` lines, so the issue's "adds no new hits" box is not met as written (disclosed). Boundaries in `Test/ProbeSpanningArrow448.v` (B1 the flipped order, POSITIVE vacuity; B2 the composite in the wrong order, CONVERSION; B5 the factor's codomain, TYPING; B3 the solution set without `PreservesWidePullbacks`, TYPING and not INSTANCE since `GP` is explicit; B4 the intersection without `HasWidePullbacks`, INSTANCE in a section without the instance; B7 the unannotated `GAFT` application, UNIVERSE). **NOT DELIVERED**: NO smallness anywhere IN THIS FILE — `FactoringFamily` is a bare `Type` and the `sol_index` carries no cardinality statement, so Mac Lane's cardinal bound is NOT attempted here (the variety file, next bullet, says what replaces it; and since 2026-09-17 `Adjunction/GAFT/Resize.v` supplies the vocabulary `SmallUpToIso`/`SmallCovering` in which the bound CAN be stated, with `Instance/Mod/TensorAFT.v`'s `tensor_spanning_SmallUpToIso` the one place it is proved — nothing of that reaches `FactoringFamily`, which is still a bare `Type`); no `HasWidePullbacks` instance and no `PreservesWidePullbacks` witness built here, so Lemma 2 is an axiom-free CONDITIONAL in docs/INHABITATION.md's sense and `GAFT_from_spanning` a conditional over four hypotheses (the tree's only `HasWidePullbacks` instance, `Instance/Sets/SubobjectLattice.v`'s `Sets_HasWidePullbacks`, is universe-wise unusable here — its index lives at `Sets`' hom universe while `FactoringFamily` lives one level up at `SubObj a`'s, "Cannot enforce <index universe> = <SubObj universe>" (two audits, differing scratch-local labels, so the shape is quoted) — so NEITHER hypothesis has an in-tree witness; the builder's report said half was dischargeable and the verifier refuted it) (CORRECTION, #451: "the tree's only `HasWidePullbacks` instance" was stale once #449's `RMod_HasWidePullbacks` landed, and #451 adds a general producer, `Structure/Pullback/Wide/Complete.v`'s `Complete_HasWidePullbacks`, a `Definition` and not an `Instance`. Neither discharges the hypothesis: `RMod_HasWidePullbacks` for the universe reason `Instance/Mod/Spanning.v` records, and `Complete_HasWidePullbacks` because at `Sets_Complete` it reads back at `Sets_HasWidePullbacks`' universes and at `SVariety_Complete E` it sets the class's second universe to the carrier, both refused by `spanning_solution_set` in scratch files as this file's own corrections record, and pinned as `Test/ProbeWellPowered451.v`'s N20 and N21. "NEITHER hypothesis has an in-tree witness" stands.) (CORRECTION, #453: it does not stand for the second hypothesis, and had not since #449. `Instance/Mod/Spanning.v`'s `Bilin_PreservesWidePullbacks : PreservesWidePullbacks (Bilin V V')` (#449, proved directly) inhabits it at one functor, and since #453 `Adjunction/SAFT/Characterization.v`'s `right_adjoint_PreservesWidePullbacks` supplies it at every right adjoint, applied by a library constant at `InverseImage f` on `Subsets Y` (`Adjunction/SAFT/Characterization/Examples.v`'s `subsets_inverse_image_thm2`) and, in a scratch file, at `Id[Sets]` beside `spanning_solution_set` over an abstract `HasWidePullbacks Sets`, as this file's header records in its own #453 correction. The FIRST hypothesis, `HasWidePullbacks A` at the universe `spanning_solution_set` asks, still has no in-tree witness at `Sets`, `SVariety E` or `RMod R`, so no `SolutionSet` is produced through the Lemma at a concrete functor and the Lemma stays an axiom-free conditional.); no converse and no uniqueness of `spanning_sub` beyond `IsIntersection_unique`; **no transport of `Spanning` along an isomorphism of the CODOMAIN object**, because `Theory/Subobject.v` carries no action of an iso on `SubObj` and the only transport in tree, `Theory/Subobject/Functor.v`'s `sub_reindex`, is a pullback needing `HasPullbacks` — what IS landed is the DOMAIN side, `spanning_precompose_split_epi` and its `spanning_precompose_iso` corollary, beside `spanning_respects` and the one-line `spanning_of_top_only`; `Structure/Factorization.v`, named as a donor by the issue, is not used and not needed
- **Adjunction/GAFT/Resize.v**, **Theory/Size.v** (extended): Mac Lane §V.7 Lemma 2's cardinality clause — **smallness as an explicit DATUM on a solution set, weakened to what is actually true, and Lemma 2 made a theorem at the module tensor** (the PR "algebraic carriers are sets", 2026-09-17). Mac Lane's Lemma says the spanning codomains are quotients of the term module and hence a SET; `Adjunction/SpanningArrow.v`'s bullet above records that the tree's Lemma-2 delivery carries NO smallness at all (`FactoringFamily` is a bare `Type`, `sol_index` no cardinality statement). This file supplies the missing datum in three forms and resizes along each. `Theory/Size.v` declares `Record SmallType@{w u} (A : Type@{u}) : Type@{max(w+1,u)}` — a carrier at `w`, maps both ways and the round trips — measured with an **EMPTY constraint block**, `w` and `u` unrelated. `Adjunction/GAFT/Resize.v` then gives `resize_solution_set@{i w dobj cobj h +}`, `SolutionSet@{i …} U d → SmallType@{w i} (sol_index S) → SolutionSet@{w …} U d`, whose constraint block is likewise EMPTY: the input index `i` and the output index `w` are DISTINCT declared binders with nothing relating them, which is what makes the lemma a resizing rather than an identity, and is the one thing the prototype this file was ported from did not state. Beside it: `resize_element_solution_set` (`h < su`), the two weakenings `SmallCovering` (a small type whose image still covers) and `SmallUpToIso` (for every large index a small index and an isomorphism in the comma category) with `resize_by_covering` (`u ≤ w`) and `resize_up_to_iso` (`h < u0`, `u ≤ w`), the passages `covering_of_iso` and `covering_of_smalltype` (empty block), and `representable_of_small`. **LEMMA 2 IS THEN A THEOREM, at the one place the tree can state it**: `Instance/Mod/TensorAFT.v`'s `tensor_spanning_SmallUpToIso` proves Mac Lane's own spanning family `tensor_esols_direct` small up to isomorphism — the small index being the `Prop`-valued congruences `CongIdx`, `si_carrier (tensor_spanning_SmallUpToIso V V') = CongIdx V V'` at `eq_refl` in the probe — and `tensor_via_AFT_maclane` feeds that to `representability_theorem` THROUGH `resize_up_to_iso`, so the p.-128 route reaches the tensor by Mac Lane's own argument rather than by replacing his family. Measured: on `tensor_spanning_SmallUpToIso` the ring's carrier slot `u10` satisfies `u10 ≤ u2`, i.e. the small index sits AT the carrier, which is exactly where `RMod_Complete` offers its shape objects. **Read the strength honestly, and the file's header says so**: `SmallCovering` IS a small-indexed solution set factored through the family, so what these lemmas buy is legibility and the correct NAME for Mac Lane's premise, not new strength; `SmallUpToIso` is DATA, not a property, and carries no uniqueness. Leibniz round trips on `SmallType` are deliberately NOT required of the spanning family, which would make them false in the intended model. **NOT delivered, measured on 2026-09-17** (`grep -rn --include='*.v' '\b<name>\b' .` outside the two defining files): `resize_up_to_iso` has exactly ONE in-tree consumer and `SmallUpToIso` eight mentions across `Instance/Mod/TensorAFT.v` and its probe, while `resize_solution_set`, `resize_element_solution_set`, `resize_by_covering`, `covering_of_iso`, `covering_of_smalltype`, `representable_of_small`, `SmallCovering` and `SmallType` have **ZERO** — they are defined, gated and "Closed under the global context", and applied nowhere. No smallness is proved for the free group's spanning family, for `Instance/Variety/Spanning.v`'s, or for `GAFT_from_spanning`'s `FactoringFamily`; and no cardinal arithmetic of any kind is formalised.
- **Structure/WellPowered.v** (+ `Structure/WellPowered/Counterexample.v`, `Structure/Pullback/Wide/Complete.v`, `Instance/Sets/WellPowered.v`, `Instance/Grp/WellPowered.v`, `Instance/FinSet/WellPowered.v`, `Instance/Powerset/WellPowered.v`, `Adjunction/SAFT/WellPowered.v`, `Adjunction/SAFT/Sets.v`; probe `Test/ProbeWellPowered451.v`): Mac Lane §V.8 (book p. 130, PDF p. 139, `maclane:V.8:def1`), nLab *well-powered category*, mathlib's `WellPowered.{w}`, issue #451 — **well-powered and co-well-powered categories WITH the exhaustiveness clause, a universe pin that gives the notion its content, and the §V.8 consequence proved over `Complete` with its dual: a well-powered complete category has the intersection of every class of subobjects of an object**. `WellPoweredAt x` has four fields — `wp_index`, `wp_to : wp_index → SubObj x`, `wp_from : SubObj x → wp_index` and the exhaustiveness clause `wp_to_from : wp_to (wp_from u) ≈ u`, which `Adjunction/SAFT.v`'s `SubobjectIndex` lacks — and the setoid bijection the issue asks for is DERIVED, not restated (`wp_index_setoid` pulls `SubObj_Setoid` back along `wp_to`; `wp_to_resp`, `wp_from_resp`, `wp_from_to`). **The content is the universe**: unpinned, the trivial datum `wp_trivial` (index `SubObj x` itself) inhabits the record at every object of every category, so `WellPowered@{o h w s t | w <= h, o <= s, h <= s, h < t +}` pins the index at or below the hom universe — the book's small sets, mathlib's common case `w = v`, and the bound `SAFT` already put on its own index (`u3 <= h`). "Small" is therefore relative to the category's OWN hom universe: a category whose hom universe is a free parameter can be instantiated with its homs raised and then passes the pin (a thin one through `trivial_small`), so the pin excludes something only where the homs are tied to the objects' carriers (`Sets`, the algebraic categories) or a free hom universe is instantiated low. The trailing `+` on both binders is load-bearing on Coq 8.19 and 8.20, which refuse the declaration without it ("Universe constraints are not implied by the ones declared: h <= sigT.u0, h <= sigT.u1, h <= eq.u0", quoted in the file). `CoWellPowered C := WellPowered (C^op)`, whose subobjects are `Theory/Subobject/Quotient.v`'s quotient objects on the nose (`quot_obj_is_sub_op`, `quot_setoid_is_sub_setoid_op`, both `eq_refl`). **The consequence, and why it needs well-poweredness**: `complete_intersection`/`complete_intersection_IsIntersection` intersect EVERY family indexed at or below `Complete`'s shape-object universe from completeness alone — the empty family included, by adjoining `sub_top` over `option J` (`sub_opt_top`, `IsIntersection_drop_top`), since `Lattice.v`'s `sub_wide_intersection` needs a point — so a statement about small families is NOT §V.8. The book's "collection of subobjects" is read in setoid discipline as a ≈-closed predicate `P` on the large type `SubObj x`, and `wellpowered_complete_has_intersections` intersects `{ m : SubObj x & P m }` by reindexing over the small `wp_class_index` (`IsIntersection_reindex`, consuming only the four fields), with `_prop` (a `Prop`-valued class, no size condition), `wellpowered_complete_intersection_all` (the class of ALL subobjects), `wellpowered_complete_least_subobject`, and `wellpowered_intersection_is_small` showing at `eq_refl` that the result IS the `complete_intersection` of the reindexed family. Necessity is measured in scratch files and quoted in the header, at the adjoint-functor regime (`C : Category@{o h h}`, `h < o`, `Complete@{h h h o}`): the class family handed to `complete_intersection_IsIntersection` without well-poweredness is refused ("cannot ensure that Type@{max(o,h)} is a subtype of Type@{...}"), and so is the identity family on `SubObj X` at `Sets@{o so}` with `Sets_Complete` ("Cannot enforce o = ... because o < so <= ...", the chain as printed with `X : Sets@{o so}` a section `Context`; with `X` a binder of the definition Rocq 9.1.1 prints an unnamed universe in the place of `so`), both accepted through the theorem given `WellPowered`. ARBITRARY large families are not claimed: the typed image `{ j : J & S j ≈ m }` is refused on universes, and the `Prop`-truncated image's greatest-lower-bound step on elimination ("Incorrect elimination ... in the inductive type ex ... the return type has sort Type@{h} while it should be SProp or Prop", as printed under `Set Printing Universes` with `h` a named universe, the default printing giving "Type"); that route needs unique choice, and nothing shows that every route does. The dual `cowellpowered_cocomplete_has_cointersections` is the theorem at `C^op` through `Complete_op_of_Cocomplete` in one line, and `cointersection_quot_le` reads its bound in `quot_le` with no conversion step. **Wide pullbacks from completeness were absent and are added**: `Structure/Pullback/Wide/Complete.v`'s `complete_wide_pullback` and its class form `Complete_HasWidePullbacks` — the first constant in tree passing from `Complete` to `HasWidePullbacks`, a `Definition` and not an `Instance` — build the wide pullback as the equalizer of `∏_J A ⇉ ∏_{J×J} z` (§V.2's products-and-equalizers recipe), with the family's index at `Complete`'s shape-object universe (`u3 <= u2` in `About complete_wide_pullback`, the one bound that matters). The route through `Structure/Equalizer/Wide.v` is not taken because `Instance/Parallel/Wide.v`'s `WideParallel@{i p} : Type@{i} → Category@{Set i p}` fixes the shape's object universe at `Set`, forcing `Complete`'s there ("Cannot enforce Set = h" at `Complete@{h h h o}`, measured by the #451 scouts) — that pin is SURFACED in the satellite's header, not lifted. The equalizer is `Structure/Limit/Finite.v`'s `FinitelyComplete_HasEqualizers`, which keeps the construction in the Structure layer at the price of stdlib upper bounds (`Fin.case0`, `VectorDef.nth`, `VectorDef.of_list`, `Logic_lemmas.equality`, `EqdepFacts.eq_sigT_sig_eq`, all visible in `About Complete_HasWidePullbacks`) that `Adjunction/GAFT.v`'s `Complete_HasEqualizers` does not carry. **STRENGTHS, measured by `eq_refl` at `Indiscrete bool` completed by `Indiscrete_Complete true`, every hom being `unit`**: the lower bounds compute (`wide_pullback_proj (complete_wide_pullback comp f) tt = tt` is accepted); the greatest-lower-bound factorizations do NOT — the wide pullback's mediator is refused at `eq_refl` by conversion ("cannot unify "unique_obj (ump_wide_pullbacks …)" and "()""), because it reduces only as far as `Structure/Pullback/Reduction.v`'s `Qed` `equalizer_of_pullback_awodey`, and `GAFT`'s equalizer would stop likewise at `Structure/Equalizer/Fork.v`'s `Qed` `limit_equalizer_desc`; `Print Opaque Dependencies wellpowered_complete_has_intersections` lists 63 constants (counted by their `name :` entries), of which that one, `SubObj_Setoid_obligation_1` and `Pullback_to_Universal_obligation_2` carry data. **Witnesses at the pin**: `trivial_small` (objects at or below homs); `Groupoid_WellPowered` (the one-point index, at every object universe) with `DiscreteCat_WellPowered`, `Indiscrete_WellPowered`, their `CoWellPowered` twins and `DiscreteCat_types_WellPowered` (objects `Type@{j}` strictly above homs, index at `Set`); `wp_of_classifier`/`Classifier_WellPowered` (a subobject classifier over pullbacks indexes the subobjects of `x` by `x ~> Ω`), instantiated unconditionally at `FinSet` as `FinSet_WellPowered`, whose index `FinSet_wp_index` reads back at `eq_refl` as `Fin.t n → Fin.t 2` — FinSet's is the only classifier in tree taking no hypothesis, by a grep for definitions whose type is a `SubobjectClassifier`. **`Sets` is shown well-powered at the pin only under `Untruncate`**: `Sets_WellPoweredAt_up` and `Sets_CoWellPoweredAt_up` are unconditional but one universe up (their indices, the `Type@{o}`-valued predicates and equivalence relations, sit at `o+1`), and the pinned expectation refuses the first ("Cannot enforce o = so because o < so"; the second was not tried at the pin; CORRECTION, #452: it has been since, and it is refused — `Test/ProbeSpecialInitial452.v`'s N15 offers `fun X => Sets_CoWellPoweredAt_up X` where the special initial-object theorem's well-powered corollary at `Sets^op` expects a `WellPowered (Sets^op)`, and the refusal is a universe inconsistency, the datum's index lying above the homs; N16 is the same at the per-object corollary); `Sets_WellPowered_untruncate : Untruncate@{o} → WellPowered@{so o o so t} Sets@{o so}`, whose per-object datum IS `wp_of_classifier` at `Sets_Classifier U`, and `Sets_wellpowered_intersection` (with `Sets_intersection_all_empty`: the intersection of all subobjects has an empty carrier) are the conditional pair. `Grp_WellPoweredAt_up` (index `Subgroup G`) is likewise one universe up and refused at the pin ("Cannot enforce o = h because h < o"). **Non-vacuity of the consequence**, searched over the tree's `Complete` constants: `Indiscrete_Complete` with `Indiscrete_types_has_intersections` at `Indiscrete@{o j j} Type@{j}` (`j < o`: objects above homs, but degenerate, one subobject per object), and `Instance/Powerset/WellPowered.v`'s `Subsets_Complete_free` (`Instance/Powerset.v`'s own term with its hom level left free), `Subsets_WellPowered` (by `trivial_small`, for `o <= u`), `Subsets_has_intersections`, `Subsets_intersection_all` and `Subsets_intersection_all_not_top` (non-degenerate: at a point of `X` the intersection of all subobjects of the full subset is not the top). No unconditional instance has BOTH objects strictly above homs AND a non-degenerate subobject lattice (`Sets_wellpowered_intersection` has both, under `Untruncate`), and at `SmallOrd_op_Complete` the `trivial_small` witness is refused by `wellpowered_complete_intersection_all` ("Cannot enforce ... = ... because ... < ...", its shape universe lying below its objects). **A formal NON-well-powered category**: `Structure/WellPowered/Counterexample.v`'s `AntichainTop@{i o h}` (objects `option Type@{i}`, thin), refuted at the pin by `AntichainTop_not_WellPoweredAt` (any index `w <= i`), `AntichainTop_not_WellPowered` (`h <= i`), `AntichainTop_Set_not_WellPowered` (homs at `Set`) and `AntichainTop_op_not_CoWellPowered` (the same theorem at `AntichainTop^op`), through `retract_paradox`, the stdlib `Coq.Logic.Hurkens` proof restated over a retract via `Generic.paradox`; the controls `AntichainTop_trivial` (one universe up) and `AntichainTop_WellPoweredAt_up` (AT the pin, at `None`, once `i < h`) show that the verdict depends on the hom instantiation. **SAFT is corrected, not re-shaped**: `SubobjectIndex` is strictly weaker than well-poweredness — `Adjunction/SAFT.v`'s `empty_SubobjectIndex` inhabits it at every object, and `SubobjectIndex_not_exhaustive` shows that no map from the subobjects into that index exists — and the bridge the other way is the satellite `Adjunction/SAFT/WellPowered.v` (`SubobjectIndex_of_WellPoweredAt`, `WellPowered_SubobjectIndex`, and `SAFT_of_WellPowered`, whose binder names `WellPowered`'s `s` and `t` and `SAFT`'s slot `u` because with only `cobj dobj h` named minimization identified `t` with `u`, and a section declaring `t < u` refused the wrapper). So `SAFT.v`'s one new `Require` is `Theory/Subobject.v` and its closure goes 43 → 44, where requiring `Structure/WellPowered.v` would have made it 109 (by `.Makefile.coq.d`). `SAFT`'s covering datum is REFUTED at the identity of `Sets`: `SAFT.v`'s `SubobjectCover_Id_retract` reads the datum at `U := Id` as every object being a retract of the domain of one indexed mono into the single object `cogen_prod comp G`, and `Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd` refutes that by Cantor's diagonal for EVERY `comp`, `G` and `WP` at `Sets@{h cobj}` with `Set < h`, which covers exactly the instances `SAFT` asks for there (the positive control `SubobjectCover_Id_Sets_absurd_at_SAFT` applies it with `cobj < u` rigid). No well-poweredness result therefore discharges `cover`: the classical argument takes its product over arrows out of `d`, and `cogen_prod` does not depend on `d`. (Since #453 the covering datum over that `d`-dependent product is a theorem, `Adjunction/SAFT/Characterization/Cover.v`'s `saft_cover_at`, and it needs no well-poweredness: it holds at every well-powering of the one product, `wp_trivial`'s among them, well-poweredness biting only at `GAFT`'s index pin in `SAFT_cover_wp`. `cover` itself is still refuted and still not discharged, and `SAFT` is unchanged.) The prose claims found falsified are corrected in place and marked "CORRECTION (#451)" — in `Adjunction/SAFT.v` (the readings of `SubobjectIndex` as well-poweredness and of `cover` as "precisely the CONCLUSION of the classical image-factorization step"), `Adjunction/GAFT.v`, `Adjunction/GAFT/Necessity.v`, `Adjunction/Representability/Sets.v`, `Adjunction/SpanningArrow.v`, `Instance/Variety/Spanning.v`, `Instance/Sets/Complete.v`, `Theory/Subobject/Lattice.v` and `Structure/Topos.v` — and the bullets and rows here and in docs/INHABITATION.md that cite them carry the same corrections. **The issue's "Current state" was stale in two places**: `SubobjectIndex`'s header did not acknowledge trivial satisfiability, it asserted the opposite ("Well-poweredness of [C] is then [forall x, SubobjectIndex x]"); and the intersection and wide-pullback construction it called absent existed since #445 (`Lattice.v`'s `sub_wide_intersection`, with `Sets_HasWidePullbacks` and #449's `RMod_HasWidePullbacks` as instances) — what was missing was a producer from completeness. **Measured**: 160 constants across the ten modules, enumerated by `Print Module` with the six `Program` obligations of `Instance/Grp/WellPowered.v` (the only ones, by `strings` on the `.vo` files) — `Structure/WellPowered.v` 48, `Counterexample.v` 16, `Wide/Complete.v` 16, `FinSet/WellPowered.v` 2, `Sets/WellPowered.v` 27, `Grp/WellPowered.v` 13, `Adjunction/SAFT.v` 27, `SAFT/Sets.v` 2, `SAFT/WellPowered.v` 3, `Powerset/WellPowered.v` 6 — every one "Closed under the global context" by its fully qualified name (re-run in one scratch file, 160 of 160, the instrument `functional_extensionality` printing its axiom), and every one carried by the `print-assumptions` gate, which gained 138 lines for them (`SAFT.v`'s other 22 were gated before); zero declaration-head collisions over the 138 newly gated short names (sweep over all 1045 `.glob` files with `find|xargs`, the instrument `sub_le` declared in exactly one). `Defined`/`Qed` by token: `Structure/WellPowered.v` 11/4, `Counterexample.v` 1/6, `Wide/Complete.v` 1/2, `Sets/WellPowered.v` 13/2 (ten of the thirteen load-bearing by flipping each in a copy of the whole file, as that header records), `Grp/WellPowered.v` 2/6 (the six are the obligations), `Powerset/WellPowered.v` 0/1, `SAFT/Sets.v` 0/1. Closures by iterated `coqdep`, not counting the file itself: `Structure/WellPowered.v` 95, `Wide/Complete.v` 72, `Counterexample.v` 96, `Sets/WellPowered.v` 126, `Grp/WellPowered.v` 123, `FinSet/WellPowered.v` 99, `Powerset/WellPowered.v` 132 (against `Instance/Powerset.v`'s 87), `SAFT/WellPowered.v` 110, `SAFT/Sets.v` 45; no `Adjunction/` file lies in `Structure/WellPowered.v`'s closure, so no cycle. The development files add no `make todo` hit (2479 at the parent and 2479 with all of them in place, the Makefile's `find | egrep` pipeline counted by `wc -l`); the probe adds 23, exactly its lines opening with the refutation keyword, so the tree counts 2502 with it. Boundaries are pinned in `Test/ProbeWellPowered451.v`: 23 refutations by that count, classified in its header by the text of each one's error with the keyword stripped in a copy of the whole file — the name-absence instrument, 16 UNIVERSE, 3 TYPING, 2 CONVERSION and 1 INSTANCE — each of the 22 negatives beside a positive control naming the same constants. The INSTANCE one is class search finding no `HasWidePullbacks (SVariety E)`; its control, the section `SearchBoth`, shows that search resolves the hole to `Complete_HasWidePullbacks (SVariety_Complete E)` (read back by `eq_refl`) once `Complete_HasWidePullbacks` is registered as a local instance AND `SVariety_Complete` as a local hint, either registration alone leaving it refused (measured in copies of the whole file), so being a `Definition` is not by itself why search finds nothing. The probe also compiles on Coq 8.19.2 and 8.20.1 over its dependency closure rebuilt locally with those toolchains, which is not the nix build. **NOT DELIVERED**: `Sets` well-powered at the pin unconditionally (no construction and no impossibility proof), or co-well-powered at the pin even under `Untruncate`; `Grp` at the pin under any hypothesis, `Grp` co-well-powered, and `Ab`/`RMod R` (CORRECTION, #454: `RMod R` no longer belongs in that list. `Instance/Mod/WellPowered.v` shows it well-powered and co-well-powered ONE UNIVERSE UP with no hypothesis (`RMod_WellPoweredAt_up`, `RMod_CoWellPoweredAt_up`, `RMod_CoWellPowered_up`) and both AT THE PIN under `Untruncate` (`RMod_WellPowered_untruncate`, `RMod_CoWellPowered_untruncate`), the one-up data being refused where the pin is expected (`Test/ProbeWatts454.v`'s N6 and N8); `RMod R` at the pin without a hypothesis is neither built nor refuted, and `Ab` is still not measured. That file is a `WellPowered.v` satellite of `Instance/Mod/` beside the ones this bullet heads with, and `Structure/WellPowered.v`, `Counterexample.v` and `Instance/Grp/WellPowered.v` carry the same correction in their headers); a co-well-poweredness witness for `FinSet`, and any category well-powered but not co-well-powered; intersections of arbitrary large families, or over `FinitelyComplete`; wide-pushout vocabulary (the dual runs through `C^op`); lifting `WideParallel`'s `Set` pin; factorizations that compute (flipping `equalizer_of_pullback_awodey` to `Defined` is the named first step, not attempted); a re-shaped `SAFT` whose covering datum is taken over a product depending on `d` (CORRECTION, #453: delivered, as new constants beside `SAFT`, which is unchanged — `Adjunction/SAFT/Characterization/Cover.v`'s `SubobjectCoverAt`, `saft_cover_at` and `SAFT_cover_wp`, and, with no covering datum at all, `Adjunction/SAFT/Characterization.v`'s `SAFT_wellpowered`, which takes this file's `WellPowered`); `AntichainTop` at `i < h` shown well-powered beyond `None`; and `Complete_HasWidePullbacks` does not discharge `spanning_solution_set`'s hypothesis at `SVariety E` or at `Id[Sets]` (refused on universes in scratch files, as `Adjunction/SpanningArrow.v`'s corrections record, and pinned as the probe's N20 and N21). No `Instance` is registered.
- **Adjunction/SAFT/InitialObject.v** (+ `Adjunction/SAFT/InitialObject/Examples.v`, `Instance/Sets/Cogenerator.v`, `Instance/Sets/SpecialInitial.v`; probe `Test/ProbeSpecialInitial452.v`): Mac Lane §V.8 Theorem 1 (book p. 128, PDF pp. 137–138, `maclane:V.8:thm1`) with the remark under §V.8 Definition 1 (book p. 130, `maclane:V.8:def1`), and Riehl Lemma 4.7.11 (printed p. 177, PDF pp. 197–198, `riehl:4.7:lem11`), issue #452 — **the special initial-object theorem, the initial object being the intersection of all subobjects of the cogenerator product ON THE NOSE, with both halves of the book's proof, the book's hypothesis in three readings, the comparison with Freyd's construction, and witnesses at `Subsets X`, `Indiscrete` and `Sets^op` unconditionally and at `Sets` under `Untruncate` or `IEM`**. `special_initial_object comp G w Hw : @Initial C` takes `comp : Complete C`, a cogenerating family `G : Cogenerator C` (`Adjunction/SAFT.v`'s record) and `Hw : IsIntersection (fun m : SubObj (cogen_prod comp G) => m) w`, the intersection of ALL subobjects of the product in `Theory/Subobject/Lattice.v`'s vocabulary; its object IS `Subobject.sub_dom w` at `eq_refl` (`special_initial_object_obj`), its arrow to `c` reads back at `eq_refl` as `` cogen_pullback_to comp G c ∘ `1 (inter_le _ _ Hw (cogen_pullback_sub comp G c)) `` (`special_initial_object_zero`), and `special_initial_IsInitialObj` states the conclusion in the type, `IsInitialObj (Subobject.sub_dom w)`. EXISTENCE is the book's: Mac Lane's j is `Adjunction/SAFT.v`'s `cogen_canonical` into the hom-indexed power `cogen_power`, monic by `cogenerator_canonical_monic` (the one place `cog_separates` is consumed), his k (Riehl's "diagonal map") is `cogen_prod_to_power`, and the pullback of the first along the second (`cogen_pullback_sub`, `Theory/Subobject/Functor.v`'s `sub_reindex` over `complete_pullbacks`) lies above the least subobject. UNIQUENESS is `least_sub_arrows_agree`, stated over a bare `HasEqualizers C` and a least subobject of any object so that it is reusable: the equalizer of `f` and `g` followed by the least subobject's mono is a subobject (`equalizer_sub`), leastness gives the equalizer a section, so it is split epi and `f ≈ g` — the book's "two distinct arrows would have a proper equalizer" as a direct argument, with no case split on the equality of arrows. **The hypothesis, three ways.** A least subobject (`special_initial_object_least`, the weakest form, converted to and from the headline's by the terms `least_of_intersection_all` and `intersection_all_of_least`). Mac Lane's "every set of subobjects of each object has an intersection" READ AS `HasClassIntersections`, every ≈-closed class of subobjects of every object having one (`special_initial_object_book`); the reading departs from the book twice and the header says so: classes, because the family that carries the proof, ALL subobjects of `cogen_prod comp G`, is indexed by the large type `SubObj`, as `Structure/WellPowered.v`'s header argues and the catalogue's summary of Riehl's lemma ("collection") supports, which STRENGTHENS the hypothesis against a literal set reading as the book's own proof requires; and a greatest lower bound (`IsIntersection`) where both books define the wide-pullback limit, which in that respect alone makes the reading weaker than the book's hypothesis, the safe direction. And Mac Lane's §V.8 remark, well-poweredness, through #451's `wellpowered_complete_intersection_all` (`special_initial_object_wellpowered`, with `wellpowered_class_intersections : WellPowered C → Complete C → HasClassIntersections C`) or at the ONE object `cogen_prod comp G` (`special_initial_object_wellpowered_at`, whose index is bounded by `Complete`'s shape universe instead of being pinned at the homs). `special_initial_object_small` needs no intersection hypothesis when the objects sit at or below the shape universe; that is Freyd's collapse regime — `Structure/Complete/Freyd.v`'s `small_complete_is_thin` is accepted at its universes in a scratch file, as the header records, so such a category with decidable object equality and hom-setoids is thin. **Freyd's construction.** `special_weakly_initial` (the existence half alone makes the least subobject's domain weakly initial), `special_vs_freyd` (the two initial objects compared by `initial_unique` over the singleton family), `wif_of_cogenerator` (well-poweredness at `cogen_prod comp G` indexes a weakly initial family with no intersection taken — the object-level solution set, which Mac Lane's §V.8 proofs do not use), `freyd_of_cogenerator` and `special_vs_freyd_of_cogenerator`. The in-tree comparison is stated only at `so = h`, because `Theory/WeaklyInitial.v`'s `initial_from_weakly_initial_complete` takes `Complete@{u1 u1 u1 u0}` (at `h < so` it is refused, probe N8 — a limit of that constant's signature, not of the mathematics, since any two initial objects are isomorphic), and Freyd's object is `Qed`-opaque, so the comparison relates an object that reads back at `eq_refl` to one that does not. **Universes, measured by `About`**: `special_initial_object@{o h so q u u0 u1}` over `Category@{o h h}`, `Complete@{so so h o}` and `Cogenerator@{so o h}`, with the block `h < u, h < u0, so < u0, u < u1, o <= q, o <= u0, h <= so, h <= q` besides stdlib bounds, and no `Set`; nothing relates `o` to `h` or `so`, and both `SAFT`'s regime (`so = h`) and `h < so` are accepted. `h <= so` is the book's "small hom-sets" and is GENUINE: with `Complete`'s limit-datum universe `r` kept apart, `cogen_prod` is accepted at `so < h <= r` while `cogen_power_limit`'s body is refused at its argument, `cogen_power_index` sitting at or above the homs (probe N5, labelled P_H_le_so). The refusal first quoted for it, "Cannot enforce h <= so because so < h", is the binder `Complete@{so so h o}`'s own, since `Complete@{r so h o}` declares `h <= r` (an audit caught it); it is kept as N4, labelled as guarding nothing about `cogen_power`. TWO DONOR COLLAPSES of `Adjunction/SAFT.v`'s unannotated constants are recorded and NOT repaired, because repairing them would change signatures that file's header quotes: `cogen_prod` identifies the family's index universe with `Complete`'s shape universe (a family indexed strictly below is refused, N2, while `cogen_power` accepts it), and `cogen_power` identifies `Complete`'s limit-datum universe with it (N3). The binders are load-bearing — unannotated drafts minimized `so` onto `h` — and `HasClassIntersections@{o h p q s t k}` names all seven universes because with four named its instance is refused ("Universe instance length for HasClassIntersections is 4 but should be 7", quoted in the header, not probed). **Witnesses.** `Adjunction/SAFT/InitialObject/Examples.v` applies the well-powered corollary with no hypothesis at the powerset lattice `Subsets X` of any setoid (`Subsets_special_initial`, over `Subsets_Complete_free` and `Subsets_WellPowered`, with any family, the empty `Subsets_Cogenerator_empty` and the one-member `Subsets_Cogenerator_bot` among them) and at `Indiscrete@{o j j} Type@{j}` with `j < o` (`Indiscrete_types_special_initial`). At both the least subobject is COMPUTED, as `Structure/WellPowered.v`'s wide-pullback `complete_intersection` (`Subsets_special_initial_obj`, `eq_refl`); `Subsets X`'s object is not degenerate (`Subsets_special_vs_bot`, `≅` the bottom subset by `initial_unique`; `Subsets_special_initial_is_empty`; `Subsets_special_initial_not_top`), `Indiscrete`'s is (`unit` at `eq_refl`, every object being initial), and both categories are THIN, so every family cogenerates them and `cog_separates` does no work. `Instance/Sets/SpecialInitial.v` runs the theorem where separation does work. At `Sets^op`, UNCONDITIONALLY (`setsop_special_initial`): over `setsop_comp := Complete_op_of_Cocomplete Sets_Cocomplete` and the small, genuine cogenerator `setsop_cog := cog_of_gen Sets_Generator` — each the first application of its donor to a concrete category, by a grep of the donor's name over the comment-stripped `.v` files outside `Test/` — with the least subobject `setsop_L` built by hand as the arrow into `Sets_Terminal`'s singleton read as a mono of `Sets^op` (`setsop_L_least`), the object reading back at `eq_refl` as `@terminal_obj Sets Sets_Terminal` (`setsop_special_obj`; `setsop_terminal_IsInitialObj` in the type). At `Sets` under `Untruncate`, three ways: the least subobject `sub_bot` at `Sets_zero_monic` (`sets_special_initial_untruncate`); the well-powered corollary with `Sets_WellPowered_untruncate` (`sets_special_initial_wellpowered`, the object `eq_refl` to that `complete_intersection`, `initial_unique` to `Sets_Initial`, and `sets_special_wellpowered_empty` proving it empty through its own zero arrow); and the book's form with `Sets_HasClassIntersections_untruncate`, `Instance/Sets/WellPowered.v`'s `Sets_wellpowered_intersection` eta-expanded (`sets_special_initial_book`). At `Sets` under `IEM` through `sub_bot` (`sets_special_initial_IEM`, and `sets_special_initial_IEM_at_Set` at `Sets@{Set so}`). **THE CIRCULARITY CAVEAT, stated in that file's header**: at `Sets^op`, and in the `sub_bot` witnesses at `Sets`, the least subobject handed to the theorem is BUILT FROM the known initial object, and the `eq_refl` readbacks rediscover it; those witnesses exercise completeness, the cogenerator product, the canonical monic with `cog_separates`, the existence pullbacks and the equalizer uniqueness, and only the well-powered and book forms at `Sets`, both conditional, COMPUTE the least subobject as an intersection. So the one unconditional witness at a category that is not thin (measured against Examples.v's two, both thin) is circular in that sense. **The cogenerators.** `Instance/Sets/Cogenerator.v` holds the tree's first `Cogenerator`s of `Sets`: `Sets_Cogenerator_untruncate` (the one-member family `Powerset_Prop_truth`, the test at `y0` being `Instance/Sets/Powerset.v`'s `Powerset_Prop_singleton_pred y0`, carrying that carrier's `Set < o`) and `Sets_Cogenerator_IEM` (Mac Lane's two-point set, `BoolSetoid`, carrying no `Set` bound, which is what `sets_special_initial_IEM_at_Set` uses; `Sets_Cogenerator_untruncate (untruncate_of_IEM E)` is accepted as well but is refused at `o := Set`, as the header quotes), both small (indexed by `unit`); and `Sets_Cogenerator_large`, UNCONDITIONAL and LARGE (every object, each tested by its identity, `o < c`), which `cogen_prod` at `Sets_Complete` refuses on universes ("Cannot enforce c = o because o < c", probe N13 and N14, labelled P_large_cog). What `Sets` lacks unconditionally in tree is therefore a SMALL cogenerator, one indexed at `Sets_Complete`'s shape universe, and the `Type`-valued truth-setoid route to one is refused on universes (N11 and N12, labelled P_type_truth), a refusal of one construction and not an impossibility proof. `Sets_terminal_not_cogenerates` shows the condition is not vacuous at `Sets`: no family of copies of the terminal object cogenerates it. The cogenerators are not in `Instance/Sets/Generator.v` for a measured reason: that file's closure is 23 counting itself, and adding the three imports they need raises it to 87. **Refused routes, pinned**: the small form at `so < o` (N6, N7) and at `Sets` (N10, the objects of `Sets@{o so}` living above `Sets_Complete`'s shape universe); and `Sets^op` through the well-powered corollary with the only co-well-poweredness of `Sets` in tree, `Sets_CoWellPoweredAt_up`, one universe up (N15, and N16 at the per-object corollary), which is why the hand-built `setsop_L` is what makes `Sets^op` unconditional. **Measured**: 70 constants, 29, 12, 7 and 22 in the four modules by `Print Module`, which lists no `Program` obligation (none of the four files uses `Program`), each "Closed under the global context" by its fully qualified name (re-run in one scratch file, 70 of 70, beside the instrument `Print Assumptions functional_extensionality`, which prints its axiom), and all 70 in ONE new block of the `print-assumptions` gate (`grep -c 'Print Assumptions' Makefile` 9378 → 9448); `Untruncate` and `IEM` are hypotheses in the statements, `Definition`s (Π-types) and not axioms. `Defined`/`Qed` by token over the comment-stripped code: 4/1, 0/2, 4/1 and 3/0, the four `Qed` being `least_sub_arrows_agree`, `Subsets_special_initial_is_empty`, `Subsets_special_initial_not_top` and `Sets_terminal_not_cogenerates`; every other constant is a transparent term, so the readbacks compute. Closures over `.Makefile.coq.d`, not counting the file itself: 110 (the same as `Adjunction/SAFT/WellPowered.v`'s), 139, 84 and 143; `Examples.v` is a satellite because the 28 files its closure adds beyond the theorem's file are all in `Instance/Powerset/WellPowered.v`'s closure. The development files add no `make todo` hit (the Makefile's `find | egrep` pipeline counted by `wc -l`: 2502 at master 7e1b3f9e, 2521 with #452), the 19 being exactly the probe's lines opening with the refutation keyword. `Test/ProbeSpecialInitial452.v` carries those 19 refutations — the name-absence instrument, 3 CONVERSION (N1, N17, N18: the object is not the product, and the `Sets^op` and `IEM` readbacks discriminate) and 15 UNIVERSE (N2–N16) — each classified by the error its copy of the whole file stops with once the keyword is stripped, each negative beside a positive control, with a guard block naming all seventy constants. The four files and the probe, with each stripped copy, compile on Coq 8.19.2 and 8.20.1 against the nix store's builds of the library for those versions, as the probe header records; that is not `nix build .#category-theory_8_19`/`_8_20` of this tree, which is run on the committed revision. The prose claims #452 falsified are corrected in place and marked "CORRECTION, #452": in `Adjunction/SAFT.v` (its twelve-file `Cogenerator` grep, sixteen now; and its "meta-argument" sentence, since under one `U : Untruncate` every premise of `SAFT` at `Id[Sets]` but the covering datum is inhabited in tree, and `SAFT` applied to them is accepted in a scratch file), `Adjunction/SAFT/Sets.v`, `Structure/Generator/Dual.v`, `Instance/Sets/Complete.v` (whose "applied only by `saft_representable`" had been stale since #451) and `Theory/WeaklyInitial.v` (whose "exactly one use" of `Build_WeaklyInitialFamily` `wif_of_cogenerator` makes two), and in the bullets and rows here and in docs/INHABITATION.md that repeated them. **NOT DELIVERED**: no unconditional SMALL `Cogenerator` at `Sets`, and no proof that there is none; `SAFT` is not re-shaped, no cogenerator of a comma category is built and `SAFT` is not derived from this theorem (#453 — the header's list of what #453 would consume is a forecast; one instantiation at a comma category is accepted in a scratch file, at `so = h`) (CORRECTION, #453: the forecast is replaced by what was consumed. A cogenerator of a comma category is built, `Adjunction/SAFT/Characterization.v`'s `comma_cogenerator`, indexed by the arrows out of `d`; the new special adjoint functor theorems ARE derived from this theorem — `SAFT_wellpowered` through `special_initial_object_wellpowered_at` in each comma (`SAFT_comma_initial`) and `SAFT_thm2` through `special_initial_object_least` (`SAFT_thm2_initial`) — while `SAFT` itself is still not re-shaped and not derived from it; and both comma instantiations are accepted at `h < so` as well as at `so = h`, in a scratch file carrying that file's import list, so the `so = h` above was minimization, as this file's header records in its own #453 correction); the two donor collapses; no witness at `Sets^op` through either well-powered corollary, co-well-poweredness of `Sets` at the pin not being in tree; `special_initial_object_small`, `special_initial_object_wellpowered_at` and the three Freyd comparisons are applied at no concrete category (the small form is accepted at `Subsets X` in a scratch file only) (CORRECTION, #453: `special_initial_object_wellpowered_at` is applied now, in the comma categories `=(d) ↓ U` of concrete functors: `Adjunction/SAFT/Characterization.v`'s `SAFT_comma_initial` applies it in every comma, and through `SAFT_wellpowered` it reaches, in `Adjunction/SAFT/Characterization/Examples.v`, the commas of `InverseImage f` on `Subsets Y` and of `Id` on `Indiscrete` unconditionally, and those of `Id` and `HomFrom A` on `Sets` under `Untruncate`; `special_initial_object_least` is applied the same way through `SAFT_thm2_initial`, reaching the commas of `InverseImage f` by `subsets_inverse_image_thm2`. `special_initial_object_small` and the three Freyd comparisons are still applied at no concrete category: a grep of each name over the comment-stripped `.v` files outside `Test/` and `doc/` finds `Adjunction/SAFT/InitialObject.v` only); no comparison with Freyd's object at `Sets` as a library constant (not attempted: `initial_unique (sets_special_initial_wellpowered U) Sets_initial_recovered` is accepted at `Sets@{o so}` under `Untruncate` in a scratch file, closed under the global context, and so are `special_vs_freyd` and `freyd_of_cogenerator` at `Sets` under `Untruncate`; an earlier revision of this bullet said `Sets_initial_recovered` is pinned at `Sets@{Set _}`, which `About` refutes); no normal form of the zero arrow at a concrete category; no comparison among the three `Sets` cogenerators, and no cogenerator of any other concrete category (`Grp`, `Ab` with ℚ/ℤ, `FinSet`) (CORRECTION, #454: false as written since #454 — `Instance/Mod/Cogenerator.v`'s `RMod_Cogenerator_large` is an UNCONDITIONAL cogenerator of `RMod R` (every module, tested by its identity), large, and refused by `cogen_prod` at `RMod_Complete R` exactly as `Sets_Cogenerator_large` is at `Sets` (`Test/ProbeWatts454.v`'s N4); `Instance/Mod/WellPowered.v`'s `RModop_Cogenerator` cogenerates `(RMod R)^op`, unconditionally, at `RingObject@{c c c}`; and `Ab` with ℚ/ℤ and `RMod R` with hom_ℤ(R, ℚ/ℤ) have SMALL cogenerators only CONDITIONALLY, `QZ_Cogenerator` and `QZ_injective_cogenerator` on Mac Lane's Exercise 2(b) premise, which `QZ_cogenerates_Ab_DNE` and `QZ_injective_WLEM` show classical; `Grp` and `FinSet` gain none, and `Instance/Sets/Cogenerator.v`'s header carries the same correction). No `Instance` is registered.
- **Adjunction/SAFT/Characterization.v** (+ `Adjunction/SAFT/Characterization/Cover.v`, `Adjunction/SAFT/Characterization/Corollaries.v`, `Adjunction/SAFT/Characterization/Examples.v`; probe `Test/ProbeSAFT453.v`): Mac Lane §V.8 Theorem 2, the Lemma inside its proof and the Corollary after it (book pp. 129–130, PDF pp. 138–139, `maclane:V.8:thm2`, `maclane:V.8:lem1`, `maclane:V.8:cor1`), Awodey §9.8 Remark 9.35 (printed pp. 255–256, `awodey:9.8:remark35`), Riehl Theorem 4.7.10 and Corollary 4.7.13 (printed pp. 177–178, `riehl:4.7:thm10`, `riehl:4.7:cor13`) and the Epilogue's two restatements (printed p. 252, `riehl:E.1:thm-gaft` clause (iii), `riehl:E.1:thm-representability-criteria`), issue #453 — **the special adjoint functor theorem as a CHARACTERIZATION over the book's hypotheses with no covering datum, by Mac Lane's comma-category proof made transparent, so that the left adjoint's value at `d` reads back at `eq_refl` as the intersection in the comma; the monos lemma in a form stronger than the book's; the Corollary and Theorem 2 as biconditionals; the covering step `Adjunction/SAFT.v` assumed, derived over a product that depends on `d`; the representability and cocompleteness corollaries; and witnesses at `Sets` under `Untruncate`, at `Subsets Y` and at `Indiscrete`**. The book statements are the catalogue's summaries under `doc/plan/books` (Mac Lane's quoted verbatim in the header, Riehl's paraphrased); the printed books were not consulted, and fidelity is claimed to the catalogue text only. **The monos lemma, stronger than the book.** `comma_monic_of_underlying`: an arrow of `=(d) ↓ U` whose C-component is monic is monic, with no hypothesis; `underlying_monic_of_comma_monic`: the converse from ONE kernel pair (a `Pullback` of the underlying arrow with itself) in C and nothing about `U`, the kernel-pair object taking a comma structure through its diagonal (`kp_diag`, `kp_obj`, `kp_fst`, `kp_snd`); `comma_monic_iff_underlying_monic` is the biconditional over `HasPullbacks C`. Mac Lane's route asks the comma projection to CREATE kernel pairs, which needs `G` to preserve them; here `U` preserves nothing, so #438's `Construction/Comma/Creation.v` is neither needed nor in the file's closure, and C's homs may sit below D's (`hc <= h`). **The comma's structure.** `comma_cogenerator`: a `Cogenerator C` gives one of `=(d) ↓ U` indexed by `{ j : cog_index G & d ~> U (cog_obj G j) }` — Mac Lane's Q', indexed by the arrows OUT OF `d` — and `comma_WellPoweredAt`/`comma_WellPowered` carry well-poweredness from C to the comma through both halves of the monos lemma (`underlying_sub` reads a comma subobject in C), with no preservation of monos asked of `U`. **The Corollary (Mac Lane's cor1, Awodey 9.35).** `SAFT_wellpowered U comp cont G WP : { F : D ⟶ C & F ⊣ U }` takes `Complete C`, the cone-level continuity `PreservesImageLimit U`, a `Cogenerator C` (`Adjunction/SAFT.v`'s record) and #451's pinned `WellPowered C`, and nothing else; at each `d`, `SAFT_comma_initial` is #452's `special_initial_object_wellpowered_at` in the comma, fed `Comma_Complete cont comp`, `comma_cogenerator` and `comma_WellPoweredAt`, and the adjoint is assembled by `Theory/Universal/Arrow.v`'s transparent `LeftAdjointFunctorFromUniversalArrows`, NOT by `Adjunction/GAFT.v`'s `GAFT_from_initials`, which ends in `Qed`. `SAFT_left_obj` reads the value at `d` back at `eq_refl` as the C-object under `complete_intersection` of all subobjects of the comma's cogenerator product — Mac Lane's initial object of the comma. `SAFT_wellpowered_iff : { F & F ⊣ U } ↔ PreservesImageLimit U`, its necessity `right_adjoint_PreservesImageLimit`. **Theorem 2.** `HasSubobjectWidePullbacks C`: every family of subobjects of an object, indexed at a universe `q`, has a wide pullback — the book's "every set of subobjects of an object of A has a pullback" read as a LIMIT (`Structure/Pullback/Wide.v`'s `WidePullback`) with a class-sized index; "preserves all pullbacks of families of monics" is #448's `PreservesWidePullbacks`, reused unchanged. `SAFT_thm2 U comp cont G HW HU` intersects in C Mac Lane's corresponding family `least_index`, lifts the intersection to a comma object through `HU` (`least_lift`, `least_sub`, `least_le`) and feeds #452's `special_initial_object_least`; `SAFT_thm2_left_obj` reads the value back at `eq_refl` as the `WPull` of that family. The necessity of the second clause, `right_adjoint_PreservesWidePullbacks` — every right adjoint preserves every wide pullback, of monos or not, at every index universe — is NEW (at master 9613c9a5 a grep for the name over the `.v` files found no file, as the header records), and `SAFT_iff : { F & F ⊣ U } ↔ (PreservesImageLimit U * PreservesWidePullbacks U)` is Theorem 2 as the book states it. `Complete_HasSubobjectWidePullbacks` supplies `HW` from completeness at every index `q <= so`, while Theorem 2 asks it at `q` at or above the objects (`o <= q`, where `least_index` lives; N9 at `SAFT_thm2`'s binder, N26 at the body, where `least_index` is handed to `HW`), so completeness serves Theorem 2 only when the objects fit in the shape universe, Freyd's collapse regime (`Structure/Complete/Freyd.v`'s `small_complete_is_thin`). #452's `HasClassIntersections` CANNOT be Theorem 2's hypothesis: a greatest lower bound carries no cone for `U` to preserve, where the comma step needs a mediator into `U`'s image of a LIMIT cone. For an empty family `WidePullback` is a terminal object, not `x` as in Mac Lane; the header says so, and the proof never consults that case. **The covering step, derived (Cover.v).** `SubobjectCoverAt U comp G d W`: every `h : d ~> U c` factors through a subobject, named by a well-powering `W`, of `saft_prod U comp G d`, the product of the cogenerating family over the arrows OUT OF `d`; `saft_cover_at` PROVES it from `Complete`, `PreservesImageLimit` and the `Cogenerator` at EVERY `W` — Mac Lane's k depending on `h` (`saft_kappa`), the pullback of #452's `cogen_canonical_sub` along it (`saft_sub`), and continuity for the lift (`saft_lift`) — with no image factorization, no intersection and no monos lemma (a case-insensitive grep of the comment-stripped code finds none of the words, the header's measurement). `W` is only the datum's indexing: `Structure/WellPowered.v`'s `wp_trivial` supplies one at every object of every category (the probe's positive control `p453_cover_trivial`), and well-poweredness becomes a condition only at `GAFT`'s index pin (N14 at `SAFT_cover_wp_at`'s declared `w <= h`, N28 with `GAFT` applied directly), in `SAFT_cover_wp_at`/`SAFT_cover_wp`, Freyd's route (the solution set `saft_solution_set_at` handed to `GAFT`; `SAFT_cover_wp_is_GAFT` at `eq_refl`). That route is opaque, `GAFT` being `Qed`; `Corollaries.v`'s `SAFT_cover_wp_iso` relates its adjoint to `SAFT_wellpowered`'s by `Theory/Adjunction.v`'s `left_adjoint_iso`, the two not being convertible (probe N6). **The corollaries (Corollaries.v).** `continuous_Set_functor_representable K comp cont G WP : Representable K` is Mac Lane's "every continuous functor K : A → Set is representable" (Riehl's E.1 criterion, with well-poweredness in place of intersections), through `Adjunction/Representability/Sets.v`'s `representable_of_left_adjoint`, the representing object being the left adjoint at the singleton by `eq_refl` (`continuous_Set_functor_representable_obj`); `continuous_Set_functor_representable_iff` is the criterion as a biconditional, `Representable K ↔ PreservesImageLimit K`; `continuous_Set_functor_representable_at` asks well-poweredness at the ONE object `saft_prod K comp G SetsOne` and runs through `Adjunction/GAFT.v`'s `comma_initial_of_sols` at `GAFT`'s regime. `saft_colim`/`saft_cocomplete` are Riehl 4.7.13: `SAFT_wellpowered` at `@Diagonal C J`, with `C` the DOMAIN, over #450's `Diagonal_continuous` and #353's `Diagonal_left_adjoint_HasColimits`, delivering `Cocomplete@{h jo h o}`; the shapes are `Category@{jo h h}` with `jo <= h`, because the theorem shares `C`'s hom universe with `[J, C]`'s (probe N19 refuses the corollary at `h < jo`), and the colimit-datum universe `h` is inherited from #353's donor (N20). **Witnesses (Examples.v), every one closed under the global context.** At `Sets@{o so}` under one `Un : Untruncate@{o}`, with `Sets_Complete`, `Sets_Cogenerator_untruncate Un` and `Sets_WellPowered_untruncate Un`: `sets_id_saft` (and `sets_id_saft_cover` by the `GAFT` route) at `Id`, CIRCULAR since `Sets_Id_PreservesImageLimit` is read off `adj_id` (`sets_id_saft_iso`: the adjoint is `≈ Id`); `sets_hom_saft` at `HomFrom A`, whose continuity `hom_ContinuousFunctor` is proved directly — the one witness whose premises are not read off a known adjunction, its left adjoint (classically `A × −`) not identified; `sets_id_repr_saft`, `sets_hom_repr_saft` and `sets_saft_cocomplete` (conclusions already known in tree; for `sets_saft_cocomplete`, `Sets_Cocomplete` needs no `Untruncate`); and `sets_id_cover_separation`, ONE tuple at which `SubobjectCoverAt` holds at every `d` and `Adjunction/SAFT.v`'s `SubobjectCover`, fed `WellPowered_SubobjectIndex` of the same well-powering, implies `False` (`Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd`). THIN and unconditional: `subsets_inverse_image_saft` at `InverseImage f` on `Subsets Y` (`subsets_inverse_image_saft_iso`: the adjoint is `DirectImage f`); `subsets_inverse_image_thm2`, the one application of `SAFT_thm2` at a concrete category, every premise inhabited, `HW` by `Complete_HasSubobjectWidePullbacks (Subsets_Complete_free Y)` and both preservation premises read off `image_preimage_adjunction f`, so it shows the premises satisfiable together and builds no adjoint not already known; and `subsets_saft_cocomplete`. DEGENERATE and unconditional: `indiscrete_saft` at `Id` on `Indiscrete@{o j j} Type@{j}`, `j < o`, with the well-powering's index at `Set`, strictly below the homs (circular through `adj_id`), and `indiscrete_saft_cocomplete`. **No unconditional witness at a category that is not thin, among the candidates measured** (Examples.v's header): without `Untruncate`, the well-powerings of `Sets` at hand, `Sets_WellPoweredAt_up` and `wp_trivial`, both index strictly above the homs, and `GAFT`'s index pin and #451's `WellPowered` itself refuse the first (N11–N13); `Sets^op` is co-well-powered only one universe up (N25, N29); and the unconditional LARGE cogenerator `Sets_Cogenerator_large` is refused by both routes (N15, N16). Not surveyed: categories with objects at or below their homs (`trivial_small`), and `Grp`, which has no `Cogenerator` built. (CORRECTION, #455: one such category is surveyed now, with a negative outcome. `Instance/Top/StoneCech/Refutations.v` applies `SAFT_wellpowered` at the inclusion `CompHaus_Incl`, whose objects fit at its hom universe: `SAFT_CompHaus_Incl` with the well-powering `CompHaus_WellPowered`, which is `trivial_small`, and the vacuity pairs `SAFT_Incl_vacuous` and `SAFT_Incl_IEM_vacuous` under any well-powering. Its completeness premise is refuted under informative excluded middle, Freyd's collapse, at every shape the theorem accepts and at the instances Refutations.v's COVERAGE records, the `Top` slot at `h`, the proof slot at `c` and the Hausdorff proof's separating opens at the points' universe `o` (`CompHaus_not_complete_IEM`, `CompHaus_not_complete_IEM_below`, and the pairs `SAFT_Incl_IEM_vacuous` and `SAFT_Incl_IEM_vacuous_below`); `SAFT_CompHaus_Incl` is a conditional at more instances than those, where the premise stays classically false, a meta-argument. A cogenerating family with ¬¬-stable members costs double-negation elimination (`CompHaus_cogenerator_stable_DNE`), so these are not witnesses; the adjunction they would conclude is not refuted.) (CORRECTION, #454: the witnesses above are no longer the only ones, and Examples.v's and `Characterization.v`'s headers now say so. `Instance/Mod/Watts.v` applies the Corollary `continuous_Set_functor_representable` at the NON-thin `(RMod R)^op`, where `cog_separates` is `RMod_ring_separates` and does work, under one `Untruncate` spent on `RMod_CoWellPowered_untruncate` — at the functor M ↦ Hom_Ab(U M, A), `watts_theorem_coext`, whose representing object is ≅ `CoextObj R A` — and at `RMod R` under `Untruncate` and a cogenerator taken as a hypothesis, `watts_ex3_forget`; its `watts_theorem_adjoint` applies `SAFT_wellpowered` itself, generically in T. All inherit `Untruncate`, so "no unconditional witness at a category that is not thin" still holds of these theorems, `RMod R`'s one-universe-up well-powerings being refused at the pin as `Sets`' are (`Test/ProbeWatts454.v`'s N6 and N8); the Corollary's STATEMENT at `(RMod R)^op` holds unconditionally by another route, `Instance/Mod/Watts/Unconditional.v`'s `RModop_continuous_representable`, through `representability_theorem` and not the special adjoint functor theorem.) **Universes, measured by `About` (the headers' readbacks).** `SAFT_wellpowered@{o dobj h so c w s t pk pa …}` over `Complete@{so so h o}`, `PreservesImageLimit@{o h dobj h pk so pa so}`, `Cogenerator@{c o h}` and `WellPowered@{o h w s t}`, with `h <= so`, `c <= so` and `w <= h` (the last `WellPowered`'s own); `SAFT_thm2` adds `HasSubobjectWidePullbacks@{o h q r k}` and `PreservesWidePullbacks@{o h dobj h pw q r rd}` with `o <= q`, `h <= q`. `About` of the forty-seven constants of `Characterization.v`, searched for " = ", finds no equation between two universes a constant names (the header); `h < so` is accepted with `so < o` as well (the probe's controls `p453_small` and `p453_thm2_small`), so the `so = h` #452 recorded for its comma instantiation was minimization (the #453 correction in `Adjunction/SAFT/InitialObject.v`'s header). Four minimization collapses were found and removed, each measured, among them `t = pa` between `WellPowered`'s `t` and `PreservesImageLimit`'s auxiliary slot (from a top-level `comma_WellPoweredAt`, and again from assembling the Σ-type with the tactic `exists`), and the probe's `p453_t_below_pa` and `p453_pa_below_t` pin that it stays removed. Two equations are inherited from donors and pinned: `su = pa` in `continuous_Set_functor_representable_iff`, from `preserves_image_of_representable` (N21), and `h = cr` in `saft_cocomplete`, from #353's donor (N20); `HW` and `HU` share their C-side wide-pullback universe `r` (N10 at `SAFT_thm2`'s own binder, N27 at `SAFT_thm2_initial`'s body, where `wide_pullback_is_pullback` hands `HW`'s cone to `HU`). Cover.v's statements carry #452's unrepaired donor collapse `Cogenerator@{so o h}` (`cogen_prod` and `cogen_power` are not annotated). **Measured**: 93 constants — 47 in `Characterization.v`, 22 in `Cover.v`, 8 in `Corollaries.v` and 16 in `Examples.v` by `Print Module`, which lists no `Program` obligation — of which 8 are opaque (`Parameter` in that listing: `comma_monic_of_underlying`, `underlying_monic_of_comma_monic`, `kp_diag_fst`, `kp_diag_snd`, `least_lift_commutes`, `saft_point_commutes`, `saft_square`, `saft_lift_commutes`) and every other a transparent term, so the readbacks compute; each "Closed under the global context" by its fully qualified name (re-run on 2026-09-23 in one scratch file, 93 of 93, beside the instrument `Print Assumptions functional_extensionality`, which prints its axiom), and all 93 in ONE new block of the `print-assumptions` gate (`grep -c 'Print Assumptions' Makefile` 9448 → 9541); `Untruncate` is a hypothesis in the `Sets` witnesses' statements, a `Definition` (a Π-type) and not an axiom. Closures over `.Makefile.coq.d`, not counting the file itself: 112, 111, 149 and 179 (the headers' figures; `Adjunction/SAFT/InitialObject.v`'s 110 is reproduced by the same count); `Corollaries.v` is a satellite because `Adjunction/Representability/Sets.v`, `Adjunction/Diagonal/Limit.v` and `Construction/Comma/Creation.v` are in neither `Characterization.v`'s closure nor `Cover.v`'s. The development files add no `make todo` hit, and the six `.v` files whose headers #453 corrects carry none either before or after: the Makefile's `find | egrep` pipeline counted by `wc -l` gives 2521 at master 9613c9a5 and 2551 with #453, the 30 of growth being exactly the probe's lines opening with the refutation keyword, so the issue's "`make todo` adds no new hits" box is met by the development files and not by the probe (disclosed). `Test/ProbeSAFT453.v` carries those 30 refutations, as its header classifies them — the name-absence instrument, 8 CONVERSION (N1–N7, N18: the two readbacks discriminate, the `GAFT_from_initials` route does not read back, the two routes' adjoints and products are not convertible, and the lift's `Compose` universe is `cont`'s), 1 TYPING (N21), 1 BINDER (N8, raised by its own binder `Complete@{so so h o}`, which a binder instrument with a trivial body reproduces while those of the other negatives are accepted) and 19 UNIVERSE (N9–N17, N19, N20, N22–N29, where N26–N29 are the body-level and `GAFT`-direct forms of N9, N10, N14 and N25, those four being refused at a constant's own declared bound) — each classified by the error its copy of the whole file stops with once the keyword is stripped, each negative beside positive controls naming the same constants (56 definitions and examples, each shown live by wrapping it in a refutation in a copy of the whole file, and each closed under the global context), under the four targets' full import list with `Instance/Sets/SpecialInitial.v` added before N25 and N29, and with a guard block naming all 93 constants of the four target files. The files compile on Coq 8.19.2 and 8.20.1 against copies of prebuilt trees of the library for those versions, the files of the closure that differ from this tree's recompiled (the #453 builders' and folds' runs: `Characterization.v`'s header records its 47 constants closed there and the probe's header records the probe, the three satellites' runs being recorded in the build and fold reports, not in their headers); that is not `nix build .#category-theory_8_19`/`_8_20` of this tree, which is run on the committed revision. The prose claims #453 falsified are corrected in place and marked "CORRECTION, #453" — in `Adjunction/SAFT.v` (half (2)'s "of necessity", with the closing paragraph THE BOOK'S HYPOTHESES, WITHOUT THE DATUM), `Adjunction/SAFT/InitialObject.v` (its forecast of what #453 would consume, and "no comma cogenerator is built"), `Adjunction/SpanningArrow.v` (the second hypothesis of its NOT DELIVERED (3), open neither since #449 nor at right adjoints), `Adjunction/Representability/Sets.v` and `Adjunction/SAFT/Sets.v` — with an update, not a correction, in `Adjunction/SAFT/WellPowered.v`; and in the bullets and rows here and in docs/INHABITATION.md that repeated them. **STATED DEVIATIONS.** Awodey's first checkbox, "`SubobjectCover` becomes a theorem", is met by SUPERSESSION and not as worded: the old datum is refuted at `Id[Sets]`, where under `Untruncate` the book's hypotheses hold (`sets_id_cover_separation`), so it cannot be derived there, and the datum over the `d`-dependent product is the theorem in its place; the box's premise, that the step needs an epi–mono factorization, does not arise, the reshaped datum being derived with a pullback and its preservation. Awodey's second checkbox, well-poweredness as a real condition, is answered by `SAFT_wellpowered`'s premise, #451's pinned `WellPowered`, which the comma route consumes through `comma_WellPoweredAt`, and whose pin does work: the probe's N12 (`GAFT` applied directly) and N13 (`WellPowered` itself) refuse `Sets`' one-universe-up well-powering, which the unpinned covering step accepts. Riehl's 4.7.10 checkbox ("the in-tree statement's hypothesis list matches the book's") and the Epilogue's clause (iii) are NOT met as worded: her catalogue summary asks continuity and intersections of every collection of subobjects; the Corollary adds well-poweredness and asks no intersections, Theorem 2 adds `PreservesWidePullbacks`, and neither is her statement — whether her "collection" is small stays open until the printed page is read; the box's other clause, "the old `cover`-taking form kept only as a corollary, if at all", is met in its "if at all" branch, the old form being kept as an independent constant and not a corollary (its datum is refuted), its removal the maintainer's call. Her Corollary 4.7.13 checkbox is MET WITH STATED DEVIATION, by the same swap: `saft_cocomplete` has the Corollary's hypothesis list, well-poweredness in place of her intersections, at shapes `Category@{jo h h}` with `jo <= h`. Riehl 4.7.10, 4.7.13 in its intersection form and E.1(iii) are re-homed as their own issue, #1324, per the maintainer's decision. The first clause of Work item 3 is not delivered: no constant derives `HasSubobjectWidePullbacks` (or `PreservesWidePullbacks`) from `WellPowered` and `Complete`, so the Corollary is proved directly and `SAFT_wellpowered` is not derived from `SAFT_thm2`; the header says what a derivation would need and claims no impossibility. The GAFT-route theorem, called `SAFT_wp` in the #453 working plan, ships as `SAFT_cover_wp`. `SAFT`, `SubobjectCover`, `SAFT_solution_set`, `SAFT_of_WellPowered`, `saft_representable` and `Adjunction/SAFT/Sets.v`'s refutations are KEPT unchanged and corrected in place; they are surfaced as removal candidates for the maintainer, not removed, and the separation witness uses them. **NOT DELIVERED**: `HasSubobjectWidePullbacks` at an index at or above the objects at any category that is not thin, so Theorem 2 is applied only at the thin `Subsets Y`, circularly, and `SAFT_iff` is a conditional elsewhere (N22–N24 pin the completeness route's refusals at `so < o` and at `Sets`); an unconditional witness at a category that is not thin; Mac Lane's creation route for the monos lemma; readbacks of the left adjoint's action on arrows or of the unit; the left adjoint of `HomFrom A`; a bridge from `HasSubobjectWidePullbacks` to `HasClassIntersections`; representability or cocompleteness over Theorem 2's hypotheses or over Riehl's intersections, or at shapes above the homs; any application of `SAFT_wellpowered_iff`, `SAFT_iff`, `continuous_Set_functor_representable_iff` or `continuous_Set_functor_representable_at` at a concrete category (a grep of each name over the comment-stripped `.v` files outside `Test/` and `doc/` finds its defining file only); annotations on `cogen_prod` and `cogen_power`. No `Instance` is registered.

## Structures (Internal Properties)
- **Structure/Thin.v**: Thin categories — `Thin C := ∀ x y (f g : x ~> y), f ≈ g`, closed under opposites, with every morphism monic and epic and every opposing pair an iso candidate (`thin_iso`); `thin_preorder C` (inhabited-hom on objects, Prop-squashed; deliberately renamed to avoid shadowing `Theory/Category.v`'s exported Type-valued `hom_preorder` instance, which is the formulation that would dissolve the choice hypothesis — the squash is forced by `Instance/Proset.v`'s Prop-valued `Proset`) with its `PreOrder` witness (a plain `Definition`, deliberately not an instance). The thin-side reconstruction `thin_Proset_iso : Thin C → HomChoice C → C ≅[Cat] Proset (thin_PreOrder C)` takes the morphism-choice hypothesis explicitly (extracting a witness from the Prop-valued ∃ needs it; on prosets it is discharged, homs being the relation itself). Mac Lane §I.2 construction 7's correspondence, with strengths measured per leg in Instance/Proset/Order.v: `thin_preorder_Proset` recovers the relation pointwise-iff unconditionally, and on the nose (`thin_preorder_Proset_eq`) under propositional+functional extensionality taken as explicit hypotheses (sufficiency is what is proved; necessity is not claimed)
- **Structure/Complete/Freyd.v**: Mac Lane §V.2 Proposition 3 (book p. 114, `maclane:V.2:prop3`) + Awodey §9.8 Proposition 9.34 (`awodey:9.8:prop34`) + Riehl §3.7 Proposition 3.7.3 and Epilogue §E.1 (`riehl:3.7:prop3`, `riehl:E.1:thm-large-products-poset`; Definition 3.7.2, `riehl:3.7:def2`, is the size measure the proposition is stated against, consumed here as an ORDER and NOT delivered as a cardinal — its checkbox is not met as written) — **Freyd's collapse: a category whose arrows are indexed by a type at which it has products, and whose hom-setoids are decidable, is thin; and a product is a greatest lower bound in the preorder reflection of any category**. The smallness witness is `ArrowIndex C` (a type `ai_index` encoding every arrow, decoded RELATIVE TO A DEFAULT with the round trip the identity up to `≈` for every default — `|hom x y| ≤ |ai_index|` on every inhabited hom-set, Riehl's cardinality form, so LARGE categories are covered and her §3.7 stronger-hypothesis checkbox is met); Theory/Size.v's `Small` is deliberately not consumed (per-hom resizing through `ObjEq` transports; the proof needs one index type). It is not vacuous: `canonical_ArrowIndex : ObjDecEq C → ArrowIndex C` at `TotalMor C` over Construction/Quotient.v's existing `ObjDecEq` (an earlier revision declared a duplicate `DecObj`; the audit found the twin), the round trip by `Coq.Logic.Eqdep_dec.UIP_dec` (a theorem); the terminal category satisfies all three hypotheses, so they are jointly satisfiable, and no non-trivial in-tree instance is exhibited. **The constructive kernel** `freyd_no_separated_pair : False` consumes one elementary `IsIndexedProduct (fun _ : K => b) P pr` and a boolean separator of the pair (respecting `≈`, true on `f`, false on `g`): the `2^K` arrows `fr_mk phi : a ~> P`, read back by `fr_decode`, make the index surject onto `K → bool`, contradicting the four-line `freyd_cantor_bool` (a named twin of Instance/Fun/Discrete.v's `cantor_bool`, which a Structure/ file cannot require; relocation surfaced) — no axiom, no excluded middle, no decidability, no `Set`. **`freyd_thin : ArrowIndex C → HasIndexedProducts C → DecHom C → Thin C`** (`DecHom C := ∀ x y (u v : x ~> y), (u ≈ v) + (u ≈ v → False)`, a `sum` since `≈` is Type-valued) with `freyd_thin_canonical` and the dual `freyd_thin_dual` (products in `C^op`, `Opposite_Thin`); **the issue's pinned `small_complete_is_thin : ArrowIndex C → Complete C → DecHom C → Thin C`** and `small_cocomplete_is_thin` (Riehl §E.1's "or coproducts", via Construction/Product/Limit.v's `Complete_op_of_Cocomplete`) through the NEW `Complete → IsIndexedProduct` bridge `complete_iprod` (any `d : A → C`, through Structure/Limit/Comparison.v's `DiscreteCat_Functor'` and `discrete_IsIndexedProduct_of_IsLimitCone`; an earlier revision of this bullet added "the unannotated route pins C's hom level to `Set`, probe N1 "Cannot enforce Set = uh" — the carrier of the pin is Structure/Limit/Product.v's `limit_is_indexed_product`, stated at a category whose hom and proof levels are the literal `Set`; `DiscreteCat_Functor`'s own binder leaves C free", which held while `DiscreteCat_Functor` was unannotated; it was annotated at `Instance/Discrete.v` in the PR "algebraic carriers are sets" (2026-09-17), probe N1 in `Test/ProbeFreyd423.v` turned over and is now a positive control, and `DiscreteCat_Functor'` is a transparent alias of the original rather than a repair of it. The headline's own signature is unaffected: re-measured by `About` on 2026-09-17 it agrees binder for binder with the `small_complete_is_thin@{u u0 u1 u2 u3}` reading this bullet already quotes, and carries no `Set` before or after). **The wall, located**: from `f ≉ g` alone `fr_mk_injective` and `fr_inj_injective : fr_inj phi = fr_inj psi → ∀ k, phi k = psi k`, an injection `(K → bool) → K` on the nose; refuting it means deciding the hom-setoid, which is exactly Hyland's effective-topos model (Structure/Complete.v) — so `DecHom` is a genuine hypothesis, not a formality, and the file stays axiom-free because the classical content is a hypothesis; the probe carries the injection as a positive control and no refutation (unprovability is metatheoretic). **The second half**: `complete_has_glbs : Complete C → HomChoice C → HasAllMeets (thin_preorder C)` — a product is a glb in the preorder reflection of ANY category, so neither smallness nor thinness enters and the issue's pinned NAME `small_complete_has_glbs` is not declared (it would assert an unused hypothesis); `HomChoice` (Structure/Thin.v) is needed because the lower bounds arrive `inhabited`-squashed; `complete_Proset_Complete : Complete C → HomChoice C → Complete (Proset (thin_PreOrder C))` closes the loop with #422's `proset_Complete_iff_all_meets` (Instance/Proset/Limit.v). **Stale premises**: the issue's "Verified ABSENT" is wrong on three of four clauses — `Thin` (Structure/Thin.v), `Small`/`LocallySmall`/`TotalMor` (Theory/Size.v) and Cantor (Instance/Fun/Discrete.v) all exist, #422 landed, and Theory/Metacategory.v's `cardinality`, its note and `ThreeArrows_card_3` all exist, the first not where the issue cites it; still absent: `SmallCategory`/`is_small`, a morphism cardinal (none added — the ORDER hypothesis is what the proof uses), and any `Complete → HasIndexedProducts` bridge (SAFT.v builds its product by hand). Structure/Complete.v edited LINE-NEUTRALLY to point at the theorem, keep the sentence that instantiating `Complete` identifies the shape's hom universe with C's, and name `DecHom` (the Instance/Sets/Products.v pointer still lands); GAFT.v and Poset.v untouched (pointers surfaced). **Universes** (`About` under `Set Printing Universes` on all 38): 35 of the 38 blocks carry no equation, while `complete_iprod`, `complete_iprod_obj` and `complete_iprod_proj` each carry `o = u0`, `h = p`, `h = uh`, `h = up`, `uh = up` — the limit vocabulary identifying the shape's hom and proof levels with C's and the shape's object level with `Complete`'s index, the fact Adjunction/SAFT.v records in prose and the FromProducts.v bullet as `u2 = u4` (an earlier revision said "NO equation in any block"; the audit measured these), and no equation reaches the kernel, `freyd_thin`, `small_complete_is_thin`, `complete_has_glbs` or `complete_Proset_Complete`; `ArrowIndex@{u u0 u1}` and `ArrowIndex_op` EMPTY blocks; the kernel `freyd_no_separated_pair@{u u0 u1 u2}` carries `u0 <= u2`, `u1 <= u2` and no stdlib bound at all, `freyd_thin` only `False_rect.u0`; `td` is bounded by `eq_rect`/`Projections` only and `td_enc`, `canonical_ArrowIndex`, `freyd_thin_canonical` add `Eqdep_dec.UIP_dec.u0`/`eq_rect_r.u1` (the price of `UIP_dec`), the `Complete`-fed constants inherit `JMeq`/`EqdepFacts.eq_sigT_sig_eq`/`eq_rect_r` bounds through `DiscreteCat_Functor'`; `small_complete_is_thin@{u u0 u1 u2 u3} : ∀ {C : Category@{u2 u3 u3}}, ArrowIndex@{u0 u2 u3} C → Complete@{u u0 u3 u2} → DecHom@{u1 u2 u3} C → Thin@{u1 u2 u3} C` with `u0 <= u` and no `Set`; the ONLY `Set` in any block is `complete_Proset_Complete`'s conclusion `Complete@{u u Set u0}`, the Prop-valued homs of `Proset`, inherited from Instance/Proset/Limit.v. **38/38 constants closed under the global context, zero `Axioms:` lines** (19 `def`, 14 `prf`, 4 `proj`, 1 `rec` declaration entries; `Build_ArrowIndex` appears in the `.glob` only as an `R … constr` reference; an earlier revision counted 39 with the duplicate `DecObj`), all gated fully qualified; thirteen `Qed`, one `Defined` (`complete_has_glbs`, flips freely, kept by the data convention); closure 72 excluding self (Instance/Proset/Limit.v 23 at the margin, Comparison.v 7, Construction/Product/Limit.v 4, Construction/Quotient.v, Thin.v and Size.v 1 each, ten `Require`s 0; the one droppable `Require` of an earlier revision, Theory.Functor, dropped); zero name collisions. `Test/ProbeFreyd423.v` mirrors the `Require` list and carries **3 refutation commands = 1 instrument + 2 negatives of TWO kinds** — N1 UNIVERSE (the unannotated route under `Constraint Set < uh`, beside `complete_iprod` at the same category), N2 TYPING (the anonymous record literal against `ArrowIndex (C^op)`, "has type ArrowIndex C while it is expected to have type ArrowIndex C^op", beside `ArrowIndex_op` and its `eq_refl` involution) — each stripped one at a time in a copy of the whole file; positive controls `canonical_ArrowIndex`, `fr_inj_injective` and `Small` coexisting with products over the category's own arrows (a coexistence check only: `Small`'s extra levels are fresh and bounded only below by C's, so the binder adds no refutable constraint); guard coverage 25 identifier tokens inside / 14 also named outside (comments stripped) with eleven exhaustive exceptions (the keyword, seven binder names and keywords of the record literal, the notation token `op`, the refuted declaration's name, the absent name); rename-simulated 6/6 (`ArrowIndex`, `DiscreteCat_Functor`, `limit_is_indexed_product`, `TotalMor`, `ai_dec_enc`, `complete_iprod`), every first break on a positive line; `make todo` grows by the probe's 3 refutation commands only (2215 → 2218), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: thinness without `DecHom` (Hyland; the wall pinned positively); `small_complete_has_glbs` under that name; a morphism cardinal (the `riehl:3.7:def2` box); a non-trivial in-tree instance of the three hypotheses (docs/INHABITATION.md untouched); a `HasIndexedProducts C` class instance from `Complete C` (only the elementary product per family — the class's single index universe not attempted); relocating `cantor_bool` to Lib/ and the GAFT.v/Poset.v pointers (surfaced); no edit to Thin.v, Size.v, Quotient.v, Instance/Proset/Limit.v, Comparison.v or Instance/Fun/Discrete.v.


- **Instance/Proset/Order.v**: The order-theoretic development over `Instance/Proset.v` — Seven Sketches §§1.2.2/1.3.1 and Riehl §1.2. `Equivalence_is_PreOrder`/`PreOrder_Symmetric_is_Equivalence` (a preorder is an equivalence minus symmetry, both ways); discrete and codiscrete preorders with codiscrete terminal and discrete initial among preorder structures on a carrier (uniqueness clauses included); `comparable x y := (x ≤ y) + (y ≤ x)` with `TotalOrder` characterized by total comparability and a header note separating comparability from decidability; discrete-preorder comparability (self-comparable, distinct incomparable); `nat` a `TotalOrder` with the reverse order exhibited as `(ℕ,≤)^op`; `(Proset P)^op ≅ Proset (flip P)` in Cat (Riehl §1.2); infimum/supremum named and proved to coincide with the discrete-two-object limit/colimit over an *arbitrary preorder* (totality is needed only for existence, not the identification), two-element order worked, colimit half also restated through `IsAColimit` — stated directly; the general identification lemma belongs to issue #422 (whose suggested module is this same directory), and the header records that these are its binary-shape case, to be consumed rather than restated when #422 lands
- **Structure/Terminal.v**: Terminal and initial objects, and their uniqueness — the bundled `Terminal` class with `one`/`one_unique` and, by the `Initial C := Terminal (C^op)` notation, `Structure/Initial.v`'s `zero`/`zero_unique`; uniqueness up to a *unique* isomorphism in three strengths (`terminal_unique` + `terminal_arrow_unique`, packaged as the single `terminal_unique_up_to_unique_iso : ∃! _ : 1 ≅ 1, True` over `iso_setoid`, with the initial duals), and `zero_object_unique`/`zero_object_unique_initial` joined by the proved `zero_object_unique_compat` (Structure/ZeroObject.v). The object-level predicates `IsTerminalObj`/`IsInitialObj` (`∀ x, Unique (fun _ : x ~> c => True)`) convert both ways with the bundled classes and are what let terminality be used as a *property*: repleteness `Terminal_iso`, the full subcategory `TerminalObjects` with `TerminalObjects_Replete` and singleton hom-sets, and — given an inhabitant — `terminal_sub_contractible`, Riehl's Lemma 1.6.16 in full (`EquivalenceOfCategories (Erase (Sub C TerminalObjects))`; the "empty OR contractible" disjunction is *not* stated, since deciding it is excluded middle for "C has a terminal object" — Construction/Subcategory/Terminal.v). `Terminal_transport`/`Initial_transport` carry either object along an equivalence (Theory/Equivalence/Terminal.v). Riehl Exercise 1.6.i gives `terminal_initial_arrow_iso` and with it `ZeroObject_from_arrow`, the first in-tree construction that *derives* `zero_coincide` rather than assuming it. Terminality as a universal property is `TerminalIsUniversalProperty`/`InitialIsUniversalProperty` — the instance `Structure/UniversalProperty.v`'s `univ_property_unique_up_to_unique_iso` had always lacked at its own illustrating case — with `terminal_unique_up_to_unique_iso_via_UP` re-deriving the concrete statement through it (Structure/UniversalProperty/Terminal.v). Strict initiality is `initial_strict : (x ~> 0) → x ≅ 0` with `initial_strict_unique` (Structure/BiCCC.v — cartesian closed + initial suffices, no `Cocartesian`), instantiated at `Sets` and at skeletal `FinSet`, where it computes to `∀ x : nat, (x ~> 0) → x = 0` (Structure/BiCCC/Strict.v)
- **Structure/Cartesian.v**: Products via universal property
- **Structure/Cartesian/Closed.v**: Exponentials and internal hom (the live
  cartesian-closed development; `Structure/Closed.v` is an incomplete
  Eilenberg–Kelly stub whose `Class Closed` is not yet in force). The currying
  adjunction is packaged in Structure/Cartesian/Closed/Adjunction.v: the
  endofunctors `Prod_Functor S = (− × S)` and `Exp_Functor S = (−)^S` (the
  latter the partial application of Functor/Hom/Internal.v's internal-hom
  bifunctor, `Exp_Functor_InternalHom`), the named naturality lemma
  `eval_natural` with its bundled `eval_Transform : (−)^S × S ⟹ Id`, and
  `Curry_Adjunction : Prod_Functor S ⊣ Exp_Functor S` in the hom-set form of
  Theory/Adjunction.v — `curry`/`uncurry` as the transposes and `eval` as
  counit, all three by `reflexivity`, with the triangle identities inherited
  from the general theory; this turns the prose remark in Theory/Adjunction.v's
  header into a theorem. `Curry_Representable` is the first instance of
  `Functor/Representable.v`'s class (and its first consumer: resolution finds
  it, and the class's `Representable_to_obj` coercion reads the representing
  object back), witnessing Riehl's Example 2.1.6(iv) — the presheaf
  `C(− × S, B) : C^op ⟶ Sets` is represented by `B^S`, with the Yoneda
  universal element `eval` (`Curry_repr_universal`). That is a claim about
  THAT class: representability content also lives under
  `Structure/UniversalProperty.v`'s `IsUniversalProperty`, which has three
  instances. Note also that the counit's being `eval` on the nose is a
  consequence of the transposes alone, not of defining the exponential
  endofunctor directly — what the direct definition buys is that the four
  naturality fields discharge from the `Closed` corollaries verbatim
  (audit-corrected). The `Sets` model, where the counit computes to
  `e(h, s) = h s` and the transpose of addition on a `nat` setoid witnesses
  non-vacuity, is Instance/Sets/Cartesian/Closed/Adjunction.v, which also
  carries the Mac Lane §V.4 corollary that `− × X` preserves all colimits
  (`Sets_prod_preserves_colimits`, `CCC_prod_preserves_colimits`) — the
  library's only colimit-preservation witness of this kind
- **Structure/Cartesian/Closed/Natural.v**: Naturality of the exponential laws (Mac Lane §II.5 Ex 2; Riehl §1.4 Example 1.4.11) — the four pointwise isos upgraded to natural isomorphisms with their sides packaged as eight DIRECT functor records over product/opposite domains: `exp_prod_l_natural : @Isomorphism ([(C^op ∏ C^op) ∏ C, C]) …` and `exp_prod_r_natural` over Cartesian+Closed alone, `prod_coprod_r_natural`/`exp_coprod_natural` after a mid-section Cocartesian Context (so the first two never carry it). The components ARE the existing `exp_prod_l`/`exp_prod_r`/`prod_coprod_r`/`exp_coprod` DEFINITIONALLY — eight `reflexivity` agreement lemmas (stated with `≈`, morphisms) plus eight `eq_refl` object-action Examples (objects) — so the upgrade adds naturality, not new components. The transpose triangle on ARBITRARY generalized elements relates the internal iso to `exp_iso`'s legs: `exp_prod_l_transpose : to exp_prod_l ∘ f ≈ curry (curry (uncurry f ∘ to prod_assoc))` and `_untranspose` dually. Two per-variable SPECIMEN corollaries — the joint square instantiated at identity arrows, covering Law 1's base variable and Law 4's first summand only, the file saying so — since the joint squares imply every per-variable square; `NaturalIso_of_pointwise` (pointwise family + one square ⟹ natural iso) and the `ihom` calculus stated for reuse (`ihom` IS `InternalHomFunctor`'s fmap read in C, machine-checked at Leibniz `eq_refl` by `ihom_is_InternalHomFunctor_fmap`); `prod_assoc_to_natural`/`_from_natural` general but disclosed as not consumed by the squares. Engineering finding recorded in-file: an inline `fobj` inside `Program Definition` lets Program defer an unresolved `Cartesian` instance into an obligation that `Unset Transparent Obligations` (Lib/Foundation.v) makes OPAQUE — `fobj[F]` then converts with nothing; elaborate object/arrow actions as ordinary Definitions first. The Cat note is Instance/Cat/Exponential.v: all four pointwise isos AND all four `_natural` upgrades instantiate at Cat with explicitly spelled functor-category types (`[C ∏ D, E] ≅ [C, [D, E]]`, `[C, D ∏ E] ≅ [C, D] ∏ [C, E]`, `C ∏ (D ∐ E) ≅ (C ∏ D) ∐ (C ∏ E)`, `[D ∐ E, C] ≅ [D, C] ∏ [E, C]`), inhabited by the generic instances so the two readings agree by conversion — no universe wall. The decategorification half (Riehl's second clause) is Instance/FinSet/Decategorify.v: `Card_Groupoid : Groupoid Set_f ⟶ DiscreteCat nat` — THE cardinality functor from the groupoid of finite sets and bijections to the discrete category of naturals (fobj `setf_cardinality`, fmap `setf_cardinality_iso_invariant`, which forgets WHICH bijection — that forgetting IS decategorification; laws by axiom-free `UIP_dec` on nat) — with the identity-on-objects skeletal shadow `Card_FinSet : Groupoid FinSet ⟶ DiscreteCat nat` (fmap := `FinSet_skeletal`), and the four arithmetic identities DERIVED from the categorical isomorphisms — each PROOF TERM is `λ x y z, FinSet_skeletal <the iso>` with no arithmetic induction of its own (the counting lives inside `FinSet_skeletal`'s pigeonhole engine and FinSet's instance layer, per the file's disclosure; none of `lia`/`Nat.pow_*`/`Nat.mul_add_distr_l` appears): `nat_exp_prod_l : z^(x·y) = (z^y)^x`, `nat_exp_prod_r : (y·z)^x = y^x·z^x`, `nat_prod_coprod_r : x·(y+z) = x·y+x·z`, `nat_exp_coprod : x^(y+z) = x^y·x^z`, each literally `FinSet_skeletal` of the corresponding iso at skeletal FinSet (whose product/coproduct/exponential object actions are `*`/`+`/`^` on nat), with `nat_exp_coprod_is_Card_image` identifying one as `fmap[Card_FinSet]` of the iso by `eq_refl`, `DiscreteCat_nat_Thin` recording that the naturality content evaporates in the discrete target, and the header disclosing that `lia` would prove the identities — the derivation route is the content. Every `=` in that file is between OBJECTS (nats) or is a `DiscreteCat` hom (an equality proof), the setoid discipline's sanctioned exceptions
- **Structure/Cartesian/Closed/Composition.v**: Mac Lane §IV.6 Exercise 4 (book p. 98, `maclane:IV.6:ex4`) — **internal composition in a cartesian closed category, its associativity, and the payoff that such a category is enriched over itself**. `internal_compose {a b c} : c^b × b^a ~> c^a := curry (eval ∘ second eval ∘ to prod_assoc)` and `internal_id {x} : 1 ~> x^x := curry exr`, both plain `Definition`s with `Arguments … : simpl never`; the DEFINITION spends exactly two `Closed` fields (`exponent_obj`, `exp_iso` — `curry'`/`uncurry'`/`eval'` are defined fields built from `exp_iso`, not independent), while the PROOFS spend the beta law `ump_exponents'` through three `eval`-level rewrite rules (`eval_internal_compose_fork`, `eval_internal_id_fork`, `eval_ihom_fork`), each `eval_curry` at the respective transpose. **THE ISSUE ASKS FOR NATURALITY IN ALL THREE OBJECTS AND THAT IS NOT WELL-TYPED — MEASURED, NOT ARGUED**: in `c^b × b^a` an arrow `f : b1 ~> b2` acts CONTRAVARIANTLY on the left factor and COVARIANTLY on the right, so the only available action is `split (ihom f id) (ihom id f)` between MIXED objects `c^b2 × b1^a ~> c^b1 × b2^a`, and ascribing it at the type a functor of `b` would need is rejected — pinned as a TYPING negative whose error carries NO `cannot unify` and NO universe clause, with the SAME fact on the TYPES pinned separately as a CONVERSION negative so no `reverse_coercion` can false-pass it. Genuine naturality holds in `a` and in `c` only (`internal_compose_natural_c`, `internal_compose_natural_a`), packaged as real `Transform`s `ComposeSrcC a b ⟹ ExpBase a` and `ComposeSrcA b c ⟹ ExpExp c` (the second between two `C^op ⟶ C`) whose components ARE `internal_compose` at `eq_refl`; **for `b` the honest statement is DINATURALITY, and it is delivered as an inhabitant of the tree's own vocabulary rather than as a bare equation** — `internal_compose_dinatural` is the equation, `internal_compose_Cowedge_cond` reads it as `Structure/Coend.v`'s `Cowedge_cond` at the mixed-variance `ComposeB a c : C^op ∏ C ⟶ C` with `ComposeB a c (b1,b2) = c^b1 × b2^a`, and `internal_compose_Wedge := covariant_cowedge …` is a cowedge for it with CONSTANT apex `c^a`, typed `Wedge (Opposite_Functor (ComposeB a c))`. **Associativity is stated in exactly the shape the enrichment class needs** — `internal_compose ∘ split internal_compose id ≈ internal_compose ∘ split id internal_compose ∘ to prod_assoc`, `Construction/Enriched.v`'s `ecompose_assoc` — costing **fourteen** tactic lines plus two lemmas, because `to prod_assoc` is not syntactically a fork and `exl_fork`/`exr_fork` will not fire on it without an intervening `simpl`, which the two lemmas localise (`exl_prod_assoc_r`, `exr_prod_assoc_r`) — an inline `simpl` was available and untried, so they are not shown to be FORCED; both sides then reduce to one normal form and close by `reflexivity`. **THE `Sets` AGREEMENT IS GRADED, MEASURED STRICT FIRST, AND ITS CAUSE LOCATED RATHER THAN GUESSED**: POINTWISE it holds at `eq_refl` (`internal_compose (g,f) t = g (f t)`, and `internal_id u t = t`), the WHOLE-MORPHISM form is REFUTED at `eq_refl` and pinned, and `sets_internal_compose_underlying` shows the two underlying FUNCTIONS are the same term by `eq_refl`, so what differs is exactly the rebuilt `proper_morphism` certificate of the `SetoidMorphism` record; `≈[Sets]` is delivered. **THE PAYOFF IS `CCC_Enriched : @Enriched C CC_Monoidal`, WITH ALL THREE LAW FIELDS FREE** — each a bare `exact` of a theorem stated in Cartesian vocabulary, nothing weakened, the content sitting entirely in `internal_compose_id_left`/`_right`/`_assoc` — which is possible because over `CC_Monoidal` the tensor IS the product, re-checked by four `eq_refl` Examples so a later change is caught (`bimap f g = split f g`, `to tensor_assoc = to prod_assoc`, `to unit_left = exr`, `to unit_right = exl`), with `Sets_Enriched := CCC_Enriched` the instantiation. **PRIOR ART IS DISCLOSED RATHER THAN IMPLIED AWAY, correcting the issue**: self-enrichment is ALREADY witnessed in tree — `Construction/Enriched/Ab.v`'s `Enriched_Ab_itself : @Enriched Ab Ab_Monoidal` — so what is new is narrower still: a CARTESIAN CLOSED category enriched over itself. **`CC_Monoidal` is NOT a first either, and the reason it looks like one is an ALIAS** — `Construction/Enriched/Two.v`'s `Enriched_of_TwoPreorder` is an `@Enriched _2 Two_Monoidal` and `Two_Monoidal` IS `@CC_Monoidal _2 Two_Cartesian Two_Terminal` at `eq_refl`, through the plain `Definition` at `Structure/Monoidal/Cartesian.v`, so a name grep for `CC_Monoidal` (18 files) does not see it; that instance is not self-enrichment, its `eobj` being `tpre_carrier P` rather than the objects of `_2`. **A `Program` HAZARD IS NARROWED BY MEASUREMENT rather than restated**: writing a functor's object action INSIDE its `Program Definition` can make `fobj` convert with nothing (the hazard `Structure/Cartesian/Closed/Natural.v` records), and it bit here — but the boundary is NARROWER than "any inline object action", since a naive clone whose inline action is an `exponent_obj` ALONE DOES reduce while the one carrying a `product_obj` does not, both halves shipped in the probe as a passing control beside the refutation; all five functors elaborate their object and arrow actions as ordinary `Definition`s first. **71/71 constants closed under the global context with ZERO `Axioms:` lines** (53 source declarations + 18 `Program` obligations, counted on the WHITESPACE-FLATTENED `Print Module` output, which WRAPS its module head — a line-anchored sweep harvests the spurious name `Composition` and reports 72); `Instance/Coq.v` is NOT required at all, so `Coq_Closed`'s one axiom (`Instance/Coq.v`) is not inherited and the `Sets` clause, whose `Sets_Closed` is axiom-free, costs nothing. Universes measured off BOTH binder and block: every constant is over `C : Category@{u u0 u0}` — hom identified with proof — in the BINDER, while **not one of the 71 constraint blocks contains a universe equation** (all `<` or `<=`), so reading blocks alone would report no identification, and a sweep of all 71 `About` dumps finds **zero** word-bounded `Set`. Closure **43** modules excluding self, measured per require: `Structure/Monoidal/Internal/Product` costs 11, `Structure/Cartesian/Closed/Natural` 5, `Structure/Coend` 2 with `Structure/Wedge` costing 0 on top of it, and every other require 0 or 1; **`Exp_Functor` is NOT the only in-tree candidate, contrary to a first draft of this bullet** — `Functor/Bifunctor/Partial.v`'s `Partial_l`/`Partial_r` applied to `Functor/Hom/Internal.v`'s `InternalHomFunctor` serve for BOTH targets, agreeing with the shipped functors on BOTH actions at `eq_refl` — no residue at all, only the whole functor RECORD refused because `Program` rebuilds the law fields — at a marginal cost of **1** module (43 → 44, `InternalHomFunctor` already being inside the 43); they are not adopted only because that is a refactor rather than a correction, and the hand-built definitions are what the `Program` hazard note is about. `Structure/Cartesian/Closed/Adjunction.v`'s `Exp_Functor` fits worse and is read off its source rather than required, requiring it taking the closure to 47 — its object action agrees but its arrow action is `curry (f ∘ eval)` where `ihom id f` is `curry (f ∘ eval ∘ second id)`. Exactly ONE `Defined` (against 34 `Qed`), `internal_compose_Cowedge_cond`, kept so by the data convention and measured NOT load-bearing by flipping it alone to `Qed`. `Test/ProbeComposition391.v` mirrors the target's `Require` list and carries **6 `Fail`s = 1 scope-free instrument check + 5 negatives of THREE kinds** told apart by the error TEXT (3 CONVERSION, 1 TYPING, 1 FORMABILITY at `Constraint ch < cp` against four controls accepted at those very levels), each stripped ONE AT A TIME and compiled alone with the whole error read, rename-simulated **17/17** with each name renamed in the TARGET ONLY (a whole-file rename is a no-op by construction) and every break landing on a `Check` control line, never inside a `Fail`; `make todo` grows by 6, all `Fail` commands in the probe, zero in the library. NOT delivered: no coend or end of `ComposeB`, so nothing says the cowedge is UNIVERSAL; no `EnrichedFunctor` and no comparison of `CCC_Enriched` with `Construction/Enriched.v`'s `Category_is_Enriched_over_Set`; no relation between `ExpBase` and `Exp_Functor`; no monoidal-closed generalisation over `Structure/Monoidal/Closed.v`; no witness at a cartesian closed category other than `Sets` — in particular nothing at `Coq` — which the issue's own work item 2 ASKS for — deliberately declined so that every constant's closure claim stays true; and nothing is registered as an `Instance`.
- **Structure/Monoidal.v**: Tensor products with coherence
- **Structure/Monoidal/CopyDiscard.v**: Copy/discard (gs-monoidal) categories — comonoid supply with no naturality; deterministic morphisms and the wide subcategory Det in CopyDiscard/Deterministic.v
- **Structure/Ring.v**, **Structure/Lattice.v**: Mac Lane §III.6 (book p. 75) — **ring objects and lattice objects by INTERNALIZATION**, the two examples Mac Lane names next after monoid and group objects, over `Cartesian` + `Terminal` and reusing `Structure/Monoid.v`'s `Monoid := @MonoidObject C CC_Monoidal`. **THE ISSUE'S SUGGESTED NAME WAS UNUSABLE, NOT MERELY CONFUSING**: `RingObject` (`Theory/Algebra/Rig.v`) AND `RigObject` are both in scope in `Structure/Ring.v`, whose `Sets` section compares against them (`Structure/Lattice.v` requires neither and compares against its own `SetoidLattice` instead) — hence `InternalSemiring`/`InternalRing`/`InternalSemilattice`/`InternalLattice`, with the header recording that the tree's own "full spelling = internal, abbreviation = set-level" convention (`GroupObject`/`GrpObject`) could NOT be followed because Rig.v inverts it. **THE MATHEMATICAL CONTENT IS WHAT IS DERIVED RATHER THAN ASSUMED, AND BOTH DERIVATIONS ARE MATCHED BY REFUTATIONS SHOWING THE ASSUMPTION IS OTHERWISE NECESSARY.** For a RING, **annihilation is a THEOREM** — `ring_annihilate_l`/`_r` via `ring_cancel_idem` (`k ≈ k + k` forces `k ≈ 0`), a morphism-level cancellation chase with NO elements — so `InternalRing_InternalSemiring` is a DERIVATION, not a repackaging; and `bool_or_not_annihilating` shows why a SEMIRING must assume it, disjunction on `bool` as BOTH halves satisfying commutativity and BOTH distributivity laws while REFUTING annihilation — so the annihilation PAIR is not implied by the rest, and `isr_annihilate_l` in particular is not; only the `_l` shape is refuted, `isr_annihilate_r`'s independence following from `bool_or_comm` but not stated separately. For a LATTICE, **idempotence is DERIVED from absorption alone** (`lattice_join_idem`/`lattice_meet_idem` — no unit, no unitor, no commutativity), so it is not a field, with `lattice_bot_meet`/`lattice_top_join` deriving the bound annihilations the same way and `bool_join_not_absorbing`/`bool_xor_not_idempotent` showing absorption and `isl_idem` are themselves independent. `InternalRing_GroupObject` passes `ir_add` through UNCHANGED with no transport anywhere — but read the inverse fields precisely: the ring's own `ir_neg_left`/`ring_neg_right` are REJECTED at those positions (`GroupObject` wants the `bimap`/`∆`/`eliminate` spelling where the ring states the `fork`/`one` one), and it is the bridging lemmas `ir_neg_left_split`/`ir_neg_right_split` whose statements meet the field types by conversion. Lattices are BOUNDED (the units are bottom and top); an unbounded variant would need a semigroup-object notion the tree does not have. `Sets` instances run both ways against Rig.v's records, with all five DATA fields returning at `eq_refl` on the set-level side. **Universes measured off BOTH the constraint block AND the binder**: all four classes are `@{u u0 u1}` over `Category@{u u0 u0}` — hom and proof IDENTIFIED, the OBJECT universe FREE (only `≤` bounds) — and the identification has **THREE INDEPENDENT DONORS**, `Cartesian`, `Terminal` and `Monoidal` each rejected under `Constraint uh < up` against a hom control accepted at those levels, so no single one is "the" cause. **`MonoidObject` is NOT a fourth donor and an earlier revision wrongly probed it as one**: its signature takes a `Monoidal@{u u0}` argument over `Category@{u u0 u0}`, so `@Monoidal C` is rejected before any field of it is consulted — it cannot be tested apart from `Monoidal`, and whether it identifies anything OF ITS OWN is UNKNOWN, not refuted. **`dup_left@{u u0}` is this tree's sharpest instance of an identification hiding in the BINDER**: its constraint block is LITERALLY EMPTY while its binder reads `Category@{u u0 u0}`, so a reader who checks only the block concludes "no identification" and is wrong — inherited from `Cartesian`, not introduced here. **A `Set` PIN WAS FOUND AND FIXED**: unannotated, `Sets_Monoid` minimizes to `SetoidObject@{Set Set}` and would have confined every `Sets` result to Set-sized carriers (the `Instance/Sets/Products.v` hazard again); explicit binders lift it, and all six Sets-side constants now carry `o < so` as their only STRICT constraint and no `Set` anywhere — the rest of each block is some fifteen `≤` bounds against donor universes (`projections`, `prod_rect`, `Basics.compose`, `eq_ind`), among them `o ≤ q` relating two DECLARED binders, so "and nothing else" would be wrong even read charitably. **A PRIOR-ART CORRECTION**: `GroupObject` (`Structure/Group.v`) is the only record with a `MonoidObject` field, but TWO more carry the SIBLING class `Monoid` — `Theory/Algebra/CommutativeMonoid.v` and `Theory/Algebra/Frobenius.v`, the latter pairing its monoid with a COMONOID (different variance, no absorption) — so what survives is the narrower "no in-tree class carries TWO monoid structures on one object **as `Monoid`-typed fields**", and all three records are named. **That qualifier is load-bearing and the headers now carry it**: the survivor was billed as a search "by shape rather than by name" when it is a sweep by the field type's NAME, and BY SHAPE the pattern DOES occur — sweeping for record bodies with two `_assoc` axioms returns `Theory/Algebra/Rig.v`'s `RigObject`, which carries `(rig_zero, rig_add)` and `(rig_one, rig_mul)` on one setoid carrier, elementwise rather than as internal monoid objects, and which is precisely the set-level theory these files internalize. **THE #389/#1003 HANDOVER IS NARROWER THAN IT LOOKS AND THE FILE SAYS SO**: `Instance/Proset/Limit.v` reserves `Meet`, `Join`, `Lattice` and `BooleanAlgebra` for this effort, but these classes are INTERNAL-ALGEBRAIC (an object of an arbitrary cartesian category carrying two operations), NOT order-theoretic, so the bridge to that file's `IsGLB`/`IsLUB` is explicitly NOT delivered and a later effort still has to build it. Two engineering findings documented in both headers: `mappend`'s type `fobj tensor (mon, mon) ~> mon` is CONVERTIBLE with `mon × mon` but not syntactically equal, so two elaborations of `mappend[M] ∘ swap` record different object arguments and `rewrite` cannot match them — every `mappend`/`mempty` is therefore ASCRIBED; and `Structure/Group.v` declares `Notation "'inverse' [ G ]"`, making `inverse` a keyword that no `@inverse` or field assignment can parse, so `InternalRing_GroupObject` applies `@Build_GroupObject` positionally. Computing witnesses `Nat_ISemiring`, `Int_IRing` (0 ≉ 1) and `Bool_Lattice`/`Bool_ILattice` (⊥ ≉ ⊤). **104/104 and 103/103 constants closed under the global context** — `Print Module` lists **102 and 100**, every `Record` INCLUDED (`SetoidLattice` among them), so the unlisted names are the **five `Build_*` constructors** and nothing else: 102+2 = 104, 100+3 = 103. `Test/ProbeRingLattice340.v` pins the three FORMABILITY donor negatives the targets cannot state (a library file cannot carry a `Constraint` declaration without constraining itself), each stripped and its kind counted separately, plus the `dup_left`/`dup_right` binder measurement as `About`-able positives; the targets' own four refutations are all CONVERSION, so the two kinds are split across the files. Rename-simulated **3/3** unpadded with `Require` lines PRESERVED. A first run scored 3/4 and showed `Monoidal` named ONLY inside negative 3 — that was the tell that negative 3 was testing `Monoidal` and never reaching `MonoidObject`, and it was first read as an incidental guard gap; the negative is now written as what it measures. NOT delivered: no Lawvere bridge (`Theory/Lawvere/Model.v`'s `Model`/`Models` are a header pointer, explicitly not a theorem); no category of internal (semi)rings or lattices, no homomorphisms or forgetful functors, so the `Sets` round trips are NOT packaged as an equivalence; no closure under products or exponentials; no instance in any ambient category other than `Sets`, so "a ring object in Top is a topological ring" and "the opens of a space form a lattice object" stay prose; no commutative-ring variant, ideals or modules; no unbounded lattice; no distributive lattices, Heyting or Boolean algebras (that is #389); and `SetoidLattice` is a set-level record living under `Structure/`, off-pattern for `Theory/Algebra/` and disclosed as forced by the two-file scope
- **Structure/Monoidal/Markov.v**: Markov categories (copy/discard + semicartesian); Fox's theorem in Markov/Fox.v (all-deterministic ⟺ cartesian)
- **Structure/Premonoidal.v**: Premonoidal categories over Structure/Binoidal.v — centre Z(C) as a monoidal wide subcategory (Binoidal/Central.v, Premonoidal/Centre.v), Monoidal ⟺ all-central premonoidal (Premonoidal/Monoidal.v), Freyd/effectful categories (Premonoidal/Freyd.v); Kleisli premonoidal structure and thunkability in Monad/Kleisli/{Premonoidal,Commutative}.v and Monad/Thunkable.v; graded monads in Monad/Graded.v; strict premonoidal = monoid in (StrictCat, □) in Instance/StrictCat/Premonoid.v
- **Structure/Monoidal/Drinfeld.v**: The Drinfeld (monoidal) centre `Z(C)` — `HalfBraiding` (a natural family satisfying the half-braiding hexagon), objects `(x, half-braiding)` with intertwiner morphisms (proof-irrelevant witness), `Drinfeld_Monoidal`, `Drinfeld_Braided` (a braided monoidal category with BOTH hexagon identities at full strength), and the forgetful `Drinfeld_Forget`; this is the monoidal centre, distinct from the premonoidal centre of Binoidal/Central + Premonoidal/Centre
- **Structure/Monoidal/StarAutonomous.v**: Star-autonomous categories (definition level) over a genuine general base `SymMonClosed` — symmetric monoidal closed, introduced in-file because the in-tree `ClosedMonoidal` bundles `CartesianMonoidal` (over a cartesian closed base a dualizing object forces a preorder by Joyal's no-go, excluding Rel/FdVect/coherence spaces); provides the dual/linear-negation functor `(- ⇒ ⊥) : C^op ⟶ C`, the double-dual endofunctor, and the `StarAutonomous` class (dualizing object, closed transpose, double-dual iso with naturality), with par/linear-distributivity and the canonicity coherence ledgered
- **Structure/Monoidal/Dual.v**: Mac Lane §IV.2 construction 3 (book p. 88, `maclane:IV.2:construction3`) — **the dual-object functor is adjoint to ITSELF on the right**, and the canonical double-dual map is CONSTRUCTED rather than assumed invertible. `dual_self_adjoint_on_the_right d : AdjointOnTheRight (dual d) (dual d)` for an arbitrary object `d` of an arbitrary `SymMonClosed` — **not** the cartesian-bundled `ClosedMonoidal`, which the issue's reviewer check excludes because a cartesian base would trivialise the example. No field failed and none was weakened: all four naturality laws are instances of `dual_transpose_nat_dom`/`_nat_cod`, and both isomorphism laws are `dual_transpose_invol`. **THE TYPE FIT IS EXACT**: with `A = X = C` and `S = T = dual d` (both `C^op ⟶ C`), #358's class unfolds to Mac Lane's `C(a, x ⇒ d) ≅ C(x, a ⇒ d)`. **THE DONOR DEVIATION DICTATES THE WHOLE FILE**: `StarAutonomous.v`'s `ump_exponents'` is in `∃!` form and is NOT wired to `curry' := to exp_iso`, so the beta law for the packaged `curry'` is underivable from the class fields — everything transposes through `dcur`, never `curry'`, and `dcur_respects` is `#[local]` so nothing downstream may `rewrite` under `dcur` (`dcur_Proper` restates it in three lines and is re-exported as `dcur_Proper_ex`). By contrast the `Remove Hints Sets_Product_Monoidal` line is **DEFENSIVE, not required, and that is measured**: deleting it leaves BOTH the target and the probe compiling clean, with both probe negatives failing byte-identically — a scout reported it as necessary from its own scratch file and that reading does NOT transfer, because here `C` arrives carrying a `SymMonClosed` instance whose coercion already determines the monoidal structure, so resolution is never offered a choice. **PRIOR ART, MEASURED — AND THIS IS NOT THE CLASS'S FIRST SELF-ADJOINT WITNESS**: `Powerset_AdjointOnTheRight` (`Adjunction/Right.v`) already has `Powerset_Prop_op` in BOTH slots, so no first-`S = T` claim is made; what is new is the first witness arising from a MONOIDAL CLOSED structure rather than a fixed concrete category, and the first identification of the resulting unit and counit with a canonical map (the words `double dual` occur nowhere in `Right.v`). Two near-collisions were found by sweeping all fifty declared names and renamed around rather than risked — `dual_ev` is taken by `Instance/FdVect/DoubleDual.v` and `double_dual_natural` has 9 occurrences across the two `Instance/FdVect` files — because `make print-assumptions` loads many modules into ONE scope, where a shared name silently audits the wrong constant. **THE UNIT AND COUNIT ARE BOTH `dd_unit`, AT `eq_refl`**: `unit_is_dd_unit` and `counit_is_dd_unit`, which is Mac Lane's remark that for a self-adjunction on the right the counit IS the unit read in the opposite category — and it is not a packaging coincidence, since `aor_to_is_from_swapped` records at `eq_refl`, on the WHOLE `SetoidMorphism` rather than pointwise, that `to aor` and `from aor` are the SAME function `dual_transpose` at swapped indices. Off the diagonal the two legs do not even share a type, so `to = from` is not statable there — pinned as the TYPING negative. **EXACTLY ONE identification falls back to `≈`, with the residue exhibited rather than described**: `dd_unit` is `dual_transpose id`, which unfolds to `dcur (eval' ∘ (id ⨂ id) ∘ braid)` against the donor header's own phrase `dcur (eval' ∘ braid)`; the two differ by `id ⨂ id`, and `bimap`'s `fmap_id` is an opaque law field so conversion cannot remove it. The DIRECTION of that fallback was a design choice made on a measurement — spelling `dd_unit` the other way round would move the `≈` onto the unit/counit identifications, which are what the construction is actually about. **THE STAR-AUTONOMOUS RELATION IS MADE EXPLICIT, FILLING A GAP THE DONOR ITSELF FLAGGED**: `StarAutonomous.v`'s header says `star_double_dual` "posits SOME natural isomorphism … rather than requiring the CANONICAL dualization map … to be invertible", deferring the pinning to its ledger 4; here `StarAutonomous_of_dd_unit` shows that if the canonical map is invertible the class is inhabited with `star_double_dual`'s FORWARD LEG literally `dd_unit` (`star_double_dual_is_dd_unit`, `eq_refl` — the field is an `Isomorphism`, so it cannot be a morphism itself), and `dual_AdjointEquivalence` upgrades the self-adjunction. `StarAutonomous.v` is NOT modified. **UNIVERSES MEASURED OFF BOTH BINDER AND BLOCK — AND THE BLOCK ALONE GIVES THE WRONG ANSWER**: every constant is over `C : Category@{u u0 u0}`, hom identified with proof by REUSING the level variable in the BINDER, while the constraint blocks carry NO equation at all, only bounds. The identification is INHERITED and FOUR donors are each sufficient alone (`Opposite`, `Monoidal`, `SymmetricMonoidal`, `SymMonClosed`, each rejected under `Constraint ch < cp` against applied hom/identity controls accepted at those very levels) — but **read four as four DONORS, not four independent CAUSES**: the monoidal three NEST (`SymMonClosed` ⊃ `SymmetricMonoidal` ⊃ `BraidedMonoidal` ⊃ `Monoidal`), so at most TWO are independent and `Monoidal` alone already suffices. No `Set` in any binder or block; none claimed unavoidable. **50/50 constants closed under the global context** — 42 source-declared plus the EIGHT `Program` obligations no source sweep sees (six for the self-adjunction, two for the unit); the file declares no `Record`/`Class`/`Inductive`, so there is no unlisted `Build_*`. **SEVEN negatives of THREE KINDS plus a separate instrument check** (eight `Fail` commands in all; the probe has two negatives plus its own instrument), and the tail alone does NOT separate two of them: the four FORMABILITY ones end `universe inconsistency`, the two CONVERSION ones end `cannot unify` between two terms of ONE type, and the TYPING one ALSO ends `cannot unify` but on the OBJECT VARIABLES, its body reporting two DIFFERENT objects of `Sets` as expected and actual. `Test/ProbeDual359.v` restates the name-bearing ones from OUTSIDE the target (an in-file `Fail` renames in lockstep with what it guards) and mirrors the target's full `Require` list AND its `Remove Hints` line, without which a negative could fail for a reason it never measured. NOT delivered: **no concrete `SymMonClosed` witness, so every result is a conditional** (inherited — the donor constructs none either, and the in-tree `ClosedMonoidal` is cartesian and excluded on the donor's stated no-go grounds); no CONVERSE to `StarAutonomous_of_dd_unit` (the class does not tie its field to the canonical map — CONVERSION negative 2 pins that conversion does not SETTLE the equation, explicitly NOT that it is false, and no countermodel is claimed); no `EquivalenceOfCategories` composite; no self-adjointness on the LEFT and no instantiation of `right_does_not_imply_left`; no uniqueness of the dual functor; no functoriality or naturality in `d`; no triangle identities restated in `Adjunction/Natural/Transformation.v`'s vocabulary; and nothing about `⅋`, linear distributivity or Barr coherence, which stay the donor's ledger 4.
- **Structure/Limit.v**: Limits and colimits — with the transfer vocabulary alongside: preservation and iso-reflection in Structure/Limit/Preservation.v, and CREATION in Structure/Limit/Creation.v. The cone-level predicate `IsLimitCone` (a given cone is universal) is what the apex-only `IsALimit` cannot express and what `PreservesImageLimit` (Construction/Comma/Limit.v) had been writing out by hand — the two bridges are proof-free repackagings whose round trips are `eq_refl` (Construction/Comma/Creation.v), so GAFT and SAFT already consume it. `CreatesLimit K F` carries three fields (a lift of every limiting cone downstairs, a cone isomorphism from its image, and reflection); Mac Lane §V.1's other two clauses are theorems, not fields — `creates_limiting` (the lift is limiting) and `creates_lift_unique` (any cone lying over the given one is canonically isomorphic to the lift, uniqueness in the form a setoid library supports). `StrictlyCreatesLimit` is Riehl §3.4.7's on-the-nose variant over `StrictLift` (object `=` on the apex, `≈` on the legs), with `StrictlyCreatesLimit_CreatesLimit` relating them. Mac Lane §V.4 Theorem 2 in both halves: `creation_preserves_limit` (cone-level, with the downstairs limit as an explicit hypothesis, per Riehl's footnote) and `creates_limits_Complete`/`creates_limits_continuous`, plus creation under composition and the colimit duals by `C^op`. Witnesses: `EM_Forget` strictly creates every limit and limits of algebras are computed on carriers (Monad/Eilenberg/Moore/Limit.v, with `EM_Complete` proving Structure/Complete.v's standing prose, and an axiom-free instantiation at `Id_Monad` over `Coq`); `comma_proj2` given a limit of the base diagram downstairs (Construction/Comma/Creation.v — an earlier revision of this clause said "given `Complete C`" and "the chosen downstairs limit", both stale since 28ee6e54 made that limit a parameter of `comma_limit`, and it read as though the tree already had STRICT creation, which it did not: `comma_strict_apex`/`comma_strict_legs` are `eq_refl` about `comma_limit HU K L` itself, whereas `comma_CreatesLimit`'s `creates_lift` discards the cone it is handed, so its apex is refused against that cone's apex; `comma_StrictlyCreatesLimit` is the strict form, see the bullet in Constructions); and every equivalence, hence `Id[C]` (Theory/Equivalence/Creation.v), which is also how `equivalence_creates_limits` finally earns its name
- **Structure/Pullback/Reduction.v**, **Structure/Pullback/Wide.v**: The classical interdefinability of the finite (co)limit generators (Mac Lane §III.3 Ex 2, §III.4 Ex 7/9/10; Awodey §5.2 Prop 5.7 + Cor 5.8, §5.4 Prop 5.16, §5.7 Ex 3/5; Riehl 2nd ed. §3.5 Lemmas 3.5.15/3.5.16) — the reduction toolkit `Structure/Topos.v` had disclosed as unformalized since it was written, and the two `jww` TODOs `Structure/Pullback.v` had carried since 2017, both retired in the same commit. Every reduction is stated FIRST at the apex-pinned level (`IsPullback`/`IsEqualizer`/`IsCartesianProduct`/`IsIndexedProduct`) and only then packaged: `pullback_of_equalizer` with `HasPullbacks_of_Cartesian_HasEqualizers`, the Awodey converse `equalizer_of_pullback` (which deliberately does NOT use `equalizer_monic` — the pullback's own uniqueness clause discharges the factorization directly, disclosed in-header as a departure from Awodey's argument), the kernel pair as an equalizer (`kernel_pair_IsEqualizer`, tied to the PRE-EXISTING `Structure/Regular.v` `kernel_pair` by `kernel_pair_is_pullback_along_itself` rather than shadowing it), `product_of_pullback`/`Cartesian_of_HasPullbacks_Terminal`/`HasEqualizers_of_HasPullbacks_Terminal`, and the dual `HasPushouts_of_Cocartesian_HasCoequalizers`. **The issue's own framing was WRONG and the file proves it rather than repeating it**: Mac Lane's Ex 9 and the Awodey/Riehl construction are NOT "the same square read the other way" — Mac Lane pulls back `⟨id,f⟩` and `⟨id,g⟩`, BOTH issuing from `x`, over `x × y`, while Awodey and Riehl pull the diagonal `y ~> y × y` back along `⟨f,g⟩`. The refutation comes in TWO KINDS kept lexically apart: the codomains differ (CONVERSION), and the legs are not even parallel so the comparison CANNOT BE STATED (FORMABILITY — `aw_pair f g : x ~> y × y` against `x ~> x × y`). Structurally, both Mac Lane legs are split monic with the one common retraction `exl`, whereas `exl ∘ aw_pair` is `f`, which is not an identity FOR GENERAL `f` — a hedge that matters, since at `f := id` Awodey's leg IS split, by this file's own `ml_right_split`; the lemma was renamed `aw_pair_retracts_to_f` because its first name, `aw_pair_not_split`, asserted more than it proves and more than is true. What IS proved is the weaker positive statement: the two vertices are canonically isomorphic AS EQUALIZERS (`maclane_awodey_iso`, `maclane_awodey_iso_commutes` — commuting with BOTH equalizing maps, not merely an iso of objects), with one degenerate definitional coincidence at `f := id`. It is NOT proved that no cospan isomorphism exists at particular `f`, `g`, and the header says so. **The duality is measured, not assumed**: `HasCoequalizers C = HasEqualizers (C^op)` is REFUTED at `eq_refl` (records are nominal) — yet every FIELD type IS convertible, so both bridges are `:=` with no tactic and both record round trips close by `eq_refl`; the residue is exactly TWO steps, `HasEqualizers_op_of_HasCoequalizers` and its mirror `HasCoequalizers_of_HasEqualizers_op`, each destructing the `sigT` (an earlier draft said one, overlooking the second in the same section). `Coprod`/`inl`/`inr` are the op-product/`exl`/`exr` by `eq_refl`, so section (A) applies at `C^op` verbatim. Wide.v adds `IsWidePullback`, `WidePullback`, `HasWidePullbacks` (an earlier revision of this bullet said "declared but UNINHABITED", true when Wide.v landed and stale since `Instance/Sets/SubobjectLattice.v`'s `Sets_HasWidePullbacks` inhabited it at index universe `o`, the carrier level of `Sets`) and Riehl's Lemma 3.5.15 in BOTH directions (`wide_pullback_product`/`product_wide_pullback`), with `one_unique` VISIBLE as a single named step — discharging the commutativity condition forward, manufacturing it backward. **The empty index comes out right, and for a structural reason**: the terminal object IS the empty wide pullback, proved both ways and genuinely consuming the emptiness — the opposite outcome from the sibling `Structure/Equalizer/Wide.v`, whose empty case is an obstruction RELATIVE TO ITS LIMIT PRESENTATION, and no pointed section or `i0` hypothesis appears here. **Read the contrast narrowly.** An earlier draft of this bullet explained it by saying a wide fork "carries a second leg the empty condition leaves unconstrained" — that is FALSE about that file's record (`IsWideEqualizer` has two fields and ONE leg, and that file says three times that no leg-carrying record is built there), and the polarity was inverted: the fork's LACK of a second leg is why `i0` is needed, the cone's second leg being MANUFACTURED as `fs i0 ∘ h`. The honest statement is weaker — this file builds NO limit presentation, so no analogous disagreement can arise, and the contrast partly reflects that less is claimed here rather than that the notion is better behaved. Non-vacuity at `Sets_HasIndexedProducts`. SCOPE, and it is narrower than "finite limits are redundant": only the BINARY and PARALLEL-PAIR generators are related. No finite-shape induction is performed, so `Complete`/`Cocomplete` is NOT concluded and Riehl 3.5.15 is not stated for arbitrary finite shape; (E)'s hypotheses are not shown minimal and have no converse; the produced `Cartesian` is not compared with any pre-existing one on the same category; `IsWidePullback` has NO limit presentation (nothing relates it to a `Limit` over a wide-cospan shape, so there is no analogue of `Structure/Pullback/Limit.v`); `wide_pullback_unique` gives a bare `≅` with no leg equations; and Reduction.v makes no universe claim at all. `Test/ProbeReduction.v` guards the boundary — 8 negatives (3 conversion in the D-section, 2 formability, 2 duality, 1 wide) plus a separate instrument check, against controls naming their own constants, each negative stripped and its failure KIND confirmed. The rename guarantee was NOT true as first shipped — an independent audit found `ml_right`, `HasCoequalizers` and `HasEqualizers` named in no control, so renaming any of them left the file compiling and its negative vacuously green; controls for all three were added, the same defect family two earlier probe files in this tree shipped with. Wide.v's three universe measurements were likewise measured-but-unguarded and are now shipped as probe sections, the donor `IsIndexedProduct` rejected at the same levels so that "inherited" is measured rather than asserted; Wide.v ships no `Fail` of its own, so its refutations are pinned there for the first time

- **Structure/Limit/Unique.v**: Riehl §3.1 Proposition 3.1.7 — essential uniqueness of limits WITH the leg equations, from the mediator calculus alone (`limit_med`/`limit_med_commutes`/`limit_med_eq`, no representability detour): `limit_unique_iso : c ≅ d` between any two `IsALimit F` witnesses, `limit_unique_iso_legs` carrying both leg-commutation families (the reviewer bar: not a bare `≅`), `limit_unique_iso_unique` (ANY morphism commuting with the two leg families is the isomorphism — the proposition's content), and the automorphism corollary `limit_endo_id`; the colimit statements are literal op-instances of the limit ones re-typed through the covariant `colimit_inj` vocabulary (`colimit_unique_iso`/`_injs`/`_unique`, `colimit_endo_id`; the mediator is definitionally the opposite side's, `colimit_unique_to_is_op`) — not a second proof. Structure/Limit/Unique/Compat.v ties the pre-existing routes to it: `equalizer_unique` and `pullback_unique` AGREE with the general isomorphism — componentwise and at iso level (`equalizer_unique_agrees`/`_iso`, `pullback_unique_agrees`/`_iso`) — through the in-tree bridges (`is_equalizer_limit`, `Pullback_from_Universal`); their terminating Qeds became Defineds for exactly this comparison, no proof text changed; so the general theorem strictly subsumes both (adding legs + uniqueness the bare `≅`s lacked); and the generic route — in tree instantiated only at terminal/initial objects — is taken at limits for the first time (`limit_unique_up_to_transport_iso` := `univ_property_unique_up_to_unique_iso` at `LimitIsUniversalProperty`), with the transport-vs-leg-precomposition identification — which would compare the generic isomorphism componentwise — disclosed as the remaining open gap (the hint-automation-built representation equivalence has no leg characterisation in tree)
- **Structure/Limit/FromProducts.v**: Mac Lane §V.2 Theorems 1 and 2 and Corollary 2 (book p. 113, `maclane:V.2:thm1`, `maclane:V.2:thm2`, `maclane:V.2:cor2`) + §V.4 Exercise 2 (book p. 118, `maclane:V.4:ex2`) + Riehl §3.2 Theorem 3.2.11, Remark 3.2.13 and Exercise 3.2.ii, §3.5 Theorem 3.5.11 and Epilogue §E.1 + Awodey §5.4 Proposition 5.23, Corollary 5.24 and Theorem 5.25 — **limits from products and equalizers: the central existence theorem, its completeness corollary in both orientations, and the continuity of a functor preserving both primitives**. The limit of `F : J ⟶ C` is the equalizer of the two canonical maps `s, t : ∏_{x ∈ ob J} F x ⇉ ∏_{f ∈ mor J} F (cod f)`, defined element-free by their components against the arrow product's projections (Riehl's Remark 3.2.13: projection at `cod f`, and `fmap[F] f` after the projection at `dom f`), with legs the object projections after the equalizing map. **THE CORE IS ELEMENTARY AND FAMILY-GENERIC**: section `Core` takes two `IsIndexedProduct`s, two maps satisfying the component equations at ANY family of arrows that GENERATES (an inductive `Gen` closing a family under identities, composition and `≈`, with `Generates` the reachability hypothesis and `ArrowIx_Generates` its one-constructor discharge at the full index `ArrowIx J := {p : obj J * obj J & fst p ~> snd p}`), and an `IsEqualizer` of them, and `pe_limiting` proves the cone limiting (Theorem 1) — cone coherence being ONE induction over `Gen` whose only case with content is the indexed arrow — so the same theorem serves the image of the construction under a functor and every sub-family of arrows that generates. Over the classes `HasIndexedProducts` and `HasEqualizers` (`Structure/Limit/Product.v`, `Structure/Equalizer/Fork.v`, consumed unchanged) it packages as the pinned **`limit_of_products_equalizer F : Limit F`**, whose apex and legs read back at `eq_refl` as Theorem 2's explicit description; **`Complete_from_products_equalizers : HasIndexedProducts C → HasEqualizers C → @Complete C`** (pinned) and its dual **`Cocomplete_from_coproducts_coequalizers`** (Riehl's §3.5 checkbox, pinned) as the primal theorem at `C^op` through `HasEqualizers_op_of_HasCoequalizers` and `Opposite_Functor` — a `:=` with no tactic, `HasIndexedCoproducts C` being `HasIndexedProducts (C^op)` on the nose (#254's `Structure/Limit/Coproduct.v`, which is why the issue's "add `HasIndexedCoproducts`" checkbox needed no work), so the colimit-first orientation is an instantiation and no covariant coequalizer-of-coproducts is built; and §V.4 Exercise 2 as **`continuous_from_products_equalizers : HasIndexedProducts C → HasEqualizers C → PreservesIndexedProducts G → PreservesEqualizers G → ContinuousFunctor G`** (pinned) over TWO NEW cone-level predicates (measured absent at the base commit), landing in `Structure/Limit/Preservation.v`'s cone-level `ContinuousFunctor` rather than the apex-only class, by running the core at `G ◯ K` on the G-images and transporting along `pe_image_ConeIso` (identity apex, legs one `fmap_comp` apart) and `FCone_iso` of `limitcone_iso`. **Riehl's Exercise 3.2.ii is delivered generally and its two clauses at their honest strengths**: `pe_gen_limiting` makes the equalizer over ANY generating family limiting and `pe_gen_restrict_iso` shows the restriction does not change the limit (a `ConeIso` to the full construction by `limitcone_iso`; at the full index the refined construction IS the construction in apex and every leg at `eq_refl` but not as a record, the two cones citing different `Qed` component lemmas — pinned); the NON-IDENTITY family generates under a DECIDER for being an identity (`nonid_Generates`, `pe_nonid_limiting`; discharged on the walking arrow in the probe, claimed for no shape in the library), and the ATOMIC family (`AtomicArrow`: non-identity, every factorization has an identity factor) under the HYPOTHESIS that it generates (`pe_atomic_limiting`), which IS Riehl's factorization hypothesis and is discharged for no shape at all. **THE ISSUE'S WORK ITEM 4 IS DECLINED ON A MEASUREMENT, NOT TASTE**: it asks that the products be routed through `Instance/Discrete.v`'s discrete-diagram encoding so as to compose with `Structure/Limit/Product.v`'s API; the API consumed IS that file's class and elementary record, and what is refused is only `iprod`/`iprod_proj`/`iprod_ump`, whose binder is `C : Category@{_ Set Set}` (ambient hom AND proof pinned to the literal `Set`, the #331/#339 measurement) — pinned as probe N8, where at a hom level declared strictly above `Set` the class, its product and `Complete_from_products_equalizers` are accepted and `iprod` is refused with `Cannot enforce Set = ch`. **Universes off BOTH binder and block over all 91 constants**: hom = proof in every binder with no block equation saying so (SIX donors each refused alone at `ch < cp` against accepted hom/identity/endofunctor controls — `HasIndexedProducts`, `HasEqualizers`, `IsIndexedProduct`, `IsEqualizer`, `Limit`, `Complete` — none introduced here); `limit_of_products_equalizer` carries `u = u3` (the class's index universe IS the shape's object universe) and `u2 = u4` (shape hom = ambient hom, the limit vocabulary's: `Limit`/`IsLimitCone` refused at a shape whose homs sit strictly below the ambient's while `Cone` and `ArrowIx` are accepted, probe N7) with C's object universe FREE; `Complete_from_products_equalizers` carries NO equation and concludes `Complete@{u0 u0 u0 u}`, `Sets_Complete`'s smallness discipline; `pe_cone`/`pe_limiting` identify the shape's hom level with the ambient's IN THE BINDER (which is why probe N7's `pe_cone` refusal, at the ambient argument, corroborates nothing about the donor) and keep the family's index universe free of any identification (bounded above by two auxiliary levels), and `Gen`'s block is LITERALLY EMPTY; zero word-bounded `Set` anywhere, the only two `Set` tokens being the `_rec` eliminators' motive sorts. **91/91 constants closed under the global context with ZERO `Axioms:` lines** (86 `Print Module` entries — 70 `Definition`, 14 `Parameter`, 2 `Inductive` — plus the five constructors listed only after a `:=`), all gated FULLY QUALIFIED; closure 38 excluding self (`Structure/Pullback/Reduction` costs 9, `Coproduct` and `Complete` 1 each, every other `Require` 0); three `Defined` tokens, NONE load-bearing by flipping each alone; zero collisions after SEVEN renames before landing (`arrow_dom`/`arrow_cod` are `Instance/Square.v`'s, `arrow_of` is `Theory/Shapes.v`'s, `sub_dom` is `Theory/Subobject.v`'s field, `two_fam` is `Structure/Pullback/Wide.v`'s — hence `aix_*`, `fam_*`, `two_gen_fam`). `Test/ProbeFromProducts416.v` mirrors the target's `Require` list plus four and carries **16 refutation commands = 1 instrument + 15 commands pinning EIGHT negatives of THREE kinds told apart by the error text** — 3 CONVERSION, 2 TYPING (one of them, feeding the products-and-equalizers pair to the cocompleteness theorem, carries a trailing `cannot unify` on the two hom FAMILIES — `hom[C]` against `hom[C^op]`, the latter printed unfolded as `λ x y, y ~> x` — not on two inhabitants of one type), 3 UNIVERSE over ten commands — each stripped ONE AT A TIME in a copy of the whole file with its error read, guard coverage measured mechanically (40 identifiers inside a refutation command, 35 also outside, the five exceptions exhaustive), rename-simulated 7/7 with every break on a `Check` line; it also carries the `Sets` instantiation `Complete_from_products_equalizers Sets_HasIndexedProducts (HasEqualizers_of_HasPullbacks_Terminal Sets_HasPullbacks)` — a SECOND inhabitant of `@Complete Sets`, NOT compared with `Sets_Complete` and built WITHOUT it, `Instance/Sets/Complete.v` measured absent from the probe's closure; an audit caught a draft that fed it `Adjunction/GAFT/Sets.v`'s `Sets_HasEqualizers`, which IS `Complete_HasEqualizers Sets_Complete`, while calling the two "independent" — and the walking-arrow generating family (three arrows, one generating index). `make todo` grows by 19, ALL in the probe, the library file contributing ZERO — the issue's "adds no new hits" box is not met as written and is disclosed. Prose repointed in the same commit, line-neutrally where cited: `Structure/Complete.v` (its closing sentence had called `iprod` "the products half of the reduction"), `Structure/Equalizer.v` (whose "Both arguments run in this library" was FALSE for the reduction until now), `Structure/Limit.v` — the issue's E.1 LIBRARY-DEFECT, where "uniqueness up to unique isomorphism" is now `Structure/Limit/Unique.v`'s (landed since the issue was filed) and the construction this file's; `Structure/Topos.v`, which the issue also names, cites the PULLBACK reduction and is untouched. NOT delivered: no covariant coequalizer-of-coproducts construction; no finite-shape variant (Awodey's finiteness predicate is #417's); no computing witness and no comparison of the `Sets` instantiation with `Sets_Complete`; no shape satisfying the atomic hypothesis and no decision procedure for identities; no converse for the product half; no naturality in `F`; nothing registered as an `Instance`
- **Structure/Equalizer/Coreflexive.v**: Mac Lane §V.2 Exercise 1 (book p. 114, `maclane:V.2:ex1`) — **coreflexive pairs and coreflexive equalizers, Manes' criterion (small products + coreflexive equalizers ⇒ complete), and part (b)'s diagonal characterisation of reflexive pairs**. `CoreflexivePair f g` (a common RETRACTION with its two laws), `HasCoreflexiveEqualizers C`, `common_retraction_coreflexive`, the `#[export] Instance HasEqualizers_HasCoreflexiveEqualizers`, `functor_preserves_coreflexive` — mirroring Structure/Coequalizer/Reflexive.v line for line; the notion is the op-dual FIELD FOR FIELD (both pair bridges are `:=`, round-tripping at `eq_refl`) but not as a type (probe N1) and the classes are neither each other's dual nor `HasEqualizers` by conversion (N2, N3; the class bridges perform Structure/Pullback/Reduction.v's destruct-and-repackage). **Mac Lane's hint as a lemma**: the identity arrow at `x` is an index of Structure/Limit/FromProducts.v's FULL arrow family with codomain `x` on the nose (`id_index_cod` at `eq_refl`), so `pe_retract` — the map into the object product whose components are the arrow product's projections at the identities — retracts both canonical maps (`pe_retract_s`, `pe_retract_t`, three lines each by `pe_iprod_ext`), giving `pe_coreflexive : CoreflexivePair (pe_s HP F) (pe_t HP F)`; then **`Complete_from_coreflexive_equalizers : HasIndexedProducts C → HasCoreflexiveEqualizers C → Complete C`** feeds the coreflexive equalizer to FromProducts.v's elementary core (which consumes `IsEqualizer`, never the class), Theorem 2's description reading back at `eq_refl` (`mc_limit_apex`, `mc_limit_leg`). The generating-family variant is not stated: `Gen`'s `gen_id` puts no identity into the index TYPE, so the projection at an abstract family is refused (N4, with the positive TRAP that an implicit `idx_cod` makes the same ascription succeed on a different product); a transported variant was not built (an earlier revision said it "was measured", with no artifact). **Part (b)** is about REFLEXIVE pairs, not (a)'s coreflexive ones: in any cartesian category `ReflexivePair_iff_diagonal_factors : ReflexivePair f g ↔ ∃ s, (f △ g) ∘ s ≈ id △ id` (empty constraint block); in Sets, `diagonal_in_image f g := ∀ b, ∃ a, (f a ≈ b) * (g a ≈ b)` follows from a reflexive pair unconditionally, the converse `reflexive_of_diagonal_in_image` takes a PROPERNESS hypothesis (the `sigT` choice function is not a Sets morphism by itself, N5), and the image-level reading through Instance/Sets/Image.v is a BICONDITIONAL with no hypothesis — **`diagonal_in_image_iff_through_image : diagonal_in_image f g ↔ ∃ d, Sets_Image_mono (f △ g) ∘ d ≈ id △ id`** — both halves free because `Sets_Image`'s setoid (Image.v) compares codomain components only, so the map `b ↦ ((b, b); (chosen preimage; _))` is proper whatever the choice function does (an earlier revision delivered only the forward half `diagonal_through_image` and called the reverse "a choice principle"; the audit compiled it closed — the choice principle sits only in the step to `ReflexivePair`). The issue's pinned name `Sets_common_section_iff_diagonal`, read with `ReflexivePair f g` on the left, is therefore NOT declared: its reverse is exactly the properness-conditional converse. **Stale premises**: the base theorem is #416's FromProducts.v (its bullet in this index); `coreflexive` was already load-bearing at Structure/Topos/Monadic.v (`coreflexive_equalizer_pullback`, unpackaged); `ReflexivePair` occurs in four files, not the two the issue names, and with `HasReflexiveCoequalizers` fourteen lines sit outside Reflexive.v and Monad/Monadicity/ in six files (Monad/Lifting.v 5, Structure/Topos/Monadic.v 4, Test/ProbeToposColimits405.v 2, three files 1 each; an earlier revision listed five files summing to nine). **Non-vacuity and its limit**: `Sets_HasCoreflexiveEqualizers` and `Sets_Complete_via_Manes : Complete Sets` compile closed, but the only route to the hypothesis in the tree is the STRONGER `HasEqualizers Sets`, so nothing separates the criterion from `Complete_from_products_equalizers` (docs/INHABITATION.md should say so). **Universes** (`About` under `Set Printing Universes` on all 48): `CoreflexivePair@{u u0}`, `HasCoreflexiveEqualizers@{u u0}`, `HasCoreflexiveEqualizers_of_op` and `ReflexivePair_iff_diagonal_factors` carry EMPTY constraint blocks; `Complete_from_coreflexive_equalizers@{u u0 u1} : … → Complete@{u0 u0 u0 u}` bounds only, its block byte-identical to `Complete_from_products_equalizers`'s; `mc_cone` and the four constants built on it (`mc_limiting`, `mc_limit`, `mc_limit_apex`, `mc_limit_leg`) inherit `u = u3`, `u2 = u4` from FromProducts.v's `Core` (an earlier revision named `mc_cone` alone); the eight `SetsHalf` constants carry `u = u0 = u1 = u2`, `u < u3` (the Sets setoid discipline; four also `u < u4`), while `Sets_HasCoreflexiveEqualizers@{u u0}` carries no equation and `u0 < u` (an earlier revision counted it among "six Sets constants" under the equations); `Sets_Complete_via_Manes@{u u0} : Complete@{u u u u0}` with `u < u0` is `Sets_Complete`'s shape; no word-bounded `Set`, no `JMeq`/`EqdepFacts`/`eq_rect_r` in any block — bounds against `prod_rect`, `Projections`, `Basics.compose`, and for the two `Sets_*` constants `eq_ind`/`eq_ind_r`/`Logic_lemmas.equality`, DO appear, so "no stdlib cap" means only those three names. **48/48 constants closed under the global context, zero `Axioms:` lines** (34 `def`, 7 `prf`, 1 `inst`, 4 `proj`, 2 `rec`; 45 at the audited revision plus the three image-level constants), all gated fully qualified; nine `Defined` (one load-bearing by flipping: `diagonal_section`, for `reflexive_of_diagonal_in_image`; eight kept by the data convention), five `Qed`; closure 50 excluding self (Instance/Sets/Image.v 4, Instance/Sets/Products.v 4, Sets/Pullback.v, FromProducts.v and Reflexive.v 1 each, twenty `Require`s 0). `Test/ProbeCoreflexive421.v` mirrors the `Require` list and carries **6 refutation commands = 1 instrument + 5 negatives of TWO kinds** — 3 CONVERSION (`CoreflexivePair f g = @ReflexivePair (C^op) y x f g`, `HasCoreflexiveEqualizers C = HasReflexiveCoequalizers (C^op)`, `HasCoreflexiveEqualizers C = HasEqualizers C`, each "cannot unify" beside the `eq_refl` round trips and the derivable bridges), 2 TYPING (the generating-family projection at `x`, codomain `F (idx_cod (idx_id x))`, beside its declared-codomain and full-index controls and the implicit-`idx_cod` trap; the bare choice function as a Sets morphism, "Cannot infer field proper_morphism", beside `diagonal_section` and the hypothesis-free image-level biconditional) — each stripped one at a time in a copy of the whole file; readbacks at `eq_refl`; guard coverage 38 identifier tokens inside / 31 also named outside (comments stripped) with seven exhaustive exceptions; rename-simulated 5/5 (`CoreflexivePair`, `HasCoreflexiveEqualizers`, `HasReflexiveCoequalizers`, `pe_gen_proj`, `diagonal_in_image`), every first break on a positive line; `make todo` grows by the probe's 6 refutation commands only (2209 → 2215), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: a category with coreflexive but not all equalizers (no witness of the criterion's strict gain); the generating-family form of `pe_coreflexive` (transport needed, not built); the unconditional Sets converse from `diagonal_in_image` to `ReflexivePair` and with it the pinned `Sets_common_section_iff_diagonal` (a choice principle, not refuted); relocating Monadic.v's lemma onto the new record (surfaced, not settled); no edit to Reflexive.v, FromProducts.v or Image.v.
- **Structure/Limit/Finite.v**, **Instance/Cones/Limit.v** (extended): Mac Lane §V.2 Definition 1 and Corollary 1 (book p. 113, `maclane:V.2:def1`, `maclane:V.2:cor1`) + Awodey §5.4 Definition 5.18 and the running "all finite limits" definition (printed pp. 109/111, `awodey:5.4:def18`, `awodey:5.4:def-all-finite-limits`), §5.6 (printed p. 116, `awodey:5.6:def-colimit`) + Seven Sketches §6.2.4 with Definition 6.30 (printed pp. 191-192, `7sketches:6.2.4:def-finite-category`, `7sketches:6.2.4:def6.30`) + Riehl §3.5 (printed p. 115, `riehl:3.5:def-finite-diagram`, `riehl:3.5:thm17`) — **the finiteness predicate on index categories the tree never had, finite (co)completeness over it, Mac Lane's corollary that a terminal object, binary products and equalizers give every finite limit, and the converse against the surrogate generator packages**. `FiniteCategory J` is Riehl's and Seven Sketches' reading — finitely many MORPHISMS — as DATA: a size, an enumeration `fc_arr : Fin.t fc_size → ArrowIx J` into `Structure/Limit/FromProducts.v`'s arrow index, and a coverage `fc_cover` returning for every arrow an index whose entry HITS it, where `ArrowHit a f` is two Leibniz endpoint equations plus the `hom_cast`-transported arrow `≈ f`. **THE HIT WITNESS IS TYPE-VALUED AND THAT IS FORCED**: FromProducts.v's `Gen` is an `Inductive … : Type` and `Generates` quantifies over it, so `fc_Generates` destructs the witness and feeds `gen_equiv`/`gen_idx` — a `Prop`-valued coverage could not (large elimination), which is exactly why the unregistered `Structure/Limit/Preservation/Shapes.v`'s `FiniteShape` is disclosed and NOT consumed — its object coverage is a `Prop`-valued `List.In` and its arrow coverage a `sigT` (Type-valued, eliminating into `Type`) whose membership component is again a `Prop`-valued `List.In`, so no `Fin.t` index is recoverable from either (an earlier revision said the whole coverage was `List.In` in `Prop`, refuted for the arrow half by a fess audit). Read the predicate at its strength: "finitely many" is UP TO `≈`, the enumeration is a RETRACTION and not a bijection (one arrow may be hit by several indices), so `fc_size` is an upper bound and no cardinality is claimed; objects are NOT a second field — Seven Sketches' footnote that finitely many morphisms force finitely many objects is the lemma `finite_morphisms_implies_finite_objects`, an object being recovered from the index hitting its identity arrow (`fc_obj_idx`/`fc_obj_rt`); `FiniteCategory_op` makes finiteness self-dual; every `∃` is a `sigT`, no choice anywhere. **THE ENGINE IS A PRODUCT OVER A RETRACT**: the corollary needs a product over the OBJECTS and the enumeration indexes ARROWS, so `Section RetractProduct` proves, over an arbitrary category, that a product over an index `A` plus ONE equalizer — of the identity against the canonical idempotent `rp_r` — is a product over any retract `B` of `A` (`rp_IsIndexedProduct`, with the chosen-structure `retract_product`/`retract_product_ump` under `HasEqualizers`); nothing in it is about finiteness. **THE COROLLARY CONSUMES THE ENUMERATION, WHICH IS THE ISSUE'S REVIEWER CHECK**: `finite_limit : Limit F` is built from THREE finite products (#335's `finite_product` over `Fin.t fc_size` for `F ∘ fc_dom`, the retract product over the objects, and the product for `F ∘ fc_cod`) and TWO equalizers, then #416's `pe_cone`/`pe_limiting` at the generating family `fc_arrow` with `fc_Generates` discharging the coverage, apex and legs read back at `eq_refl` (`finite_limit_apex`, `finite_limit_leg`); `finitely_complete_from_generators (T : Terminal C) (CP : Cartesian C) (HEq : HasEqualizers C)` (the pinned name) is that at `Cartesian_Terminal_HasFiniteProducts`, `finitely_complete_from_finite_products` the `HasFiniteProducts` form, and `finitely_cocomplete_from_generators` the primal at `C^op` through `HasEqualizers_op_of_HasCoequalizers`, `FiniteCategory_op` and `Opposite_Functor` as a `:=`. Riehl 3.5.17 is `finitely_complete_of_pullbacks_terminal : HasPullbacks C → Terminal C → FinitelyComplete C` and `finitely_cocomplete_of_pushouts_initial`, the corollary composed with `Structure/Pullback/Reduction.v`; the CONVERSE is proved shape by shape — the terminal object as the limit of the empty diagram over `EmptyShape := DiscreteCat False` (`FinitelyComplete_Terminal`), pullbacks as limits over `Roof^op` through `Structure/Pullback/Limit.v`'s `Pullback_to_Universal` (`FinitelyComplete_HasPullbacks`), products and equalizers by Reduction.v — and packaged as `FinitelyComplete_iff_pullbacks_terminal` with its dual `FinitelyCocomplete_iff_pushouts_initial` read at `C^op`. **THE SURROGATE PACKAGES ARE THEREBY THEOREMS**: `Structure/Regular.v`'s terminal-plus-pullbacks clause and `Structure/Topos.v`'s `ElementaryTopos` fields yield `FinitelyComplete` (the topos instantiation is the probe's `topos_FinitelyComplete`), and both headers, plus `Structure/Limit/Creation.v`'s scope note and Shapes.v's, are rewritten LINE-NEUTRALLY so every cited line number stands. **TWO UNIVERSE MEASUREMENTS DECIDE THE CONVERSE'S ROUTE, AND ONE PIN WAS FOUND AND LIFTED**: the small shapes SPLIT — `Roof@{u u0} : Category@{u u0 u0}` and `Parallel` are FREE while `Instance/Two.v`'s `_2`, `Two_Discrete` and `Instance/Zero.v`'s `_0` are `Category@{u Set Set}`, so at an ambient whose homs sit strictly above `Set` a limit over `_2` is refused and `FinitelyComplete C` cannot be instantiated at `_2` though the diagram is accepted (pinned), which is why pullbacks are read at `Roof^op` and the terminal object at the polymorphic `DiscreteCat False` rather than at `_0`, `Two_FiniteCategory : FiniteCategory@{u Set u} _2` inheriting `_2`'s pin as the ONLY constant of the 94 carrying a word-bounded `Set`; and the empty-shape route WAS pinned when first written — with a bare `(C : Category)` binder `EmptyDiagram` minimized to `∀ C : Category@{u0 Set Set}, …` and dragged `empty_cone`, `FinitelyComplete_Terminal` and the biconditional down to a `Set`-homed ambient — measured by `About`, lifted by explicit binders on those four, and pinned against a bare clone (probe N7). `FiniteCategory@{u u0 u1} : Category@{u u0 u0} → Type@{u1}` identifies the shape's hom and proof in its BINDER, inherited from FromProducts.v's `ArrowIx`, which is refused ALONE at a shape whose hom is declared strictly below its proof universe with `x ~> y` and `id[x]` accepted (pinned; `FiniteCategory` refuses at the same argument with the same message and so is not measured on its own); `FinitelyComplete@{u u0 u1 u2 u3} : Category@{u3 u2 u2} → Type@{u}` keeps the ambient OBJECT universe free and puts the shape's hom at the ambient's (the `Limit` vocabulary's identification), exactly FIVE of the twenty-four `fl_*`/`finite_limit` constants — `finite_limit`, `finite_limit_apex`, `finite_limit_leg`, `fl_cone`, `fl_limiting` — carrying the one block equation `u2 = u5` saying the same and the other nineteen no equation at all (an earlier revision said the whole family; a fess audit counted). **THE CONE-CATEGORY CHECKBOXES NEEDED A CORRECTION RATHER THAN A DUAL**: Awodey's "a limit is a terminal cone" had only `Limit_Cones : Terminal (Cones F) → Limit F`, and `Cones_Limit` is the other direction (round trips at `eq_refl` on the cone, every mediator, the terminal cone and the APEX MAP of every unique arrow — the whole arrow is re-paired, stdlib `sigT` having no eta, and the probe's `p417_round_one` checks exactly the first projection; an earlier revision said every unique arrow — the whole records refuted and pinned); but with `Cocones F := Cones (F^op)` a cocone morphism is an arrow of `C^op`, so the colimit is the TERMINAL object of `Cocones F` and `Instance/Cones.v`'s comment saying "initial" was WRONG — delivered as `Colimit_Cocones : Terminal (Cocones F) → Colimit F` and `Cocones_Colimit`, with the covariant spelling `Initial ((Cocones F)^op)` proved THE SAME TYPE at `eq_refl` (`initial_op_is_terminal`), while the issue's literal `Initial (Cocones F)` is a DIFFERENT type, refuted as types (CONVERSION) and refused as the hypothesis of the passage (TYPING), both pinned. The Metacategory donor the issue names is NOT taken: `Theory/Metacategory.v`'s own header records that its guard-free `identity` is unsatisfiable over a finite table so `FromArrows` yields the EMPTY category, and its inline note beside the field still asserted the encoding "is sound for the finite … metacategories built here" — that sentence is corrected line-neutrally in this commit and nothing here consumes that module. **94/94 + 7/7 constants closed under the global context** (77 `Definition` + 11 `Qed`-closed + 3 records + 3 `Build_*`, counted on `Print Module` with its wrapped `Record` heads; Cones/Limit.v's six new plus `Limit_Cones`), all gated FULLY QUALIFIED, the gate's `Closed` count 6501 = its entry count with the `Axioms:` line count at 1; zero collisions over the 100 library names (Finite.v's 94 `Print Module` entries plus Cones/Limit.v's 6 new), by a whole-word declaration-head sweep over every other `.v`, instrument-checked at `Full` = 3 and `Monoid` = 3 files under that pattern (the bare `rg -lw` file counts are `Full` = 97, `Monoid` = 94) — an earlier revision said 109 with no name set stated, a figure that cannot be reconstructed; closure 66 excluding self, of which `Structure/Limit/Product/Finite` costs 22 by drop-one (its own `Coq` witness section), Pullback/Limit, FromProducts, Instance/Two and Construction/Quotient 1 each, every other `Require` 0; nine `Defined` tokens in Finite.v of which exactly ONE is load-bearing by flipping each ALONE (`empty_cone`, whose `Qed` form stops `FinitelyComplete_Terminal` in the file itself, `vertex_obj[empty_cone c]` no longer reducing to `c`), the other eight kept by the data convention, while Cones/Limit.v's one `Defined` (`Cones_Limit`) IS load-bearing, its `Qed` form stopping the probe's `eq_refl` readback of the round-tripped cone. `Test/ProbeFinite417.v` mirrors the target's `Require` list plus the witness modules and carries **13 `Fail`s = 1 instrument + 12 negatives of THREE kinds told apart by the error TEXT** — 5 CONVERSION (the two cone-category types; the two whole-record round trips; a `FinitelyComplete` ascribed at `Complete`, whose `cannot unify` clause compares the SECOND binders `J ⟶ C` against `FiniteCategory J`, the two Π-types differing in arity, classified by the clause as the tree's convention has it; and the FinSet computation below), 2 TYPING (the literal `Initial (Cocones F) → Colimit F`; the generator corollary fed `HasCoequalizers`), 5 UNIVERSE (the bare `EmptyDiagram` clone; `Limit` over `_2` and `FC _2 Two_FiniteCategory` at an ambient above `Set`, with `FC` at `Roof^op` and at `Parallel` accepted; `ArrowIx` and `FiniteCategory` at hom < proof) — each stripped ONE AT A TIME in a copy of the WHOLE file and compiled alone with its whole error read, guard coverage measured mechanically under a STATED tokenization — comment-stripped, every `[A-Za-z_][A-Za-z0-9_']*` token inside a `Fail` command minus the four command keywords — 44 identifiers inside a `Fail`, 33 also named outside one, the 11 exceptions exhaustively the seven names the `Fail`s declare, the instrument's absent name and the three bound variables `CP`, `HCo`, `I`, with stdlib's `nat` `Check`ed (an earlier revision's 32 carried no tokenization and listed `nat` as an exception it then said was `Check`ed); the probe's in-body labels, which a fess audit found shifted by one from its header table from N3 on and calling N5 TYPING, are realigned to the table; rename-simulated 8/8 over the target constants the negatives name — seven break the probe on a control line and the eighth, the pre-existing `Limit_Cones`, breaks `Structure/Limit/Initial.v` before the probe is reached; `make todo` grows by 18, ALL in the probe (2151 → 2169: 13 `Fail` commands + 5 header prose lines), the library files contributing ZERO — true only after a fess audit found the first draft's own disclosure sentence in `Structure/Limit/Finite.v` was a hit (2170, +19, one in the library) and it was reworded to stop spelling the token — so the issue's "adds no new hits" box is not met as written and is disclosed. Witnesses in the probe: `FinitelyComplete FinSet` and `FinitelyCocomplete FinSet` from `FinSet_Pullbacks`/`FinSet_Terminal` and `FinSet_HasPushouts`/`FinSet_Initial`, `FinitelyComplete Sets`, every elementary topos, and the refutation `Omega_not_FiniteCategory` through `Instance/Fun/Closed.v`'s `fin_no_nat_injection` (the identity arrows would inject `nat` into `Fin.t n`) — and one honest negative: the terminal object recovered at `FinSet` from the empty finite limit is NOT the numeral `1` by conversion, `lazy` leaving a `fin_countP` over a predicate that tests `unique_obj (FinSet_Pullbacks_obligation_2 …)`, the pullback UMP `Program` closed opaquely, so it is delivered `≅ 1` by `terminal_unique` and the `eq_refl` form is pinned (N10). NOT delivered: no class bundling finite completeness and nothing registered as an `Instance`; no closure of `FiniteCategory` under products, coproducts or quotients of shapes; no bijective enumeration, cardinality or decidable equality of arrows; no creation or preservation vocabulary at finite shapes (no `PreservesFiniteLimits`, no left-exact functors); no comparison of `finite_limit` with a `Complete` category's chosen limit at any strength; no computing witness — an earlier revision's clause, corrected by #415: Instance/FinSet/Limit.v's native-equalizer route makes the terminal object recovered from the empty finite limit at `FinSet` the numeral `1` by `eq_refl` and the finite limit over `Parallel` at a concrete pair the numeral `2`, while the pullback route this bullet's N10 measures stays stuck on `FinSet_Pullbacks_obligation_2` (its probe witness is now `FinSet_FinitelyComplete_pb`); no bridge to Shapes.v's `FiniteShape`; no filtered categories (Riehl 3.8.7); no equivalence-invariance of `FinitelyComplete`; and the `Set` pin on `Two_FiniteCategory` is `_2`'s, not repaired and not claimed unavoidable
- **Structure/Limit/Comparison.v**: Mac Lane §V.2 Exercise 4 and §V.4 Exercise 5 (book pp. 114, 118, `maclane:V.2:ex4`, `maclane:V.4:ex5`) + Riehl §3.4's opening construction, Lemma 3.4.3 and Exercise 3.4.i, §4.6 Exercise 4.6.iii (`riehl:3.4:construction-canonical-comparison`, `riehl:3.4:lem3`, `riehl:3.4:exi`, `riehl:4.6:exiii`) — **the canonical comparison arrow with a change of index category, the colimit biconditional, and the bridge from `CartesianFunctor`/`TerminalFunctor` to cone-level limit preservation**. THE ISSUE'S PREMISES WERE STALE: the comparison IS named and its biconditional proved since #427 (Structure/Limit/Preservation.v `cone_comparison` … `comparison_iso_of_PreservesLimitCone`, `limit_comparison`, `cocone_comparison`), `fmap_cone` merged into `FCone` (Creation.v), and Riehl's whiskering identity is componentwise at Structure/Limit/Constant.v; what was missing is delivered: `cone_reindex W N : Cone (F ◯ W)` (apex and legs at `eq_refl`, functoriality along `Id` and composites at `eq_refl` apex- and leg-wise — NOT as an equation of cones, `Cone ((F ◯ W) ◯ V)` and `Cone (F ◯ (W ◯ V))` being distinct record types, probe N1, `cone_assoc` repackaging); `reindex_comparison W H N HM : H (vertex N) ~> vertex M` for `M : Cone ((H ◯ F) ◯ W)` limiting, as `cone_comparison H (cone_reindex W N) (islimitcone_assoc HM)`, with its leg equations, uniqueness and invertibility ⇔ limit-ness of the image of the reindexed cone; `preserves_iff_comparison_iso` (the issue's name, the two Preservation.v directions as one Type-valued `↔`); `PreservesLimitCone_of_cone` (preserving ONE limit cone is preserving them all, via `limitcone_iso` + `FCone_iso` + `limitcone_transport`); the colimit biconditional built line for line in the cocone vocabulary (`comparison_iso_ColimitCocone`, `PreservesColimitCocone_of_comparison`, `comparison_iso_of_PreservesColimitCocone`, `preserves_colimit_iff_comparison_iso`), NOT by op-instantiation since `FCocone F N` is a cone over `(F ◯ K)^op` and `FCone F^op N` over `F^op ◯ K^op` (probe N5's clause "cannot unify"; the `cone_op_comp` route accepted as control); `const_image_iso : F ◯ Δ[J](c) ≈ Δ[J](F c)` in `Functor_Setoid` with identity components at `eq_refl`; `pullback_comparison` at `Cospan C` (`eq_refl` to `cone_comparison`); `rapl_comparison_iso` from Adjunction/Continuity.v. **DISCRETE SHAPES AND THE BRIDGE (item 4)**: for ANY `G : DiscreteCat A ⟶ C` a cone is limiting iff its legs are an `IsIndexedProduct` (`discrete_IsLimitCone_of_IsIndexedProduct`, `discrete_IsIndexedProduct_of_IsLimitCone` — the converse of `limit_is_indexed_product` that Functor/Hom/Limit.v recorded as absent), over a LOCALLY annotated `DiscreteCat_Functor'@{o h p uo uh up +}` because the unannotated Instance/Discrete.v functor pins the comparison to `Set` (probe N2, and N3 through Product.v's `family_cone`; Instance/Discrete.v and Test/ProbeHomLimit331.v untouched); `binary_comparison f : F (f true × f false) ~> F (f true) × F (f false)` is `cone_comparison` at the two product cones and `≈ fmap exl △ fmap exr` (the class's `prod_out`, computed); **`cartesian_functor_iff_preserves_binary_products : CartesianFunctor F ↔ ∀ x y, PreservesLimitCone (DiscreteCat_Functor' (binary_fam x y)) F`** and `cartesian_functor_iff_comparison_iso`; `terminal_comparison f : F 1 ~> 1` (`≈ one`), the class's `fobj_one_iso : 1 ≅ F 1` being oriented so the comparison is its `from` (probe N4), **`terminal_functor_iff_preserves_terminal : TerminalFunctor F ↔ PreservesLimitCone (DiscreteCat_Functor' nullary_fam) F`** and `terminal_functor_iff_comparison_iso`. **Universes**: no word-bounded `Set` in any of the 57 blocks; ambients at `C : Category@{u u0 u0}`, `D : Category@{u1 u0 u0}` (one shared hom level, `IsLimitCone`'s identification); `const_image_iso` carries `u0 = u3` and the discrete bridge `h = p`, `h = uh`, `h = up` (the same identification named); nineteen blocks (`DiscreteCat_Functor'` and the eighteen constants built over it) carry the stdlib caps `JMeq`, `EqdepFacts`, `eq_rect_r` of the file's one `Program Definition`, `DiscreteCat_Functor'` itself (an earlier revision attributed them to Structure/Cartesian.v, whose constants carry none); fifty blocks carry one strict bound, always a hom or proof level below a `Program` auxiliary universe (`Compose`'s `u3 < u2`, `DiscreteCat_Functor'`'s `up < u`), the seven elementary constants none. **57/57 constants closed under the global context, zero `Axioms:` lines** (45 `def`, 12 `prf` in the `.glob`), all gated fully qualified; six `Defined`-terminated proofs, two load-bearing by flipping each alone (`const_image_iso` breaks its component readback, `discrete_cone` breaks `discrete_cone_leg`), four kept `Defined` by the data convention; twelve `Qed`; closure 37 excluding self (Functor/Diagonal.v costs 6, Adjunction/Continuity.v 2, four `Require`s 1 each, the other fourteen 0). `Test/ProbeComparison419.v` mirrors the `Require` list and carries **6 refutation commands = 1 instrument + 5 negatives of THREE kinds** (N2/N3 and N5 told from the rest by their universe and unification clauses; N1 and N4 share the bare has-type shape and differ in the types named) — 1 CONVERSION (the two bracketings of a twice-reindexed cone, a has-type mismatch with no universe clause, beside `cone_assoc`), 2 UNIVERSE (`cone_comparison` at the unannotated `DiscreteCat_Functor` under `Constraint Set < uh`, its type naming `DiscreteCat@{_ Set Set}`, and the same through `family_cone`; beside the formable cone type, the annotated comparison and `binary_comparison`), 2 TYPING (`to fobj_one_iso : F 1 ~> 1`, plain has-type, beside `from`; `cone_comparison (F^op) N HM` on an unrepackaged cocone, with the clause "cannot unify Cone (F^op ◯ K^op) and Cone (F ◯ K)^op", beside `islimitcone_op_comp` and `cocone_comparison`) — each stripped one at a time in a copy of the whole file; readbacks at `eq_refl`; guard coverage 35 inside / 31 outside under the plain tokenization, the four exhaustive exceptions the keyword, two binder names and the absent name; rename-simulated 5/5 with every first break on a positive line; `make todo` grows by the probe's 6 refutation commands only (2196 → 2202), so the issue's "adds no new hits" box is not met as written (disclosed, not met by moving the negatives out of the build). NOT delivered: reindexing functoriality as an equation of cones or of comparisons (the types differ); any `IsLimitCone` statement about `cone_reindex` alone (false in general); the comparison as a natural transformation; the Kan-extension reading of `lim (F ◯ W)`; the strict arrow form of the whiskering identity (pinned at Constant.v); the bridges at the tree's `Two_Discrete`/`0` shapes (`Cartesian_Limit`, `Terminal_Limit`, both `Qed` at `Limit` level; no passage between the shapes); `PreservesProductCones` of the tracked but UNREGISTERED Structure/Limit/Preservation/Shapes.v (surfaced, not settled); the image cone built BY whiskering, the operative half of Riehl's checkbox (`const_image_iso` is stated and read back but consumed by nothing; `FCone` stays hand-built); the duplicate `bool_fam` at Instance/Fun/Terminal.v left in place, as is Structure/Limit/Product.v's `family_cone`, the same construction as `discrete_cone` specialised to `DiscreteCat_Functor f`; whether to annotate Instance/Discrete.v upstream instead of carrying the local `DiscreteCat_Functor'` is put to John in the PR.
- **Structure/Limit/Diagrams.v**: Mac Lane §V.2 Exercise 5 (book p. 115, `maclane:V.2:ex5`; the §IX.7 Ex 3 increment `maclane:IX.7:ex3` measured and declined) — **the category of ALL diagrams in a fixed target and the limit as a functor on its opposite, with the colimit dual, the fixed-shape agreement with #353, and a compiled refutation of the issue's comma-form clause**. `Diagrams C` has objects `DObj C` (an index category with a diagram) and arrows `DHom X Y` (a shape functor `dW : didx X ⟶ didx Y` with `dtau : ddiag Y ◯ dW ⟹ ddiag X`, Mac Lane's direction, the one Comma.v produces; the reverse direction also forms a category but limits are not functorial on it, both limits mapping INTO `lim (F ◯ W)`); its hom setoid `DHom_equiv` is Cat's `Functor_Setoid` on the shape functors plus agreement of the transformations THROUGH the isomorphism's image (no transport), `Did`/`Dcomp` are built componentwise because `◯` is neither strictly unital nor associative (probe N1-N4), and the four laws are witnessed by `iso_id`. Under `L : Complete C`, `dlim_map f := limit_med (dlim_is X) (cone_along (dtau f) (cone_reindex (dW f) (isalimit_cone (dlim_is Y))))` (Theory/Equivalence/Colimit.v `cone_along`, Comparison.v `cone_reindex` — NOT `reindex_comparison`, which targets a separately supplied limit) gives **`LimDiagrams : (Diagrams C)^op ⟶ C`**, respectfulness closing by `limit_leg_coherence` (the limit cone's own coherence absorbs the hom-setoid isomorphism, so no `StrictCat` deviation). **Fixed shape = #353**: `fixed_hom b` reads `b : F ⟹ G` as an identity-shape arrow, `lim_obj_is_dlim` at `eq_refl`, `dlim_map_fixed : dlim_map L (fixed_hom b) ≈ Lim_map b` (Adjunction/Diagonal/Limit.v). **Colimit dual**: `Cocomplete C` and `Complete (C^op)` are not convertible (probe N5) but Construction/Product/Limit.v's existing `Complete_op_of_Cocomplete` (reused) and the new `cocomplete_of_complete_op` round-trip at `eq_refl` because `(F^op)^op` IS `F` by conversion; `ColimDiagrams (L : Cocomplete C) : Diagrams (C^op) ⟶ C := Opposite_Functor (LimDiagrams …)`, and `Diagrams (C^op)` IS the covariant colimit category (arrows `F_X ⟹ F_Y ◯ dW^op`); `dcolim_is_colimit_apex` at `eq_refl`. **The comma form**: `CommaDiagrams C := Id[Cat] ↓ Δ(C)` elaborates (also at `Sets` and `Coq`), its square is a natural ISOMORPHISM not required to be an identity (the invertible sub-case, not the identity one), and its hom setoid forgets the square (`CommaDiagrams_equiv` at `eq_refl`, Comma.v); `comma_diagrams_arrow` reads each comma arrow as a `Diagrams` arrow as a FUNCTION only, and the probe's countermodel in `CommaDiagrams Sets` (`p420_f_id ≈ p420_f_negb` with squares `id`/`negb`, `p420_no_respectful_arrow_part`) proves no `Proper` arrow assignment can return the square's component on both arrows, so the canonical arrow part is not respectful (a different arrow part is not excluded) — the issue's "sub-case with the identity transformation" is false on both halves. **Stale premises**: #353 landed (Adjunction/Diagonal/Limit.v; the bullet above already lists "no functoriality in `J`"); of the issue's four "category of diagrams" prose citations only Instance/Fun/Topos.v and Structure/Cone/Const.v still hold. **Universes**: nothing annotated; `Diagrams@{u u0 u1 u2 u3 u4} : Category@{u2 u4 u4} → Category@{u u0 u0}` with `u3 < u`, `u4 < u` (object universe above the index categories' object and hom levels, `Cat`'s shape; the target's hom level is the index categories'); `LimDiagrams` carries `u2 <= u0` (index objects at or below the target's hom level — the smallness side condition, arriving from `Complete`'s quantification); `dlim_map_fixed` and `lim_obj_is_dlim` carry `u0 = u3`, `u1 = u4` (`HasLimitsOfShape`'s identification; `fixed_hom` only the latter); no word-bounded `Set`, no `JMeq`/`EqdepFacts`/`eq_rect_r` cap in any of the 39 blocks. **39/39 constants closed under the global context, zero `Axioms:` lines** (28 `def`, 4 `prf`, 1 `inst`, 4 `proj`, 2 `rec`), all gated fully qualified; two `Defined` (`comma_diagrams_arrow` for its `dW` readback, `fixed_hom` for `dlim_map_fixed`), thirteen `Qed`; closure 78 excluding self (Adjunction/Diagonal/Limit.v 26 at the margin, Theory/Equivalence/Colimit.v 6, Comparison.v 5, Construction/Product/Limit.v 4, fifteen `Require`s 0). `Test/ProbeDiagrams420.v` mirrors the `Require` list plus Instance/Sets.v and carries **7 refutation commands = 1 instrument + 6 negatives of TWO kinds** — 3 CONVERSION (`W ◯ Id = W` and `F ◯ (U ◯ W) = (F ◯ U) ◯ W` at `eq_refl`, "cannot unify", beside `fobj`/`fmap` `eq_refl` controls; `Cocomplete C = Complete (C^op)` beside the round-tripping bridges), 3 TYPING (`nat_id` at `ddiag X ◯ Id ⟹ ddiag X ◯ Id`; the whiskered composite at `(ddiag Z ◯ dW f) ◯ dW g`; the naive bridge `fun D G => L D G` with `G : D ⟶ C^op`) — each stripped one at a time in a copy of the whole file; the compiled countermodel; readbacks at `eq_refl`; guard coverage 37 identifier tokens inside the refutations / 27 also named outside, comments stripped, with ten exhaustive exceptions (the keyword, two binder names, the six refuted-declaration names, the absent name); rename-simulated 5/5 (`Compose`, `nat_id`, `nat_compose`, `Cocomplete`, `Complete_op_of_Cocomplete`), every first break on a guard line; `make todo` grows by the probe's 7 refutation commands only (2202 → 2209), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: the §IX.7 Ex 3 route through ends (Structure/End.v has only `Class End` and `Coend`; no limit-as-end, and the only (co)end functoriality is Theory/Coend/Fubini.v's concrete `Inner`); a limit functor on the comma form (its canonical arrow part refuted, no other attempted) or any functor between `CommaDiagrams C` and `Diagrams C`; a separately built covariant category of C-diagrams for the colimit dual (it is stated on `Diagrams (C^op)`); the fixed-`W` natural-transformation form `LimitFunctor_J ⟹ LimitFunctor_J' ◯ Induced W`; functoriality of `Diagrams` in the target; a colimit-side `dlim_map_fixed`.
- **Structure/Limit/Initial.v**: Mac Lane §III.4 Exercise 3 (book p. 72) + Seven Sketches §3.5.3 Ex 3.98 + Riehl §3.1 — **limits computed by EVALUATION when the INDEX category has an initial object**. `initial_IsALimit I F : IsALimit F (F initial_obj)` with legs `initial_leg := fmap[F] zero`, the mediator out of a competing cone being that cone's own leg at the initial object (`initial_med N := cone_leg N initial_obj`), plus `initial_IsLimitCone` at cone level, `initial_Limit`, and the terminal-cone reading `initial_Cones_Terminal : @Terminal (Cones F)`. **Nothing in tree previously constrained the INDEXING category's own initial object** — `rg 'Initial J\b|@Initial J\b' -g '*.v'` returns 0 hits, which the file header is careful to scope as a search over the SPELLING of the hypothesis and not over its meaning, so it does not rule out an equivalent statement phrased another way, every prior `initial object … limit` hit concerning the initial object of the CONE/comma/factorization category or of the TARGET. **The dual is genuinely covariant**: `terminal_IsAColimit T F : IsAColimit F (F terminal_obj)` and its siblings are routed through `C^op` but **no `^op` occurs in any type in that section**, the four packagings and both accessors being `:=` with no tactic. **NO `Limit F ↔ Initial J` IS CLAIMED** — that converse is neither proved nor asserted; the honest biconditional delivered instead is `limiting_iff_initial_leg_iso : IsLimitCone N ↔ IsIsomorphism (cone_leg N initial_obj)`, both directions proved, with `colimiting_iff_terminal_inj_iso` dual. **BOTH APPENDED-CHECKBOX DONORS HAD TO BE BUILT, AND THAT IS MEASURED**: `Cat_Terminal` (`Instance/One.v`) makes `_1` the terminal object OF CAT, which is NOT `_1` having an initial object WITHIN itself, so `One_Initial : @Initial _1` is new here; and `rg 'Terminal.*Ordinal|ord_top'` returned 0 hits, so `Ordinal_Succ_Terminal n : @Terminal (Ordinal (S n))` and `ord_top` are new too — each verified to exist in no other file. On those: Seven Sketches Ex 3.98 is `point_IsALimit F : IsALimit F (F ttt)` as the SPECIAL CASE of the headline, cross-checked against the terminal-cone definition by `point_Cones_strict` (`eq_refl`), with a `Sets` witness whose mediator COMPUTES (`point_Sets_med b : limit_med … (elt_cone b) ttt = b` by `eq_refl`) — though that identification holds at ARBITRARY arguments (`initial_med_strict`), so instantiating it proves nothing by itself and the content is `point_Sets_separates`, which shows the apex has two distinct elements (by `discriminate`, the mediator being definitionally the leg — it is not the universal property that separates); Riehl's dual is `ordinal_succ_IsAColimit G : IsAColimit G (G (ord_top n))`. **RIEHL'S ω+1 IS NOT INSTANTIATED and the file says so** — no ω+1 category exists in tree, so what is delivered is finite `Ordinal (S n)` together with `Omega_Initial : @Initial Omega` and `Omega_no_Terminal : @Terminal Omega → False`, which is the half of Riehl's remark that ω itself supplies; nothing claims `Ordinal (S n)` IS ω+1. Universes: `initial_IsALimit`/`initial_Limit` identify the index category's hom-and-proof universe with the ambient category's (`u0 = u2`) while **both OBJECT universes stay free**, and the identification has **at least THREE co-equal donors, two of them CONE VOCABULARY** — with the levels declared apart `ACone`/`Cone` elaborate while `cone_leg` (`Structure/Limit/Preservation.v`), `IsLimitCone` and `IsALimit` (`Structure/Limit.v`) are ALL rejected with the same `Cannot enforce jp = jh`; an earlier revision of this bullet blamed `IsALimit` alone on the strength of the `ACone` control, which rules out only `ACone`/`Cone` and licenses no such conclusion, and `IsALimit` is not even the donor met FIRST (`initial_med` already carries `u0 = u2` while its type mentions no `IsALimit`, its body being `cone_leg N initial_obj`, and `initial_leg` carries only the bound `u0 <= u2`). None of the three is annotated, so it stays a repairable defect, NOT claimed unavoidable. **SEVEN constants carry `Set` in a constraint block, not one** — `bool_set`, `elt_acone`, `elt_acone_obligation_1`, `elt_cone`, `point_Sets_apex`, `point_Sets_med`, `point_Sets_separates`, all `Set < u` from `bool : Set`; an eighth, `ordinal_omega_apex`, has `Set` in a universe INSTANCE while acquiring no constraint, an instance not being a constraint. 62/62 constants closed under the global context (57 glob `def`+`prf` — there are no `inst`/`proj`/`rec` entries and no `Build_*` — plus 5 `Program` obligations invisible to the glob, all queried by fully-qualified name). `Test/ProbeLimitInitial334.v` pins every refutation the file measures, with **5 negatives of 2 KINDS** — two CONVERSION (`initial_leg` at the initial object is not `id` on the nose, `zero` there being only `≈`-equal to `id`; and the whole-record `Limit_Cones … = initial_Limit`, whose CONE components DO agree by `eq_refl`) and three FORMABILITY (the universe identification at each of `IsALimit`, `cone_leg` and `IsLimitCone`, with `ACone`/`Cone` formable at the same levels and the same three applications elaborating at levels left unconstrained, so each negative is shown to fire on the CONSTRAINT) — plus an instrument check, each stripped and its kind confirmed, rename-simulated **8/8** unpadded over the constants the NEGATIVES name (an earlier revision scored 7/7 by counting `ACone`, which occurs in no `Fail`). **DISCLOSED, NOT ACTED ON**: the in-tree `InitialFunctor` notation (`Functor/Structure/Terminal.v`) means "preserves the initial object" and collides with the standard "initial functor" = cofinal; the issue defers that rename to wherever the cofinality vocabulary lands, and nothing is renamed here. NOT delivered: no cofinality/final-functor machinery; no ω+1; no converse `Limit F → Initial J`
- **Structure/Limit/Indexed/Hom.v**: Riehl Remark 3.1.27 clauses (i) and (ii) — the indexed (co)product hom bijections upgraded from unique EXISTENCE to a NATURAL isomorphism. `icoprod_hom_iso H : @Isomorphism ([C, Sets]) (Curried_Hom C p) (icoprod_hom_functor f)` with `icoprod_hom_functor f : c ↦ ∏ₐ C(f a, c)` over `Sets_iprod_obj`, and the symmetric `iprod_hom_iso H : @Isomorphism ([C^op, Sets]) (Curried_CoHom C q) (iprod_hom_functor f)`. This is what `Structure/Limit/{Product,Coproduct}.v` did NOT have: `iprod_desc`/`icoprod_desc` give `∃! u, ∀ a, proj a ∘ u ≈ pi a` with no naturality, and neither file contains `≅[Sets]`, `@Isomorphism` or `hom_iso` (measured by `rg -n` over exactly those two files). **THE COPRODUCT SIDE IS FREE**: every coproduct constant is its product counterpart at `C^op`, supplied by `:=` with no tactic and no obligation, on two conversions each measured by `eq_refl` — `(C^op)^op = C` and `@Curried_CoHom (C^op) p = @Curried_Hom C p` — so Riehl's two clauses are symmetric BY CONSTRUCTION rather than by a parallel development. **The relation back to the `∃!` accessors is at `eq_refl`, which is the strongest available no-drift guarantee**: `iprod_hom_iso_from_is_desc`/`icoprod_hom_iso_from_is_desc` show the backward leg's value IS `unique_obj (iprod_desc H fam)`; the converses `iprod_of_hom_iso`/`icoprod_of_hom_iso` recover `IsIndexedProduct`/`IsIndexedCoproduct` from invertibility, packaged as the biconditionals `iprod_iff_hom_iso`/`icoprod_iff_hom_iso` whose projections return the named legs by conversion. The biconditional is the honest statement rather than a repackaging because the comparison transform is defined for ANY candidate family with no universal property assumed — its naturality is associativity alone — so all the content sits in invertibility, and the hypothesis is proved non-vacuous by `Sets_bad_not_iprod`/`Sets_bad_not_icoprod`, a candidate cone in `Sets` whose comparison map is not invertible. **The `(DiscreteCat A)^op`-vs-`DiscreteCat A` trap is SIDE-STEPPED, not settled**: everything is stated against the elementary records `IsIndexedProduct`/`IsIndexedCoproduct`, which mention no discrete diagram, so the question never arises — and the payoff is measured rather than asserted, all four compositions with the other presentations being exhibited (`limit_hom_iso`, `colimit_hom_iso`, `indexed_product_hom_iso`, `indexed_coproduct_hom_iso`); the translation between the two shapes remains open, and `Structure/Limit/Coproduct.v`'s own header records why (`eq_refl : (DiscreteCat A)^op = DiscreteCat A` is rejected). Universes measured in the constraint blocks: `iprod_hom_functor` bounds the index and C's homs by the target `Sets`' carrier universe INDEPENDENTLY of each other, so the target functor alone tolerates an index strictly larger than C's homs, while `iprod_hom_transform` forces `ua <= uh` — attributed to neither `Setoid` nor `SetoidObject` being cumulative here (measured: lifting `Setoid@{a a}` to `Setoid@{b b}` with `a < b` is rejected), so the ambient carrier universe is forced EQUAL to C's hom universe; and the `{A : Set}` pin on `limit_hom_iso`/`colimit_hom_iso` has its two causes SEPARATED by probe — `DiscreteCat` pins C's hom/proof universes to `Set` (donor, index still free) and this file's index bound is what then cuts the index, neither alone sufficing. Seven negatives (5 formability, 2 conversion) against 10 controls, each stripped and confirmed; the two refuted round trips are diagnosed to `iprod_desc`'s uniqueness clause being `≈`-level, and at `Sets` sharpened further — the `morphism` fields agree on the nose, so what differs is the `proper_morphism` certificate the tupling rebuilds. A NEW FILE rather than an in-place extension because it is a dependency cycle and not taste: the right-hand side is `Sets_iprod_obj` from `Instance/Sets/Products.v`, which already Requires both `Structure/Limit/Product.v` and `Structure/Limit/Coproduct.v`, so neither can Require it back — `Structure/Limit/Weighted.v` is the in-tree precedent. 59/59 constants closed under the global context. NOT delivered: naturality in the family `f` or the index `A`, Riehl clause (iii)'s `(I × J)`-matrix form (#336), and `Representable` instances for the two hom-functors
- **Structure/Limit/Power.v**, **Structure/Limit/Power/Hom.v**: Mac Lane §§III.3–III.4 + Riehl §3.5 Examples 3.5.4/3.5.8 + Awodey §3.2 — powers and copowers of an object by a SET, the constant-family case named at last. `power J b := indexed_product (fun _ : J => b)` and `copower := @power (C^op)`, with `IsPower`/`IsCopower`, `power_ev` (Riehl's `ev_i`) / `copower_inj`, covariant `power_desc`/`copower_desc` (no `^op` visible to a consumer), and the limit-shaped `power_of_limit`/`limit_is_power` + duals. **The characterizing isomorphisms are DERIVED, not reproved** — `power_hom_iso`/`copower_hom_iso` are `:=` of `Structure/Limit/Indexed/Hom.v`'s at `fun _ : J => b`, and the header says plainly that their being the donor's terms at `eq_refl` is trivially true BECAUSE they are defined that way, with ten `*_is_donor` Examples pinning it so a later edit cannot silently make them parallel constructions. **The non-trivial strict fact is `power_hom_functor_is_Sets_power`**: Riehl's `C(X,A)^I` IS this same `power` operator applied in `Sets`, by `eq_refl` — which is what the constant family actually buys — with `power_hom_iso_at` deriving the literal pointwise `C(X, A^I) ≅[Sets] C(X,A)^I` from the natural iso's two laws plus one `id_right`. **NOTATION `J ⋔ b` / `J · b` in an OPT-IN `power_scope`** (the `addition_scope` precedent): Mac Lane's own `b^J` is unavailable (`^` is the exponential in both `object_scope` and `category_scope`) and `⊙` is unavailable (globally taken, no scope); `⋔` has zero tree-wide hits and `·` has 71, ALL in comments and none in a `Notation`/`Infix`/`Reserved`. Neither of the two existing `Pow`s is shadowed — `Instance/Fun/Discrete.v`'s n-fold product OF CATEGORIES and `Structure/Topos.v`'s topos power OBJECT are different notions, and this file takes neither name. **The `Sets` witness was ALREADY IN TREE and is reused rather than rebuilt**: `Sets_constant_iprod_exponent` (`Instance/Sets/Products.v`, built for Awodey §2.9 Ex 7(b)) with `Sets_exponent_eval`/`Sets_exponent_IsIndexedProduct`, so `Sets_power_exponent` is a `:=` of it; genuinely new are `Sets_power_carrier` and `Sets_power_ev_computes` (both `eq_refl`), `Sets_copower_prod : J · Y ≅[Sets] Sets_discrete J × Y`, and Awodey's decomposition `Sets_copower_one`/`Sets_copower_fin`/`Sets_copower_two : bool · 1 ≅ 1 + 1` — the literal `2 = 1+1`, obtained from `Sets_icoprod_bool` at the constant family so the passage to the binary coproduct costs nothing, with `Sets_copower_three_distinct` for non-vacuity. **TWO FILES, not the issue's one, and the reason needed restating**: `Power.v` is the light naming layer, `Power/Hom.v` the heavy satellite — but **`Instance/Sets` itself is ALREADY in both closures, and in GAFT's**, one hop through `Structure/Cone.v`, so "kept free of `Instance/Sets`" would be false and a textual grep of `Adjunction/GAFT.v` (which returns zero) measures that file's own text rather than what it Requires. What the split avoids is the **satellites**: comparing closures, a combined file would add TEN modules to GAFT's — `Functor/Hom.v`, the four `Instance/Sets` satellites (Cartesian, Cartesian/Closed, Cocartesian, Products), the three `Structure` ones, `Structure/Limit/Indexed/Hom.v` and `Structure/Limit/Power/Hom.v`. The #320 Product.v-vs-Indexed/Hom.v split is the precedent, as a split by DIRECT Require, and `Structure/Limit/Weighted.v` shows the separation is not obligatory for a `Structure/Limit/` file. **The optional refactor is HALF DONE and the other half is IMPOSSIBLE, which corrects the issue**: `Theory/WeaklyInitial.v`'s anonymous endomorphism power now reads through `power_of_limit`, machine-checked non-disruptive by compiling the pre-refactor text under a second module name and finding the type AND the universe constraint block identical; but `Adjunction/SAFT.v`'s `cogen_power` is NOT a power at all — `cogen_power_fam G c := fun p => cog_obj G (projT1 p)` genuinely varies over its Σ-index, so there is no `b` with family `fun _ => b`, and the word "power" there names the classical phrase rather than the operator. SAFT is untouched. Universes measured in the constraint blocks: `power` carries `u <= u0` and nothing else touching the index; `power_of_limit` pins C's hom and proof universes to `Set` (Product.v's doing) but leaves the INDEX FREE, which is exactly why the WeaklyInitial refactor works there, its index being a hom-type; and at the iso level the donor's constraint is stronger than a bound — `iprod_hom_iso` IDENTIFIES the index universe with C's hom universe, inherited in full by `power_hom_iso`. Six negatives (3 formability, 3 conversion) against 8 controls, each stripped and quoted; the conversion three are worth knowing — a power's carrier is the bare function type while the exponential's is setoid MORPHISMS out of the discrete setoid, so `Sets_power_exponent` is an iso and nothing stronger. Engineering finding, second sighting of the hazard `Instance/Sets/Products.v` records: a `Program Definition` here raises NO obligation because instance resolution closes `proper_morphism` during elaboration, and silently pins `Set = u`; supplying the certificate under `unshelve refine` fixes it. 80/80 constants closed under the global context. NOT delivered: functoriality of `power`/`copower` in either argument, the enriched/cotensor reading over `Structure/Limit/Weighted.v`, and a `HasIndexedCoproducts FinSet` (so Awodey's decomposition lands in `Sets`, not the computable category)
- **Structure/Limit/Power/Adjunction.v**: Mac Lane §IV.2 Exercise 12 (book p. 90, `maclane:IV.2:ex12`) + §IV.7 Exercise 1 (book p. 102, `maclane:IV.7:ex1`) — **the copower `c ↦ J·c` is left adjoint to the power `c ↦ c^J`, and the adjunction is natural in the index `J`**: `Copower_Power_Adjunction J : Copower_Functor J ⊣ Power_Functor J` (the issue's one pinned name, delivered verbatim) with the parameter half in the same file. **THE FILE PLACEMENT IS FORCED BY A DEPENDENCY CYCLE, NOT A COST**: `Structure/Limit/Power/Hom.v` Requires `Power.v`, and the adjunction COMPOSES that file's two `_at` bijections, so it cannot live in `Power.v`; extending `Power/Hom.v` was possible and declined because `Adjunction/Parameter.v` is a marginal 46 on that base. **§A** — `Power_Functor {C HP} (J : Type) : C ⟶ C` with all three laws from `power_desc`'s uniqueness clause, and `Copower_Functor := Opposite_Functor (@Power_Functor (C^op) HC J)` at zero obligations; functoriality in the OBJECT was genuinely absent (`Power_Functor` returns zero files tree-wide), while the copower/power vocabulary is #321's and is consumed, not rebuilt — the issue's "suggested" module path was already TAKEN by that file. **§B** — the adjunction is built through `Build_Adjunction'` by COMPOSING #321's `copower_hom_iso_at` and `power_hom_iso_at` (`cp_adj_iso := iso_compose (iso_sym …) (…)`, a `:=` with no tactic), the reviewer's "does not fork the API" check; **the two donors are shown to meet on the nose at a NAMED middle** (`cp_middle b c`, with `cp_copower_at_middle`/`cp_power_at_middle` supplying each donor as a TERM at a type naming it), and a first draft of that check was an `X = X` tautology, replaced and now shown non-vacuous by a DISCRIMINATING negative at the swapped middle; nine `eq_refl` readbacks, including that `Copower_Functor`'s arrow action IS the `copower_desc` mediator with no `^op` in the statement. **§C** — the power as `Structure/Limit/Weighted.v`'s `WeightedLimit` at the terminal shape `_1` with a DISCRETE weight picking out the index set (`power_WeightedLimit`, `wlim_obj = power J b` at `eq_refl`), the copower half built DIRECTLY over `Opposite _1` because `Opposite _1 = _1` and `Opposite_Functor (copower_diagram b) = @power_diagram (C^op) b` are both refuted (one CONVERSION, one TYPING whose error reports the SHAPES), and — beyond what either scout attempted — the CONVERSES `power_of_weighted`/`copower_of_weighted`: an ARBITRARY weighted limit of that weight and diagram has a power as its object, over a section assuming nothing about the ambient category; `wlim_natural` is spent at exactly two sites; `conical_weighted` is a DIFFERENT theorem (its constant weight sits at the TERMINAL setoid over an ARBITRARY shape, where §C's picks out the index setoid over `_1`) and is cited as such; the round trips return the evaluations only up to a residual `∘ id` (`wrt_ev_residue`/`wrt_inj_residue` at `eq_refl`, the strict forms refuted and pinned). **§D — MAC LANE'S EX 1, DELIVERED TWICE, AND THE PRINTED PAGE HAS THE VARIANCE WRONG**: the book prints `C(X·a, c) ≅ Set(X, C(c,a))`, whose right side is contravariant in `c` where the left is covariant — read at 400 dpi, a MISPRINT and not an OCR artifact — so the delivered form is the corrected `C(J·a, c) ≅ Set(J, C(a,c))`, the printed one disclosed with the variance count. §D1 is unrestricted: `Copower_Bifunctor_Iso J : CopLeft J ≅ CopRight J` in `[C^op ∏ C, Sets]`, where `CopRight J` is literally `@Power_Functor Sets Sets_HasIndexedProducts J ◯ Hom C` — §A's own power functor read at `Sets` — and the pointwise inverse is upgraded through #369's `componentwise_iso` rather than hand-built. §D2 is the PACKAGED reading, `Copower_Object_ParametrizedAdjunction : ParametrizedAdjunction Ex1_Bifunctor` under **`HomAllStrict C := ∀ x y, @HomStrict C x y`** — CONSUMING `Construction/Comma/Special.v`'s per-hom-set predicate rather than redeclaring it, after the first draft's `HomStrict`/`One_HomStrict` collided with two names already in that file — whence #396's Theorem 3 assembles **`MacLane_Ex1_hom_bifunctor : C^op ∏ C ⟶ Coq`, THE HOM-FUNCTOR**, with `fobj (a,c) = (a ~{C}~> c)` at `eq_refl` and the arrow action pointwise `g ↦ k ∘ g ∘ unop h` through `conj_mate`'s own formula; the hypothesis has exactly ONE in-tree model, `_1` (its hom-setoid IS `Morphism_equality`, so `One_HomAllStrict` is the identity implication), labelled DEGENERATE. §D1 and §D2 are NOT related to each other (they land in `Sets` and `Coq` respectively, and no comparison functor is built). **THE ISSUE'S ITEM 4 AND EX 1 PARAMETRIZE DIFFERENT VARIABLES** — Ex 1 fixes the index and varies the OBJECT, item 4 fixes the object and varies the INDEX — so **§E** delivers the other: `Copower_ParametrizedAdjunction`, `Power_Bifunctor : Coq^op ∏ C ⟶ C`, with `pa_right … J = Power_Functor J` and the parametrized bijection = `cp_adj_iso` both at `eq_refl` (so §E does not fork §B's bijection), and `power_reindex_conjugate := pa_natural_p PA h` ascribed at #394's `Conjugate`, the ascription being the whole proof; uniqueness and functoriality are #396's `pa_param_mate_universal`/`_id`/`_comp` cited by name, and what is proved fresh is the elementary reading `copower_reindex_inj`/`power_reindex_ev`. **`Sets` AS THE INDEX CATEGORY IS REFUTED FOR THE CANONICAL ACTION, BY A SHIPPED AXIOM-FREE COUNTERMODEL, AND THE SCOPE IS STATED**: the copower injections are indexed by ELEMENTS, so the `fmap_respects` obligation of the canonical arrow action (the copower mediator of the injections, which any action commuting with the injections must be) demands `f j = f' j` at Leibniz equality from `f ≈ f'`; `sets_index_respects_absurd : SetsIndexRespects → False` refutes exactly that obligation over a two-element setoid under the total relation (`Print Assumptions` Closed), which is why the index category is `Coq` — and no other arrow action over `Sets` is claimed refuted. **UNIVERSES, off BOTH binder and block over all 147 constants**: every constant that binds a general `C` is over `Category@{u u0 u0}` in the BINDER (hom identified with proof by reusing the level; the ten that bind none are the `_1` witnesses, the two weights, `index_setoid` and `one_hom_eq`), no word-bounded `Set` anywhere; §A carries BOUNDS ONLY (index-free); `cp_adj_iso` and `Copower_Power_Adjunction` carry FOUR block equations (`u0 = u1`, `u0 = u2`, `u0 = u4`, `u0 = u5`), and **an isolating experiment shows they are PER-DONOR and present BEFORE composition** — `copower_hom_iso_at (copower_ump J b) c` applied alone already carries two, `power_hom_iso_at (power_ump J c) b` alone the other two, composing adds nothing, and the cause is the bare donor's BINDER (`∀ {C : Category@{u3 u u}} {J : Type@{u}}`, hom, proof AND index at ONE level with an empty block) meeting a class-drawn `_ump` whose index slots are separate levels. **BOTH SCOUTS GOT THIS WRONG AND THE BUILDER'S EXPERIMENT REFUTED THEM**: they read the UNAPPLIED donors, saw no block equation, and attributed the four to "the composite iso" — a reading of an unapplied constant measures its binder, not what a consumer gets. §C's forward direction carries `u0 = u3` (C's hom-and-proof = the index universe), LOCALIZED by a guarded probe to `WeightedLimit` itself — `power`, `power_ev`, `Power_Functor`, `index_setoid`, `power_weight`, `power_diagram` and `HomDiagram cu (power_diagram bu)` are all ACCEPTED at `Constraint ch < ji` while `WeightedLimit (power_weight Ju) (power_diagram bu)` alone is refused, so it is putting the weight and the hom-diagram into ONE `Sets` that forces it — while its converse carries NO equation; §D2 carries none; §D1 two (an ATTRIBUTION to the coproduct class, not an isolation); §E inherits §B's four. **As a boundary a consumer meets**: at an index universe strictly above `C`'s hom level, `Power_Functor`, `Copower_Functor` and `power_fmap` are ACCEPTED while `Copower_Power_Adjunction` is REFUSED (probe negative 14, the three as controls). None of it is claimed unavoidable; the hom = proof identification is inherited, with `Opposite` and `IsIndexedProduct` the two INDEPENDENT donors (`HasIndexedCoproducts` is a `Definition` at `C^op` and `HasIndexedProducts` mentions `IsIndexedProduct`, so neither can be tested apart — the #340 trap) — that donor attribution is MEASURED out of tree and pinned nowhere. **147/147 constants closed under the global context** (86 `Definition` + 61 `Parameter` on flattened `Print Module`; the glob sees 110, the 37 `Program` obligations existing only after elaboration — cross-checked by 37 `Next Obligation`s under `Obligation Tactic := idtac`), zero `Axioms:` lines, all 147 in the gate FULLY QUALIFIED (block 5 of that target 25.5 → 38.9 KiB; largest 73.6); zero collisions over the 147 plus the probe's 30 (criterion: whole-word in any `.v` other than the two new files; instrument `power` 42, `Monoid` 91 under that exclusion); closure **111** (from §A–§B's 38; drop-one marginals `Adjunction/Parameter` 28, `Instance/Coq` 13, `Instance/Fun/Morphisms` 10, `Comma/Special` 4, `Weighted` 1, `Power/Hom` 2). **Seven `Defined`s, exactly THREE load-bearing by flipping each alone** (`Copower_Power_Adjunction`, `ex1_pa_adj`, `cp_pa_adj`), all three PINNED by opaque `Qed`-closed clones whose readbacks are refuted. `Test/ProbeCopower366.v` mirrors the target's 28 `Require`s and carries **15 `Fail`s = 1 instrument + 14 negatives of THREE kinds told apart by the error TEXT** (9 CONVERSION, 3 TYPING, 2 FORMABILITY), each stripped one at a time and compiled alone, guard coverage checked mechanically over every identifier occurring inside a `Fail` (comment-stripped), rename-simulated 15/15 with zero vacuous guards. **Negatives 2 and 3 are ONE equation at two spellings and come out as DIFFERENT kinds** — with both sides' implicits explicit, `@Copower_Functor C HC J = @Power_Functor (C^op) HC J` is refused at the STATEMENT (TYPING, no `cannot unify`), while `Copower_Functor J = Power_Functor J` at `C` elaborates and is refused at `eq_refl` (CONVERSION). **A FALSE GUARD WAS CAUGHT BETWEEN BUILDER AND GUARD AND IS RECORDED IN THE PROBE, and its tell is general**: negative 2 as first written left the left side's category implicit, the elaborator re-solved it as `C^op` (leaving the coproduct instance an unresolved evar) so the two sides DID share a type, and the error fired at `eq_refl` — refuting negative 3's fact restated at `C^op`, not the type mismatch the comment claimed. A negative whose stated reason is "the two sides do not share a type" but whose error fires at `eq_refl` elaborated its statement and is measuring something else; write both sides' implicits out (the `obj[?C]` family `Test/ProbeFiniteProducts335.v` records). `make todo` grows by 29, ALL in the probe (15 refutation commands + 14 prose lines matching that pattern); the library file contributes ZERO after a prose verb was reworded. NOT delivered: no `Set`-indexed reading and no investigation of a discrete-setoid subcategory of `Sets`; no non-degenerate model of `HomAllStrict`; no comparison between §D1 and §D2; no naturality of the §C comparison in `J` or `b` and no uniqueness for `wlim_obj`, so §C is an agreement of presentations at fixed data, not an equivalence of structures; no `WeightedLimit` statement for a general indexed product; no 2-categorical reading and nothing about `Adjunction/Map.v` or `Instance/Adj.v`; nothing registered as an `Instance`; no concrete witness at a named category for §B, §D1 or §E; and two `rewrite` failures Core met (a goal DISPLAYING the pattern it is given, its implicit object arguments MIXED under `Set Printing All`; repaired by `apply compose_respects` and conversion) are recorded in prose and pinned nowhere, a failing TACTIC not being expressible as a `Fail` command
- **Structure/Limit/Product/Finite.v**: Mac Lane §III.5 Proposition 1 (book p. 73) + Awodey 1st ed. §2.7 — **all finite products from a terminal object and binary products**, at the ELEMENTARY level. `fin_IsIndexedProduct : IsIndexedProduct f (fin_prod f) (fin_proj f)` for every `f : Fin.t n → C` under `Cartesian C` + `Terminal C` (which are genuinely separate hypotheses — `Class Cartesian` carries no terminal object), packaged as `Cartesian_Terminal_HasFiniteProducts` into the new `Class HasFiniteProducts` mirroring `HasIndexedProducts`'s three-field shape. **THE NOVELTY IS THE VARYING FAMILY, NOT THE FOLD — and the blanket claim would have been FALSE.** Three iterated binary-product folds already exist: `law_pow` (`Theory/Lawvere.v`), `pow` (`Theory/Multicategory/Endomorphism.v`) and `Pow` (`Instance/Fun/Discrete.v`) — **all three fold a CONSTANT family**, computing a power `X × X × … × X`. What was absent is a fold of a VARYING family together with its universal property — and read that census at its actual scope, which is folds of the BINARY PRODUCT `×`: monoidal `⨂` folds exist too, `nf` and `tpower` constant-family, and `tensor_list`/`tfold` (`Theory/Multicategory/Representable.v,67`) genuinely varying, so “a fold of a varying family” is not by itself distinguishing and the binary product plus the universal property is what is; a name-based `rg` for `HasFiniteProduct|FiniteProduct|…` returns 0 hits and would have licensed the wrong sentence, the shape search is what corrected it. The gap is conceded in the donor's own prose (`Structure/Cartesian.v`: the class "axiomatizes the binary product; the nullary product (the terminal object 1) is supplied separately by [Terminal]…"). **THE FOLD IS `Pow`'s FOLD, AND SO IS THE WART**: right-associated with the terminal object as the empty case, so `fin_prod 1 f` is `f F1 × 1` and NOT `f F1`, and `fin_prod 2 f` is `f F1 × (f (FS F1) × 1)` — both refuted at `eq_refl` and pinned, with `fin_prod_one_iso`/`fin_prod_two_iso` the repairs. **HOW IT SITS BETWEEN ITS TWO NEIGHBOURS, which the issue does not mention**: `Terminal_Limit` (`Structure/Limit/Terminal.v`) is the n=0 case and `Cartesian_Limit` (`Structure/Limit/Cartesian.v`) the n=2 case, both iff-shaped at LIMIT level over a named shape. This file forms no `Limit` and no `Cone` at all — DELIBERATELY, because `DiscreteCat_Functor` (`Instance/Discrete.v`) is universe-unannotated and routing through it would pin C's hom and proof universes to `Set` (the #331 measurement, re-verified here — `Limit (DiscreteCat_Functor f)` elaborates at `C : Category@{u1 Set Set}`; that bullet records the pin as a minimization artifact NOT claimed unavoidable, and neither is it here) — so **no `Limit`-shaped corollary is delivered** and the neighbours are related only through their CONCLUSIONS: `fin_zero_IsTerminalObj` and `fin_two_IsCartesianProduct`, each proved FROM the universal property rather than by inspection. It quantifies over all finite arities where both neighbours fix one, and it is a biconditional like them (`HasFiniteProducts_iff C : HasFiniteProducts C ↔ (@Cartesian C * @Terminal C)`) — but **NOT an equivalence of structures**, and four refutations measure that gap: neither recovered `Cartesian` nor recovered `Terminal` is the input on the nose, and the round trip is not the identity, because the recovered product is the PADDED `x × (y × 1)`. The positive half is pinned beside it and is the sharper statement — the `Terminal` RECORD is not recovered while `terminal_obj` IS, by `eq_refl`. **AWODEY §2.7 IS DISCHARGED, NOT DISCLAIMED**: `iprod_unique_iso` proves two indexed products of one family canonically isomorphic — read its type precisely, since `Check` returns a BARE `p ≅ q` and the projection-commutation lives in the two sibling constants `iprod_compare_commutes` and `iprod_compare_inv_commutes` delivered alongside it (`to (iprod_unique_iso …) = iprod_compare …` by `eq_refl`), so it is the PAIR that beats the bare-iso form this tree's reviewers reject and not the one constant — which then licenses `awodey_fold_iso` relating the LEFT-associated `awodey_prod := ((x × y) × z)` — built here with its own `awodey_IsIndexedProduct` — to the right fold at n=3; the two are provably different OBJECTS, which is why the comparison is an isomorphism and nothing stronger. The GENERAL left-fold comparison is NOT delivered — but the reason an earlier revision of this bullet gave is FALSE, and is corrected in the file header rather than dropped: it said a left fold peels the LAST index while `Fin.t (S m)` peels at the FRONT so a back-peeling eliminator would be needed first, and both halves are wrong. Stdlib HAS one (`Fin.case_L_R'`), and none is needed anyway — an ACCUMULATOR turns front peeling into left association, with `fin_prod_l 2 (fin3 x y z) = awodey_prod CP x y z` holding by `eq_refl` (audit-supplied, compiled out of tree). The comparison is UNATTEMPTED, not obstructed; what is genuinely untested is its universal property, not its definition. Universes measured in the constraint blocks: `fin_prod@{u u0}` and `fin_tuple@{u u0}` have **EMPTY** blocks, the object universe neither identified with nor bounded by the hom universe; `iprod_unique_iso` leaves the index universe only BOUNDED, never identified; and the hom-and-proof identification is INHERITED from **three donors INDEPENDENTLY** — `Cartesian`, `Terminal` and `IsIndexedProduct` are each rejected under `Constraint uh < up` while a control naming a hom at those levels is accepted, so no single one is "the" cause (the #334 lesson applied: siblings tested before attributing). Non-vacuity is five computing `coq_*` Examples over `Coq` — the fold's object shape at arities 0/2/3, a mixed two-factor tupling, and a projection — exercising the fold's DATA and not its universal property, with no concrete `HasFiniteProducts` instance registered anywhere. **77/77 constants closed under the global context** (76 glob `def`+`prf`+`proj`+`rec`, no `inst`, plus `Build_HasFiniteProducts`; the file contains no `Program`, hence no obligations). `Test/ProbeFiniteProducts335.v` pins **10 negatives of 2 KINDS** — 7 CONVERSION (the two padding refutations, three round-trip legs, the whole round trip, and right-fold-vs-left-fold) and 3 FORMABILITY (the universe rejection at each of the three donors) — plus four positive `eq_refl` identifications and an instrument check, each negative stripped and its failure KIND confirmed, rename-simulated **10/10 unpadded** with control-only names explicitly excluded. Two probe defects were caught before the audit and recorded in-file: one negative was a FALSE GUARD firing on an unresolved implicit (`obj[?C]`) rather than the universe identification, repaired by giving the category explicitly; and `@Cocartesian_Initial_HasFiniteCoproducts` is UNPARSEABLE — `Structure/Cocartesian.v` declares `Notation "@Cocartesian C"`, which captures it and reports the missing reference `_Initial_HasFiniteCoproducts` — so that control is written bare. The dual is complete (`fin_coprod`, `HasFiniteCoproducts := @HasFiniteProducts (C^op)`, `Cocartesian_Initial_HasFiniteCoproducts`) with covariant accessors and **no `^op` in any consumer-facing type**, though `Check` displays the implicit category so a conclusion prints as a bare `Cartesian` — a display hazard flagged in-file. NOT delivered: no `Limit`/`Cone` and hence no bridge to the neighbours AS STATEMENTS; no `HasFiniteProducts` instance for any concrete category and no `Instance` registration; no functoriality or naturality of `fin_prod` in `f` or `n`; no associativity or commutativity coherence for the fold; no `Fin.t (m + n)` splitting; and no relation to `Structure/Limit/Power.v`'s `power`, the constant-family case
- **Theory/Algebra/Monoid/Product.v**: Mac Lane §VII.3 Exercise 1 (+ Awodey §2.6's componentwise product) — **the category of internal monoids has finite products**: `Mon_Cartesian : @Cartesian (Mon C)` and `Mon_Terminal : @Terminal (Mon C)`, with the product object `x × y` carrying `mu := (μX ∘ exl⨂exl) △ (μY ∘ exr⨂exr)` and `eta := ηX △ ηY`, the terminal monoid being `1` with both structure maps `one`, and `exl`/`exr`/`fork`/`one` each proved a monoid homomorphism. **THE ISSUE'S CENTRAL OBSTACLE IS REAL, THOUGH NARROWER THAN A FIRST DRAFT OF THIS BULLET SAID — TWO SPELLINGS PRINT ALIKE, NOT THREE.** `Theory/Algebra/Monoid.v` declares `Class Monoid (X : C)` with fields `mu`/`eta`/`mu_assoc` over a GENERAL monoidal base, and that is what `Mon` is built on (`Theory/Algebra/Monoid/Hom.v` Requires `Category.Theory.Algebra.Monoid` and never `Structure/Monoid.v`); `Structure/Monoid.v` declares the sibling `Class MonoidObject` (`mempty`/`mappend`/…); and `Structure/Monoid.v` declares `Definition Monoid := @MonoidObject C CC_Monoidal`, an ALIAS at the CARTESIAN base. `Product_Monoid` takes the alias as its ARGUMENTS and concludes `MonoidObject (product_obj x y)` **with the monoidal instance suppressed in the display**. Measured precisely: `@Monoid` and `@MonoidObject` both print `∀ C : Category, Monoidal → obj[C] → Type` and are indistinguishable, but the alias prints `∀ C : Category, Cartesian → Terminal → obj[C] → Type` — **visibly different**, so a single `Check` separates it and the claim that display "cannot tell the three apart" was FALSE and is withdrawn. What genuinely bites is that `Product_Monoid`'s CONCLUSION does not reveal which tensor it is at. `Structure/Monoid.v` calls the two "equivalent up to field renaming", which is exactly right, and the passage built here (`Monoid_of_MonoidObject`/`MonoidObject_of_Monoid`, both round trips at `eq_refl` on the WHOLE record, both being primitive-projection records with eta) is trivial — **but trivial only AT A FIXED BASE, and that qualification is the whole design question**. **THE RECONCILIATION ROUTE WAS CHOSEN ON A MEASUREMENT, NOT ON TASTE, AND THE PAYOFF IS CONCRETE**: the product monoid is proved DIRECTLY on the `mu`/`eta` class over an ARBITRARY `Monoidal`, never routing through `Product_Monoid` — because `Product_Monoid`'s tensor IS the categorical product, so routing through it would force `Mon`'s monoidal structure to be `CC_Monoidal` and shrink the theorem from "every monoidal base with finite products" to "bases monoidal via their own products", narrower than both Mac Lane and `Mon`'s own signature. That is not hypothetical: the tree's own `Mon_Sets` is `@Mon Sets Sets_Product_Monoidal` (`Instance/Roster.v`), and **`Sets_Product_Monoidal` is NOT `CC_Monoidal Sets`** — pinned as a conversion negative — so the routed version would not have reached it, while the direct one does (`Mon_Sets_Cartesian`/`Mon_Sets_Terminal`). Two scope notes: the identification with Roster's `Mon_Sets` is by SOURCE READING, `Instance/Roster.v` being deliberately not required, so no `eq_refl` pins it; and the negative refutes CONVERTIBILITY, not isomorphism — a transport along a monoidal equivalence is simply not in tree. **NO CARTESIAN HYPOTHESIS IS NEEDED ON THE MONOIDAL SIDE**: `Mon_Cartesian` takes a general `Monoidal C` plus `Cartesian C`, the two binary operations (⨂ and ×) never interacting — the proof's naturality inputs are `Monoidal` FIELDS, though "only" would be inaccurate — it also spends `bimap_comp` and the two `bimap_id_*` lemmas (tensor functoriality), the component monoids' own laws, and the `Cartesian` fork calculus. `Mon_Forget`'s action on the product and terminal DATA is Leibniz-equal to `C`'s, recorded by six `eq_refl` Examples (product object, terminal object, `exl`, `exr`, `fork`, `one`) — but **"creates" in `Structure/Limit/Creation.v`'s sense is NOT claimed**, and cone-level preservation is not stated. Strengths measured strict-first: the unit agrees with `Product_Monoid`'s at `eq_refl` while the MULTIPLICATION is `≈` only, and the diagnosis is neither proof fields nor missing eta but genuinely different TERMS — `Product_Monoid` writes `split μX μY ∘ toggle` where this file writes the fork of two composites, the residue being one `fork_comp` then `fork_inv` then `exl_fork`/`exr_fork`, all `Qed` corollaries in `Structure/Cartesian.v` so no unfolding converts them. **Universes measured off BOTH block and binder**: every constant THAT BINDS A `C` is over `C : Category@{u u0 u0}` (the `Mon_Sets_*` pair binds none, being at a fixed base) — hom and proof identified — and that is attributed by probe rather than assumed, each of `@Monoidal C`, `@Cartesian C` and `@Terminal C` being independently rejected under `Constraint uh < up` while `x ~> y` and `id[x]` are accepted at those levels, so there are three donors and nothing here adds to it; the blocks carry only bounds, with `Monoid_Product` and the hom lemmas adding a strict `u0 < u1` and three `prod_rect` bounds that have **TWO SEPARATE CAUSES** (a first draft attributed both to the first): `fork_inv`'s Type-valued conjunction accounts for the `prod_rect` bounds ONLY, while the strict `u0 < u1` comes from setoid `rewrite` inside a `Program` obligation — isolated by rebuilding the same shape twice, `apply`-discharged obligations giving `@{u u0}` and `now rewrite` ones giving `@{u u0 u1}`, with the supplying `Morphisms` constant not identified; **the packaging is NOT free** — `Mon_Cartesian@{u u0 u1 u2}` concludes `Cartesian@{u2 u0}` with a NEW level `u2` for `obj[Mon C]`, bounded below by both of `C`'s levels and identified with neither (the bounds are measured, the sigma explanation an attribution) — and at `Sets` it costs nothing, `Mon_Sets_Cartesian@{u u0} : Cartesian@{u0 u}`, since `Sets`' own `u < u0` already satisfies both. No `Set` anywhere. **A PROBE-HYGIENE EPISODE IS RECORDED IN-FILE RATHER THAN QUIETLY FIXED**: the first version of the typing negative sat INSIDE the section, where `C` and `M` are section VARIABLES rather than parameters, so the application passed `C` into the object slot and the command failed with an ARITY error at an argument elaborated before the nominal subject — a false pass saying nothing about the monoidal instance. Moving it after `End` fixed it, and the negative now fires on `Product_Monoid X Y` itself. **SEVEN negatives now, THREE KINDS**: the original four (2 typing, 2 conversion), each stripped and its kind read, plus **three FORMABILITY negatives added after audit** — the universe measurement had been REPORTED in the header and pinned NOWHERE, so nothing in the build would have noticed an upstream annotation changing it; `@Monoidal C`, `@Cartesian C` and `@Terminal C` are now each rejected under a section-local `Constraint uh < up` against controls at those very levels. A missing control was added too: `@CC_Monoidal` applied to explicit arguments had occurred ONLY inside its own `Fail`, so a signature change would have turned that guard vacuously green with an `Illegal application` rather than a `cannot unify`. Rename-simulated over the constants the negatives name — **twelve, not thirteen**, `Monoidal` being named by no negative. No separate `Test/` probe file: the target carries all seven with controls beside them, section-local `Universes`/`Constraint` being measured not to leak. **40/40 constants closed under the global context** — `Print Module` lists all 40 including the 9 `Program` obligations, and the file declares no `Record`, so there is no unlisted `Build_*`. Read the GRADE: that is a ONE-TIME measurement; the `make print-assumptions` gate carries **18 of the 40** permanently, the ungated remainder being `Monoid_Product_mu_is_Product_Monoid`, four of the six `Mon_Forget_*`, `prod_fork_eta`, `mon_prod_mu`/`_eta`, the four `mon_prod_*_ex*` lemmas, `mon_prod_assoc_leg` and all nine obligations. Note also that a glob name sweep of this file returns 33 rather than 31, `Product.glob` recording the two `Fail`ed names as `def` entries although no such constants exist. NOT delivered: no `Cocartesian (Mon C)` — the coproduct of monoids is the free product, not carried by the coproduct of underlying objects, stated as a reason and not proved here; no exponentials or `Closed (Mon C)` (`Hom_Monoid` is a different statement and is not related); no indexed or infinite products, matching `Cartesian`'s binary shape; no monoidal structure ON `Mon C`; the class passage is an object-level bijection and is **NOT** claimed to be an equivalence of categories, there being no category of `MonoidObject`s in tree; nothing about groups, that being #343's (this file is the root of the chain #503 → #343 → #344); `Mon_Sets_Cartesian`/`_Terminal` are plain `Definition`s, not registered `Instance`s, and no other concrete base is instantiated; and no separation proof shows the extra generality non-vacuous by counterexample — the `Sets` fact above is the concrete substitute and is labelled as such
- **Theory/Algebra/Group/Hom.v**: Mac Lane §III.6 Exercises 1 and 2 + Awodey §4.1 — **group homomorphisms, the category `GrpCat` of group objects, and its finite products**, riding #503 rather than reproving it. **THE AWODEY CLAUSE IS A THEOREM AND THE CLASS HAS NO INVERSE FIELD**: `GroupHom Gx Gy f := MonoidHom (group_monoid Gx) (group_monoid Gy) f` is a plain `Definition`, so the identification with `MonoidHom` is DEFINITIONAL (pinned by `eq_refl` in the probe section), and `GroupHom_inverse : f ∘ ι_x ≈ ι_y ∘ f` is proved in ten tactic lines from the two monoid-hom laws alone — reshape `(f ∘ ι_x) ⨂ f` by `bimap_comp`, push `f` through the multiplication square, collapse by the SOURCE object's `left_inverse`, finish with the unit triangle — with uniqueness of left inverses in the TARGET supplied by `Structure/Group/Proofs.v`'s `left_inverse_unique`, which is where the diagonal's naturality and terminality of `I` are actually spent. **`right_inverse` appears nowhere in THAT tactic script, but the theorem DOES depend on it** — an audit found `left_inverse_unique`'s own proof does `rewrite <- right_inverse` (`Structure/Group/Proofs.v`), so what is asymmetric is the script, not the dependency; the header's list of what the donor spends is explicitly not exhaustive. (The direct in-file use of `right_inverse` is in `right_inverse_r`, not in `grp_prod_right_leg`, which uses `right_inverse_r`.) **HOW #503 IS CONSUMED, AT THREE MEASURED GRADES — and `Mon_Cartesian` itself is NOT among them, which is the honest part**: `Mon C`'s objects are `{x & Monoid x}` and `GrpCat`'s are `{x & GroupObject x}`, a DIFFERENT sigma, so there is no `Cartesian` transport between them and none is claimed; what instantiates are #503's factored COMPONENTS. DEFINITIONAL — `grp_prod_monoid` shows by `eq_refl` on the WHOLE RECORD that the product group's underlying monoid IS `Monoid_Product (group_monoid X) (group_monoid Y)`, riding #503's own `monoid_round`, with `grp_prod_mappend`/`_mempty` reading its structure maps back as `mon_prod_mu`/`mon_prod_eta`. CONVERTIBLE — `grp_exl_hom`, `grp_exr_hom`, `grp_fork_hom` and `grp_one_hom` are each a bare `exact` of `mon_exl_hom`/`mon_exr_hom`/`mon_fork_hom`/`mon_one_hom`, the `GroupHom` and `MonoidHom` goals being the SAME TYPE up to conversion, so **not one obligation about multiplication or unit occurs in the file**; `Terminal_Monoid` likewise. REBUILT — only the four-line `Build_GroupObject` assembly and the two inverse laws with their leg lemmas. **WHAT COULD NOT BE INSTANTIATED IS A FACT ABOUT THE DEFINITION, NOT A WEAKNESS**: #503 quantifies over an ARBITRARY `Monoidal`, this file cannot, `GroupObject` being declared over `CartesianMonoidal` because its inverse laws are written in copy/discard vocabulary (∆ and `eliminate`) — pinned as a TYPING negative firing on the `M` argument itself. The consolation is delivered: since `CartesianMonoidal_Cartesian` (Fox) and `SemicartesianMonoidal_Terminal` exist, `GrpCat_Cartesian_tensor`/`GrpCat_Terminal_unit` give the same two structures with **NO hypothesis at all**, with `grpcat_tensor_product`/`grpcat_unit_terminal` recording by `eq_refl` that the product is then the tensor and the terminal object `I`; the general forms are kept and are strictly more general, taking `Cartesian C` and `Terminal C` with no assumed compatibility with ⨂. Note `CartesianMonoidal_Cartesian` is a `Coercion` on a plain `Definition`, NOT an `Instance`, so the two coexist with no resolution ambiguity. **THE NAME IS `GrpCat`, NOT `Grp`, and there are FIVE collisions**: `Instance/Grp.v` declares `Grp`, `Grp_Forget`, `Grp_Terminal`, `Grp_Cartesian` and `Grp_Forget_Faithful` — and the cause of the first count saying four is worth recording, because the obvious explanation is wrong: it is NOT the `#[export] Program Instance` shape (`Grp_Cartesian` has exactly that shape and was found once attribute prefixes were allowed). The list was built from four GUESSED names rather than derived from this file's own declared names under the rename, so `GrpCat_Forget_Faithful` ↦ `Grp_Forget_Faithful` was never searched for at all. **TWO ENGINEERING FINDINGS, both about NAMES and both reproduced with their exact messages**: `inverse` is effectively a KEYWORD downstream of `Structure/Group.v`, whose notation quotes the token, so `@inverse _ _ _ G` is a **PARSE** error (`[global] or [pattern_ident] expected after '@'`) and `{| … ; inverse := _ |}` is rejected (`['|}'] expected after [record_declaration]`) — and **neither can be pinned as a `Fail` probe, because a parse error aborts the file even inside `Fail`**, which is why both are documented in prose; the workarounds are the qualified `@Category.Structure.Group.inverse` (a qualified path DOES parse after `@`) and `Build_GroupObject`, with `group_inverse_is` pinning agreement by `eq_refl`. And `fork`/`fork_respects` are SHADOWED — `Structure/Monoidal/Relevance.v` declares both and `Heunen_Vicary.v` re-exports Relevance — so `GrpCat_Cartesian` must write `Cartesian.fork` (the same workaround `Heunen_Vicary/Cartesian.v` uses); the `△` notation is unaffected, `Structure/Cartesian.v` declaring it against the resolved constant. **Twenty `eq_refl` Examples** (10 identifications, 6 that `GrpCat_Forget` carries product/terminal/`exl`/`exr`/fork/one to C's own, 2 the same for `GrpCat_Mon` into `Mon C`, 2 for the hypothesis-free instances). ONE fallback, and it is a REFUTATION: `Mon_Forget ◯ GrpCat_Mon = GrpCat_Forget` is rejected at `eq_refl` — `Compose` rebuilds `fmap_respects`/`fmap_id`/`fmap_comp` while BOTH data fields agree on the nose, which is exactly what the two controls pin. **An earlier draft justified delivering no `≈` form by saying it would pull a functor category into a `Theory/Algebra` closure; an audit refuted that** — `Functor_Setoid` and `Functor_StrictEq_Setoid` are both in `Theory/Functor.v`, already required by the header — so the equation IS delivered, at the STRICTER strength: `forget_compose_strict` proves it in `Functor_StrictEq_Setoid` with every object component literally `eq_refl`. The negative therefore refutes only the same-RECORD claim. **Universes: the BINDER and the BLOCK disagree, and that is the finding** — every constant binding a `C` is over `Category@{u u0 u0}` (hom and proof IDENTIFIED; 110 occurrences over the 55 named constants, 140 over all 70), while the constraint blocks contain **no `=` at all**, so reading the blocks alone would report no identification. There is exactly ONE other `Category@{…}` instance — `GrpCat`'s own result `Category@{u2 u0 u0}`, which identifies hom with proof as well — and the measurement trap that hid it is worth carrying: **`Print` wraps a universe instance across lines, so a line-based `grep -o` silently misses a split one**; flatten whitespace before counting. Attribution is probed with four donors — `@Monoidal C`, `@Cartesian C`, `@Terminal C`, `@CartesianMonoidal C` each rejected under `Constraint uh < up` while `x ~> y` and `id[x]` are accepted — each sufficient on its own but **NOT independent**: `CartesianMonoidal` contains `RelevanceMonoidal` contains `SymmetricMonoidal` and so `Monoidal`, so at most THREE are independent. None claimed unavoidable, all four being over unannotated `Context {C : Category}`. The cost over `Mon` is measured ENTRY FOR ENTRY on whole blocks: `Mon`'s has TEN entries and `GrpCat`'s TWELVE, the same ten renamed plus exactly `u <= u1` and `u0 <= u1` for the level `CartesianMonoidal` carries; seven of the ten are `sigT` bounds. No `Set` anywhere. **70/70 constants closed under the global context** — 55 from the `.glob` plus 15 `Program` obligations invisible to it, queried by fully qualified name; the file declares no `Record`/`Class`/`Inductive`, so there is no unlisted `Build_*`. Read the GRADE: that is a ONE-TIME measurement, and the `make print-assumptions` gate carries **25 of the 70** permanently. **SEVEN negatives, THREE KINDS** kept lexically apart (2 typing, 1 conversion, 4 formability), each stripped and confirmed to fire where claimed by character position, rename-simulated 10/10 — where the two same-file constants needed the definition-site method, a whole-file rename being a no-op by construction and giving a FALSE 'vacuous guard' verdict. NOT DELIVERED: no `Cocartesian GrpCat` (the coproduct of groups is a free product, not carried by the coproduct of underlying objects — #503 records the same for monoids); no abelian group objects and nothing about exponentials — but read that narrowly, since the `Hom_Monoid` analogue is NOT a library gap: `Structure/Group/Representable.v`'s `exp_GroupObject` already puts a group structure on the hom-object, in a file this one does not require; no indexed or infinite products, matching `Cartesian`; **no monoidal structure on `GrpCat`, so it is NOT claimed cartesian MONOIDAL and the Eckmann–Hilton consequence is neither stated nor proved** (that is #344's); `GrpCat_Mon` is proved **FULL as well as faithful** (`GrpCat_Mon_Full`, three lines — the two hom types are convertible, so the identity section works), superseding a draft that called fullness unaddressed; nothing is claimed about reflecting or creating limits; **no concrete instantiation in THIS FILE**, `Instance/Grp.v`'s `Grp_GroupObject`/`GroupObject_GrpObject` being neither used nor related to `GrpCat` (that reconciliation fixes `C := Sets`, `Instance/Grp.v`) — but an audit refuted a draft that went further and said no witness exists: `GrpCat Sets` IS inhabited, six lines away, via `Sets_CartesianMonoidal` (`Instance/Grp.v`) plus this file's own hypothesis-free `GrpCat_Terminal_unit`, or one object per group through `Grp_GroupObject`; no uniqueness or `Isomorphism` statement for the product group object beyond `ump_products`; and `prod_ext` is a general `Cartesian` fact that is **not new** — `Theory/Lawvere/Sets.v`'s `prod_separates` is the identical statement at identical generality (dual: `Construction/Cospan/Bridging.v`) — redeclared only because that file is not in this closure, and likewise not moved into `Structure/Cartesian.v`
- **Instance/Grp/EckmannHilton.v**: Mac Lane §III.6 Exercise 4 (book p. 76) — **groups in `Grp` are abelian groups**, both directions, over `Structure/Group.v`'s `GroupObject` at `CC_CartesianMonoidal` and the abstract engine of #285. **`GrpCat` is NOT used** — an audit caught an earlier draft of this bullet saying the work was built over it while the file explicitly declines to require `Theory/Algebra/Group/Hom.v` (measured: that would add nine modules to a 40-module closure); `GrpCat` is named only to correct the issue's stale prior-art claim. **THE ISSUE'S OWN "Current state" SECTION IS STALE AND IS CORRECTED RATHER THAN REPEATED**: it says "no category Grp and no category of group objects exists", but `Grp` is `Instance/Grp.v` (#255) and `GrpCat` landed with #343. **THE CRUX IS ONE TACTIC-FREE LINE**: the group-object multiplication `mappend : G ⨂ G ~> G` of a group object in `Grp` IS a `GrpHom`, so its `grp_map_mul` field applied at the pairs `(a,c)` and `(b,d)` IS `Theory/EckmannHilton.v`'s `interchange` verbatim — `gob_interchange` is `exact (grp_map_mul mappend (a,c) (b,d))`, with the middle-two variable swap absorbed entirely into WHICH PAIRS are fed in rather than requiring any reassociation. `eh_ops` then collapses the two multiplications (`gob_mul_agrees`) and `eh_g_comm` gives commutativity (`group_object_abelian`). **The unit side is cheaper than the interchange side and the reason is a transparency fact worth knowing**: `gmul2_unit_left`/`_right` are literally `@mempty_left … (ttt, a)` and `@mempty_right … (a, ttt)` because `CC_Monoidal`'s unitors are the TRANSPARENT cartesian isos `prod_one_l`/`prod_one_r` — only its six naturality and two coherence fields become opaque `Program` obligations, so a reader scanning that file meets the opaque ones first and would guess wrong. **`ginv2 ≈ grp_inv` is proved separately (`gob_inv_agrees`) and is NOT an Eckmann–Hilton consequence** — EH says nothing about inversion; it comes from `left_inverse` plus uniqueness of inverses. **UNIQUENESS IS WHAT "ARISES THIS WAY" MEANS**, and a bare biconditional does not carry it: `group_object_mul_unique`/`_unit_unique`/`_inverse_unique` plus `group_object_structure_forced` show the group-object structure is FORCED, and `abelian_iff_group_object` / `maclane_III_6_ex4` package the two halves. **The ↔ is a biconditional and NOT a type isomorphism** — the round trip routes through `eh_g_comm` and is a different term, and there is no setoid on `AbelianGrp G` under which to state agreement; the uniqueness triple is what replaces it. Strengths measured strict-first: `abelian_roundtrip_mul`/`_unit`/`_inv` all close by **`eq_refl`** (the half-(a) structure has `grp_mul`/`grp_unit`/`grp_inv` literally inside and the pair projections reduce by iota), as do `grp_tensor_is_product` (the tensor IS `Grp_product`), `grp_tensor_unit_is_trivial` (the unit IS `Grp_trivial`) and all four `Z2` computations; the REVERSE direction reaches only `≈`, and **the diagnosis is not rebuilt law fields** — `gmul2 G GO` projects a VARIABLE `GO`, so nothing reduces on that side at all and no downstream transparency would help. **NON-VACUITY IS PROVED IN BOTH DIRECTIONS**: `Z2_GroupObject` with `Z2_GroupObject_nondegenerate` and four computing `eq_refl` Examples, `trivial_GroupObject_degenerate` proving the degenerate case degenerate, and — the sharper half — **`Sym3_no_group_object`**, so the theorem EXCLUDES something, the nonabelian `S3` carrying no group-object structure at all. **THE `Set` PIN IS INHERITED, LOCATED BY PROBE, AND NOT REPAIRED**: `GrpS : Category@{gu Set Set}` with block `Set < gu` — `Grp`'s second binder fills both the hom and proof slots, and that identification is the whole of it. Under `Constraint Set < ph` the category `Grp@{pu ph}` elaborates AND `Grp_Cartesian` is accepted, while **`Grp_Terminal` is rejected with "Cannot enforce Set = ph"** and `CC_CartesianMonoidal`'s rejection is reported AT `Grp_Terminal` — root `Grp_trivial@{u} : GrpObject@{u Set u}` (`Instance/Grp.v`) reached through `Grp_Terminal`, the same donor defect `Instance/Grp/Quotient/Colimit.v` already records. **CORRECTION (#450): THE DONOR DEFECT IS REPAIRED** (the readback just quoted predates the role permutation of the PR "algebraic carriers are sets": at #450's parent `About` read `Grp_trivial@{u} : GrpObject@{u u Set}`, the pin the same) — #450 wrote out the universes of `Instance/Grp.v`'s zero-object constants in place, `About` now reading `Grp_trivial@{p} : GrpObject@{p p p}` and `Grp_Terminal@{u p} : Terminal@{u p}`, so `Grp_Terminal` and the cartesian monoidal structure are ACCEPTED at `Grp@{pu ph}`; this file's statements are still at `GrpS = Grp@{gu Set}`, now a choice rather than a necessity. **NO constraint block anywhere contains a universe EQUATION** — every entry is `<` or `<=`. **58/58 constants closed under the global context**, of which the gate carries 21; the file declares no `Record`/`Class`/`Inductive`, so there is no unlisted `Build_*`, and its `.glob` shows 65 because it records the seven `Fail`ed probe names as `def` entries (since #450, 63 and five: the `def` plus `prf` entries of the `.glob`, and the guarded commands 7 → 5 by `grep -c '^Fail'`). **SIX negatives of THREE KINDS** (since #450 FOUR of TWO kinds, the two formability negatives having become `Check` controls when the donor pin was lifted) against five positive controls (seven since #450: the five `ctrl_` constants and those two `Check`s) plus an instrument check, each stripped and its kind read off: two FORMABILITY (both firing at `Grp_Terminal`), one TYPING (on `GrpMon`, `Monoidal` vs `CartesianMonoidal`), three CONVERSION — the sixth added after an audit found the header claiming the second round trip was "pinned" when no negative mentioned it at all; rename-simulation 16/16 — a process claim recorded nowhere in the file, though an audit independently confirmed the underlying property (every constant a negative names is also named by a positive command) — which FOUND a real gap — `Terminal`/`Grp_Terminal` appeared in no control until `ctrl_probe_terminal` was added, and it doubles as the sharp contrast, the same constant accepted at `Set` and rejected one line above (until #450; since then accepted at both). **THREE NAME COLLISIONS WERE FOUND AND RENAMED AWAY**, and the reason is mechanical rather than aesthetic: a first draft called the predicate plainly `Abelian`, which collides with `Structure/Abelian.v`'s `Class Abelian` — a different notion, but one that IS required into the `print-assumptions` gate's single scope, where a bare `Print Assumptions Abelian.` would resolve to whichever was imported last; `grp_ab_inverse` and `eh_probe_instrument` likewise collided with `Instance/Ab/Character/Finite.v` and `Test/ProbePolynomial.v`. All three were found only by sweeping the file's OWN declared names with attribute prefixes allowed. **PROSE→THEOREM PROVENANCE**: `Structure/Group.v` already asserted this result in prose, and there are THREE such sites (`Structure/Group.v`, and twice in `Theory/EckmannHilton.v`) — with a disclosed pre-existing defect, `Theory/EckmannHilton.v`'s cross-reference to `Structure/Group.v` being off by one, recorded and NOT edited. This is `Theory/EckmannHilton.v`'s **fourth** consumer by `Require` after `Structure/Semiadditive.v`, `Instance/Top/LoopSpace.v` and `Theory/Centre.v` — a bare name grep suggests more because three further files mention it only in prose. NOT DELIVERED: no `GrpCat`-level statement (it would add nine modules to the closure for no statement made); no bridge to `Instance/Ab.v`'s `AbObject` (a different record, not in the closure, and the only in-tree passage — `Instance/Grp/Abelianization.v`'s `Ab_to_GrpOb` — runs the other way); no morphism-level or functorial statement and hence no equivalence of categories; no monoid analogue; no link to `Structure/Group/Representable.v` or to `Instance/Fun/Group.v`, which is the SIBLING exercise (§III.6 Ex 3, group objects in a functor category); the `Set` pin located but not repaired; and no second nonabelian witness
- **Theory/Density.v**: Mac Lane §III.7 Theorem 1 + the dual remark (book pp. 76-77; Awodey §8.6 Prop 8.10) — **every set-valued functor is canonically a colimit of representables**, delivered at CONE level: **`density : IsColimitCocone DensityCocone`** in `[D, Sets]`, over `DensityDiagram := Curried_Hom D ◯ (Elements_proj K)^op : (Elements K)^op ⟶ [D, Sets]`, with the mediator built by evaluation at the identity and its uniqueness; the apex-only `density_IsAColimit` is derived and is the WEAKER reading, while the bundled `DensityColimit` is a REPACKAGING AT THE SAME STRENGTH (`Class Limit` carries both the cocone and the cone-level UMP, so it loses nothing) — an audit corrected a draft that labelled both weaker, with `density_elements_covered` and `density_jointly_epic` (K is generated by representables) alongside. **THE ROUTE DECISION THE ISSUE DEMANDS IS MADE AND THE COEND BRIDGE IS BLOCKED, NOT MERELY DEARER** — three measured steps: (i) `Colimit` has 0 hits in each of `Structure/Coend.v`, `Structure/End.v`, `Instance/Sets/Coend.v`, and a coend here is a cowedge over an integrand `C^op ∏ C ⟶ Sets` whose index is the PRODUCT category while the colimit is over `(Elements K)^op`, so relating them is a construction and not a repackaging; (ii) even with a bridge the transport runs the wrong way, `Theory/Coend/Yoneda.v` opening its Section with `Context (c : C)` so `coyoneda_reduction` is pointwise and work item 3 would have to precede item 1; (iii) that naturality needs `c ↦ coend_obj (SetsCoend (YoI c))` to be a FUNCTOR, and `Structure/Coend.v` declares no such action, mentioning `Functor` only in its three `Require` lines. **An earlier draft concluded from that that the tree supplies no such action, and an audit refuted it**: `Construction/Day.v` already builds one for a different integrand family — `Day_map` and `Day : C ⟶ Sets` whose `fobj` is literally `fun c => coend_obj (SetsCoend (DayI F G c))`, plus `DFG_c`, the action on a map of integrands, and the integrand-AGNOSTIC `day_theta_leg_cowedge`. Transcribing that to `YoI` is about sixty lines. **So the honest word is DEARER, not BLOCKED** — steps (i) and (ii) stand, the direct route is still right on cost, and the bad evidence is named as bad: a name grep on the abstract class file rather than a survey of its CONSUMERS. The direct route costs nothing extra: the diagram is a plain `Definition` with no obligation, so all three functor laws are inherited. **THE PRESHEAF DUAL IS GENUINELY ZERO-CONTENT, AND FOR A CHECKABLE REASON**: `PElements P` is DEFINED in `Construction/Elements.v` as `(Elements P)^op`, which IS the general index — `presheaf_index` records it by `eq_refl` — so `PDensityDiagram`/`PDensityCocone`/`presheaf_density` are the general constants at `D := C^op` with no tactic, no transport and no coercion, and that file's disclosed `PElements P ≅[Cat] (=(1) ↓ P)^op` gap never arises, the dual never touching the comma presentation. **WHAT THE COEND CONNECTION DOES DELIVER**: `coy_leg_is_density_inj` shows by `eq_refl` that the coend leg's VALUE at `(g,a)` is the density injection's component at `c` applied to `g` — read that precisely, since a draft over-read it as "the same morphism on the nose" and the two have different domains: the coend leg is the UNCURRIED family of injections, `coy_leg_natural_in_apex` is the naturality-in-`c` upgrade OF THE LEGS (the part stateable without coend functoriality), `coy_leg_natural_witness` identifies that transformation as `yo_inj`, and `coy_from_at`/`coy_to_from_is_covering` show the coend's inverse is "insert at the identity" — so the two constructions share both their legs and their inverse formula, all at `eq_refl`. **`Construction/Elements/Kan.v` IS NOT THE VARIANCE-DUAL OF THIS, WHICH CORRECTS A GUESS OF MINE**: Kan.v relates `K ⟹ [Hom a,─]` (maps INTO a representable) to cones over `Elements_proj K`, a diagram in **D**; density is about `[Hom d,─] ⟹ K` (maps OUT of one) and a cocone over a diagram in **[D, Sets]** — different diagrams in different categories, neither the formal dual of the other, and `KanNat`/`KanCone`/`KanDelta`/`kan_coyoneda` are used NOWHERE here. What transferred is ONE constant, `elements_same`, used once in `den_med_respects` for the reason Kan.v records for `kan_leg_respects`: objects of `Elements K` are compared by LEIBNIZ equality, so two `≈`-equal elements over one object are DIFFERENT objects with a priori unrelated legs, and respectfulness of the mediator is DERIVED from cocone coherence rather than assumed. **A PRIOR-ART CORRECTION OF MINE, NAMED AS BAD EVIDENCE**: I claimed no `IsColimitCocone`/`IsAColimit` existed beyond preservation results and prose; that is FALSE — fourteen files carry them, `Structure/Limit/Initial.v`'s `terminal_IsColimitCocone` is a real theorem, and there are concrete instantiations at `Instance/Top/Forgetful.v` (`bool_cocone_colimit`, ambient `Sets`) and `Instance/Ab/DirectedColimit.v` (`ab_fg_colimit`, ambient `Ab`). What survives is the sharper claim: no `IsColimitCocone` in the tree has a FUNCTOR CATEGORY as its ambient — a statement about those fourteen files, which happens to be exhaustive for STATED results since the two tokens occur in exactly fifteen files counting this one. Strengths strict-first: **eighteen** delivered `eq_refl` Examples (21 occurrences of `:= eq_refl`, less the two inside `Fail`s and one probe control — an audit recounted a draft that said thirteen), including `density_med_is_den_med` (the mediator EXTRACTED from the theorem IS the named one, so the theorem is not `Qed`-opaque where it matters); **exactly ONE strict→`≈` fallback**, `density_med_commutes`, refuted at `eq_refl` and pinned, the cause being that the mediator evaluates the competing cocone at `(e, fmap[K] g x)` while the other side evaluates at `j`, reconciled only by cocone coherence at `Elements_lift`. Universes measured off BOTH binder and block: every general constant is over `Category@{u u0 u0}` (hom and proof identified, the donors' doing), and **the `u <= u0` pin enters at the DIAGRAM, not at the index or at `yo_inj`** — guarded, not merely read off, by a formability negative under `Constraint uh < uo` where `ElementsOp` and `yo_inj` are formable and `DensityDiagram` is rejected. The witness section is `Set`-FREE (measured; `bool : Set` contributes only the bound `Set <= u`, and the carrier uses `Lib/Setoid.v`'s polymorphic `eq_Setoid` rather than instance resolution). **62/62 constants closed under the global context**; the `.glob` shows 64 because two of the five `Fail`s are NAMING commands (`Fail Example`) while three are `Fail Check` and declare nothing. **FOUR negatives of THREE KINDS** (2 conversion, 1 typing on the variance — `Curried_Hom D ◯ Elements_proj K` is rejected with `cannot unify D and D^op` — and 1 universe-formability) plus an instrument check, each stripped and its kind read off. **The rename guard needed a SECOND FILE and the delegate said so rather than reporting a clean score**: in-file controls are renamed in lockstep with the constants they guard, so the six file-local names could not be covered from inside; `Test/ProbeDensity346.v` restates the boundary from outside and is rename-simulated **6/6**, each rename leaving `Theory/Density.v` itself compiling rc=0 — which is exactly the failure mode an in-file probe cannot catch. Non-vacuity over `1` with `K` constant at a two-element setoid: the index has two distinct objects with distinct injections, the diagram is constant at one representable with no morphisms between the elements — **`kbool_no_hom_et_ef`, added after an audit found that load-bearing claim asserted in prose and pinned nowhere** — so the theorem there says 2 = 1 + 1, the apex is PROVED not representable (`kbool_not_representable`), and the mediator COMPUTES (`transform[density_med KBool NegCocone] ttt true = false` by `eq_refl`, with `neg_med_not_id` showing it is not the identity). Two engineering findings recorded in-file: `Category.Functor.Opposite` opens `functor_scope`, so a bare ascription `(ttt : _1^op)` parses as an opposite FUNCTOR and fails naming neither culprit (same family as `Instance/Rng/Mod.v`'s notation guard); and a `Transform` is `hom` of a functor category only by unfolding, so `f ∘ g` leaves `∘`'s category an unresolved metavariable unless the definition is typed `_ ~{[D, Sets]}~> _`. NOT DELIVERED: no coend-is-a-colimit identification, hence `coyoneda_reduction` is NOT upgraded to a natural isomorphism; no pointwise-colimit lemma for functor categories, so nothing says this colimit is computed pointwise — that is the step that would make density at `c` literally the co-Yoneda reduction; no `Dense` predicate and no left Kan extension along `Curried_Hom`, so no link to `Theory/Kan/Extension.v` or `Structure/Limit/Kan/Pointwise.v`; no functoriality or naturality in `K`; no uniqueness of the diagram or cocone; nothing about `Elements_proj` being a discrete opfibration and no Grothendieck comparison; no "a colimit-preserving functor out of `[D,Sets]` is determined on representables"; nothing in `StrictCat`; and no separation proved between the cone-level statement and its apex-only reading
- **Theory/Connected/Components.v**: Mac Lane §IV.2 + Riehl §1.5 — **π₀ of a category, the connected components as full subcategories, and the action of a functor on zig-zags**. **THE ISSUE'S "Current state" IS FALSE, AND MEASURABLY SO**: it reports that a whole-tree search for the identifier `Connected` returns no hits and that "there is no π₀, no zigzag relation on objects", and asks for the relation plus its reflexivity, symmetry and transitivity to be built from scratch — whereas `Structure/Groupoid/Connected.v` already declares `ZigZag` (constructors `zz_nil`/`zz_fwd`/`zz_bwd`), `Connected C := ∀ x y : C, ZigZag x y`, `hom_zigzag`, `arrow_connected`, `zigzag_trans` and `zigzag_sym`, together with `Fence`, `Roof_Connected` and `Two_Discrete_not_connected`. Measured by name: `ZigZag` occurs in exactly ONE `.v` file, the one declaring it; `Connected` in FIVE, of which one declares it, three consume it (`Structure/Groupoid/Basepoint.v`, `Construction/Deloop/Transform.v`, `Instance/Top/FundamentalGroupoid.v`, which adds `PathConnected`) and one matches only because the file PATH appears in a comment. So the issue's item 1, and the reflexivity/symmetry/transitivity HALF of its item 2, were discharged before this file existed, and re-declaring them would have produced a SECOND, incompatible zig-zag type — while item 2's OTHER half, "and that it is preserved by every functor", was NOT discharged and is exactly what `zigzag_fmap` supplies (an audit caught the header and this bullet both claiming the whole of item 2 was already done, contradicting the very next paragraph) — the recurring shape being that the issue checked the neighbouring `Construction/Groupoid.v` (correctly: prose only) and missed the file next to it. **NOTHING IS REDECLARED; the donor is consumed.** What was genuinely absent, and is what this file supplies: (a) **no action of a functor on zig-zags** — `zigzag_fmap`, whose recursion consumes NO functor law, only `fmap`, so it holds for an ARBITRARY functor with no fullness, faithfulness or surjectivity hypothesis, with `zigzag_fmap_id`/`_compose`/`_trans`/`_sym`; (b) **no π₀ of a category** — `pi0 C := SetsQuotient (ObjSetoid C) (@ZigZag C) (zigzag_Equivalence C)` over `Instance/Sets/Quotient.v`, applied DIRECTLY since `ZigZag` is already a `crelation obj[C]`, with the carrier UNTOUCHED and only `≈` coarsened (`pi0_carrier`, `pi0_equiv` by `eq_refl`), the projection `pi0_proj`, the functorial action `pi0_fmap`, and **`Pi0 : Cat ⟶ Sets`**; (c) **no subcategory spanned by a zig-zag class** — `ComponentSub`/`ConnectedComponent` over `Construction/Subcategory.v`, with `Component_Incl` proved Full and Faithful, `Component_Connected` (every component IS connected) and `Component_reindex_equiv` relating the components at two joined representatives. The nearest prior art is disclosed rather than implied away: `Instance/FinSet/Pushout.v`'s `components` is a union-find fold over a LIST OF EDGES on `Fin.t N` whose input is not a category and whose output is an idempotent labelling, and it decides membership through `fin_eqb`, which nothing here can do. **THE INHABITEDNESS DISCREPANCY IS PROVED, NOT ASSERTED**: the in-tree `Connected` has NO inhabitedness clause where the issue asks for "inhabited AND any two joined", and the two differ EXACTLY at the empty category — `Zero_Connected : Connected _0` holds vacuously while `Zero_not_ConnectedNonempty` refutes the bundled record, packaged as `connected_readings_differ`, with `connected_nonempty_iff_pi0_singleton` on the other side. Consequences: `terminal_connected`/`initial_connected` (and their object-level forms), `One_Connected`, `DiscreteCat_connected_iff` (a discrete category is connected exactly when its objects are all equal) and `groupoid_connected_iff_arrow`. **THE UNIVERSE BINDERS ARE LOAD-BEARING IN THREE PLACES, EACH MEASURED WITH A DISCRIMINATING EXPERIMENT — same body, binders removed, nothing else changed — AND EACH NOW GUARDED**: written unannotated, `zigzag_fmap` IDENTIFIES the two categories' hom universes on an EMPTY constraint block (so reading the block alone reports no identification — the binder-hiding trap), and is then rejected at categories whose homs are declared strictly apart with "Cannot enforce uhd = uhc", where the shipped form carries `hc <= hd` and is accepted; `pi0_proj` unannotated elaborates at `Category@{u u u}` — ALL THREE identified — and is rejected at a category whose homs sit strictly below its objects with "Cannot enforce vh = vo", where the shipped form leaves only `h <= o` and `pi0_proj Sets` is formable (`Sets@{o so}` IS `Category@{so o o}`); and `ObjSetoid` takes FOUR binders so the setoid's relation universe is not C's proof universe, the three-binder variant making `pi0_coarser` carry `o = h`. The SAME cause forces an IDENTIFICATION that hides in a binder, and the file reports it against itself: `pi0_fmap`'s binder writes `{C : Category@{o hc hc}} {D : Category@{o hd hd}}` with `o` TWICE, so the two categories' OBJECT universes are identified while its constraint block carries no equation at all — and it is FORCED rather than a minimization accident, measured by restating the same body with the object universes declared apart, which puts `oc = od` INTO the block. `Pi0` inherits it. What does NOT go away is `h <= o`, which is `Sets`' own object type (`obj[Sets@{o so}]` is `SetoidObject@{o o}`) — that one a BOUND, not an identification — and `pi0` itself, which never mentions `Sets`, restricts C not at all. Two inherited identifications are named rather than repaired and neither is claimed unavoidable: `ZigZag@{u u0}` is declared over `Category@{u u0 u0}`, and `eso_connected` keeps `hc = hd` even annotated, which is `EssentiallySurjective`'s doing. **RIEHL §3.4's DERIVED NOTION IS DELIVERED TOO, and it came from an APPENDED CHECKBOX the issue's own Work-items list never mentions** — found only by grepping the FULL body, and it is the form Riehl's Propositions 3.4.8 and 3.6.5 quantify over: `ConnectedDiagram D := Connected J` and `ConnectedNonemptyDiagram`, each recorded by `eq_refl` as the indexing category's own predicate, with `connected_diagram_of_nonempty`, `connected_diagram_pi0`/`pi0_connected_diagram` (the biconditional read at the index) and witnesses `Parallel_ConnectedDiagram`/`bool_diagram_not_connected` by pure instantiation. **The unqualified name is NOT Riehl's reading, and the file says so**: her definition is the INHABITED one ("non-empty and any two objects joined"), so the notion the section is named after is `ConnectedNonemptyDiagram`, while `ConnectedDiagram` is the bare in-tree reading that holds vacuously at the empty shape — nomenclature inherited from `Structure/Groupoid/Connected.v`, disclosed rather than repaired. It is a property of the SHAPE alone, and **`connected_diagram_reindex` is the identity function** — two diagrams on one shape are connected together or not at all, which is the honest content of the remark rather than a theorem with a proof. NOT delivered with it: **no connected LIMIT**, so neither of Riehl's propositions is stated — what is supplied is the hypothesis they quantify over. **105 constants closed under the global context** — 90 named, 12 `Program` obligations, the two projections `cn_obj`/`cn_zigzag`, and `Build_ConnectedNonempty`. Read the GRADE: that is a ONE-TIME measurement of all 105, and the `make print-assumptions` gate carries **50 of them** permanently. `Print Module` lists 104, omitting only the constructor, and WRAPS the `Record` line, so a line-anchored sweep of its output reports 103 and silently misses `ConnectedNonempty`. `Test/ProbeConnected352.v` converts every measured rejection into a pinned negative — **5 negatives of THREE KINDS that are genuinely distinguishable by the error TEXT rather than by label**: two CONVERSION (`zigzag_fmap Id[C] s = s` and the composition law, both at a VARIABLE chain, each ending `cannot unify`, with the SAME statements at `zz_nil` as passing controls, which is what locates the failure at the stuck recursion rather than at `Id` or `◯`), one TYPING (`proper_morphism := @zigzag_fmap C D F` as a record-literal field, reporting a plain type mismatch with NO `cannot unify` and NO universe clause, against a control supplying THE SAME TERM by a one-step script and succeeding — so the rejection is about unification through `Proper`/`respectful`, not about the term), and two FORMABILITY (the two unannotated clones above, each ending `universe inconsistency: Cannot enforce …`, each against the shipped annotated form accepted at the very same levels) — plus an instrument check, each negative stripped and its whole error read, and rename-simulated END TO END: **12 target constants renamed one at a time, 12/12 broke the probe at a NON-`Fail` line, zero vacuous guards** (`ConnectedNonempty` is inconclusive by that method, renaming it breaking the target's own projections, and is `Check`-guarded instead) — a figure that replaces an earlier "29/29", which an audit correctly called unreconstructable because it counted distinct TOKENS inside the `Fail`s, bound variables included, rather than constants. The textual pass that produced it did earn its keep once: it found `proper_morphism` named ONLY inside its own `Fail`, and a control was added for it. NOT delivered: no decision procedure (`ZigZag` is proof-relevant and every negative goes through an invariant of the ambient category, never through an analysis of chains); no normal form for zig-zags; **no disjoint-union theorem** — every object lies in its own component and every component is connected, but C is NOT exhibited as a coproduct of its components, which would need them indexed by `carrier (pi0 C)`, a family of categories over a setoid; no π₀ of a groupoid as isomorphism classes and no comparison with `Theory/Skeleton.v`; **no left adjoint** — `Pi0` is built but the discrete functor `Sets ⟶ Cat` and π₀ ⊣ discrete are not; no connected limits; **no reflection** — only the forward direction, and the essential-surjectivity hypothesis of `eso_connected` is not merely convenient, `image_connected_not_connected` witnessing that dropping it makes the statement FALSE, while whether some weaker hypothesis suffices is neither proved nor refuted; no `StrictCat` statement for `Component_reindex_equiv`; no naturality of `pi0_proj` (`ObjSetoid` is not exhibited as a functor, so the projection is not a natural transformation to `Pi0`); and no `Fence` analogue, everything being stated with `ZigZag`
- **Adjunction/Diagonal/Limit.v**: Mac Lane §IV.2's table of adjoints of the diagonal (book p. 87) + §V.2 Ex 3 + §X.1 + Awodey §9.3 (Example 9.8 and the limit/colimit constructions) + Awodey §9.9 Ex 3 + Riehl §4.6 Prop 4.6.1 and exercises i–ii — **the limit and colimit FUNCTORS `[J, C] ⟶ C` and the sandwich `Colim[J] ⊣ Δ[J] ⊣ Lim[J]`**. **THE CENTRAL ABSENCE WAS CONFIRMED BY SHAPE, NOT BY NAME, AND THE DIFFERENCE IS INSTRUCTIVE**: a NAME sweep for `Lim`/`Colim` returns hundreds of constants and would suggest the feature existed, while a TYPE sweep finds only **ELEVEN** constants tree-wide typed `[X, Y] ⟶ _` — `YoEvalAt`, `Postcompose`, `PostcomposeViaHcompose`, `SubPostcompose`, `Fun_Proset_forward`, `Endo_Monotone_forward`, `MSet_to` (`Instance/Fun/Action.v`), `Fun_Discrete_to` (`Instance/Fun/Discrete.v`), `Rep_to` (`Instance/Rep.v`), `One_Eval` (`Theory/Shapes.v`) and `Arrow_of_Fun` (`Theory/Shapes.v`) — and NONE is a limit functor. **An earlier revision of this bullet said SIX and named only the first six; an audit corrected it.** The cause is worth carrying: a per-LINE regex over declaration heads misses a type that wraps, and it is not even a consistent under-approximation — two of the five it missed (`One_Eval`, `Arrow_of_Fun`) are single-line. The CONCLUSION is unaffected, none of the five extras being a limit functor, but the enumeration was wrong. Before this file the only genuine row was the binary-product one (`Adjunction/Diagonal/Product.v`), and the general rows existed only as `RightKan`/`LeftKan` CLASS FIELDS (`ran_adjoint` at `Theory/Kan/Extension.v` and the `LeftKan` twin — fields, not standalone constants, which is why a declaration regex reports them missing and which is exactly the issue's point that they hold only when a Kan instance exists). Delivered: `LimitFunctor`/`ColimitFunctor` over a PER-SHAPE hypothesis `HasLimitsOfShape J C := ∀ F : J ⟶ C, Limit F` rather than `Complete` (the adjunction is about one fixed `J`; `Complete_HasLimitsOfShape`/`Cocomplete_HasColimitsOfShape` instantiate the global classes in one line each, so nothing is lost), all three functor laws from uniqueness of the mediator; **`Diagonal_Limit_Adjunction : Δ[J] ⊣ LimitFunctor`** and **`Colimit_Diagonal_Adjunction : ColimitFunctor ⊣ Δ[J]`** in hom-set form, routed through `Structure/Cone/Const.v`'s `Cone_Natural_Transform` (`Defined`, so it computes) composed with `ump_limits` — the hom-set identification the proposition rests on, consumed rather than re-derived; **both Riehl 4.6.1 biconditionals as genuine `iffT`s in both directions** (`limits_iff_diagonal_right_adjoint`, `colimits_iff_diagonal_left_adjoint`, with the converses `Diagonal_right_adjoint_HasLimits`/`Diagonal_left_adjoint_HasColimits`); Riehl §4.6 ex ii's unit/counit identification at BOTH grades — `lim_counit_is_limit_leg`/`colim_unit_is_colimit_inj` at `≈` and `lim_counit_component_strict`/`colim_unit_component_strict` at `eq_refl`; Mac Lane §V.2 Ex 3 as `Lim_map`/`Colim_map` with `Lim_map_commutes`/`Lim_map_unique` and the colimit twins as separately named lemmas; and the degenerate rows (Awodey §9.3 Example 9.8, Riehl §4.6 ex i) as `Erase_right_adjoint_iff_Terminal`/`Erase_left_adjoint_iff_Initial` with `Erase_right_adjoint_value`/`Terminal_Erase_value` and the initial twins pinning "the adjoint's value at `ttt` IS the object" at `eq_refl`, plus `HasLimitsOfShape_0_iff_Terminal` and `Terminal_iff_Diagonal_0_right_adjoint` connecting the degenerate row to the general theorem through `Terminal_Limit`. **THE COLIMIT HALF IS COVARIANT AND THAT IS MEASURED**: only FOUR lines in the whole file mention `^op` (inside `cocone_apex` and `Cocone_Natural_Transform`), and the covariance claim must be read exactly — `Colimit_Diagonal_Adjunction`, `ColimitFunctor`, `Colim_map`, `colim_unit`, `colimits_iff_diagonal_left_adjoint`, `colim_unit_is_colimit_inj` and `cocone_apex` carry no `^op` in their types, but **`Cocone_Natural_Transform` DOES**, its right-hand side being an `ACone` over the opposites, so the blanket "none in a consumer-facing type" an earlier revision claimed is FALSE and was caught by audit — bought by consuming `Structure/Limit/Preservation.v`'s existing covariant accessor layer (`cocone_inj`, `colimit_inj`, `colimit_med`, `ump_colimit`, `colimit_apex`, …), which an earlier donor list of mine omitted and which is the single largest saving in the file. `Cocone_Natural_Transform` is the dual of the donor and is NEW (measured: `Structure/Cone/Const.v` declares exactly two results and no file in the tree carries a cocone counterpart). **TWENTY-FIVE `eq_refl` Examples**, among them the finding that `Cone_Natural_Transform`'s bijection is DEFINITIONAL on components in BOTH directions. Non-vacuity is concrete and non-degenerate: `Sets_Diagonal_Limit_Adjunction`/`Sets_Colimit_Diagonal_Adjunction` inhabit the sandwich at every shape over `Sets_Complete`/`Sets_Cocomplete` — **the issue's own claim that no category in the tree is shown complete or cocomplete is STALE, both exist** (`Instance/Sets/Complete.v`, `Instance/Sets/Cocomplete.v`) — with `lim_one_iso` general in `C` and `sets_bool_lim_two_elements` exhibiting two provably `≈`-distinct elements of a limit apex. Universes measured off BOTH binder and block, and **the trap fires in BOTH directions in this one file**: `HasLimitsOfShape` has a block of BOUNDS ONLY with no equation while its BINDER reuses one level for J's hom, J's proof, C's hom AND C's proof; whereas `LimitFunctor`, both adjunctions, `Lim_map` and five others have binders that look free while the BLOCK carries `u0 = u2`. Both headline biconditionals additionally collapse J's OBJECT universe, and that enters at the PACKAGING — both directions were measured separately and keep it free — with `iffT`-vs-`sigT` NOT separated and not claimed unavoidable. **A `Set` PIN WAS FOUND AND FIXED BY A DISCRIMINATING EXPERIMENT**: `Terminal_Erase_Adjunction` first elaborated at `C : Category@{u3 Set Set}`; annotating `one_arrow_eq` did NOT lift it, which RULES THAT CAUSE OUT, and the identical proof text compiles inside a section declaring `Constraint Set < ch`, so it was minimization at an unannotated `Context {C : Category}` — section-level `Universes` lift it and the `Erase` results now carry no `Set`. What remains pinned is `HasLimitsOfShape_0_iff_Terminal` and `Terminal_iff_Diagonal_0_right_adjoint` at `Category@{u7 Set Set}`, INHERITED from `Instance/Zero.v`'s own unannotated `_0 : Category@{u Set Set}` (its hom is `Empty_set : Set`) — not repaired, not claimed unavoidable. **177/177 constants closed under the global context** (117 non-obligation + 60 `Program` obligations, which is exactly the gap a `.glob` sweep cannot see), counted by me on the WHITESPACE-FLATTENED `Print Module` output so a wrapped line cannot be missed. Since #450 the section `DiagonalContinuous` adds eight more — `diag_eval_cone`, `diag_med`, `diag_med_commutes`, `diag_med_unique`, `Diagonal_continuous` and three `Program` obligations — so `Print Module` enumerates 185 names, each of the eight closed and gated. NOT delivered, and disclosed in the file's own list: **no NATURALITY for the Kan comparison** — `lim_Ran_iso` is pointwise via `Kan_Limit`, and **an earlier revision of this bullet stated the obstruction wrongly in BOTH clauses, each refuted by audit**: an evaluation functor DOES exist (`One_Eval : [_1,C] ⟶ C`, `Theory/Shapes.v`, its block free of `Set`), and **`Sets` DOES meet `Category@{_ Set Set}`** — `Check (@One_Fun_iso Sets)` succeeds, universe polymorphism instantiating it at `Sets@{Set _}`, the same trap `Theory/Size.v`'s erratum records for `Check (Cat : obj[Cat])`. What actually stands is that the transport needs the OTHER variance, `C ⟶ [1,C]`, and both in-tree candidates for it (`One_Const`, `One_Fun_iso`) ARE pinned at `Category@{_ Set Set}`, together with `Kan_Limit` being `Qed` so the iso is opaque; the transport was not attempted; no `Kan_Colimit` in tree, so nothing relates `ColimitFunctor` to `Lan`; **no dual of `Terminal_Limit`**, hence no `HasColimitsOfShape 0 C ↔ Initial C`, blocked by the refuted `Opposite _0 = _0`; `[0,C] ≅ 1` NOT built, so `Δ[0]` is not shown to BE `Erase C` and the two degenerate readings connect only through `Terminal C`; **no RESTRICTED limit functor** on the full subcategory of `[J,C]` spanned by the diagrams that have limits — the form Riehl uses for Corollary 3.3.3 and the one not requiring all `J`-shaped limits, which is its own checkbox in the issue and is NOT discharged, everything here taking `HasLimitsOfShape` as a hypothesis; no uniqueness of the adjoint, no preservation/creation/RAPL/LAPC (the "no preservation" stopped holding with #450: `Diagonal_continuous : ContinuousFunctor (Diagonal J)` proves that the diagonal preserves every limit, the preservation premise of the adjoint functor theorem at Δ[J]; preservation of COLIMITS by the diagonal, creation, RAPL and LAPC are still not stated), no functoriality in `J`; and the non-vacuity witness is at shape `1` only
- **Instance/Cat/Objects.v**, **Instance/Indiscrete.v**, **Instance/Cat/Components.v**: Mac Lane §IV.2 Exercise 9 (book p. 90, attributed to Smythe) — the adjoint string on the objects functor. **THE HEADLINE IS THAT THE STRING SPLITS, AND THE SPLIT IS FORCED BY A COUNTEREXAMPLE RATHER THAN BY CONVENIENCE.** Delivered: `adjoint_string : (StrictCat_Disc ⊣ StrictCat_Objects) * (StrictCat_Objects ⊣ StrictCat_Indisc)` — a two-link string whose middle term is shared ONLY AT A SHARED UNIVERSE INSTANCE, which costs a `Set` pin inherited from `Indiscrete` (at the default instance the two `StrictCat_Objects` occurrences elaborate differently; the shared reading is pinned by a `Check` in `Instance/Indiscrete.v`) — plus `Components_Disc_Adjunction : Pi0 ⊣ Cat_Disc` at `Cat`/`Sets`, which **cannot join it AS TYPED** (`Pi0` runs `Cat ⟶ Sets` while the string runs `Coq`/`StrictCat`); whether some differently-built `Coq`-valued π₀ could join is left OPEN and is not proved either way. So all three adjunctions the DoD asks for exist, and the reason they are not one string is a theorem. **`objects_not_functorial_over_Cat : ObjRespectsCat → False` IS THE KEYSTONE**: over `Cat` the swap endofunctor of `Indiscrete bool` is naturally isomorphic to the identity (every hom-set is `unit`, so `Id ≈ SwapI` — `swap_iso_in_Cat`) while moving every object, and any objects functor must compare object maps by LEIBNIZ equality since `ObjSetoid`'s `equiv` IS `@eq`. Hence no objects functor exists on `Cat` **INTO A TARGET COMPARING OBJECT MAPS BY LEIBNIZ EQUALITY** — and that qualifier is load-bearing, not a hedge: an objects functor `Cat ⟶ Sets` over the ISOMORPHISM setoid DOES exist (compiled, zero obligations, closed under the global context, both actions `eq_refl`), and issue #357's item 1 offers exactly that alternative. So the strict reading is the EXERCISE'S CHOICE; what is forced by refutation is that ONCE it is taken the domain and codomain follow. This vindicates the note at `Theory/Connected/Components.v`. **A SECOND CRUX THE ISSUE DOES NOT ANTICIPATE**: the target must be `Coq`, not `Sets` — `disc_carrier_not_Sets_functor` and `ind_carrier_not_Sets_functor` refute both halves, because `Sets`' hom-setoid identifies maps that are not pointwise Leibniz-equal (a 'blurred' `bool` witnesses it) while `StrictCat`'s `eq_on_obj` demands exactly that. `Coq` is AN in-tree category of sets whose `≈` on morphisms is pointwise `=` and the natural choice here — not the only one, `Instance/Ens.v` and `Instance/EnsV.v` also qualify. **TWO OF THE ISSUE'S PREMISES ARE FALSE, both about things already in tree**: it says the codiscrete half 'has no construction at all — searching for it finds only comments', whereas `Indiscrete (A : Type) : Category` is `Instance/Discrete/Reconstruct.v` with `hom := fun _ _ => unit` (reused, not rebuilt); and its item 4 asks that the components functor be defined, whereas `Pi0 : Cat ⟶ Sets` is `Theory/Connected/Components.v` (reused — only the adjunction was missing). **A DONOR `Set` PIN WAS FOUND AND ROUTED AROUND**: `Instance/Discrete.v`'s `DiscreteCat_Functor` (which this commit's own prose edit moved) is universe-unannotated and pins its SOURCE at `DiscreteCat@{u Set Set}`, so the string is built over a locally annotated `disc_ext` instead. Strengths measured strict-first: the objects functor's object and arrow actions, the discrete functor's, both legs of the transposition, and — the two worth naming — **the unit IS the identity and the counit's object action IS the identity, both at `eq_refl`**; the single `≈`-only step IN `Objects.v` — the other two files have their own — is the round trip `disc_ext (fobj[F]) ≈ F`, which spends exactly `fmap_id` (and note `apply fmap_id` FAILS there, the elaborator declining to unify `id{?C}` against `eq_refl` — `exact (@fmap_id _ _ x a)` works). `Build_Adjunction'` is the cheap presentation because its two naturality clauses live in `Coq`, where `≈` is pointwise `=`, so both close by `reflexivity`; the full `Build_Adjunction` would have put them in `StrictCat` as transport-laden strict-equality goals. Separations proved rather than assumed: `Objects_not_Faithful` and `disc_indisc_not_iso`. **184/184 constants closed under the global context** — 118 source-declared plus **66 `Program` obligations** no source sweep sees — with **zero name collisions** across all 118. The prose at `Instance/Discrete.v` and `Structure/Discrete.v` that named these adjoints without building them is updated to cite them AND corrected: both said `Set ⟶ Cat`, which the keystone refutes. NOT delivered: Mac Lane's full four-term string as one object — nothing here claims it is in the tree; and the issue's Verification block pins `Discrete_Objects_Adjunction`, `Objects_Indiscrete_Adjunction` and `Components_Discrete_Adjunction`, none of which exist (delivered as `Disc_Objects_Adjunction`, `Objects_Indisc_Adjunction`, `Components_Disc_Adjunction`).
- **Structure/Limit/Constant.v**: Mac Lane §IV.2 Exercise 8 (book p. 90) — **the limit and colimit of a CONSTANT diagram over a connected shape are the constant itself**, with the diagonal fully faithful and the limits ABSOLUTE. **THE HYPOTHESIS IS `ConnectedNonempty`, NOT `Connected`, AND BOTH HALVES ARE PROVED NECESSARY** — which is sharper than the issue asks: `const_limit_not_from_bare_connected` refutes the bare in-tree `Connected` reading at the EMPTY shape (`_0` is vacuously connected, `Zero_Connected`, yet the limit of a constant diagram there is the TERMINAL object, and `TwoX_not_terminal` shows that is not `c`), while `const_limit_not_from_point_alone` refutes inhabitedness alone at the two-object discrete shape. So the in-tree `Connected` (`∀ x y, ZigZag x y`, with no inhabitedness clause) is genuinely the wrong reading here, and the statement says so rather than footnoting it. **THE ISSUE'S "Current state" IS FALSE ON TWO OF ITS THREE CLAIMS, both because of already-merged work**: it says no connectedness predicate exists (`ZigZag`/`Connected` are `Structure/Groupoid/Connected.v`, pre-existing and reused, never redeclared here) and that there is no `Δ ⊣ Lim` adjunction for part (b) to describe (`Diagonal_Limit_Adjunction` and `Colimit_Diagonal_Adjunction` are `Adjunction/Diagonal/Limit.v`); its third claim, that `Diagonal_Unique` is a factorisation rather than a computation of `Lim (Δ c)`, is accurate. **THE CENTRAL ABSENCE IS REAL AND WAS MEASURED BY SHAPE**: the only near-hits are in `Structure/Limit/Weighted.v` and put the constant on the **weight** side (`conical_weighted`'s weight is `Δ[J](terminal_obj)` with `F` arbitrary) — a different statement. **THE ENGINE IS ONE REUSABLE LEMMA WITH NOTHING CATEGORICAL IN IT**: `leg_zigzag` closes a bare leg family under zig-zags, `zz_fwd` and `zz_bwd` consuming the SAME hypothesis at swapped arguments and opposite rewrite directions, so there is no asymmetry to manage; each side's step lemma is then one `id_left` through cone coherence, because `fmap[Δ[J](c)] f` IS `id[c]` by `eq_refl`. That identity is also why `const_acone` is a plain `Definition` with ZERO obligations. **THE COLIMIT HALF CANNOT BE GOT BY INSTANTIATING THE LIMIT HALF AT THE OPPOSITES, AND THAT IS MEASURED**: `Opposite_Functor Δ[J](c)` and `Δ[J^op](c)` are NOT convertible (pinned), so `ACone n G` does not transfer — yet BOTH DATA FIELDS agree at `eq_refl`, locating the difference in the three rebuilt law fields. It is built directly instead, and the cocone side needs NO `Connected (J^op)` (which the tree could not supply — there is no `zigzag_op`): `obj[J^op]` IS `obj[J]`, so `leg_zigzag` is instantiated at `J` with ambient `C^op`, the explicit `@` being required or inference picks `J^op` and demands opposite arrows. Strict readbacks: the apex, every cone leg, every limit leg and the mediator (which IS the competing cone's leg at the chosen point) all at `eq_refl`. Item 3 is delivered with a FINER decomposition than the issue anticipates — **`Diagonal_Faithful` needs only a POINT of `J`, not connectedness**, while `Diagonal_Full` needs the whole hypothesis, and each half has its own refutation (`Diagonal_Zero_not_Faithful`, `Diagonal_Two_Discrete_not_Full`). The reviewer check is met by `two_discrete_const_not_limit`, whose argument makes the pair of legs surjective onto `bool × bool` from the universal property alone and closes by pigeonhole — with `td_const_IsALimit_product` showing what the disconnected shape computes INSTEAD (`c × c`), so the refutation is quantitative rather than an existence failure. **89/89 constants closed under the global context** — 87 source-declared plus two `Program` obligations no source sweep sees — with **zero name collisions** across all 87. NOT delivered: the general (non-connected) case of part (b) via `ComponentDecomposition` — nothing composes with `Structure/Limit/Components.v` and no bridge exhibits the connected case as the one-component instance; and no absolute-limit THEORY beyond the two definitions `AbsoluteLimit`/`AbsoluteColimit` — which this file DOES introduce, the string having had zero prior occurrences — and their two witnesses: nothing relates them to `PreservesLimit`, `PreservesLimitCone`, `ContinuousFunctor` or `CreatesLimit`, and split coequalizers are not related to them. Only the FORWARD direction of part (b)'s "exactly when" is proved; no converse forces the hypothesis. The issue's Verification block pins `connected_limit_of_constant`, a name that exists nowhere in the tree; the delivered name is `const_IsALimit`.
- **Structure/Limit/Components.v**: Mac Lane §IV.2 Exercise 7 (book p. 90) + Riehl §3.6 `riehl:3.6:construction-cat-coproducts` — **a limit over a coproduct of index categories is the product of the limits over the summands; a category EQUIPPED WITH A DECOMPOSITION AS DATA whose index satisfies `IdxUIP` is the coproduct of its connected components; and hence every limit over a shape carrying a decomposition is a product of limits over connected shapes**. Read clause (b)'s hypotheses in that headline rather than in the correction below: Mac Lane's own sentence quantifies over EVERY category and this one does not. All three clauses are delivered as theorems, with a fourth result factored out because nothing in it is about components: **`xfer_IsALimit`, a FULL and ESSENTIALLY SURJECTIVE functor of shapes transfers limits, with faithfulness NEVER CONSUMED** — which is exactly why **part (c) costs no `IdxUIP` at all** while the equivalence of part (b) does. **THE ISSUE'S PREMISE IS FALSE AND WAS FALSE BEFORE #352**: it asks for the zig-zag relation to be built from scratch, claiming no connectedness predicate exists anywhere in the tree, whereas `Structure/Groupoid/Connected.v` has declared `Inductive ZigZag {C : Category}` and `Definition Connected (C : Category)` for an ARBITRARY category since a pre-existing commit; `Theory/Connected/Components.v` reuses them and so does this file, which redeclares neither (measured: zero `ZigZag`/`Connected` declarations here). **PART (b)'s OBSTRUCTION IS PROVED RATHER THAN ASSERTED, AND IT IS WHAT FORCES THE DESIGN**: π₀ is a COARSER SETOID ON THE SAME CARRIER (`carrier (pi0 C) = obj[C]` by `eq_refl`), so it supplies no quotient TYPE to index a coproduct by, and indexing `SigmaCat` by it yields ONE SUMMAND PER OBJECT — at the walking arrow, two summands where there is one component. `naive_pi0_sum_not_connected` proves that category disconnected while `Two_Connected` proves `_2` connected, and `no_ESO_into_naive_pi0_sum` turns it into the consumer-facing form: NO essentially surjective functor `_2 ⟶ naive_pi0_sum` exists. **Read that at its strength** — it refutes THAT index and is NOT a proof that the two categories are inequivalent. The decomposition is therefore DATA, on `Theory/Skeleton.v`'s `Skeleton` model: `ComponentDecomposition` carries `cd_index`, `cd_rep` (a chosen representative), `cd_part`, `cd_join` and the uniqueness field `cd_sep`, **consuming NO choice principle**, and no category is shown to HAVE a decomposition — the two concrete ones are built by hand. Riehl's uniqueness clause is `cd_transfer`/`cd_transfer_round`, sharpened by `cd_transfer_part` so that two decompositions cut the category into the same pieces rather than merely having equinumerous index sets. **A UNIVERSE TRAP DECIDED THE SHAPE OF PART (a), AND IT IS GUARDED RATHER THAN MEASURED**: `@iprod`'s binder reads `C : Category@{_ Set Set}` — hom AND proof pinned to the literal `Set`, since it is defined over `Limit (DiscreteCat_Functor f)` — while `@IsIndexedProduct` is `C : Category@{_ u u}`, identified but FREE (the #139 binder trap: `iprod`'s constraint block shows only a bound on a different level). The issue's own reviewer check asks that part (a)'s right-hand side use `iprod`; taken literally that would have confined the theorem to `Set`-homed categories WITHOUT SAYING SO, so part (a) is stated FIRST at `IsIndexedProduct` and only then read through `iprod` as `coprod_IsALimit_iprod`, with the pin pinned as a FORMABILITY negative (`Cannot enforce Set = uh`) against APPLIED controls at those very levels. The projections agree with restriction along the injections (`coprod_proj_is_restriction`), and the isomorphism packaging `coprod_limit_iso` carries its leg equations (`coprod_limit_iso_legs`) rather than being a bare `≅`. **`IdxUIP` is spent in exactly ONE place** — `cd_compare_Faithful`, inherited by `cd_equivalence` — while `cd_compare_Full` and `cd_compare_ESO` take no hypothesis; it is **NOT shown necessary** (no countermodel, no forcing theorem in the style of `sigma_inj_Full_forces_UIP`) and is discharged by Hedberg for decidable indices. **117/117 constants closed under the global context, of which the `make print-assumptions` gate carries 51 permanently** — 111 source-declared plus the five `ComponentDecomposition` field accessors and the constructor, none of which a source sweep sees — and **zero name collisions** across all 111. Non-vacuity is proved rather than asserted at three points: `_2` as a one-component shape, `TwoSum` as a genuinely two-component one with its summands proved separated (`TwoSum_not_connected`, `TwoSum_component_objects_differ`), and a `Coq` witness whose mediator COMPUTES (`CoqPair_med_computes`) with a two-element apex. Four in-file `Fail`s (2 conversion, 2 formability) plus `Test/ProbeComponents355.v`, which exists for what an in-file negative cannot do — survive a rename — mirroring the target's full 22-line import list, and **rename-simulated 10/10 after the first run found TWO VACUOUS GUARDS** (`coprod_IsALimit_iprod` and `component_diagram` were named only inside `Fail`s, so renaming either left the guard green). NOT delivered: no dual (nothing is stated for colimits); no proof that a category HAS a decomposition; no relation between `ComponentDecomposition` and `pi0` as an isomorphism of index types; `IdxUIP`'s necessity; and no naturality of any identification in the diagram.
- **Adjunction/Diagonal/Finite.v**: Mac Lane §IV.2 Exercise 6 (book p. 90) + the finite rows of the §IV.2 table — **the units and counits of the equalizer, coequalizer, pullback and pushout adjunctions**, and the four functors themselves. **THE FOUR FUNCTORS AND FOUR ADJUNCTIONS ARE PURE CONSUMPTION OF `Adjunction/Diagonal/Limit.v` — though the FILE is not, and the distinction matters**: `EqualizerFunctor`, `CoequalizerFunctor`, `PullbackFunctor` and `PushoutFunctor` are `:=` of `LimitFunctor`/`ColimitFunctor`, and the four adjunctions `Diagonal_Equalizer_Adjunction : Δ[Parallel] ⊣ EqualizerFunctor`, `Coequalizer_Diagonal_Adjunction`, `Diagonal_Pullback_Adjunction : Δ[Roof^op] ⊣ PullbackFunctor` and `Pushout_Diagonal_Adjunction` are `:=` of the two general ones — nothing from that file is re-proved. What the file DOES build is general theory of its own: `ShapeLinked` heading an eleven-constant constant-diagram development, four `*_respects` transports that exist nowhere else in the tree, and the whole pushout bridge `po_legs`/`po_cocone`/`po_inj_ump`. All four are COVARIANT (the `Roof^op` on the pullback side is the SHAPE, not a dualized ambient). **The absence they fill was confirmed BY TYPE, not by name**: a multi-line-aware sweep over 802 `.v` files finds no equalizer, coequalizer, pullback or pushout FUNCTOR — the only seven constants whose name mentions those words and whose type contains `⟶` all have the `⟶` in an ARGUMENT (a diagram `Parallel ⟶ C`), never as their own type — a first run said SIX, missing `Structure/Coequalizer/Split.v`'s `split_coequalizer_preserved` because it is declared with `Corollary`. **THE EXERCISE'S ACTUAL CONTENT IS THE IDENTIFICATION AGAINST THE ELEMENTARY APIs, AND THAT IS WHAT IS DELIVERED** — not merely against the generic cone machinery: `eq_counit_IsEqualizer` shows the counit component at `ParX` IS an equalizing arrow in `Structure/Equalizer/Fork.v`'s `IsEqualizer`, `coeq_unit_IsCoequalizer` dually, `pb_counit_IsPullback` gives the two counit components as the projections of `Theory/Morphisms/Stability.v`'s apex-pinned `IsPullback`, and `po_unit_IsPushoutSquare` the two unit components as the injections of `Theory/Morphisms/CokernelPair.v`'s `IsPushoutSquare` — the apex-pinned form being REQUIRED because `Structure/Pushout.v`'s `IsPushout` is bundled and carries its apex as data, so it cannot state that a GIVEN square is a pushout. Packaged as `HasLimitsOfShape_HasEqualizers` and three siblings, so a consumer's chosen elementary arrow IS the unit/counit component. **"UNIT THE IDENTITY" IS DELIVERED AT ITS TRUE STRENGTH, AND THE EXERCISE'S PHRASING CANNOT BE READ LITERALLY**: for `Δ ⊣ lim` the unit at `x` is `x ~> lim (Δ[J](x))`, a morphism between DIFFERENT objects, so `dia_unit L x = id[x]` is not false but ILL-TYPED (measured, and pinned as the probe's TYPING negative — the error names `x ~> lim_obj L (fobj[Diagonal Parallel] x)` as the expected type). What holds instead is invertibility under the sufficient condition `ShapeLinked j0 := ∀ j, (j0 ~> j) + (j ~> j0)`, with `dia_unit_iso`/`dia_counit_iso` and the leg at `j0` as inverse; `Parallel_linked`, `Roof_linked`, `Roofop_linked` discharge it and the four rows instantiate it. `ShapeLinked` is SUFFICIENT and is NOT claimed to be connectedness — no link to `Structure/Groupoid/Connected.v` is made and no shape failing it is exhibited. **THE TWO LIMIT-SIDE ROWS COST DIFFERENTLY FROM THE TWO COLIMIT-SIDE ONES, AND THE ASYMMETRY IS DISCLOSED**: `pb_leg_IsPullback` is a `:=` with NO tactic, consuming `Structure/Pullback/Limit.v`'s `Pullback_to_Universal` so the limit apex and both legs land on the pullback apex and projections BY CONVERSION; the pushout side has no such donor — `Pushout_Limit` is a bare alias for `Colimit` with no conversion to any pushout UMP — so row 3 is reuse and row 4 is construction. **33 `eq_refl` Examples**, among them that each row's unit/counit IS the general one and that the unit IS the transpose of the identity. Universes: all 119 `Category@{…}` occurrences identify hom with proof (the donors' doing), and exactly 14 constants carry the equation `u0 = u2`, all in the two generic constant-diagram sections inherited from `Limit.v` — **the four ROW sections carry no equation at all, but read that narrowly**: nothing is more general, the identification being discharged by INSTANTIATION, which is possible only because `Parallel@{u u0}` and `Roof@{u u0}` are polymorphic with EMPTY constraint blocks and carry no `Set`, unlike `_0`. `Set` was swept in BINDERS as well as blocks (#139): exactly 20 constants carry it, all in the concrete `Sets` witness block, and the cause is discriminated — `DiagBoolSet` is itself polymorphic, so it is minimization at the `bool` carrier, and the competing explanation (`proper_morphism` left to instance resolution) was ruled out by compiling both forms and getting identical instances; a variant inside `Constraint Set < o` lifts it, so it is NOT claimed unavoidable, and the four `Sets_*` adjunctions and four `Has*` classes are `Set`-FREE. Non-vacuity is proved in both senses for all four rows: the diagrams are proved NOT constant and the four objects proved not subsingletons, with the isomorphisms coming from the degenerate readings rather than assumed, and no `eq_refl` evaluating a (co)limit element — the witnesses exercise the universal property, not the construction. **140/140 constants closed under the global context** (136 source-declared + 4 `Program` obligations a `.glob` sweep cannot see), counted on the whitespace-flattened `Print Module` output. `Test/ProbeDiagonalFinite354.v` pins the rejections in three kinds — four CONVERSION (each elementary arrow against its leg, with the SAME statement plus the residual `∘ id` on the limit side or `id ∘` on the colimit side as the passing control, which is what locates the difference at the residue), one TYPING (the ill-typed `unit = id`), two CONVERSION for `Opposite Roof = Roof` and `Opposite Parallel = Parallel` (why the pushout row is not read off the pullback row), and NINE FORMABILITY — all nine donors the header measures, not the four an earlier revision pinned — **with `Cone` as the discriminating control**, accepted at the very levels where each of the nine is rejected alone, so "the cone vocabulary" would be the wrong attribution. Read "nine" as nine DONORS rather than nine independent causes (`HasLimitsOfShape` is `∀ F, Limit F`, so it has no reason of its own), and note the control must be APPLIED: `Check @Cone.` succeeds, but so does `Check @Limit.`, an unapplied polymorphic constant never meeting the declared levels — it is `@Cone Parallel Cu` against `@Limit Parallel Cu` that separates. **The probe records TWO hazards that each cost a false pass**: a `Fail` that succeeds prints NOTHING under this repo's `coqc`, so every negative must be stripped and run alone; and **`Roof^op = Roof` is a FALSE PASS** — inside an `eq` argument there is no expected type for the category scope to win on, so `^op` parses as the FUNCTOR opposite and the command fails with "`Roof` has type `Category` while it is expected to have type `?C ⟶ ?D`", which is notation and not mathematics. Both are spelled `Opposite Roof`/`Opposite Parallel`. NOT delivered: **the four `Sets`-level classes are SECOND inhabitants and no agreement with the first is claimed** (`DiagSets_HasEqualizers`, `DiagSets_HasCoequalizers`, `Sets_HasPullbacks_of_shape` and `Sets_HasPushouts_of_shape` duplicate `Adjunction/GAFT/Sets.v`, `Instance/Sets/Coequalizer.v`, `Instance/Sets/Pullback.v` and `Instance/Sets/Pushout.v`; nothing relates the two routes, and the four here are plain `Definition`s so the pre-existing exported instances still win resolution); no converse for any of the four packagings (nothing builds `HasLimitsOfShape Parallel C` from `HasEqualizers C` — the elementary classes quantify over pairs/spans whose diagrams are `APair`/`ASpan`/`ACospan` records, and a general `F : Parallel ⟶ C` is not one on the nose; transporting a limit along a diagram isomorphism is not attempted); no uniqueness of the four functors, so the four biconditionals assert EXISTENCE of an adjoint and not that it is the one named; no naturality of any identification in the diagram; no preservation/reflection/creation and no RAPL/LAPC; the pushout row is deliberately NOT derived from the pullback row (it compiles, but every downstream statement would then be about `C^op`, losing covariance); no wide (co)equalizers or pullbacks and no link to `Structure/Pullback/Reduction.v`; and no element-level computation of any of the four objects
- **Theory/Equivalence/Colimit.v**: Riehl §3.3 Corollary 3.3.3 and its surrounding prose — **the colimit transport results, the reusable `Opposite_Functor` respectfulness lemma they need, and the non-invertible case**. **THE PRIOR-ART CORRECTION IS THE HEADLINE AND IT CORRECTS ME**: I claimed no `Proper`/transport lemma for `Opposite_Functor` existed anywhere, and that was FALSE — the content is already `Op_obligation_1`, the `fmap_respects` obligation of `Op : Cat ⟶ Cat` (`Instance/Cat/Opposite.v`), typed literally `∀ x y : obj[Cat], Proper (equiv ==> equiv) (λ F : x ⟶ y, F^op)`. **WHY EVERY SOURCE GREP MISSES IT IS THE GENERAL LESSON**: the string `Op_obligation_1` occurs ZERO times in that file, and so does the token `Proper` — both the name and the type are GENERATED by `Program`, so no source search can find the fact by name OR by the keywords of its statement; only `Print Module`/`About` on the COMPILED module can, and `About` on the short name fails, needing the fully qualified one. So `Opposite_Functor_respects` (+ the `#[export] Instance Opposite_Functor_Proper`) is a **strictly more general, TRANSPARENT restatement, not a first sighting**, and the file says so in those terms. TWO reasons survive and each is measured against a control: **OPACITY** — `Op_obligation_1` is `Qed`, so the component readback `` `1 (Opposite_Functor_respects e) x = Isomorphism_Opposite (`1 e x) `` holds by `eq_refl` while the SAME readback routed through `fmap_respects Op` is REJECTED; and **UNIVERSES** — the `Op` route elaborates with J's and C's object levels IDENTIFIED (`u = u1`, and hom levels too, `u0 = u2`), while the restatement keeps them SEPARATE, measured under a declared `Constraint jo < co` where the restatement is accepted and the `Op` route rejected. `Opposite_Functor_reflects` gives the inverse passage as a tactic-free `:=` (`Opposite_Functor_respects e` lands at `(G^op)^op ≈ (G'^op)^op`, which IS `G ≈ G'` since `Functor/Opposite.v`'s `Opposite_Functor_invol` closes by `reflexivity` — it is a `Qed` constant, so what the `:=` relies on is the underlying convertibility rather than the lemma reducing), though the two passages are **not** mutually inverse at record level. Delivered on top: `cocone_transport`/`cocone_transport_inv`/`isacolimit_transport`/`colimit_transport`, **all four `:=` with NO tactic** — because `IsAColimit F c := IsALimit (F^op) c` (`Structure/Limit/Preservation.v`) and `Colimit F := Limit (F^op)` (`Structure/Limit.v`) are DEFINITIONAL, so the dualization runs in ONE direction and the whole passage costs exactly the respectfulness lemma (this also refutes the natural guess that the functor involution makes it cheaper — there is nothing for the involution to do there) — mirroring `Theory/Equivalence/Limit.v`'s structure so the two files read side by side, with `colimit_transport_apex` recording that the apex is unmoved and the HANDEDNESS stated rather than left to unfold: a cocone pushed across `e` POSTcomposes with `from` while a colimit's injections PREcompose with it, the opposite of the limit side. Riehl's non-invertible case is `cone_along` (needing naturality only, never invertibility) with `limit_induced` + `_commutes`/`_unique`/`_id`/`_comp` and the colimit dual, **no `^op` in any of their types**, hypotheses being two supplied (co)limits and nothing assuming `C` has all `J`-shaped ones; bridges `fun_equiv_transform` (a `Functor_Setoid` equivalence's forward components as a `Transform` — the tree had no such passage), `cone_along_is_cone_transport` (Limit.v's `cone_transport` IS `cone_along` at those components, leg by leg, `eq_refl`) and `limit_induced_of_transport`. Witnesses: `two_endo`/`two_IsALimit`/`two_IsAColimit` (no `_2` (co)limit existed in tree), a SHARP non-invertible witness (`two_shift_induced_computes` by `eq_refl`, `two_shift_induced_not_iso`, and `two_shift_diagrams_not_isomorphic` proving the transport results do NOT apply to that pair, so it separates the two developments), and — reached at ZERO module cost after an initial false dichotomy was recognised — a transport witness with **provably distinct diagrams** (`two_diagrams_differ`) and a **provably moved injection** (`two_transport_moves_inj`): `Walk` needs its dependent annotation because it varies the OBJECT action, so holding that constant makes the arrow match ordinary and `two_endo` is the case that annotation exists to avoid. **A SCOPE RESTRICTION THAT A CONSTRAINT-BLOCK SWEEP CANNOT SEE**: `two_IsALimit` and `two_IsAColimit` read `∀ {C : Category@{_ Set Set}}` — the AMBIENT category's hom and proof pinned to the literal `Set` — with **NO `Set =` in either block**, so sweeping blocks for `Set =` returns a clean bill and is wrong; the binder must be swept for the literal token. Donors: `Instance/Two.v`'s `TwoHom : … → Set` fixes the shape's hom, and `IsALimit` identifies the shape's hom-and-proof with the ambient's. `two_endo` is unaffected (both levels free), so the BUILDER is general and only the two (co)limit lemmas are pinned; both witnesses satisfy the restriction, so nothing is vacuous. **81/81 constants closed under the global context** (70 source + 11 `Program` obligations), module closure 32, no `Set` pin anywhere in (A)/(B)/(C). NOT delivered: no preservation/reflection/creation, no cone-level form, no "α invertible ⇒ induced iso", no uniqueness-up-to-unique-iso, no limit functor or `Δ ⊣ lim` (that is `Adjunction/Diagonal/Limit.v`), no naturality in the witnesses, and the two (A) passages are not shown mutually inverse
- **Instance/Fun/Group.v**: Mac Lane §III.6 Exercise 3 (book p. 76) — **group and monoid objects in a functor category are POINTWISE**: a functor `T : B ⟶ Sets` is a group object in `[B, Sets]` (pointwise cartesian) exactly when each value `T b` is an ordinary group and each `T f` a homomorphism. Both directions are genuine `iffT`s and both are `Defined` — `monoid_object_iff_pointwise` and `group_object_iff_pointwise`, with the four passages (`monoid_object_pointwise`, `pointwise_MonoidObject`, `group_object_pointwise`, `pointwise_GroupObject`) available as named constants rather than projections out of an opaque iff. **The elementary laws are read off BY CONVERSION, not by a chase**: `fun_unit_l_raw`, `fun_unit_r_raw`, `fun_assoc_raw`, `fun_inv_l_raw`, `fun_inv_r_raw` are each the class field applied to an explicit element, supplied by `:=` with no tactic and no transport, and naturality of `mappend` IS "`T f` preserves multiplication" on the nose. **The issue's "the monoid analogue is nearly free" is TRUE and the reason is definitional**: `CC_CartesianMonoidal`'s underlying monoidal IS `CC_Monoidal`, so `fun_gmonoid` reads a `MonoidObject` off a `GroupObject` with NO adapter and NO transport — the group half reuses the monoid half through `groupobject_is_monoid` forward and the `pg_monoid` field backward, at a cost of one transformation, two laws and one naturality clause — but NOT "four lines", as an earlier draft said: measured non-blank non-comment source, the monoid halves are 92 lines against the group halves' 71, so the group side is 77% of the monoid side, and it is qualitatively harder besides. **THE ONE PERVASIVE `eq_refl`→`≈` FALLBACK IS AN ARGUMENT RESIDUE, NOT A REBUILT FIELD**: `[B, Sets]`'s identity transformation has component `fmap[F] id` rather than `id` (`Theory/Natural/Transformation.v`), so every law arrives with `fmap[T] id` wrapped round an argument — TWICE in the inverse laws, because `CartesianMonoidal`'s `diagonal` feeds through `bimap` — which is exactly why the `*_raw` forms are `:=` while `fun_unit_l`/`fun_inv_l` and siblings are `Qed` lemmas with a `transitivity` step through `T_fmap_id`; and `rewrite !T_fmap_id` does NOT close the inverse pair (setoid rewriting leaves undischarged `Proper` evars), `apply proper_morphism; split` does. **Universes measured off BOTH block and binder, with a REAL localization**: the two records are FREE — `PointwiseMonoid@{bo bh bp o so}`/`PointwiseGroup@{bo bh bp o so}` keep `B`'s object, hom and proof universes SEPARATE with no equation anywhere in the block — obtained by hoisting them out of the section, which had made them inherit the collapse; the theorems over them identify `B`'s hom, `B`'s proof and `Sets`' carrier universe all at one level, and that identification sits **entirely in the BINDER**, the constraint blocks containing no equation at all, so reading a block alone would miss it. **The identification has TWO INDEPENDENT CAUSES, and the file separates them by probe rather than attributing**: for the headline theorems it is `Fun`, whose two halves are shown distinct (`UniverseBoundary` rejects `[Bu, Sets]` already at `ubh < ubp`, so `Fun` forces the source's hom and proof to agree; `UniverseBoundary2` gives them equal by construction and is rejected again at `vbh < vo`, so `Fun` additionally forces the source's hom to be the target's — neither half alone accounts for it); but for `PointwiseGrpFunctor`, `pointwise_GrpObject` and `pointwise_GrpHom` the cause is NOT `Fun`, those mentioning no functor category at all, and is instead the section's unannotated `Context {B : Category}` — `B ⟶ Grp` being formable with the levels apart is the discriminating control — which the probe section now actually CARRIES as `Check (Bu ⟶ Grp)`, an earlier revision having asserted it in the header without it existing in the file. Not claimed unavoidable; the lift was not attempted. **The ordinary-group reading is delivered as a LIFT**, and since the PR "algebraic carriers are sets" (2026-09-17) it takes a hypothesis on the presheaf's VALUES — `PT : ∀ b, PropEquiv (is_setoid (T b))`, a `GrpObject` now carrying `grp_prop` and the values of an arbitrary `T : B ⟶ Sets` supplying none — on `pointwise_GrpObject` (carrier `T b` by `eq_refl`), `pointwise_GrpHom` (underlying map `fmap[T] f` by `eq_refl`), `PointwiseGrpFunctor : B ⟶ Grp` and its two `_forgets_` Examples, with `pointwise_GrpObject_carrier` following; the converse is unaffected, and the round trip discharges the hypothesis itself, `grp_functor_PointwiseGroup` supplying `fun b => grp_prop (F b)`. The `115/115 constants closed under the global context` figure below was NOT re-measured after that PR and should be read as the pre-PR count; the file's three refutation Examples were re-measured and still refuse, `roundtrip_obj_whole` now at the new `PT` binder. The lifted functor's composite with `Grp_Forget` agrees with `T` on objects and arrows by `eq_refl` — but the STRICT equality of functors is REFUTED (`lift_is_strict`), the composite rebuilding `fmap_respects`/`fmap_id`/`fmap_comp` as its own opaque proofs, so no equality of functors is asserted and no equivalence between `PointwiseGroup T` and lifts through `Grp_Forget` is claimed. The converse needs NO hypothesis (`grp_functor_PointwiseGroup`/`grp_functor_GroupObject`: every functor into `Grp` carries a group object on its underlying functor). **#341's PROOF CONTENT TURNED OUT NOT TO BE USABLE, AND THE TRAFFIC RUNS THE OTHER WAY** — settled by measurement, and stated as *proof content* deliberately, since the file does `Require` that module and the cross-link consumes three of its DEFINITIONS (`RepresentablyMonoid`, `RepresentablyGroup`, `RepPresheaf`): `HomGroupData e` carries structure on the hom-SETS `x ~> e` and so presupposes the functor is `C(−,e)`, while `T` here is arbitrary with plain-setoid values; `Structure/Group/Representable.v`'s `FromPresheaf`/`FromPresheafGroup` sections ARE the forward direction of this theorem but their `Context` FIXES the object to `RepPresheaf e`, a scoping wall rather than a naming one; its generic `rg_*_law` product-form readings WOULD have applied at `C := [B, Sets]` and are deliberately unused, since over `Sets` the laws evaluate at elements and the product form is a detour. Instead `representably_monoid_iff_pointwise`/`representably_group_iff_pointwise` obtain #341's presheaf statement as INSTANCES of this one, `:=` with no tactic. **The issue's own absence claim splits in two, and only ONE half is newly false** — an earlier draft of this bullet dated the whole thing to `f3b797fd` and was wrong to. **The MONOID half was never true**: `Monad/Monoid.v` has carried `Monoid_Monad : @MonoidObject (Endofunctors C) Compose_Monoidal M ↔ Monad M` since **2017-05-29**, where `Endofunctors C := [C, C]` — a monoid object at a functor category, in the build set at `_CoqProject`, and named in `Structure/Monoid.v`, which `Instance/Fun/Group.v` itself Requires. The tensor there is `Compose_Monoidal` rather than the pointwise cartesian one, but the absence claim is unqualified as to tensor, so that does not rescue it. **The alias is how the miss happened and is the lesson**: grepping for `@MonoidObject ([` does not match, the functor category hiding behind `Endofunctors` — a name search again, the fifth of this session. **The GROUP half** is what `f3b797fd` genuinely falsified, landing `RepPresheaf_Group : … → GroupObject (RepPresheaf e)`. **A METHODOLOGY CORRECTION TO TWO MERGED FILES RIDES THIS COMMIT**: `Test/ProbeRingLattice340.v` and `Test/ProbeGroupRepr341.v` both asserted that a library file "cannot carry a `Constraint` without constraining itself", and that is FALSE — this file carries its universe probes in-file as section-local `Universes`/`Constraint` with `Context` variables and `Check` controls, and the declarations are measured NOT to leak (a downstream file importing it still declares its own levels strictly apart). Both headers are corrected here, naming the bad claim as bad; consequently **no `Test/ProbeFunGroup342.v` exists** — the target already pins 5 negatives of BOTH kinds (3 conversion, 2 formability) each beside controls, and the separate-file rule's stated rationale is gone. **115/115 constants closed under the global context** — `Print Module` lists 113 and the two unlisted names are the records' `Build_*` constructors; the 34 `*_obligation_*` constants are invisible to `Print Assumptions` under a BARE name and resolve only fully qualified, which is how an initial count of 81 came out, and the file header carried that stale 81 (with the false reason "the `Program` obligations leave no separately named constants behind") until it was corrected. `Print Module` also prints `Qed` constants as `Parameter`, a display convention rather than an axiom leak. Non-vacuity at `B := Grp`, `F := Id[Grp]` with `Und_GroupObject` and four computing `eq_refl` Examples at `Z2`, two degeneracies excluded by proof (`und_nontrivial` two distinct elements; `und_fmap_not_id` the arrow action genuinely moves one, so the homomorphism clauses are applied to a non-identity) — but a THIRD is NOT excluded and the header now says so: the group is `Z2`, where inversion IS the identity, so `pg_inv`, `pg_inv_l`, `pg_inv_r` and `pg_map_inv` are never exercised at an element that is not its own inverse. NOT delivered: no SMALL-index witness (the witness index is `Grp`, which is large; a `_2 ⟶ Grp` attempt was abandoned on a real obstruction — `_2`'s composition is `Program`-generated, so `destruct f; destruct g` in the `fmap_comp` obligation yields an ill-typed application of `Two._2_obligation_1`, recoverable by destructing the OBJECTS first but traded for the zero-cost `Id[Grp]` witness); no `Mon`-valued analogue of `PointwiseGrpFunctor`, and that is a real obstruction rather than a choice — `Construction/Deloop.v`'s `MonObject` has no category attached, and `Theory/Algebra/Monoid/Hom.v`'s `Mon Sets` is taken at `Sets_Product_Monoidal`, which `Instance/Grp.v` records as a DISTINCT, non-convertible term from the `CC_Monoidal` used here; no general cartesian target `D` in place of `Sets` (the development reads laws at ELEMENTS, and a general `D` needs the product-form route — which is precisely where #341's `rg_*_law` would be consumed); no naturality of the biconditional in `T`, no functoriality of `PointwiseGroup`, no abelian/Eckmann–Hilton clause, no coproduct story; and the universe lift of the `Grp`-reading block was not attempted
- **Structure/Group/Representable.v**, **Theory/Lawvere/Model/Closure.v**: Mac Lane §III.6 Proposition 1 + remark 2 (book pp. 75-76) — **group and monoid objects through their representable functors**, the functor-of-points criterion. `Structure/Group.v` had ASSERTED the theorem in prose since it was written ("equivalently, `[grp]` is a group object iff each hom `Hom(X, grp)` is a group naturally in X"); this discharges it. **THE WHOLE DEVELOPMENT IS FACTORED THROUGH ONE RECORD** — `HomGroupData e` (monoid half `HomMonoidData e`), the group operations on each hom-set `x ~> e` with the laws and with naturality stated as compatibility with precomposition, over an ARBITRARY category — and FOUR passages relate it to everything else: `group_object_hom_data` (needs products) and `data_GroupObject` (needs products) on the internal side, `RepPresheaf_Group` and `psh_group_hom_data` (both BASE-FREE) on the presheaf side. **THE ENABLING FINDING IS A CONVERSION**: the five diagrammatic `GroupObject` laws convert ON THE NOSE to their elementary product forms — `rg_assoc_law`, `rg_unit_left_law`, `rg_unit_right_law`, `rg_inv_left_law`, `rg_inv_right_law` are each proved by a bare `exact` of the corresponding class field, so the statement types are CONVERTIBLE and nothing is transported, which is what makes each transfer chase three lines instead of `Product_Monoid`'s hundred. `group_object_iff_representable` and `monoid_object_iff_representable` are both `Defined`, over the genuine `[C^op, Sets]` with its POINTWISE cartesian structure (`Functor_Category_Cartesian` + #339's `Functor_Category_Terminal`, which is exactly why #341 depended on #339). **Remark 2 is a THEOREM here, not a definition-by-fiat**: `RepresentablyMonoid`/`RepresentablyGroup` are stated over an arbitrary category (the presheaf category is cartesian and terminal because `Sets` is, whatever `C` is), the two base-free passages make them INTERDERIVABLE with the hom-set datum with no products anywhere — interderivable by two named passages, which is weaker than an equivalence, no base-free biconditional being stated (the file's only two `↔`s are the product-requiring headline theorems) — and where `C` does have finite products the headline identifies both with `GroupObject c`. The three Awodey clauses: `exp_GroupObject`/`exp_MonoidObject` on the internal exponential under cartesian closure — `exp_GroupObject` takes a `HomGroupData y` rather than a `GroupObject y`, the composite through `group_object_hom_data` being immediate but, unlike the monoid side's `exp_MonoidObject_of`, NOT delivered — — obtained by transporting hom-set data along curry/uncurry rather than by an internal diagram chase, ~30 lines against `Hom_Monoid`'s ~110, with `exp_mappend_is_Hom_Monoid` recording by `eq_refl` that the multiplication IS `Structure/Monoid.v`'s (the UNIT is only `≈`, blocked by a `one_unique (exl : 1 × x ~> 1) one` residue, pinned); **the EXTERNAL formulation with NO cartesian closure and NO products at all** — `HomGrpObject` (each hom-set as an `Instance/Grp.v` `GrpObject`), `HomGrpHom` with precomposition PROVED a homomorphism (`grp_map_unit` from `hmd_unit_natural`, `grp_map_mul` from `hmd_mul_natural`), and `HomGrpFunctor : C^op ⟶ Grp`, the section taking only `{C : Category} (e : C) (D : HomGroupData e)` — until the PR "algebraic carriers are sets" (2026-09-17), which adds to all three the CODOMAIN-ONLY hypothesis `HP : ∀ x : C, PropEquiv (@homset C x e)`, a `GrpObject` now carrying `grp_prop`. Read that shape deliberately: the natural-looking `{LP : LocallyPropositional C}` is REFUSED as a route, because the one in-tree instantiation is at `C := Sets`, which is not locally propositional and cannot be (both the class and a hand pointwise route are refused, measured), so the codomain-only form is strictly weaker and the only usable one; `HomGrpA` discharges it by `fun x : Sets => @hom_PropEquiv x gobj Pg`, and `elem_GrpObject`, `iprod_GrpObject`, `HomGrpA` and `sets_hom_iprod_iso` take the section hypothesis `Pg : PropEquiv (is_setoid gobj)` with `sets_hom_to_iprod`, `sets_iprod_to_hom` and `HomGrpFunctor_Forget_agrees` following. The `184/184 constants closed under the global context` figure below was NOT re-measured after that PR and should be read as the pre-PR count. And `sets_hom_iprod_iso : HomGrpA ≅[Grp] iprod_GrpObject A` is an isomorphism IN `Grp` rather than merely of setoids, over `Instance/Sets/Products.v`'s `Sets_iprod_obj`. **THAT LAST ONE CARRIES A GENUINE RESTRICTION AND IT IS NOT COSMETIC**: `X` must be a DISCRETE setoid (`DiscSet A`), because for a general `X : Sets` the construction is unavailable — `Hom_Sets(X,G)` consists of `≈`-RESPECTING maps while `Sets_iprod_obj (fun _ => G)` consists of ALL maps out of the carrier; Awodey's clause is a Set-level statement and `DiscSet A` is exactly that. **Read that at its actual strength**: the carrier fact is measured, but "the two carriers differ" does not by itself entail "not isomorphic as groups", and NO counterexample is exhibited — the unrestricted statement is ARGUED to fail, not PROVED to. `Theory/Lawvere/Model/Closure.v` is the general positive half — a model of a Lawvere theory composed with a finite-product-preserving functor is again a model (`Model_Compose`, with `Model_Compose_fun` recording by `eq_refl` that the composed model's functor IS `K ◯ model_fun M` rather than isomorphic to it) — and **the absence was WIDER than measured**: `CartesianFunctor` and `TerminalFunctor` have NO composition instance anywhere in the tree either, so `CartesianFunctor_Compose` and `TerminalFunctor_Compose` had to be built and are the genuinely reusable half, mentioning no Lawvere theory and noted in-file as movable beside their donor classes. **Universes measured off BOTH the block AND the binder, and `HomGroupData` is this tree's sharpest instance of the rule**: `HomGroupData@{u u0}`'s constraint block is LITERALLY EMPTY — `(* u u0 |= *)` — while its binder reads `forall {C : Category@{u u0 u0}}`, identifying hom with proof by reusing the level variable, so a reader who checks only the block concludes "no identification" and is wrong; `group_object_iff_representable@{u u0 u1 u2 u3 u4 u5}` has NO equation in its block at all, only bounds plus the two strict inequalities `Sets` forces, so the OBJECT universe stays FREE of the hom universe — bounded, never identified — and no `Set` appears in any block of either file. **Read "either file" as `Set`-freedom only, not as "both measured"**: `Theory/Lawvere/Model/Closure.v` carries an identification of its own that the first draft did not disclose — `CartesianFunctor_Compose`, `TerminalFunctor_Compose` and `Model_Compose` each have `u0 = u2`, `u0 = u4` and `u2 = u4`, so a model can only be transported between categories at the SAME hom level, which is a real bound on what that file calls its reusable half; the cause is the donor `Compose` (`Theory/Functor.v`), declared over three categories sharing one hom-and-proof level, and it is NOT claimed unavoidable since no re-annotated `Compose` was attempted. **FIVE CORRECTIONS, and the first two are to this index's own working notes**: the two sites cited in `Instance/Top/ContinuousRing.v` are HEADER PROSE, the definitions being `CRingOb` and `ContinuousRingFunctor` further down; there are THREE `FieldObject` inhabitants, not two — `Q_Field` (`Instance/FdVect.v`), `F2_Field` (`Instance/Field.v`) and the parametric `FracField` (`Instance/Field/Frac.v`) — though the conclusion survives, since none is ℝ, which is why Awodey's field-vs-`C(X)` counterexample is still untypeable and the header says so with THAT reason rather than the issue's stale "reals, topology and fields do not exist in-tree" (all three do; #275 is closed); the product-comparison iso `C(−,c) × C(−,c) ≅ C(−, c×c)` is DEGENERATE in this library — a point of the pointwise product presheaf literally IS a pair of morphisms, so the comparison is `fun (f,g) => f △ g`, inlined at its two use sites and never built as a named natural iso; **Yoneda is not used at all**, every passage being a direct hom-set argument through the naturality field, so no `Curried_Hom` pin is inherited — but read that precisely, since `Yoneda_Lemma@{u u0}`'s identification of object, hom and proof was READ and the pin AVOIDED BY CONSTRUCTION, with no measurement showing it would have blocked this development; and **`Structure/Group/Proofs.v` turned out NOT to be reusable** — zero references — because the hom-set route never needs uniqueness of inverses or the antihomomorphism law, `hmul_inv_l`/`hmul_inv_r` coming straight from the `left_inverse`/`right_inverse` FIELDS precomposed with `f`. **184/184 constants closed under the global context** — `Print Module` lists 182 (173 + 9) and the two unlisted names are the `Build_*` constructors of the two records; the per-file headers say 175/175 and 9/9 for the same reason, the first having been corrected from a draft 172/172 that excluded the very constructors this count includes. Strengths graded: the presheaf/data round trip returns its DATA fields at `eq_refl` (`data_psh_data_mul`/`_unit`/`_inv`) while the WHOLE record does not, diagnosed by measurement rather than guess — `HomGroupData` has primitive projections WITH eta (so eta is NOT the obstruction and record equality IS field equality), and what differs is exactly the opaque LAW fields, pinned as `data_psh_data_law` against a control that is the same law field against itself; the object-level round trip is `≈` not `eq_refl` because of an EXTRA-ARGUMENT residue rather than proof fields, both `rt_` proofs opening with a `change` that SUCCEEDS, so the two sides are convertible down to a final `∘ id` / `∘ (exl △ exr)`. `Test/ProbeGroupRepr341.v` supplies the kind the target cannot state — the target's own four `Fail`s are ALL conversion, so the probe carries 3 FORMABILITY negatives (`Cartesian`, `Terminal`, and the target's own `HomGroupData`, which takes no monoidal argument and so is attributable to itself), each stripped and confirmed a genuine universe inconsistency with zero `cannot unify`, rename-simulated **3/3** unpadded with `Require` lines preserved. **The probe records a trap it was written to avoid**: an earlier revision probed `@GroupObject C M x` under `(M : @Monoidal C)` and labelled it the `GroupObject` donor, which is FALSE — elaboration never reaches `GroupObject`, the error landing at the `C` argument of `Monoidal` (and `GroupObject` takes a `CartesianMonoidal`, not a `Monoidal`, so the line was doubly wrong), the same trap the #340 probe records for `MonoidObject`. NOT delivered: no separation proof, so "strictly more general than `GroupObject`" is NOT claimed and no base lacking finite products is exhibited carrying a `RepresentablyGroup`; no functor `Models T C ⟶ Models T D` and no functoriality in `K`, so the Lawvere closure is OBJECT-level only, with no converse; no named product-comparison iso; no concrete non-degenerate witness, every result being for an arbitrary `c` with `Sets` the only concrete category exercised; and Awodey's negative half is a scoping note rather than a theorem
- **Structure/Bicartesian/Matrix.v**: Mac Lane §III.5 pp. 73-74 (`maclane:III.5:remark1/2`) + Riehl §3.1 (`riehl:3.1:remark27` clause (iii), `def-direct-sums`, `exix`) — **the matrix calculus from coproducts to products, at ARBITRARY index**. `matrix_ext`/`matrix_determined`: two arrows from an indexed coproduct to an indexed product agree iff all their components `p_k ∘ u ∘ i_j` agree — and this is stated over arbitrary index TYPES `J`, `K`, not merely finite ones, which is Riehl's clause (iii). **THE ISSUE SAYS THAT STATEMENT IS "NOT WRITABLE IN TREE TODAY" AND IT IS WRONG**: it rests on `rg -i 'icoprod|IsIndexedCoproduct|HasIndexedCoproducts'` returning 0 hits, whereas the API is right there — `Structure/Limit/Coproduct.v/86/99/103/107/118` declares `IsIndexedCoproduct`, `icoprod_desc`, `icoprod`, `icoprod_inj`, `icoprod_ump`, `HasIndexedCoproducts` (most likely landed by #254), so the domain `∐_{i∈I} A_i` exists and the arbitrary-index statement is what this file proves. **The uniqueness half is not all that is delivered**: `matrix_mor` builds an arrow from a matrix, `matrix_entry` reads its components back, `matrix_mor_entry` closes the round trip and `matrix_ump` packages the correspondence, so both halves are delivered where the issue's Work item asked only that two arrows agreeing componentwise are equal — read that precisely, since what is delivered is `∃!` plus mutually inverse maps at `≈` and there is NO `≅[Sets]` object, unlike the sibling `Structure/Limit/Indexed/Hom.v` which packages the same-family bijections AS isomorphisms; the `.v` header is careful never to use the word "bijection". **The identity matrix needs a DECIDABLE index and the asymmetry is disclosed**: `kron fam dec` is `id` on the diagonal and `zero_mor` off it (`kron_diag`/`kron_off`), giving `can_matrix` with `can_matrix_diag`/`_off`/`_unique`/`_ump` under a `ZeroObject` — but the BINARY statement needs no decider, which the header states rather than leaving the reader to notice. `binary_can_is_can_comparison` ties it to the pre-existing binary `can_comparison` (`Structure/Semiadditive.v`) at `≈`, and the two are provably NOT convertible (pinned), the one being a cotuple of tuples from two descent data and the other the hand-written `(id △ zero_mor) ▽ (zero_mor △ id)`. **Riehl §3.1 Ex ix is the cheap win the tree had never taken**: `indexed_inj_Section`/`_SplitMono`/`_Monic` at arbitrary index, and `inl_Section`/`inr_Section`/`inl_Monic`/`inr_Monic` binarily, all under MINIMAL hypotheses — a zero object plus coproducts, **no `Cartesian`** — with the retraction `id ▽ zero_mor` an instance of `Structure/Cocartesian.v`'s `inl_merge`; the only prior in-tree DERIVED retraction for a `Cocartesian` `inl`, `Structure/Semiadditive.v`'s `exl_can_inl`, additionally assumes `Cartesian` and routes through `can_comparison` — the qualifier is load-bearing, since `Structure/Biproduct.v`'s `bi_exl_inl` is an ASSUMED retraction, a record field rather than a theorem. The finite reading `fin_matrix`/`fin_matrix_entry`/`fin_matrix_ext` CONSUMES #335's `fin_prod`/`fin_coprod` rather than rebuilding them. **Universes read off the constraint blocks, with one load-bearing annotation**: `mat_entry@{uo uh up uj uk}` carries `uh <= up` ALONE — both index universes free of the category's, and the category's hom and proof kept APART — and that is the explicit binders' doing, measured rather than assumed, since written unannotated the same body minimizes to `Category@{uo uh uh}` and is then rejected at `Constraint uh < up` where the annotated form is accepted (pinned). Everything downstream does identify hom with proof, and that is the DONORS' doing, attributed on their own blocks to THREE of them independently — `IsIndexedProduct` over `Category@{u1 u2 u2}`, `IsIndexedCoproduct` over `Category@{u2 u3 u3}`, `ZeroObject` over `Category@{u u0 u0}` — with no attempt to lift them; within that the blocks are BOUNDS not identifications (`matrix_ext` carries seven `<=` and no equation, `kron`'s whole block is the transport's `eq_rect` bounds, `inl_Monic`'s is EMPTY). **62/62 constants closed under the global context**, the count exact rather than a floor — the file has no `Program`, no `Record`/`Class`/`Inductive` and no `Instance`, so nothing exists for a glob sweep to miss, and 24 `Definition` + 19 `Theorem` + 10 `Lemma` + 8 `Example` + 1 `Corollary` = 62 independently; `UIP_dec` (Hedberg) is the only nontrivial stdlib import and is axiom-free. The zero-object results are CONDITIONALS in the target, and that is a CHOICE rather than an unavailability: the tree has SIX registered `ZeroObject` instances and `Ab` supplies both hypotheses as exported instances (`Ab_Cocartesian`, `Instance/Ab/Coproduct.v`), so Riehl Ex ix is witnessed by pure instantiation — shipped in the probe rather than the target because `Instance/Ab` is not in the target's closure (0 of 19 modules) while `Ab/Coproduct`'s own closure is 17, and a `Structure/` file should not nearly double its footprint for two `Example`s. `Test/ProbeMatrix336.v` pins **3 negatives of 2 KINDS** — two CONVERSION (the Kronecker delta is not `id` on the diagonal even at closed indices, the cause located at the DECIDER since branch selection does reduce but the returned equality proof is not `eq_refl`, leaving the `eq_rect` transport stuck; and `binary_can` vs `can_comparison`) and one FORMABILITY (the unannotated `mat_entry` body at `Constraint uh < up`, against three controls) — plus an instrument check, each stripped and its failure KIND read off the error TAIL, rename-simulated **3/3 unpadded** with control-only names explicitly excluded. NOT delivered, and the two deferrals are DIFFERENT KINDS — an earlier revision of this bullet wrongly called both #536's. The general matrix COMPOSITION formula IS owned by **#536** (open), whose QA correction says to "cite it rather than re-proving", so it is cross-referenced and nothing here depends on it. But #336's remaining Riehl box — that the identity-matrix comparison **IS AN ISOMORPHISM** for finite index, with `⊕` the common value — is **#336's OWN work, BLOCKED on #536's `⊕` object** (only the object is transferred: "is #536's first Work item; consume it"), and its own text says "the n-ary case is genuine new work". **#336 states it "cannot close while [these boxes] are unproved", so this work ADVANCES #336 rather than closing it** — the other box of that pair, the arbitrary-index determination, IS delivered here with both halves; no invertibility criterion for `can_matrix` and hence no n-ary semiadditivity; no functoriality or naturality of `matrix_mor`; no transpose or duality. Riehl's abelian-category footnote is recorded as **structurally unprovable as stated in-tree** rather than pursued: `Structure/Abelian.v` carries `abelian_additive` and `Structure/Additive.v,40` carries `additive_biproducts` as DATA, so an in-tree abelian category HAS biproducts by hypothesis and nothing remains to derive
- **Instance/Parallel/Wide.v**, **Structure/Equalizer/Wide.v**, **Structure/Coequalizer/Wide.v**: Mac Lane §III.3's passing remark made precise — (co)equalizers of an ARBITRARY family of parallel maps. `WideParallel I` is the shape (two objects, one non-identity arrow `ParX ~> ParY` per `i : I`), `AWide fs` the diagram. **The obvious generalization is NOT the one taken, and the reason is a measurement**: `Instance/Parallel.v`'s `ParHom : bool → ParObj → ParObj → Set` invites reading its two arrows as already index-parameterized, but THREE of its four constructors (`ParIdX`, `ParIdY`, `ParOne`) sit at the tag `true` and only `ParTwo` at `false` — the `bool` is a DISCRIMINATOR on the hom-set `ParX ~> ParY` doubling as a dummy tag on the identities, not a free index, so substituting an arbitrary `I` leaves the three identity-side constructors with no distinguished element of `I` to sit at and does not typecheck. The wide shape is therefore a different ENCODING rather than a substitution instance, which is exactly why the comparison had to be PROVED. The encoding computes the hom family by matching on the two objects (`hom ParX ParY = I`, so an arrow IS an index by `eq_refl`, and `fmap[AWide fs] i = fs i` likewise); `WParVoid` is a universe-polymorphic empty type declared in-file so all four branches of the hom family sit at one universe by construction — the tree has no polymorphic empty type (measured: `Lib/Setoid.v` supplies the polymorphic `poly_unit`, and all ten `Empty_set` users including `Instance/Zero.v` take the pinned one) — but it is NOT forced, rebuilding the shape with the `Set`-pinned `Empty_set` giving a character-for-character identical constraint block, since `Set` sits below every `Type@{i}`. Named `WideParallel` because `Parallel` is taken by the shape it generalizes and this file Requires it; `ParObj`/`ParX`/`ParY` are IMPORTED, not redeclared, so "the same two objects" is definitional. `IsWideEqualizer`/`IsWideCoequalizer` are literal generalizations of the binary records — one equation-family field (`wfork_eq : ∀ i j, fs i ∘ e ≈ fs j ∘ e`) plus one descent field, no extra data — and BOTH have **EMPTY universe constraint blocks** over `C : Category@{u0 u1 u1}` with the index free. **THE MAIN FINDING: the elementary record and the (co)limit COME APART AT AN EMPTY INDEX.** A cone over `AWide fs` carries TWO legs; with `I` inhabited the second is determined by the first and the cone condition collapses to the fork equations, which is what makes the two presentations agree — but with `I` empty the fork condition is vacuous, so `id[x]` satisfies the record while a limit over the shape is a binary product. So all four round trips (`wide_equalizer_is_equalizer`/`is_wide_equalizer_limit` and the coequalizer duals) take an explicit `i0 : I` — but **necessity is proved for only two of the four**, and the scope is stated rather than blurred: what is refuted is the ELEMENTARY-to-(CO)LIMIT direction at a PINNED apex, so `is_wide_equalizer_limit` and `is_wide_coequalizer_colimit` have `i0` proved necessary while `wide_equalizer_is_equalizer` and `wide_coequalizer_is_coequalizer` merely ASSUME it, no unpointed version of those being refuted. The proof is in three steps: `wide_empty_id_IsWideEqualizer` (the identity satisfies the record over an empty index), `wide_unpointed_round_trip_gives_hom` (an unpointed round trip landing at that same apex, applied to it, manufactures an arrow `x ~> y` from nothing, by reading the limit leg at `ParY`), and `wide_round_trip_needs_point` (instantiate at `_2` with `x := TwoY`, `y := TwoX`, whose hom-set is empty — `False`). The binary donor never meets this because its `fork_legs h ParY := f ∘ h` silently uses the distinguished member. What needs NO inhabitant, holding at every index including the empty one: `wide_equalizer_monic`/`wide_coequalizer_epic`, `wide_equalizer_unique`/`wide_coequalizer_unique`, `wide_limit_equalizes`/`wide_colimit_coequalizes`. `HasWideEqualizers`/`HasWideCoequalizers` are declared but NOT inhabited for any category. Comparison with the binary shape, strongest form pinned: Leibniz equality of the two `Category` records and convertibility of the hom families are both REFUTED (probes, genuine `cannot unify`), while `WideParallel_bool_Parallel : @Isomorphism StrictCat (WideParallel bool) Parallel` HOLDS — both comparison functors identity-on-objects, both round trips at `Functor_StrictEq_Setoid` with `eq_refl` object components — with the `≅[Cat]` reading derived and labelled the weaker (equivalence) one; and since a shape isomorphism says nothing about diagrams, `AWide_bool_APair` separately proves the wide diagram at a two-element family IS `APair f g ◯ Par_of_Wide`, again at strict functor equality. Universes measured in the constraint blocks: `WideParallel@{i p}` carries three constraints where `DiscreteCat` — the tree's other `Type`-parameterized `eq`-hom shape, and the one `Theory/WeaklyInitial.v` already consumes — carries six; the round trips IDENTIFY index with hom universe (`u0 = u1`), and where that enters is MEASURED rather than attributed: with index and hom declared separately, forming the cone and reading `cone_leg`/`vertex_map` yield only the BOUND `u1 <= u0`, as do the files' own `wfork_cone`, `is_wide_equalizer_cone` and `is_wide_equalizer_cone_ump`, so it is NOT inherited from `Structure/Cone.v` — it first appears at `wide_limit_equalizer_desc` and at the `Limit`-record assembly, both local to these files (an earlier draft attributed it to the donor on the strength of an unannotated probe, which collapses the two universes by minimization; whether any rearrangement avoids it is UNTESTED). It costs a consumer nothing, an identification being satisfiable exactly where the bound would be. Engineering finding, the same FAMILY of hazard as `Build_Quiver_Standard_Eq` in Construction/Free/Quiver/Examples.v though neither the same cause nor the same repair (there a monomorphic `eq_equivalence@{u}` pins the PROOF universe and the fix is to spend the polymorphic one; here minimization pins the HOM universe and the fix is explicit binders): written without annotations, minimization pins the shape's hom universe at `Set` (because `bool : Set`) and `Functor_StrictEq_Setoid` then drags the TARGET's hom and proof universes down with it; explicit binders on `Wide_of_Par`/`Par_of_Wide` lift it. **Theory/WeaklyInitial/Wide.v** is the optional item done ADDITIVELY: `initial_from_weakly_initial_wide` runs Freyd's argument over `HasWideEqualizers`, equalizing ALL endomorphisms of the product at once — dropping the power entirely and needing no separate binary-equalizer supply, with `endo_absorb` becoming ONE instance of the fork condition at `u` and `id` where the original needed four rewrites through two tuplings. **`Theory/WeaklyInitial.v` is byte-identical** (`git diff --exit-code`): the existing theorem keeps its statement, proof and universe constraints, GAFT is NOT rerouted, and both theorems stand with neither derived from the other — chosen because rerouting would change hypotheses at GAFT's call sites and that file's header gives an explicit reason (universe over-commitment) for taking its limits as explicit hypotheses. Four negatives (3 conversion, 1 formability) against 10 controls (8 `Check`s and 2 control `Definition`s) in Test/ProbeWideParallel.v, each stripped and confirmed a genuine `cannot unify` or a universe inconsistency naming the declared levels, the formability pair guarded by a positive control at an index universe declared strictly BELOW the hom universe. 34/34 gated constants closed under the global context. NOT delivered: no leg-carrying elementary record that would round-trip unconditionally; no identification of `WideParallel` at an empty index with a discrete category, hence no "limit = binary product" theorem (the sharpness argument goes through an empty hom-set instead); no `WideParallel poly_unit ≅ _2`; no functoriality of `WideParallel` in `I`; no `HasWideEqualizers`/`HasWideCoequalizers` instance for any category, so the new Freyd theorem is a conditional exactly as its donor is; no comparison between the wide (co)equalizer RECORDS at `I := bool` and the binary ones (the shapes and the diagrams are compared, the records are not); no wide analogue of Structure/Limit/Unique.v's leg-carrying essential uniqueness; no preservation/creation; no split/reflexive wide coequalizers; no presheaf reading of the shape
- **Structure/AbCategory.v**: Mac Lane §I.8's Ab-categories, exactly — the intermediate neither neighbour supplied: `AbEnriched` = `Preadditive` (CMon-enrichment) + negation + the inverse law, with NO zero object or biproducts (the reviewer bar; `Additive` bundles both), negation's bilinearity DERIVED by uniqueness of additive inverses (`compose_abneg_left`/`_right`), and `Additive_AbEnriched` reading every additive category down. `AdditiveFunctor` has preservation of `padd` as its ONLY field — `fmap_pzero` ("whence T0 = 0", by the idempotency cancellation `padd_idem_zero`) and `fmap_abneg` are theorems — with identity and composition closure instances. `AbCat` is Mac Lane's category of Ab-categories: sigma objects, functors bundled with the additivity law, hom-equivalence `Functor_Setoid` (the Cat choice, documented). The standalone witness is Ab itself — `Ab_Preadditive`/`Ab_AbEnriched` by pointwise addition and negation of homomorphisms, closing the instance Instance/Ab.v's header records as available but not attempted. The QA increment corrects Construction/Enriched.v's "hand-built Ab-enriched" misdescription (it is CMon-enriched) and points the Preadditive/Additive headers at the new class. The tensor side (Mac Lane §I.8 def 5 + §VII.1): Instance/Ab/Tensor.v builds `AbTensor G H` as an inductive setoid quotient of formal sums by exactly the group laws and bilinearity (Coend.v shape, reflexivity derived), with the `Bilinear` interface, the definitionally-computing `tensor_ump`, the agreement-on-generators engine `tensor_hom_ext`, and `AbTensor_Functor : Ab ∏ Ab ⟶ Ab` (the issue's free-abelian-group machinery deliberately traded for the direct quotient — disclosed; the reusable surface is the Bilinear/ump/hom_ext interface); Instance/Ab/Monoidal.v assembles ⟨Ab, ⊗, ℤ⟩ — integer action `zsmul` by nat iteration (additivity via `Z.pos_sub_spec`), unit `ZAb := ring_ab Int_Ring`, full `Ab_Monoidal`/`Ab_Braided`/`Ab_Symmetric` with every coherence law a generator computation; Construction/Enriched/Ab.v proves Mac Lane's "described completely in these terms": `Enriched_Ab_iff_AbEnriched : @Enriched Ab Ab_Monoidal ↔ { C : Category & (LocallyPropositional C * AbEnriched C)%type }`, both directions constructive and identity on objects/hom-carriers, exercised at `Enriched_Ab_itself` (Ab enriched over itself). An earlier revision of this bullet quoted the right-hand side as `{ C : Category & AbEnriched C }`; measured on 2026-09-17 after the PR "algebraic carriers are sets", an `AbObject` carries `cmon_prop`, so the eight constants of the forward section — `ehom_ab`, `precomp_hom`, `postcomp_hom`, `zsmul_precomp`, `zsmul_postcomp`, `ab_eid`, `ab_ecompose`, `Enriched_of_AbEnriched` — take `{LP : LocallyPropositional C}`. The extra factor is not a hypothesis smuggled in: the REVERSE direction produces it (`LocallyPropositional_of_Enriched_Ab`, `Construction/Enriched/Ab.v`), so the equivalence is the same one with its content made explicit. Universe enablement in Theory/Algebra/Rig.v: Type-ascribed `Z_eqT` and plain-definition `Int_Rig`/`Int_Ring` with explicit binders (minimization had pinned the setoid-proof levels to Set, which would have confined `Enriched Ab` to Set-sized homs); the monoidal instances carry matching annotations, `Ab_Monoidal@{s o} : Monoidal@{o s}` fully parametric
- **Structure/Kernel/Universal.v**: Mac Lane §III.1 remark 4 — the kernel as a UNIVERSAL, packaged so it survives where zero morphisms do not. The "kill-f" presheaf `KillPresheaf f : C^op ⟶ Sets`, `d ↦ { h : d ~> x & f ∘ h ≈ zmor }`, and `IsKernel f i` identified with `⟨k,i⟩` being a universal element of it, over #303's `AUniversalElement`. **Delivered at THREE strengths, deliberately kept apart because they are three different claims.** (1) Universe-free passages `kernel_aue`/`aue_kernel`, both `:=` with NO tactic — the two records' field types are convertible one for one, since `homset[C^op] k d` IS `homset[C] d k` on the nose and the presheaf's `≈` unfolds to a morphism equation — with the underlying morphism surviving both round trips by `eq_refl` and the mediator surviving one direction as the SAME TERM (`kernel_aue_mediator`). (2) Representability: `kernel_Representable` (`repr_obj = k` by `eq_refl`) and `kernel_representation : [Hom ─,k] ≅[[C^op,Sets]] KillPresheaf f` with component `u ↦ i ∘ u` by `eq_refl`, routed through Element.v's DIRECT (Yoneda-free) constructions, plus `kernel_iff_representable`. (3) The bundled `kernel_universal_element_iso`, an isomorphism of setoids in `Sets` — **and this one alone carries a universe restriction, measured and pinned**: `obj[Sets@{o so}]` is `SetoidObject@{o o}`, which IDENTIFIES a setoid's carrier and relation universes, so the bundled form is stated over `C : Category@{u u0 u0}` with `u <= u0` (objects at or below homs) while `Ab@{u u0}` is declared with `u0 < u` — it does NOT instantiate at `Ab`, and its equalizer twin does not reach `Rng`. Both refusals are `Fail`-pinned against a positive control at an abstract `C`; the universe-free forms and `kernel_representation` DO reach `Ab`, and that pair IS the control. This is a DIFFERENT wall from the Yoneda one Element.v records, and the Yoneda-free route does not rescue it; not claimed unavoidable (an annotated variant was attempted and abandoned as the wrong bet for a tree building on 8.19–9.1). **The fork packaging is definitionally TIGHTER than the kill packaging** — the opposite of what the naming suggests: `aue_equalizer (equalizer_aue E) = E` closes by `eq_refl` on the WHOLE RECORD (`IsEqualizer` has primitive projections with eta; `eq_desc` closes by function eta) and the fork-side mediator round trip is Leibniz, whereas the kill-side composites are blocked by the `Qed`-opaque `kill_fork`/`fork_kill` reshaping (`f ∘ h ≈ 0` versus `f ∘ h ≈ 0 ∘ h`); the other fork round trip fails ONLY for want of `sigT` eta, which `aue_equalizer_round_universal` states positively. `IsKernel f i = IsKernelOf f i` and `zmor (ZeroMorphisms_of_ZeroObject Z) = zero_mor` both by `eq_refl`, so everything applies to Structure/Kernel.v's kernels with no adapter. Deliverable 2 at two grades: `ZeroMorphisms` (a chosen family plus two absorption laws — weaker than `ZeroObject`, with `ZeroMorphisms_of_ZeroObject` giving the one direction that IS witnessed; the converse non-implication is true but has NO in-tree witness, and Rng's having neither is a different, weaker statement — disclosed rather than asserted) and, below it, `ForkPresheaf f g` for an ARBITRARY parallel pair with `equalizer_universal_element_iso`, needing nothing at all; `kill_fork_iso : KillPresheaf f ≅ ForkPresheaf f zmor` says exactly how much the zero morphism buys. **Instance/Ab.v's kernel gains its universal property here** (`ab_kernel_IsKernel` — **the tree's FIRST concrete `IsKernel` inhabitant**, and the claim is sourced rather than surveyed: `AbKernel`/`Grp_kernel`/`RModKernel` existed as objects with inclusions but no universal property of any of them was proved, and every other `IsKernel`-typed term in the tree — `Structure/Abelian.v`'s fields, accessors and hypothesis binders, plus two lemma conclusions in `Structure/Kernel.v` — is conditional on `Abelian` or `HasKernels`, NEITHER of which has an instance — `Instance/Ab.v` says it "instantiates NONE of `Preadditive`, `Additive` or `Abelian`" and docs/INHABITATION.md records the same for `image_mediator_epic`. So the issue's "no algebraic category exists to instantiate it" was wrong in the letter and right in the spirit: the categories were there, the universal property was not. A new *Witnessed results* row in docs/INHABITATION.md records the witness and its scope). Witness: `ab_parity : ℤ ⟶ Z/2` whose kernel is the even integers, PROPER and nontrivial (`ab_parity_kernel_has_two`, `ab_parity_kernel_proper`), with `ab_parity_med_computes` evaluating the unique factorization of doubling at 3 to 6 by `eq_refl`. The `Rng` half is a necessity argument, not a generalization for its own sake: `Rng_no_hom_zero_to_Z` shows the hom-set from the zero ring to ℤ is EMPTY (1 ≈ 0 there would force 1 = 0 in ℤ), hence `Rng_no_zero_morphisms` and `Rng_no_zero_object`, and `Rng_fork_presheaf`/`Rng_equalizer_aue` show what survives. Deferred: no cokernel dual (inherits Element.v's own missing couniversal-element class); no uniqueness-up-to-unique-iso in `C` (instantiating `universal_element_unique` lands in `C^op` and the transport is not performed); no `HasKernels` instance; no Grp/RMod instantiation; `ZeroMorphisms` lives in this file rather than its own, disclosed as a move awaiting a second consumer
- **Structure/Abelian.v**: The additive-structure spine — zero objects with the tunnelled `zero_mor` (Structure/ZeroObject.v), UMP-form biproducts (Structure/Biproduct.v), commutative-monoid hom-enrichment with opt-in `addition_scope` (Structure/Preadditive.v), the two semiadditivity theorems (Structure/Semiadditive.v: biproducts compute the enrichment; a bicartesian category with invertible coproduct-to-product comparison is preadditive, by Eckmann–Hilton), additive categories with negation (Structure/Additive.v), the elementary fork API `IsEqualizer` (Structure/Equalizer/Fork.v), kernels/cokernels with normality and the cokernel⇒regular-epi bridge (Structure/Kernel.v), and abelian categories with the full epi-mono factorization (`image_mediator_epic` by the Freyd/Borceux chase) assembled as `Abelian_OFS`; the concrete semiadditive witness is CMon (Instance/CMon.v, Instance/CMon/Biproduct.v — commutative monoids over setoids, universe-polymorphic)
- **Structure/Biproduct/Cartesian.v**, **Instance/{CMon,Ab,Mod}/Coproduct.v**, **Instance/Top/{Coproduct,Wedge}.v**: Mac Lane §III.3's roster of concrete coproducts (+ Awodey §3.2 Examples 3.6/3.10, Riehl §3.1 Ex 3.1.xi) — direct sums, disjoint unions and wedge sums, over a general engine the tree did NOT have. **The engine is the missing direction of an existing pair**: `Structure/Semiadditive.v` supplies Cartesian+Preadditive ⟹ Biproducts, and nothing supplied the converse, so `biproduct_Cartesian`/`biproduct_Cocartesian` do — a category with a zero object and binary biproducts is both cartesian and cocartesian. Its headline is a CONVERSION, not a constructed comparison: `biproduct_prod_is_coprod` records `x × y = x + y = biproduct_obj (biproduct x y)` by **`eq_refl`**, which is the sentence "in a category with biproducts the binary product and the binary coproduct are the same object" machine-checked rather than asserted, with both mediators and all four structural morphisms likewise `eq_refl`. The round trip against Semiadditive is graded rather than claimed uniform: object and **both projections** return by `eq_refl`, the **injections only up to `≈`** (rebuilt as `id △ 0`), with the two strict failures pinned as conversion negatives. Neither is registered as an `Instance` — doing so would close a resolution cycle with `cartesian_has_biproducts`, and the header says so. **THREE roster entries ride it**: `CMon_Cocartesian` (80 lines, ZERO new proof — pure bridge application, and the cleanest demonstration that the engine is general), `Ab_Cocartesian` and `RMod_Cocartesian`. **Two more are built DIRECTLY and provably cannot ride it** — the engine takes a `ZeroObject` and `Instance/Top.v`'s `Top_no_zero_object` refutes one for `Top` — namely `Top_Cocartesian` (disjoint union) and `Top_pointed_Cocartesian` (wedge sum), each assembled as a `Build_Cartesian` on the opposite category with no `Biproduct` module required at all. **The transfer from CMon to Ab is far cheaper than a reader would guess, and the file corrects its own first draft on the point**: `AbHom A B := CMonHom A B` is a bare `Definition` (`Instance/Ab.v`), so Ab's arrows ARE CMon's, and `@zero_mor Ab Ab_Zero M N` is `@zero_mor CMon CMon_Zero M N` by `eq_refl` (`ab_zero_mor_is_cmon_zero_mor`, the reason being that `Ab_Zero`'s `one`/`zero` fields are literally `CMon_one`/`CMon_zero_hom`) — so **ten of the biproduct record's eleven fields transfer by `:=` with no tactic**, only `biproduct_obj` being new. An earlier draft claimed those laws "do not transfer" and were "not even well-typed here"; that was false and is named as false in-header. At R-Mod the transfer is graded in three: **data** must be rebuilt (record wrapper plus one `rm_map_smul` obligation each), **laws** transfer unchanged, and **`∃!`-packages** must be repackaged though not re-argued — that last being a genuine formability failure, pinned. Awodey's `O(X+Y) ≅ O(X) × O(Y)` is `eq_refl` (it IS the definition of `sum_open`), and "the disjoint union of discrete spaces is discrete" is proved as a biconditional of the two openness predicates rather than as an isomorphism. The two topological entries are proved DISTINCT objects rather than assumed: `wedge_basepoints_identified` against `wedge_is_not_sum`, the same two points being provably distinct in `Sum_Top`. Non-vacuity throughout — ℤ⊕ℤ computes by `eq_refl` (merge, projections, scalar action), injections provably differ, wedge non-basepoints provably not glued. Universes measured in the constraint blocks with levels declared APART (not by minimization): `Cartesian` IDENTIFIES hom and proof and `biproduct_Cartesian` inherits that from `Cartesian`'s signature — but the CAUSE is minimization, not `Terminal`, and an audit refuted the causal reading: a one-field class `{ t1 : obj }` over an unannotated `Context {C : Category}` with no `Terminal` or `Cartesian` in scope acquires the same `Category@{u u0 u0}`, and the field shape re-elaborates at `Category@{uo uh up}` when the levels are declared apart, so the content never required it; the `Terminal` negative records shared shape, not causation; `Sum_Top`/`WedgeSum` carry `u = u0`, a genuine identification forced by the glued carrier being one `SetoidObject@{o o}`. **A donor `Set` pin is LOCATED rather than guessed and deliberately NOT repaired**: `Instance/Ab.v`'s `Ab_trivial` is declared with zero universe binders at `AbObject@{Set Set Set}` while `Instance/CMon/Biproduct.v`'s `CMon_trivial@{o}` is polymorphic, so everything through `Ab_Zero` inherits it and `RMod_trivial` propagates it to R-Mod — the evidence being that `CMon_Cocartesian` is `Set`-free while `Ab_Cocartesian` is not, though the direct-sum OBJECTS on both sides are free; same family as the `Grp_trivial` pin recorded above for Instance/Grp/Quotient/Colimit.v (a pin #450 has since lifted for `Grp_trivial`, now `Grp_trivial@{p} : GrpObject@{p p p}` by `About`; `Ab_trivial@{} : AbObject@{Set Set Set}` is unchanged). **SCOPE IS PRINCIPLED, NOT ARBITRARY**: delivered are exactly the coproducts computed from the underlying data of the summands; deferred are the two needing a NEW CARRIER by generators and relations — the Grp free product and the CRng tensor (the former since built twice, by generators and relations in `Instance/Grp/Pushout.v`'s `Grp_Cocartesian` and by the adjoint functor theorem in #450's `Instance/Grp/Colimit.v`) — which is the asymmetry `Structure/Cocartesian.v`'s own header quotes from Mac Lane 1950 ("the proof that the direct product exists is not dual to the proof that the free product exists"). Nine negatives against twenty-eight positive commands, each stripped once and its failure KIND confirmed, with the pairing audited so every negative's constants appear in a succeeding control; among them a compiled obstruction showing that ONE natural proof-relevant encoding of a product topology does not fit at `Type@{o}` — which is weaker than "no `Cartesian Top`", since the impredicative-truncation idiom of `Instance/Sets/Powerset.v` elaborates a squashed variant and is not investigated. A false pass was caught in the probe's own construction and recorded: a wedge negative first written against the non-existent name `WedgeSum_Top` passed for "reference not found", found only by stripping the `Fail` and reading the error. Two corrections to the surrounding prior-art picture, both compile-verified: **`Rel_Cocartesian` is NOT live** — `Instance/Rel.v` sits inside a comment that also swallows `Rel_Cartesian` — and `Instance/Proset/Limit.v`'s `Proset_Cocartesian` IS live (a `Program Definition` taking join data, not a registered instance). 35/35 gated constants closed under the global context. NOT delivered, scoped to this slice: the Grp free product and the CRng tensor as above; Riehl §4.6.6's `F(X ⊔ Y) ≅ F X ∗ F Y`, which needs both of those AND a `Cocartesian`-level left-adjoint-preservation bridge the tree lacks (only general LAPC at `Colimit` level exists, and the dual of `Structure/Limit/Cartesian.v`'s product half is missing, which `Instance/Two/Discrete.v` asserts in prose); no indexed or infinite coproducts (all binary, matching `Cocartesian`); no `Cartesian Top` or `Cartesian Top_pointed`; no initial object of `Top_pointed`, so the wedge's unit laws are unavailable though `coprod_comm`/`coprod_assoc` are exhibited; no `Additive`/`Abelian` for Ab or RMod; no smash product or descent of the wedge to `Toph_pointed`; and Awodey's poset clauses of Example 3.6 (juxtaposition, rooted posets) are untouched, only the pointed-SPACE reading being delivered
- **Structure/Pushout/Split.v**, **Instance/Grp/Pushout.v**, **Instance/Top/Pushout.v**: Mac Lane §III.3 p. 66 — pushouts in `Grp` and `Top`, and the amalgamated product. **Both substrates delivered**, `Grp_HasPushouts` and `Top_HasPushouts`, neither dominating the other (≈739 and ≈577 lines, cores ~250 each). On the `Grp` side the PRESENTATION route costs almost nothing — every group law is a CONSTRUCTOR of the congruence, so `AmalgamGrp` is a record literal with ZERO proof obligations and `am_eval` is a `Fixpoint`, making the mediator's homomorphism laws close by `reflexivity`; the UMP's price is two inductions (twelve cases over the congruence, five over the word), with two more outside it — `am_eq_refl` and `Grp_Cocartesian`'s `fork_respects` — so four in the file. **It subsumes the free product**: `Grp_free_product` is an INSTANCE of the amalgamated product and `Grp_Cocartesian : @Cocartesian Grp` is built and registered, which discharges part of what #1194 owns — and both injections are **split monic with no hypothesis at all** — but read that precisely: what is discharged is the strictly weaker FACTORIZATION hypothesis (`Grp_fp_inl_Section` routes through `am_inj1_Section_of_factor`, not `..._of_Retraction`), and Mac Lane's own monic-legs hypothesis is FALSE for that span, its legs being CONSTANT maps and so not monic into a nontrivial group; this is his CONCLUSION by a different, weaker route, not an instance of his refinement. **The universe route matters and the first attempt was refuted by its own measurement**: building the free product through `Grp_Zero` (the obvious route, and the one I suggested) yields `GrpObject@{Set Set Set}` — `Instance/Grp.v`'s `Grp_zero_hom` is itself declared there, the donor defect `Instance/Grp/Quotient/Colimit.v` already records — so a `Cocartesian Grp` on that route would cover **Set-sized groups only**, materially weakening the subsumption (true of the tree when written; since #450 the donor's universes are written out in place — `Grp_zero_hom@{u p}`, `Grp_Zero@{u p}` — so that route would no longer inherit a pin, and the constant-leg construction stands as it is). Routing instead through the amalgam along CONSTANT legs needs no zero object (the cocone condition is automatic, every hom preserving the unit) and gives `Grp_free_product@{u u0}` and `Grp_Cocartesian@{u u0}` with **no `Set` anywhere**; `Grp_zero_hom_Section` is kept as the honest record of the route not taken, with the pin pinned as a formability negative (since #450 the probe's two formability negatives are its CONTROLS 9 and 10, the lifted pin guarded in the other direction, and `About` reads `Grp_zero_hom_Section@{u u0}` over `GrpObject@{u0 u0 u0}`). **THE MONIC-LEGS REFINEMENT IS AT SPLIT STRENGTH ONLY, AND THE GAP IS STATED RATHER THAN BLURRED**: `pushout_both_Monic`/`Grp_split_legs_monic_injections` prove that split legs give monic injections, by feeding the cocone `(id, f ∘ retraction)` to the UMP — a generic argument, which is why it lives in `Structure/Pushout/Split.v` and applies to `Top` verbatim. For merely MONIC legs this is the Schreier normal-form theorem, whose usual (van der Waerden) proof builds an action on alternating normal forms and must CHOOSE coset transversals; that is not attempted, the split hypothesis is NOT claimed necessary, and no in-tree monic-but-not-split group hom is exhibited, so the strictness of the gap is ARGUED, not proved. A second honesty point sits beside it: **"the vertex is the amalgamated product" is DEFINITIONAL here, not a theorem** — the vertex IS the presented group and nothing relates it to an independent definition. On the `Top` side the pushout is the quotient topology on the disjoint union, glued by an inductive closure in the SHAPE of `Instance/Sets/Pushout.v`'s `pushout_eq` — same five constructors, RESTATED for spaces, that file not being `Require`d, so a structural copy rather than reuse; `adjunction_glues` is the motivating adjunction-space case. **A prior-art correction that inverted the expected cost**: the tree DOES carry quotient-topology donors — `Instance/Top.v`'s `CP_open` (the cokernel-pair space) and `Instance/Top/Wedge.v`'s section literally headed "The quotient topology" with `wedge_open` — so the `Top` half is those two with the relation generalized from a closed-form `match` to the inductive closure, and was CHEAP rather than expensive. (An earlier measurement of mine reported no such donor; that was produced by truncating an absence search with `head -5` when it had thirteen hits, and the donors were below the cut. The constants are named `CP_open`/`wedge_open`, not `*quotient*`.) **#862's boundary is respected, not claimed**: the general "pushout over the initial object IS the binary coproduct" is NOT built; both files carry a cross-reference paragraph naming #862 as owning it and stating that what is built is the ad-hoc instance with the commutation hypothesis discharged once, leaving literally the coproduct UMP — `Top_pushout_empty_iso` is the same move on the topological side, and is an `≅[Top]` rather than an identity, two conversion negatives showing the glued setoid and the glued topology both differ from `Sum_Top`'s. Strengths graded: apex, both injections, carrier and all three group operations at **Leibniz `=`** on the `Grp` side, apex and both injections likewise on the `Top` side, with `pushout_med` vs the named mediator only `≈` — diagnosed to DONOR OPACITY, `Structure/Pushout.v` stating `pushout_ump` with `Qed`, and pinned identically on BOTH substrates so the cause is visibly the donor rather than either instance. Universes measured with causes probed rather than attributed: `AmalgamGrp`'s binder displays three free object triples while its constraint block collapses all nine, but the cause is NOT the amalgamation — the bare `fun (A B : GrpObject) (f : @hom Grp A B) => B`, which mentions no pushout, already elaborates at `GrpObject@{u0 u0 u0}`, so merely NAMING a `Grp` hom is what identifies them; `Pushout_Top`'s `u = u0`/`u = u1` is an identification with the same probed cause, and no `Top` constraint block contains `Set`. Non-vacuity on both substrates and in both directions: `fp_generators_distinct`/`fp_inl_injective` by mapping OUT into `Z2` (no induction on a generated congruence can yield a negative) against `amalgam_over_Z2_merges`, which merges the very pair the free product keeps apart; and `empty_span_keeps_summands_apart` — via a `tp_side` invariant, since `tpr_trans` passes through arbitrary intermediates and plain induction will not do it — against `point_span_merges`. Six negatives against eleven controls (eighteen positive commands; since #450 four against thirteen by `grep -cE 'CONTROL [0-9]+' Test/ProbePushoutGrpTop.v`, 11 → 13 counting the labels 2b, 3b and 4b, with twenty positive commands, the two formability negatives having turned controls — guarded commands 7 → 5 with the instrument by `grep -c '^Fail' Test/ProbePushoutGrpTop.v`, positive commands 18 → 20 by `grep -cE '^(Check|Example|Definition|Lemma|Theorem|Goal|Compute|Print)'` on the same file), **with the pairing verified by RENAME SIMULATION — 25 constants by the author, and an audit independently renamed 31 with all 31 breaking at a control and none going vacuous**; that exercise FOUND three genuine pairing gaps (`pushout_med`, `Top_po_med`, and `IsOpen`/`Sum_Top` with `Grp_free_product`/`Grp_fp_inl`) where a rename would have been swallowed, and the controls closing them exist for that reason. **16/16 gated constants closed under the global context — read that precisely: 144 constants were verified once by `Print Assumptions`, and the SIXTEEN principal artifacts are what the `make print-assumptions` gate carries permanently.** `Theory/Morphisms/CokernelPair.v`'s count of `HasPushouts` instances is corrected in the same commit from three to five, naming the two new ones and recording that the count read three when that note was written. NOT delivered, scoped: on the `Grp` side the Schreier/normal-form refinement for merely monic legs, any normal form or decision procedure for `am_eq`, HNN extensions, Bass–Serre theory, functoriality of the pushout in the span, and any comparison with `Instance/Grp/Free.v`'s free group; on the `Top` side any separation or compactness of the glued space (nothing about when an adjunction space is Hausdorff), CW/cell attachment, mapping cylinders or cones, a free-standing general quotient-topology construction, indexed pushouts, `Cocomplete Top`, a pointed variant, and — though `Structure/Pushout/Split.v` applies verbatim — no `Top` span whose legs split is exhibited, so there is no `Top` monic-legs instance; and across both, no van Kampen theorem, hence no bridge between the two new files and no connection to `Instance/Top/FundamentalGroupoid.v`
- **Structure/Factorization.v**: Orthogonal factorization systems — `MorphismClass` vocabulary (Theory/Morphisms/Classes.v), orthogonal lifting `e ⫫ m` with left-class closure (Theory/Orthogonality.v), `Factorization`/`OFS` with uniqueness up to unique iso and the Fact.v comparison, strong epis (Factorization/StrongEpi.v: composition, cancellation, split ⇒ strong, strong+mono ⇒ iso); pullback pasting/stability toolkit in Theory/Morphisms/Stability.v (apex-pinned `IsPullback`, pasting both ways, monos/isos pullback-stable, `pullback_transport`); elementary cofork API `IsCoequalizer` with Colimit conversions in Structure/Coequalizer.v; regular categories in Structure/Regular.v (kernel pairs, `RegularEpi`, stability field) with the (RegularEpi, Mono) image factorization `Regular_OFS` in Regular/Factorization.v (double-cover monicity argument); concrete images in Instance/Sets/Image.v
- **Structure/Projective.v**: Mac Lane §V.4 Definitions 3-4 (book p. 118, `maclane:V.4:def3`, `maclane:V.4:def4`) + Awodey §2.4's prose definition (`awodey:2.4:def-projective`) — **projective and injective objects, with the hom-functor characterization in BOTH directions, orthogonality, closure under retracts and (indexed) coproducts, computing witnesses, and the price of "every object is projective"**. `Projective p` is a `Class` like `Epic`/`Monic`, one field `projective_lift : Epic g → (f : p ~> c) → ∃ h : p ~> b, g ∘ h ≈ f` — the lift is DATA and there is no other option: `∃` IS `sigT` (Lib/Foundation.v) and a Prop-valued existential over `≈` cannot be stated, `≈` being Type-valued (probe N6). **`Injective C` is a NOTATION for `@Projective (C^op)`** (the Structure/Initial.v idiom) with covariant accessors `injective_extend`/`inj_extend`/`inj_extend_comm` through Theory/Morphisms/Duality.v's `op_Epic_of_Monic` — `Monic` in C and `Epic` in `C^op` are DISTINCT records (probes N4-N5) though the objects and homs of the duality convert; `inj_is_op_proj`, `inj_stmt_readback` at `eq_refl`. **Hom characterization**: `proj_is_hom_surjectivity` (`eq_refl`: "surjections of hom-setoids" is the definition unfolded), `projective_hom_surjective`, `projective_hom_epic`, `hom_epic_projective` and the pinned **`projective_iff_hom_preserves_epi`** — both directions constructive through Instance/Sets.v's `surjectivity_is_epic` (`surjective_implies_epic`/`epic_implies_surjective`), which the issue does not mention; dually `cohom_is_op_hom` (`eq_refl`), `injective_cohom_epic`, `cohom_epic_injective`, **`injective_iff_cohom_preserves_mono`**, each a `:=` at `C^op` plus one bridge. **Orthogonality**: `ortho_lift_exists` (Theory/Orthogonality.v's `Orthogonal` with uniqueness dropped), `ortho_zero_Projective` and `Projective_ortho_weak` over an `Initial` object (the arrow is `zero : 0 ~> p`); no relation to `StrongEpi` claimed. **Closure**: `Retraction_Projective`, `Section_Projective`, `Coprod_Projective`, `IndexedCoprod_Projective` (the lift chosen at each index by `icoprod_desc` — the case that shows why the lift must be data), duals `Prod_Injective`, `Section_Injective`, `IndexedProd_Injective`, `terminal_obj_Injective` as `:=` instantiations (`op_op_Cartesian : @Cartesian C = @Cocartesian (C^op)` and `op_Coprod_is_product` at `eq_refl`). **Witnesses**: `initial_obj_Projective`, `Retraction_lifts`/`every_epi_splits_Projective`, **`Sets_Leibniz_Projective : ∀ T, Projective (LeibnizSetoid T)`** (every Leibniz setoid is projective in `Sets`, constructively — the preimage is `epic_implies_surjective`'s, respectfulness free), and on the walking arrow `TwoX_Projective` with `TwoY_not_Projective` packaged as `two_projective_not_all` (neither vacuous nor universal); the three witnesses' lifts COMPUTE (probe readbacks at `eq_refl`, which make those three `Defined` load-bearing). **The price**: `Projective_codomain_splits`, `all_projective_iff_every_epi_splits`, and **`sets_all_projective_entails_LEM : (∀ p : Sets, Projective p) → ∀ P : Prop, P + (P → False)`** through Instance/Sets/Regular.v's `BlanketSplitting` — "every set is projective" decides every proposition, so Awodey's examples of that shape (`awodey:2.4:example15`, `awodey:2:ex3`) cannot be discharged constructively. **Stale premises**: "no projectivity/injectivity notion" TRUE (0 declarations) but the issue's counts are off ('projective' 9 prose lines in 6 files — projective limits, a free module, projective geometry, a Banach tensor product, "enough projectives"; 'injective' 437 lines in 100 files, all but Structure/Abelian.v's "injective resolutions" the setoid-map notion — `Lib/Setoid.v`, `injectivity_is_monic`, `InjectiveOnObjects`, …); "no lifting property beyond Orthogonality.v" PARTIAL (`StrongEpi`'s `strong_lift` and `ortho_left_cobase_change` route through it; every lifting statement in the tree carries uniqueness); "no hom-functor-preserves-epis lemma" TRUE (only `faithful_reflects_epic`). **Universes**: `Projective@{u u0}` over `C : Category@{u u0 u0}` with an EMPTY block — hom = proof, inherited from `Epic`/`Monic` (probes N1-N3 refuse all three alike at `vh < vp`, the category, its homs and an arrow accepted), every constant over a `Category@{a b b}` binder with no equation; the Sets characterizations at `u2 < u1` (Sets' object level above C's hom); `Sets_Leibniz_Projective@{h p}` with `p < h` and stdlib `eq`/`prod_rect` bounds; `Set` only in the four `_2` witnesses (`_2` is declared bare, Instance/Two.v) and in `sets_all_projective_entails_LEM@{u}` (`Set < u`, from `bool_setoid_object` — the entailment holds already at `Set`-sized carriers). **48 constants closed under the global context, zero `Axioms:` lines** — 45 `.glob` heads (40 `def`, 3 `prf`, the class and its projection) plus 3 `Program` obligations; the gate carries the 45; twelve `Defined`, three LOAD-BEARING (`Retraction_lifts`, `initial_obj_Projective`, `Sets_Leibniz_Projective`; flipped to `Qed` the probe's lift readbacks are refused), nine not and kept transparent as lift-producers; two `Qed`; closure 34 excluding self (Structure/Limit/Coproduct.v 8 at the margin, Functor/Hom.v 3, Duality.v 2, four files 1 each, ten `Require`s 0; `[Hom p,─]` used directly since `HomFrom` would add 23 files — an earlier revision said 12); `two_X_initial : @Initial _2` DUPLICATES Theory/Equivalence/Strict.v's `Two_Initial`, whose `Require` would add 17 files — consolidation into Instance/Two.v surfaced, not done; `Projective` occurs as a word in Structure/Limit.v's prose ("projective limit"), no other name anywhere else. **Name hazard recorded**: the `@Injective C` notation claims the token `@Injective`, as `@Initial C` and `@Cocartesian C` claim theirs — no constant starting with `Injective`, `Initial` or `Cocartesian` can be written with `@` while those notations are in scope (probes N7-N8; `Terminal`/`Coproduct` prefixes are NOT blocked — an earlier revision listed them). `Test/ProbeProjective429.v` mirrors the `Require` list and carries **9 refutation commands = 1 instrument + N1-N3 UNIVERSE + N4 CONVERSION + N5-N6 TYPING + N7-N8 PARSING**, each stripped one at a time in a copy of the whole file beside its accepted controls; ten `eq_refl` readbacks (an earlier revision said eleven); guard coverage 28/22 with six exhaustive exceptions (31/25 before the probe's `Corelib.Init.Logic.ex` — unknown to Coq 8.19/8.20, whose prelude is `Coq.` — became the unqualified `ex`); rename-simulated 5/5 over the library names the negatives use, every first break on a positive line (the parsing placeholders are probe-local, so their rename is uninformative); `make todo` grows by those 9 lines only (2237 → 2246 over f86ebdbe, master 1de68608 with #428), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: enough projectives, resolutions, injective hulls, Baer's criterion (since #454 its case at the ideals of ℤ, into ℚ/ℤ, is `Instance/Mod/Cogenerator.v`'s `QZ_extends_along_mul`; the general criterion is still absent, and the same file's `QZ_injective_WLEM` shows that ℚ/ℤ's injectivity in `Ab` entails the informative weak excluded middle); free modules projective (Instance/Mod/Free.v); any `StrongEpi` relation; "every set is projective" (refuted as a constructive goal); no `Instance` declared; no edit to Theory/Morphisms.v, Theory/Morphisms/Duality.v, Theory/Orthogonality.v, Instance/Sets.v, Instance/Sets/Regular.v or Instance/Two.v.
- **Structure/Dagger.v**: Dagger categories — the light `DaggerCategory` class (involution + `dagger_id` + the `dagger_compose` anti-homomorphism law, with `†` notation), the common vocabulary the ZX and compact-closed prose had been citing without a definition; deliberately minimal, no monoidal compatibility demanded. First instance: Instance/Rel/Dagger.v — Mac Lane §I.7's converse relation and Awodey §1.9 Exercise 2 resolved: `Rel_Dagger`; the converse as an identity-on-objects functor with the self-duality at BOTH strengths — `Rel_self_dual_strict : Rel ≅[StrictCat] Rel^op` (the genuine isomorphism of categories Awodey asks for, free by strict functor equality) and the `Rel_self_dual : Rel ≅[Cat] Rel^op` equivalence reading, the Groupoid/Inversion.v two-tier presentation (Awodey (a), affirmative); `Relation_Functor_Faithful` (functions with pointwise-equal graphs are equal — Coq genuinely a WIDE subcategory of Rel, closing Rel.v's comment-only claim); `Rel_Terminal`/`Rel_Zero` completing the zero-object story Rel.v recorded as missing; and Awodey (c) `Powerset_self_dual : Powerset X ≅[Cat] (Powerset X)^op` by complementation over DECIDABLE (bool-valued) subsets — the constructive transposition, disclosed: the Prop-valued complement is an involution only up to double negation (Top/Closed.v's `pred_comparison_forces_DNE` precedent), while the bool round trips close by case analysis, axiom-free. Awodey (b) — Sets is NOT self-dual — is PROVED (`Sets_not_self_dual`): a Cat-isomorphism repackages as an equivalence (`Cat_Iso_to_Equivalence`), hence full+faithful; the empty Sets(1, ∅) read through fullness empties carrier (F 1), and faithfulness then collapses Sets(1, 2)'s two points into vacuously equivalent parallel pairs — true = false. All three parts of the exercise answered: (a) yes, (b) no, (c) yes
- **Structure/Coend.v**: The coend calculus — covariant coend accessors (`coend_obj`, `coend_inj`, the cowedge condition, `coend_ump`/`coend_med`/`coend_med_eq`, and the smart constructor `Build_Coend`) over the definitional `Coend F := @End (C^op)(D^op)(F^op)` (`Coend` itself, like `End`, is declared in Structure/End.v); the end and coend in `Sets` funext-free (Instance/Sets/End.v as the subsetoid of compatible families, Instance/Sets/Coend.v as the inductive setoid quotient by exactly the dinaturality relation); the ninja (co-)Yoneda reductions and the Fubini interchange (Theory/Coend/{Yoneda,Fubini}.v); profunctors `C ⇸ D := C^op ∏ D ⟶ Sets` with representables, composition by the coend `∫^d P(c,d) × Q(d,e)`, the unit/associator laws, and `representable_adjunction : (F ⊣ U) ↔ (Repr_left F ≅ Repr_right U)` (Theory/Profunctor.v, Construction/Profunctor/{Compose,Laws}.v, Theory/Profunctor/Adjunction.v); Day convolution `∫^{a,b} C(a ⨂ b, -) × F a × G b` with unitors and associator (Construction/Day.v)
- **Structure/Groupoid.v**: Groupoids as a *property* — `IsGroupoid C := ∀ x y (f : x ~> y), IsIsomorphism f` (Mac Lane §I.5 Definition 9; Awodey §7.7; Riehl Definition 1.1.12), the predicate the tree previously lacked: `Construction/Groupoid.v`'s `Groupoid C` is the *core construction*, and `core_is_groupoid` is the first proof that it satisfies a groupoid property. A plain `Definition`, not a `Class`, and never registered for resolution (the Theory/Equivalence.v rule — it carries a *chosen* inverse), with the `ginv` calculus (`ginv_unique_r`/`ginv_unique_l` from `comp_inverse_unique`, `ginv_respects` derived rather than assumed, involution/identity/composite, and `ginv_choice_irrelevant`: any two `IsGroupoid` structures agree up to `≈`). Groups ARE one-object groupoids, as a biconditional: `deloop_groupoid_iff : MonInverses M ↔ IsGroupoid (Deloop M)` (Fong–Spivak §3.2.5 Exercise 3.32), with both witnesses PROVED — `deloop_nat_not_groupoid` exhibits the arrow `1` of `B (ℕ,+)` as non-invertible, `deloop_bool_groupoid` is `B (Z/2)` — and `vertex_group G x : GrpObject` for the converse, round-tripping with `Deloop` on the nose in both data fields. `conjugation_iso G f : MonIso (vertex_group G x) (vertex_group G x')` is Mac Lane's conjugation, over locally defined `MonHom`/`MonIso` (a group homomorphism needs no separate class: `MonHom_grp_inv` derives preservation of inverses). **Structure/Groupoid/Connected.v** adds `ZigZag`/`Connected` in Riehl's §1.5 finite-zig-zag form — with `Fence` (strictly alternating spans) interderivable with it, and `zigzag_hom`/`hom_zigzag` collapsing it to the one-arrow form for groupoids — and the structure theorem `connected_deloop_equiv` (Riehl Proposition 1.5.13): a connected groupoid is equivalent to `B (hom(x,x))` at any object x, through `vertex_incl` (fully faithful with no hypothesis, essentially surjective by connectedness). The hypothesis is proven NECESSARY: `connected_iff_deloop_equiv` is a biconditional, and `Two_Discrete_no_deloop_equivalence` refutes the conclusion for the disconnected 2-object discrete groupoid. Non-vacuity: `WideDeloop M A` (the delooping widened over an object type A) gives the 2-object connected groupoid `Bool_Wide` with vertex group Z/2 at which the theorem is instantiated; `Roof_Connected` + `Roof_not_arrow_connected` prove the zig-zag form STRICTLY weaker than the one-arrow form outside groupoids. **Structure/Groupoid/Inversion.v** is Riehl Example 1.3.14: the identity-on-objects inversion functor and `Inversion_iso : C ≅[StrictCat] C^op` — stated in `StrictCat` because `≅[Cat]` in this library IS equivalence, with `Inversion_Cat_iso` the weaker derived form; the Z/2 instance is flagged degenerate (every element is its own inverse) and `Z3_Grp` is added so `Z3_inversion_not_identity` exhibits the functor actually moving an arrow. **Structure/Groupoid/Basepoint.v** turns the structure theorem into a group isomorphism: `Full_Compose`/`Faithful_Compose`, then `deloop_ff_moniso` — a fully faithful functor between one-object categories IS a monoid isomorphism, the content being that `Full`'s `prefmap` is only a section with no functoriality demanded, so that it is a homomorphism must be proved, and faithfulness is what proves it — and `deloop_equiv_moniso`, `connected_vertex_moniso` (the vertex groups at any two objects of a connected groupoid are isomorphic), instantiated at `Bool_Wide_vertex_moniso`. That instantiation is honest about its own weakness: `WideDeloop` fixes the hom type uniformly (`hom := fun _ _ => carrier M`), so the vertex groups at the two DISTINCT objects are the SAME monoid — `Bool_Wide_vertex_groups_agree` records it by `eq_refl` and `Bool_Wide_vertex_moniso_trivial` inhabits the same type with the identity maps alone — so the witness exercises the two HYPOTHESES and the derivation, not the conclusion; no in-tree object does better (docs/INHABITATION.md carries the note). The longer route is taken deliberately over `conjugation_iso`'s short one; the two words are kept in their exact senses (the structure theorem gives an equivalence of categories, what comes out is a bijection of underlying setoids preserving unit and multiplication). Zero axioms: all 27 constants report "Closed under the global context", and the file is wired into the `print-assumptions` make target alongside its `Structure/Groupoid` siblings

## Constructions (External Combinators)
- **Construction/FreeMonoidal.v**: The free monoidal category on one generator — Mac Lane §VII.2 in full (`maclane:VII.2:def1/construction1/thm1`). Binary words `Word` (exported standalone for the §VII.2 coherence corollary and §XI word-functors), the category `W` with hom the length equation and a constantly-true hom-setoid (Instance/Proset.v's thin-category device), so `W_thin` discharges every categorical, bifunctorial and monoidal law uniformly — the file contains no coherence argument, and `wlen`'s structural `+` makes the left unitor literally `eq_refl`. FreeMonoidal/Normal.v is the engine room via the strictification/NbE route (Beylin–Dybjer, Schauenburg; disclosed — the statement is Mac Lane's, the route is not his rank induction): the tail-parameterised `nf`/`J`/`subst`/`can` all typecheck by conversion with no transport in any definition, every cast has the single shape `id_cast (f_equal (nf -) e)` over a nat equality (so cast-UIP is Hedberg, axiom-free, and no `ObjDecEq` on the target is needed), and the two theorems where the target's coherence is spent are `J_assoc` (the pentagon) and `J_right_unit` (triangle + Kelly). FreeMonoidal/Universal.v proves Theorem 1: `Subst` with `Subst_tensor` (Mac Lane's `T(f□g) = Tf□'Tg`), the three strictness equations, `Subst_Monoidal` (strong) and `Subst_Strict` — both object equations `eq_refl` ON THE NOSE, witnessed by Examples — and the uniqueness triad `JW_unique`/`canW_unique`/`Subst_unique` with the package `FreeMonoidal_universal`: object agreement `Theta` is DERIVED from strictness plus the single datum `G WI = b` (where the PROP precedent takes the whole family as a hypothesis), and arrow agreement holds for every arrow by thinness decomposition through the normalisers. The morphism notion is STRICT because that is Mac Lane's: his Moncat has strict morphisms as arrows (`maclane:VII.1:construction2`), and uniqueness among strong monoidal functors is false; uniqueness up to monoidal natural isomorphism needs a class the tree lacks (ledger 19)
- **Construction/Deloop.v**: Delooping — Mac Lane §I.2 constructions 3 and 4, the monoid/one-object-category dictionary. `MonObject` is a bare setoid monoid (carrier + unit + op + `Proper` + the three laws), deliberately NOT `Theory/Algebra/Monoid.v`'s internal monoid object, since the construction needs an *element*-level multiplication and nothing about an ambient monoidal category; it is `Instance/CMon.v`'s `CMonObject` minus commutativity, so it carries both unit laws as fields rather than deriving one. `Deloop M : Category` has `obj := poly_unit` (universe-polymorphic, so the object universe stays free rather than pinned at `Set`) and `hom _ _ := carrier M`, composition the multiplication. The converse is `hom_monoid C a : MonObject`, the endomorphism monoid of any object, and the two compose definitionally: `hom_monoid_Deloop : hom_monoid (Deloop M) ttt = M` holds by `eq_refl`. (The opposite composite has no equational form — `Deloop (hom_monoid C a)` is one-object while `C` need not be — and the honest statement, that it is the full subcategory on `{a}`, is functor-level and deferred with the §I.3 dictionary.) `GrpObject` extends `MonObject` with a two-sided inverse, and `Deloop_group_invertible` makes every arrow of `Deloop G` invertible, with `to` the arrow and `from` the group inverse — a one-object groupoid. (The companion `Deloop_group_iso` has a vacuous *type*, since `obj[Deloop G]` is a singleton; the content is in `Deloop_group_invertible`.) `Construction/Deloop/Opposite.v` adds the opposite monoid and group and Riehl Example 1.2.2(iii), `Deloop_op : (Deloop M)^op ≅ Deloop (MonObject_op M)`. Witnesses `Nat_Plus`, `Bool_Xor`, `Bool_Xor_Grp` compute. Distinct from `Theory/Bicategory/OneObject.v`, which deloops a *monoidal category* into a one-object *bicategory*, one level up
- **Construction/Product/Special.v**: Mac Lane §II.3 Ex. 1 — `∏` on Cat restricts to the classical products along the standard full embeddings (a prose note; no Mon ⟶ Cat embedding functor exists in-tree): `MonObject_prod`/`GrpObject_prod` over Construction/Deloop.v's records (the group's monoid part definitional by `eq_refl`; distinct from Instance/Grp.v's identically-named record and its `Grp_product`, which live a layer down), `Deloop_prod_iso : Deloop (MonObject_prod M N) ≅[Cat] Deloop M ∏ Deloop N` with both functors identity on arrows and unit-conjugation object bookkeeping, `Deloop_grp_prod` the same term read at product groups, and `Discrete_Product : DiscreteCat (A * B) ≅[Cat] DiscreteCat A ∏ DiscreteCat B` — UIP-free, over a transparent `pair_eta`/`pair_equal` kit (the stdlib's `surjective_pairing`/`f_equal2` are opaque and would leave the arrow actions stuck); computing witnesses at `Nat_Plus`/`Bool_Xor` and a concrete discrete pair; Cat (equivalence strength) chosen over the issue's StrictCat alternative, disclosed
- **Construction/Product/Indexed.v**: Mac Lane §II.3 Ex. 3 — set-indexed products of categories: `PiCat (C : I → Category)` with objects the choice functions `∀ i, C i` and componentwise homs/setoid/laws, projections `PiCat_Proj`, pairing `PiCat_Pair` with the triangle `PiCat_Pair_Proj` (components `iso_id` — the composite is definitionally `R i`) uniqueness `PiCat_ump_unique` up to Cat's hom-equivalence (the setoid rendering of the book's on-the-nose uniqueness), and the bundled `PiCat_ump : ∃! F, ∀ i, P i ◯ F ≈ R i` matching `iprod_ump`'s packaging. One `Category` universe instance for the whole family (the function type forces it), distinct from Structure/Limit/Product.v's `iprod` (products of objects INSIDE one category); the binary case's recovery of `Cat_Cartesian`'s fork is a disclosed prose note
- **Construction/Product/Limit.v**: Mac Lane §V.2 Exercise 2 (book p. 114, `maclane:V.2:ex2`) + Riehl §3.4 Exercise 3.4.v (printed p. 108, `riehl:3.4:exv`) — **limits in a product of categories are computed componentwise: a product of complete categories is complete, dually cocomplete, and the projections JOINTLY CREATE limits**. Over a diagram `K : J ⟶ C ∏ D`, `prod_cone` pairs a cone over `Fst ◯ K` with one over `Snd ◯ K` (apex the pair of apexes, legs the pairs of legs, both `eq_refl`), **`prod_IsLimitCone`** makes the pairing of two limiting cones limiting with the mediator THE PAIR OF THE TWO MEDIATORS at `eq_refl` (`prod_IsLimitCone_med`), **`Fst_PreservesLimitCone`/`Snd_PreservesLimitCone`** deliver Riehl's clause (i) UNCONDITIONALLY — no limit assumed to exist in either factor; a competing cone in `C` is spliced with the `D`-image of the limiting cone, and uniqueness comes from `(v, id)` being a mediator for the spliced cone — `prod_reflect` is the reflection half of clause (ii) through the cone isomorphism `prod_cone_iso` (apex iso `(id, id)`, needed only because `prod` has no definitional eta), and **`Product_JointlyCreateLimit`** inhabits the NEW class **`JointlyCreateLimit`** added to `Structure/Limit/Creation.v` (a family-indexed `CreatesLimit`, with `jcreates_limiting`, `jcreates_lift_unique`, `jcreates_limit_lift` and the one-member bridges `JointlyCreateLimit_of_CreatesLimit`/`CreatesLimit_of_JointlyCreateLimit` at index `unit`) at the `bool`-indexed family `ProdProj`. Mac Lane's statement is **`Product_Complete : Complete C → Complete D → Complete (C ∏ D)`** (the issue's pinned name, via `Product_Limit`), and its dual **`Product_Cocomplete`** costs NOTHING: `Product_Opposite` (`Construction/Product.v`) makes `(C ∏ D)^op` and `C^op ∏ D^op` convertible, so the dual is `Product_Complete` at the opposites applied to `K^op`, with `Complete_op_of_Cocomplete` the one-line passage `Cocomplete E → Complete (E^op)` through the definitional `(G^op)^op = G`. Riehl's "only if" is delivered under an EXPLICIT POINT of the other factor (`Product_Limit_Fst : Limit (pair_const d G) → Limit G`, `Product_Complete_Fst : Complete (C ∏ D) → D → Complete C`, and the `Snd` mirrors): with `D` empty, `C ∏ D` is empty and has no terminal object, so the exercise's tacit assumption is made a hypothesis rather than derived through `Instance/Zero.v`'s `Set`-pinned `_0`. The componentwise `Product_Terminal` (an `#[export]` instance), `Product_Initial` and `Product_Cocartesian` (the primal instances at the opposites, plain `Definition`s since `Terminal ((C ∏ D)^op)` does not syntactically match `Terminal (?C ∏ ?D)`) close the gap the issue's verifier found beside `Product_Cartesian`. The `I`-indexed clause is stated over the EXISTING `PiCat` (`Construction/Product/Indexed.v`), consumed rather than rebuilt: `pi_cone`, `pi_IsLimitCone` (mediator the family of mediators, `eq_refl`), `pi_cone_iso` (apex iso `iso_id` — a function HAS definitional eta, so on this side even the legs of the round trip agree on the nose and the whole-record refusal is located exactly in the coherence field), `pi_reflect`, `PiCat_JointlyCreateLimit`, `PiCat_Limit`, `PiCat_Complete`. **NOT delivered, with reasons**: Riehl's clause (i) at an arbitrary index (the splice must decide `j = i`, so it needs decidable equality on `I` plus a transport; delivered for the BINARY product only, as `Fst_PreservesLimitCone`/`Snd_PreservesLimitCone` stated for the two projections directly — not as a family and not through `ProdProj`, which packages the pair for clause (ii) alone), `PiCat_Cocomplete` (`(PiCat D)^op` and `PiCat (fun i => (D i)^op)` agree on all four data fields but NOT as records, `PiCat`'s laws being `Program` obligations — pinned; a direct cocone development or a transport of `Complete` along the identity-on-everything equivalence would supply it), the indexed "only if", and any comparison with `Product_Cartesian` at a discrete shape (the `DiscreteCat_Functor` `Set` pin). Universes off BOTH binder and block: every constant mentioning `Fst ◯ K` identifies the shape's hom level with both factors' and the product's and hom with proof (`prod_cone`'s block `u0 = u2`, `u0 = u4`, `u0 = u6`), **`Compose`'s doing on the binary side** — at a shape whose homs are declared strictly below the factors' the diagram and a `Cone` over it are accepted while `Fst ◯ K` is refused, pinned with those controls (probe N6); **the `PiCat` side carries a SECOND identification the binary side does not, and it restricts the INDEX**: all twelve `PiCat`-side constants put the index type's universe at the factors' hom-and-proof level — eleven in the BLOCK (`pi_cone` carries `u = u1`, `u = u3`, `u = u5` over `I : Type@{u}` and `D : I → Category@{u0 u1 u1}`) and `PiCat_Complete` in its BINDER alone (`D : I → Category@{u5 u u}` with no block equation, so a block-only reader sees nothing) — whose donor is `PiCat_Proj` (its own binder `C : I → Category@{u1 u u}`) and NOT `Compose`: at an index declared strictly above the factors' homs `PiCat Du` is accepted while `PiCat_Proj Du` and `PiCat_Complete` are refused with `Cannot enforce dh = … because dh < ii`, both firing at the `Du` argument (probe N7 with `PiCat` as the control; the inherited constant cannot be measured apart from the donor), a restriction the first commit's header attributed wholly to `Compose` and a fess audit found — the donor is unannotated and it is not claimed unavoidable; object universes free; no word-bounded `Set` in any of the 52. Fifteen `eq_refl` readbacks; of fifteen `Defined` tokens exactly SIX are load-bearing by flipping each alone (`prod_cone`, `prod_IsLimitCone`, `pair_const`, `const_pair`, `pi_cone`, `pi_IsLimitCone`). **52/52 + 10/10 constants closed under the global context** (51 heads + `Product_Terminal_obligation_1`; the ten `Creation.v` additions), all gated fully qualified; closure 29 excluding self. `Test/ProbeProductLimit418.v` carries 10 `Fail`s = 1 instrument + 9 negative commands pinning SEVEN negatives of TWO kinds — five CONVERSION (the two whole-record refusals, the `prod` apex eta, the `PiCat` opposite, and the swapped-handedness ascription, which LOOKS like typing but carries a `cannot unify` clause and is classified by the tree's convention) and two UNIVERSE each pinned twice (N6: the `Compose` donor and `prod_cone` inheriting it, both firing at the `K` argument; N7: the `PiCat_Proj` donor and `PiCat_Complete` inheriting it, both firing at the `Du` argument) — each stripped one at a time with its whole error read, every constant a negative names guarded by a `Check`; `make todo` grows by 19, all in the probe, the library files contributing ZERO (the first commit said 15 when it was 17, a fess-audit finding; the corrected probe's two new `Fail` commands make it 19). The pairing functors `pair_const`/`const_pair` are built with `unshelve refine` rather than `Program` because the default obligation tactic discharges `fmap_id` on its own and so scrambles the order the remaining `Next Obligation`s are met in
- **Construction/Arrow/Limit.v**: Mac Lane §V.1 Exercise 3 (book p. 112, `maclane:V.1:ex3`) — **the arrow-category projection `C^→ ⟶ C × C` creates limits, delivered as the case `S = T = Id` of a TWO-SIDED comma theorem: `comma_proj : (S ↓ T) ⟶ A ∏ B` creates the limit of every `K : J ⟶ (S ↓ T)` under the ONE hypothesis `HT : PreservesLimitCone (comma_proj2 ◯ K) T`, and creates it STRICTLY**. Nothing is asked of `S` — the section binds nothing about it beyond its type, and offering the domain-side `PreservesLimitCone (comma_proj1 ◯ K) S` instead is pinned as the probe's TYPING negative (a has-type mismatch between two DIFFERENT types, whose trailing `cannot unify` is on the two cone TYPES and not on two inhabitants of one type). `comma_lift_arrow N HN : S (fst apex) ~> T (snd apex)` is the comparison arrow — the mediator of `dom_cone N` (apex `S` of the domain apex, legs the comma arrows of `K` after the domain legs) into `FCone T (cod_cone N)`, limiting by `HT` — with `comma_lift_arrow_commutes` its square at every leg and `comma_lift_arrow_unique` Mac Lane's uniqueness clause; `comma_lift_cone N HN : Cone K` has apex `(vertex_obj[N]; comma_lift_arrow)` and legs `(cone_leg N j; square)`, its projection `N`'s apex and legs at `eq_refl` (`comma_lift_apex_strict`, `comma_lift_leg_strict`), which is what makes `comma_lift_StrictLift : StrictLift K comma_proj N` a record literal with `eq_refl` for the object equation; `comma_proj_reflect` is the reflection half, its mediator's square FORCED by joint monicity of the preserved limit's legs (`limit_med_eq`) and its uniqueness the downstairs uniqueness read through the comma hom-setoid; **`comma_proj_StrictlyCreatesLimit : StrictlyCreatesLimit K comma_proj`** (Riehl's on-the-nose reading) and `comma_proj_CreatesLimit` through `StrictlyCreatesLimit_CreatesLimit`, with `comma_proj_Limit` the existence half bundled, its apex the downstairs apex and its arrow `comma_lift_arrow` at `eq_refl`. For `C^→ := Id[C] ↓ Id[C]`: **`arrow_proj_creates_limits`** and **`Arrow_Complete : Complete C → Complete (C^→)`** (the issue's two pinned names, verbatim), with `arrow_proj_strictly_creates_limits` and `arrow_proj_creates_all_limits`, through `creates_limits_Complete` over #418's `Product_Complete HC HC` — and "limits are computed componentwise" is a READBACK rather than prose, `Arrow_Complete_dom`/`Arrow_Complete_cod` showing at `eq_refl` that the chosen limit's domain and codomain are `HC`'s own chosen limits of `Fst ◯ (comma_proj ◯ K)` and `Snd ◯ (comma_proj ◯ K)`. The `Id` instantiation needs `Id_PreservesLimitCone`, which the tree did not have (measured by SHAPE: no constant of the form `PreservesLimitCone _ Id`; the nearest neighbour, `Theory/Equivalence/Creation.v`'s `Id_CreatesAllLimits`, does not serve, since turning creation into cone-level preservation through `Structure/Limit/Creation.v`'s `creation_preserves_limit` costs an extra `L : Limit (F ◯ K)` the new lemma does not need — a neighbour a fess audit asked to see named), declared at the head of the file and disclosed as belonging beside `PreservesLimitCone` in `Structure/Limit/Preservation.v`, kept here only because that module is upstream of most of the tree. **What is consumed and what is not**: #418's unconditional `Snd_PreservesLimitCone` is what turns a limiting cone in `A ∏ B` into one in `B`, and `cod_cone` REPACKAGES its image over `comma_proj2 ◯ K`, because `Snd ◯ (comma_proj ◯ K)` and `comma_proj2 ◯ K` agree on BOTH actions at `eq_refl` but are different functor RECORDS (`Compose`'s three law fields are separate opaque obligations) — pinned as a CONVERSION negative with the two action agreements as controls; the one-sided donor the issue points at, `Construction/Comma/Creation.v`'s `comma_CreatesLimit` for `comma_proj2 : (=(d) ↓ U) ⟶ C`, is NOT consumed, and the `apex_obj`/`apex_leg` beneath it — declared in `Construction/Comma/Limit.v`, the module Creation.v builds on, and NOT in Creation.v (a first draft of this bullet and of the file header attributed them there; the fess audit's grep corrected it) — are not reused — those are stated over the constant-domain comma, whose objects are `(ttt, b)`-pairs and whose lifting argument goes through the fixed `Gdiag`, while the two-sided projection needs the domain half to vary, so the argument here is the same shape one level up rather than an instance; issue #438 (that one-sided lemma, Mac Lane §V.6 Lemma 1) is still OPEN and nothing here depends on how it is resolved. **Strictness measured strict first**: six `eq_refl` statements in the library file; the WHOLE record `FCone comma_proj (comma_lift_cone N HN) = N` is refused at `eq_refl` though apex and every leg are `N`'s on the nose — the coherence field is a rebuilt proof and `≈` is Type-valued — pinned with those two controls; **non-vacuity COMPUTES, at a DEGENERATE shape and labelled so** — `_1` has one object and one arrow, so cone coherence is vacuous and the comparison arrow is the diagram's own; the witness shows the whole chain REDUCES, not that a non-trivial limit is computed: over `Coq` at the shape `_1`, with the diagram constant at the arrow `negb` and the limiting cone `Structure/Limit/Initial.v`'s `initial_cone` (whose mediator is definitionally the leg), the created arrow IS `negb` at `eq_refl` and sends `true ↦ false`, `false ↦ true` by computation, the lift's apex `(bool, bool)` and its leg `(id, id)` likewise at `eq_refl` — in the probe, because it costs `Instance/Coq`, `Instance/One` and `Structure/Limit/Initial`, which the library file should not carry. **Universes measured off BOTH binder and block over all 31**: every one of the 24 two-sided constants binds `A : Category@{u u0 u0}`, `B : Category@{u1 u2 u2}`, `C : Category@{u3 u4 u4}`, `J : Category@{u5 u6 u6}` — hom identified with proof in the BINDER, with no such equation in any block — and EIGHTEEN of them carry `u0 = u2`, `u0 = u4`, `u0 = u6`, `u0 = u9` (all four hom-and-proof levels collapse to one; `u9` is NOT the comma category's own level — that category sits at `Category@{u7 u4 u4}`, its hom level being C's `u4`, and `u7` carries no equation anywhere — but the level instantiating `comma_proj2`'s own universe `u6`, an auxiliary bounded below by A's and B's hom levels, a mislabel a fess audit caught) while the SIX cone-building constants (`cod_leg`, `cod_coherence`, `cod_cone`, `dom_leg`, `dom_coherence`, `dom_cone`) carry `u2 = u4`, `u2 = u6`, `u2 = u9` instead and leave A's hom level only BOUNDED, the ten `Section Lift` constants adding `u15 = u20`, `u17 = u18` on levels that occur in no `Category` binder (they sit inside the `PreservesLimitCone`, `Compose` and `IsLimitCone` instances of the `HT`/`HN` binders — "no binder" was an overstatement); no equation in any block touches an OBJECT universe; the collapse is `Compose`'s and NOT the comma category's — `Comma@{…}` binds `A`, `B` and `C` at three separate hom levels, `Su ↓ Tu` is ACCEPTED at `ah < bh`, and at a shape whose homs are declared strictly below the factors' `Su ↓ Tu`, `K` and a `Cone` over `K` are accepted while `comma_proj ◯ K` is refused at `K` ("Cannot enforce ch = jh") and `comma_proj_CreatesLimit` is refused too — but at its SHAPE argument `Ju`, with "Cannot enforce jh = ah", its own block equation `u0 = u6` met at an already-refused argument (the #340 shape), so the inherited `Fail` pins the boundary a consumer meets and does NOT independently corroborate the `Compose` attribution, which rests on the donor `Fail` alone (both pinned; a first draft of this bullet and of the probe header said the inherited refusal fired at the composite, which a fess audit refuted by stripping it); the six arrow-section constants and `Id_PreservesLimitCone` carry NO block equation at all, the identification sitting in their binders (`C : Category@{u u0 u0}` with a bound shape at `Category@{u17 u0 u0}`; `J : Category@{u2 u4 u4}`, `D : Category@{u3 u4 u4}`), and `Arrow_Complete` is accepted at `Set < ch`; ZERO word-bounded `Set` in any binder or block. **31/31 constants closed under the global context with zero `Axioms:` lines** (26 declared heads plus the 5 `Qed`-closed lemmas `Print Module` renders as `Parameter`; no `Program`, no `Record`/`Class`/`Inductive`, so no obligation and no `Build_*`), all in the `make print-assumptions` gate FULLY QUALIFIED as a new block of exactly 31 commands, the gate passing with 6309 `Closed` lines and the `Axioms:` line count unchanged at 1; four `Defined` tokens, THREE load-bearing by flipping each alone to `Qed` (`Id_PreservesLimitCone` and `cod_limiting` each stop the probe's witness at `neg_created = negb`, `comma_proj_StrictlyCreatesLimit` stops the file's own `comma_proj_Limit_apex`; `comma_proj_reflect` flips with both files compiling and is `Defined` by the data convention only); zero collisions over the 48 names the two files introduce (whole-word over every other `.v`, instrument `Full` = 97 files, `Monoid` = 94); closure 33 excluding self (`Construction/Product/Limit` costs 6, `Construction/Arrow` 1, every other `Require` 0), probe 65; zero lines over 80 columns. `Test/ProbeArrowLimit412.v` mirrors the target's `Require` list plus the witness modules and carries **6 `Fail`s = 1 instrument + 4 negatives of THREE kinds told apart by the error TEXT, the universe one pinned twice** — N1 and N2 CONVERSION, N3 TYPING, N4 UNIVERSE at the donor `comma_proj ◯ K` and at the inherited `comma_proj_CreatesLimit` — each stripped ONE AT A TIME in a copy of the WHOLE file and compiled alone with its whole error read (which is how N3's `cannot unify` on the cone types was found, correcting a scratch measurement that had called it a plain mismatch), guard coverage measured mechanically under a STATED tokenization — comment-stripped, every `[A-Za-z_][A-Za-z0-9_]*` token inside a `Fail` command with the three command keywords `Fail`/`Check`/`Example` excluded: 22 identifiers inside a `Fail`, 19 also outside, the three exceptions exhaustively the names the `Fail`s themselves declare (a first draft said 24/21 under an unstated tokenization that counted the keywords; the fess audit asked for the criterion), with `Su ↓ Tu` at `ah < bh` and `@Arrow Cu`/`@Arrow_Complete Cu` at `Set < ch` as further accepted controls; `make todo` grows by 13, ALL in the probe (6 `Fail` commands + 7 prose lines; it was 11 before the fess-driven header rewrite added two prose mentions), the library file contributing ZERO — so the issue's "adds no new hits" box is not met as written and is disclosed. NOT delivered: colimits — no `Arrow_Cocomplete` and no dual of the two-sided statement (it would need `S` to preserve the COLIMIT of the domain diagram and a comma read at the opposites, and neither the duality of `(S ↓ T)^op` against `(T^op ↓ S^op)` nor a direct cocone development is attempted); no converse saying `T` MUST preserve the limit for `comma_proj` to create it; no comparison of `Arrow_Complete`'s limits with the one-sided `Comma_Complete`'s; no identification of `Fst ◯ comma_proj` with `comma_proj1` beyond their agreeing actions — both now EXHIBITED at `eq_refl` in the probe, mirroring the `Snd` pair, after a fess audit found the clause asserted with no control behind it — and none with `Arrow_dom` at all (the readbacks are stated in the `Fst ◯ (comma_proj ◯ K)` form `Product_Complete` chooses); no functoriality of the lift in `K` and no naturality of the comparison arrow; and nothing registered as an `Instance`, a chosen limit not being allowed to become globally resolvable
- **Construction/Deloop/Transform.v**: Mac Lane §I.4 Exercise 3 — natural transformations between group homomorphisms are conjugations. The functor-level half of the delooping dictionary (`Deloop_map`/`Deloop_unmap`, with `Deloop_unmap (Deloop_map f) = f` by `eq_refl`); the characterization in BOTH directions, over monoids as the intertwiner equation `T g · h ≈ h · S g` (`transform_iff_intertwines`, whose forward direction is literally the `naturality` field) and over groups as `transform_iff_conjugate : (Deloop_map S ⟹ Deloop_map T) ↔ ∃ h, T g ≈ h · S g · h⁻¹`; the setoid-level correspondence at BOTH levels, each a genuine bijection of setoids — `transform_intertwiner_iso : Deloop_Transforms ≅[Sets] Deloop_Intertwiners` over a bare monoid (where there are no inverses, so 'conjugator' would have no referent) and `transform_conjugator_iso : Deloop_Transforms ≅[Sets] Deloop_Conjugators` over a group, which is the issue's literal 'set of conjugating elements'; both identify witnesses by their underlying element, disclosed rather than silent. `transform_iff_conjugate_SWAPPED` gives the source-on-the-left form, the two ∃-level statements being equivalent in-file by composing biconditionals with a common left-hand side (h ↦ h⁻¹), while the COMPONENT-level statements remain direction-sensitive. Awodey §7.7 Example 7.21's consequence: `Fun_IsGroupoid : IsGroupoid D → IsGroupoid [C, D]` (stronger than Awodey's, whose domain is a groupoid too), hence `Deloop_Fun_IsGroupoid`, with the inverse's component `= grp_inv h` by `eq_refl` and `deloop_ginv_conjugates` showing it conjugates back — and H^G is NOT a group: `Deloop_S3_two_objects` gives two provably distinct objects, and `Deloop_Fun_S3_not_deloop` (+ `_rev`) shows it is not equivalent to the delooping of any monoid, in either direction. COMPOSITION ORDER is derived, not assumed (`deloop_compose_mon_op` by `eq_refl`; `conjugates_is_conjugation` identifies `Conjugates` with Structure/Groupoid.v's `conjugate` on the nose): the TARGET multiplies on the left of h, matching Awodey's 2nd edition (Oxford Logic Guides 52, p. 167: `g(x)·h = h·f(x)`, inner automorphism `y ↦ h·y·h⁻¹`). Awodey CHANGED this formula between editions — the 1st-edition CMU pre-print (Sept 2005) §7.7 Ex. 7.21, printed p. 175, has `f(x)·h = h·g(x)` with `y ↦ h⁻¹·y·h` — and issue #241 cites the 1st edition and quotes it CORRECTLY; both readings are consistent under their own edition's composition-order convention, and there is no erratum in the issue. VACUITY is proved away rather than asserted — `abelian_conjugates_agree` shows the theorem is empty over an abelian target and `Bool_Xor_abelian`/`Z3_abelian` disqualify the older Z/2 and Z/3 witnesses, so the witness is S3: two homomorphisms Z/2 → S3 conjugate by the non-central 3-cycle (`S3_hom_conjugate`, `S3_conjugator_noncentral`), with `S3_conjugation_needs_nonabelian` deriving nonabelianness from the witness itself
- **Construction/Deloop/Functors.v**: Mac Lane §I.3 Exercise 3, complete across its four-book coverage — what a functor IS at the special categories, built by REUSE: the homomorphism record is Structure/Groupoid.v's `MonHom` (inverse preservation already its `MonHom_grp_inv`), and part (b) is already Construction/Deloop/Transform.v's `Deloop_map`/`Deloop_unmap` with its stronger eq_refl record-equality round trip — neither is re-proved. The spine is one correspondence: functors `Deloop M ⟶ C` are a choice of object c with a monoid homomorphism `M → hom_monoid C c` (`functor_of_hom_monoid`/`hom_monoid_of_functor`, round trips both ways), and what the file adds at (b) is `Deloop_map_spine` (`Deloop_map` IS the spine at the single object, the types agreeing definitionally by `hom_monoid_Deloop`) plus the functor-side round trip at full `Functor_Setoid` strength (`Deloop_functor_round`; Transform.v states it only pointwise). Part (c): functors `B G ⟶ Sets` = `MSetoidAction`s (permutation representations, Riehl Example 1.3.9 left convention) and `B G ⟶ Matr R` = `MatrixRep`s (dimension = the chosen object); right actions translate to left actions of the opposite monoid along `Deloop_op` (mutual inversion NOT stated — it would combine `action_functor_round` with `Deloop_op`'s iso laws); Riehl Corollary 1.3.10 is the named `fobj_iso` instantiation `action_automorphism` — the iso TYPE is vacuous at one object, the content is the definitional `_to`/`_from` readings; Awodey §7.1's cocycle characterization `Cocycle`/`functor_of_cocycle`/`cocycle_of_functor` with round trips on BOTH sides (`cocycle_round`, `cocycle_functor_round`) and the worked witness `translation_cocycle` ((ℤ,≤)→(ℤ,+) by differences — Awodey's ℝ transposed to ℤ for axiom-freedom). Instance/Proset/Monotone.v is part (a): the ordinary, DECIDABILITY-FREE correspondence between functors `Proset P ⟶ Proset Q` and bare `MonotoneFun`s — completing two in-tree fragments: the enriched `EnrichedFunctor_Two_monotone` case-splits on `tpre_dec`, and Instance/Pos.v's `MonotoneAsFunctor` is the forward leg only over posets — with the thin-target smart constructor operationalizing "functor laws are automatic in a thin category" (Seven Sketches 3.42), both round trips, and witnesses: `seventeen_twentyfour` (Example 60's 17/24 map as a genuine `MonotoneFun` over bool's false≤true order, its functor via the constructor; plus a hand-built `_2` reading, `_2` not being a `Proset`), endofunctors of (ℕ,≤) = non-decreasing sequences, and the tree-of-life `taxonomy_rank` (Example 61 — five terms, four generating edges via `clos_refl_trans`, monotonicity by induction on the generating relation). Mac Lane §II.4's construction 2 rides the same dictionary one level up (Instance/Fun/Action.v): `MSet M` — M-actions with `Equivariant` maps — with the equivalence `MSet_Fun_equiv : [Deloop M, Sets] ≅[Cat] MSet M` (a natural transformation over one object IS a single equivariant map; the functor-side round trip rebuilt with a transparent component family since the in-tree lemma is opaque), with `MSet_hom_iso` stating the unwinding literally as a hom-setoid isomorphism, and Exercise 5's permutation reading: `perm_of_group_element` (= the donor's `action_automorphism` read at FinSet, disclosed reuse) plus the object-level identification `FinPermRep`/`functor_of_perm_rep`/`perm_rep_of_functor` along the spine with invertibility automatic (`perm_rep_acts_by_bijections`); no separate category of permutation representations and no group-finiteness hypothesis, disclosed in the header
- **Construction/Elements/Kan.v**: Mac Lane §III.2 Ex 3 — Kan's form of the coyoneda lemma, `Nat(K, D(a,−)) ≅ Nat(Δa, Q)` for `K : D ⟶ Sets` with `Q := Elements_proj K`, delivered WITH the naturality in `a` the exercise's "natural isomorphism" reading requires: **`kan_coyoneda : KanNat K ≅[[D^op, Sets]] KanCone K`**, an isomorphism in the functor category, with `kan_coyoneda_to_component`/`_from_component` `eq_refl` against the pointwise `kan_iso_at`'s legs — so the naturality statement is a machine-checked UPGRADE of the pointwise one, not a parallel construction. `KanNat := Curried_Hom ([D,Sets]) K ◯ Curried_Hom D` is a PURE ASSEMBLY (zero obligations, all three functor laws inherited) and `KanCone := ConePresheaf (Elements_proj K)` is pure reuse; both object actions pinned by `eq_refl`. **The primary form is `ACone`, deliberately**: `Structure/Cone.v`'s `ConePresheaf` already IS the right-hand side as a functor of `a` with its functor laws proved, so taking the Δ-reading as primary would mean rebuilding that presheaf for no gain — the Δ-reading is delivered as a proved bridge both ways (`kan_iso_transform a : KanNat a ≅[Sets] KanDelta a`, Mac Lane's literal statement) rather than dropped, and the two are provably NOT convertible, the cone law `fmap f ∘ ψx ≈ ψy` sitting one `id_right` from Δ-naturality `fmap f ∘ τx ≈ τy ∘ id` with `Transform` additionally carrying `naturality_sym`. **The mathematical crux is that objects of `Elements K` are compared by LEIBNIZ equality**, so `(d,x)` and `(d,y)` are different objects when `x ≈ y` without `x = y`; the backward passage therefore needs its component at `d` to respect `≈`, and that respectfulness is DERIVED (`kan_leg_respects`) from cone coherence at the identity-carried morphism `elements_same` rather than assumed — the same shape as `Theory/Natural/Transformation/Arrows.v`'s `τ_arr_respects`. **The universe restriction enters at the PACKAGING, not the mathematics, and that is pinned rather than inferred**: `KanNat` and `KanCone` each carry `u <= u0` (D's objects at or below its homs), hence so do `kan_iso_at`, `kan_coyoneda` and `kan_iso_transform`, while `kan_cone_of_nat` and `kan_nat_of_cone` carry no such constraint — three formability negatives reject the packaged forms under `Constraint uh < uo` while two positive controls accept the raw passages at that very setting; the two halves are independent (the RHS through `ConePresheaf` chained via `Elements`, the LHS because `KanNat K a` must be a `Type@{u0}` indexed by `obj[D] : Type@{u}`), the inherited donor pins (`Fun`'s `u0 = u2`, `Compose`'s single hom-and-proof level) are named and neither introduced here, and none is claimed unavoidable. Seven conversion negatives reduce to THREE causes — rebuilt law fields (both records have primitive projections WITH eta, so record equality is field equality and the law fields are this file's own opaque lemmas; a positive control shows the underlying FUNCTIONS agree by `eq_refl`, locating the obstruction exactly at the proofs), missing `sigT` eta (stdlib `sigT` is not covered by `Lib/Foundation.v`'s `Set Primitive Projections`, which defeats even the bare leg family), and the Δ-bridge's `id_right` (where the DATA returns on the nose in both directions, so only the law field fails). Engineering finding recorded in-header: `nat_id`'s component is `fmap[F] id` and not `id` (`Theory/Natural/Transformation.v`), so the identity of a FUNCTOR category reindexes and `kan_coyoneda`'s inverse-law obligations need an `id_right` that `kan_iso_at`'s — living in `Sets`, where `id` is `setoid_morphism_id` — do not; the tree records the same `nat_id` fact twice, at `Functor/Representable.v` about `repr_induced_id` and at `Functor/Representable/Functorial.v` about the comparison with `repr_pair_iso`, which is itself declared in `Functor/Hom/Yoneda/Iso.v`. Distinct from `Theory/Coend/Yoneda.v`'s `coyoneda_reduction` (∫^x C(x,c) × F x ≅ F c), a different theorem sharing the name, which gets its own header paragraph. 46/46 constants closed under the global context. NOT delivered: the presheaf/contravariant orientation over `PElements`, naturality in `K` as well as `a`, any identification of either side with `Structure/Limit/Kan/Pointwise.v`'s pointwise formula or of `Q` as a discrete opfibration, and — the one worth knowing — the Δ-reading is NOT upgraded to a naturality statement, `KanDelta` never being exhibited as a functor `D^op ⟶ Sets`, so Mac Lane's own phrasing lands one strength below the `ACone` headline
- **Construction/Opposite.v**: Op categories with auto-duality
- **Instance/Cat/Opposite.v**: Mac Lane §II.2's duality involution as a bundled functor — the piece the tree's `Opposite`/`Opposite_Functor`/`Opposite_Transform` trio never packaged: `Op : Cat ⟶ Cat` with `fobj := Opposite`, whose `fmap_respects` is the 2-CELL REVERSAL tamed by invertibility (a natural iso F ≅ G yields F^op ≅ G^op with INVERSE components via `Isomorphism_Opposite`; the honest 2-categorical statement is Cat^co ⟶ Cat, recorded — the 1-categorical instance works because Cat's hom-equivalence consists of invertible 2-cells); `Op_invol : Op ◯ Op ≈ Id[Cat]` with components the identity-on-objects functors `op_invol_to`/`op_invol_from` (the record equality `op_invol` is Qed-opaque, but hom/compose/id of (C^op)^op collapse to C's definitionally — disclosed); `Op_Strict : StrictCat ⟶ StrictCat` whose laws hold at strict functor equality (`Functor_StrictEq_Setoid` transports collapsed by generalizing the two fmap terms then destructing the object equalities) WITH the strict involution `Op_Strict_invol` closing by `eq_refl` object components — record eta makes the double opposite definitional, `op_invol_iso` being a quotable convenience rather than a necessity; and Riehl Exercise 1.3.v's hom-setoid bijections `op_flip_iso : (C^op ⟶ D) ≊ (C ⟶ D^op)` and `op_conjugate_iso : (C ⟶ D) ≊ (C^op ⟶ D^op)`, the 2-cell reversal absorbed as in `Op`
- **Instance/Cat/Pullback.v**: Mac Lane §III.5 Ex 3 — the fibre product of categories, and which category of categories it is a pullback IN. **THE ISSUE'S REQUESTED ARTIFACT `Cat_Pullbacks` IS NOT BUILT, AND ITS NATURAL CANDIDATE IS REFUTED**: `FibreProduct_not_Cat_pullback` shows the fibre product is NOT a pullback in `Cat` at the cospan `1 --true--> Indiscrete bool <--false-- 1`, where the iso `true ≅ false` supplies the natural isomorphism Ho(Cat)'s square condition asks for while `IB_FibreProduct_empty` shows the apex has no objects at all, so no mediator can exist. **Read that narrowly and the file insists on it**: `point_cospan_Cat_IsPullback` proves the SAME cospan DOES have a `Cat` pullback (the terminal category), so what is refuted is this CANDIDATE APEX, not pullbacks in `Cat` — `HasPullbacks Cat` is NEITHER PROVED NOR REFUTED and the iso-comma/pseudopullback, the standard replacement, is explicitly UNATTEMPTED. The cause is the ambient: `Instance/Cat.v` says in terms that this `Cat` is Ho(Cat), `Functor_Setoid` identifying naturally isomorphic functors, while the fibre product is built out of Leibniz equalities `F a = G b`. So the universal property is proved in `StrictCat` (`Instance/StrictCat.v`, `Functor_StrictEq_Setoid`): `FibreProduct_IsPullback : ObjUIP C → IsPullback F G (FibreProduct F G) FP_fst FP_snd`, stated first at `Theory/Morphisms/Stability.v`'s APEX-PINNED `IsPullback` and only then packaged as `FibreProduct_Pullback`/`StrictCat_HasPullbacks` (the #326 discipline). **THE HYPOTHESIS IS PROVED NECESSARY, NOT MERELY TAKEN**: `FP_uniqueness_forces_UIP` shows that deriving the uniqueness clause uniformly — every cospan, no hypothesis on the base — entails UIP for every type, the countermodel being `1 --x--> Indiscrete X <--x-- 1` whose fibre-product objects ARE the loops at `x`, each naming a mediator satisfying both triangles; it is the shape of `arrow_mul_respects_forces_UIP` (Theory/Category/Monoid.v) and `Discrete_DiscreteRigid_forces_UIP` (Instance/Discrete/Reconstruct.v), and it is axiom-free (`inj_pair2_eq_dec` at a decidable index). Scope it precisely: it is about the uniqueness clause OF THIS APEX, so it does not refute `HasPullbacks StrictCat` with some other apex, and no such refutation is offered — hence `StrictCat_HasPullbacks` is delivered only from the blanket `∀ C, ObjUIP C`, free for decidable object equality by Hedberg (`ObjUIP_of_ObjDecEq`, `FibreProduct_IsPullback_dec`). **Where the hypothesis is spent is measured rather than gestured at, and the two halves differ**: on the fibre product exactly ONE of the pullback's fields needs it (`FP_med_unique`, through its sole-consumer helper `FP_obj_eq`) while `FP_commutes_strict`, `FP_med`, `FP_med_fst` and `FP_med_snd` take no hypothesis at all; on the comma side it is spent by THREE LEMMAS in FOUR USES per side — `slice_arrow_reflect` (where it does work nothing else could: the codomain coordinate of a transported morphism equality is a loop at `c` and must be `eq_refl`) plus `Slice_med_arrow`, which spends it twice, and `Slice_med_unique`, both through `fp_hom_cast_irr` — a header sentence claiming a single point, and a later one claiming three uses, were both corrected before landing. The six `Program Definition`s of those sections (`Slice_proj`/`Slice_Arrow`/`Slice_med` and the coslice twins) are genuinely hypothesis-free, `Default Proof Using "All"` reaching only `Qed`/`Defined` results — a header claiming otherwise was corrected too, and it had erred toward making the file look WORSE than it is. **The comma half IS delivered**, consuming `Construction/Comma.v`'s own PIE-limit framing rather than restating it — and the tree's PRE-EXISTING comma UMP over these same two arrow-category projections, `comma_diagram_ump` (`Construction/Comma/Diagram.v`, Mac Lane §II.6 Ex 5, in a module this file Requires and whose own header calls it "what exhibits it as a 'pullback'-style limit"), is disclosed rather than left to be found: it mediates into the GENERAL comma `(S ↓ T)` over the `MediatesDiagram` competitor class at `≈[Cat]`, where these two are about the ONE-OBJECT cospans, use the pullback's own competitor class, and are stated at `≈[StrictCat]`: `Slice_IsPullback` (C/c is the pullback of `Arrow_cod` along `Diagonal 1 c`) and `Coslice_IsPullback` (dually, `Arrow_dom`), both in `StrictCat`, both with the tree's OWN `Slice`/`Coslice` as apex — genuine assemblies, not instances of the general theorem, and no isomorphism between `Slice C c` and the fibre product of its cospan is built. Strengths measured strict-first: `FP_commutes_strict` is a `:=` with NO tactic (the object component IS the equality each object carries, the morphism component IS the equation each morphism carries), both triangles have `eq_refl` object components with `reflexivity` arrow halves, and `fobj`/`fmap` of `FP_fst ◯ FP_med` are `q1`'s at LEIBNIZ equality — while the whole-record equality is REFUTED, the three `Functor` law fields being rebuilt proofs, as is `Slice C c = FibreProduct Arrow_cod (Diagonal 1 c)`, those two being different categories (one stores an arrow, the other a pair with a proof). **Universes measured off the CONSTRAINT BLOCKS with the packaging blamed rather than the construction**: `FibreProduct` carries bounds only and no identification between A's and B's object universes, whereas `FibreProduct_IsPullback` displays three separate category binders while its block contains `u = u1`, `u = u3`, `u0 = u2` and `u0 = u4` — all three object universes identified and all three hom-and-proof universes too, which is what stating an `IsPullback` in `StrictCat` costs, inherited and not claimed unavoidable; the probe's first negative localizes it by accepting `FibreProduct`, `FP_fst`/`FP_snd` and `ObjUIP` at three object universes declared strictly apart and rejecting only the packaged form. `FP_uniqueness_forces_UIP` has its HYPOTHESIS at `Category@{u0 Set Set}`, which weakens the hypothesis and so strengthens the theorem; `Slice_IsPullback`/`Coslice_IsPullback` carry no `Set` at all. Prior art measured by shape, not by name alone: nothing in the tree states an `IsPullback` or `Pullback` in either `Cat` or `StrictCat`, `FibreProduct` is a new name, and the only two "fibre/fiber product" occurrences are prose about schemes (`Structure/Pullback.v`) and about base change along a slice (`Construction/Slice/Pullback.v`); the concrete `HasPullbacks` roster BEFORE this commit was `Sets_HasPullbacks` and `FinSet_Pullbacks` with three generic conditionals besides, cited from `Instance/Sets/Pullback.v`'s own header rather than re-derived — the issue's "exactly one inhabitant" reading being stale, and stale because of that file — with `StrictCat_HasPullbacks` a FOURTH conditional added here, not a generic one, being for one named category under a blanket hypothesis. `Test/ProbeCatPullback337.v` pins the boundary with **4 negatives of THREE kinds** kept lexically apart — one FORMABILITY (the universe identification, with its localizing controls), two CONVERSION differing in CAUSE (Negative 2 fails only in the LAW fields, its controls showing the data agrees at `eq_refl`; Negative 3 fails in the DATA) and one TYPING (`IB_FibreProduct_empty` at term level, controlled at the AGREEING cospan so the failure is the cospan's) — plus an instrument check, each stripped and its kind read off the WHOLE error message — deliberately not the tail, since the typing negative's tail ends in a `cannot unify` that would misread as conversion, its distinguishing `cannot satisfy constraint` sitting mid-message — rename-simulated 10/10 unpadded against an instrument-checked baseline. **108/108 constants closed under the global context** (62 by the glob, the other 46 `Program` obligations the glob does not record, all reached by fully-qualified name). NOT delivered: no `HasPullbacks Cat` and no refutation of it; no iso-comma, pseudopullback, bilimit or 2-categorical universal property; no unconditional `HasPullbacks StrictCat`; no essential-uniqueness statement for pullbacks in `StrictCat` at all; the GENERAL comma `(S ↓ T)` as the pullback of `⟨dom, cod⟩` along `S ∣→ T` is not built, only the two one-object cospans; and nothing about pasting, stability, equalizers, other finite limits, or completeness for either category of categories
- **Instance/Cat/Creation.v**: Mac Lane §V.6 Exercises 3 and 4 (book p. 125) — **creation of limits is stable under the fibre product of categories, and the comma category is that fibre product**. Read the first word of "stable under pullback in `Cat`" precisely, because the tree already refutes the naive reading: `Instance/Cat/Pullback.v`'s `FibreProduct_not_Cat_pullback` shows this object is NOT a pullback in `Cat`, and its universal property holds in `StrictCat` only under `ObjUIP C`. Read that refutation as narrowly as its own bullet at insists: it is at the single cospan `1 → Indiscrete bool ← 1`, and nothing refutes `FibreProduct (Coslice_Proj d) U` as a `Cat` pullback of ITS cospan — an earlier revision of this bullet said the coslice apex was "refuted for this object", which the measurement does not support. So the honest Exercise 3 is a theorem about `FibreProduct`, and **nothing here asserts a pullback square in `Cat`**. `fp_snd_StrictlyCreatesLimit` : if the left functor STRICTLY creates the limits of the projected diagram and the bottom one preserves them, the second projection strictly creates them. Two things are measured rather than assumed. It needs **no `ObjUIP`** — the proof never touches `FibreProduct_IsPullback`, working instead with the apex's own stored data, the equality `` `2 (K j) `` and the cast condition on arrows, both definitional (`fp_square_obj` reads back at `eq_refl`) — and the printed type carries no `uip` argument. What it DOES need is STRICT creation below, because the created apex stores `slift_eq` in a slot the fibre product declares as a Leibniz `=`; probe negative n3 refuses an isomorphism there against the accepted control that builds the same object from an equality. In exchange the conclusion is strict too: both `StrictLift` fields are `eq_refl` and `reflexivity` at every limiting cone downstairs. `fp_snd_CreatesLimit`, `fp_snd_StrictlyCreatesLimits` and `fp_snd_CreatesAllLimits` follow — the all-shapes packaging in BOTH strengths, since the per-diagram theorem is already strict and quantifying over shapes costs nothing (an earlier revision shipped only the non-strict `fp_snd_CreatesAllLimits`, silently dropping the strictness). `Comma_FP_iso : (=(d) ↓ U) ≅[Cat] FibreProduct (Coslice_Proj d) U` is Exercise 4's half of the picture and is new — the closest the tree gets to "the comma category is a pullback", and an ISOMORPHISM in `Cat`, not a pullback in `Cat` (unrefuted at this cospan but unproved), and THIS ROUTE yields none in `StrictCat` either, since the round trip carries `id ∘ φ` and so does not transport `FibreProduct_IsPullback` across — a failure of this comparison, not a proof that no comparison works, and an earlier revision of this bullet asserted the stronger "not one in `StrictCat` either". **`CreatesLimit_transport`** is the generic lemma the assembly needs and the tree lacked: creation transports along a natural isomorphism of the functor, the creation analogue of #428's `PreservesLimitCone_transport` (`Functor/Hom/Continuous.v`). It carries CREATION, not strictness — there is still no `StrictlyCreatesLimit_transport` and none is claimed. Its natural home is `Structure/Limit/Creation.v`, where appending is line-neutral for all fifteen of that file's citations (the highest of them well inside a 518-line file); it lives here to avoid rebuilding that file's large reverse-dependency set, and a later move costs nothing. Exercise 4 itself, `comma_proj2_creates_second`, composes the equivalence's creation with Exercise 3 at the coslice cospan and transports along `ex4_functor_iso`, whose object and arrow actions agree with `comma_proj2` at `eq_refl` while the two functor RECORDS do not (n1). **It is a CROSS-CHECK of #438, not a strengthening** — but against the right constant. Against `comma_CreatesAllLimits` the two are of EQUAL strength: that one's `PreservesImageLimit` premise quantifies over every shape exactly as `ContinuousFunctor U` does, the two being interderivable by identity functions in that very file (both in `Construction/Comma/Creation.v`), and its conclusion `CreatesAllLimits` is pointwise `CreatesLimit`, not `StrictlyCreatesLimit`. The "weaker in both directions" comparison is true, and true only, of `comma_StrictlyCreatesLimit`, which takes the per-diagram `PreservesLimitCone (Gdiag K) U` and concludes `StrictlyCreatesLimit`. An earlier revision of this bullet, of the file header and of the commit message made that comparison against `comma_CreatesAllLimits`; measured with `Check`, it was false in both halves, under-selling this route while crediting that constant with a premise and a conclusion it does not have. The issue's two Verification-block names, `creation_pullback_stable` and `comma_creates_limits_second_proof`, ship as binder-explicit aliases, each `eq_refl`-equal to the constant it names. The premise of Exercise 4 is inhabited with nothing assumed: `Id_ContinuousFunctor` is `right_adjoint_Continuous` at `adj_id`, so `ContinuousFunctor Id` holds for every category — the same shape as `Construction/Slice/Creation.v`'s `Id_PreservesImageLimit` — and `coslice_comma_proj2_creates_via_pullback` and `coslice_comma_Complete_via_pullback` are unconditional, which is what keeps the Exercise 4 results from being vacuous. Measured: 59 `.glob` heads (51 `def`, 8 `prf`) plus 28 `Program` obligations = **87 constants Closed under the global context**, all gated (the gate's absolutes are read by `grep -c "echo 'Print Assumptions"`, which is the criterion under which the target grows 7323 → 7410; plain `grep -c 'Print Assumptions'` gives 7325 → 7412, counting the banner and one double-quoted line, and the delta is +87 either way); closure 65 excluding self (probe 66), of which `Instance/Cat/Pullback.v` costs 11 at the margin and `Construction/Slice/Creation.v` **7** — an earlier revision said 8 — every other `Require` 0, and two of the original 23 named nothing in the file body and were dropped, leaving 21 — `Category.Construction.Slice` and `Category.Structure.Limit`, both transitively supplied and both at margin 0, so the drop saves no build time and is hygiene only (an earlier revision kept the second on the false ground that the file "names `Limit` directly"; no identifier from that module occurs in the body); sixteen `Defined.` occurrences, twelve at line start, **TWO** load-bearing under a flip — `fun_equiv_whisker_r`, whose `Qed` stops the file at `CreatesLimit_transport`'s `change` with "Not convertible", and `fp_lift_leg`, which stops it at `fp_lift_coh` — the other fourteen kept transparent as functor and cone data and disclosed (an earlier revision said ten `Defined` with one load-bearing, measured before the tactic rewrite of `fun_equiv_whisker_r` added the sixteenth); renaming each of the 59 heads, in that file alone, stops the probe every time, all 59 first breaks on a positive line and none inside a refutation (58 measured by the audit before the strict all-shapes packaging was added, the 59th after); zero collisions over the heads and the probe's names, after two renames of which only one was forced — `sq_obj` is genuinely `Theory/Diagram.v`, while the primed `cast_unit'` broke only the gate's SINGLE-quoted `echo` style and could have been carried by the double-quoted style the gate already uses at `Makefile`, so `cast_unit_cod`/`cast_unit_dom` is a choice, recorded as one; `make todo` +4, all four the probe's refutation lines. Two disclosures of prior art that this file does not consume: `fp_square_iso` re-derives `Instance/Cat/Pullback.v`'s `FP_commutes_cat` (whiskering that with this file's `fun_equiv_whisker_r` typechecks at exactly `fp_square_iso`'s type, the two terms not being convertible), and `islimitcone_dtransport` has no consumer anywhere, shipped for symmetry with `islimitcone_dtransport_inv`, which is the one the kit rests on. `Test/ProbeCatCreation439.v`: 4 refutations = 1 instrument + n1, n2 CONVERSION + n3 TYPING, each stripped one at a time in a copy of the whole file with the error read, against paired controls, plus `eq_refl` readbacks for the strict lift's apex and legs, the square, and the two aliases, and two type ascriptions that pin what the prose claims rather than mere existence — Exercise 3's argument list, carrying no `uip`, and `CreatesLimit_transport`'s conclusion, `CreatesLimit` and not `StrictlyCreatesLimit`. Its import list contains the target's in full; an earlier revision claimed to mirror it while omitting `Adjunction/Continuity` and `Instance/Adjoints`, the short-import shape that makes probes pass vacuously. The `p439_square` readback pins `fp_square_obj`'s TRANSPARENCY, its definiens being `` `2 (K j) ``, not the commutation of the square; the other seven controls compare genuinely distinct expressions. NOT delivered: no abstract Exercise 3 over an arbitrary `IsPullback` square, only over the concrete `FibreProduct` — the lifted cone would need a functor out of an augmented shape the tree does not have; no `HasPullbacks Cat`, still neither proved nor refuted; no pullback statement for the comma category in `StrictCat` either, and no refutation of one — only the observation that THIS comparison does not transport; no dual, `FP_fst` creating limits being a re-derivation through `eq_sym` rather than a duality argument, parallel to #438's missing `comma_proj1`; no strict creation for `comma_proj2` by this route, #438's `comma_StrictlyCreatesLimit` remaining the only strict statement for it; no colimit dual; and no witness for Exercise 3's two premises at a cospan other than the coslice one, and no concrete category at which the Exercise 4 corollaries are instantiated beyond the generic `Id`
- **Construction/Coproduct/Indexed.v**, **Instance/Cat/Coproduct.v**: Mac Lane §III.5 Ex 4 + Riehl 2nd ed. §3.6 — the indexed disjoint union of categories and **`Cat_HasIndexedCoproducts : HasIndexedCoproducts Cat`**, the dual the tree had only in its product half. **THE HEADLINE IS THAT NO HYPOTHESIS IS SPENT ON THE CONSTRUCTION OR ITS UNIVERSAL PROPERTY** — `SigmaCat`, `SigmaCat_inj`, `SigmaCat_case`, `SigmaCat_case_inj`, `SigmaCat_case_unique` and `SigmaCat_ump` take NO decidability and NO UIP, on the index or on anything else (verified at the signatures, not inferred). The reason is structural: every obligation quantifies over BOTH endpoint indices, so the index proof `e : i = j` relates two universally quantified variables and can be ELIMINATED by `destruct`, after which every transport disappears and the goal is the summand's own law; uniqueness survives because `∀ i, G ◯ inj i ≈ F i` is a DEPENDENT FUNCTION of `i` and so already carries whatever coherence the index type's paths demand. **The encoding is what buys that**: `hom (i;x) (j;y) := ∃ e : i = j, ob_cast C e x ~> y` with the hom-SETOID `∃ p : e₁ = e₂, mor_cast C p f ≈ g` comparing the two index proofs at LEIBNIZ equality — the `Theory/OGraph.v` move of localizing the escape to a SETOID CHOICE. Read that precisely: the setoid QUOTIENTS BY NOTHING, which is exactly why fullness later needs UIP. **Rival encodings are refuted BY ARGUMENT, and the file says so rather than implying they were compiled** — THREE refuted plus one excluded, an earlier revision of this bullet having called all four "refuted": a decider is not refuted at all, it is EXCLUDED by the exercise's hypotheses; the `∀ e : i = j, …` reading fails twice over, once on MATHEMATICS (for provably distinct `i`, `j` that type is INHABITED by the vacuous function, so it adds a morphism between every cross-summand pair and yields the INDISCRETE category on the summands, not their disjoint union — this half IS compiled, in the probe, as `probe_forall_encoding_inhabited`) and once because no identity is definable at a loop; the sigma-plus-∀ HYBRID inherits that second failure; and a conversion-only setoid on index proofs is not a category, index-proof associativity holding only propositionally. **WHERE A HYPOTHESIS IS SPENT IT IS PROVED NECESSARY**: `SigmaCat_inj_Full` takes `IdxUIP I`, and **`sigma_inj_Full_forces_UIP`** shows fullness of ONE injection — given an object of that summand with the relevant hom-sets inhabited — ENTAILS `∀ e : i = i, e = eq_refl`, instantiated at the terminal category as `inj_Full_forces_UIP_at_One` (`_1`'s hom family ignores its endpoints, so the inhabitation hypothesis is `ttt`); the pattern is `Theory/OGraph.v`'s `coarse_respectfulness_entails_UIP` and `Theory/Category/Monoid.v`'s `arrow_mul_respects_forces_UIP`. **Faithfulness SPLITS**: free for a CONSTANT family (`SigmaCat_const_inj_Faithful`, via the retraction `SigmaCat_case (fun _ => Id)`, which is definitionally the identity on the summand), `IdxUIP` plus the axiom-free `UIP_shift` in general — and **no necessity result is claimed for faithfulness**, no countermodel being exhibited. By-product: **`Faithful_cancel : Faithful (G ◯ F) → Faithful F`** is new, the tree having only the closure direction `Faithful_Compose` (`Theory/Functor.v`). **The `Colimit`/`DiscreteCat_Functor` route the issue prescribes is DECLINED for TWO independent reasons kept apart**: routing an indexed product through `Limit (DiscreteCat_Functor …)` pins the ambient hom and proof universes to `Set` — fatal at `Cat`. **CORRECTED by #339's measurement, which this sentence originally got wrong by naming one donor: it takes TWO.** `DiscreteCat_Functor` (`Instance/Discrete.v`) is universe-unannotated and fixes the SHAPE at `DiscreteCat@{u Set Set}`, but ALONE it leaves the ambient FREE (`C : Category@{u0 u2 u2}`), and a `Cone` over it is free too, so `Cone` is NOT a co-donor; the ambient pin arrives only with `IsALimit`/`Limit`, which identify the shape's hom and proof with the ambient's. `Structure/Limit/Product/Finite.v`'s bullet cites the composite and is sound; `Functor/Hom/Limit.v`'s names both donors and was right all along. `DiscreteCat` ITSELF is properly annotated `@{o h p}`, so the defect is the FUNCTOR's — AND, independently, **the passage does not exist**: `Structure/Limit/Coproduct.v` carries `colimit_is_indexed_coproduct` (Limit → IsIndexedCoproduct) with NO converse, so a `Colimit` reading is not one step from what is proved. The elementary apex-pinned `IsIndexedCoproduct` is taken first and `Build_HasIndexedCoproducts` second (the #326/#335/#336 discipline), both `:=` with NO tactic — and **the packaging is transparent in ALL THREE fields**, the class's object, injection AND universal-property fields returning the elementary data at `eq_refl` (probe), so the bundled class carries the very record `SigmaCat_IsIndexedCoproduct` proves rather than a rebuilt copy. **The binary comparison reaches ISOMORPHISM OF CATEGORIES**: `SigmaBool_strict_iso : SigmaCat (BoolFam C D) ≅[StrictCat] C ∐ D`, both round trips at `Functor_StrictEq_Setoid` with every object component `eq_refl`, with `SigmaBool_iso` the DERIVED and weaker `≅[Cat]` (equivalence) reading — no stronger identification is ACHIEVED (what is established is that `eq_refl` does not typecheck; no impossibility of a Leibniz equality is proved), the object types being a dependent pair against a tagged sum, refuted at `eq_refl` and pinned. **Universes measured in the CONSTRAINT BLOCKS**: `SigmaCat@{ui uo oh op us}`'s block is ALL BOUNDS and no identification, and it is SIX constraints rather than the three an earlier revision of this bullet listed — `ui <= us`, `uo <= us`, `oh <= op` (the last being `Class Category`'s own) PLUS `ui <= Projections.u0`, `uo <= Projections.u1`, `oh <= Projections.u1`, which are the stdlib's, `projT1`/`projT2` not being universe polymorphic, so every universe passing through a sigma is additionally capped below a fixed global level; no constraint in the block is an EQUATION, which is the headline, but the enumeration was incomplete — and **the explicit binders are LOAD-BEARING**, measured rather than assumed: written unannotated the same body minimizes so as to IDENTIFY the summands' hom and proof universes and is then rejected under `Constraint uh < up` where the annotated form is accepted (pinned). **The index universe does NOT bound the hom universe, because `eq` is Prop-valued** — a genuine asymmetry with `Construction/Product/Indexed.v`'s `PiCat`, whose hom IS an I-indexed dependent function, so that file's note about `I` bounding the hom universe is correct for the PRODUCT and does not carry over. The UMP-level results DO identify hom with proof (`C : Category@{a b b}`) and the cause is localized to a DONOR with controls: `Functor_Setoid` (`Theory/Functor.v`, an unannotated `Program Instance`) is rejected at separated levels while the functor TYPE `C ⟶ D` and a `Unique` over a hom-setoid both elaborate — and since `≈` on functors IS that setoid, no statement of the UMP in this library's vocabulary avoids it; not introduced here, not claimed unavoidable. **Smallness is measured with a PINNED instance**: at an index as large as `obj[Cat@{a b c d e}]` the construction still elaborates but lands one universe UP, so `SigmaCat_IsIndexedCoproduct` is rejected — written unpinned it succeeds, exactly the `Check (Cat : obj[Cat])` phenomenon `Theory/Size.v`'s erratum records. The Grothendieck route is MEASURED, not built: `Construction/Grothendieck/Strict.v`'s `Fuip` is UIP on the OBJECTS of every fibre (not on the index), so that route costs strictly more than the direct construction pays, disclosed as a measurement of the donor's stated hypothesis with no claim it could not be rearranged. `Construction/Product/Indexed.v`'s `PiCat` is the exact dual and is treated as a STRUCTURAL PRECEDENT only — the coproduct is not `PiCat` of opposites. Non-vacuity at empty (`SigmaCat_empty_no_obj`), singleton (`SigmaCat_unit_iso`, its one round trip's UIP on `poly_unit` discharged by Hedberg) and two-element indices (`bool_cross_empty_lr`/`_rl`, `bool_summands_not_isomorphic`, and the case functor computing by `eq_refl` with `case_separates_summands` by `discriminate`), with the general `sigma_hom_cross_empty`. **87/87 constants closed under the global context** (50 + 37, counted by `Print Module` so `Program` obligations are included). `Test/ProbeCatCoproduct338.v` pins the three boundaries the targets cannot — 2 FORMABILITY (the load-bearing binders; the `Functor_Setoid` localization, both needing a section that declares levels strictly apart) and 1 CONVERSION (the binary object types) — plus an instrument check and a compiled POSITIVE (`probe_forall_encoding_inhabited`, the inhabitedness half of the encoding-(c) refutation), each negative stripped and its kind read off the error, **rename-simulated 3/3 unpadded** against an instrument-checked baseline; the denominator counts only constants a NEGATIVE names, read off the `Fail` commands rather than from memory, an earlier revision having scored it 4/4 by crediting `SigmaCat_inj`, which occurs only in a control. TWO probe defects were caught before landing and are recorded rather than quietly fixed: a fourth negative fired on an over-application typing error while the statement it reached for was itself FALSE (the packaging being definitional in all three fields), and `Instance/Cat/Coproduct.v` shipped a FALSE PASS whose `Fail` fired on `∐` binding looser than `=` — `SigmaCat (BoolFam C D) = C ∐ D` parses as `(SigmaCat (BoolFam C D) = C) ∐ D` — with the adjacent comment asserting a conversion failure it did not have. NOT delivered: no notation; no `Colimit` reading; no `Cocomplete Cat` and no `HasIndexedProducts Cat`; no functoriality of `SigmaCat` in `C` or `I`, no dual, no distributivity; no relation between the two coproducts' universal properties AS RECORDS and none with `Cat_Cocartesian` (the binary comparison is between the two CATEGORIES); no `Initial`/`_0` identification at the empty index, only `SigmaCat_empty_no_obj`
- **Instance/Cat/Limit.v**: Mac Lane §V.1 Exercise 5 (book p. 112, `maclane:V.1:ex5`) + Awodey §7.1 (`awodey:7.1:construction-cat-equalizer`, `awodey:7.1:remark-cat-complete`) + Riehl §3.1 Exercise 3.1.x, §3.6 Proposition 3.6.6, §4.6 Corollary 4.6.15 (`riehl:3.1:exx`, `riehl:3.6:prop6`, `riehl:4.6:cor15`) — **`Cat` has all set-indexed products outright, and `StrictCat` is small-complete under two hypotheses, each proved necessary**. `Cat_HasIndexedProducts : HasIndexedProducts Cat` is UNCONDITIONAL and its object field IS `PiCat`, its projections `PiCat_Proj`, its universal property `PiCat_ump` (Construction/Product/Indexed.v consumed, nothing rebuilt; the probe reads the class's object and projections back at `eq_refl`). The equalizer of `F G : C ⟶ D` is Awodey's and Riehl's SUBCATEGORY verbatim — `EqSub F G : Subcategory C` on the objects with `F c = G c` at LEIBNIZ equality and the arrows whose two images agree after transport (`hom_cast p p' (fmap[F] f) ≈ fmap[G] f`), `EqCat F G := Sub C (EqSub F G)`, `EqIncl := Incl C (EqSub F G)`, the fork equation `Eq_commutes` a `:=` with no tactic — and its universal property `Eq_IsEqualizer (uip : ObjUIP D) : @IsEqualizer StrictCat C D F G (EqCat F G) (EqIncl F G)` is stated in **`StrictCat`**, packaged as `StrictCat_HasEqualizers : (∀ C, ObjUIP C) → HasEqualizers StrictCat` (Structure/Equalizer/Fork.v's class, the issue's requested form); `StrictCat_HasIndexedProducts : (∀ C, ObjUIP C) → DepFunext → HasIndexedProducts StrictCat` on the same `PiCat`, and `StrictCat_Complete := Complete_from_products_equalizers …` — Mac Lane's "by products and equalizers" as ONE application of #416's theorem. **THE ISSUE'S PINNED `Cat_HasEqualizers`/`Cat_Complete` ARE NOT DELIVERED, FOR RIEHL'S OWN REASON ON THE ISSUE, AND THE REASON IS A THEOREM HERE**: this `Cat` is Ho(Cat) (`Functor_Setoid`, natural isomorphism), and `EqCat_not_Cat_equalizer` shows the on-the-nose subcategory is NOT an equalizer in it — at the pair `1 ⇉ Chaotic bool` picking `true` and `false`, two functors EQUIVALENT in `Cat` (`ChaoticBool_points_equiv`) whose strict equalizer has no objects (`EqCat_ChaoticBool_empty`) while `Id[1]` is a competing fork; read narrowly, it refutes THIS apex at THIS pair, and whether Ho(Cat) has equalizers or is complete is NEITHER proved NOR refuted, the inserter replacement UNATTEMPTED. `Chaotic@{o h p}` (a `poly_unit`-homed chaotic category, free in all three universes where `Indiscrete` pins `Set`) is the witness shape throughout. **WHERE THE HYPOTHESES ARE SPENT, AND THAT BOTH ARE NECESSARY**: `ObjUIP D` enters the equalizer side at ONE lemma (`Eq_obj_eq`, feeding `Eq_med_unique`; `Eq_commutes`, `Eq_med`, `Eq_med_incl` and all five obligations take none), `DepFunext` and `ObjUIP` the product side at ONE lemma (`pi_obj_eq`, feeding `PiCat_ump_unique_strict`; `PiCat_Pair_Proj_strict` takes neither); `Eq_uniqueness_forces_UIP` derives UIP for every type at the small object universe from the uniqueness clause stated uniformly (loops at a point of `Chaotic X` name mediators; axiom-free through `inj_pair2_eq_dec` at `poly_unit`), Instance/Cat/Pullback.v's `FP_uniqueness_forces_UIP` one shape over, and `PiCat_uniqueness_forces_funext` derives dependent function extensionality the same way — each about the uniqueness clause OF THIS APEX, refuting nothing about other apexes. Relation to #337: same design (StrictCat, `ObjUIP`, loop-space necessity); pullbacks are a special case in principle but no derivation of `StrictCat_HasPullbacks` from `StrictCat_Complete` is built (it wants a `Cartesian StrictCat` that does not exist), Pullback.v is not required (its own closure is 38; requiring it would take this file's from 52 to 58) and its `fmap_hom_cast` is restated as `eq_fmap_hom_cast`. Riehl's `ob`/`mor` lead-in is not taken — Instance/Cat/Objects.v refutes an objects functor on this `Cat` into a Leibniz-comparing target — and Awodey's coequalizer caveat scopes the colimit side out explicitly. **Universes off BOTH binder and block over all 58**: `Cat_HasIndexedProducts@{i so sh u u0} : HasIndexedProducts@{i i i so u so} Cat@{u so u0 so sh}` with, among ten constraints, the bounds `i <= sh`, `i <= so`, `sh <= so` and NO equation, the explicit binders LOAD-BEARING (with the universe binders removed and the lambda binders untyped, `i`, `so` and `sh` minimize to one level, `Cat`'s ambient level staying separate); the bound `sh <= so` is `PiCat_Proj`'s, MEASURED — at `so < sh` the product at `Category@{so sh sh}`, the product as an object of `Cat@{_ sh _ so sh}`, the class applied up to the projections and the bare `PiCat_Proj C a`/`PiCat_ump R` are all accepted while `PiCat_Proj C a : PiCat@{i so sh so sh} C ⟶ C a` is refused (its declared `PiCat` instance identifies the index with the small hom universe and bounds the product's OBJECT universe below by it), two annotations of `Cat`'s hom universe measured NOT to lift it, not claimed unavoidable; the equalizer development is over `C : Category@{u u0 u0}`, `D : Category@{u1 u2 u2}` in the BINDER with `EqCat` carrying no equation, `EqIncl` `u0 = u2`, and every `StrictCat` equation `u = u1`, `u0 = u2` (StrictCat identifies both categories' object AND hom universes); `StrictCat_Complete@{u u0 u1} : … → Complete@{u0 u0 u0 u}` carries the SMALLNESS side condition — diagram shapes at the small categories' own `Category@{u0 u0 u0}` — forced by `Complete_from_products_equalizers`; the necessity theorems match the sufficiency shapes (`Eq_uniqueness_forces_UIP` no equation, `PiCat_uniqueness_forces_funext` `h = i`); ZERO word-bounded `Set` in the binder or block of any of the 58. **58/58 constants closed under the global context, zero `Axioms:` lines** (40 heads + 18 `Program` obligations, all gated fully qualified); closure 52 excluding self; ten `Defined`, THREE load-bearing by flipping each alone (`Chaotic` for the library, `Eq_IsEqualizer` and `StrictCat_HasEqualizers` for the probe's readbacks); zero collisions after three renames (`punit_eq` is Pullback.v's; `CB` matched two files' section variables and one section-local `Notation CB`, Structure/Limit/Power/Adjunction.v). `Test/ProbeCatLimit414.v` mirrors the `Require` list and carries **10 refutation commands = 1 instrument + 9 negatives of THREE kinds told apart by the error text** — 6 FORMABILITY (the `PiCat_Proj` donor beside five accepted controls, its two inheriting constants, the strict product and completeness at an index universe strictly below the hom universe, and their donor `Complete_from_products_equalizers`), 1 TYPING (`Eq_IsEqualizer` ascribed at `@IsEqualizer Cat …`, no `cannot unify`, no universe clause), 2 CONVERSION (the two triangles as whole functor records beside their `eq_refl` data controls) — each stripped one at a time in a copy of the whole file, guard coverage measured (48 inside / 41 outside, seven exhaustive exceptions), rename-simulated 9/9 with every break on a `Check` or control line of the probe (one on an `Example`), none inside a refutation command; `make todo` grows only by the probe. NOT delivered: any statement about Ho(Cat)'s equalizers or completeness; the inserter; any colimit; the pullback special case as a theorem; `Cartesian StrictCat`; the `ob`/`mor` functors; an axiom-free inhabitant of `DepFunext` or of blanket `ObjUIP`; nothing registered as an `Instance` except the hypothesis-free `Cat_HasIndexedProducts`
- **Construction/Product.v**: Product categories C × D
- **Construction/Comma.v**: Comma categories (F ↓ G)
- **Construction/Comma/Special.v**: Mac Lane §II.6's remark as recorded statements — the comma of two CONSTANT functors is the "discrete category on the hom-set", the historical origin of the comma notation, delivered at the honest setoid strength: a comma morphism's square degenerates to a WITNESS of `f ≈ g`, so the target is `DiscreteSetoidCat` (objects the carrier, homs the `≈`-witnesses — built here because Instance/Proset.v's `Proset` is Prop-valued `relation` while `≈` is a crelation and the comma carries its square as sigT DATA; `Blurry` of Instance/Discrete/Reconstruct.v is this construction at the `eq` setoid, cited not imported), with `Comma_Discrete_Hom : (=(b) ↓ =(a)) ≅[Cat] HomDiscrete b a` (one round trip definitional on objects, the other renormalizing unit paddings; the comma-side laws instances of `comma_const_thin`, the HomDiscrete-side ones discharged by that category's own trivially-true hom-setoid). The plain `DiscreteCat` reading is PRICED EXACTLY: `Comma_Discrete_eq` (unconditional functor, ESO), `Full ↔ HomStrict` (≈→=), `Faithful ↔ HomUIP`, the conditional `Comma_Discrete_Hom_eq` under BOTH hypotheses — HE alone is NOT sufficient, since DiscreteCat's hom-setoid is strict eq of proofs — the unconditional necessity `comma_discrete_iso_forces_UIP` (ANY Cat-iso forces HomUIP, via `Cat_Iso_to_Faithful` + thinness), and the countermodel `Blur_no_discrete_iso` (one object, bool homs, trivially-true hom-setoid: HomUIP HOLDS yet the plain reading is FALSE — all comma objects isomorphic, `fobj_iso` extracts `true = false`), with `One_Comma_Discrete_Hom_eq` the degenerate inhabitation check. Header carries the specialization table (slice/coslice/arrow/discrete-hom/`Two_Fun_Arrow`) and upgrades Construction/Product/Comma.v's prose. Universe finding recorded in-file: `Full`/`Faithful`/`EssentiallySurjective` carry three universe parameters identifying source and target hom+proof levels, so `DiscreteCat_Functor` minimizes to Set and is UNUSABLE here — the discrete-side functors are hand-built. The Ex 2 half is **Construction/Slice/Terminal.v**: `Slice_Terminal : Slice C 1 ≅[Cat] C` and `Coslice_Initial` — Mac Lane says "isomorphic", and the on-the-nose reading is MACHINE-CHECKED FALSE: `slice_terminal_not_strict` (in Special.v, adapted from the adversarial audit's probe) refutes `Slice Blur ttt ≅[StrictCat] Blur` over `Blur` with a terminal instance — slice objects carry `f : a ~> t` as data and `one_unique` gives only `≈`, so `Blur`'s two `≈`-identified arrows give the slice two objects where the base has one; the achieved strengths are recorded asymmetrically: `Slice_Forget ◯ Slice_Section` is Id at STRICT `Functor_StrictEq_Setoid` strength (`Slice_Terminal_strict_section`, object witness eq_refl), the other composite Id up to natural iso with identity carriers; `Coslice_Initial` proved DIRECTLY because `Coslice` is not definitionally `(Slice (C^op) c)^op` — the hom equations differ in ORIENTATION, disclosed rather than transported around. 46/46 constants closed under the global context
- **Construction/Comma/Diagram.v**: Mac Lane §II.6's commuting diagram over the arrow category, displays (5)-(6) + Exercises 3 and 5 — `Arrow_dom`/`Arrow_cod : Arrow C ⟶ C` as DEFINITIONAL specializations of the comma projections (`Arrow_dom_is_comma_proj1` by `eq_refl`; the Ex-3 arrow-level readings for dom, cod, P, Q all `eq_refl`), `Comma_to_Arrow : (S ↓ T) ⟶ Arrow C` (Mac Lane's R; the square obligation IS the comma morphism's own square), and the diagram equations `comma_diagram_dom`/`_cod` at STRICT `Functor_StrictEq_Setoid` strength with `eq_refl` object components (Cat-level corollaries derived, labelled strictly weaker; Leibniz record equality not claimed — the law fields differ as proof terms). `comma_proj_nat` could NOT be plugged in and the file measures why: its component is a Program obligation that matches on the comma object, and neither sigT nor prod has definitional eta here, so it is not convertible with `` `2 x `` — the relationship is recorded both ways instead (`Comma_to_Arrow_is_proj_nat` Leibniz, `Comma_to_Arrow_naturality` by `naturality_sym`). Ex 5 is **`comma_diagram_ump`**: with the two hypotheses at strict strength (house ToArrows idiom, no UIP taken), a mediator with STRICT `comma_mediator_proj1`/`_proj2`/`_arrow` factorizations — existence on the nose in Mac Lane's sense — and uniqueness at `≈[Cat]`. The file's real finding, argued in the header: the three commuting equations are INDEPENDENT data in a proof-relevant setting (S need not be injective on objects, so the R'-equation yields no A-equation), and a competitor carries TWO independent proofs of one object equation whose reconciliation is exactly UIP on C's objects — so `MediatesDiagram` states the third equation RELATIVE to the first two (transported along their object equalities, `≈` on the mediating arrow), satisfied on the nose by the built mediator; strict uniqueness of objects and the pseudo/2-categorical comma OBJECT (Construction/Comma.v's documentation-level deferral) are both named as not delivered. Non-vacuity, both halves: `comma_diagram_self` — the comma's own diagram is a competitor and the produced mediator is `≈[Cat]` the identity (strictly so, `comma_diagram_self_strict`) — and `comma_id_mediates`/`comma_diagram_self_via_ump` (audit-adopted): the identity satisfies `MediatesDiagram` by three reflexivities, so the UNIQUENESS theorem is exercised at the one competitor whose answer is forced. Mac Lane's literal uniqueness clause is rendered, not subsumed — the competitor class is incomparable with his (≈ on the mediating morphism, relative to the competitor's proof families), itemized in the not-delivered list. 48/48 constants closed under the global context
- **Construction/Comma/Natural/Transformation.v**: Huq's correspondence made a BIJECTION (Mac Lane §II.6 Ex 4) — the file had both directions (`Comma_Functor` τ ↦ section functor, `Comma_Transform` conjugated recovery from a functor + up-to-iso section witnesses) with no round trips; now: section lemmas at BOTH strengths (`Comma_Functor_proj1_strict`/`_proj2_strict` at `≈[StrictCat]` ON THE NOSE — Mac Lane's literal `P τ = Q τ = id`, transports vanishing by iota — and Cat-level canonical witnesses built DIRECTLY with `iso_id` rather than through the Qed-opaque `strict_equiv_implies_fun_equiv`, whose components would not reduce for round trip 1), both round trips (`Comma_Transform_Comma_Functor` at canonical witnesses, cat-trivial once the witnesses reduce; `Comma_Functor_Comma_Transform` for ARBITRARY (F,p,q), the comparison iso a transparent Definition so its components stay usable), and **`Huq_roundtrip` : an `@Isomorphism Sets` between the transformation setoid and `HuqSection` triples** under the GEOMETRIC compatible-witness equivalence `HuqCompatible` (a comparison iso θ through which both witness families factor). The design crux, THEOREMS not prose: `Comma_Transform` genuinely depends on the witnesses — `huq_witness_separates` exhibits, over `B(ℤ/2)` = `Deloop Bool_Xor`, TWO witnesses on ONE functor whose recovered transformations differ (`false` vs `true` by `eq_refl`), so a witness-forgetting setoid provably cannot carry the backward map, and `huq_witness_sections_incompatible` shows the quotient is strictly finer than functor equality (non-vacuous both ways); `Comma_Transform_witness_shift` is the general conjugation formula relating any two witness pairs, with `_witness_irrelevant` its ≈-level corollary; and `huq_compatible_iff` proves the geometric equivalence IS exactly the kernel of `Comma_Transform` — both directions, with θ's components FORCED by the triangles and θ's naturality FREE (derived from the witnesses' coherence alone), so there is no gap between "isomorphic as sections" and "same recovered transformation". Header letter-correspondence note (his T and S are the file's S and T — both texts name the SOURCE first in their own letters), catalog-paraphrase attribution, the design note (phrased as such) that strict witnesses would relocate rather than remove the dependence, and the universe disclosure (general results fully polymorphic; the B(ℤ/2) witness alone pinned at `Category@{u Set Set}` by `Bool_Xor`'s bool carrier — the concrete-witness price, S3-precedent). The round-trip LEMMA avoids the kernel characterisation but the packaged `Huq_roundtrip` still consumes it (forward respectfulness + the setoid's Equivalence obligation), scoped precisely in-file. 41/41 new constants closed under the global context
- **Construction/Comma/Functorial.v**: Functoriality of the comma construction (Mac Lane §II.6 Ex 6) — the one-sided actions on ARBITRARY transformations (`Comma_map_left (σ : S' ⟹ S) : (S ↓ T) ⟶ (S' ↓ T)` contravariant, precomposing the mediating arrow; `Comma_map_right` covariant, postcomposing; the iso-restricted `Comma_Iso_to/from_Left/Right` quartet has the SAME fobj/fmap data by `eq_refl`, eight lemmas + four `≈` restatements — `Comma_Iso` itself is Qed-opaque so no leg-level comparison is claimed; `Comma_Bifunctor_Iso` re-derives its statement through the bifunctor instead). `Comma_map := Comma_map_left ◯ Comma_map_right` makes ONE exchange order Leibniz-definitional (disclosed as such), and the OTHER order is measured exactly: pair-components and fmap agree by `eq_refl`, the mediating morphisms differ by ONE `comp_assoc` (`Comma_map_exchange_med`), the `≈` form with identity-carrier components (`Comma_map_exchange`). **`Comma_Bifunctor` : `(([A, C])^op ∏ ([B, C])) ⟶ Cat`** with laws at `Functor_Setoid` through named identity-carrier iso families; universes measured per constant — `Comma_Bifunctor`'s only identification is A/B/C's hom universes becoming one, which is `Fun`'s doing (it identifies source and target hom universes), with the identified level FREE not pinned (`Comma_Bifunctor_above_Set` instantiates strictly above `Set`, and the audit's companion probe instantiates it AT `Set`); the two non-vacuity witnesses identify MORE — `Comma_postcompose` four hom universes, `Comma_precompose` five — which is `Compose`'s doing (its three categories share one hom universe), neither mentioning a functor category; `Comma_map_left`/`_right`/`Comma_map`/`Comma_reindex` identify nothing. Part (b) DELIVERED: `Comma_reindex` over six independent categories from raw square families (chosen over `Transform` phrasing to avoid Conjugate.v-style padding bookkeeping, disclosed), recovering `Comma_map` at identity index functors (`Comma_reindex_recovers_Comma_map`, fobj at Leibniz `=` modulo prod's missing eta), with non-vacuity beyond identities (`Comma_postcompose` varying the codomain, `Comma_precompose` varying both domains); functoriality of reindexing in the triple explicitly scoped out (no category of cells pinned down by the exercise). 53/53 constants closed under the global context
- **Construction/Comma/Creation.v** (extended) and **Construction/Slice/Creation.v**: Mac Lane §V.6 Lemma 1 and Exercise 1 (book pp. 121-125), §V.1 Exercise 1 (p. 112), and Riehl §3.4 Proposition 8's limit half, §4.7 Lemma 2 and the exercise asking for that lemma's direct argument — **the comma projection STRICTLY creates the limits the base functor preserves, from a PER-DIAGRAM hypothesis, and the coslice projection strictly creates them with no hypothesis at all**. Three gaps, each measured before it was filled. (i) HYPOTHESIS: `comma_limit` demanded the all-shapes `PreservesImageLimit`, so the two per-shape clauses Mac Lane's Theorem 2 consumes were not derivable; `comma_limit_at` re-derives the whole of Construction/Comma/Limit.v's construction from `PreservesLimitCone (Gdiag K) U` at the ONE base diagram, with the original tactic scripts and about a hundred lines of re-derivation, ADDITIVELY — Comma/Limit.v is not touched, because nine `.v` files and nine planning or documentation files cite it at fifteen distinct line numbers (`grep -rloE 'Comma/Limit\.v:[0-9]+'`; ten `.v` files at this revision, one of them added by the same commit). The two-sided analogue is prior art and is named as such: `Construction/Arrow/Limit.v`'s `comma_proj_StrictlyCreatesLimit` did this for `comma_proj : (S ↓ T) ⟶ A ∏ B` from a hypothesis of the same shape; the functors differ and there is no `StrictlyCreatesLimit_compose`, so the re-derivation was not avoidable. (ii) STRICTNESS: `comma_CreatesLimit`'s `creates_lift` DISCARDS the cone it is handed and returns the lift of its fixed `L`, so the tree had no strict creation for `comma_proj2` — probe negative n5 refuses its apex against that cone's apex, against a paired control accepting the new lift; `comma_StrictlyCreatesLimit` builds the lift from `Build_Limit N HN` instead, so both `StrictLift` fields are `eq_refl` and `reflexivity` at EVERY limiting cone downstairs (Mac Lane's `F a = x` and `F σ = τ` in full), and `comma_CreatesLimit_at` carries no `L` argument at all. (iii) SHAPES: `comma_equalizer_at` at `Parallel`, which costs nothing since `Parallel` leaves its hom universe free, and `comma_IsIndexedProduct` stated ELEMENTARILY over `IsIndexedProduct`. The shape-indexed classes ship too, at full strength: **`comma_CreatesAllLimits`** over the all-shapes hypothesis, with `comma_CreatesProducts` its `DiscreteCat` instance through `CreatesAllLimits_CreatesProducts` — an earlier revision of this bullet said the discrete route was FORCED shut, and that was wrong; the `Set` pin belongs to ELIMINATING the shape's `x = y` into a hom (`DiscreteCat_Functor`, `Instance/Discrete.v`, prints `DiscreteCat@{u Set Set} A ⟶ C`, and a hand-rolled eliminator is refused identically, even at `C = D = Sets`), not to `DiscreteCat`, whose universes are free, so what is refused is a diagram built AT A FAMILY (probe n2, "Cannot enforce Set = …") while a CONSTANT discrete diagram is formable and the class applies to it (`p438_const_creates`, the non-vacuity witness). The elementary form is kept because it takes the family directly, which is what Mac Lane's Theorem 2 consumes. The coslice, both ways: through `Comma_Coslice`, `Id_PreservesImageLimit` (`right_adjoint_PreservesImageLimit` at `adj_id`) makes `coslice_comma_StrictlyCreatesLimit` and `coslice_comma_proj_CreatesAllLimits` unconditional and `Coslice_Complete` conditional only on `Complete C` (witnessed at `Sets` in the probe) — but strictness does NOT survive that transport (n3, n4). It does not have to: `Coslice_Proj` (`Construction/Slice/Adjunction.v`) is unconditioned — its section's `Cocartesian` variable is never discharged into it — and both data fields reduce, so **`Coslice_Proj_StrictlyCreatesLimit` does Riehl's §4.7 exercise directly, with NO hypothesis whatever**, the structure maps `` `2 (K j) `` forming a cone with apex `c` whose mediator into the given limiting cone is the lifted object's own structure map; `Coslice_Complete_direct` follows without transport. An earlier revision recorded this as blocked, on the false premise that all three in-tree coslice projections carry a hypothesis: measured, only `Coslice_Forget` does. `n6` pins that the plain and transported projections are different functors, so neither result transfers to the other by conversion. Issue #438's "Current state" is FALSE tree-wide in three substantive claims — creation proper IS stated (`comma_CreatesLimit`), the reflection clause IS present (`comma_creates_reflect`), and the projection of the constructed limit IS asserted to be the given one (`comma_strict_apex`/`comma_strict_legs`, `eq_refl` at an arbitrary downstairs limit) — though two of the three are written as claims about Construction/Comma/Limit.v and remain true OF THAT FILE; half-false in a fourth (only `Comma_Complete` still consumes `Complete C`; `comma_limit` has taken its downstairs limit as a parameter since 28ee6e54); and stale in seven citations, six inside that file and one into `Instance/Adjoints.v`. Its LIBRARY-DEFECT item was already resolved by this file's first commit. Its Verification block's three names ship as aliases with binders written out, each `eq_refl`-equal to the constant it names and each printing with FEWER universe variables (27/23/11 against 33/32/16) because the originals' block equations — `u0 = u2` identifying `C`'s hom-and-proof universe with `D`'s, with two, four and one more besides — are solved into the aliases' binders; no block on either side mentions `Set`. Measured: 42 `.glob` heads (38 `def`, 4 `prf`, nine from the first commit) plus Slice/Creation.v's 30 = **72 constants Closed under the global context**, zero `Program` obligations, every one carried by the gate; closure 35 → 38 excluding self with the three added modules EXACTLY the three new `Require`s and `comma_CreatesProducts` needing none, `Adjunction/Representability/Sets.v` unchanged at 99, Slice/Creation.v 52 (of which +5 buys `Coslice_Proj` from Construction/Slice/Adjunction.v), probe 58; thirteen `Defined` across the two files, four stopping the file itself, two stopping the probe, seven not load-bearing and disclosed; zero collisions over the 72 heads and the probe's declared names; renaming each of the 72 in its DECLARING file alone, 71 stop the probe with every first break on a positive line and none inside a refutation, and the 72nd stops the satellite at the line that consumes it, so nothing survives a rename; `make todo` +7, all seven the probe's refutation lines. `Test/ProbeCommaCreation438.v`: 7 refutations = 1 instrument + n1/n3/n4/n5/n6 CONVERSION + n2 UNIVERSE, each stripped one at a time in a copy of the whole file with the error read, plus paired controls, sixteen `eq_refl` readbacks, a `Sets` witness, the constant-discrete-diagram witness and a 72/72 guard block. NOT delivered: no weakening of `Comma_Complete`'s exported type, so Adjunction/GAFT.v and Adjunction/SAFT.v are untouched; no per-shape completeness CLASS; no instantiation of `comma_CreatesProducts` at an arbitrary FAMILY over a generic `C`; no on-the-nose uniqueness of the lift, only `creates_lift_unique`'s `ConeIso`; no finiteness, the product index being an arbitrary `Type`; no creation by `comma_proj1`; and no colimit dual, Riehl's connected-colimit half being out of the issue's scope
- **Construction/Cylinder.v**: The cylinder C ∏ 2 and the universal natural transformation (Mac Lane §II.3 display (6), Riehl Lemma 1.5.1/Exercise 1.5.i) — named inclusions `Cyl_incl0`/`Cyl_incl1` with restriction-along operations, the universal `Cyl_mu` (component `(id, TwoXY)`), the classifying functor `Cyl_functor τ` (the mixed composition case IS the naturality square) restricting definitionally to S and T and carrying μ to τ, uniqueness `Cyl_functor_unique` (the crossing action is forced — `(f, TwoXY)` factors as `μ ∘ (f, TwoIdX)`), all bundled as `cylinder_universal`, and the inverse leg `Cyl_extract` (whisker μ) with the round trip `Cyl_functor_Cyl_extract` closing Riehl's bijection in both directions; the whisker condition for competitors is stated through the restriction isomorphisms in Arrow/Functor.v's intertwining form, and the file carries no Set-level universe pin (verified by instantiation above Set). The Exercise II.4.8/Awodey 7.16 comparison is quarantined in Construction/Cylinder/Arrow.v (`Cyl_transpose` = `Cat_Closed`'s transpose by `eq_refl`, `Cyl_to_arrow` via Shapes.v's `Arrow_of_Fun`, `cylinder_arrow_agree`: extracting the triple recovers (S, T, τ) up to `ArrowTriple_Setoid`, with `cylinder_arrow_natural` the setoid-sense naturality and a small-category instantiation Example) — that side measures at `Category@{Set Set Set}` (object universe included, a stronger pin than the donor `Arrow_of_Fun` carries; whether avoidable is unsettled), which is why it is a separate file
- **Construction/Slice.v**: Slice C/c and coslice c/C
- **Construction/Slice/Adjunction.v**: Mac Lane §IV.2 Exercise 11 (book p. 90, `maclane:IV.2:ex11`) + Awodey §9.9 Exercise 6 (starred, `awodey:9:ex6`) — **the coslice projection has a LEFT adjoint when `C` has binary coproducts, the slice projection a RIGHT adjoint when it has binary products, and the starred left-adjoint question is SETTLED in both handednesses**. `Coslice_Projection_Adjunction a : Coslice_Coprod a ⊣ Coslice_Proj a` (the issue's pinned name) with `Coslice_Coprod a` sending `c` to `(a + c; inl)` and `f` to `right f`, the transposition `(h; _) ↦ h ∘ inr` forward and `k ↦ (g ▽ k; _)` backward, through `Build_Adjunction'`; the covariant dual `Slice_Projection_Adjunction a : Slice_Proj a ⊣ Slice_Prod a` with `x ↦ (x × a; exr)`, built DIRECTLY so the tokens `Opposite` and `^op` occur in the header prose only; and `slice_proj_left_adjoint_iff_terminal a : { L & L ⊣ Slice_Proj a } ↔ IsTerminalObj a` with `coslice_proj_right_adjoint_iff_initial` its mirror, each direction a separately named lemma, over a section binding ONLY `{C} (a : C)` — the enabling facts being that `(a; id)` is terminal in `Slice C a` and initial in `Coslice C a`. Placed here rather than at the suggested `Coslice.v` because the file carries both duals plus the settle results (disclosed in-header). **READ THE OBJECT PRECISELY, because my brief had it backwards and the builder corrected it**: the coslice structure map of `Coslice_Coprod a c` is `inl : a ~> a + c`, while the UNIT `coslice_projection_unit a c` is `inr : c ~> a + c` — a bare `Definition` whose body is the constant, which is the reviewer check. **The four unit/counit grades against the class SPLIT MIRROR-IMAGE, two strict where the brief predicted one residue**: the coslice unit carries `id ∘ inr` on the LEFT (`coslice_unit_strict` states the residue literally at `eq_refl`; `unit = inr` is refuted and pinned), the coslice counit is STRICT on the underlying morphism (`` `1 (counit x) = `2 x ▽ id ``), the slice unit is STRICT (`` `1 (unit x) = id △ `2 x ``), and the slice counit carries `exl ∘ id` on the RIGHT — because `⌊id⌋` feeds the identity into the forward transpose and `⌈id⌉` into the backward one, and in each block exactly one of the two composes with its argument. **THREE OF THE ISSUE'S CLAIMS ARE STALE, and its central one is right**: "the functor itself does not exist" is FALSE — `Instance/Cat/Pullback.v`'s `Slice_proj` and `Coslice_proj` are this same three-line record (both DATA fields agree at `eq_refl` in the probe, the whole records refuted since the law fields are each file's own opaque obligations; rebuilt rather than imported because that file's coqdep closure is 39 against this one's 31, adding 14), and `Construction/Slice/Terminal.v`'s `Slice_Forget`/`Coslice_Forget` are its terminal/initial specialisations; "the whole slice development is four definitions plus two base-change functors" omits `Terminal.v`'s twelve declared results; and "a search for `⊣` finds only the commented-out stub" misses `Construction/Comma/Adjunction.v`. What it gets right: no coproduct-injection functor, no adjunction for either projection and no injection unit existed. Work item 4's "one `Opposite` transport away" holds up to isomorphism ONLY — `Coslice C a` is not `(Slice (C^op) a)^op` on the nose, the hom equations differing in ORIENTATION (pinned as a conversion negative against a control showing the OBJECT types agree definitionally), which `Terminal.v` already records. The issue's own transport route (`comma_proj2` across `Comma_Coslice`) is built in the probe as `p365_via_comma` and MEASURED: not even its object action reduces, `Comma_Coslice`'s `to` being `{| fobj := _; fmap := _ |}` with both data fields `Program` obligations. Universes measured off BOTH binder and block: every headline is over `C : Category@{u u0 u0}` — hom identified with proof in the BINDER — while every constraint block carries bounds ONLY, no equation and no `Set` anywhere across all 108 constants; FIVE donors each force the identification ALONE under `Constraint uh < up` (`Slice`, `Coslice`, `Cartesian`, `IsTerminalObj`, `IsInitialObj`), none claimed unavoidable. A `Set` pin WAS found and fixed: a constant `Sets`-morphism written as a `Program Definition` lets instance resolution supply `reflexive_proper@{Set Set}` — the `Theory/Universal/Element.v` hazard, second sighting — so `sets_point` writes the pointwise certificate by hand, and the two-element witness is the coproduct `1 + 1` in `Sets` rather than `bool`. `Sets` witnesses in both directions: both adjunctions at an arbitrary object with a COMPUTING transposition (`sets_coslice_transpose_computes` by `eq_refl`), `sets_coslice_unit_not_iso` for any inhabited `a` (so the adjunction is not an equivalence), and Block C exercised both ways — refuted outright at `sets_bipoint` (`sets_slice_proj_no_left_adjoint`, `sets_coslice_proj_no_right_adjoint`) and inhabited at `Sets_Terminal`'s and `Sets_Initial`'s objects. **108/108 constants closed under the global context** — 60 declared heads (the `.glob`'s `def`+`prf` entries, equal to the source count) plus 48 `Program` obligations (`Print Module` flattened and split on `Parameter|Definition`), no `Record`/`Class`/`Inductive` so no `Build_*`, and no `Fail` in the library file so nothing to subtract — of which the gate carries 27; **zero collisions over 73 names** (the 60 heads, the probe's 8 declared names and its 5 `Fail`-declared ones), instrument-checked at `Full`/`Monoid` = 3 each, after two real collisions — `sets_two`/`sets_two_distinct` against `Instance/Sets/Split.v` — were renamed to the `sets_bipoint*` family before landing. `Test/ProbeSliceAdjunction365.v` carries ALL the negatives, so the library file has zero `make todo` hits and the probe all 17 (13 `Fail` commands, 4 header prose): **12 negatives of THREE KINDS plus an instrument check** — 6 FORMABILITY (the five donors and the inherited `Coslice_Proj`, each "Cannot enforce up = uh"), 5 CONVERSION (both residues, the orientation, the whole-record `Coslice_proj` comparison, the transport route) and 1 TYPING (the reversed handedness — which ALSO ends in `cannot unify`, on the two CATEGORIES rather than two inhabitants of one type, so the tail alone does not separate it) — each stripped one at a time with the whole error read, the probe mirroring the target's full `Require` list plus four. NOT delivered: no comparison with the `Comma_Coslice`/`Comma_Slice` readings beyond the object-action measurement; no monad or comonad from either adjunction; no naturality in `a` (`Construction/Slice/Pullback.v`'s `Bang_Functor` untouched); no uniqueness of the adjoints, so both biconditionals assert EXISTENCE only; no equivalence-level relation to `Slice_Terminal`/`Coslice_Initial`, which are cited and not consumed; no `Σ_f ⊣ f* ⊣ Π_f`; no indexed variants; no `Sets`-side computation of the adjoint FUNCTORS beyond the transposition; and no proof that Block A's `Cocartesian` hypothesis is necessary
- **Construction/Slice/Pullback.v** (completed): Mac Lane §IV.5 Exercise 3 (book p. 97, `maclane:IV.5:ex3`) + Awodey §9.7 Proposition 9.18's left-adjoint leg (`awodey:9.7:prop18`) — **base change is right adjoint to composition on slices**: for `f : a ~> b` in a category with chosen pullbacks, **`Base_Functor_Adjunction pullbacks f : Bang_Functor f ⊣ Star_Functor pullbacks f`**, dependent sum on the LEFT. Both functors had existed since the file was written (`Bang_Functor` post-composition, `Star_Functor` the pullback functor under the section `Hypothesis pullbacks`); **the adjunction — the entire content of the exercise — survived only as a commented-out stub whose statement had the adjointness the wrong way round, and two documentation sites asserted more than that comment supported — `Structure/Pullback.v` that the adjunction "is built", `Construction/Slice.v` that it was "recorded in that file's header".** The hom-set bijection IS the pullback's universal property read twice: a slice morphism `Σ_f x ~> y` is a `g : o ~> p` with `k ∘ g ≈ f ∘ h`, and that equation is exactly the square condition of the competing cone `⟨g, h⟩`, so the transpose is `unique_obj` of `ump_pullbacks` and the inverse is postcomposition with `pullback_fst`; because `Construction/Slice.v`'s hom-setoid compares ONLY the underlying C-morphism, both round trips, both respectfulness obligations and both naturality clauses of `Build_Adjunction'` are equations in C alone, each one appeal to the uniqueness half. **THE UNIT AND COUNIT ARE NAMED, AND THEIR IDENTIFICATION WITH THE CLASS'S IS MEASURED AT TWO DIFFERENT GRADES RATHER THAN ASSUMED UNIFORM**: `bang_star_unit` at `(o; h)` is the comparison into `Pull (f ∘ h) f`, the mediator of the cone `⟨id, h⟩`; `bang_star_counit` at `(p; k)` is `pullback_fst k f`, **whose slice condition IS `pullback_commutes` verbatim — the constant is a pair carrying no proof of its own**; against the class, `bang_star_counit_strict` and `bang_star_unit_strict` hold at `eq_refl` (the counit as the projection composed with a residual `id`, the unit as `base_to_mor` of the slice identity) while against the bare named morphisms only `≈` holds, refuted and pinned — **and the unit's residue is NOT the counit's `∘ id` but the PROOF ARGUMENT**: both sides are mediators of the SAME cone with the SAME legs, `ump_pullbacks` takes the square condition as an argument the mediator depends on, and feeding it the slice identity's own witness against `id_right (f ∘ h)` gives two mediators related by `uniqueness` and not by conversion, exhibited by two shipped `eq_refl` Examples (`bang_star_unit_residue`, `bang_star_unit_mor_residue`) rather than described. **THE DELETED STUB'S ORIENTATION IS NOT ILL-FORMED, AND SAYING SO IS THE POINT**: `Star_Functor f ⊣ Bang_Functor f` TYPECHECKS — both functors have the shapes the class wants once the roles are swapped — so it is a well-typed statement that is simply false, which no `Fail` can express; the probe records it as formable AND **refutes it**, `reversed_orientation_refuted` deriving `False` over `Sets` along the unique `∅ ~> 1`, where base change carries every object over `1` to one with empty carrier while `Σ_f` leaves the carrier alone, so the unit such an adjunction would supply is a map from the singleton into the empty setoid (the proof consumes only `unit`; no morphism into the reversed adjunction is constructed). Read its scope: it refutes the orientation AT ONE `f`, which is what "false in general" needs and all that is claimed. **BOTH COMMENTED STUBS ARE DELETED** (the mis-oriented adjunction and an unrelated `Production` sketch), as the issue's Definition of Done demands, and with the second went the section's `Context {A : @Cartesian C}`, which existed only for it — **measured before acting**: `About` showed no `Cartesian` argument in either functor, so a `Context` variable never generalized into them, and after deletion both printed types are unchanged character for character; **what DID change, and what reading the type line alone would have missed, is the universe INSTANCE, which narrowed from `@{u u0 u1 u2 u3}` to `@{u u0 u1 u2}`** — isolated by a discriminating experiment (the original require list and section body, verbatim, with and without that one line), and safe because `coqdep` over the whole build set shows **no `.vo` in the tree depends on this module**, every in-tree mention being prose. **FOUR PROSE SITES ARE REPOINTED AT THE REAL THEOREM, TWO MORE THAN THE ISSUE NAMES**: `Structure/Pullback.v` and `Construction/Slice.v` are the issue's own (the first now names the theorem, the second said the adjunction was merely "recorded in that file's header"), and the other two were found by sweeping the collision report — `Construction/Slice/Adjunction.v`, which asserted "The stub is real … commented out", and `Instance/Powerset/Quantifier.v`, which said "its adjunction is a commented stub" while noting no `Π_f` exists (the second half stays true and is kept). **Π_f IS NOT DELIVERED AND THE REASON IS MEASURED RATHER THAN GUESSED**: no class of local cartesian closure is declared anywhere in the tree — the phrase occurs only in prose, in `Structure/Pullback.v`, `Construction/Slice.v` and `Instance/Sets.v` — and `Structure/Cartesian/Closed.v`'s `Closed` is a structure over a `Cartesian C` while **no `Cartesian` instance for a slice category is declared either**, so the dependent product would be built from scratch; the deferral is recorded as PROSE, never as commented code, which is what the issue asks. **Universes measured off BOTH binder and block**: every constant is over `C : Category@{u u0 u0}` — hom identified with proof in the BINDER — while **not one of the 47 constraint blocks contains an equation at all**, the binder-versus-block trap, and no block mentions `Set` (the file's single `Set` token is `#[local] Set Transparent Obligations`); three donors are each rejected ALONE under `Constraint uh < up` (`Slice`, `Pullback`, `Adjunction`) against controls accepted at those very levels, among them **`@Functor Cu Cu`, so `Functor` is NOT a donor** — the identification is inherited, not introduced here, and is not claimed unavoidable. **47/47 constants closed under the global context** (`Print Module`, each queried fully qualified: 35 glob heads plus 12 `Program` obligations, of which 10 pre-existed — the two functors and their eight obligations; the file declares no `Record`/`Class`/`Inductive`, so there is no unlisted `Build_*`) — and note the counting trap, that `Print Module` WRAPS the module head, so a whitespace-flattened name sweep harvests a spurious `Pullback`. Closure **27** excluding self, up ONE from 26 — the increment is `Instance/Sets`, which `Build_Adjunction'`'s hom-set isomorphism needs; the `Sets` instantiation lives in the PROBE because requiring `Instance/Sets/Pullback` would take that to 44, **+17 modules for a `Construction/` file** (probe closure 45). Zero collisions over all 47 target names and the probe's 23 declared names (27 heads less the four declared inside a `Fail`, which never enter the environment). `Test/ProbeBaseChange387.v` mirrors the target's full `Require` list and carries **9 `Fail`s = 1 instrument + 8 negatives of THREE kinds** — 3 CONVERSION (the two `≈`-only identifications and the whole-slice-morphism counit comparison, the last differing from the second in that the commuting witnesses differ too), 4 FORMABILITY (the headline plus its three donors), 1 TYPING (ascribing the delivered adjunction at the reversed type: a plain mismatch with no `cannot unify` and no universe clause, and a comparison of TYPES so it cannot false-pass through `reverse_coercion`) — each stripped ONE AT A TIME and compiled alone with the whole error read, rename-simulated **6/6** with every rename breaking the probe at a `Check` line, after a first cut left `Adjunction` named only inside its own negative and three `Check @…` controls were added for it. **The issue's appended checkbox asking to discharge the pullbacks hypothesis for `Sets` rests on a STALE premise** — it says `Sets` has no pullback instance and that only the finite-sets model does, whereas `Sets_HasPullbacks` is `Instance/Sets/Pullback.v`, an `#[export] Instance` landed by #333, which is the very issue #387 depends on; `Sets_Base_Functor_Adjunction` instantiates the headline there, and **the transposes COMPUTE** — five `eq_refl` Examples evaluate the forward transpose to a literal pair in the agreement sub-setoid, read the first coordinate back through the inverse, and evaluate the counit to `true` — correcting a hedge in the brief that the chosen mediator might not reduce (`Sets_IsPullback` is `Defined` and `sets_pb_med`'s `morphism` field is given explicitly, so the whole chain reduces on closed input). Non-degeneracy is proved rather than asserted: `sets_star_is_fibre` shows every element of `f* (bool; id)` carries `true` in its first coordinate, so base change genuinely cut the object to the fibre and is not the identity in disguise; the unit witness is degenerate in that its slice object is `(1; id)`, so the comparison lands in a one-element pullback — a fact recorded here and NOT in the probe, which does not label it. Four `Defined` tokens, **exactly two load-bearing** by flipping each alone to `Qed` (`base_adj` and `Base_Functor_Adjunction`; `base_to_snd` and `base_from_ok` compile as `Qed` with every readback intact). `make todo` grows by 9, all `Fail` commands in the probe, zero in the target. An engineering finding recorded HERE and not in the file, whose two comments say only that the arrow action and its leg equations are stated with the pullback objects already in reduced form: `to_adj_nat_r` cost FIVE constants beyond the expected single `uniqueness` appeal (`star_fmap_mor`, `star_fmap_fst`, `star_fmap_snd`, `base_nat_r_fst`, `base_nat_r_snd`, whose only consumers are each other and that clause), because `simpl` unfolds `fmap[Star_Functor f]` all the way to a raw `ump_pullbacks` application even at a variable argument, and because `rewrite` then fails on a goal that PRINTS the sought pattern — the composite's implicit MIDDLE OBJECT elaborates as `` `1 (fobj[Star_Functor f] _) `` rather than `Pull k f _`, convertible but opaque to keyed unification — so a named `star_fmap_mor` with the pullback form in its DECLARED type, plus two leg lemmas, is what makes the clause go through. NOT delivered: Π_f as above; the **topos/subobject-level `Σ_f ⊣ f* ⊣ Π_f`** of Awodey §8.8, whose left adjoint is direct image and so needs an image factorization the tree supplies only under a regularity hypothesis; no Beck–Chevalley condition; no comparison with `Construction/Displayed/Codomain.v`'s codomain fibration; no monad or comonad from the adjunction; no criterion for when the unit or counit is invertible; no naturality in `f` and no functoriality of `f ↦ Base_Functor_Adjunction f`; no witness at a category other than `Sets` and none with a non-identity structure map on the target slice object; and nothing registered as an `Instance`, the chosen pullback being a section hypothesis that must not become globally resolvable
- **Construction/Free/Quiver/Examples.v**: Mac Lane §II.7's worked free categories and Exercise 2 — the concrete identifications the tree's `FreeOnQuiver` had never been pointed at — it is used substantially elsewhere (Construction/Free/Quiver/Presented.v builds the presented category on it, Theory/Diagram.v the diagram/functor bridge), but nothing said what any PARTICULAR free category IS. The engine is ONE reusable lemma: `graded_free_thin` — a quiver carrying a rank function that every edge raises by one, injective on nodes, with edges unique up to `≈`, has a THIN free category — after which every round trip and every functor law below is an equation between parallel arrows and closes in a line; node-UIP is DERIVED from rank-injectivity by `Eqdep_dec.UIP_dec` on nat (Hedberg, axiom-free), so no UIP hypothesis is taken anywhere, and the real content is `graded_het`, the induction in the HETEROGENEOUS form `tlist'_quiver_equiv` demands (parallel paths pass through nodes only propositionally equal, that equality carried as data). **`ordinal_free : FreeOnQuiver (LinQuiver m) ≅[StrictCat] Ordinal m`** — Exercise 2, every finite ordinal free on its linear quiver, at ISOMORPHISM-OF-CATEGORIES strength (the `≅[Cat]` form is derived and labelled weaker), for EVERY m including the empty ordinal (`ordinal_free_0` is separate because the donor `Functor_of_Steps` is stated only for `Ordinal (S n)`; without it the headline would silently exclude the empty case). This is the theorem Instance/Ordinal.v's header had recorded as "remains open" — that note and its bullet above are updated in the same commit rather than left contradicting the tree. `arrow_free : FreeOnQuiver ArrowQuiver ≅[StrictCat] _2` with both comparison functors identity-on-objects, so both strict witnesses are literally `eq_refl`; the composable pair is the m=3 INSTANCE (`chain_free := ordinal_free 3`), not a duplicate construction, with its content delivered as PROVED hom classification — three singleton ascending hom-sets, three identity-only diagonals, three provably EMPTY descending ones, and `chain_e10_length = 2` pinning the composite as the two-edge path rather than a new generator — plus `linear_hom_iff` (a hom-set is inhabited exactly when the indices are ordered, and parallel paths agree). Bonus `Ordinal_2_strict_iso` supplies the strict form Ordinal.v had only at Cat strength. **ERRATUM (#300): the universe pin this bullet used to call "PROVED forced" is GONE. Read the correction precisely — the pin was REAL and the mechanism diagnosed here was RIGHT; what was wrong was only its PERMANENCE. It was forced by the constructor as written, and the constructor has since been generalized.** `Build_Quiver_Standard_Eq` did return `Quiver@{u u0 Set}`, and `FreeOnQuiver`'s `o <= h <= p` did drag objects down — but only because the constructor resolved `eq_equivalence` to the stdlib's `Corelib.Classes.CRelationClasses.eq_equivalence@{u}`, which carries ONE universe binder and so cannot separate carrier level from proof level. Naming the proof universe and spending `Lib/Setoid.v`'s own `eq_equivalence@{t u}` — polymorphic since it was written, for exactly this purpose — lifts it in six lines. **Scope the lift precisely** (an earlier draft of this erratum overstated it and was itself corrected): the lift is at the CONSTRUCTOR, demonstrated by `FreeOnQuiver@{uo uh up} (LinQuiver@{uo uh up} 3)` being accepted now and REJECTED before with `Cannot enforce Set = up`. It does NOT move the identifications in that file — their statements were untouched, so `ordinal_free` still reads `FreeOnQuiver@{Set Set Set}` before and after, and their outer binders were instantiable above `Set` beforehand anyway, so accepting `Set < o` on them demonstrates nothing. **The old note's closing caveat therefore STANDS**: these are identifications of SMALL categories at `Set`, and a consumer wanting them larger cannot get there by instantiation, only by restating them. What the lift buys is that NEWLY WRITTEN statements over these quivers may be stated at an arbitrary level — which is what Instance/Square.v needs. Of the old note's two corrected measurements, only one was an error (`Ordinal_2_strict_iso` is NOT pinned by `TwoHom : … → Set`, since `Set <= h` is not `h = Set`, and it accepted a larger instance before the lift too); its record of `linear_hom_iff` as `@{}` was CORRECT when written and became stale only because this commit changed that constant to `@{u}`. The ENGINE was never pinned (`graded_free_thin@{u u0 u1}` over an arbitrary quiver). Mac Lane's loop example is NOT built here — issue #802 owns it and its file is not yet in tree, credited as such. 72 named constants / 80 with obligations, all closed under the global context
- **Construction/Free/Quiver/Constructions.v**: Mac Lane §II.7 Ex 1 — opposite and product graphs, defined so the forgetful functor preserves them, and here it does ON THE NOSE: `QuiverOfCat (C^op) = QuiverOp (QuiverOfCat C)` and `QuiverOfCat (C ∏ D) = QuiverProd …` (with the `Forgetful`-phrased forms `Forgetful_preserves_op`/`_prod`) all hold at **Leibniz equality of the whole `Quiver` record, by `eq_refl`** — not field-by-field, not an isomorphism in `QuiverCategory`. Three measured facts allow it: `QuiverOfCat` carries no Program obligation, `Opposite`/`Product` are plain Definitions, and `Quiver` and `Setoid` both have primitive projections with eta. Arrow-level preservation for the opposite is `eq_refl` too (`Forgetful_preserves_op_fmap`), and `QuiverOp_invol`/`QuiverOp_map_invol` are `eq_refl` where the donor's own `op_invol` (Construction/Opposite.v) is Qed-opaque. A product UMP is proved and HONESTLY WEAKENED: both triangles `QuiverPair_Fst`/`_Snd` hold at Leibniz equality, `QuiverPair_eta` at `≈`, and `QuiverPair_unique`'s hypotheses are Leibniz rather than `≈` — the `≈` form needs respectfulness of `QuiverPair` in its arguments, which is NOT proved and is disclosed; no `Cartesian QuiverCategory` instance is claimed. The cases that do NOT reach `eq_refl` were measured with `Fail` rather than assumed: `Forgetful_preserves_fst`/`_snd` (`Fst`/`Snd` are `Program Instance`s, so `fmap_respects` is opaque) and `QuiverSwap_invol` (surjective pairing is not definitional for stdlib `prod`), both delivered at `≈`; likewise `Lib/Datatypes.v`'s `prod_setoid` is shown unusable as the edgeset donor, which is why `edgeset_prod` spells its `Equivalence` fields explicitly. Those measurements are GUARDED, not merely recorded: **Test/ProbeQuiverConstructions.v** (the Test/ProbeFunnyPoly.v convention) states each strengthening as a `Fail` — the two projections, `QuiverSwap_invol`, the `prod_setoid` substitution, and `QuiverPair_eta` — every one pinned to a genuine CONVERSION failure and paired with a positive control that must SUCCEED, so a rename or a record change breaks the file loudly instead of turning the `Fail`s vacuously green. Disclosed and NOT witnessed: no competitor failing the two triangles is exhibited, so `QuiverPair_unique`'s discriminating power rests on eta's non-definitionality alone. Distinct from Theory/OGraph.v's ×_O, which multiplies over a FIXED node set and defines no opposite — cross-referenced in-header. Quiver.v had Required `Opposite` and `Product` since forever without using either; this file finally does. 29/29 constants closed, zero `Qed` (transparency is what the `eq_refl` claims rest on)
- **Construction/Free/Quiver/Presented.v**: Categories presented by generators and relations — Mac Lane §II.8 chained onto §II.7, closing the free-and-quotient story Construction/Free.v's header narrates. The general engine is in Construction/Quotient.v: the Type-valued relation family `HomRelT` (over which `HomCongruence`/`Quotient`/`QuotientLift` are now stated, `HomRel` surviving as the Prop-valued abbreviation the PROP towers use and `Quotient_HomEqProp` staying pinned to it), the five-constructor generated congruence `CongClosure` with `cc_least`, and Mac Lane's Proposition 1 as `cc_kernel` — the kernel lemma is a one-line corollary of leastness at `FunctorKernel F`, and the closure must be Type-valued precisely because that kernel congruence is (the hom-setoid `≈` is a crelation, so a Prop closure cannot be eliminated into it). On top: `PresentedQuiver G R` as the quotient of `FreeOnQuiver G` by the congruence generated by path equations, with `presented_universal` — naming the images of the generators subject to R determines a functor out of the presented category uniquely — and `presented_restrict`/`presented_restrict_satisfies` for the converse leg, so the correspondence runs both ways though it is not packaged as a bijection; uniqueness is at STRICT (`hom_cast`/`Functor_StrictEq_Setoid`) strength, stronger than a Cat-level statement and implying it through `strict_equiv_implies_fun_equiv`. Parallelism of equations is structural (`PathRel` is indexed by a shared source and target) and the projection is full but deliberately not faithful — Fong and Spivak's "equations merge, never add". Finiteness is data (`FiniteQuiver`/`FiniteEquations` over `Fin.t` indices, with retractions on the quiver's nodes and edges, no choice principle), witnessed by the commutative-triangle presentation whose two halves are proved (`ord3_free_distinct`, `ord3_relation_holds`) — the isomorphism with `Instance/Ordinal.v`'s `Ordinal 3` is stated as a target, not proven; Riehl's two distinct diagonals are proved natively over the square quiver (with `presented_no_eqns_separates` showing the empty presentation collapses nothing), and presented monoids fall out as the one-object case through `hom_monoid`
- **Construction/Free/Quiver/Limit.v**: Mac Lane §III.4 p. 71 (`maclane:III.4:def8`, `maclane:III.4:remark6`) — cones and limits over a diagram of GRAPH shape, and the reduction to functor limits. A graph cone needs **NO functoriality of the diagram**, and the file shows exactly where that lives: `AGraphCone D c` carries `gvertex_map (x : nodes G) : c ~> D x` with **one triangle per EDGE** (`gcone_coherence e : dedge D e ∘ gvertex_map x ≈ gvertex_map y`), where `Structure/Cone.v`'s `ACone` states one per ARROW. **The issue's suggested donors were already in tree under other names and are REUSED rather than rebuilt**: `Theory/Diagram.v` already defines `Diagram G C := QuiverHomomorphism G (QuiverOfCat C)` — the issue's "graph morphism into the underlying graph of C" — and `FunctorOfDiagram D := InducedFunctor G D` is already the factorization through the free category, so nothing here re-derives from raw `QuiverHomomorphism`. (The issue's own citations are largely STALE — it names five sites in `Construction/Free/Quiver.v` and only one of its citations into that file lands, one of the five holding `UnitQuiverCatAdjunction`, not `UniversalArrowQuiverCat` — the same error recorded in the **`Instance/Rng/Polynomial.v`** bullet below, there being no `Construction/Free/Quiver.v` bullet in this index at all. The five real sites lie elsewhere in that file.) **THE CORRESPONDENCE RUNS BOTH WAYS AND THE LIMIT HALF IS PURE CONVERSION**: `ACone_of_AGraphCone` extends a graph cone along PATHS (the file's one induction, via `gcone_dpath`) and `AGraphCone_of_ACone` restricts to single edges; at limit level `graph_limitcone` and `limitcone_graph_limit` are `:=` terms with **NO tactic** — machine-checked by printing the bodies, which are bare applications — because apex and legs agree on the nose and `obj[FreeOnQuiver G]` IS `nodes G`, so the two `∃!` statements are convertible and every bit of proof content sits in the cone-level passages. `graph_limit_iff_limitcone` packages both. `graph_limit_IsALimit` is the apex-only reading, labelled the WEAKER one — no separation is proved, so "strictly" is not claimed. Strengths measured rather than guessed: legs and apexes cross **both ways at `eq_refl`**, while the three WHOLE-RECORD round trips are REFUTED at `eq_refl` — all four records have primitive projections with eta, so record equality is field equality and the coherence fields are rebuilt proofs (`≈` being `crelation`-valued makes them data); the cone-side round trip is delivered at `≈` through `AConeEquiv`. **A UNIVERSE QUESTION THE FILE DECLINED TO ANSWER IS ANSWERED HERE, AND ATTRIBUTED**: `AGraphCone` displays `C : Category@{u u0 u0}` where `ACone` displays hom and proof apart, and the identification is REAL rather than a display artifact — and it is **`Diagram`'s**, not this file's. The localization needs three controls, not two: under `Constraint uh < up`, `QuiverOfCat C`, `ACone c F` **and `QuiverHomomorphism G (QuiverOfCat C)`** are all formable while only `Diagram G C` is rejected (`Cannot enforce up = uh`) — an earlier revision shipped only the first two, which leave standing the obvious alternative that `Diagram` definitionally unfolds to. Sharper still, and it changes what the pin means: `Diagram` opens with an unannotated `Context {C : Category}` (`Theory/Diagram.v`), so this is a minimization artifact of the family `Construction/Free/Quiver/Examples.v` records, repairable upstream with explicit binders rather than inherent content. Non-vacuity is general in the category and the two arrows, though NOT in the diagram: `triangle_graph_limit u v : IsLimitGraphCone (TriangleGraphCone u v)` holds for an ARBITRARY `C` and arbitrary composable `u`, `v`, but at the diagram `TriangleDiagram u v (v ∘ u)` whose third edge is LITERALLY the composite — and that restriction is load-bearing, since for a general `w` the same legs are not even a cone (the X→Z coherence would demand `w ≈ v ∘ u`). The `.v` header says this; an earlier revision of this bullet dropped it, with `graph_limit_med … (tri_gcone_of_arrow f) = f` by `eq_refl` and a mediator that COMPUTES over `Coq`; the `loop_*` block exhibits the no-functoriality point at a shape with arbitrarily long paths (`loop_paths_distinct`), and the header discloses that it is a witness for the cone condition only — no limit, since that would be an equalizer nobody builds here. 73/73 constants closed under the global context (counted over the glob's `def`+`prf`+`proj`+`rec` entries plus the three `Build_*` constructors, which appear in none of the usual sources). `Test/ProbeQuiverLimit332.v` pins the boundary with 2 negatives of DIFFERENT KINDS — one CONVERSION (`dpath D p` is not `fmap[FunctorOfDiagram D] p` at a VARIABLE path, `dpath` being a `Fixpoint` against a `tlist'_rect`; they DO agree at one edge, which is the shipped `dpath_singleton_is_fmap`) and one FORMABILITY (the `Diagram` universe identification, whose two controls localize the cause) — plus an instrument check, each stripped and its kind confirmed, rename-simulated at 7/7. NOT delivered: no dual (cocones/colimits over a graph); no setoid on `AGraphCone` and hence no essential uniqueness; no preservation, reflection or creation; no link to `Theory/Diagram.v`'s `Commutative`; no `GraphLimit` from a completeness hypothesis; and the three refuted whole-record round trips are measured in the header but pinned NOWHERE
- **Construction/Free/Groupoid.v**: Mac Lane §II.7 Ex 3 — the free groupoid on a graph, and (Instance/Grp/Free.v) the free group with its adjunction. Built by REUSE, not by a new inductive: `FreeGroupoid G := QuotientCong (FreeOnQuiver (SignedQuiver G)) FreeGroupoidRel` — adjoin formally reversed edges, then quotient by exactly TWO generating cancellation equations, `CongClosure` (Construction/Quotient.v) supplying symmetry, transitivity, composition-congruence and `≈`-inclusion, and `cc_kernel` supplying `QuotientLift`'s hypothesis with no second induction. `FreeGroupoid_IsGroupoid : IsGroupoid (FreeGroupoid G)` is the PREDICATE (Structure/Groupoid.v), NOT Construction/Groupoid.v's core construction — the two are different things and the file says so. **First consumer of `Lib/TList.v`'s `tlist_rev`**, the edge-flip-parameterized reversal kit that had sat unused since it was written (measured: 21 lines of TList.v, no other file; the `Lib/NETList.v` hit is the sibling `netlist_rev`, a different function, disclosed so a later grep does not appear to contradict the claim). That reuse buys STRENGTH, not convenience: `tlist_rev_app_distr` IS the anti-homomorphism law and `tlist_rev_involutive` IS involutivity, both at **Leibniz `=`** — so `fginv_involutive`, `fginv_compose`, `fginv_gen` and `sflip_involutive` are `=`, while the inverse LAWS (`fginv f ∘ f`) are `≈` only, pinned by a `Fail` probe. The UMP `free_groupoid_universal` bundles `Hobj` INSIDE the sigma (Presented.v's shape, forced: the generator clause is not well-typed without object agreement in scope — also pinned by a `Fail`), is STRICT (`hom_cast`/`Functor_StrictEq_Setoid`), and its real content is that the generator clause constrains only POSITIVE generators, the negative ones being FORCED through `ginv_unique_l` (`unique_neg`) rather than assumed; both round trips are proved. The free group is built DIRECTLY as an Instance/Grp.v `GrpObject` from the one-node word quotient — deliberately not through `vertex_group`, which yields Construction/Deloop.v's different `GrpObject`, the two in-tree bridges from `Instance/Grp.v`'s record to a `MonObject` (`Instance/Rep.v`'s `grp_mon` and this file's own `grp_deloop_monoid`, not convertible with each other, a measurement `Instance/Grp/Galois.v`'s header records (out of tree, pinned nowhere)) both dropping the inverse and so NOT supplying `Deloop_IsGroupoid`'s hypothesis. `FreeGrp : Sets ⟶ Grp` (the free group on a SETOID, since `Grp_Forget` lands in `Sets`) with `free_group_adjunction : FreeGrp ⊣ Grp_Forget` via the universal-arrow route, unit and counit NAMED and both triangles proved. Asymmetry measured, not glossed: the UNIT computes (`free_group_unit X a = fg_insert X a` by `eq_refl`, as do `FreeGrp X = FreeGrpObject X` and the three data fields of the underlying monoid) but the COUNIT does NOT — it routes through the `Qed`-opaque `ump_universal_arrows`, so what holds is agreement with word evaluation up to `≈` (`free_group_counit_evaluates`), and the negative is pinned in Test/ProbeFreeGroupoid.v (5 `Fail` probes against 7 positive controls, instrument sanity-checked, each negative stripped and confirmed to be a genuine conversion or typing failure). Non-vacuity PROVED at every point that matters: `free_signed_not_groupoid` (the quotient is NECESSARY — the free category on the signed quiver is not a groupoid once an edge exists), `free_signed_no_cancellation`, `fg_proj_not_faithful`, `loop_generator_not_identity`, and the two-generator witness `free_group_two_generators_nonabelian`/`_distinct` over `Deloop S3_Grp` — not a one-generator degeneracy. DEFERRED and disclosed: `FreeGroupoid` is not exhibited as a functor out of `QuiverCategory` (only the UMP plus both round trips). That deferral's stated reason — "no category of groupoids in tree, so 'free ⊣ forgetful for groupoids' is not statable" — was true when written and is FALSE since #972, which builds `Grpd` (`Instance/Grpd.v`); the functor-out-of-`QuiverCategory` half is still open, and #972's own `Construction/Fractions.v` runs the same recipe one level up, on a CATEGORY rather than a quiver, with the constructor-by-constructor correspondence recorded there; no reduced-word normal form, hence no word problem and no Nielsen–Schreier, every negative going through a concrete groupoid or a path-length invariant; extend/restrict respectfulness unproved, so the correspondence is not a bijection of types or of setoid quotients. #442 reaches a free-group left adjoint by the adjoint-functor-theorem route with no normal form — a genuinely different construction, cross-referenced; Mac Lane presents both deliberately
- **Instance/Grpd.v**, **Construction/Groupoid/Core.v**, **Construction/Fractions.v** (+ **Construction/Fractions/Weak.v**, **Construction/Fractions/Adjunction.v**, **Test/ProbeFractionsAdjunction972.v**): Riehl §4.1 Example 4.1.15 (printed p. 137) — **the category of groupoids, the core adjunction, and the category of fractions**. `Grpd` is the FULL subcategory of `Cat` cut out by `Structure/Groupoid.v`'s `IsGroupoid`, built with `Build_Subcategory` in exactly `Instance/Variety.v`'s shape (`shom := fun _ _ _ _ _ => True`, closures `I`), so `Grpd_Incl` comes free from `Construction/Subcategory.v` and is full and faithful. `Core : Cat ⟶ Grpd` makes `Construction/Groupoid.v`'s object-level core a functor, and **`Incl_Core_Adjunction : Grpd_Incl ⊣ Core` is a real `Adjunction` record**, built with `Build_Adjunction'` — the hom-setoid bijection itself, with both naturality squares supplied — from which `core_factorization` is derived rather than the other way round. **`AdjunctionFromUniversalArrows` is the WRONG DIRECTION for this half and is not used**: `Theory/Universal/Arrow.v` builds a LEFT adjoint from universal arrows, and `Incl ⊣ Core` needs the right adjoint; there is no `RightAdjointFunctorFromCouniversalArrows` in tree, which is why the hom-setoid route was taken. The direction is pinned both ways (`Core ⊣ Grpd_Incl` is refused). `Construction/Fractions.v` builds the zig-zag localization by generators and relations — the free category on the quiver of `C` glued to that of `C^op`, quotiented by `CongClosure` over the composition relations and the two cancellation relations — **parameterised over a class `W`**, with `Fractions C := FracZigZag (WAll C)` the all-morphisms case, so #1028's `C[W⁻¹]` can instantiate it rather than rebuild it. It is NOT `Construction/Localization.v`, which is the reflective orthogonal-subcategory localization, and the header says so. **The adjoint triple IS assembled — `riehl_4_1_15 : AdjointTriple FractionsF Grpd_Incl Core`, over one shared inclusion, with both reversed directions refused — but only after a strength gap that is worth recording because it was measured rather than estimated.** The pieces fit on the object side — `(Fractions C; Fractions_IsGroupoid C)` types as an `obj[Grpd]` and `FractionsProj C` as `C ~{Cat}~> fobj[Grpd_Incl] (…)`, both checked — but `Fractions_UMP_groupoid` supplies its `∃!` at `Functor_StrictEq_Setoid` while `universal_arrow_from_UMP` wants it at the ambient's, and `Grpd` inherits `Cat`'s `Functor_Setoid`; the refusal is quoted in the header. EXISTENCE transfers along `Instance/StrictCat/ToCat.v`'s `strict_equiv_implies_fun_equiv`; **UNIQUENESS does not**, because a functor factoring only up to natural isomorphism need not factor up to object equality. Two routes close it — restate the UMP at `Functor_Setoid` strength, or build `Grpd` over `StrictCat` (the audit did the latter in ~35 lines and got a real `Adjunction`, so the LEFT adjunction is available over the strict ambient today) — and route (b) is cheap for the fractions half and expensive for the core half, whose adjunction is a natural-isomorphism statement. Route (a) was taken. `Construction/Fractions/Weak.v` reproves the uniqueness clause against natural isomorphism — the induction of `ZigZagLift_unique` with `hom_cast` replaced by conjugation with the isomorphism's components, and `iso_conj_inverse` standing in for `hom_cast_inverse` — while EXISTENCE transferred for free. **The restated property is genuinely weaker, and that is verified rather than asserted**: the audit instantiated it at `C := _1`, `D := Indiscrete bool` (two distinct but ISOMORPHIC objects) with `F` constantly `true` and `L` constantly `false`, where the `Functor_Setoid` hypothesis is satisfiable and the `Functor_StrictEq_Setoid` one is PROVABLY FALSE (`Hobj ttt : true = false`, `discriminate`). `Test/ProbeFractionsAdjunction972.v`'s N10 pins the same boundary by refusing the strict statement where only the weak one is proved. The uniqueness half needs NO groupoid hypothesis on the target — the invertibility it uses is of the generators' images, which the localization already forces — so only existence consumes `IsGroupoid D`. `Construction/Fractions.v`'s account of the gap is KEPT, as the record of what closing it cost: its strict statements are the sharper reading and are not weakened. **`Construction/Free/Groupoid.v` already ran this recipe one level down, on a QUIVER**, and neither the issue nor the donor map said so: its `sedge`/`FreeSigned`/`fgr_left`/`fgr_right` are one-for-one this file's `ZEdge`/`FreeZigZag`/`zr_inv_l`/`zr_inv_r`. The correspondence is PROVED, not asserted — `FreeGroupoidToFractions` is the comparison functor, produced by the free groupoid's own universal property with no new construction, identity on objects at `eq_refl`; what is NOT proved is that it exhibits `Fractions C` as a quotient. Rebuilding on it was declined because `sedge` is a bare sum with no room for a class `W`, which would have dropped the parameterisation. `core_is_groupoid` was NOT reproved — `Structure/Groupoid.v` already had it, a correction to the issue's own work item. Measured: **111 constants gated and all "Closed under the global context"** (7885 → 7996) — 13 heads for `Grpd.v` (no `Program` at all), 17 + 22 for `Core.v` (16 obligations plus 6 `abstract` subproofs), 58 + 1 for `Fractions.v`. `make todo` UNCHANGED at 2360: all three files add zero hits. Traps: two name collisions caught by a `.glob` sweep and renamed — `ZigZag` against `Structure/Groupoid/Connected.v` and `IsIso_id` against `Theory/Equivalence/Adjoint/Compose.v` — and the `strings`-based obligation census returns serialization artifacts with a junk prefix (`E6core_lift_obligation_2`), one of which broke the gate until the list was filtered against the declared heads. `Construction/Groupoid.v`'s retraction ("no standalone category of groupoids exists in-tree, so the adjunction remark … remains prose rather than a theorem") is replaced in place, as a correction. Not delivered: no `Adjunction` record for `Fractions ⊣ Incl` (above); no functoriality of `Fractions` in `C` and no naturality of the bijection; no `Test/Probe*` file, so every strength here is measured and none is guarded; and nothing exhibits a `C` whose projection is NOT faithful, so the degenerate reading in which `C[C⁻¹]` merely reproduces `C` is unrefuted for any single `C`.
- **Construction/PROP/**: Free PROP on a signature, complete spine — shared Monoidal/Braided/Symmetric/Strict instances, bundled FreePROP, interpretation with universal property (Interp.v, Universal.v), models (Algebra.v), presented PROPs by generators and equations with factorization (Presentation.v, Presentation/Universal.v), Tietze/definitional extensions (Tietze.v); generic hom-congruence quotients in Construction/Quotient.v; skeletal FinSet with computable coproducts in Instance/FinSet.v
- **Construction/ColouredPROP/**: Coloured PROPs, complete — coloured signatures and terms over list-of-colour boundaries, free coloured PROP instantiating the class with definitional coherence, interpretation + universal property, presentations, colour base change with identity/composition functoriality laws up to hom_cast (pseudofunctor coherence cells not formalized), per-colour selective supplies (Fong–Spivak) with linear fan-out discipline, and the LNL bridge; generic comonoid tensor in Theory/Algebra/Comonoid/Tensor.v
- **Construction/Funny.v**: Funny tensor product C □ D — same objects as C × D, morphisms freely generated by one-sided steps with no interchange law; satellites in Construction/Funny/ (bifunctor, unitors, swap, associator, comparison to ×, unnatural-transformation hom ⟦-,-⟧, closed structure), symmetric monoidal instance on StrictCat in Instance/StrictCat/Funny.v
- **Construction/FAlg.v** / **Construction/FCoalg.v**: The categories of F-(co)algebras — `FAlg F` bundles carriers `∃ a, FAlgebra F a` with the `FAlgHom` commuting-square hom class; `FCoalg F := (FAlg (F^op))^op` by duality with covariant accessors and `FCoalg_Forget`. The initial-algebra / final-coalgebra theory rides these: the ω-chain `Chain F : Omega ⟶ C` (Construction/Chain.v) over the ordinal ω (Instance/Omega.v, a Type-valued `le_t` order avoiding stdlib `le`, with `Cochain` by duality); Lambek's lemma both ways (`lambek : F μ ≅ μ`, `lambek_final : ν ≅ F ν`, Theory/Lambek.v); catamorphism/anamorphism universal properties (`cata`/`ana` with commutes/unique/fusion, Theory/Recursion.v); Adámek's theorem over the honest `AdamekData` hypothesis (`adamek : AdamekData → Initial (FAlg F)`, Theory/Adamek.v — the `PreservesColimit → AdamekData` leg-agreement bridge is withheld, ledger 17); the `Cocomplete` corollary supplies the ambient colimit but STILL requires the `AdamekData` leg-agreement as a hypothesis, and no concrete `AdamekData` is constructed in-tree — the initial algebras below (list/nat/streams) are proved directly (the `NatF` note, Theory/Adamek/Corollaries.v); concrete witnesses `list A` as the initial `ListF`-algebra on Coq (Instance/Coq/Lists.v, axiom-free), `nat` as the initial `NatF`-algebra (Instance/Coq/Nat.v: `nat_initial` with primitive recursion as the mediator, `nat_lambek : 1 + nat ≅ nat`, the algebra-iso characterisation `nat_initial_unique`, the Riehl triples bridge `alg_of_triple`/`alg_of_triple_eta`, and the §2.4 representability reading — `Endos := FAlg Id[Coq]` with `nat_succ_represents` exhibiting `(nat, S)` as representing the forgetful functor with universal element `0`, and `nat_initial_via_universal_element` re-deriving initiality through the universal-element criterion), and streams as the final `StreamF`-coalgebra on Sets via bisimilarity (Instance/Sets/Streams.v)
- **Construction/Elements.v**: The category of elements of `K : D ⟶ Sets` — objects the pairs `(d, x)` with `x` an element of the setoid `K d`, morphisms the `D`-morphisms carrying the element (`fmap[K] f x ≈ y`), with the hom-setoid comparing only the underlying `D`-morphism, so faithfulness of `Elements_proj` is definitional (`Elements_proj_faithful_definitional` is the identity implication); the chosen lift `Elements_lift` with `Elements_lift_over` by reflexivity; the comma presentation `ElementsComma := =(1) ↓ K` related by the proved comparison `Elements_Comma : Elements K ≅[Cat] ElementsComma` (an isomorphism in Cat, hence an equivalence — one round trip is the identity on objects and morphisms, the other agrees pointwise up to `≈`), with projection compatibility as the identity-component natural isomorphism `Elements_proj_comma_iso`; the presheaf orientation `PElements P := (Elements P)^op` for `P : C^op ⟶ Sets`, whose hom shape `fmap[P] f x' ≈ x` holds by `reflexivity` and whose projection `PElements_proj` stays covariant into `C`. The header records why the Grothendieck/`IndexedCat` route is not used (`DiscreteCat`'s strict-equality homs versus setoid fibres).
- **Construction/Karoubi.v**: The Karoubi envelope — objects are idempotents, the identity of `(x, e)` is `e` itself; fully faithful embedding, every envelope idempotent splits (`SplitIdempotent` witnesses). Universal property in Karoubi/Universal.v: `IdempotentsSplit` (chosen splittings, satisfied by every envelope), `Karoubi_Extend` along the embedding with `Extend ∘ Embed ≈ G` and uniqueness up to `≈`, `CauchyComplete := IdempotentsSplit`, and split idempotents ⇒ the embedding is an equivalence (forward direction); Sets is Cauchy complete (Instance/Sets/Karoubi.v via the fixed-point sub-setoid)
- **Construction/Grothendieck.v**: Displayed categories, fibrations, and the Grothendieck construction — `Displayed C` (Theory/Displayed.v) as the primitive with `dtransport` and the interchange fields `dtransport_comp_l`/`dtransport_comp_r` (underivable from the base laws by a 2-cocycle countermodel in the header, and load-bearing — without them the total category's composition is not a congruence); the total category `Total`/`Total_Proj` (Construction/Displayed/Total.v); fibrations in both presentations (Theory/Fibration.v: `DCartesian`, `Cleaving`, `CartesianMorphism`, `ClovenFibration`, `SplitCleaving`, opfibrations by `Displayed_op`); the coherent pseudofunctor-lite `IndexedCat` (Construction/Indexed.v, with an honesty note on why a bare `Functor B Cat` is insufficient in a setoid library — a central-automorphism twist of `fmap_comp` breaks the cross-application cocycle); the Grothendieck construction `Grothendieck_Displayed`/`Grothendieck`/`Grothendieck_Proj` with every displayed law discharged from the `IndexedCat` coherence pack; the projection is a split opfibration (Construction/Grothendieck/Fibration.v); fibre categories and the fibre equivalence `fiber_grothendieck_equiv` (Construction/Grothendieck/Fiber.v); constructors from strict functors under fibrewise UIP with an inline axiom-free Hedberg (Construction/Grothendieck/Strict.v: `IndexedCat_of_StrictFunctor`, `Constant_IndexedCat`, `Grothendieck_Constant_iso : Grothendieck (Constant_IndexedCat B D) ≅[Cat] B ∏ D` — note that `≅[Cat]` is an EQUIVALENCE of categories, not an isomorphism, since Cat's hom-setoid is `Functor_Setoid` (Instance/Cat.v), so the name reads stronger than the statement); the fibred-to-indexed round-trip `IndexedCat_of_SplitCleaving` with `RoundTrip_Comparison` fully faithful and the full `RoundTrip_Equivalence` (Construction/Grothendieck/RoundTrip.v, over a split opfibration of P — the split laws are provably inert, so a cloven cleaving already suffices); and the codomain displayed category with cartesian-lifts-iff-pullbacks `codomain_cleaving`/`codomain_cleaving_pullbacks` (Construction/Displayed/Codomain.v)
- **Construction/Enriched/Natural.v**: The enriched upgrade over Construction/Enriched.v — V-natural transformations `EnrichedTransform` with the unitor-conjugated V-naturality square `enaturality` (typed-equality `<< A ~~> B >>` form) and componentwise setoid; identity/composite enriched functors and both whiskerings (Construction/Enriched/Compose.v); the ordinary category of V-functors `Enriched_Fun` with vertical composition by `ecompose` under the inverse unitor at I (Construction/Enriched/Fun.v — genuine V-category hom-OBJECTS need ends in the base, ledger 11); V=Sets recovers ordinary category theory at all three levels, the transformation level being `EnrichedTransform_is_Transform` over the two in-tree `Defined` round trips (Construction/Enriched/Sets.v); enrichment in the walking arrow = preorders both ways with enriched functors = monotone maps (`Enriched_Two_preorder`, `EnrichedFunctor_Two_monotone`, Construction/Enriched/Two.v, over Instance/Two/Monoidal.v's cartesian `Two_Monoidal`). Sets-weighted (co)limits by representability with `HomDiagram`/`WeightedLimit`/`WeightedColimit` and the conical case `conical_weighted` proved in both directions (Structure/Limit/Weighted.v; V-valued weights need ends, ledger 11)

## Concrete Instances
- **Instance/Ab.v**: `Ab`, the category of abelian groups (Mac Lane §I.7) — `AbObject` extends `Instance/CMon.v`'s `CMonObject` by a negation (`ab_cmon :> CMonObject`), so carrier and the four monoid laws are inherited; `AbHom` IS `CMonHom`, making preservation of negation the theorem `ab_map_neg` rather than a field. `Ab_Forget : Ab ⟶ Sets`; `Ab_Zero` from the one-element group. Mac Lane's §I.7 proposition in both halves: `ab_monic_iff_injective` probing with `AbKernel`, and `ab_epic_iff_surjective` probing with the quotient `AbQuotient` = B/fA — the epic half now concluding the PROPOSITIONAL surjectivity `AbPropSurjective`, an earlier revision of this bullet having stated it against the Σ-valued `AbSurjective`: measured on 2026-09-17 after the PR "algebraic carriers are sets", `ab_coset_eq` is `Prop`-valued (an `ex` over the carrier's `pequiv`, `Instance/Ab.v`), so `ab_epic_surjective` and the biconditional conclude the truncated notion, while `AbSurjective`, `ab_surjective_epic` and the Σ→`Epic` half are byte-identical to their pre-PR statements — in a category of sets epi implies surjective, and surjective implies SPLIT only with choice. The quotient probe is available here for ONE reason — commutativity: `ab_coset_eq` is a congruence only because `f a` can be commuted past another element (`cmon_plus_comm` in `AbQuotient`'s `cmon_plus_respects`), which is what makes B/fA an OBJECT of `Ab` rather than a bare setoid. No comparative claim is made against `Sets`: `Instance/Sets.v` now proves `surjectivity_is_epic` outright via a cokernel-pair setoid — the same kind of quotient probe — so both categories have the result and both get it the same way. NOTE the monic half is NOT special to `Ab` — monic ⟹ injective already holds in `CMon`, probing with `nat`; the kernel is a convenient probe, not a necessary one. SCOPE: `Preadditive Ab` and a forgetful `Ab ⟶ Grp` are NOT delivered, and this file instantiates none of `Preadditive`/`Additive`/`Abelian`. Erratum in #256: `Instance/Comp.v` DOES have group inverses (`inv`, `inv_left`/`inv_right`, `Group`, and a `Bool` witness); it is unusable as a donor only because it is a Leibniz-`=` development invoking `functional_extensionality`.
- **Instance/CMon.v**, **Instance/Ab.v**, **Instance/Mod.v**, **Instance/Grp.v**, **Theory/Algebra/Rig.v**, **Instance/Rng.v**, **Instance/Rg.v** (all extended), **Test/ProbeTermModelProp.v**, **Test/ProbeTermModelPropRng.v**, **Test/ProbeAbPropSurjective.v**, **Test/ProbeGrpProp.v**: **the object records of the concrete algebraic categories now carry the property that their carrier is a SET** — the PR "algebraic carriers are sets" (2026-09-17), phases P2 and P3, the field regime the `Adjunction/GAFT/Resize.v` and `Instance/Mod/TensorAFT.v` bullets consume. Three new record fields: `cmon_prop : PropEquiv (is_setoid cmon_setoid)` on `CMonObject` (`Instance/CMon.v`), reaching `AbObject`, `RModObject`, `RgObject` and the bimodule, Lie, representation and finite-dimensional-vector-space objects through their coercions; `rig_prop` on `RigObject` (`Theory/Algebra/Rig.v`), reaching `RingObject`, `Rng`, `CRng`, `FieldObject` and the algebras; and `grp_prop` on `GrpObject` (`Instance/Grp.v`) — a fourth such field, `soa_prop` on `Instance/Variety/Free.v`'s `SetoidOpAlgebra`, came with #450 (its bullet under Concrete Instances). The property PROPAGATES rather than being asserted twice: hom-setoids inherit it pointwise into the codomain (`CMonHom_PropEquiv`, `RModHom_PropEquiv`, `GrpHom_PropEquiv`), so `CMon`, `Ab`, `RMod R` and `Grp` are `LocallyPropositional`; limit vertices inherit it from their LEGS (`alim_prop`, `glim_prop`, `rlim_prop` in `Instance/Ab/Limit.v`, `Instance/Grp/Limit.v`, `Instance/Rng/Limit.v`) with **NO hypothesis on the limit**, which is not a convenience but forced — `CreatesLimits` quantifies over EVERY limiting cone and leaves nowhere to put one; kernels, centres, commutator and subgroup objects through `sigma_first_PropEquiv` and products and function setoids through `dep_fun_PropEquiv` (`Lib/Setoid/Propositional.v`). **The term models moved sort, and each is eliminated exactly ONCE**: twelve inductive congruences are now `Prop` inductives — `mt_eq`, `fv_eq`, `bs_eq`, `ts_eq`, `fa_eq`, `mr_eq`, `fr_eq`, `pt_eq`, `tt_eq`, `et_eq`, `uenv_eq` and `am_eq` (criterion: `grep -rn --include='*.v' -E 'Inductive <rel>' .`, each reading `→ Prop`) — storing their `Type`-valued `≈` premises verbatim, eliminated only in their own `*_respects` lemma into the target object's `pequiv` and converted back by `pequiv_to`, so every universal property keeps its full domain. **Quotients are propositional truncations of their congruences while MEMBERSHIP stays data**: `ab_coset_eq`, `groth_rel`, `mquot_rel`, `rquot_rel`, `lquot_rel`, `abel_eq`, `quot_equiv` and `fg_equiv` are `Prop`-valued (criterion: each `Definition`'s ascribed sort, `: Prop`), and `Instance/Grp/Congruence.v`'s `ns_prel` is the ninth in substance though not in sort — a `HomRelT`, which `Construction/Quotient.v` declares `Type`-valued, every one of whose values IS the `Prop` `quot_equiv` — while the MEMBERSHIP predicates `smod_mem`, `idl_mem`, `lidl_mem`, `sub_mem`, `InCommutator`, `InNormalClosure` and `torsion_mem` stay `Type`-valued with their leastness theorems' motives intact. **THE STRENGTH CHANGES, ENUMERATED RATHER THAN SUMMARISED, because an earlier revision of the P2 report recorded three of the eighteen and the P3 report recorded twenty-one in kind without naming them.** (i) Propositional surjectivity: `ab_epic_surjective`, `rmod_epic_surjective` and the biconditionals `ab_epic_iff_surjective`/`rmod_epic_iff` conclude `AbPropSurjective`/`RModPropSurjective`; the split Σ notions and the Σ→`Epic` halves are unchanged, since in a category of sets epi implies surjective while surjective implies SPLIT only with choice. (ii) Kernel characterisations up to `inhabited`, with the untruncated direction kept separately in each case: `mquot_rel_zero_iff`, `mquot_proj_kernel`, `EvenSub_is_kernel_of_proj`, `Z_proj_kernel_is_EvenSub`, `mod_psi_kernel_is_meet`, `rquot_rel_zero_iff`, `rquot_proj_kernel`, `EvenIdeal_is_kernel_of_proj`, `digit_equiv_is_divides`, `quot_proj_kernel`, `psi_kernel_is_meet`, `S3_proj_kernel_is_A3`, and the memberships of `NmodM` and `MeetNS`. (iii) `quot_eq_elim` (`Instance/Ab/TorsionFree.v`) went from a Σ to a `Prop` continuation `∀ P : Prop, x ≈ y → (∀ t, torsion_mem A t → x ≈ y + t → P) → P`, a real loss of data extraction whose three consumers are all in its own file and none of which changed statement. (iv) EIGHTEEN statements about an ARBITRARY Ab-enriched or preadditive category take `LocallyPropositional`, the price of reading an ambient hom-setoid as a concrete algebraic object: ten in `Adjunction/Additive.v` (`hom_ab`, `hom_ab_carrier`, `hom_ab_plus`, `hom_ab_zero`, `hom_ab_neg`, and on BOTH categories `adj_to_ab`, `adj_from_ab`, `adj_hom_ab_iso`, `adj_hom_ab_iso_to`, `adj_hom_ab_iso_from`), eight in `Construction/Enriched/Ab.v` (`ehom_ab`, `precomp_hom`, `postcomp_hom`, `zsmul_precomp`, `zsmul_postcomp`, `ab_eid`, `ab_ecompose`, `Enriched_of_AbEnriched`), with `EndRig` and the packaged `rig_iff_one_object_preadditive` and `Comm_algebra_CMon` carrying the same shape — and the nine bimodule and coextension objects that were the predicted chokepoint gained NOTHING, the four instances discharging them. (v) In the `Grp` layer the hypothesis is `PropEquiv` on a bare setoid rather than the class: `SymGrp` (14 call sites in `Instance/Grp/Epi.v` discharge it by `grp_coset_power_prop`), `UnitsOf`/`UnitsOf_map`, `GroupObject_GrpObject`, `elem_GrpObject`/`iprod_GrpObject`/`HomGrpA`/`sets_hom_iprod_iso`, `pointwise_GrpObject`/`pointwise_GrpHom`/`PointwiseGrpFunctor`, and `HomGrpObject`/`HomGrpHom`/`HomGrpFunctor` — these last with a **CODOMAIN-ONLY** `∀ x, PropEquiv (@homset C x e)` and not the class, DELIBERATELY: the one in-tree instantiation is at `C := Sets`, which is not locally propositional and cannot be, so the class form would leave `HomGrpA` unbuildable. Their twenty-one downstream propagations were measured and are not separately listed here: twelve constants of `Instance/Grp/Epi.v` now read `SymGrp (GrpCosetPower f) (grp_coset_power_prop f)`, nine of `Instance/Rep.v` and `Instance/Matr/GL.v` thread `UnitsOf`'s new argument, and four more (`HomGrpFunctor_Forget_agrees`, `sets_hom_to_iprod`, `sets_iprod_to_hom`, `pointwise_GrpObject_carrier`) follow. (vi) The free group's carrier equality is the propositional truncation of the free groupoid's hom-equality (`fg_equiv`, `Instance/Grp/Free.v`) — and **no theorem of that file changed statement**: `FreeGrpObject`, `FreeGrp : Sets ⟶ Grp`, `free_group_universal_arrow`, `free_group_adjunction` and `Instance/Grp/FreeAFT.v`'s five constants are unchanged and unconditional, nothing being asked of the generators, because `free_grp_extend`'s respectfulness is proved at the TARGET group through `pequiv_to`. **Universes, verified against a fully built master (`27f68f10`) rather than asserted**: the field's sort contributes `Set+1` to each record, so `CMon`, `Ab`, `Rig`, `Ring`, `Rg` and `Grp` are pinned BY HAND to `Category@{u p p}` (an unpinned record would gain a third, redundant universe and refuse every two-universe annotation in the tree for arity), and each record's universe ROLES permuted from `(carrier, proof, aux)` to `(aux, carrier, proof)`, the seven annotation sites with distinct levels being permuted with them. **No expressible instance is lost**: the only new constraint is `Set < u`, already implied by the baseline's `p < u`; `CMon@{u h}` under `Constraint h < u` alone compiles in BOTH trees, `CMon@{Set Set}` was refused in both, and `CMonObject@{Set Set Set}`/`RigObject@{Set Set Set}`/`RingObject@{Set Set Set}`/`GrpObject@{Set Set Set}` still `Check`, which matters because the tree carries 21 such all-`Set` annotations (criterion: `grep -rn --include='*.v' -E '(CMonObject|AbObject|RigObject|RingObject|RgObject|GrpObject)@\{Set Set Set\}' . | wc -l` on 2026-09-17; over the five records of the CMon and rig towers alone, the figure the P2 phase measured, it is 17). Arity DID move in thirteen places, recorded here because the P2 report named one: `CRng` 4 → 5 universes (an earlier revision of that report said it was unchanged; measured, it is not — it has zero annotation sites tree-wide), `Ab_Forget`/`CMon_Forget`/`Grp_Forget` 2 → 3, `Ab_Complete` 3 → 4, `RMod_Complete` 6 → 8, `Abelianization_Functor` 2 → 3, and `TensorMod`, `FreeModObject`, `AbQuotient`, `RModQuotient`, `QuotientMod`, `GrothendieckObject` and `FreeAbObject` each by one or more — every one with its STATEMENT and its `Print Assumptions` output unchanged. **Measured on 2026-09-17**, criterion `grep -rn --include='*.v' '<field>[[:space:]]*:=' . | wc -l` over the whole tree, probes included: 34 `cmon_setoid :=` sites against 31 explicit `cmon_prop :=`, 24 `rig_setoid :=` against 22 `rig_prop :=`, 26 `grp_setoid :=` against 23 `grp_prop :=` — each gap being a construction that supplies the field as a trailing `unshelve refine` obligation or by instance resolution rather than as a literal, and `Instance/Grp/Quotient.v`'s `SubgroupGrp` and `Instance/Matr/GL.v`'s `UnitsOf` doing so ON PURPOSE, since naming their carrier setoid puts one delta step in front of every term-mode `existT` built against it and the Coq 8.19/8.20 unifiers then infer a constant predicate and refuse the term. **Probes**: `Test/ProbeTermModelProp.v` and `Test/ProbeTermModelPropRng.v` pin the eleven ring-and-module `Prop` moves ("Cannot find the elimination combinator `<rel>_rect`, the elimination of the inductive definition `<rel>` on sort Type is probably not allowed") with `pequiv` readbacks as controls; `Test/ProbeAbPropSurjective.v` pins that the propositional existential cannot be destructed into the split notion ("Incorrect elimination in the inductive type \"ex\""); `Test/ProbeGrpProp.v` pins `am_eq`'s move, both truncations and the free group's TOTALITY with fifteen applied controls — every negative in all four stripped alone in a copy of the WHOLE file. Six negatives of `Instance/Mod/Closed.v` and `Instance/Mod/BaseChange.v` turned into positive controls and are recorded as corrections with their cause: `mt_eq`'s induction into a `Type` goal was what had identified the ring's carrier with an auxiliary level. **NOT delivered, and the non-deliveries are the point of several decisions**: nothing was removed, the glob census over the edited files finding 0 constants lost; `AbelianizationOb` and `LimitAb` take NO hypothesis, `abel_eq` being truncated instead — a section variable of family type pins `Grp`'s object universe and `S3` is then refused, and explicit threading would have made `Abelianization_Functor : Grp ⟶ Ab` conditional, i.e. weakened a theorem; `grp_coset_rel` is deliberately NOT truncated, so `transposition_decides_image` keeps full `GrpImageDecidable` (byte-identical type in both trees) — and nothing GUARDS that, a later truncation would weaken it silently; `Sets` gains no `PropEquiv` and `Cat` cannot have one; and `Instance/Grp/Abelianize.v` still hand-builds `grp_two_peq`/`grp_two_PropEquiv` for `AbTwo` although `GrpTwo` now carries `grp_prop`, which is redundant and is surfaced as a REMOVAL CANDIDATE rather than deleted
- **Instance/Ab/DirectedColimit.v**: Mac Lane §III.3 Exercise 7 (book p. 68) — an abelian group IS the colimit of its finitely generated subgroups, delivered at CONE level: **`ab_fg_colimit A : IsColimitCocone (FGCocone A)`** (`Structure/Limit/Preservation.v`), so every competing cocone factors uniquely through THESE legs; the apex-only `ab_fg_isacolimit : IsAColimit (FGDiagram A) A` is derived through `colimitcocone_isacolimit` and is the WEAKER reading (its legs are unconstrained) — no separation between the two is proved, so "strictly" is not claimed, with `ab_fg_Colimit` the packaged form. **`AbSubgroup` is new here, and the survey behind that was CORRECTED by audit**: `Instance/Grp/Quotient.v`'s `Subgroup` is over `GrpObject` and `Instance/Mod/Quotient.v`'s `Submodule` over `RModObject`, so neither transfers to `AbObject` — but a THIRD record does exist over the right base type and an earlier draft missed it: `Instance/Ab/Character/Finite.v`'s `Record Subgroup (G : AbObject)` has exactly `AbSubgroup`'s five fields PLUS `sg_dec`, demanding DECIDABLE membership, and carries its own `Generated`. The real ground for not reusing it is `sg_dec` — membership here is the inductive `InGen`, which nothing constructively decides — not the false ground first given (that no in-tree record is over `AbObject`). The miss was avoidable: the donor header this file cites names that record twice, four lines above the cited line. `Instance/Mod/Quotient.v` asks for exactly this interface IN `Instance/Ab.v`; it is declared here instead and that placement is disclosed, not silently taken. **`Proset` CANNOT HOST *THIS* INDEX — AND AN EARLIER DRAFT OVERSTATED THAT INTO AN IMPOSSIBILITY, WHICH A COMPILING COUNTER-DEVELOPMENT REFUTES**: `Proset` takes a stdlib `relation A` = `A → A → Prop` while `FGHom` is `Type`-valued *because `absub_mem` is*, so `Fail Check (FGHom ab_Z : relation (FGObj ab_Z))` is a genuine universe inconsistency against a PASSING control at the `inhabited`-squashed variant — that measurement is real and pinned. What it does NOT show is that #330's suggested `Proset` route is impossible: the argument covered only SQUASHING a Type-valued hom, never making MEMBERSHIP itself Prop-valued, and under that variant `FGHom` is natively a `relation`, nothing is squashed, `fmap` merely applies a `Prop → Prop` function into a `sigT`, and the colimit goes through (audit-built, closed under the global context; the transport and the `Proset` application independently re-checked). So the honest claim is *impossible GIVEN this file's Type-valued membership convention* — a design choice following `Instance/Grp/Quotient.v`, not a necessity. The index is built DIRECTLY, COPYING (not reusing — the file never `Require`s it) `Instance/Proset.v`'s trivially-true hom-setoid, with `FGSub_Thin` inhabiting `Structure/Thin.v`'s `Thin` rather than a lookalike, though that lemma is consumed nowhere. Directedness — the exercise's word — is `FGSub_directed : ∀ X Y, {Z & hom X Z * hom Y Z}`, the join presented by `++` on generator lists. The engine is `gen_least` (the file's ONLY induction over `InGen`), and `InGen` follows `Instance/Grp/Abelianization.v`'s `InCommutator` shape, its `ig_resp` constructor being what makes the generated set ≈-saturated. **SCOPE, STATED RATHER THAN GLOSSED**: index objects carry a `FinGen` PRESENTATION, so two presentations of ONE subgroup are DISTINCT objects — a deviation from the literal "poset of subgroups", harmless because the category is thin (`Ztwo_dup_iso` exhibits such a pair as isomorphic) and unavoidable because truncating the presentation would block extracting the generators that BOTH directedness and the mediator consume; the two presentations are NOT proved unequal, and no comparison with a poset-indexed diagram is formalized. `fg_med_commutes` is `≈` and NOT Leibniz, the cause being the design rather than an accident — the mediator evaluates the competing cocone at the cyclic object `cyc a` while the triangle's other side evaluates at `X`, different objects of the index — with seven other identifications holding at `eq_refl` and shipped as `Example`s. Non-vacuity over `ab_Z` (REUSED from `Instance/Ab/Coproduct.v`, no new group): `Ztwo_proper`/`Zthree_proper` (1 ∉ 2ℤ, 1 ∉ 3ℤ, so the subgroups are PROPER), **`Zsub_incomparable`** — no morphism EITHER WAY between 2ℤ and 3ℤ, so the index is genuinely not a chain — and `Zjoin_strictly_larger`, the join containing 1 = 3 + (−2) that neither factor does. 108/108 constants closed under the global context — the audit reconciled three counts: 101 is what `Print Module` prints, 106 adds the five `InGen` constructors the glob records separately, and 108 further adds `Build_AbSubgroup`/`Build_FinGen`, which none of those sources lists; so the earlier 106 was an UNDERCOUNT, not an overcount. Of these, EIGHT are in the permanent `make print-assumptions` gate; the rest were verified once. `Test/ProbeDirectedColimit330.v` pins the boundary with 2 negatives of DIFFERENT KINDS kept lexically apart — one CONVERSION (the triangle at Leibniz) and one FORMABILITY (the `Proset` universe inconsistency) — plus a separate instrument check, each stripped and its kind confirmed, rename-simulated at 7/7 over the constants in the NEGATIVES. NOT delivered: no `Filtered` class and no general filtered/directed-colimit theory (there is NONE in tree — `FGSub_directed` is a lemma about one concrete index, not an instance of a class); no `Cocomplete Ab` and no other colimit in `Ab`; no analogue for non-abelian groups or modules; and `AbSubgroup` is not placed in `Instance/Ab.v`, so `Instance/Mod/Quotient.v`'s request is acknowledged rather than discharged
- **Instance/Ab/TorsionFree.v**: Mac Lane §IV.3 Exercise 2 (book p. 92, `maclane:IV.3:ex2`) — **the torsion-free abelian groups form a full reflective subcategory of `Ab`**, delivered as the RECORD `TorsionFree_Reflective : Reflective TorsionFree_Sub` (`Construction/Reflective.v`'s three fields: `TorsionFree_Full`, `TorsionFree_reflector`, `TorsionFree_adj`), which at `9a1fe0f2` is the tree's FIRST `Reflective` built at a named concrete category — measured: `rg -n 'Build_Reflective' --glob '*.v'` returns exactly one prior hit, `Construction/Reflective/Idempotent.v`, over an abstract category with an idempotent monad, every other `Reflective`-typed term being a hypothesis. **THE ISSUE'S "Current state" IS STALE**: it says there is no `Ab` and that "torsion" has zero hits; `Ab` is `Instance/Ab.v` (#256) and the seven case-insensitive hits at `9a1fe0f2` (excluding the new file and its probe) are five lines of prose, the lemma `ZZ_no_2_torsion` (`Instance/Ab/Character/NonNatural.v`) and its one use — a statement about one group at one exponent, not a predicate. Torsion is `torsion_mem A a := { k : nat & (0 < k) * (nat_smul A k a ≈ 0) }` with the EXPONENT AS DATA, so two exponents are multiplied when torsion is closed under addition and read back when the quotient is proved torsion-free, and no choice principle appears anywhere; the torsion subgroup is `Instance/Ab/DirectedColimit.v`'s `AbSubgroup` REUSED (`TorsionSub`, `TorsionAb := AbSubgroupAb`, `torsion_incl := absub_incl`) — not a third subgroup record, the other one in tree (`Instance/Ab/Character/Finite.v`'s `Subgroup`) demanding DECIDABLE membership that an exponent found rather than decided cannot supply — and A/T(A) is `Instance/Ab.v`'s pre-existing image quotient applied to that inclusion (`AbModTorsion A := AbQuotient (torsion_incl A)`), so NO new quotient machinery and no coset object anywhere. The route is `Instance/Grp/Abelianize.v`'s: the `∃!` (`torsion_universal`) packaged by `universal_arrow_from_UMP` (`Theory/Universal/Arrow.v`) and read off `LeftAdjointFunctorFromUniversalArrows`/`AdjunctionFromUniversalArrows` with nothing re-derived; the inclusion is proved Full AND Faithful (`TorsionFree_Incl_Full`/`_Faithful`), and `torsionfree_reflect_iso` instantiates `reflective_counit_iso` — B/T(B) ≅ B for torsion-free B — at ℤ as `ZAb_reflect_iso` with no tactic. **Both reviewer checks land, the second at its true strength**: the subcategory is full by construction (`shom` is `True`, the `CRng_Sub` pattern of `Instance/Rng.v`), and the adjunction's unit IS the quotient projection POINTWISE at `eq_refl` (`torsion_unit_is_proj`) but only `≈` as a morphism record (`torsion_unit_is_proj_hom`), because `AdjunctionFromUniversalArrows` builds the unit as the composite `fmap[Incl] id ∘ ab_quot_proj …` — the same pointwise grade `Instance/Mod/Free.v`'s `free_module_unit_is_insert` has, which the brief had misread as a morphism-level precedent. Seven `eq_refl` readbacks (reflector object = the quotient, universal arrow and its object = `ab_quot_proj`/`TorsionFreeMod` on the nose, quotient zero and addition = the base group's, the scalar action at a CLOSED scalar); at a VARIABLE scalar the action is Leibniz by induction only (`nat_smul_quot`), the `Fixpoint` being stuck on `k` so that conversion compares two separately built `AbObject`s that differ in their setoid field and in every law field — and the setoid field is not the discriminating cause, an audit having measured that a variant of `A` agreeing on every data field and differing in one law field alone is rejected identically — measured against the brief's expectation and pinned. **`nat_smul_mul` is REQUIRED from `Adjunction/Unitalization.v`, not restated, on a measurement**: the file's closure is 61 modules (transitive in-project `.vo` dependencies, excluding itself) and dropping that require gives 59 — the delta is exactly `Adjunction/Unitalization.vo` and `Instance/Rg.vo`, the other 51 of Unitalization's 52 already being inside; `ZMod2` (`Instance/Ab/Character/Finite.v`) is reused as the torsion witness for the same reason (delta two; both dropped, 57). ℤ has FIVE names in tree (`ZAb`, `ab_Z`, `Zgroup`, `Ab_Z`, `ab_int`, all `ring_ab Int_Ring`); `ZAb` alone is used. Non-vacuity both ways: ℤ torsion-free (`ZAb_TorsionFree`, via `nat_smul_ZAb : nat_smul ZAb k n = Z.of_nat k * n` — `nat_smul_int_one` covers the generator only, correcting the brief), ℤ/2 ALL torsion and provably not torsion-free with `true ≈_T false` in its quotient, and the MIXED `Ab_product ZAb ZMod2` where `(0, true)` is torsion and `(1, false)` is not — so `TorsionSub` is proper and nontrivial, and the quotient merges the one while keeping the other apart; the one negative that needs it, `mixed_gen_not_torsion`, maps out through the ℤ projection, the others closing by `discriminate` on the ℤ/2 component. Universes: NO constraint block of the 58 carries an equation; ELEVEN witness-block constants carry `Set` as a BOUND only (from `ZMod2`'s `bool`); 24 constants — exactly those mentioning a hom-set of `Ab` or the quotient, and none of the torsion predicate's own — bind the group at `AbObject@{u u u}`, carrier, relation and proof identified, an identification that sits in the BINDER and not in any block and is the donors' (`Print Ab` shows `obj := AbObject@{u0 u0 u0}`, so an `AbObject` at three separated levels is not an object of `Ab`; `AbQuotient` and `absub_incl` carry equations of their own), not introduced here and not claimed unavoidable; the hom = proof identification of `Subcategory` is guarded by a formability negative, and the probe's second formability negative names `Reflective` but fires at its `Subcategory` ARGUMENT with the identical message, so it separates nothing (the `MonoidObject` trap of #340) and whether `Reflective` identifies anything of its own is not measured. 58/58 closed under the global context (55 glob heads + 3 `Program` obligations, no records so no `Build_*`), zero collisions over 70 names, `Test/ProbeTorsionFree371.v` carrying 7 `Fail`s — 3 CONVERSION, 1 TYPING (`Reflective` is a record, not the adjunction), 2 FORMABILITY, 1 instrument — each stripped one at a time with its whole error read, rename-simulated 15/15; `make todo` grows by 12, all in the probe. NOT delivered: the page's torsion COREFLECTION (a different catalog item; `Coreflective` is `Construction/Reflective.v`), functoriality of `TorsionSub`, the reflection's idempotent monad, the `Grp` analogue, p-torsion refinements, any decision procedure for torsion, and any cokernel or coequalizer reading of the quotient
- **Instance/Grp/Epi.v**: epimorphisms of groups are surjective (Mac Lane §I.5 ex. 5). The entire constructive content is the unconditional, axiom-free density theorem `grp_epic_image_dense` (`Epic f → ∀ h, ¬¬ GrpImage f h`), reached through the CONTRAPOSITIVE WITH AN EXPLICIT WITNESS `grp_not_epic_of_witness` — the classical "choose an element outside the image" is `¬∀ → ∃¬` and is not available — with `grp_surjective_is_epic` for the easy direction. The classical biconditional `grp_epic_iff_surjective` is exactly the double-negation elimination of density, and NO separate "cost" can be isolated: under `Epic f` the named hypothesis `GrpImageStable` is EQUIVALENT to the conclusion (`stability_is_the_conclusion`, `surjective_gives_stable`), and so is the strictly weaker `DenseImpliesSurjective` (`weaker_hypothesis_suffices`, `stable_implies_weaker`, `weaker_is_the_conclusion`) — the conclusion is the weakest hypothesis that completes the proof. The design point is a theorem rather than an assertion: `transposition_decides_image` shows that ANY permutation of `Grp_Coset f ⊔ pt` satisfying only HALF the textbook transposition's specification (image cosets ↦ the adjoined point) already yields full `GrpImageDecidable f`, by matching on the coproduct constructor with no decision procedure — so the transposition is replaced by a twist on the `¬¬`-stable power set `GrpCosetPower` that complements at the base coset without deciding which case obtains, involutive precisely because the values are stable. Machinery: the coset space of an image and `SymGrp`, the symmetric group of a setoid (permutations as invertible setoid maps, the inverse being data, so no choice) — `SymGrp` takes a `PropEquiv (is_setoid X)` since the PR "algebraic carriers are sets" (2026-09-17), a `GrpObject` now carrying `grp_prop`, and all fourteen call sites here discharge it by the new `grp_coset_power_prop`, so twelve constants of this file (`grp_action` and its satellites through `grp_two_twist_not_identity`) read `SymGrp (GrpCosetPower f) (grp_coset_power_prop f)` where they read `SymGrp (GrpCosetPower f)` before. **What that PR deliberately did NOT do is the point of this file**: `grp_coset_rel` is NOT truncated — `GrpCosetPower`'s witness constrains only the CODOMAIN `StableSetoid`, so `Grp_Coset f` is asked nothing — and `transposition_decides_image`'s type is byte-identical to its pre-PR one, still concluding the full `GrpImageDecidable f` with no double negation, as are `GrpImage` (a `Type`-valued `sigT`) and `GrpSurjective`. That is the headline strength the PR saved, and nothing in `Test/` guards it: a later truncation of `grp_coset_rel` would weaken `GrpImageDecidable` with no negative firing. Two deliberately non-interchangeable witnesses: `grp_two_incl` (Z/2 into Z/2 × Z/2) exercises the outside-the-image half, but its image is normal and the equivariance step degenerates there — `grp_two_incl_image_acts_trivially` proves the whole image acts as the IDENTITY; `grp_two_sym3` (Z/2 into the symmetric group of a three-letter setoid, as a transposition subgroup — built as `SymGrp`, so associativity is composition's) has a NON-NORMAL image, with `grp_two_sym3_conj_outside`, `grp_two_sym3_moves_a_coset` and `grp_two_sym3_image_acts_nontrivially` exhibiting a concrete stable subset that an element OF the image moves. `Epic` and `Monic` are incomparable in Grp, both halves witnessed (`grp_two_epic_monic_incomparable`). Mac Lane's own hint (2nd ed., printed p. 21) instead permutes the SET H, with three cosets M, Mu, Mv and a separate index-2 case; the `Perm(H/M ⊔ pt)` form built here is the one issue #251 specifies
- **Instance/Grp.v**: Grp, the category of groups over `Sets` (Mac Lane §I.6; Riehl §1.4) — the catalog's most-depended-on Chapter I gap, mirroring `Instance/CMon.v`. `GrpObject` (setoid carrier, unit, multiplication, inverse; the right unit/inverse laws and inverse-respectfulness DERIVED from the left ones, so the record carries only what a group genuinely needs — and, since the PR "algebraic carriers are sets" (2026-09-17), an eighth field `grp_prop : PropEquiv (is_setoid grp_setoid)` at `Instance/Grp.v`, the property that the carrier's `≈` is logically a proposition; `Grp` is pinned by hand to `Category@{u p p}` because that field's `Set+1` would otherwise give the category a third, redundant universe, and the record's universe ROLES permuted from `(carrier, proof, aux)` to `(aux, carrier, proof)`, with no expressible instance lost — `GrpObject@{Set Set Set}` and `Grp@{u h}` under `h < u` are accepted in both trees and `Grp@{Set Set}` was refused in both), `GrpHom` preserving unit and multiplication with inverse preservation derived (`grp_map_inv`), the category `Grp`, and faithful `Grp_Forget : Grp ⟶ Sets`. One-element group as `Grp_Zero : ZeroObject Grp` (initial and terminal coincide on the nose, `zero_coincide := iso_id`); componentwise `Grp_Cartesian`. `Grp_injectivity_is_monic` is a genuine biconditional — the monic→injective direction probes f with its KERNEL inclusion against the trivial map, no ℤ needed. Reconciliation both ways with `Structure/Group.v`'s internal `GroupObject` at `Sets`' cartesian monoidal structure (`Grp_GroupObject`/`GroupObject_GrpObject`) — read those two at their now-different strengths: `Grp_GroupObject` is unconditional and unchanged, while `GroupObject_GrpObject` takes a `PropEquiv (is_setoid X)` since the PR "algebraic carriers are sets" (2026-09-17), a bare `X : Sets` supplying no `Prop` mirror of its `≈`, and the six round-trip `Example`s take it as a parameter. Riehl §1.4: the opposite-group endofunctor `Grp_Op` with `Grp_op_Isomorphism : Id[Grp] ≅ Grp_Op`, whose component is inversion — an anti-homomorphism, which is exactly why it is a homomorphism into the opposite group. `Z2` on `bool` is the nontrivial witness (audit-supplied): `Monic_in_Grp_is_not_vacuous` and the kernel's non-degeneracy demonstrate the machinery beyond the one-element group, and the `rt_back_*` examples record the second reconciliation round trip
- **Instance/Grp/Limit.v**: Mac Lane §V.1 Theorems 2 and 3 (book pp. 111-112) + Awodey §5.6 Proposition 5.32 (printed p. 119) — **`Grp_Forget` STRICTLY CREATES every limit**, hence `Grp_Complete` and `Grp_Forget_continuous : ContinuousFunctor Grp_Forget` — the cone-level reading, which is what the word means in Mac Lane §V.4, with the apex-only `Grp_Forget_PreservesAllLimits` derived from it (all three of the issue's pinned names, plus `Grp_Forget_lifts_limits` and `Grp_Forget_reflects_limits`). **TWO FIRSTS, MEASURED BY SHAPE RATHER THAN BY NAME**: `Grp_Complete` is the FIRST inhabitant of `@Complete` at ANY ALGEBRAIC category — sweeping declaration heads whose statement mentions word-bounded `Complete`/`Cocomplete`, comment-stripped and across line breaks, the whole prior unconditional concrete roster is `Sets_Complete` (`Instance/Sets/Complete.v`), `ConeSet_Complete`, `Sets_Cocomplete` (`Instance/Sets/Cocomplete.v`) and `Subsets_Complete`/`Subsets_Cocomplete` (`Instance/Powerset.v`), with NOTHING for `Grp`, `Ab`, `CMon`, `Rng`, `RMod`, `Mon`, `Top` or `Cat` — and `Grp_Forget_creates_limits` is the FIRST creation inhabitant at a NAMED CONCRETE category, the ELEVEN declaration heads outside the declaring file whose statement mentions one of those classes being four `EM_Forget T` (generic in the monad) and seven generic in their functor (`comma_proj2` once, a subcategory inclusion three times, an arbitrary equivalence twice, `Id` once); correspondingly `EM_Forget` was the ONLY forgetful functor anywhere carrying a preservation, reflection, lifting or creation result — a sweep for `_Forget` heads INHABITING a creation class returns exactly those four, and widening to lifting and reflection adds `em_strict_lift`, `monadic_creates` and `em_forget_reflects_isos`, still all `EM_Forget` — and NO limit, cone, completeness, creation, lifting, preservation or continuity statement about `Grp` or `Grp_Forget` existed at all. **THE HEADLINE SENTENCE IS MACHINE-CHECKED AT `eq_refl`, NOT ARGUED**: at the limits `Sets_Complete` chooses — the compatible families — the created group is the COORDINATEWISE one on the nose, `grp_complete_carrier`, `grp_complete_mul`, `grp_complete_unit`, `grp_complete_inv` and `grp_complete_leg` being five `eq_refl` Examples at an ARBITRARY shape and an ARBITRARY diagram of groups, so the file needs no concrete witness to be non-vacuous and builds none. **THIS IS NOT AN INSTANCE OF `EM_Complete` AND THAT IS MEASURED**: no `.v` file exhibits `Grp` as an Eilenberg-Moore category (the token `EilenbergMoore` co-occurs with `Grp` nowhere), so the monadicity route is unavailable and the argument is rerun one level down, sharing the ARCHITECTURE of `Monad/Eilenberg/Moore/Limit.v` — build the structure as the MEDIATOR of a cone whose legs are the diagram's own operations, then get every law from joint monicity — clause for clause. **TWO PLACES WHERE THE GROUP CASE COMES OUT CHEAPER THAN THAT TEMPLATE**: no image cone is built, `FCone Grp_Forget N` being the elementwise cone on the nose (`grp_fcone_apex`, `grp_fcone_leg`, both `eq_refl`) where the donor declares its own `car_cone` and `rcar_cone`; and `grp_reflects` takes NO leg hypothesis, being stated directly against `IsLimitCone (FCone Grp_Forget M)` — precisely the `screates_reflect` clause — where `em_reflects` must carry an `Hlegs` argument. **THE ENGINE IS ONE REUSABLE LEMMA WITH NOTHING GROUP-THEORETIC IN IT**: `sets_limit_ext` says the legs of a limiting cone in `Sets` are jointly monic ELEMENTWISE, proved from the mediator's uniqueness alone with the two constant maps out of the apex itself as probes — so it needs no terminal object and pulls in no further module — after which every group law is three lines. It belongs beside its donors in `Instance/Sets/Complete.v` and is declared here only because that file is upstream. **REUSABILITY (the issue's work item 3) AT THE STRENGTH IT HAS**: that lemma and the skeleton transfer to `CMon`, `Ab`, `Rng`, `RMod` — an engineering judgement, nothing compiled for it; what is NOT done is a signature-parameterised statement, and the reason is NOT that the obvious substrate is axiom-carrying, since measured, `Instance/Comp.v`'s `Algs` and its `GroupOp` are BOTH Closed under the global context — it is that `Algs S` is the category of algebras for an OPERATION signature with no equations, so a category of GROUPS there needs the equational layer, where `GroupEq`, `Group` and `Product` each carry `functional_extensionality_dep`, and `Group` is a `Type` of algebras with no category attached, nothing in tree relating it to `Grp`; a scope choice with one measured cost attached, not a proof that no such route exists. **UNIQUENESS IS THE WHOLE CONTENT OF THEOREM 2 AND IS PROVED AT TWO STRENGTHS**: elementwise, `glim_unit_unique`/`glim_mul_unique`/`glim_inv_unique` with `glim_structure_unique` bundling them — and note what they consume, ONLY that the legs preserve the candidate structure and NO law of it, so the statement is sharper than the book's; categorically, `Grp_lift_cone_unique` is the generic `screates_lift_unique` at this instance. A whole-record "`G' = LimitGroup`" is NOT delivered and could not be closed by more work here: it would need `grp_setoid G' = vertex_obj[L]` at Leibniz equality and a transport in the type of every operation, which is why the donor's `em_alg_unique` pins its carrier the same way. **THE ISSUE'S "Current state" IS STALE ON EVERY COUNT** and the file records the measurements rather than repeating them: `Grp` exists (word-bounded `Grp` matches 1022 LINES across 88 files — 1112 occurrences — not three), the creation vocabulary exists (the issue's literal command returns four hits in two files, all prose, so it measured the phrase and not the vocabulary), the limit theory for an algebra category exists in a file the issue does not look at, and `CMon_Forget` is neither the only forgetful functor into `Sets` nor without consumers — only its last clause, that no preservation/reflection/lifting/creation result about it exists, survives. **83/83 constants closed under the global context with ZERO `Axioms:` lines**, all gated FULLY QUALIFIED; universes measured off BOTH binder and block over all 83: ZERO word-bounded `Set` anywhere, and only TEN carry a block equation at all — the five generic `Sets` constants `sets_pre`/`sets_med_eq`/`sets_limit_ext`/`sets_const` and the last one's `Program` obligation `sets_const_obligation_1`, which no source-level reading sees, all five carrying `u0 = u3` (the shape's hom-and-proof universe identified with `Sets`' carrier universe, `IsALimit`'s doing rather than this file's — the probe pins it with `Cone` ACCEPTED at the very levels where `IsALimit` is refused) and the first three additionally `u1 = u2`, plus the five `grp_complete_*` readbacks, which BIND their shape explicitly and so carry `u = u0`, `Complete`'s own `@{u u u u0}` shape written out — the remaining 73 carrying NONE, `Grp_Complete@{u u0 u1} : Complete@{u u u u0}` among them, so the smallness discipline is exactly `Sets_Complete`'s and nothing here narrows it; and read the hom = proof identification in those binders correctly, since every `Category@{u u0 u0}` is the SHAPE and the identification is the LIMIT VOCABULARY's rather than `Grp`'s or `Sets`' — at a shape whose hom is declared strictly below its proof universe, `Jv`, `Jv ⟶ Grp`, `Cone` and `@Complete Grp` are ACCEPTED while `IsALimit`, `Limit`, `CreatesLimit`, `StrictlyCreatesLimit` and `Grp_Complete Jv Kv` are REFUSED. Zero collisions over the 86 names the change puts into the environment (88 counting the two a `Fail` declares and the environment never receives). `Test/ProbeGrpLimit411.v` carries **4 negatives of TWO kinds plus a scope-free instrument check** — 2 UNIVERSE (`IsALimit` at a shape whose homs are declared strictly below `Sets`' carrier universe, with `Cone` ACCEPTED at those very levels as the discriminating control; `sets_limit_ext` inherited, firing at its FUNCTOR argument), and 2 CONVERSION (that `CreatesAllLimits` and `StrictlyCreatesLimits` are different types, and that the created group depends definitionally on WHICH limit oracle it is fed — over `ConeSet_Complete` the carrier IS `cone_apex` and NOT `Sets_limit_obj`, which is what keeps the five `grp_complete_*` readbacks honest) — each stripped ONE AT A TIME in a copy of the whole probe and compiled alone, with guard coverage measured mechanically (25 identifiers inside a `Fail`, 21 also outside, the four exceptions exhaustive) and rename-simulated 2/2; the two universe negatives DO close with the tree's usual `universe inconsistency: Cannot enforce so = jh because jh < so` tail, which is what tells them apart from the two conversion ones. `make todo` grows by 13, ALL in the probe, the library file contributing ZERO. NOT delivered: no colimits or cocompleteness for `Grp` (Awodey §5.6's ω-colimit half is #561's) — true when written; since #450 `Grp_Cocomplete_via_GAFT` (`Instance/Grp/Colimit.v`) makes `Grp` cocomplete by the adjoint functor theorem on this file's `Grp_Complete`, and what stays undelivered is the filtered-colimit route and any colimit that computes; no signature-generic variant; no comparison of the created binary product with `Grp_Cartesian`, which would go through a discrete diagram and hence through `Instance/Discrete.v`'s unannotated `DiscreteCat_Functor`, pinning the ambient hom and proof universes to the literal `Set` (a reason that had stopped holding before #450, whose builder noticed it: the PR "algebraic carriers are sets" annotated `DiscreteCat_Functor` in place, and `About` now reads `DiscreteCat_Functor@{o h p uo uh up u}` with no `Set` in its constraint block; the comparison itself is still not made, and this file's own header records the same correction since #450); no analogue for `CMon`, `Ab`, `Rng` or `RMod`, though the engine transfers unchanged; no monadicity statement and no comparison functor; and nothing registered as an `Instance` — the file declares none, since a chosen limit must not become globally resolvable
- **Instance/Grp/FreeAFT.v**, **Test/ProbeGrpFreeAFT442.v**: Mac Lane §V.6 (book p. 123) with Awodey §7.2 and Riehl §4.7 Example 6 / §5.1 Example 4 — **the free group functor read through the adjoint functor theorem, and the honest residue of that reading**. The issue's survey of the tree is false in SEVEN places, each re-measured against a green build on 2026-09-12 and enumerated in the header: `Grp` and `Grp_Forget` (`Instance/Grp.v`), `FreeGrp` and `free_group_adjunction` (`Instance/Grp/Free.v`, delivered by #298), GAFT's first two hypotheses `Grp_Complete`/`Grp_Forget_continuous` (`Instance/Grp/Limit.v`), and the Riehl §4.7 section's "NO concrete solution set is exhibited anywhere in the tree" — refuted by `Sets_Id_SolutionSet` (`Adjunction/GAFT/Sets.v`), hand-built and fed to GAFT in that same file, which also refutes the claim that the only worked GAFT application is `Δ ⊣ (×)`. So `free_group_via_GAFT` compiles — **and WAS CIRCULAR, which is why it shipped under a name that says so**: its solution set was `solution_set_of_adjunction` of the very adjunction Freyd's theorem is meant to produce, and the probe pinned that literally (`p442_solution_set_is_circular`, `p442_sol_arr`: the family is a singleton whose member IS `FreeGrpObject X` and whose arrow IS `free_group_unit X`). **IT IS NO LONGER CIRCULAR.** In the PR "algebraic carriers are sets" (2026-09-17) the fourth argument moved to `Grp_Forget_solution_set_prop` — the `Prop`-valued congruences on the free group's word carrier `FGWord X`, covering every `X → U G` through the kernel of `free_grp_extend` into `G`, which is a congruence because `G` carries `grp_prop` — while the TYPE of `free_group_via_GAFT` is byte-identical, so nothing downstream changed. Measured: `Grp_Forget_solution_set_prop@{u u0 u1 u2 u3 u4}` at SIX universes against the circular solution set's nine, and `free_group_via_GAFT@{u u0 u1 u2 u3}` at FIVE against the circular application's ten, with the only bounds on its index slot `Set < u` and `carrier ≤ u`; on `FGCongIdx` the bound relating carrier to index is `u ≤ u1` and NOT `u < u1`, which is the whole difference from a Σ over objects. The circular solution set is KEPT and re-headed as a removal candidate, with the application built from it kept beside it as the new `free_group_via_GAFT_from_adjunction` (named for the solution set it takes), so the contrast is visible at every use site; `free_group_via_GAFT_agrees` is unchanged in body and now compares `FreeGrp` against a GAFT output that does not presuppose it. The one cost is `Set < carrier`, attributed from `About Sets` and `About Grp` rather than guessed, and it bites only at a NAMED object: the probe's N13 pins that `` `1 free_group_via_GAFT TwoLetters `` (the `bool` setoid) is refused while `SetOne` is not, and while `Grp_Forget_solution_set_prop TwoLetters` and the circular application both elaborate. **The other four deliverables do not depend on the AFT application at all.** The issue's QA-correction clause is `free_group_via_GAFT_agrees`, at `≈` by `left_adjoint_iso` — N1/N2/N3 refute the three stronger readings (the functors are not convertible, not even at the singleton, and the two adjunctions are not of the same type), since `GAFT` is `Qed`-opaque at `Adjunction/GAFT.v`. Awodey §7.2's `Grp_Forget_Representable` is the tree's first representation of `Grp_Forget` (the three prior mentions of `Representable` under `Instance/Grp/` — `Quotient.v`, `EckmannHilton.v`, `Abelianize.v` — are all disclaimers), `repr_obj = FreeGrp unit_setoid_object` at `eq_refl`, both transposes read at `ttt`, and NON-VACUOUS by two probe theorems: the representation separates the two elements of `Z2`, hence `repr_obj` is not isomorphic to `Grp_trivial` — a degenerate transformation could carry neither. Riehl §5.1's `free_group_monad` records `ret`, `join` and the carrier at `eq_refl` (`free_group_unit`, `fmap[Grp_Forget] (free_group_counit (FreeGrp X))`, `FGWord X` — the last pinning Riehl's "words in the letters and their formal inverses"), with N8 pinning that the multiplication inherits the counit's `Qed`-opacity and so reaches word evaluation only up to `≈`. **Mac Lane's injectivity clause ships in two strengths and the gap between them is the finding**: `free_group_unit_injective_of_embedding` needs no hypothesis on `X` — any embedding of `X` into some group's underlying set forces the unit injective, by transposing the embedding — while the book's own two-element-group separation argument (`free_group_unit_injective_dec`, over `Instance/Grp.v`'s `Z2`) manufactures that embedding by a characteristic function, which on a SETOID does not exist without a decision procedure for `≈`. The uniform statement is therefore neither proved nor refuted: the obstruction is to the ARGUMENT, and closing it would mean analysing `FreeGroupoidRel` (`Construction/Free/Groupoid.v`), which that file states offers no normal form and no decision procedure. `free_group_two_letters_distinct` recovers `Instance/Grp/Free.v`'s S3 proof as an instance, and the probe pins that the two statements are convertible (`p442_two_letters_same_statement`) while the proofs are not (N9), so neither is an alias of the other. **A causal claim was refuted mid-flight and is corrected in place.** An earlier revision of the header said Mac Lane's literal solution set is blocked because GAFT with `Grp_Complete` pins the index universe, and that relaxing it meant editing a `Qed`-opaque theorem, so it "belongs to that file's own issue". The refusal is real — N5 pins it, against a paired control at a `Set`-level index that IS accepted, and the measured floor was `Set` exactly — but the cause is neither GAFT nor `Grp_Complete`: it is a universe-minimization artifact of `Instance/Discrete.v`'s unannotated `DiscreteCat_Functor`, which elaborates at `DiscreteCat@{u Set Set}` and reaches GAFT's statement through `GAFT.v`. `Instance/Cat/Objects.v` had already recorded that about the same donor ("the pin is NOT claimed unavoidable -- it is a minimization artifact"), with `disc_ext@{o h p q}` as the `Set`-free twin. **The diagnosis was acted on.** The donor was annotated in place at `Instance/Discrete.v` in the PR "algebraic carriers are sets" (2026-09-17); re-measured there, `GAFT@{cobj dobj h u u0 u1}` carries no literal `Set`, probe N5 of `Test/ProbeGrpFreeAFT442.v` turned over and is kept as a positive control, and the `Set` FLOOR quoted above is gone. What is NOT gone is the wall itself: with the floor removed the solution-set index must still sit at `Complete`'s shape-object universe, which `Grp_Complete` puts at the carrier universe, and Mac Lane's `Subgroup G` index sits strictly above it. `free_group_via_GAFT` therefore remained circular for the size reason recorded in the size note in the header of `Structure/Complete.v`, not for a `Set` pin — until the later commit of the same PR that lifted it through `PropEquiv`-indexed congruences, which is the correction recorded above. What is STILL not built is Mac Lane's own `Subgroup G` index: the congruence index replaces it rather than resizing it, so #1309 stays open on that count. Measured in a full copy of this worktree: annotating that one donor `@{o h p co ch cp +}` — three lines, no `Qed`-opaque term touched, `Adjunction/GAFT.v` and `Instance/Grp/Limit.v` byte-identical — makes the `Subgroup G` solution set ACCEPTED, and a whole-tree `make -k -j8` over the 977-file build set turns over exactly eleven negative probes (`Instance/Cat/Objects.v`, `Structure/Limit/Components.v` and nine `Test/Probe*` lines, this development's own N5 among them) and breaks ZERO proofs. That annotation is NOT made here: it is bookkeeping against eleven recorded boundaries and belongs to `Instance/Discrete.v`. It also corrects `Instance/Cat/Objects.v`'s remark that a `Program Definition` cannot be annotated there — the `+` is what makes it possible. Traps met: inside `{| to := … |}` the names `to`/`from` resolve to the fields being defined, not to `Isomorphism`'s projections, so both transposes are lifted out; `fmap[Grp_Forget] f ∘ grp_repr_const x a` is pointwise equal to `grp_repr_const y (f a)` but is a different record, so `from_adj_nat_r` does not apply directly and each naturality obligation goes through `transitivity` + `proper_morphism`; `unit_setoid` is `poly_unit` under `eq`, so `iso_from_to` needs a `destruct` on the point; and `Construction/Deloop.v`'s `Bool_Xor_Grp` inhabits that file's OWN `GrpObject` record, so the in-tree `Z2` is the one to reuse. Measured: 20 `.glob` heads (15 `def`, 5 `prf`) plus **9 `Program` obligations invisible to that census** (8 `Grp_Forget_Representable_obligation_k` and `grp_chi_obligation_1`, enumerated by `strings` over the `.vo`; `grp_repr_const`'s is inlined and mints no constant), all 29 "Closed under the global context" and all 29 gated (7545 → 7574) — `free_group_via_GAFT` included. A new gate trap: these obligations are NOT in the short-name space after `Require Import`, so `Print Assumptions` needs the fully qualified `Category.Instance.Grp.FreeAFT.<name>`. The probe carries the target's full 18-Require import list plus `Instance/Grp/Quotient.v` and the target, 12 refutations each stripped alone in a copy of the whole file (kinds kept apart: NAME-ABSENCE instrument, TYPE for I1/N3/N6/N7/N10, CONVERSION for N1/N2/N4/N8/N9, UNIVERSE for N5) and 22 positive readbacks; N6/N7 pin that neither `Grp_Forget_continuous` nor `Grp_Forget_PreservesAllLimits` ascribes where GAFT asks for cone-level `PreservesImageLimit`, which is why the `Continuous_PreservesImageLimit` bridge stands in the term. `make todo` 2317 → 2329, all twelve the probe's own refutation lines. Not delivered: the generated-subgroup API (filed as #1309, together with the `Instance/Discrete.v` annotation and the eleven boundaries it would turn over — an earlier revision of this clause also listed "any non-circular solution set", which the PR "algebraic carriers are sets" delivered on 2026-09-17 and which is corrected above); no uniform unit injectivity; and no cardinality bound for THIS family, so §V.7's cut-down is not formalised here — `Adjunction/GAFT/Resize.v` supplies the vocabulary and `Instance/Mod/TensorAFT.v` the one proof, neither reaching the free group. `free_group_monad_via_GAFT` is an addition of that PR and has NO `eq_refl` readbacks at all, `GAFT` being `Qed`-opaque, which the file says in terms.
- **Instance/Ab/Limit.v**, **Instance/Rng/Limit.v**, **Instance/Rng/AFT.v**, **Test/ProbeAlgLimit443.v**: Mac Lane §V.6 Exercise 2 (book p. 125) with Awodey §9.9 Ex 9 and Seven Sketches §3.4.2 Example 3.74 clause 1 — **limits of abelian groups and of rings are computed on underlying sets, and the adjoint functor theorem is then applied over both**. The limit half is the substance: `Ab_Forget` and `Rng_Forget` each STRICTLY CREATE every limit, on the `Instance/Grp/Limit.v` template (the only algebraic limit-creation file in tree before this one), with the created cone lying over the given one ON THE NOSE — both readbacks `eq_refl` in each file, because each forgetful functor's object map is the carrier projection, and for `Ab` that projection is the TWO-step `cmon_setoid ∘ ab_cmon` through the `ab_cmon :> CMonObject` coercion, which does not disturb it. From one creation result per category the whole cascade follows by application: `Ab_Complete`/`Rng_Complete` and the continuity, preservation and reflection corollaries. **Neither category had a `Complete` instance before** (`@Complete Ab` and `@Complete Rng` returned nothing tree-wide), which is what made #443's own AFT clauses unstatable. **The `Ab` continuity result is the point, and its name says so.** `Ab_Forget_Continuous` already existed at `Instance/Ab/FreeNotContinuous.v` — but as `right_adjoint_Continuous free_ab_adjunction`, i.e. RAPL applied to the free/forgetful adjunction, so feeding it to Freyd's theorem would presuppose the adjunction the theorem is asked to produce. `Ab_Forget_creates_continuous` presupposes nothing beyond `Sets_Complete` and creation; both inhabit `ContinuousFunctor Ab_Forget`, and the probe pins that they are different terms. The names differ only by case and coexist deliberately — an earlier probe comment claimed the lower-case name "could NOT be given" because the other was taken, which is false (Coq identifiers are case-sensitive, measured), and the real reason is the choice recorded here. **`Rng_Forget` factors through `Ab` on the nose** — `fobj[Rng_Forget] R = fobj[Ab_Forget ◯ Rng_Forget_Ab] R` and the `fmap` twin are both `eq_refl` — and `Rng_Forget_Ab_continuous` is derived from that plus `Rng_Forget_continuous` and `Ab_Forget_reflects_limits`, with no adjunction and no RAPL. It is NOT derived without creation (both inputs are creation results, and an earlier header revision claiming otherwise is corrected in place); what it avoids is a creation argument for `Rng_Forget_Ab` ITSELF, which genuinely stops — multiplication is bilinear, so the would-be cone is not a cone in `Ab` at all, and `Ab`'s initial object is a ZERO object (`Instance/Ab.v`) so no canonical map out of it selects `1`. That non-delivery is recorded with its reason rather than as an omission. **The AFT half applies GAFT twice and discloses what is circular**, on the `Instance/Grp/FreeAFT.v` precedent: at `Rng_Forget_Ab` and at `Rng_Forget`, each on a solution set named `*_solution_set_from_adjunction` so the circularity is visible at the use site. **Since the PR "algebraic carriers are sets" (2026-09-17) neither application is circular.** `free_rng_ab_via_GAFT` and `free_ring_via_GAFT` consume the new `Rng_Forget_Ab_solution_set_prop` and `Rng_Forget_solution_set_prop`, congruence-quotient solution sets on the free-ring terms — the second built DIRECTLY on `FRTerm (FreeAbObject X)` rather than through an `Adjunction/Compose.v` composite — indexed by the `Prop`-valued `RngCongIdx` and closed under the global context; the circular solution sets are KEPT and re-headed as removal candidates, with the circular applications preserved beside them under the new names `free_rng_ab_via_GAFT_from_adjunction` and `free_ring_via_GAFT_from_adjunction`. ONE statement change was forced and is recorded with its measured reason: the five `_mon` clauses of this file (`..._agrees_mon`, its `_via_left_adjoint_iso` Example, `..._comparison_mon`, its `_is_component` Example, `..._unit_mon`) are now stated about `free_ring_via_GAFT_from_adjunction`, because `free_ring_via_mon` is `Functor@{u Set Set u …}` while the non-circular application carries `Set < carrier`, and the attempt is refused with "Cannot enforce Set = u0 because Set < u0"; the `_ab` comparisons ARE about the new constant and are unaffected, and widening `free_ring_via_mon` belongs to `Instance/Rng/Free.v`'s own commit. `Test/ProbeRngAFTProp.v` is the new guard: an instrument check, fourteen controls, `eq_refl` index readbacks and two conversion negatives, with witnesses at `ring_ab Int_Ring`, `nat_setoid_object` and `bool_setoid_object`. The second application looked impossible and is not: no adjunction in tree NAMES `Rng_Forget` as its right adjoint (`grep -P '⊣\s*Rng_Forget(?![A-Za-z0-9_])'` returns nothing, instrument-checked), but `Instance/Rng/Free.v`'s `free_ring_via_ab_adjunction` has right adjoint `RngUnderlyingAb := Ab_Forget ◯ Rng_Forget_Ab`, which the factorization identifies with `Rng_Forget` on data; every field of `Adjunction` mentions `U` only through `U y` and `fmap[U] f`, so the record copies across field by field. `free_ring_via_ab_adjunction_set : free_ring_via_ab ⊣ Rng_Forget` is the tree's first inhabitant of that type — **the grep measured NAMES and the question was TYPES**, and the same manoeuvre is already in tree at `Instance/Rng/Free.v`. Three comparison clauses (`≈` only, since `GAFT` is `Qed`-opaque) discharge the issue's "compare with the usual construction", via `Instance/Rng/Free.v`'s `left_adjoints_agree` and, recorded beside each, `Theory/Adjunction.v`'s `left_adjoint_iso`. Measured: **241 constants gated and all "Closed under the global context"** (7574 → 7815) — 75 `.glob` heads + 10 `Program` obligations for `Ab/Limit.v`, 104 + 18 for `Rng/Limit.v`, 34 + **0** for `AFT.v`, which uses no `Program` at all. `make todo` 2329 → 2354, all 25 the probe's refutation lines; the three development files add ZERO. The probe carries both limit files' full import lists and 25 refutations, each stripped alone in a copy of the whole file, kinds kept apart (NAME-ABSENCE, TYPE, CONVERSION) with paired positive controls, and it pins the pointwise-ness of every lifted operation against refutations of the obvious wrong statements. **A quoted refusal is import-sensitive, which two independent measurements discovered the hard way**: the `cannot unify` parenthetical of the `ContinuousFunctor`-does-not-ascribe-to-`PreservesImageLimit` error is rendered with the module short-names in scope, so the same refusal prints `"Limit.Limit K"`/`"Cone.Cone K"` under one import list and `"Cone.Cone (Ab_Forget ◯ K)"`/`"IsLimitCone N"` under another (four import lists measured). One agent recorded a "correction" to a header that was in fact right; only the STABLE head of the error is quoted now, in all three files. Traps: the four-lemma `Sets` joint-monicity block is duplicated in both limit files under `absets_*`/`rng_sets_*` prefixes rather than hoisted into `Instance/Sets/Complete.v`, which would edit an upstream file — disclosed in both headers, and the right repair is named; `RigHom` has no negation clause (`Theory/Algebra/Rig.v`'s `RigHom_neg` derives it), so `Rng`'s negation coherence is a theorem where its four siblings are projections; and two intended names collided with two in `Monad/Eilenberg/Moore/Limit.v` and were renamed. Not delivered: no creation result for `Rng_Forget_Ab`; no cardinality bound, so §V.7's cut-down is absent (an earlier revision of this clause also listed "no non-circular solution set for either application (the same gap `#1309` records for `Grp`)", which the PR "algebraic carriers are sets" delivered on 2026-09-17 and which is corrected above; the `241 constants gated` figure earlier in this bullet was measured before that PR and has NOT been re-run by its own criterion — re-measured on 2026-09-17 by a different one, `Instance/Rng/AFT.v` alone holds 57 `.glob` heads and 5 `Program` obligations); and the issue's graph clause — an AFT route to the free category on a graph, to be compared with `Construction/Free/Quiver.v`'s hand-built `FreeForgetfulAdjunction` — is NOT attempted, since it would need a completeness result for the strict-category side that no file supplies.
- **Instance/Grp/Colimit.v**, **Instance/Grp/Colimit/Presentation.v**, **Instance/Rng/Colimit.v**, **Instance/Variety/Limit.v**, **Instance/Variety/Colimit.v**, **Theory/Subobject/Disjoint.v**, **Adjunction/Diagonal/Limit.v** (section `DiagonalContinuous`), **Instance/Variety/Free.v** and **Instance/Grp.v** (both extended), **Test/ProbeAlgColimit450.v**, **Test/ProbeVarietyColimit450.v**: Mac Lane §V.7 Exercises 1, 2 and 4 (book p. 128, PDF p. 137, `maclane:V.7:ex1`, `maclane:V.7:ex2`, `maclane:V.7:ex4`), §IX.1 Exercises 1 and 2 (book p. 214, PDF p. 221, `maclane:IX.1:ex1`, `maclane:IX.1:ex2`) and Riehl §5.6's free-product construction (printed pp. 212–213, PDF pp. 232–233, `riehl:5.6:construction-free-product`), issue #450 — **`Grp`, `Rng` and every setoid variety `SVariety E` are COCOMPLETE, each by the adjoint functor theorem applied at the DIAGONAL, and the free product of groups, the coproduct of rings and the coequalizers of a variety are read off that**. **ONE THEOREM AT ONE FUNCTOR**: for every shape `J` within the discipline stated below, `Adjunction/GAFT.v`'s `GAFT` is applied to `Δ[J] : C ⟶ [J, C]` (`Grp_colim_via_GAFT`, `Rng_colim_via_GAFT`, `SVariety_colim_via_GAFT`) on completeness (`Grp_Complete`, `Rng_Complete` and the new `SVariety_Complete`), on the new `Diagonal_continuous : ContinuousFunctor (Diagonal J)` through `Continuous_PreservesImageLimit` (`right_adjoint_PreservesImageLimit` would be circular, Δ[J] being a right adjoint exactly when the colimits exist), and on a solution set at each diagram (`Diagonal_Grp_solution_set`, `Diagonal_Rng_solution_set`, `Diagonal_SVariety_solution_set`); `Diagonal_left_adjoint_HasColimits` reads the colimits off, assembled as `Grp_Cocomplete_via_GAFT`, `Rng_Cocomplete_via_GAFT` and `SVariety_Cocomplete_via_GAFT` (§IX.1 Exercises 1 and 2). **THE SOLUTION SETS ARE CONGRUENCE-INDEXED, NEVER A Σ OVER OBJECTS**: each index is the `Prop`-valued COCONE congruences on a term model over the disjoint union `Σ j, carrier (D j)` (`FGWord` for groups, `FRTerm` over the free abelian group for rings, `UA.Tree` for the variety) — congruences that respect each factor's `≈`, make every insertion a homomorphism and make the insertions a cocone — and a cocone is covered by the KERNEL of its extension, a `Prop` exactly because the target carries `grp_prop`, `rig_prop` or `soa_prop`; the ADDENDUM (#450) to item 4 of the size note in `Structure/Complete.v` records the three consumers. `dker_cocone` and `rker_cocone` MUST end in `Defined`: closed with `Qed` in a copy of the file, the solution set is refused with "The term "fg_ker_med (dflat c h)" has type … while it is expected to have type "DQ (dker_idx c h) ~{ Grp }~> c"" (both headers quote the full texts). **FINITE COLIMITS THROUGH THE EXISTING BRIDGES, AND WHAT THAT ASSEMBLY IS**: `Grp_Cocartesian_via_GAFT` (Exercise 1's free product), `Rng_Cocartesian_via_GAFT` (Exercise 2), `SVariety_Cocartesian_via_GAFT`, `Grp_HasCoequalizers_via_GAFT`, `Rng_HasCoequalizers_via_GAFT`, `SVariety_coequalizers_via_GAFT` (Exercise 4) and the three `*_Initial_via_GAFT` are `Structure/Limit/Finite.v`'s bridges (`FinitelyComplete_Cartesian`; `HasCoequalizers_of_HasEqualizers_op` after `FinitelyComplete_HasEqualizers`; `FinitelyCocomplete_Initial`) applied to `Cocomplete_FinitelyCocomplete` read in `C^op`, with no new bridge. Read off those bridges' bodies, the coproduct is the AFT pushout over the AFT initial object and the coequalizer is ASSEMBLED from AFT pushouts and the AFT initial object, every one of them a `GAFT` colimit at a diagonal; the direct reading, at a discrete two-object shape or at the walking parallel pair, is NOT the one used. **THE SHAPE DISCIPLINE IS `Cocomplete@{u u0 u u1}`**: shape homs AT the carrier universe and shape objects at or below it, with the one side condition `Set < carrier`. `Instance/Two/Discrete.v`'s `Two_Discrete : Category@{u Set Set}` is refused as a shape ("universe inconsistency: Cannot enforce Set = TD.57 because Set < TD.57", a scratch file's label; `DiscreteCat bool` accepted by the same file), which is why the coproducts come through the finite bridges. The side condition is `GAFT`'s, not the solution set's, and that is measured: under `Monomorphic Constraint Set < gu`, `@Diagonal_Grp_solution_set J D` and `@Diagonal_Rng_solution_set J D` at `J : Category@{Set Set Set}` are ACCEPTED while `Grp_colim_via_GAFT@{gu _ _ Set Set _ _} J` and `Rng_colim_via_GAFT@{gu _ _ Set Set} J` are REFUSED with "Universe inconsistency. Cannot enforce Set < Set because Set = Set." — `GAFT@{cobj dobj h u u0 u1}` takes its index AT the hom universe, which for these categories is the carrier. The two probes pin it for all three categories (`Test/ProbeAlgColimit450.v`'s N4 and N5, `Test/ProbeVarietyColimit450.v`'s N4, the applications left to unification): at `J : Category@{Set Set Set}` the diagonal solution set is accepted, and so is `GAFT` applied to every argument BUT the solution set (the diagonal, `Grp_Complete`/`Rng_Complete`/`SVariety_Complete` and `Continuous_PreservesImageLimit Diagonal_continuous`), so neither the completeness nor the continuity is what a `Set` carrier refuses; at `Grp` and `Rng`, `About` reads the argument that partial application still awaits as `SolutionSet@{Set gu gu Set}`, its index AT the `Set` carrier, and the solution-set controls as `SolutionSet@{u gu gu Set}` with `Set < u`. **EXERCISE 1 IN FULL AT `Grp`**: `Grp_inl_via_GAFT_Monic`/`Grp_inr_via_GAFT_Monic` are `Structure/Bicartesian/Matrix.v`'s `inl_Monic`/`inr_Monic` at `Grp_Zero`, each injection split by the copairing of the identity with the trivial map, and `Grp_free_product_meet_trivial` is the new generic `coproduct_injections_meet_trivial` (`Theory/Subobject/Disjoint.v`): in ANY category with a zero object, binary coproducts and pullbacks, `sub_meet (sub_inl x y) (sub_inr x y) ≈ coprod_bot x y` in #445's subobject order, with nLab's form `coproduct_injections_pullback_zero` (the pullback of the injections is isomorphic to the zero object) and, at `Grp`, the bottom's domain the trivial group at `eq_refl` (`Grp_free_product_meet_bottom_is_trivial`). **MAC LANE'S METHOD HINT IS NOT FOLLOWED, AND THE HEADERS SAY SO**: the in-repo catalog's summary of Exercise 1 opens this half with "Using the product G x H" — in this library `Structure/Semiadditive.v`'s `can_comparison` — while the retraction alone suffices, so the theorem takes NO `Cartesian` (`About`: its binders are `C`, `Z`, `CC`, `PB`, `x` and `y`); the issue's own text names generated subgroups and #445's meet, not the product. **AGREEMENT WITH WHAT THE TREE ALREADY HAD**: `Grp_coproduct_agrees` (`left_adjoint_iso`: the two `+(Grp)` bifunctors agree up to natural isomorphism) against `Instance/Grp/Pushout.v`'s words-based `Grp_Cocartesian`, and at one pair `Grp_free_product_iso` with `_inl`/`_inr` carrying injections to injections; `Grp_initial_agrees` against `Grp_Initial`; `Grp_coequalizer_agrees_normal_closure` comparing the coequalizer of `(f, 0)` with `Instance/Grp/Quotient/Colimit.v`'s normal-closure quotient (Riehl Example 3.1.25). `Pushout.v`'s instance being `#[export]`, every `Cocartesian` argument is passed EXPLICITLY, so no headline that says "via GAFT" can resolve to the words. Non-vacuity above `Set`: `Grp_free_product_via_GAFT_nontrivial`, the AFT coproduct of `Z2` with itself is not a subsingleton (`¬ ∀ a b, a ≈ b`). **RIEHL §5.6 IN BOTH DIRECTIONS** (`Instance/Grp/Colimit/Presentation.v`): the pair `l = F(Uε_G + Uε_H)`, `r = ε_{F(UG+UH)} ∘ F[UF inl, UF inr]` and `e` the transpose across `free_group_adjunction`; `Grp_canonical_presentation` is her Proposition 5.4.2 at the free-group adjunction, proved on letters with no monadicity theorem and no split coequalizer; `Grp_coproduct_is_coequalizer CC` holds at EVERY `Cocartesian` structure on `Grp`, and conversely `Grp_Cocartesian_of_coequalizers HC` makes the coequalizer of `(l, r)` a coproduct for EVERY choice of coequalizers, which is Riehl's own direction. At the theorem's structures: `Grp_free_product_is_coequalizer` at `Grp_Cocartesian_via_GAFT`; `Grp_Cocartesian_via_riehl` on `Grp_HasCoequalizers_via_GAFT`, whose coproduct object IS the AFT coequalizer's apex at `eq_refl` (`Grp_Cocartesian_via_riehl_obj`); `Grp_free_product_coequalizer_agrees` with `_e`, `_inl`, `_inr` and a `_words` twin into `Pushout.v`'s free product; and `Grp_free_product_words_is_coequalizer` is the one reading that uses no adjoint functor theorem at all. Non-vacuity: `Grp_riehl_pair_nontrivial` and `Grp_riehl_e_not_monic` at `Z2`. `coequalizer_unique_along` is a transparent twin of `Structure/Coequalizer.v`'s `Qed` `coequalizer_unique`, carrying one universe more (`u0 < u1`); it is a MERGE candidate for that file, surfaced rather than made. **THE VARIETY**: `Instance/Variety/Limit.v`'s `SVariety_Complete` computes every limit on the compatible families of `Sets`, carrier, operation and leg at `eq_refl` (`svariety_complete_carrier`, `svariety_complete_op`, `svariety_complete_leg`), and `SVariety_Forget_continuous` is cone-level preservation of every limit; creation in `Structure/Limit/Creation.v`'s sense is NOT claimed. `SVariety_Initial` is the free algebra on the empty set, its carrier `UA.Tree S False` at `eq_refl` (`SVariety_Initial_carrier`), isomorphic to the AFT initial object (`SVariety_Initial_agrees`). **"WHEN IS `Alg_τ` EMPTY" IS READ OF THE INITIAL CARRIER**, the category never being empty (`SVariety_Terminal`): `closed_terms_empty_iff_no_constant`, empty exactly when no operation is a constant, is constructive, and `SVariety_initial_empty_iff` states it at the initial object; **the converse, "inhabited ⇒ a constant", NEEDS DECIDABILITY** — `dec_closed_term_constant` proves it under decidable emptiness of each arity (the book's finitary case), and `closed_term_constant_implies_WLEM` shows that its uniform form implies ¬P ∨ ¬¬P for every proposition P. Mac Lane's own §V.6 route is in tree too: `Free_Variety_via_GAFT`, from the congruence-indexed `SVariety_Forget_solution_set`, identified with `Free_Variety_Functor` up to natural isomorphism by `Free_Variety_via_GAFT_agrees`. **EXERCISE 4 IS ON `SVariety E` AND NOT ON #440's LEIBNIZ VARIETY, AND THE REASON IS A THEOREM**: `leibniz_coequalizers_iff_quotients` proves that coequalizers in the Leibniz `Variety` of the EMPTY signature are exactly a quotient-type former (`QuotElim`), `leibniz_coequalizers_give_quotients` being the forward half; the boundary is stated at `NoEq` only (a degenerate `E` such as x = y has coequalizers without quotients, an argument in the header and not a constant), and no claim is made that `QuotElim` is unprovable. Closed non-vacuity at a presentation built without extensionality: `CommMagma_Cocartesian` and `CommMagma_coequalizers` at #440's `CommEq`. **TWO DONORS CHANGED IN PLACE, NO TWINS OF THEM**: `Instance/Variety/Free.v`'s `SetoidOpAlgebra` gains `soa_prop : PropEquiv (is_setoid soa_obj)` and `tree_equiv` becomes `Prop`-valued (`About` ends in `→ Prop`; `tree_equiv_rect` and `tree_equiv_rec` no longer exist; `free_alg_ext_respects` goes through the target's `pequiv_to`), with NO hand pin — measured by `About`, `SetoidAlgs` keeps its seven universes and its `Category@{u1 u2 u2}` shape and gains only `Set < u1`; and `Instance/Grp.v`'s zero object and `Z2` are lifted off `Set` (`Grp_trivial@{p} : GrpObject@{p p p}`, `Grp_Zero@{u p}`, `Z2@{p} : GrpObject@{p p p}`, where `About` had read `Grp_trivial@{u} : GrpObject@{u u Set}` and `Z2@{u} : GrpObject@{u Set Set}`), which is what lets `Grp_initial_agrees`, the trivial meet at `Grp_Zero` and the `Z2` witnesses meet `Set < carrier` at all. That lift turned exactly six guarded negatives in four files (two each in `Test/ProbePushoutGrpTop.v` and `Instance/Grp/EckmannHilton.v`, one each in `Test/ProbeGrpQuotient.v` and `Test/ProbeGalois381.v`) into controls and cost one proof repair, `grp_chi`'s obligation in `Instance/Grp/FreeAFT.v`, whose premise name changed; the bullets recording those negatives are corrected in place. **STRENGTHS, STRICTEST FIRST**: NOTHING ABOUT ANY COLIMIT OBJECT COMPUTES — `GAFT` and `left_adjoint_iso` end in `Qed` (`About` reports both opaque), and `Example conv (G H : Grp) : @Coprod Grp Grp_Cocartesian_via_GAFT G H = Grp_free_product G H := eq_refl` is refused with "cannot unify "G + H" and "Grp_free_product G H""; every agreement is an isomorphism carried as data, or `≈` of functors; the meet is `≈` in the subobject setoid, an `eq_refl` against `coprod_bot` being refused. In `Presentation.v` the counit and `FreeGrp` on arrows do not compute, both being defined by universal factorization, so every letter-level fact there is `≈`. The `eq_refl` readbacks are the three limit readbacks, `SVariety_Initial_carrier`, `Grp_Cocartesian_obj_is_free_product` (about the words-based instance), `Grp_free_product_meet_bottom_is_trivial`, `Grp_Cocartesian_via_riehl_obj` and `Grp_riehl_inl_is_restriction`. Transparency, measured by flipping each `Defined` to `Qed` in a copy: all four of `Instance/Variety/Limit.v`'s and nine of `Instance/Variety/Colimit.v`'s fifteen are load-bearing. Portability: `Instance/Variety/Limit.v`'s term-form `svlim_med_map` was refused by Coq 8.20.1 although Rocq 9.1 accepts it, so three definitions there are tactic proofs; the tree then compiled with rc=0 on Rocq 9.1, and on Coq 8.20.1 and 8.19.2 in clean source copies — the whole build set with the flake's own toolchains before `Instance/Grp/Colimit/Presentation.v` was added, and that file's 152-module dependency closure separately afterwards — and the committed revision then built with rc=0 under `nix build` from its `git+file` source on Coq 8.19 and 8.20, 1035 `COQC` lines each (`grep -c '> COQC '` on the `-L` logs). **MEASURED**: the gate grows 8982 → 9240 (`grep -c 'Print Assumptions' Makefile`) by ONE block of 258 names over nine modules — 8 in `Adjunction/Diagonal/Limit.v`, 8 in `Disjoint.v`, 39 in `Grp/Colimit.v`, 60 in `Presentation.v`, 27 in `Rng/Colimit.v`, 32 in `Variety/Limit.v`, 73 in `Variety/Colimit.v`, the 9 re-annotated `Instance/Grp.v` constants not gated before (the zero-object family less the already-gated `Grp_Zero`, plus `bool_setoid` and `Z2`) and `Free.v`'s `soa_prop` and `Build_SetoidOpAlgebra`, counted by grouping the block's added `echo 'Print Assumptions …'` lines by module — every one "Closed under the global context", and `make print-assumptions` passes; the `Print Module` listings of the six new modules, 239 names with the `Program` obligations and all four record constructors (`Build_IsCoconeCong`, `Build_IsRngCoconeCong`, `Build_IsVCong`, `Build_IsFCong`) among them, were compared with the block name for name and every one is gated (only the `DiagonalContinuous` section and `Instance/Variety/Limit.v` use `Program`); no new constant carries `functional_extensionality_dep`, and no instantiation at `UA.GroupEq`, which would inherit it, is named. `make todo` (`direnv exec . make todo 2>/dev/null | wc -l`) goes 2464 → 2458 → 2479: first −6 for the negatives turned controls, −1 for one reworded prose line and +1 for `Test/ProbeVarietyFree441.v`'s new `p450_tree_equiv_elim_Type`, which pins that `tree_equiv` eliminates only into `Prop`; then +21, exactly the refutation lines of the two new probes; the library files add none. `Test/ProbeAlgColimit450.v` carries 15 refutations (`grep -c '^Fail'`) = 1 NAME-absence instrument + 7 UNIVERSE (N2–N7, N13) + 7 CONVERSION (N1, N8–N12, N14), and `Test/ProbeVarietyColimit450.v` 6 = 1 NAME + 2 CONVERSION (N1, N2) + 3 UNIVERSE (N3–N5), each negative stripped one at a time in a copy of the whole file and classified by its error, which the headers quote; against them stand 91 and 63 positive commands (`grep -cE '^(Check|Example|Definition|Lemma|Theorem|Goal|Compute|Print)'`, the guard blocks' 28 and 30 `Check @` lines included), every negative with a control naming the same constants. Among the controls are the side-condition controls above and `p450_n8_by_fmap_alone`, which closes N8's `≈` with `free_group_fmap_generators` and no counit fact, so N8 is refused on `FreeGrp`'s arrow map rather than on the counit. **DEVIATIONS FROM THE ISSUE, STATED UP FRONT**: the modules are `Instance/Grp/Colimit.v`, `Instance/Rng/Colimit.v` and `Instance/Variety/Colimit.v` rather than the suggested `Instance/Grp/Coproduct.v`, `Instance/Rng/Coproduct.v` and `Instance/Variety/Coequalizer.v`, each delivering every colimit within the shape discipline rather than one; the Verification block's names do not match — `Print Assumptions Grp_free_product` certifies `Instance/Grp/Pushout.v`'s WORDS construction and no AFT one (the AFT coproduct is `Grp_Cocartesian_via_GAFT`, related to it by `Grp_free_product_iso`), `Rng_coproduct` does not exist (it is `Rng_Cocartesian_via_GAFT`; `grep -rnw Rng_coproduct` over the `.v` files finds only that file's NAMES paragraph), and `Variety_coequalizers` is deliberately not declared, since it would name #440's Leibniz variety, which has no coequalizers here (it is `SVariety_coequalizers_via_GAFT`); Exercise 4 is on `SVariety E`; the shape discipline is `Cocomplete@{u u0 u u1}`; "inhabited ⇒ a constant" holds only under decidability; and the issue's survey was stale — `Grp` (#255) and `Rng` (#257) exist, and `Instance/Grp/Pushout.v` had already built the free product by generators and relations. **NOT DELIVERED**: no element-level description of any colimit (no reduced words, normal form or word problem) and no explicit transparent colimit (the least cocone congruence is not built); no coequalizer of a general pair as a quotient (the normal closure of the image of f · g⁻¹ is not built, only the pairs `(f, 0)` are compared); no filtered-colimit route (Mac Lane's primary argument in §IX.1) and no monadicity; Riehl's general Proposition 5.6.11 (#1009) and Proposition 5.4.2 beyond `Grp`; binary free products only; for `Rng`, no statement about the injections (in ℤ/2 ⊔ ℤ/3 one has 1 = 0, a remark and not a theorem), no nontrivial ring above `Set`, and no comparison of `Rng_Initial_via_GAFT` with ℤ, `Rng_Initial_Z` being `@Initial Rng@{u Set}` by `About` and not lifted; for `Disjoint.v`, nothing without a zero object (in `Sets` no retraction of `inl` exists when x is empty and y is not), no indexed form and no converse; no colimits at shapes outside the discipline and none at `Set`-level carriers; no coequalizers for the Leibniz variety; no merge of `coequalizer_unique_along`; `Instance/Grp/EckmannHilton.v` is not restated at `Grp@{u p}`, though it now could be; and nothing is registered as an `Instance`
- **Instance/EnsV.v**: Mac Lane's Ens_V (§I.2) — the category of sets within a set V, as codes with decoding: the general `Spanned C V El` (objects codes `V`, homs ALL C-morphisms between decoded objects) instantiated as `EnsV El := Spanned Coq` and `EnsV_Sets El := Spanned Sets`, each with its inclusion functor. The recovery at the tautological family is **definitional** and general: `Spanned_recovers_ambient : @Spanned C (@obj C) (λ x, x) = C` for *every* C by `eq_refl` (this is where primitive-projection eta is genuinely load-bearing — C is a variable, so no delta is available), with `EnsV_recovers_Coq`/`EnsV_Sets_recovers_Sets` its instances (each also checkable by delta alone, their ambients being transparent record constants) — measured, not asserted. Distinct from `Instance/Ens.v`'s `Ens`/`EnsT`, which deliberately constrain morphisms to preserve and reflect membership; both are kept, cross-linked
- **Instance/Simplex.v**: The simplicial category Δ (Mac Lane §I.2) — objects `nat`, morphisms monotone maps `Fin.t m → Fin.t n` (function + monotonicity witness, hom-setoid comparing the function pointwise), the algebraist's Δ including the empty ordinal. The wide inclusion `Simplex_FinSet : Simplex ⟶ FinSet` is proved faithful AND proved non-full — `no_monotone_swap`/`Simplex_FinSet_not_Full` exhibit the swap on `Fin.t 2` as a FinSet map hit by no monotone map. Face and degeneracy generators `sface`/`sdegen` with computing examples, and ALL FIVE simplicial identities proven (`simplicial_face_face`, `simplicial_degen_degen`, and `simplicial_degen_face` in its lt/eq/succ/gt cases). Deferred, disclosed in the header: the presentation theorem (every map factors as degeneracies-then-faces, and the five identities are a complete relation set) — nothing in-tree depends on it. Named `Simplex` because `Δ` in this tree is already the diagonal functor (`Functor/Diagonal.v`, notations `Δ[J](c)`/`Δ(c)`), with lowercase `delta` also taken by comonoid comultiplication
- **Instance/Variety.v** (+ `Instance/Variety/Clone.v`, `Instance/Variety/GroupComparison.v`): Mac Lane §V.6 (book p. 124) and Awodey §9.8 (printed p. 256) — **the algebras of a variety as a genuine category, and the clone of derived operators**. `Instance/Comp.v` had the entire syntactic apparatus and stopped one step short: `Algs` is the category of algebras of an OPERATION signature, with the equations dropped, and `Group := Algebra GroupOp GroupEq` is a TYPE, not a category — `Instance/Roster.v` records that gap in prose, and this development closes it. `Variety` is the FULL SUBCATEGORY of `Algs` cut out by satisfaction, built with `Sub` in exactly the shape `Theory/Lawvere/Model.v` uses for `Models_sub` (an `sobj` that selects, an `shom` of `True`, closure proofs `I`); a fresh `Category` record was rejected because it would duplicate `Alg_id`/`Alg_comp`/`AlgHom_Setoid` and the four laws, give up `Incl` and `Incl_Faithful`, and make fullness a proof rather than `fun _ _ _ _ _ => I`, and the "`Models_sub` analogue" is not a third option since `Models` IS a `Sub`. The record presentation survives as `Variety_pack`/`Variety_unpack`, mutually inverse ON THE NOSE against `Algebra S E`. `Variety_Forget : Variety ⟶ Sets` lands on the discrete setoid and is faithful, which is Awodey's forgetful functor. **THE AXIOM BOUNDARY IS THE MAIN MEASUREMENT, AND AN EARLIER REVISION OF THIS BULLET DIAGNOSED IT WRONGLY**. `GroupVariety` reports `functional_extensionality_dep`, inherited from `Instance/Comp.v`'s `GroupEq`, through `EqSignature`'s `lhs_natural`/`rhs_natural`, which demand a Leibniz equation between two `arity o → B` argument bundles that are only pointwise equal. That much holds. What was false was the diagnosis "a fact about equation DEPTH — depth 1 free, depth ≥ 2 not", and it is refuted BY CONSTRUCTION: `InvInvEq`, the law `inv (inv x) = x` at DEPTH 2 over the very same `GroupOp`, is **Closed under the global context**. **The real criterion is the SHAPE OF EACH ARGUMENT BUNDLE**: a naturality field converts exactly when every bundle the equation builds is a REINDEXING `args ∘ φ` (the field is then `op_commute` at that bundle) or a CONSTANT bundle (`f_equal` against the constant-bundle former); what defeats it is a bundle MIXING a compound subterm with an `args` leaf, as every group axiom's `mul` node does. Two further measurements pin it: associativity ALONE over `GroupOp`, with no nullary operation mentioned, still needs the axiom; and the SAME law at the EMPTY arity needs it spelled as a literal empty match and is CLOSED spelled as a reindexing — so the empty arity is dodgeable and probe n1 pins one conversion refusal, not a root cause. The surviving claim is narrower and carries its qualifier: no `EqSignature GroupOp` CAPTURING THE GROUP LAWS is axiom-free by this route (an `eq := Empty_set` one trivially is), and that is a statement about the criterion and the routes tried, not a proof of impossibility. Both sides are exhibited rather than asserted — `CommMagmaVariety` (depth 1, reindexing bundles) and `InvInvVariety` (depth 2, constant bundles, over `GroupOp` itself, the sharper exhibit) are both closed. `GroupVariety` and `GroupVariety_Bool` are therefore deliberately NOT gated (gating them would make the axiom audit report a stdlib axiom) and `docs/AXIOMS.md` carries them instead. `Instance/Variety/Clone.v` grades derived operators on `Fin.t`, which costs NO new `Require` (`Lib/Setoid.v` already pulls `Coq.Vectors.Fin` and every file requires `Category.Lib`), and closes them under composition and under substitution along an ARBITRARY `Fin.t m → Fin.t n` — duplication and deletion included, which is what `Construction/PROP/Term.v`'s braid-only `Term` does not do. `clone_subst` IS `induced_map` at the free algebra, so the issue's "recovering `induced_hom`/`from_free_unique` as the free-algebra instance" is literal rather than a remark. **Mac Lane's unique-extension theorem ships TWICE, and which name carries which statement is the point**: `action_extends_to_clone` — the name the issue's Verification block checks — is stated against a `SetoidAction` (carrier setoid plus an argumentwise congruence field `sact_op_respects`) and is **Closed under the global context**; `action_extends_to_clone_leibniz` is the same theorem with `=` for `≈` and reports `functional_extensionality_dep`, because its inductive step compares two argument bundles the IH makes pointwise equal and that are not convertible — the identical wall as `Instance/Comp.v`'s `from_free_unique`, whose own `jww` at names the cure. Nothing is smuggled: `sact_op_respects` is precisely what extensionality supplies for `eq_Setoid`, so the setoid form is strictly more general and strictly weaker in assumptions. `Instance/Variety/GroupComparison.v` discharges the issue's QA correction, which asks for `GroupVariety ≅[Cat] Grp` now that #255 has landed. **THAT ISOMORPHISM IS NOT AVAILABLE AND THE REASON IS STRUCTURAL**: `Grp`'s objects (`Instance/Grp.v`) carry an ARBITRARY setoid where the variety's carry a bare type under Leibniz `=`, so essential surjectivity would need a quotient of a setoid by its own equivalence returning a TYPE, which the tree does not have (`Construction/Quotient.v` quotients hom-setoids, not carriers). What ships instead is the strongest comparison that holds and is labelled as such: `GroupVariety_to_Grp`, FULL and FAITHFUL, i.e. an isomorphism onto a full subcategory. Its file name is `GroupComparison`, not `Grp`, because `Instance/Variety/Grp.v` was tried first and MEASURABLY shadows `Instance/Grp.v`'s `Grp` constant — `Locate Grp` then reports both a Constant and a Module and `GroupVariety ⟶ Grp` stops resolving. Every constant in that file carries the axiom and none is gated. Measured: 27 + 20 + 12 = 59 `.glob` heads after the audit fold (22 + 20 + 11 = 53 before it) PLUS **14 `Program` obligations that a head census does not see** — four in `Instance/Variety.v`, all closed, and ten in `GroupComparison.v`, all axiom-carrying; an earlier revision of this bullet said "zero obligations" and gave the split as 21 + 21 + 11, and both were wrong. Counting them: **46 constants Closed under the global context, all gated (7410 → 7456), and exactly 5 `.glob` heads plus all 22 constants of `GroupComparison.v` carrying `functional_extensionality_dep`** (`GroupVariety`, `GroupVariety_Bool`, `leibniz_action`, `action_extends_to_clone_leibniz`, `clone_act_subst_leibniz`) plus the eleven of `GroupComparison.v`; the axiom audit passes, which is itself the check that the exclusion list is right. `Test/ProbeVariety440.v`: 5 refutations = 1 instrument + n1, n2, n4 CONVERSION + n3 TYPING, each stripped one at a time in a copy of the whole file with the error read, against paired controls, and n3's substance is carried by a POSITIVE theorem rather than by the `Fail` — `AndOp_not_group` proves that the booleans under conjunction admit NO satisfaction proof, which a `Fail` on one candidate term could not, while the control beside it shows `AndOp` is a perfectly good object of the equation-free `Algs`. Renaming each of the declared names, in its declaring file alone, stops the build every time — re-run over all **59** after the audit fold, with 58 breaking inside the probe BODY, every one on a POSITIVE line and none inside a refutation, and the 59th (`GroupVariety`) consumed by the dependent `GroupComparison.v` and so breaking there, reaching the probe only at its `Require` — but only after the sweep FOUND A REAL GAP: six names (`magma_op`, `magma_mul`, `comm_eq`, `comm_law`, `comm_swap`, `sact_setoid`) were unguarded on the first run and the guard block was extended to cover them. Zero collisions after three FORCED renames and three precautionary ones: `act_op` and `act_setoid` are record fields in `Construction/Deloop/Functors.v` and `mmul` is in `Instance/Coq/Monoid/Free.v`, so the record's field family became `sact_*` and the magma operation `magma_mul`, while `comm` → `comm_law` was precaution against a very generic constructor name; `satisfies` was measured clean in the sense that matters — no DECLARATION of it exists anywhere else, checked across all 973 `.glob` files — though it occurs as a word 141 times in 108 files, so an earlier revision's "its only tree-wide hit being prose at `Theory/Concrete.v`" was simply wrong about the count. Closures 19 / 13 / 40, probe 42. ONE `Defined` (`CommEq`, transparent by the convention `GroupEq` already follows); flipping it to `Qed` breaks nothing, so it is disclosed as not load-bearing. TWO TRAPS worth recording: `Instance/Comp.v`'s `:>` coercions (`eq`, `carrier`, `map`) are NOT active under a module ALIAS — only an `Import` activates them, which `Instance/Roster.v` warns against because those three names shadow the library's — so `UA.eq S E` and `UA.carrier A` are written out throughout; and `Construction/Subcategory.v`'s `Full` shadows `Theory/Functor.v`'s, so the latter is qualified. And the rename sweep's own regex, which deliberately refuses to rewrite a name preceded by a dot so that qualified occurrences survive, therefore also skipped the `Makefile` gate's fully-qualified lines — the gate failed with a not-found reference until those were rewritten separately. NOT delivered: no bridge to `Theory/Lawvere/Model.v` beyond a stated connection at the end of `Instance/Variety.v` — building the associated Lawvere theory needs the clone as a CATEGORY with the congruence as its hom-setoid and the two propositional object equalities of `Theory/Lawvere.v`, and the comparison then meets the same setoid-versus-Leibniz wall, so it is filed as a follow-on; no quotient of `Tree` by an equational congruence — and note that an earlier revision of this bullet gave a FALSE reason for that, claiming such a congruence risks `Eqdep`/`JMeq_eq` through inverting `existT` on an `operation S`; measured, making symmetry and transitivity CONSTRUCTORS rather than theorems means no inversion happens at all and the congruence is closed under the global context, so the real reason is only that this development does not need it (the clone needs a setoid on the TARGET of an action, which `SetoidAction` supplies) and it belongs to the free-algebra construction of #441; no axiom-free `GroupVariety`; no removal of `Instance/Comp.v`'s two commented-out instances, which is an edit to that file and is surfaced rather than made; and no clone as a category, no free-algebra adjunction (#441) and no colimits — still true of this Leibniz `Variety E`, and since #450 a theorem at the empty signature: `Instance/Variety/Colimit.v`'s `leibniz_coequalizers_iff_quotients` shows that its coequalizers there are exactly a quotient-type former, while the SETOID variety `SVariety E` (`Instance/Variety/Free.v`) is complete and cocomplete since #450 (`SVariety_Complete`, `SVariety_Cocomplete_via_GAFT`)
- **Instance/Variety/Spanning.v**: Mac Lane §V.7 Remark 1 (book pp. 127–128, `maclane:V.7:remark1`), issue #448 — **the underlying-set functor of a variety has a solution set, and it is the spanning arrows**. `SubAlg` is a subalgebra of an `SVariety E` object (`Instance/Variety/Free.v`) as a `≈`-respecting, operation-closed predicate on the carrier; `sa_satisfies` proves the subalgebra is in the variety by transporting A's satisfaction along the inclusion through `Instance/Comp.v`'s `lhs_natural`/`rhs_natural` (consuming those fields, never proving one), and `sa_subobj` packages it as a `SubObj` of A in `SVariety E`. **Monicity is settled in BOTH directions, and no such bridge existed for `SAlgHom`** (40 non-Test files name both `Monic` and `injective` besides this work's two, including the generic `Theory/Concrete/Morphisms.v` `concrete_injective_monic`, none applying because `SVariety E` has no `Concrete` instance; a first draft of the report said only `Sets` and `Grp` had one, which an audit refuted): `svariety_injective_monic` from `Sub`'s pointwise hom-setoid, and `svariety_monic_injective` from a PROBE — the free algebra on one generator, `Free_Variety E unit_setoid_object`, separated by `Free.v`'s `free_alg_hom_unique`, the analogue of `Instance/Sets.v`'s singleton probe; `svariety_monic_iff_injective` is the biconditional and `svariety_iso` the analogue of `Instance/Sets.v`'s `bijective_is_iso`, an injective split-surjective homomorphism being invertible AS AN ALGEBRA MAP. `SubGen` is the subalgebra generated by the image of `h : X ~{Sets}~> SVariety_Forget E A`, an INDUCTIVE closure rather than the impredicative intersection of all containing subalgebras (that intersection quantifies over `SubAlg E A`, whose `sa_mem` field lands in the universe the predicate would have to inhabit — a design reason stated, not a proved impossibility). `sgen_factors` factors h through it at `reflexivity`, `sgen_least` is the leastness clause by recursion on the derivation, and `variety_spanning : Spanning (SVariety_Forget E) (sgen_corestrict h)` is Mac Lane's Definition 6 for the CORESTRICTION — proved DIRECTLY, because `SVariety` has no wide pullbacks in tree (files other than this one naming `HasPullbacks`/`HasWidePullbacks` and also naming `SVariety`: zero — a count already stale at #450's parent, where `Adjunction/SpanningArrow.v` and `Test/ProbeVarietySpanning448.v` name both; and since #450 `SVariety E` HAS binary pullbacks, `FinitelyComplete_HasPullbacks (Complete_FinitelyComplete (SVariety_Complete E))` accepted and closed as `Adjunction/SpanningArrow.v`'s correction records, while wide pullbacks remain absent) (CORRECTION, #451: they are no longer absent. `Structure/Pullback/Wide/Complete.v`'s `Complete_HasWidePullbacks (SVariety_Complete E)` inhabits `HasWidePullbacks (SVariety E)`, closed under the global context — measured in a scratch file, as this file's correction says; no library file defines a constant that is that term, and the only ones in tree are `Test/ProbeWellPowered451.v`'s controls `p451_svariety_hwp` and `p451_n22_hwp_by_search_both`, both closed by the same measure; the route through `spanning_solution_set` stays closed for a universe reason, that inhabitant putting the class's second universe at the carrier where `FactoringFamily` needs it strictly above, refused, with `PreservesWidePullbacks` as a section hypothesis, with "universe inconsistency: Cannot enforce <carrier> = <the class's second universe> because <carrier> < ..." as this file's own correction records — that text in the section form only, Rocq reporting the index slot's equation first when the hypothesis is an ordinary binder (the probe's N20) — so "proved DIRECTLY" stands) and so `Theory/Subobject/Lattice.v`'s `sub_wide_intersection` route is closed; the argument is that the image of a subobject through which the corestriction factors is a subalgebra containing every `h x` (`img_subalg`), hence by leastness the whole closure, so the subobject's mono is surjective, injective by monicity, and invertible by `svariety_iso` — which IS `sub_le sub_top m`. The two lattice facts used are TRANSPORTED (`Lattice.v`'s `sub_top_greatest`, `sub_le_antisym`) by `spanning_forces_top` and `sub_le_top_iso`, first proved here under a "CANDIDATE FOR Adjunction/SpanningArrow.v" note and lifted into that file at integration, where `variety_spanning_forces_top` consumes `spanning_forces_top` (`sub_le_top_iso` is stated and not yet consumed). `variety_solution_set X : SolutionSet (SVariety_Forget E) X` (`Adjunction/GAFT.v`) has `sol_index := SpanningArrowsOutOf (SVariety_Forget E) X`, and three `Example`s read that back at `eq_refl` — the index, the object map, and that the member covering a given h IS `variety_spanning_arrow` — so the claim is not that a solution set exists but that it is Mac Lane's. **NOT DELIVERED: the CARDINALITY BOUND.** Mac Lane bounds the generated subalgebra by the terms over X, which is what makes the spanning arrows range over a SET; the tree has no cardinal arithmetic and `Instance/Comp.v` gives arities arbitrary types, so there is no finitary hypothesis to count with (`Instance/Variety/Free.v` says so already). What replaces it is weaker and named as such: `sgen_covered_by_terms`/`sgen_of_term`/`sgen_free_cover_surjective` make the generated subalgebra a split-surjective quotient of the term algebra on X — the shape of the counting argument with none of the counting. No smallness of `sol_index` is claimed; `About` puts it one universe above the carrier (`u3 < u6`), a strict bound owned by `SolutionSet`'s own `sol_index : Type` field, not introduced here (that readback predates #450, when the type read `SolutionSet@{u6 u6 u6 u3}`; re-read by `About` since #450 split the universes with `soa_prop`, `variety_solution_set` returns `SolutionSet@{u8 u9 u8 u3}` with `u3 < u5`, `u5 <= u8` and `u3 <= u6` but no `u3 < u6`, so the index still sits strictly above the carrier `u3`, through `u5`). Also not delivered: no leastness in the SUBOBJECT order (`sgen_least` is on membership predicates; `sub_le (sgen_subobj h) (sa_subobj B)` is not stated); no `HasWidePullbacks (SVariety E)` and so no route through the general Lemma (probe P1 pins the missing instance); (CORRECTION, #451: what is missing is an instance that class search can find, not an inhabitant — `Complete_HasWidePullbacks (SVariety_Complete E)` is closed, but search resolves the class only with `Complete_HasWidePullbacks` registered as an instance AND `SVariety_Complete` as a hint, `Complete` being a plain definition, either registration alone leaving the hole refused, and the tree registers neither outside the probe's local section (`Test/ProbeWellPowered451.v`'s N22 and its control `SearchBoth`) — so P1, an INSTANCE refusal, still stands, and "no route through the general Lemma" stands for the universe reason in the correction above); no left adjoint rebuilt from this solution set (an earlier revision gave the reason "`Free.v` has one directly, and `Free.v` says why GAFT is not the route"; since #450 `GAFT` DOES rebuild the free algebra, `Instance/Variety/Colimit.v`'s `Free_Variety_via_GAFT`, from a congruence-indexed family, and THIS family is refused by `GAFT` even with `SVariety_Complete` and `SVariety_Forget_continuous` as the other arguments — "universe inconsistency: Cannot enforce VS.103 = VS.160 because VS.160 < VS.115 <= VS.103", a scratch file's labels, quoted in this file's header — its index being a Σ over objects); no uniqueness of the factorization. **Measured**: 45 `.glob` heads plus 17 `Program` obligations (nine of them appearing in `strings … .vo` only under a junk digit prefix, stripped not dropped; `Print Module` cross-checks the count), `Build_SubAlg` and the four `SubGen` eliminators — 67 `Print Assumptions`, 67 "Closed under the global context", NO `functional_extensionality_dep` anywhere, because nothing here builds an `EqSignature` (the place `Instance/Variety.v`'s satellites pay #440's axiom). 4 `Defined`, 23 `Qed` by token, and exactly one `Defined` load-bearing: flipping `variety_solution_set`'s obligation stops `variety_solution_set_covers` at "cannot unify `projT1 (sol_covers (variety_solution_set X) h)` and `variety_spanning_arrow E A X h`". Closure 71 files by iterated `coqdep` NOT counting the file itself (72 with it) — 40 more than `Instance/Variety/Free.v`'s 31, the delta dominated by `Adjunction/GAFT.v`'s own Complete/Limit/Regular stack (a first draft of the report counted 8 new direct `Require`s and called that the delta; the audit measured the closure). No `Set` in the universe block or printed type of any declared head or obligation, the only `Set` in the module being the auto-generated `SubGen_rec` motive — true when written; since #450 every one of the 67 gated names carries a local lower bound `Set < …` (`About` on each, 67 of 67), the cost of `soa_prop`, and none instantiates a universe at `Set`. Zero collisions over all `.glob` (instrument-checked on `sub_le`; `Gen`, `gen_incl` and `gen_least` were found taken and renamed `SubGen`/`sgen_base`/`sgen_least`; the file's `Section` was renamed away from the constant name `Spanning` at integration so that a head sweep stays clean). Boundaries in `Test/ProbeVarietySpanning448.v` (P1 no `HasWidePullbacks (SVariety CommEq)`, INSTANCE; P2 h itself is not the spanning arrow, only its corestriction is; P3 the generated subalgebra is not A; P5 the index is not the generating set — all CONVERSION; P4 the eq_refl control).
- **Instance/Variety/Free.v**: Mac Lane §V.6 (book p. 124) — **the free-algebra adjunction for a variety, and it is AXIOM-FREE**. **AN EARLIER REVISION OF THIS BULLET JUSTIFIED THE DESIGN WITH A CLAIM THAT IS FALSE**, and the correction is the most useful thing here. It said the free object CANNOT live in #440's Leibniz `Variety E` because Coq has no quotient types. Refuted by construction: the free commutative magma on two generators DOES exist there, built by sorting terms into normal forms under a decidable total order, with its universal arrow packaged through the same `universal_arrow_from_UMP` this file uses; and a cheap in-tree witness of the same point, `free_empty_eq_leibniz`, shows that with no equations the term algebra is already a Leibniz-variety object. What IS true is two-part. (i) The TERM ALGEBRA is not a Leibniz-variety object once the equations bite — `free_magma_not_comm_leibniz` proves `x*y` and `y*x` are different terms, via a leftmost-generator measure so the refutation needs no projection out of `node`'s dependent argument (an earlier revision justified that measure by an `Eqdep` risk in the injection route, which is also false HERE, since both sides carry the same literal operation — the measure is kept because it stays robust when they do not). (ii) Making it land requires a computable NORMAL FORM per presentation, i.e. solving that presentation's word problem, which is not available uniformly. **So the setoid carrier buys UNIFORMITY in ⟨Ω,E⟩, not existence** — `Free_Variety` is defined for every `E` with no decidability hypothesis. The setoid variety sits beside the Leibniz one in deliberately the same `Sub` shape (`SetoidAlgs`, `ssatisfies`, `SVariety`); `SVariety_Forget` carries the algebra's own setoid where #440's lands on the discrete one, and that too buys uniformity rather than existence (an earlier revision said it was "what lets a free object exist over it"). **The congruence is axiom-free, and #440's header said it could not be**: `tree_equiv` takes symmetry and transitivity as CONSTRUCTORS rather than theorems, so no inversion is ever performed and the `Eqdep`/`JMeq_eq` hazard that `Instance/Variety/Clone.v` feared does not arise; `tc_refl` is derived by plain induction, and that file's not-delivered block is corrected in place to say so. An earlier revision said here "It must be `Type`-valued, since `Setoid`'s `equiv` is a crelation"; since #450 it is `Prop`-valued (`About tree_equiv` ends in `→ Prop`, and `tree_equiv_rect`/`tree_equiv_rec` no longer exist) — the crelation field accepts a `Prop` relation by cumulativity, and `free_alg_ext_respects` now goes through the target's `pequiv_to` — which is what makes the congruence indexes of `Instance/Variety/Colimit.v` carrier-sized. **THE HEADLINE MEASUREMENT**: `Free_Variety_adjunction : Free_Variety_Functor E ⊣ SVariety_Forget E` is **Closed under the global context**, as is the whole general theory — and #440's `GroupVariety` is not. The two are consistent and the reason is exact: the equations enter here ONLY through `EqSignature`'s `lhs_natural`/`rhs_natural` (an earlier revision said "consumed in one place, `free_alg_ext_respects`'s `tc_law` case", already false then — `soa_prod_satisfies` consumes them too — and since #450 `Instance/Variety/Limit.v`'s `svlim_satisfies` and `Instance/Variety/Colimit.v`'s `vker_cong` also do), and every construction CONSUMES those fields, so #440's `functional_extensionality_dep` is the price of BUILDING an `EqSignature`, not of using one. Exactly four constants carry the axiom, all by instantiating at `GroupEq` or by construction — `SGroupVariety`, `FreeGroupOn`, `FreeGroup_adjunction`, `leibniz_soa` — and they are not gated. The adjunction is packaged through `universal_arrow_from_UMP` and `AdjunctionFromUniversalArrows`, the same two steps `Construction/Free/Quiver.v` uses; **GAFT was considered and loses on four counts**, of which the `Set` universe pin is the LEAST — it would need `Complete (SVariety E)`, `PreservesImageLimit` for the forgetful functor, and a `SolutionSet` with §V.7's cardinality bound, which does not exist constructively here at all because `Instance/Comp.v` makes arities ARBITRARY TYPES rather than finite ones. **CORRECTION (#450): none of the four counts stands** — `Instance/Variety/Limit.v` supplies `SVariety_Complete` and `SVariety_Forget_continuous`, a solution set needs a carrier-universe index rather than a cardinality bound (`Instance/Variety/Colimit.v`'s `SVariety_Forget_solution_set`, the `Prop`-valued congruences on the term algebra), and the `Set` pin went with the PR "algebraic carriers are sets", so `Free_Variety_via_GAFT` is Mac Lane's own route and `Free_Variety_via_GAFT_agrees` identifies it with `Free_Variety_Functor` up to natural isomorphism; the direct construction stays primary because `GAFT` is `Qed`, which leaves no `eq_refl` readback such as `p441_left_adjoint_obj`. **Fields are not a variety** (`fields_are_not_a_variety`), and the proof is NOT the issue's stated motivation that inversion is partial: it is Mac Lane's own, that varieties are closed under products and fields are not, via `soa_prod_satisfies` and `F2 × F2`. That theorem ships in TWO strengths for a reason an audit exposed: a first revision proved it of a predicate imposing only the DIVISION property — no ring axioms, no `1 ≉ 0` — which the one-element algebra and a unit-less "ring" both satisfy, so no `E` whose models are exactly the fields could satisfy that theorem's soundness hypothesis and the theorem said nothing about such an `E`. That weaker statement now ships under the name it deserves, `division_is_not_equationally_definable`, and `fields_are_not_a_variety` is restated over an `IsField` carrying the ring axioms, additive inverses (a Σ-clause, since `RingOp` has no negation), `1 ≉ 0` and division. Since #450 the setoid algebras carry `soa_prop`, so `soa_prod_satisfies` ranges over propositional algebras only, `not_equationally_definable` is incomparable with its earlier form, and the two non-examples, whose witness `F2` is propositional, are weakly STRONGER (Free.v's section "The carrier's `≈` is a proposition (#450)"). `soa_prod` is the componentwise construction only — two projections, no pairing map, no uniqueness — so it is NOT established to be the categorical product, and the name is descriptive; closure under it is all the non-example needs. Measured: 71 `.glob` heads plus 22 `Program` obligations, of which **89 are Closed under the global context and all 89 are gated** (7456 → 7545, axiom audit passing; since #450 the gate names 91 constants of this file, `soa_prop` and `Build_SetoidOpAlgebra` added, all closed, by `grep -o 'Print Assumptions Category\.Instance\.Variety\.Free\.[A-Za-z0-9_]*' Makefile | sort -u | wc -l`, 89 at the parent); four `Defined`, exactly ONE load-bearing (`FreeUA`, whose flip to `Qed` stops the probe's `p441_left_adjoint_obj` readback). NOT delivered: no `Free_Variety : Variety E` — and note the corrected reason, that it exists whenever the presentation has a computable normal form but not uniformly, the 250-line normal-form construction for the one presentation not being shipped; no functor `Variety E ⟶ SVariety E`, `leibniz_soa` being an object map only; no general non-collapse statement, the free algebra being legitimately a single `≈`-class for an `E` whose laws identify everything; no Birkhoff HSP theorem — the non-example is stated over a FIXED signature and rules out every `EqSignature RingOp` whose models are exactly the fields, no more; no cardinality bound (an earlier revision continued "and so no GAFT route"; since #450 the route exists without the bound, above); and no free ring, free abelian group or free module as named instances, the two shipped instances being the ones the issue's own Verification block runs
- **Instance/Matr.v**: Mac Lane §I.2's matrix category, stated over a RIG per the issue's QA corrections (the category laws never subtract): `Matr (R : RigObject)` with objects the naturals and an arrow `n ~> m` an m × n matrix `Fin.t m → Fin.t n → carrier R` (Mac Lane's orientation, documented), composition the matrix product, identity the Kronecker `delta` (spending `Fin.eq_dec`), morphism equality entrywise. The engine is a reusable rig-parameterized finite-sum library — `fin_sum` with congruence, additivity, exchange (`fin_sum_swap`) spending only the additive half (`rig_cmon`; a monoid-level refounding is disclosed-deferred), bilinearity (`fin_sum_mul_l/r`, where distributivity and annihilation are spent), and the Kronecker collapses (`fin_sum_delta_l/r`) — kept general for the GL_n/determinant issue. `Matr_N := Matr Nat_Rig` is Awodey §1.4's category of ℕ-matrices with computing `eq_refl` examples. The transpose self-duality (Mac Lane §II.3 Ex 4, Riehl 1.3.14(v)): for a COMMUTATIVE rig — exactly where (AB)ᵀ = BᵀAᵀ spends commutation — `Matr_Transpose` is an identity-on-objects functor to the opposite, involutive on the nose, packaged as the isomorphism of categories `Matr_transpose_iso : Matr R ≅[Cat] (Matr R)^op`, instantiated at ℕ (`Matr_N_transpose_iso`) and at Mac Lane's own commutative-ring headline ℤ (`Matr_Z`, `Matr_Z_transpose_iso`), with the one-element-rig collapse sanity (`Matr_zero_collapse`); over a non-commutative rig the transpose lands in matrices over the opposite rig, noted and deferred. Instance/Matr/Yoneda.v is Riehl Corollary 2.2.10 made categorical: a row operation IS a natural endomorphism of the represented presheaf `[Hom ─,n]` (naturality = commuting with right multiplication, both directions stated), and `row_operations_are_left_multiplication` derives — purely from `Yoneda_Embedding`'s round trips, no entry ever inspected — that every row operation is left multiplication by the unique matrix obtained by applying it to the identity (`representing_matrix_at_id`, `representing_matrix_unique`), with the two-row swap as the computing witness (its representing matrix is the permutation matrix by `eq_refl` over ℕ)
- **Instance/Matr/Elimination.v**, **Instance/Matr/Coequalizer.v**: Mac Lane §III.3 Exercise 3 — coequalizers in the matrix category, delivered at FULL strength: **`Matr_HasCoequalizers`** inhabits `HasCoequalizers (Matr K)` for EVERY parallel pair over ANY field with a zero decider, built on a genuine Gaussian-elimination engine rather than an interface. `Instance/Matr/Elimination.v` is that engine: `LeftNullBasis` (`lnb_dim`/`lnb_basis`/`lnb_annih`/`lnb_univ`) with `left_null_basis` inhabited for every matrix, by recursion on COLUMNS (pivot search, then the basis `e_{d r} − (c_{d r}/c_p)·e_p` indexed through `Determinant.v`'s `fin_delete`), plus the pre-shaped `left_null_basis_diff` and the subtraction kit `mat_sub`/`mat_mul_sub_r`/`mat_sub_zero`/`mat_mul_sub_zero_iff`. **THE DECIDABILITY HYPOTHESIS IS FORCED RATHER THAN CHOSEN — argued, not proved**: `FieldObject` (Instance/FdVect.v) carries NO decider — `finv` is TOTAL and `finv_l` is GUARDED by `x ≉ 0` — and elimination must DECIDE whether a pivot is zero, so a zero-decider is carried as explicit data on the `Instance/Field.v` `field_dec_stable` precedent. Correspondingly **a rig will not do**: `Matr R` is over a `RigObject`, which has no subtraction, so the cokernel-of-a-difference framing needs the RING layer's `ring_neg` and nothing here applies to `Matr_N`. **The `Qed`/`Defined` distinction is load-bearing twice over, and both were found by REFUTED probes rather than anticipated.** `Instance/Field.v`'s `F2_Field_dec` and `Q_Field_dec` are `Qed` lemmas: they inhabit the hypothesis perfectly and the engine is total and correct when fed them, but nothing REDUCES through an opaque constant, so the coequalizer object does not compute — transparent copies (`f2_dec`, `q_dec`) are used for every `eq_refl` witness, and `Instance/Field.v` is deliberately NOT modified. Dually, keeping `matr_coeq_desc` a transparent `Definition` rather than a `Qed` lemma is what keeps **the MEDIATOR computing** (`unique_obj (coeq_desc …) F1 F1 = true` by `eq_refl`). The stage handoff is by PURE CONVERSION — all twelve section constants of Coequalizer.v are `:=` bodies with no tactic, no `change` and no transport, consuming `Matr K`'s own `∘` and `≈[Matr K]`; the only friction was imports, `Structure/Coequalizer.v` not re-exporting `Coequalizer`/`APair`/`^op`. Orientation pinned by `eq_refl` (`matrix_is_hom`, `mat_mul_is_compose` with object order (c,b,a), `mat_id_is_id`, `mat_equiv_is_homset_equiv`) — and read the `#[local] Instance Matrix_Setoid` correctly: an earlier draft of this bullet said that last Example NEEDS it, which an audit REFUTED by experiment. The two setoids are CONVERTIBLE, so the Examples close by `eq_refl` either way and deleting the instance outright leaves the file compiling; what it buys is that the two `≈`s are the same TERM rather than merely convertible ones. **Non-vacuity is proved four ways, not asserted**: over F₂ with `A = (1,0)ᵀ`, `B = (0,1)ᵀ` the object is `k = 1` at `m = 2` — neither 0 nor m — with `coeq_f2_obj_not_zero`/`_not_two` by `discriminate` off the computation, `coeq_f2_pair_distinct`, and the sharpest, `coeq_f2_map_not_monic` paired with `coeq_f2_map_epic`: the coequalizing map identifies two provably inequivalent arrows, so it is a PROPER QUOTIENT and not an isomorphism in disguise. A second non-degenerate point `k = 2` at `m = 3`, degenerate controls at `k = m` and `k = 0`, and a ℚ witness where `finv` actually divides (`E = (−2, 1)`). Also `matr_coeq_epic`, `matr_coeq_iso`, `matr_coeq_colimit`, and `Matr_op_HasEqualizers` reading the result through #326's `IsEqualizer_op_of_IsCoequalizer`. SCOPE: **no matrix rank function exists in either of these two files** (the tree has a `rank` and seven `*_rank` constants elsewhere, none of them a matrix rank), so `k = m − rank M` is NOT claimed; `k`'s independence of the construction is NOT claimed as a NUMBER (`matr_coeq_iso` gives only `k ≅ k'` in `Matr K`); no echelon or normal form (the three refuted map-normalization probes are three faces of that); no right null space, so **equalizers in `Matr K` itself are NOT delivered** — only `HasEqualizers ((Matr K)^op)`, which the header labels a reread rather than a new theorem; and nothing applies over a bare rig. `Matr_HasCoequalizers` is a plain `Definition`, NOT a registered `Instance`, since resolution can never produce a decider for an abstract field. `Test/ProbeMatrCoeq.v` guards the boundary with 7 conversion negatives plus an instrument check, rename-simulated over the constants appearing in the NEGATIVES — a check that found FIVE unguarded on its first run, and which an independent audit re-ran at 19/19 (my own pass covered 18 names; the audit added one more)
- **Instance/Matr/FunExercises.v**: Matrix equivalence and similarity via functor categories (Mac Lane §II.4 Exercise 6, p. 42 — NOT #244's §I.4 ex 6) — both characterizations as one-line instantiations of GENERAL categorical lemmas: `arrow_iso_iff` (an iso of `Arrow C` IS a pair of component isos with the commuting square — forward pure projection, the one genuine step being the BACKWARD square for the inverse pair, `iso_square_from`) gives **`matrix_equivalence_iso`** at an ARBITRARY rig (iso in `Arrow (Matr R)` ⟺ invertible P, Q with `B ∘ P ≈ Q ∘ A`; classical `B ≈ Q A P⁻¹` as `matrix_equivalence_classical`), with Mac Lane's literal `[_2]` reading delivered by transport along Shapes.v's round trips (`two_fun_iso_iff` for arbitrary functors; `matrix_equivalence_iso_Fun` at Walk-shaped ones, every `[_2]`-object being a `Walk` by `Walk_arrow_of_iso`) — the `[_2]` side pins only R's FIRST TWO universes to `Set` (the corollary lives in its own `RigObject@{Set Set q}` section, the third universe measured free by the fess audit), which is why the general theorem lives on the Arrow side; and `endo_iso_iff` (functors out of `Deloop Nat_Plus` are single square matrices by (ℕ,+)-freeness — `mat_pow` with `h k ≈ A^k` by induction, naturality collapsing to the GENERATOR square k=1) gives **`matrix_similarity_iso`** (iso in `[Deloop Nat_Plus, Matr R]` ⟺ invertible P with `P ∘ A ≈ B ∘ P`; classical conjugation form alongside) at `RigObject@{Set Set Set}` — the pin forced by Deloop through three separate mechanisms (MonHom, Functor_Setoid, Fun each identify universe instances), disclosed, with all three in-tree rigs checked to inhabit it. The powers development is stated for an ARBITRARY category — nothing in it is about matrices — with `pow_intertwine` and `similar_to_id` hoisted OUTSIDE the pinned section so they carry no needless `Set` pin (fess-measured); `pow_hom`/`functor_of_endo`/`endo_iso_iff` keep the pin their `Deloop`/`MonHom` content forces. Witnesses SEPARATE the relations: diag(1,0) ~ diag(0,1) similar via the transposition (isos exhibited in all three ambient categories); id₂ vs the shear `[[1,1],[0,1]]` EQUIVALENT but NOT SIMILAR (`equivalence_is_weaker_than_similarity`, riding the general `similar_to_id`: an endomorphism conjugate to an identity IS an identity); `zmat_id_not_similar_zero` the cheap 1×1 negative. Determinant.v/GL.v cited, not required (the 1600-line build not spent). All 62 top-level constants Closed (70 with the Program obligations, each measured)
- **Instance/Rng.v**: Mac Lane §I.7's Rng — the category of unital rings — over #839's rig spine: `Rng := Ring` definitionally (a `RingObject` IS a unital ring; `RigHom`s preserve 0/+/1/· with unit preservation a field, negation preservation the theorem `RigHom_neg`; the name per the QA audit: `Rng` unital here, future #362's `Rg` non-unital, `Ring` stays the set-level class). Content: forgetful functors to `Ab` (via `ring_ab`, riding Instance/Ab.v's derived-negation design) and to `Sets`; the zero ring terminal (`Rng_Terminal_zero`); ℤ initial (`Rng_Initial_Z` — `zring` by sign case analysis over `rig_iter`, verified through a hand-rolled Type-valued `Z_peano_rect` assembled from `Pos.peano_rect` with `eq_rect` transports, since stdlib's `Z.peano_ind` eliminates only into Prop); the commutative full subcategory `CRng` (witness: ℤ); injections monic and surjections epi (the converse of the former needed the free ring ℤ[x], and is now proved — `rng_monic_iff_injective`, Instance/Rng/Polynomial.v); and Mac Lane §I.5 Exercise 4, `ZtoQ_epi_not_surjective`: the inclusion ℤ → ℚ (stdlib QArith under `Qeq`) is an epimorphism — a hom out of ℚ is determined on ℤ because g(1/b) is THE two-sided inverse of g(b) (`rig_inv_unique`), no fraction chosen, only cancelled — yet not surjective (1/2 has no preimage), the standard witness that epis need not be onto; all axiom-free including the QArith layer
- **Instance/Rng/Algebras.v**: Mac Lane §II.6 Ex 1 — commutative K-algebras ARE the objects under K in commutative rings, and the identification is delivered at the strength Mac Lane's "exactly" asks for: **`KAlg_Coslice_strict_iso : KAlg K ≅[StrictCat] Coslice CRng K`** is a genuine ISOMORPHISM OF CATEGORIES (object equality at Leibniz `=` plus transported agreement of the arrow actions), with the pinned `KAlg_Coslice_iso : ≅[Cat]` DERIVED from it through `strict_equiv_implies_fun_equiv` and labelled the weaker equivalence reading, and `KAlg_Comma_iso : KAlg K ≅[Cat] (=(K) ↓ Id)` composing with `Comma_Coslice` (at Cat strength only, that being where the donor is stated). The definition is deliberately ELEMENTARY — `KAlgObject` is a record (bare `RingObject` + commutativity + a `RigHom` out of K), NOT a CRng object with a CRng morphism — so the comparison genuinely assembles and dismantles a `Sub` membership witness and two sigmas rather than being true by construction; `kalg_comm_is_CRng_membership` records that the commutativity field IS `CRng_Sub`'s predicate, its proof term being the field itself, which is what lands the comparison in `Coslice CRng K` rather than `Coslice Rng (`1 K)`. The round trips are ASYMMETRIC and the asymmetry is measured, not guessed: KAlg→Coslice→KAlg is the identity on objects DEFINITIONALLY (`eq_refl`, primitive projections giving `KAlgObject` eta) while Coslice→KAlg→Coslice is Leibniz-provable but `eq_refl`-REJECTED (`sigT` twice plus `CRng_Sub`'s `True` have no definitional eta) — which blocks nothing, case-splitting first making the transports vanish. Witnesses: `Int_KAlg` (ℤ over ℤ), `Q_CRng`/`Q_KAlg` (ℚ over ℤ via the pre-existing `ZtoQ` — a genuinely non-identity structure map, with `Q_KAlg_unit_not_surjective` re-exported, and NO Frac.v dependency), `Int_to_Q_KAlg`, and `Z_KAlg` (every commutative ring is a ℤ-algebra) with `Z_KAlg_unit_unique` from ℤ's initiality — the general "coslice under an initial object ≅ the category" is NOT claimed, only the pointwise uniqueness. All 35 NAMED constants closed under the global context (47 with the twelve Program obligations, each measured — the house's FunExercises.v convention); universes measured per named constant, with the `Set` instances on six of them attributed to their donors (`ZtoQ`, `rng_from_Z`), the hom/proof identification to `Rng`'s own shape, and the three-parameter collapse at either isomorphism to the cost of stating an `Isomorphism` in ANY category of categories — it is already present at the STRICT form, so `StrictCat` does not keep those universes apart. Deferred and disclosed: the module-theoretic definition via `RModObject` (which this coslice definition sidesteps — no equivalence with a module category is claimed), and smallness. **The non-commutative deferral this file's header names — "there the structure map is required to land in the CENTRE … No centre-valued variant is built here" — is DISCHARGED** by `Instance/Rng/Algebras/Associative.v`'s `AAlg K`, whose `aalg_central` field IS that condition
- **Instance/Rng/Frac.v**: Mac Lane §I.3 Exercise 1 — the field of quotients as a functor, with the exercise's real content being the DOMAIN-CATEGORY CHOICE: a hom sending a nonzero d to 0 supports no map on fractions, so `IntDom` takes integral domains with INJECTIVE ring homs (`DomHom`), the standard monomorphisms fix — injective ⇒ monic in Rng is `rng_injective_monic`, machine-checked per-hom here as `DomHom_monic`, the converse now proved as `rng_monic_injective` (Instance/Rng/Polynomial.v), so injectivity being carried as data is a design choice rather than a necessity. `DomObject` is constructive: commutativity + 1 ≉ 0 + CANCELLATION as the defining field (exactly what the construction consumes; the classical no-zero-divisors disjunction gives it via the `DomObject_of_no_zero_divisors` smart constructor, and `dom_mul_nonzero` — nonzero elements multiply to nonzero — follows from cancellation alone; the reverse implication would need double-negation elimination, so cancellation is primitive). `FracRing D`: pairs (n, d ≉ 0) under cross-multiplication `frac_eq` (transitivity IS cancellation), full commutative-ring laws by deterministic transitivity chains over a shuffle kit, the fraction pairs built through the explicit-`existT` smart constructor `mk_frac` (the 8.19/8.20-safe form; nix builds for 9.1, 8.19, and 8.20 all run locally); `Frac : IntDom ⟶ CRng` acts componentwise; `IntDom_Incl : IntDom ⟶ CRng` with faithfulness proved (`IntDom_Incl_Faithful`); non-fullness is expected (ℤ → ℤ/2ℤ) but left unstated — no quotient rings in tree yet for the witness. Field property constructively: `frac_recip`/`frac_nonzero_recip` (numerator apart from zero ⇒ reciprocal, apartness from the zero fraction = numerator apartness); embedding `frac_embed` n ↦ n/1 injective — the universal-arrow unit, full UP awaiting a Fields category. The Lie-algebra half of the exercise is descoped in the header (smooth manifolds out of scope). Witness ℤ (`Int_Dom` via `Z.mul_cancel_r`): 1/2 + 1/3 ≈ 5/6 computes by `eq_refl` (cross-multiplication 30 = 30)
- **Instance/Rng/Polynomial.v**: Mac Lane §III.1 Exercise 7 + Awodey §9.3 Example 9.10 + Riehl §2.1 Example 2.1.5(iv,v) and §2.3 Example 2.3.4 — the polynomial ring as a universal arrow, with `Instance/Rng/Pointed.v` and `Instance/Mod/Representable.v`. `PolyRing K` is built by generators and relations (the Instance/Ab/Tensor.v idiom) over any `RingObject`, with `poly_extend`/`poly_extend_unique`, and Ex 7 lands directly in the PRE-EXISTING `Instance/Rng/Algebras.v` — `poly_universal_element : AUniversalElement (KAlg_Forget K) (PolyAlg K)`, with `poly_representation`, `poly_representable` and both Arrow encodings, so no coslice needed building. **`Instance/Rng.v`'s standing deferral is DISCHARGED**: `rng_monic_injective` and the biconditional `rng_monic_iff_injective` are proved axiom-free, two elements of R being separated by the two homomorphisms out of ℤ[x] sending x to them — and it holds over ALL of `Rng`, not merely `CRng`, because `zring_central` (the integers are central in every unital ring) discharges the commutation for an arbitrary possibly-non-commutative target. That deferral was cited in four `.v` headers (`Instance/Rng.v`, `Instance/Field.v`, `Instance/Rng/Frac.v`, `Instance/Field/Frac.v`) and in two bullets of this file, and all six are corrected; the two `Frac` files keep injectivity as DATA in `DomHom` and only the stated REASON changed — it is now a design choice rather than a necessity. **Awodey's adjunction is stated over COMMUTATIVE rings and the restriction is PROVED rather than assumed**: `poly_hom_value_central` shows x is central in K[x], so its image under any homomorphism commutes with the image of the coefficients, which is exactly why `Rng` will not do — the right object there is a free algebra on a non-commuting generator, named in the header and not attempted. `CRngPt := Elements CRng_Forget` gets pointed objects for free as the category of elements (no new category machinery), with `poly_pointed_adjunction : PolyPointed ⊣ CRngPt_Forget` and evaluation as the transpose in two forms. Riehl's representability boxes are delivered on BOTH sides as natural isomorphisms — `rmod_representation : [Hom R,─] ≅ RMod_Forget R` in `[RMod R, Sets]`, its representing object `Ring_RMod R` on the nose and proved directly rather than through formal sums, and `zpoly_representation : [Hom ℤ[x],─] ≅ Rng_Forget` in `[Rng, Sets]` — with `zpoly_universal_element`'s `aue_elem = poly_x` by `eq_refl` naming the indeterminate as a first-class universal element, and `free_one_generator_iso : FreeModObject SetsOne ≅ Ring_RMod R` WITH its uniqueness clause tying the new representation to #305's free module. Strengths measured strict-first, and **two strict attempts were made and REFUTED**, both pinned as `Fail` probes: preservation of ZERO and ONE by `poly_extend` is NOT definitional (`rig_zero (PolyRing K)` is `pt_const (rig_zero K)`, so the fold returns `phi (rig_zero K)` and closing needs `phi`'s own law) — and the probe is stated at a VARIABLE ring precisely because at ℤ with the initial map it DOES close by `eq_refl`, the counter-behaviour recorded beside it; and `poly_pointed_unit A = poly_pointed_arrow A` is rejected, only the pointwise action being `eq_refl`, which corrected a header sentence claiming the unit IS the coefficient inclusion. What does close strictly: `poly_extend_const`/`poly_extend_x`, preservation of + and ·, and evaluation COMPUTING over ℤ (x²+2x+1 at 3 = 16, at 0 = 1, at −1 = 0). Non-vacuity is general rather than ℤ-only — `poly_const_injective` (K ↪ K[x] for every commutative K) and `poly_x_not_constant` (x is no constant whenever 1 ≉ 0) are proved by evaluating in K itself, so neither needs a normal form. With this, Mac Lane's §III.1 roster of free constructions is COMPLETE in tree, recorded by path rather than issue number: free category (`Construction/Free/Quiver.v` — note is `UnitQuiverCatAdjunction`, an error the issue itself carried), free monoid (`Instance/Coq/Monoid/Free.v`), free group (`Instance/Grp/Free.v`), free module (`Instance/Mod/Free.v`), polynomial ring here. Deferred and disclosed: no degree, no normal form, no coefficient uniqueness, hence no equality decision procedure and no division algorithm; no multivariate K[x,y]; no proof that K[x] is a domain when K is, so no link to `IntDom`; and the kernel of `pt_const` over a non-commutative K is NOT claimed to be precisely the commutator ideal — only the forcing direction is available, and it is immediate from the two constructors rather than stated as a lemma; the converse was cut back when it could not be proved
- **Instance/Rng/Zp.v**: Mac Lane §V.1 construction 2 with Exercises 6 and 7 (book pp. 110-112, `maclane:V.1:construction2`, `maclane:V.1:ex6`, `maclane:V.1:ex7`) + Riehl §3.1 Example 3.1.22 (printed p. 88, `riehl:3.1:example22`) and §5.6 Example 5.6.8 (printed p. 211, `riehl:5.6:example8`) — **the p-adic integers as the limit of the residue tower, their digit expansion with schoolbook carrying, and the formal power series as the SAME construction at a different generator**: `Zp_limit : IsALimit (ResTower Rcomm d) Zp`, `Zp_digits_iso` and `PowerSeries_limit`, the issue's three pinned Verification names, delivered verbatim. **PRIMALITY IS NEVER USED, AND THAT IS A STRENGTHENING MEASURED AT THE SIGNATURES**: sections (A)-(C) quantify over an ARBITRARY commutative ring and an ARBITRARY element `d`, section (E) over an arbitrary integer `p > 1`, and no `prime` HYPOTHESIS occurs anywhere in the file, the word appearing only in prose — Mac Lane's ring of p-adic integers is the special case at a prime, and the tower `R/(d) ← R/(d²) ← …` is built for every `d`. **THE ISSUE'S "Current state" IS STALE SEVERAL TIMES OVER AND THE HEADER SAYS SO**: it reports `rg -w 'Rng|CRing|RingObject'` → 0 hits and "no category of rings in the tree at all", whereas `Instance/Rng.v` declares `Rng : Category`, the commutative full subcategory `CRng`, and `Instance/Rng/Quotient.v` carries the entire quotient apparatus — the five-field `Record Ideal`, `QuotientRing`, `rquot_proj`, and `rquot_med` with `rquot_med_commutes`/`rquot_med_unique`, i.e. the universal property — all consumed except `rquot_med_unique`, which is cited only. **A FIRST DRAFT'S ABSENCE SURVEY WAS WRONG ON BOTH CLAUSES AND THE HEADER NOW SAYS SO**: it claimed no `Z/n` as a `RingObject` and no PRINCIPAL ideal, on a sweep returning `TotalIdeal`, `TrivialIdeal` and `KernelIdeal` “and nothing else”. `QuotientRing` (`Instance/Rng/Quotient.v`) concludes `RingObject`, so `QuotientRing EvenIdeal` IS ℤ/2 and `QuotientRing SixIdeal` IS ℤ/6 — unnamed, but with computing examples at (1+1 = 0, 1·1 = 1, 3 ↦ 1) and a residue transition map `Z6_to_Z2` at built through `rquot_med`, which is the closest prior art to `res_proj`; and the sweep for the REQUIRED field `idl_mem :=` — exhaustive by construction, the criterion `Adjunction/Enveloping.v` ALREADY STATES — returns **SIX** `Ideal` inhabitants, not three: those three plus `EvenIdeal` (2ℤ) and `SixIdeal` (6ℤ) in the same file and `StrictUpper` (`Instance/Rng/Quotient/OneSided.v`), of which 2ℤ and 6ℤ ARE principal. The draft read by NAME while claiming to read by shape, and contradicted a sibling header the gate loads into one scope. **What survives is narrower and is what this file adds**: all six inhabitants are CLOSED-FORM membership predicates, so no ideal is GENERATED by an element and no PARAMETRIC principal-ideal constructor exists — `Adjunction/Enveloping.v` says the same in its own words — which is exactly what `PrincipalIdeal` is; and there is no residue TOWER anywhere, nothing building `dⁿ` as a family, relating `R/dⁿ⁺¹` to `R/dⁿ`, or taking a limit, `Z6_to_Z2` being one map at one pair, built by hand. **COMMUTATIVITY IS A BARE PREDICATE AND IS SPENT AT EXACTLY TWO SITES, ONLY ONE OF WHICH IS A FIELD — AND A FIRST DRAFT SAID “ONLY” THE FIELD**: `RingComm R` is `Instance/Rng.v`'s `CRng_Sub` predicate taken directly (the `Instance/Mod/Monoidal.v` idiom, avoiding a sigma unpack at every use site; `ring_comm_is_CRng_predicate` records the agreement AGAINST `CRng_Sub` ITSELF — its first form compared `RingComm` with its own body, an `X = X` after delta that named `CRng_Sub` nowhere and so could not have broken if that predicate changed), and it is `rewrite`-consumed by `PrincipalIdeal`'s `idl_absorb_l` obligation — the left absorption `r·(d·k) ≈ d·(r·k)` needs the two factors exchanged where the right one is associativity alone, so a principal LEFT ideal needs no commutativity and a two-sided one does, which is why the hypothesis sits on the ideal rather than on the file — AND AGAIN by the tower's nesting lemma `res_ideal_succ_sub`, which is no field of `Ideal`: `dpow (S n)` is `d · dⁿ`, so the witness `d · k` must move that `d` past `dⁿ`, and deleting that one `rewrite` leaves the goal with no matching subterm (measured by stripping it). That second spend is an ACCOUNTING cost rather than a mathematical one — a power of `d` commutes with `d` in any ring, by induction — but nothing here proves that, so it is real as the file stands. **THE THREE FACTS SECTIONS (A)-(D) LEAVE TO BE MEASURED ARE PINNED AT `eq_refl` AT THE HEAD OF SECTION (E), AND ALL THREE HOLD**: the tower's transition map is the IDENTITY on integers (`digit_transition_is_id`, so the quotient map ℤ/d^(n+1) → ℤ/dⁿ is induced by `id`), `x ≈ y` in the n-th residue ring IS `@divides Int_Ring (dpow p n) (x + -y)` (`digit_equiv_is_divides`), and `divides` is the Type-valued sigma `{ k : Z & b = a * k }` (`digit_divides_is_sigma`) — after which everything in (E) is ordinary integer arithmetic. **RIEHL §5.6's "CREATED" CLAUSE IS DELIVERED AS ITS ELEMENTARY CONCLUSION AND THE HEADER REFUSES TO OVERSELL IT**: `zp_zero_forced`, `zp_one_forced`, `zp_add_forced`, `zp_mul_forced` and `zp_neg_forced` say that an operation on the limit carrier which every projection preserves IS the coordinatewise one — but MONADICITY OF THE UNDERLYING-SET FUNCTOR IS NOT PROVED and is not used, and each lemma is one line precisely because an element of #408's matching-string setoid IS its family of coordinates, so "agrees at every stage" and "equals" are the same statement; the content sits in the choice of carrier, upstream, and these lemmas record the conclusion rather than re-deriving it. **EXERCISE 6 IS DELIVERED IN FULL, AND ITS SHAPE IS STATED RATHER THAN BLURRED**: `Zp_digits_iso` is an isomorphism in **`Sets`** between the carrier of `Zp` and the setoid of digit strings below p — NO ring structure is put on digit strings, so it is NOT a ring isomorphism (pinned as the probe's TYPING negative, `DigitSeq p : obj[Sets]` where `obj[Rng]` is wanted) — and the correspondence of the operations is instead the pair `Zp_digits_add` and `Zp_digits_mul`. Its two legs are read back at `eq_refl` as the NAMED morphisms rather than as lookalikes (`zp_digits_iso_to_leg`, `zp_digits_iso_from_leg`), so the two correspondence theorems are about this bijection and not about something isomorphic to it. The forward map is `zpd f i := (f (S i) mod d^(S i)) / dⁱ`, the backward one the partial sums `dsum`, and the whole round trip turns on ONE invariant, `zpd_dsum : dsum (zpd f) n = f n mod dⁿ` — the digits rebuild the canonical representative — whose step needs exactly `mod_mod_mul` (`(x mod (c·b)) mod b = x mod b`, which the stdlib states only at equal moduli) plus the tower's own compatibility. **CARRYING IS ONE OPERATOR AND BOTH CORRECTNESS THEOREMS RUN THROUGH ONE IDENTITY**: `ncarry`/`dnorm` push carries left through an arbitrary integer sequence, `dadd a b := dnorm (a + b)` and `dmul a b := dnorm (mraw a b)`, and `dnorm_dsum` (`dsum (dnorm c) n + ncarry c n · dⁿ = dsum c n`, the value preserved EXACTLY up to the carry out of position n) is what both spend. **THE CONVOLUTION'S CORRECTNESS IS THE ONE PIECE WITH REAL CONTENT, AND THE INDUCTION IS MADE TO WORK BY A SHIFT**: a congruence mod dⁿ is too weak to carry itself (the middle terms of the product are not visible at that precision), so `mraw` is DEFINED in the peel-the-leading-digit form `mraw a b (S k) = a₀·b_{S k} + mraw (shift a) b k` — making the reindexing `eq_refl` rather than an argument, with `mraw_0`/`mraw_1`/`mraw_2` pinning that it IS the convolution — and `mraw_correct` then inducts on n with the sequences UNIVERSALLY QUANTIFIED, applying its own hypothesis at `(shift a, b)`; the remaining arithmetic is `dsum_shift`, `dsum_add` and `dsum_scal` and one `ring`. **NON-VACUITY IS PROVED AND COMPUTES**: `zpd_neg_one` shows every digit of the constant family at −1 is p−1 — the classical …9999 = −1, with `zp_const_neg_one_is_neg_one` identifying that family with the ring's own additive inverse of 1 — and section (F) evaluates base ten by `eq_refl` throughout (7+5 gives units digit 2 with the carry landing in the tens place, `ncarry` at position 1 is genuinely 1 and not 0, 7·5 gives 5 then 3, and −1 reads 9, 9, 9). **EXERCISE 7 IS AN INSTANTIATION, WHICH IS THE EXERCISE'S OWN POINT, AND ITS REMAINING GAP IS DISCLOSED**: `PolyRing K` is commutative by CONSTRUCTION — `pe_mul_comm` is a constructor of `Instance/Rng/Polynomial.v`'s congruence and that file already packages it as `poly_comm`, CONSUMED not reproved — so `PowerSeriesTower := ResTower ZPolyComm poly_x`, `PowerSeries` and `PowerSeries_limit` are sections (A)-(C) at a different generator with no tactic anywhere, and `padic_and_series_share_the_construction` records that both are the same term; but the limit is NOT shown isomorphic to a ring of coefficient sequences under convolution, so that parenthetical of the issue is UNMET and no such ring is built. **A COLLISION WAS FOUND AND ROUTED AROUND, AND THE FIX WAS TO CONSUME RATHER THAN RENAME**: the obvious `Int_comm` is already `Instance/Matr/Determinant.v`, and `make print-assumptions` loads many modules into one scope — but `Instance/Rng.v`'s `Int_Ring_commutative` has EXACTLY the wanted statement (its binders elaborate at `carrier (rig_setoid Int_Ring)`, so it IS `RingComm Int_Ring`) in a module already required, so the file consumes it under the short local name `ZComm` and adds no third copy of the fact. **Universes measured off BOTH binder and block over all 156 constants**: NO block contains a word-bounded `Set` on any of the file's OWN levels — the five `Set` mentions across the 134 printed blocks are all `Set < Basics.flip.u{0,1,2}`, stdlib levels — and exactly **56** blocks carry an equation, always the same one, `u = u0` and `u = u1`, identifying the ambient ring's carrier, relation and proof universes. **THE DONOR IS `Rng` ITSELF, NOT `RKills`, AND A FIRST DRAFT SAID OTHERWISE**: `Instance/Rng.v`'s `Rng := Ring` has `obj` at `RingObject@{u u u}`, so at a ring whose three levels are declared strictly apart `R : obj[Rng]` ALONE is refused — “Cannot enforce rr = ro because ro < rr”, no `RKills` in the command — and `RKills`'s own type mentions `R ~{Rng}~> K`, so it cannot be tested apart from that; the `@RKills` negative's error fires at its `R` ARGUMENT, the already-refused-argument trap. What the three controls DO discriminate is that `PrincipalIdeal`, `ResIdeal` and `ResRing` are all ACCEPTED at those levels, so section (A) and the ideal/ring layer are free of the identification, which arrives at `res_kills`, the first constant to mention `Rng`; the section-(D)-(F) constants at `Int_Ring` carry no equation at all, the identification being discharged by instantiation (`ZComm@{u4 u4 u4}`). **156/156 constants closed under the global context with ZERO `Axioms:` lines** — the file declares no `Record`/`Class`/`Inductive`, so `Print Module` flattened is the whole environment and there is no unlisted `Build_*` — all 156 in the `make print-assumptions` gate FULLY QUALIFIED, whose `closed` count rises by exactly 156 (5594 → 5750) with the `Axioms:` line count unchanged at 1. **Exactly ONE collision across all 156 names**, `Int_comm`, and it was removed by consumption as above; the sweep is whole-word over every other `.v`, instrument-checked at `Full` = 97 files and `Monoid` = 94. Closure **112** modules excluding self (113 counting the file, `coqdep -sort` listing its argument last). `Test/ProbeZp409.v` carries **9 refutation commands = 1 instrument + 8 negatives of THREE kinds told apart by the error TEXT** — 4 CONVERSION (the carry is real, so `Zp_digits_add` is an equation in `ResRing n` and NOT at Leibniz equality of the partial sums, refuted in base ten at n = 1 with 2 against 12 and controlled at n = 2 where the carry has been absorbed; the residue relation is coarser than `=`; the `mod` in `zpd` is load-bearing, the units digit of −1 being 9 and not −1; `mraw` is the convolution and not the pointwise product), 2 TYPING (the bijection is in `Sets` and not in `Rng`; `ResTower` at separated ring universes, whose error carries NO universe clause where negative 6's does), 2 FORMABILITY (`RKills`, and negative 8, which isolates the donor: `R : obj[Rng]` at those levels, with no constant of this file in the command) — each stripped ONE AT A TIME in a copy of the WHOLE file and compiled alone with its error read, rename-simulated **14/14** after the simulation FOUND a vacuous guard (`ResTower` was named only inside its own negative, so renaming it left the probe green; a `Check` control was added for it and the file records why). `make todo` grows by **10, ALL in the probe**, the library file contributing ZERO. **AN 8.19/8.20 PORTABILITY REGRESSION WAS FOUND BY BUILDING AND IS FIXED, AND IT IS A REUSABLE FINDING**: EIGHT definitions are written in TACTIC MODE where term-mode `existT _ … …` reads better — `res_kills`, the five limit operations, and `dadd_seq`/`dmul_seq` — because all eight compile on Rocq 9.1 in term mode and are REJECTED on Coq 8.19 AND 8.20, whose unifier will not, while CHECKING an anonymous sigma against a record type, insert a coercion or delta-unfold a definition on the way; three shapes of the one failure were met (`rquot_proj … a` where `RKills` wants `rig_map h a`; `res_proj … n x` where `tower_compat` wants `fmap[USetTower] (tower_step n) x`; `dnorm (fun j => …)` where `DigitCarrier` wants `dadd …`, one delta step away). Stating the type FIRST and closing with `exists`/`exact` puts the check in conversion's hands; the constants stay transparent, so every `eq_refl` is unaffected, and the term-to-tactic rewrite CHANGES NO NAME — measured by extracting declaration heads from both sources, the term-mode version's 118 heads all occur in the tactic-mode one, whose only two extra heads are the `Example`s added beside them. NOT delivered: the coefficient-sequence ring for Exercise 7 as above; monadicity of `Rng`'s underlying-set functor; any digit expansion over a general commutative ring, sections (E)-(F) being at `Int_Ring` only; any normal form or decision procedure for equality in `ResRing n`; any identification of `ResRing ZComm 2 1` with `Instance/Field.v`'s `F2_Ring`; any ring structure on `DigitSeq` and hence no `Isomorphism Rng` anywhere; no p-adic valuation, metric, topology or completeness; no proof that the tower is a proper tower at a non-unit `d` (nothing shows `ResRing n` and `ResRing (S n)` differ); and nothing is registered as an `Instance`
- **Instance/Field/Frac.v**: Mac Lane §III.1 construction 2 — the field of quotients as a UNIVERSAL ARROW, together with his cautionary non-example, over `Instance/Rng/Frac.v`'s `IntDom`/`FracRing`/`frac_embed` and #971's `Instance/Field.v` (nothing there is rebuilt). **The universal property `frac_ump` is UNCONDITIONAL** — verified at the type: arbitrary domain, arbitrary field, no stability, no decidability, no field structure on the fractions, injectivity spent in exactly one lemma (`frac_hom_den_nonzero`), which is precisely why Mac Lane's domain category has monomorphisms only. **The forgetful functor is where the constructive content sits, and it is NOT unconditional**: a field homomorphism is not constructively injective by any route the tree has, and #971 had already measured why — `field_hom_distinct` (`a ≉ b → f a ≉ f b`) is the unconditional strength and its contrapositive yields only `¬¬(a ≈ b)`, while the strict form is `field_stable_at_zero_suffices`, which consumes ¬¬-stability. Since `DomHom` demands the strict form AS DATA, the functor is delivered in two shapes rather than narrowed silently: `StableField_IntDom : StableField ⟶ IntDom` hypothesis-free over the full subcategory cut by `FieldStableAtZero`, and `Field_IntDom : (∀ F, FieldStableAtZero F) → Field ⟶ IntDom` with the blanket hypothesis explicit (`field_every_monic`'s shape). **`Field_Dom : Field ⟶ Dom` is unconditional, and that asymmetry is the point** — it is proved, not asserted. Necessity of the hypothesis is NOT claimed; whether some other construction gives an unconditional FORGETFUL `Field ⟶ IntDom` is left open in #971's own words (unconditional functors of that type trivially exist — a constant one, for instance — so the open question is about the forgetful one specifically). **The non-example needs no quotient rings and no second prime**: `ZtoF2 := Z.odd : Int_Ring ~{Rng}~> F2_Ring`, where `Z.odd_add`/`Z.odd_mul` ARE the two homomorphism clauses against F₂'s `xorb`/`andb`, and `no_field_over_Q_and_F2` refutes a common field with maps to both ℚ and F₂ — with NO case split on characteristic, since a hom `K → ℚ` delivers `1+1 ≉ 0` in K outright and `field_hom_nonzero` then refutes `K → F₂`. So Mac Lane's ℤ/pℤ family is not needed; #971's own ℚ-vs-F₂ pair separates. Stronger than the issue asked: `no_DomHom_Z_F2` (`IntDom` has NO morphism ℤ → F₂ at all) gives `IntDom_Dom_not_Full` for the new inclusion, and — on the SAME witness — `IntDom_Incl_not_Full` for `IntDom_Incl : IntDom ⟶ CRng`, which is the different functor `Instance/Rng/Frac.v` actually left open, having deferred it for want of a counterexample OBJECT. No quotient ring is needed for that object after all: F₂ is a field, hence a domain. The refutation consumes only the EXISTENCE of the factorizations — uniqueness and the arrow ℤ → K are never used (`no_field_maps_to_all_fields`) — and is stated over BOTH field categories so it is not an artifact of `Field` being the larger one; `frac_universal_over_monos_not_over_all` packages the contrast. Two hypotheses, both disclosed and neither Mac Lane's: ¬¬-stability above, and `DomZeroDec`, needed ONLY to name Q(D) as an OBJECT because `FieldObject`'s `finv` is TOTAL and a total reciprocal must decide what to return at zero (stated at zero in `FieldStableAtZero`'s located style). Strict-first measurement: `StableField_IntDom_Dom_obj`/`_map` and `Field_IntDom_agrees_obj` close by `eq_refl` (the `_map` one was first written at `≈`, then measured strict and upgraded), and the extension of ℤ ↪ ℚ carries 1/2 to `1 # 2` at LEIBNIZ equality; the one strict attempt REJECTED is `Field_IntDom_agrees_map`, where the two `DomHom`s share an underlying map but carry different `dom_map_inj` proofs — guarded as a `Fail` probe paired with the control that the same shape one functor along IS strict, so the probe says the proofs differ HERE and not that proof-carrying records rarely convert. A probe-hygiene episode is recorded in the probe header: negatives 1–2 in a reduced-import scratch file aborted with "Illegal application" (the `SetoidMorphism` coercion was absent) rather than a unification error — a genuine FALSE PASS, fixed by giving the probe file the real import list; and negative 2 is a real boundary, `2 # 4` being `Qeq`-equal to the answer but not Leibniz-equal. Two shadowing traps recorded in-file: `Coq.QArith.QArith` transitively exports `Field_theory.num`, shadowing `Frac.num` (but not `den`) with an error naming neither culprit, pinned by a local notation rather than import order; and `Construction/Subcategory.v`'s category-indexed `Full` shadows `Theory/Functor.v`'s. Deferred: no `Q(ℤ) ≅ ℚ` isomorphism (the computations relate them only through the extension), `Dom` defined in this file rather than beside `IntDom`, and nothing claimed about fullness of `IntDom_Incl` into `CRng`
- **Instance/Rng/Mod.v**: Restriction of scalars and the fibered category of all modules (Mac Lane §II.2 constructions 8 and 9, p. 35) — `Restrict ρ : RMod S ⟶ RMod R` along a ring hom (same carrier, action through ρ; identity on underlying homs), Mac Lane's RIGHT-module reading riding `RigHom_op` (`RestrictR : ModR S ⟶ ModR R` — a ring hom is its own opposite-ring hom); **`ModIndexed : IndexedCat (Rng^op)`, the first IndexedCat INSTANTIATED at a concrete varying family** (the general constructors `RT_Indexed`/`IndexedCat_of_StrictFunctor` do handle varying fibres but are applied nowhere outside their own files; `Constant_IndexedCat`, the one prior concrete instantiation, has all fibres one category) — every structural iso identity-carrier via the `rmod_iso_of_ab` smart constructor, `idx_id`/`idx_comp` actions DEFINITIONALLY equal (identity ring map is `Datatypes.id`, hom composition is `Basics.compose`), all ten law fields pointwise reflexivity; `ModTotal := Grothendieck ModIndexed` with `ModProj`. The scope point the file is careful about: the covariant machinery yields the OPFIBRED companion — ModTotal's morphisms are the cosemilinear pairs (ρ : S → R, g with g(ρ(s)·m) ≈ s·g(m)) — while **Mac Lane's construction 9 proper is `ModFibred := (Grothendieck OpModIndexed)^op`** over `OpModIndexed` (fibres `(RMod R)^op`, isos the flipped identity-carrier ones), where the (ρ,f)-description is DEFINITIONAL: `ModFibred_hom_unfold` is `eq_refl` at `∃ ρ : R ~{Rng}~> S, M ~> RestrictObj ρ N`, `ModFibred_semilinear` reads f(r·m) ≈ ρ(r)·f(m) (the word "semilinear" sits here; the opfibred pairs are `mod_cosemilinear_hom`), the composite's ring leg is Mac Lane's ρ'∘ρ by `eq_refl`, and the fibre leg is (Restrict ρ f') ∘ f up to the CANONICAL comparison made visible (`ModFibred_compose_fibre` — genuinely needed: iterated and composite restrictions share an action but differ in law fields, so are distinct records; `ModFibred_compose_carrier` shows it invisible on carriers). `ModFibredProj : ModFibred ⟶ Rng` ((Rng^op)^op collapses by `eq_refl`); fibres: `ModTotal_fibre` identifies ModTotal's own fibre with `RMod R` outright; `ModFibred_fibre` lands at `RMod R` from the OPPOSITE of the underlying Grothendieck's fibre (ModFibred is not presented as a displayed total over `Rng`, so `Fiber` cannot apply to it directly — the file states this precisely); ℤ-initial witnesses both ways (`mod_to_Z`, `mod_from_Z`). NOTATION GUARD recorded in-file: `C^op` (category_scope) and `F^op` (functor_scope) are the same token, and Functor/Opposite.v opens functor_scope — the file pins `Open Scope category_scope` and names `Opposite_Functor` explicitly. Deferrals disclosed: extension of scalars (no module tensor in tree); ModFibredProj-is-a-fibration stated as the op-read of the Grothendieck opfibration citation, not formalized
- **Instance/Grp/Abelianization.v**: Mac Lane §§I.3–I.4's commutator subgroup and abelianization functors, on the setoid-quotient design: G/[G,G] needs NO new carrier — it is G's carrier under the coarser equivalence `abel_eq` (a·b⁻¹ lies in the ≈-saturated inductive generation `InCommutator`), so commutativity of the quotient is the generating constructor itself. Since the PR "algebraic carriers are sets" (2026-09-17) `abel_eq` is the PROPOSITIONAL TRUNCATION of that, `inhabited (InCommutator G (a · b⁻¹))` (`Instance/Grp/Abelianization.v`), because an `AbObject` now carries `cmon_prop`; `InCommutator` itself stays `Type`-valued with every consumer and every leastness motive intact, `abel_eq_is_quot_rel` is restated to match, and — the reason the truncation was taken rather than a hypothesis threaded through nineteen sites — **`Abelianization_Functor : Grp ⟶ Ab` is UNCHANGED in statement and unconditional**, where a section variable of family type would have pinned `Grp`'s object universe (`S3` is then refused) and explicit threading would have made the functor conditional, i.e. weakened a theorem and the projection family `abel_projection : Id[Grp] ⟹ Ab_to_Grp ◯ Abelianization_Functor` has IDENTITY components with Mac Lane's naturality square closing by reflexivity of the coarser relation. Two inductions over the one generation: `hom_commutator` (homomorphisms push the subgroup forward) serves functoriality (`Commutator_Functor : Grp ⟶ Grp`, Center.v's sigma pattern) and normality (`conj_commutator` := the `Grp_conj` instance — what `abel_eq_mul`/`abel_eq_inv`'s well-definedness spends), while `hom_to_abelian_kills` (homs into abelian groups kill commutator elements) is the descent germ — the reason the optional adjunction Abelianization ⊣ Ab_to_Grp is close at hand, disclosed as future Reflective.v work rather than built. The quotient is SPECIALIZED to `InCommutator` (the congruence proofs consume only the normal-subgroup interface, so a generic `NormalSubgroup` quotient is extractable — **and it has since been extracted**: this file now RIDES `Instance/Grp/Quotient.v`'s generic quotient via `CommutatorNS`, its six `abel_eq_*` lemmas reduced to one-line citations with every Lemma statement byte-identical. The old disclosure named THREE unshared constructions to unify; that count was imprecise, and the header now says so — `Instance/Grp/Epi.v`'s `Grp_Coset` is a `SetoidObject`, not a `GrpObject`, being the coset space of a possibly NON-NORMAL image, which is exactly what a normal-subgroup quotient declines to assume, so it is not a candidate at all; `Instance/Ab.v`'s remains separate by a measured decision, routing it would give `Ab.v` a `Grp` dependency it does not have); non-degeneracy is witnessed (`commutator_GrpTwo_proper`, `commutator_S3_nontrivial`, `abelianization_S3_nontrivial` — the quotient of nonabelian S₃ does not collapse, seen through the sign character). `Ab_to_Grp : Ab ⟶ Grp` is the evident inclusion, new here (no bridge existed). Universe-polymorphic throughout (`Abelianization_Functor@{u u0}` — no Set pinning, the Center.v hazard avoided; re-measured on 2026-09-17 after the PR "algebraic carriers are sets" the constant carries THREE universes rather than two, `Sets`' object level no longer being identified with `Ab`'s, and its TYPE is unchanged)
- **Instance/Grp/Center.v**: Mac Lane §I.3's remark and Exercise 4 — the center is not functorial. `IsCentral`/`CenterGrp`/`CenterAb`: central elements form a subgroup (closure by the classical chains) that is abelian BY CONSTRUCTION (commutation of two central elements is one's centrality at the other), so G ↦ Z(G) is a genuine object function Grp → Ab; `Center_incl` includes it. Computations: `Center_GrpTwo_iso : CenterGrp GrpTwo ≅[Grp] GrpTwo` (Z(S₂) is everything; ℤ/2 IS S₂, no permutation model built) and `S3_center_trivial` (six-way case analysis, the proof spending only the two generators), with `central_not_preserved` recording Mac Lane's REMARK proper — the inclusion carries ℤ/2's central nonidentity to the non-central reflection. The counterexample retract uses TwoFunctors.v's SEMIDIRECT S3 (rot × bool), whose sign character is definitionally the bool projection — `s3_sign`/`grp_two_s3`/`sign_retract` — with the header disclosing why Epi.v's `GrpSym3`/`grp_two_sym3` (a different S₃ presentation, serving that file's non-normal-image counterexample, with no in-tree retraction) is not the donor, AND the cost of that choice: TwoFunctors' S3 pins the carrier universe at Set, so `no_center_functor` quantifies over functors on Grp at carrier universe Set (the hazard Epi.v's poly_unit carriers exist to avoid; a polymorphic restatement awaits a poly_unit semidirect S₃ or a sign character on GrpSym3). The headline: `no_center_functor` — for ANY functor T : Grp ⟶ Ab (at that universe) with `fobj T G = CenterAb G` strictly on objects (the literal reading of Mac Lane's "has Z as its object function"; a functor agreeing only up to iso escapes the statement, disclosed; arbitrary arrow function), functoriality pushes the retract S₂ → S₃ → S₂ through the trivial Z(S₃), collapsing Z(S₂)'s nontrivial element into the unit — the two elements of ℤ/2 are distinct constructors, contradiction
- **Instance/Proset/Closure.v**: Mac Lane §II.3 Ex 2 + §II.4 Ex 4 over Structure/Thin.v's existing `Thin` vocabulary (issue #223's file, consumed per #273's correction and EXTENDED there with the closure kit `Thin_Product`/`thin_natural`/`Thin_Transform` — into a thin target any component family is natural, so functors into thin categories are compared by object actions alone): `PreOrder_prod` with `Proset_prod_iso : Proset (PreOrder_prod P Q) ≅[Cat] Proset P ∏ Proset Q` (also Seven Sketches Ex 3.90 clause 4); `Thin_Fun : Thin D → Thin [C, D]`; and the concrete half — `mm_le`/`MonotoneProset` with `Fun_Proset_iso : [Proset P, Proset Q] ≅[Cat] MonotoneProset`, the functor category between preorders IS the pointwise-ordered preorder of monotone maps, riding Instance/Proset/Monotone.v's dictionary with the monotone-side round trip definitional by record eta (thinness of `Proset` itself is Instance/Proset/Order.v's `proset_thin`, reused not reproved). Seven Sketches §1.2.2's worked product is `roof_two_hom_dec`: one total transparent decision procedure over the six objects of `Roof ∏ _2` whose fifteen-related/twenty-one-unrelated split is machine-checked by computation (Roof.v draws RZero at the top — the reverse of the book's Hasse convention; the warning is the issue's, Roof.v itself does not remark on orientation)
- **Instance/Square.v**: Mac Lane §II.8 Ex 1 — the walking commutative square by generators and relations, `Square := FinitelyPresented SquarePresentation` over Construction/Free/Quiver/Presented.v's existing `SquareQuiver` (REUSED, not redeclared, along with `square_diag_fh`/`_gk`), one relation equating the two composites. A sibling of that file's commutative TRIANGLE, not the tree's first presented finite category. The count's engine is `wsq_classify`: one induction classifying every path of the free square by its endpoints at **LEIBNIZ** strength — every A→D path IS `square_diag_fh` or IS `square_diag_gk`, on the nose, which is what makes the result a count of ARROWS rather than of classes (the rank route of Theory/Diagram.v's `sq_len_rank` is the road not taken, disclosed). From it: `Square_Thin`, `square_endo_id`, the seven empty hom-sets, `wsq_pairs_sound`/`_complete`/`_nodup`, and **`square_arrow_total = 9`, `square_identity_total = 4`, `square_nonidentity_total = 5`** by `reflexivity` over all 16 pairs. (The issue's verification block names `square_arrow_count`; the delivered name is `square_arrow_total`, consistent with its sibling counters.) NON-VACUITY three independent ways: `free_square_arrow_total = 10` against 9, so exactly one merge (`square_merge_arithmetic`); `free_square_diagonals_neq` (Leibniz-distinct upstairs) paired with `square_relation_holds` (equal downstairs); and `square_generator_total = 4` computed from the `FiniteQuiver` data, so the diagonal is provably a composite and not a fifth generator. Index shape: `CommutingSquare C` ↔ `Square ⟶ C` both ways with `square_universal`, the functor-side round trip at STRICT strength (`square_functor_round_strict` in `Functor_StrictEq_Setoid`), and the Arrow-category connection — a commutative square IS a morphism of `Arrow C` — at whole-record Leibniz `eq_refl` (`square_arrow_round`); this is a MORPHISM-level comparison, distinct from Theory/Shapes.v's `Two_Fun_Arrow`, which is about objects. **Instance/Square/Product.v** is Riehl §1.6 Example 1.6.6: `Square_2x2_iso : Square ≅[StrictCat] (_2 ∏ _2)` — a genuine ISOMORPHISM of categories, both round trips at `Functor_StrictEq_Setoid`, with `Square_2x2_Cat_iso` derived and labelled weaker (`≅[Cat]` IS equivalence here); `Square_to_22` goes THROUGH `SquareFunctor`, so it is the universal property applied rather than a parallel hand-rolled functor, and 2×2's count is proved from scratch (3 × 3 = 9) with `square_22_counts_agree` the cross-check — proving it twice is what makes it one. **Instance/Square/Rectangle.v** is Example 1.6.9 over `_2 ∏ _3` (Instance/Ordinal.v's `_3`; the issue's suggested `Theory/Metacategory.v` `Three` is NOT usable — that category has no objects, per its own header): six objects, seven arrows, both squares, the outer rectangle, both short diagonals and the inner parallelogram, all with NO hypothesis since the shape supplies them, plus the free-standing `paste_squares` and `rect_outer_two_ways`; count 18 = 3 × 6 with `ord3_count_agrees` tying the `_3` factor to `ord_pairs 3`. Deferred and disclosed: the rectangle's CONVERSE is pinned at `C : Category@{co Set Set}` (INHERITED from `Functor_of_Pair` and `Cyl_functor`, not introduced — the forward half, where all the commutativity content lives, carries no pin); no presentation of the 2×3 shape by generators and relations, so no free-versus-presented count for it; no uniqueness clause for `RectFunctor`. `Test/ProbeSquare.v` pins the boundary (instrument sanity-checked, each negative stripped and confirmed genuine — two `cannot unify` typing errors and one real universe inconsistency naming the declared level), and its `BigTarget` section is a positive control guarding the donor lift. **That lift is this issue's by-product and is recorded under Construction/Free/Quiver/Examples.v above**: `Build_Quiver_Standard_Eq`'s silent `Set` pin on the proof universe is removed, which is what keeps clause 3 from being near-vacuous — `square_universal` instantiates at `Coq`
- **Instance/Presented/Cyclic.v**, **Instance/WalkingIso.v**, **Instance/Presented/Three.v**: Awodey §4.4 Example 4.9 and §4.5 Exercise 3 (1st ed., CMU pre-print September 2005, printed pp. 90-91) + Fong–Spivak §3.2.1 Exercise 3.10, §3.2.2 Example 3.18 and Exercise 3.19 (printed pp. 83-85) — **the finite non-free presented categories: the one-generator family, the walking isomorphism, and the poset 3 with its full composition table**. **THE ISSUE'S OWN "Current state" IS STALE ON FOUR COUNTS, EACH RE-MEASURED BEFORE ANY HEADER PROSE WAS WRITTEN**: it says the examples "require the finitely-presented-category machinery … which is itself the subject of a separate issue", whereas `FinitelyPresented` is `Construction/Free/Quiver/Presented.v` and `Instance/Square.v` already consumes it; it says the category 3 "is not presented by generators and relations" and that "its freeness is not addressed", and BOTH are false — the commutative-triangle presentation is `Presented.v` (`Ord3Quiver`, `ord3_path_c`, `ord3_path_ab`, `ord3_free_distinct`, `Ord3`, `ord3_relation_holds`, all nineteen cited line numbers verified exact), and `Theory/Shapes.v` ALREADY NAMES all three non-identity arrows of `_3` — a fourth piece of prior art the first draft of this bullet and of that file's own survey both missed, found by audit: `three_a`, `three_b` and `three_c` ARE that file's `three_01`, `three_12` and `three_02` at `eq_refl`, respelled as raw `le_t` terms only so that the composition table is a COMPUTATION rather than the unfolding of `three_02 := three_12 ∘ three_01`, with the identification recorded; and the freeness verdict is `ordinal_free 3` (`Construction/Free/Quiver/Examples.v`, with the m = 3 instance `chain_free` beside it), CONSUMED here and not reproved; and — the one the issue does not mention at all — `Presented.v` ALSO carried `OneNodeQuiver`, `FreeMonoidOn` and `PresentedMonoidOn`, the one-object machinery Example 4.9 needs, with **no consumer anywhere in the tree** before this work (a whole-word sweep returns the declaring file and two prose mentions in `Instance/Grp/Free.v`, and nothing else). All three are consumed. **THE ONE IDEA THAT MAKES THE FOUR ONE-OBJECT EXAMPLES ONE DEVELOPMENT RATHER THAN FOUR**: a path of `FreeOnQuiver (OneNodeQuiver unit)` from the single node to itself is a `tlist` over one node, i.e. a natural number — its length — and `spath_complete` proves that at **LEIBNIZ** equality, not merely up to `≈`, so the free hom-set is EXACTLY `nat` and the free hom-monoid is (ℕ, +, 0); imposing `s^(t+c) = s^t` then quotients ℕ by the congruence generated by `t + c ~ t`, whose normal form is the rho shape `rho_nf t c k = if k <? t then k else t + (k - t) mod c` — a tail of length `t` running into a cycle of length `c` — so ℤ/2ℤ, the two-element idempotent monoid, ℤₙ and Seven Sketches' `s⁵ = s²` are `SCat` at (0,2), (1,1), (0,n) and (2,3), each costing one `Definition` plus readbacks. The three theorems are `rho_sound` (congruent words have equal normal forms, by induction on `CongClosure`'s five constructors, the `cc_comp` case being exactly what the absorption law `rho_nf_add` exists for), `spath_nf` (every word reduces to its normal form, by well-founded descent — and the descent must use `well_founded_induction_type`, since `≈` here is TYPE-valued and a `BoolSpec` cannot be destructed in such a goal) and **`spath_iff`, the DECISION PROCEDURE**: two words are equal in the presented category exactly when their normal forms agree, with the two directions named separately. `scat_equiv_is_closure` records at `eq_refl` that the `≈` all three speak of IS the presented category's own — `Quotient`'s hom-setoid `equiv` is literally the closure — and not a lookalike. Seven Sketches §3.2.2's "one equation forces others" is `z2_cube` (`s³ = s`) and `z2_fourth` (`s⁴ = id`); Exercise 3.19's monoid is `TailCat`, whose five words are pairwise distinct (`tail_distinct`) and which **`tail_not_group` proves is not a group** — the generator has no right inverse — unlike every other example here. **THE SEVEN SKETCHES BRIDGE, WITH ITS AXIOM COST MEASURED FIRST RATHER THAN DISCOVERED AFTER**: the checkbox asks that the presentation and `Instance/Comp.v`'s equational `UniversalAlgebra.Bool : Group` not be left unconnected — note the constants live under the MODULE `UniversalAlgebra`, so the path is `UniversalAlgebra.Bool` and not `Category.Instance.Comp.Bool` — and measurement says `UniversalAlgebra.BoolOp` and `UniversalAlgebra.xor` are BOTH "Closed under the global context" while `Bool` itself carries `functional_extensionality_dep`, it being the proof of the group EQUATIONS that spends it. So the bridge is built to `BoolOp`, which costs nothing: `z2_bool_compose` sends composition to `xor` and `boolop_mul`/`boolop_one` record at `eq_refl` that the `xor` and `false` in question ARE `BoolOp`'s own operations; **nothing in the file mentions `Bool`**, since any constant that did would inherit that axiom, and that is disclosed rather than glossed. Requiring `Instance/Comp` costs the closure exactly TWO modules (33 → 35 excluding self), measured by dropping it. **THE WALKING ISOMORPHISM** is two nodes with `f : A → B` and `g : B → A` and the two relations `g ∘ f = 1_A`, `f ∘ g = 1_B`: `wi_classify` classifies every path of the FREE category by its endpoints at LEIBNIZ strength (the `Instance/Square.v` `wsq_classify` idiom — a bare `induction` on a free path is ill-typed, the endpoint analysis must live in the SPECIFICATION), `walking_iso : WI_A ≅ WI_B` is the exercise's headline with `g` as inverse, and `WalkingIso_Thin`/`wiso_hom_singleton`/`WalkingIso_IsGroupoid` identify it as the chaotic groupoid on two points, every hom-set a singleton — inhabited AND unique, both halves. **THAT THIS PRESENTATION IS NOT FREE ON ITS OWN QUIVER IS MEASURED, in the shape `Instance/Square.v` established — and the file scopes the word carefully**: `free_wi_AA_inj` proves the alternating loops `wi_AA n` pairwise distinct AS ARROWS of the free category, `walking_iso_merges_the_loops` is the pair (distinct upstairs) × (equal downstairs) at one concrete instance, and `WalkingIsoProj_not_faithful` is the consumer-facing form; what that does NOT say — and `Instance/WalkingIso.v` says so in terms at its own scope paragraph — is that `WalkingIso` is free on NO quiver; `free_iso_lengths` (a free category has no non-identity isomorphism, stated for an ARBITRARY quiver and so an upstreaming candidate for `Construction/Free/Quiver.v`) is where such an argument would START, and the isomorphism-invariance step it would still need is neither proved nor attempted; the counts `wiso_object_total = 2`, `wiso_arrow_total_4`, `wiso_identity_total_2`, `wiso_nonidentity_total_2` and `wiso_generator_total_2` are all proved, the last from the `FiniteQuiver` data, and `walking_iso_universal` bundles existence, the two generator readbacks and uniqueness, with the two round trips BESIDE it as `wiso_round_morphisms` and `wiso_functor_round_strict`, the latter at `Functor_StrictEq_Setoid`. **THE CATEGORY 3** gets Awodey's four presentations, **each proved isomorphic to `Instance/Ordinal.v`'s `_3` in `StrictCat`** — the genuine isomorphism, not `≅[Cat]`, which in this library IS equivalence — namely `Ord3_iso` (three generators, one relation, CONSUMED from `Presented.v`), `Lin3_iso` (two generators, NO relations: the free one, built by consuming `ordinal_free 3`), `Ord3Rev_iso` (the same equation written the other way round) and `Tri4_iso` (four generators, two relations). **What makes the four distinct is machine-checked rather than asserted**: their generator counts are 3, 2, 3, 4 and their relation counts 1, 0, 1, 2, so three of them differ pairwise in that data (`three_presentation_counts_differ`), while (i) and (iii) agree on both counts and are separated instead by their equations' left-hand sides being `ord3_path_c` against `ord3_path_ab`, which `Presented.v`'s own `ord3_free_distinct` proves are different arrows of the free category — **no single constant states "the four records are pairwise distinct", and the header says so**. A FURTHER SCOPE POINT, disclosed here as it is twice in that file: (iii) differs from (i) ONLY by exchanging the two sides of its one equation (`Ord3RevEqns` is `Ord3Eqns` with `fe_lhs` and `fe_rhs` swapped over the SAME quiver), so as presentation DATA the four are four, but a reader who wants four with genuinely different generator sets has (i), (ii) and (iv) — the categories coincide because a congruence is symmetric, and that is why (i) and (iii) need the equation-level separation rather than a count. The comparison engine is one general lemma, `pq_same_iso` (two relation families generating the same congruence present isomorphic categories, by identity-on-everything functors), with `pq_trivial_iso` its degenerate case; only (i) and (iv) need the free paths classified (`o3_classify`, `t4_classify`, again at Leibniz strength, whence `ord3_thin`/`tri4_thin`). Seven Sketches Exercise 3.10's composition table is stated at `_3` itself: **`three_arrow_total_6`**, `three_identity_total_3`, `three_nonidentity_total_3`, the enumeration duplicate-free, sound and complete, the three descending hom-sets EMPTY, all ten composable pairs computed at **Leibniz** equality by `eq_refl` (composition in `Ordinal n` is `le_t_trans`, a `Fixpoint` that reduces on closed indices, so `three_table_ba : three_b ∘ three_a = three_c` is an equation of TERMS — though read what the table CARRIES: `_3` is thin with a Leibniz hom-setoid, so every equation between parallel arrows already follows from `ord_thin`, and what the ten entries pin down is the INHABITATION pattern rather than the composites, an honest qualification an audit supplied and the file now states), and equality of morphisms DECIDED by `three_mor_eq_dec` through `Instance/Ordinal.v`'s `ord_coords_inj`. The count of six is kept from being vacuous three ways: the three objects are pairwise distinct, the three descending hom-sets are empty so 3 is no total preorder, and `three_a_not_iso` shows the generating step is not invertible so 3 is no three-object groupoid in disguise. **383/383 constants closed under the global context with ZERO `Axioms:` lines** (69 + 110 + 204, enumerated from whitespace-flattened `Print Module` and each queried fully qualified), ALL 383 in the `make print-assumptions` gate, whose `closed` count rises by exactly 383 (5750 → 6133) with the `Axioms:` line count unchanged at 1; **zero collisions over all 391 names** — the 383 `Print Module` entries plus the EIGHT `Inductive` constructors (`WI_A`, `WI_B`, `WI_f`, `WI_g`, `Tri4_a`–`Tri4_d`) that `Print Module` lists only after a `:=`, so a keyword-keyed extraction misses them, a gap an audit found in the first draft of this sentence — instrument-checked, after THREE were renamed away before landing — `nf` is `Construction/FreeMonoidal/Normal.v`'s, and `sound` and `SMon` are generic enough to be worth avoiding, hence `rho_nf`, `rho_sound` and `RhoMon`; closures 35, 35 and 41 excluding self; zero lines over 80 columns in any of the three; and **`make todo` grows by ZERO — all three are library files and none contributes a hit**, so that Definition-of-Done box is met as written. NOT delivered: no identification with `Bool` itself, for the axiom reason above, so the presentation is related to the equational construction at the level of the `OpAlgebra` and its operations and not at the level of the `Group` record; no claim that the four one-generator categories are pairwise non-isomorphic; `presented_universal` is consumed by NONE of the three — every occurrence of that name in them is header prose, measured — so nothing says what a functor out of `SCat`, `Ord3`, `Lin3`, `Ord3Rev` or `Tri4` is; what `Instance/WalkingIso.v` does have is its own `walking_iso_universal`, written in the same `hom_cast` shape but built by hand rather than by applying that donor; no group structure is built on `Z2Cat` or `CycCat n` — `tail_not_group` refutes it for `TailCat` and nothing affirms it for the others; no comparison of `WalkingIso` with `Indiscrete bool` or with `_2`, and no proof that it is not free on some OTHER quiver; and nothing in any of the three is registered as an `Instance`
- **Instance/Mon/Coproduct.v**, **Construction/Free/Quiver/Coproduct.v**: Mac Lane SIII.3 Exercise 4 — coproducts in **Mon** and **Grph** (Awodey 1st ed. §3.2 Examples 3.5 and 3.9 cover the same ground). The Cat third was already done (`Cat_Cocartesian`); these are the other two, both delivered as genuine `Cocartesian` INSTANCES rather than bare universal properties. **`Mon_Sets_Cocartesian : @Cocartesian (@Mon Sets Sets_Product_Monoidal)`** is the free product `FreeProd` — an inductive term type `FPTerm` quotiented by a 12-constructor congruence `fp_eq` — fully general over ALL objects with no side condition, no decidability and no smallness, with `Mon_Sets_Initial` alongside so the trivial-factor control lands at isomorphism strength. **The target category was chosen by measurement, not convenience**: `Theory/Algebra/Monoid/Hom.v`'s `Mon` is the only category of GENERAL monoids in the tree (`Instance/CMon.v`'s `CMon` is a category whose objects are COMMUTATIVE monoids, ruled out for a different reason; an earlier draft of this bullet said "the ONLY monoid category", which the file's own header already qualifies away) — `Construction/Deloop.v`'s `MonObject` is a bare record with no category, so a coproduct there could not be stated as `Cocartesian` at all, and `Instance/Coq/Monoid/Free.v`'s `MonCoq` is a DIFFERENT monoid notion over `Coq`. The file deliberately does NOT require `Instance/Roster.v` (which names this same term `Mon_Sets`), because Roster pulls in `Instance/Top` and hence the stdlib reals; the name identification is instead GUARDED in the probe file. **Non-degeneracy is proved twice over ON THE MON SIDE** (the quiver half has no general statement — its `injections_not_equivalent` is at one concrete quiver, an asymmetry recorded in that file's header): generally, both injections are split monic hence injective for ARBITRARY factors (`fp_injections_Monic`, `fp_inl_injective`) — the congruence collapses neither factor, ever; and concretely over (ℕ,+) ∗ (ℕ,+), whose free product is the free monoid on two letters, `fp_generators_do_not_commute` and `fp_word_not_left`/`_right` show the two-letter alternating word is `fp_eq`-distinct from EVERY one-letter word on either side, all by mapping OUT through `fp_eval_resp` into `(list bool, ++, nil)` and `discriminate` — no induction on the congruence can yield a negative. The degenerate one-factor-trivial case is a CONTROL, not the witness. **`QuiverCategory_Cocartesian`** is the componentwise disjoint union, with the cross edge-sets literally `False` (`QuiverCoprod_edges_lr`/`_rl` by `eq_refl`) and `coprod_no_cross_edge` proved by the identity. **It succeeds where the sibling product could not, for a structural reason worth recording**: `Construction/Free/Quiver/Constructions.v` discloses that its `QuiverPair_unique` takes LEIBNIZ hypotheses because respectfulness of `QuiverPair` is unproved — a pairing's node action pairs two functors, so relating two pairings needs an assembled equality of PAIRS with edges transported along it. A copairing's action is a `match`, so at each constructor the required equality IS the given one; hence `QuiverCopair_respects` IS proved and `QuiverCopair_unique` takes `≈` hypotheses. That is also what made the `Cocartesian` instance affordable. Both triangles hold at whole-record LEIBNIZ equality (`QuiverCopair_Inl`/`_Inr` := `eq_refl`), and the gap Constructions.v discloses — no competitor exhibited that FAILS a triangle — is filled by `copair_swap_fails_left`/`_right`. **Forgetful preservation is MEASURED and comes out NEGATIVE at `eq_refl`**, with both causes located rather than guessed: `Construction/Coproduct.v` is a `Program Definition` whose match compilation gives `hom` an equation-passing shape that does not convert under binders, and its `homset`'s `setoid_equiv` is a `Qed`-opaque obligation. The sharp measurement is that `edgeset` fails at constructor arguments where `edges` SUCCEEDS at the very same arguments. Delivered instead as `QuiverOfCat_Coproduct_iso`, whose legs are the identity on nodes AND on edges in every case, plus `Forgetful_preserves_coprod_inl`/`_inr` at `≈` — so the coproduct DIAGRAM is preserved and the failure is presentational; `eq_refl`-level preservation is NOT claimed unreachable, only not achieved. `CoproductInl`/`CoproductInr` had to be built here, the tree having no named inclusion functor into a coproduct of categories. SCOPE: **Awodey §3.2 Ex 5 (`M(A) + M(B) ≅ M(A+B)` via LAPC) is NOT DELIVERED, for a verified reason** — the free-monoid adjunction `free_monoid_adjunction` lives at `MonCoq := @Mon Coq Coq_Monoidal` (`Instance/Coq/Monoid/Free.v,326`) while this coproduct is at `@Mon Sets`; they are DIFFERENT CATEGORIES, so there is no adjoint at this category to apply LAPC to. `Construction/Funny.v`'s unproven prose claim (the funny tensor of one-object categories is the free product in Mon) is NOT proved either and is neither weakened nor strengthened — closing it needs a bridge between `FunHom` words and `FPTerm` words AND between two different monoid records. Also not delivered BY THESE FILES: no alternating normal form, hence no decision procedure for `fp_eq`; no nullary quiver case, so `Cocartesian QuiverCategory` is binary only and no `Initial QuiverCategory` is built; no naturality of the quiver preservation in C and D; no hom-setoid bijection packaging. `Test/ProbeCoproduct328.v` guards the boundary with 5 conversion negatives plus a separate instrument check, rename-simulated at 15/15 over the constants appearing in the NEGATIVES — a check that found one (`edges`) unguarded on its first run

- **Instance/Ordinal.v**: The finite ordinals as categories — the uniform family `Ordinal n` (thin, carrier the record `Ord_obj n` in proved bijection with `Fin.t n`, order `Instance/Omega.v`'s Type-valued `le_t`), where the tree previously stopped at `_2`. Agreement isos `Ordinal 0 ≅ _0`, `Ordinal 1 ≅ _1`, `Ordinal 2 ≅ _2` in `Cat`, with `_3` and `_4` exported for downstream shapes; full and faithful embeddings `Ord_Incl : Ordinal n ⟶ Ordinal (S n)` and `Ord_Omega : Ordinal n ⟶ Omega` ("an ordinal is the set of its predecessors", categorically), commuting in the evident triangle. The Seven Sketches §3.2.1 morphism count is a closed formula by induction, not per-n enumeration: `ord_pairs n` enumerates the arrows duplicate-free and `ord_morphism_count : 2 * length (ord_pairs n) = n * (n+1)`. Riehl §1.6's freeness is stated as a direct UMP at the level of `Cat` (`Construction/Free/Quiver.v`'s `FreeOnQuiver` + `FreeForgetfulAdjunction` do exist, but their universal property lives in `StrictCat` under strict functor equality; the literal `FreeOnQuiver (linear quiver) ≅ Ordinal (S n)` identification is NO LONGER OPEN — it is `ordinal_free` in Construction/Free/Quiver/Examples.v, at `≅[StrictCat]` and for every m including the empty ordinal, though pinned to `Category@{Set Set Set}` by the free side while `Ordinal` here stays polymorphic): a path of n composable arrows (`OrdSteps`) generates `Functor_of_Steps : Ordinal (S n) ⟶ C` acting as the path on generating steps, every functor out of `Ordinal (S n)` is `Functor_Setoid`-equivalent to one so obtained (`Functor_of_Steps_of_Functor`), and naturality on the n generating steps suffices for naturality everywhere (`ord_functor_equiv_from_steps`); shape-4 diagrams specialize this at n=3, the four triangles commuting by thinness. On-the-nose functor↔path bijection is deliberately NOT claimed — a path's object part is a function on all of `nat`, so paths agreeing below n induce the same functor
- **Instance/Ab/Character.v**: Pontryagin duality of finite abelian groups, Mac Lane's §I.4 worked pair — the character group `D(G) := hom(G, ℚ/ℤ)` with `QZ` the circle as an `AbObject` (carrier ℚ identified up to integer difference — the setoid IS the quotient — with DECIDABLE ≈ via `Qred`; ℚ/ℤ-for-ℝ/ℤ disclosed: finite characters land in torsion, and stdlib ℚ is axiom-free where ℝ is not), the contravariant `D : Ab^op ⟶ Ab`, the covariant `DD` (defined directly so both flips unfold), and the natural evaluation `tau`/`tau_natural`/`tau_Transform : Id ⟹ DD`. Character/Finite.v is the substantial half, fully proven and axiom-free: `tau_iso_finite : FiniteCarrier G → G ≅ DD G` over finiteness-as-data (enumeration + ≈-completeness + ≈-decider), via a Type-valued pigeonhole toolkit (stdlib `In`/`NoDup` are Prop and cannot eliminate), `smul_annihilator` (no Lagrange, no well-ordering), the CHARACTER EXTENSION lemma (fuel recursion whose progress lemma `outside_count_generated` re-establishes its own bound — no cardinality anywhere; bounded-minimization coset decomposition with uniqueness by period-minimality; k-th roots computed in ℚ), separation with value 1/k forcing `tau_injective_finite`, and surjectivity by the SAME subgroup induction (`Realizes`/`VanishesOn` — every functional vanishing on H's annihilator is evaluation at a member of H), deliberately avoiding |D(G)| = |G| — though `zmod2_characters` verifies the count concretely on the ℤ/2 witness. Character/NonNatural.v is the negative half (`maclane:I.4:remark1`): the groupoid core `AbIso`, the covariant `D' f = D(f⁻¹)`, and `sigma_not_natural` — no family of isos `σ_G : G ≅ D(G)` survives even the single naturality square at the automorphism ×2 of `Z5` (2q ≈ 3q forces q ≈ 0, so σ(1) is the zero character and σ is not injective) — Mac Lane's m² ≡ 1 obstruction at the first working prime, p = 5
- **Instance/Mod.v**: Module categories R-Mod and friends (Mac Lane §I.7) — `RModObject` extends `AbObject` by a scalar action with the four module laws (0·m, r·0, and both negation compatibilities DERIVED, not fields), `RModHom` a group hom preserving the action (negation preservation cited from Ab's `ab_map_neg`), the category `RMod R` with forgetful functors to Ab and Sets, `RMod_Zero` on the trivial module, and Mac Lane's proposition both ways — `rmod_monic_iff` (kernel probe) and `rmod_epic_iff` (cokernel quotient) — with `RModKernel`/`RModQuotient` literally riding Instance/Ab.v's `AbKernel`/`AbQuotient`. An earlier revision of this bullet called `rmod_epic_iff` a "constructive cokernel quotient"; measured on 2026-09-17 after the PR "algebraic carriers are sets", `mquot_rel` is `Prop`-valued and `rmod_epic_iff` concludes `RModPropSurjective`, the propositional surjectivity, so "constructive" overstates what the biconditional now hands back — the untruncated `RModSurjective` and `rmod_surjective_epic` are unchanged and the Σ direction is kept whole (the only new obligations are kernel action-closure and the action descending to cosets). Right modules are `ModR R := RMod (Ring_op R)` over the transparent opposite ring (`Ring_op_additive_agrees` by eq_refl; involutivity deliberately not claimed — `rig_mul_assoc` routes through an opaque helper), justified by `bimodule_right_RMod : Bimodule R S → RModObject (Ring_op S)` where `Bimodule` carries the r(ms) ≈ (rm)s compatibility; `RMod_Preadditive` reuses Ab's hom-addition for the group part; the witness is `Ring_RMod` (every ring is a left module over itself, each law literally a rig law) with `Int_RMod` and eq_refl Examples. The `Field` class and the `Vct_F` name are #244's (delivered there as a `Definition`, not a notation), per that issue's QA correction — stated in the header, not built here
- **Instance/Ab/ModFunctor.v**: R-Mod as an additive functor category (Mac Lane §II.4 Exercise 1, p. 41) — the ring-as-one-object-Ab-category clause delivered by REUSE + one step: `DeloopRig` (Theory/Algebra/Rig.v) already carries the Preadditive structure, and `DeloopRing_AbEnriched` adds `abneg := ring_neg` (five `eq_refl` readings record that the hom-group IS R's additive group and composition IS multiplication; `rig_iff_one_object_preadditive` cited as the rig-level version). `AbFunAdd R := Sub [DeloopRig (ring_rig R), Ab]` on the `AdditiveFunctor` predicate (a FULL subcategory), and **`RMod_AbFun_equiv : AbFunAdd R ≅[Cat] RMod R`** (`≅[Cat]` IS equivalence, Functor_Setoid) — an additive functor is exactly a module structure on its image group, with THE PIVOT localized: `rm_smul_distr_l/assoc/one` come free from `cmon_map_plus`/`fmap_comp`/`fmap_id` (composition in the delooping IS `rig_mul`, the identity IS `rig_one`), and `rm_smul_distr_r` is EXACTLY `fmap_padd`, the sole `AdditiveFunctor` field, in both directions (`functor_of_module_additive`'s only content); one-object naturality collapses to `rm_map_smul` (the Rep.v/#279 sibling architecture, the additive enrichment added). Module-side round trip DEFINITIONAL (carrier and smul by `eq_refl`, iso legs `cmon_hom_id`); functor-side pointwise (the rebuilt `CMonHom` records are not convertible though their underlying maps are — the in-file note beside `AbFunAdd_functor_round` says exactly this). The subcategory is PROVED PROPER at `Int_Ring` (`AbFunAdd_proper` — the nontrivial-ring qualifier matters: everything-↦-id IS additive over the trivial ring): two multiplicative-but-not-additive functors — `unit_act_functor` (everything ↦ id, refuted 1 ≠ 2 at ℤ) and `square_functor` (n ↦ n²·(−) over ℤ, refuted 4 ≠ 2) — so the additive predicate genuinely cuts. Ring-level `EndRing` converse NOT built (listed in the file's WHAT-IS-NOT-BUILT; the rig-level biconditional stays Rig.v's). Witnesses `Ring_AbFun` + ℤ `eq_refl` computations (fmap 3 acts as ·3). Gotcha recorded in-file: `[C, D]` is level-90 and needs parens as a bare application argument (`Sub ([X, Y]) …`)
- **Instance/Ab/Graded.v**: Graded abelian groups as Ab^ℕ (Mac Lane §II.4 Exercise 3, p. 41) — `GradedAb` defined DIRECTLY (objects `nat → AbObject`, degreewise `AbHom`s, componentwise ≈) with the PiCat comparison recorded and its DIVERGENCE measured: objects/homs/equiv-relation/id/compose agree with `PiCat (fun _ : nat => Ab)` by `eq_refl` (five Examples, true by construction), while the records are provably not convertible — the `homset` field itself, `compose_respects`, the four laws and the `Equivalence` witness all reject (probed, not assumed) — so `PiCat_GradedAb_iso` is a real identity-on-data functor pair with `iso_id` round-trip components, and that leg is STRICTER than `≅[Cat]` (which IS equivalence, Cat's hom-setoid being `Functor_Setoid`): identity on data both ways, while the `Fun_Discrete_PiCat` leg is an equivalence proper. **`Graded_Fun_equiv : ([DiscreteCat nat, Ab]) ≅[Cat] GradedAb`** := that comparison composed with #276's `Fun_Discrete_PiCat` — REUSE, not re-proof: no transformation over the discrete shape is rebuilt. `GradedAb_shift` (13 lines) earns its keep through **`graded_degree_one`**: `(∀ n, F n ~> G (S n)) = (F ~{GradedAb}~> shift G)` by `eq_refl` — a degree-one map IS an ordinary arrow into the shift. Witnesses genuinely degree-dependent and PROVED so (`graded_deg_mul`: multiplication by n in degree n, with `graded_deg_mul_not_constant` refuting any constant-family presentation; `conc0` ℤ-concentrated-in-degree-0 with shift computations). Header disambiguates Monad/Graded.v (monoid-graded MONADS — grades interact via `T m ◯ T n ⟹ T (m·n)`; these degrees never interact) and scopes the index honestly: the DONORS are index-generic, this file's own definitions hardcode ℕ as the exercise asks — a ℤ-graded variant would reuse the donors unchanged but restate these definitions. All 44 constants Closed (25 glob + 19 Program obligations, Print Module methodology)
- **Instance/Mod/Free.v**: Mac Lane §III.1 construction 1 + §IV.1 construction 1 + Riehl §4.0 — the free module on a setoid, its universal arrow, and the free ⊣ forgetful ADJUNCTION, with `Instance/Vect/Free.v` the vector-space reading. **The issue's own sketch could not be followed and the header argues why, with the obstruction located exactly**: it asks for finitely supported K-valued functions, and the CARRIER is not the problem — a `≈`-respecting function paired with a list witnessing its support is writable over a bare setoid with no decider. The BASIS INSERTION is: the basis vector at `x` is "1 at x, 0 elsewhere", which cannot be WRITTEN without a decider for `x ≈ y`. That is fatal rather than merely restrictive because `RMod_Forget` lands in `Sets`, so the left adjoint must exist for EVERY setoid, decidable or not — a construction available only at decidable setoids yields no adjunction at all. So encoding 1 is not a trade-off, it is unavailable. The carrier is instead an inductive of formal expressions (`fv_gen`/`fv_zero`/`fv_plus`/`fv_neg`/`fv_smul`) with `fv_eq` an inductive congruence closing under exactly the four abelian-group and four module laws plus saturation under X's `≈` and R's `≈` (Instance/Ab/Tensor.v's shape, reflexivity derived; `fv_Setoid` deliberately NOT an instance, that file's convention). **The list description survives as a THEOREM rather than as the carrier** — `fv_normal_form`: every element is `fv_eq` to a finite formal combination `fv_lc l` — so reordering and collection of like terms are consequences of the congruence rather than obligations on the representation. What that buys, measured: every module law is a constructor, so `FreeModObject` is a record literal with ZERO proof obligations — RE-MEASURED on 2026-09-17 after the PR "algebraic carriers are sets" and STILL TRUE (`grep -c '_obligation_' Instance/Mod/Free.glob` returns 0), although the literal gained one field, `cmon_prop := @PropEquiv_of_relation _ fv_Setoid fv_eq …`, which is neither a law nor a constructor and costs no obligation, `fv_eq` having become a `Prop` inductive in that PR; the setoid constraint `x ≈ y ⟹ e_x = e_y` is the constructor `fe_gen`, making respectfulness of the insertion a one-term proof; and the mediator is a `Fixpoint`, so all three homomorphism laws of the linear extension close by `reflexivity` (recorded as `eq_refl` Examples so the claim is machine-checked rather than inferred from which tactic branch fired). What it costs: seven inductions, of which two carry the universal property (`fv_eval_respects`, 14 cases — eight met by a law of the target module, the other six being generator saturation, congruence and the target setoid's symmetry/transitivity — and `fv_extend_unique`, one case per former) while four more exist only to prove `fv_normal_form`; and **coefficient uniqueness is given up** — hence no decision procedure, no dimension, and NO bridge to Instance/FdVect.v's `FdVectObject`, so it is NOT proved here that the free vector space on a finite generating setoid is finite-dimensional in that file's sense. `free_module_adjunction : FreeMod ⊣ RMod_Forget R` and `free_vect_adjunction : FreeVect F ⊣ Vct_Forget F` are the headline artifacts (via `universal_arrow_from_UMP` → `LeftAdjointFunctorFromUniversalArrows` → `AdjunctionFromUniversalArrows`), with §IV.1's TWO naturality clauses stated and proved in the free module's OWN vocabulary — `free_module_naturality_in_set` (by evaluating at a generator) and `_in_module` (pointwise `reflexivity`) — and then `free_module_naturality_in_set_is_to_adj_nat_l` and its sibling recording by `eq_refl` that those statements ARE the class's fields, a Leibniz equality of statements rather than a parallel restatement. The unit computes (`free_module_unit_is_insert`, `_is_generator`) and the forward transpose IS `fv_transpose` (`free_module_transpose_is_adj`), all `eq_refl`; the COUNIT does not, routing through the `Qed`-opaque `ump_universal_arrows`, and `fmap[FreeMod] u` on a generator is `≈` only since `LeftAdjointFunctorFromUniversalArrows` defines `fmap` by factorization — both pinned as `Fail` probes. **Vect/Free.v specializes by CONVERSION, not by transport**: `Vct_F F = RMod (field_ring F)` by `eq_refl` (it is a `Definition`), every vector-space statement inhabited by its `RMod`-level term, and the field layer buys EXACTLY ONE thing — `field_one_neq_zero` is a field of `FieldObject`, so `1 ≉ 0` is discharged for free; nothing else spends `finv` or commutativity, and an earlier header sentence claiming otherwise was corrected. Non-vacuity at THREE prices, each proved and witnessed over ℤ and ℚ: `free_module_scalars_faithful` needs NO hypothesis, `free_module_two_independent` (the basis is linearly independent — the §III.1 statement a reader wants) needs the decider ONLY, and the reason it can drop `1 ≉ 0` is precisely that over the zero ring every scalar is already 0, so it degenerates there to a truth rather than to a falsehood and survives unassisted, its content carried entirely by the nondegenerate case; `free_module_basis_injective` — which IS false over the zero ring — needs decider AND `1 ≉ 0`. That both hypotheses are unavoidable is ARGUED in the section header, not proved: no converse is machine-checked, and the tree's precedent for exactly that claim shape (`arrow_mul_respects_forces_UIP` and siblings) is deliberately not followed
- **Instance/Mod/Tensor.v**: Mac Lane §III.1 construction 6 + Riehl §2.3 Example 2.3.8 — the tensor product as a UNIVERSAL ELEMENT of the bilinear-maps functor, with `Instance/Vect/Tensor.v` the vector-space reading. `RBilinear V V' W` (additive in each variable, commuting with the action in each), `TensorMod V V' : RModObject R`, `Bilin : RMod R ⟶ Sets` as a genuine functor (action by postcomposition), and the headline `tensor_universal_element : AUniversalElement (Bilin V V') (TensorMod V V')` over Theory/Universal/Element.v's class. **The generality is an arbitrary `RingObject` with NO commutativity hypothesis** — verified at the signatures, not asserted: `TensorMod`, `RBilinear`, `Bilin`, `tensor_med`, `tensor_hom_ext` and `tensor_universal_element` all take a bare `{R : RingObject}`, so the universal element lives in `RMod R` where V and V' do, which is what makes the vector-space reading a conversion rather than a transport (`VctBilinear_is_RBilinear` by `eq_refl`). What that generality COSTS is stated in the right direction and PROVED, not argued: two-sided bilinearity forces the commutators of R to annihilate every bilinear image (`rbl_commutator`: `(r·s)·β(v,w) ≈ (s·r)·β(v,w)`, with `rbl_commutator_annihilates` and `tensor_commutator`), so over a non-commutative ring this object collapses further than a reader of the commutative case expects — and the vacuity half is machine-checked too, `rbl_commutator_from_commutativity` deriving the same identity from commutativity ALONE with no bilinearity and no additivity, so over a commutative ring the identity carries no information. The converse ("vacuous exactly where R is commutative") is FALSE by trivial modules and is not claimed. Balancedness survives as a theorem, `tensor_balanced : (r·v) ⊗ w ≈ v ⊗ (r·w)`. Built as a direct inductive setoid quotient in Instance/Ab/Tensor.v's and Instance/Sets/Coend.v's shape rather than through #305's free module — `MTerm` has 5 formers and `mt_eq` 18 rules against Ab/Tensor.v's 4 and 11 (the formal scalar former, the four module laws, and the two scalar-action-on-generators rules whose composite IS the balanced law are all new), and NO code is shared with that file, only the design pattern. Strengths measured strict-first: the mediator agrees with β on generators definitionally (`tensor_med_gen`), and — in contrast with Instance/Mod/Free.v, whose counit does not compute because `ump_universal_arrows` is `Qed`-opaque — the mediator extracted from the CLASS is the fixpoint on the nose (`tensor_factor_is_med`), the universal element's own element is `tensor_gen`, and the ℤ witnesses compute (2⊗3 ↦ 6). Four `Fail` probes locate the boundary: formal-sum commutativity and the balanced law are not definitional even at closed ℤ scalars, the ℚ mediator's value is not Leibniz-equal (`Qmult (1#2) (4#1)` reduces to `4#2`, so the boundary is the scalar setoid rather than the mediator — the ℤ form of the same statement DOES hold strictly), and the triangle is a value equality but not a record equality. Non-vacuity pins the object exactly rather than gesturing: `Int_tensor_iso : ℤ ⊗_ℤ ℤ ≅ ℤ` in `RMod Int_Ring` and `Q_tensor_iso : ℚ ⊗_ℚ ℚ ≅ ℚ` in `Vct_F Q_Field`, with negative results (`1⊗1 ≉ 0`, `1⊗1 ≉ 2⊗1`) obtained by mapping OUT through `tensor_med_respects`, since no induction on `mt_eq` could give a negative. DEFERRED and disclosed: the collapse is proved as a general identity but is NOT witnessed strict at a concrete non-commutative ring, because the tree has none (every `RingObject` in it is commutative and `EndRig` yields a rig, there being no `EndRing`); no bifunctoriality and no monoidal structure on `RMod R` (Ab/Tensor.v's `AbTensor_Functor` has no counterpart here); no coefficient uniqueness; no comparison with `Instance/Ab/Tensor.v` at ℤ (an earlier revision added "the ℤ-module structure of an `AbObject` is not in the tree"; CORRECTION, #454: it is, as `Instance/Mod/Cogenerator.v`'s `Ab_to_ZMod : Ab ⟶ RMod Int_Ring`, each group with its own `zsmul` action, and no comparison of the two tensors is built on it; the file's header carries the same correction); and the right⊗left→`Ab` reading over a non-commutative ring is explicitly NOT delivered, being a different universal property whose universal element would live in a different category from V and V'. Distinct from `Instance/FdVect/Tensor.v`, which is the diagonal square endofunctor `TensorSq` and not a tensor of two modules; no name collides
- **Instance/Mod/Monoidal.v**, **Instance/Mod/Closed.v**: Mac Lane §IV.1 p. 80 + §IV.6 p. 98 (`maclane:IV.1:construction4`, `maclane:IV.6:construction1`) — **the tensor-hom adjunction for modules, and with it the tree's FIRST `SymMonClosed` INSTANCE**: `RMod_SymMonClosed (R : RingObject) (Rcomm : …) : @SymMonClosed (RMod R)`, a plain `Definition` whose four explicit fields are filled by `:=` with no tactic and no transport (`smc_is_symmetric := ModSymmetric Rcomm`, `exponent_obj := HomMod R Rcomm`, `exp_iso := exp_iso_Mod`, `ump_exponents' := ump_exponents_Mod`), the remaining three being the class's own derived `curry'`/`uncurry'`/`eval'`. Before this the class had ZERO inhabitants tree-wide — only its declaration site — so the parametric results over it, notably `Structure/Monoidal/Dual.v`'s self-adjoint dual functor whose own header records "no concrete witness category, so every result is a conditional", now have a model; `docs/INHABITATION.md`'s row is corrected in the same commit. **THE PENTAGON IS DEFINITIONAL, AND THAT IS THE FILE'S MOST SURPRISING FACT**: `tensor_med`'s underlying map is a `Fixpoint` computing on constructors, so both reassociations send `((a⊗b)⊗c)⊗d` to `a⊗(b⊗(c⊗d))` on the nose and the obligation closes by `simpl; apply mt_refl` — the riskiest-looking field is the cheapest. **Only THREE of `ModMonoidal`'s eight obligations carry mathematical content** — the two `to_unit_*_natural` (each `rm_map_smul`) and `triangle_identity` (`tensor_balanced`); the other five, and all four `Braided`/`Symmetric` fields, close by conversion. **THE ENABLING GADGET** is making generator-insertion a first-class module homomorphism (`gen_r b : A ~> A ⊗ B`, `gen_l`), after which iterated extensionality (`tensor_hom_ext_l`/`_r`/`_ll`) is COMPOSITION rather than induction — there is not one induction over `MTerm` in either file, where `Instance/Ab/Monoidal.v` needs an `AgreeOnL`/`AgreeOnR` predicate layer (measured on the same basis: 129 lines here against 165 there — the two cited ranges are 98 and 67 lines, the contiguous span 603-775 being 173, so 165 is the like-for-like figure; a narrow comparison and stated as such). **COMMUTATIVITY IS CONFINED TO FOUR SITES AND THE CLAIM IS STRUCTURAL, NOT MERELY MEASURED**: three in the monoidal half (`act_bilin`, `act_bilin_r`, `rmod_hom_smul`, all of shape `r·(s·v) ≈ s·(r·v)`) and exactly one in the closed half (`hm_smul_linear`, "r·f is still linear"). The monoidal file is arranged so that `tensor_hom_ext_ll`, `braid_bilin`/`ModBraid` and both inner associator legs sit in sections that do NOT bind `Rcomm`, so no proof in them COULD have used it — and in the closed file the repo's `Set Default Proof Using` MACHINE-MEASURES it, demanding `Proof using R Rcomm` on that one lemma and no other. Note in particular that **the braiding itself needs no commutativity**; only the monoidal base it sits on does. Commutativity is spelled as a bare hypothesis matching `Instance/Rng.v`'s `CRng_Sub` predicate — the same proposition after elaboration, though written with an explicit binder annotation and with `ring_rig R` where the predicate leans on the coercion, so NOT character for character — so a `CRng` object supplies it by projection and `Int_Ring_commutative` discharges it at ℤ — the category is NOT `CRng`-indexed, which would force unpacking a sigma at every use site. **NO BRAID IS NEEDED IN THE CLOSED HALF**: the tree's `exp_iso {x y z} : x ⨂ y ~> z ≊ x ~> y ⇒ z` with `eval' : (x ⇒ y) ⨂ x ~> y` matches the module convention `Hom(V⊗W,X) ≅ Hom(V,Hom(W,X))` on the nose, with no reordering. **PRIOR ART, MEASURED**: `HomMod` is NOT a first sighting — `Instance/FdVect/DoubleDual.v`'s `DualMod` is already the `W := R` case with the same pointwise action over a `FieldObject`, and its header already localises the one use of commutativity to the same lemma; `HomMod` generalises in two directions (arbitrary target, commutative `RingObject`). `rmod_hom_neg` is likewise TAKEN there, hence `hm_neg`. **The JOIN between the two halves corrected a scout's premise**: the tensor's `fmap` is `mt_fmap`, built on `tensor_med` whose generator equation is `eq_refl`, NOT on `tensor_factor` whose commutes-lemma is `Qed` — and taking the former made every proof strictly cheaper, so the three BACKWARD naturality squares need `tensor_hom_ext` and nothing else, while the three FORWARD ones are `reflexivity`. Both directions are stated deliberately, since the forward triviality alone could be mistaken for vacuity. **216/216 constants closed under the global context** (123 in Monoidal, 93 in Closed, counted as DISTINCT `Print Module` names and each queried fully qualified), with 112 source-declared names and ZERO collisions tree-wide — two were found and renamed away before landing, `assoc_to` (`Construction/Day.v`) and `probe_instrument` (`Test/ProbePolynomial.v`), the hazard being that `make print-assumptions` loads many modules into ONE scope. **TWENTY-ONE negatives across the two files, none vacuous** (8 + 13, each stripped and run alone; the two scope-free instrument checks are guards and are NOT counted among them, so the pair carries twenty-three `Fail` commands in all), spanning FOUR genuinely distinct kinds told apart by the error text — TYPING (plain mismatch, no `cannot unify`), CONVERSION, RESOLUTION (`Cannot infer this placeholder`, for `ModMonoidal _` at an abstract ring) and FORMABILITY (universe) — plus a scope-free instrument check in each. A UNIVERSE FINDING that is the packaging's own: `ModMonoidal@{u u0 u1 u2}` carries `u = u0`, identifying the `RingObject`'s first two universes, while ELEVEN applied controls — including `@Monoidal (RMod Ru)` and `Ring_RMod Ru`, the two obvious suspects — elaborate at levels declared apart, so the equation is introduced by the packaging and not by any field it packages; not attributed further and not claimed unavoidable. NOT delivered: no proof that ⊗ is not the categorical product, so "first non-cartesian witness" is NOT machine-checked (`RMod R` does have biproducts via `Instance/Mod/Coproduct.v`'s `RMod_Biproducts`, whence `RMod_Cartesian`, so `×` exists and is the direct sum; separating it is an easy-looking extra theorem); no `StarAutonomous` instance, which additionally needs a dualizing object; no Kan/representability route (#348) and no proof it agrees with the universal-property construction; no non-commutative `Hom : (R-Mod)^op ∏ R-Mod ⟶ Ab` variant (since #454 its one-variable slices exist for every ring, `Functor/Representable/Additive.v`'s `HomAb (RMod_AbEnriched R) A` and `CoHomAb (RMod_AbEnriched R) A`, and the non-commutative hom-tensor adjunctions into `Ab` are `Instance/Mod/HomTensor.v`'s; the bifunctor itself is still absent); no distributivity corollary via `left_adjoint_preserves_colimits`; no `⊣` record, the adjunction being delivered as the hom-setoid isomorphism plus `ump_exponents'`; and no naturality in `R`.
- **Instance/Mod/BaseChange.v**, **Instance/Mod/Coextension.v**: Mac Lane §IV.2 Exercise 2 (book p. 89, `maclane:IV.2:ex2`) — **the forgetful functor `RMod R ⟶ Ab` has BOTH adjoints**, extension of scalars along ℤ ⟶ R on the left and coextension on the right: **`zext_adjunction R : Adjunction (ZExt R) (RMod_Forget_Ab R)`** and **`coex_adjunction R : Adjunction (RMod_Forget_Ab R) (Coextension R)`**. **THE EXERCISE'S OWN REVIEWER BAR IS THE UNIT AND COUNIT, AND BOTH ARE MET AT LEIBNIZ `=`, NOT MERELY `≈`**: `zext_unit_is_gen` records that the unit at `a` IS `ts_gen (rig_one …) a` — Mac Lane's `a ↦ 1 ⊗ a` — and `coex_counit_is_eval_at_one` that the counit at `φ` IS `φ (rig_one …)`, evaluation at 1; `coex_counit` is moreover DEFINED as the genuine `@counit … coex_adjunction` rather than a lookalike, so the identification is of the adjunction's own component and not of a parallel construction. **NEITHER ADJOINT NEEDS R COMMUTATIVE, AND THAT IS MEASURED RATHER THAN ASSERTED**: each file's only `Context` is `(R : RingObject)`, `zext_act_assoc` and `coex_act_assoc` close by `rig_mul_assoc` ALONE, and the two `rm_map_smul` obligations by a single `rm_smul_assoc` each — the TARGET module's in the extension (`zext_from` produces `ZExtObj A ~> M`) and the SOURCE module's in the coextension (`coex_to` consumes `M ~{RMod R}~> …`), a distinction both file headers draw and which "the target's" would flatten — **a real contrast with `Instance/Mod/Closed.v`, which does need commutativity**, and the reason is structural rather than incidental: there `r·φ` must be shown LINEAR, which forces an exchange of two scalars, whereas here the scalar only ever translates the ring argument (coextension) or acts on the LEFT factor of `R ⊗ A` while the module element carries no scalar of its own (extension). What the two halves DO spend is itemized: extension consumes both unit laws at DIFFERENT sites (`rig_mul_one_l` for `1·x ≈ x`, `rig_mul_one_r` for the `from∘to` round trip), and coextension consumes both distributivity laws AND both unit laws at four separate sites — over a non-commutative ring those are four independent axioms. Coextension's handedness `(r·f)(s) := f (s · r)` is the one giving a LEFT module and is pinned, the swapped spelling `f (r · s)` giving an anti-action. **THE DONOR IS THE ℤ-TENSOR OF ABELIAN GROUPS, NOT THE R-TENSOR** — `Instance/Ab/Tensor.v`'s `AbTensor` at `(ring_ab R, A)` with `Bilinear`/`tensor_ump`/`tensor_hom_ext`, whose additivity in each variable IS ℤ-bilinearity — a distinction the issue's own work-item 4 blurs by pointing at the module tensor-hom development; and the coextension object needed no construction at all, `hom_ab Ab_AbEnriched (ring_ab R) A` (`Adjunction/Additive.v` + `Structure/AbCategory.v`) already being the additive maps `R → A` as an abelian group, so only the R-action is added. **NO INDUCTION AND NO `Fixpoint` IN EITHER FILE**: all four module laws, both functor laws and both isomorphism laws go through `tensor_hom_ext` or pointwise `CMonHom` laws. **THIS IS NOT #312 RE-DONE, AND THE DISTINCTION IS MEASURED**: `Instance/Mod/Extension.v` (924 lines, `_CoqProject`) is extension of scalars along an ARBITRARY ring hom `φ : R ~{Rng}~> S` over the R-tensor, and its `extend_restrict_adjunction` has right adjoint **`Restrict φ`, not `RMod_Forget_Ab`**; since there is **no `Ab ≃ RMod Int_Ring` anywhere in tree**, `Ab ⟶ RMod R` is not an instance of `RMod R ⟶ RMod S`. **THE ISSUE'S "Current state" IS STALE ON ALL FOUR OF ITS ABSENCE CLAIMS** — most consequentially `RMod_Forget_Ab : RMod R ⟶ Ab` was ALREADY BUILT (`Instance/Mod.v`, `fobj := rm_ab`, `fmap := rm_hom`), so its work item 1 was discharged before this commit began; the file placement also departs from its suggested `Instance/Module/BaseChange.v` because its own text says "or wherever the module categories land once they exist", and they landed in `Instance/Mod/`. **140/140 constants closed under the global context** — 67 in BaseChange (43 source + 24 `Program` obligations) and 73 in Coextension (49 + 24), counted as DISTINCT `Print Module` names and each queried fully qualified — with **zero tree-wide collisions across all 140**, the sweep instrument-checked. **24 `Fail` commands = 22 negatives + 2 scope-free instrument checks**, in three kinds told apart by the error text (10 conversion, 11 formability, 3 typing), each stripped and compiled ALONE and each verified to fire at its OWN command rather than earlier. Non-degeneracy is proved by mapping OUT through the counit and `discriminate` — no induction over a quotient relation could yield a negative — with ℤ witnesses that COMPUTE (`3 ⊗ 4 ↦ 12`). NOT delivered, and disclosed rather than implied away: **the three in-tree records of "no `Ab ⟶ RMod _` exists"** (`Instance/Ab/Free.v`, `Instance/Mod/Quotient.v`, `Instance/Mod/Tensor.v`) **are NOT claimed discharged** — at `R := ℤ` this sends `A` to `ℤ ⊗ A`, not to `A` carrying its own ℤ-action, and that difference is itself pinned as a negative (CORRECTION, #454: the three records are false since #454 as records of ABSENCE — `Instance/Mod/Cogenerator.v`'s `Ab_to_ZMod : Ab ⟶ RMod Int_Ring` carries each abelian group with its own ℤ-action `zsmul` (`Ab_to_ZMod_group`, `Ab_to_ZMod_smul` and `Ab_to_ZMod_fmap` at `eq_refl`), and each of the three files, with this pair's two headers and `Instance/Mod/Closed.v` and `Instance/Mod/Monoidal.v`, carries the correction in place; what the reductions those records describe also need is still absent, a proof that every ℤ-module's action agrees with `zsmul`, hence `Ab ≃ RMod Int_Ring`, and no comparison of `Ab_to_ZMod` with `ZExt Int_Ring` is built); no monad or comonad from either adjunction; no triple `Extension ⊣ Forget ⊣ Coextension` packaged as one object; no comparison with #312 at `φ : ℤ → R` (which would need the missing `Ab ≃ RMod Int_Ring`); and no naturality of either identification in R. Three further disclosures the first draft omitted: **`make todo` grows by 33 lines** (12 + 21), all `Fail`-family matches of its `(Fail|abort|admit|undefined|jww)` pattern, with zero TODO/FIXME/jww added — the same growth prior probe-carrying commits in this tree disclose; the issue's Verification block pins `extension_of_scalars_adjunction`/`coextension_of_scalars_adjunction`, **neither of which exists**, the delivered names being `zext_adjunction`/`coex_adjunction`; and read the GRADE on the axiom figure — 140/140 is a ONE-TIME measurement, while the `make print-assumptions` gate carries **22 of the 140** permanently. The commutativity generality is established AT THE SIGNATURE and **no in-tree witness exercises it** — both concrete instantiations are at the commutative `Int_Ring`, and the tree's one closed non-commutative `RingObject`, `UT2`, is not instantiated here
- **Instance/Mod/Bimodule.v**: Mac Lane §IV.8 Exercise 3 (book p. 104, `maclane:IV.8:ex3`) + Riehl §4.4 Example 4.4.8 and Definition 4.4.7 (printed p. 152, `riehl:4.4:example8`) + Riehl §4.6 Corollary 4.6.10 (printed p. 169, `riehl:4.6:cor10`) — **tensoring with a bimodule is left adjoint to the hom out of it, between two DIFFERENT module categories; the family is an adjunction with the bimodule as parameter; the composite of two such adjunctions is the adjunction of the tensor of the two bimodules, up to natural isomorphism on BOTH sides**. **THE PAGE FIXES THE HANDEDNESS, AND THE ISSUE'S PARAPHRASE OMITS IT**: Mac Lane writes `− ⊗_R E : Mod_R → Mod_S` with right adjoint `hom_S(E, −)` over RIGHT modules, so the file works over `ModR R := RMod (Ring_op R)` (`Instance/Mod.v`) — `Ring_op (Ring_op R) = R` is REFUTED at `eq_refl` though all six data fields agree (pinned), so nothing here writes `ModR (Ring_op R) = RMod R`. **ONE generic balanced tensor serves every handedness**: `BalTensor N M`, the balanced tensor of a right X-module and a left X-module as an `AbObject`, by generators and relations in `Instance/Ab/Tensor.v`'s four-former shape plus ONE balance constructor, with the UMP `bal_med` for `BalBiadditive` maps (`bal_med_gen` at `eq_refl`) and the extensionality lemma `bal_hom_ext` — `Instance/Mod/Tensor.v`'s `TensorMod`/`RBilinear` serve NEITHER handedness (four typing refusals, pinned), and the HAZARD that `@TensorMod (Ring_op R) N (bimodule_right_RMod F)` TYPECHECKS while computing the tensor of two RIGHT modules is shipped as an accepted control beside a refutation of its ring. Part (a): `RTensor E N : RModObject (Ring_op S)` with the right S-action by the mediator of `(n,e) ↦ n ⊗ (e ⊲ s)` (balanced by `bm_compat` — NO centrality, which is what doing it bimodule-aware buys over `Instance/Mod/Extension.v`), `TensorWith E : ModR R ⟶ ModR S`, `HomSObj E M` the group `hom_ab (RMod_AbEnriched (Ring_op S)) E M` of right-S-module maps made a right R-module BY TRANSLATION through E's LEFT action (`(f ⊲ r)(e) := f (r·e)`, associativity only, the `Instance/Mod/Coextension.v` idiom; `Instance/Mod/Closed.v`'s `HomMod` takes commutativity at its signature and cannot host it), `HomS E : ModR S ⟶ ModR R`, and **`bimodule_tensor_hom_adjunction : TensorWith E ⊣ HomS E`** through `Build_Adjunction'` — **RESIDUE-FREE**: both round trips and both naturality clauses close by `reflexivity`, and the unit `n ↦ (e ↦ n ⊗ e)` and counit `f ⊗ e ↦ f e` read back at `eq_refl` (`bth_unit_is_gen`, `bth_counit_is_eval`), because the forward transpose READS a generator and the backward one WRITES one. By-product: **`RMod_AbEnriched : AbEnriched (RMod R)`**, thirteen lines over `rmod_hom_negate`, the gap `Instance/Mod/Coextension.v` records. Part (b): **`BimodCat R S`** (homs `BimodHom` = an `RModHom` on `bm_left` plus right-linearity, none existed by shape), the BIFUNCTOR `BimodTensor : ModR R ∏ BimodCat R S ⟶ ModR S` (built directly — `Build_Bifunctor` recovers its partials only up to `≈`, where `bt_partial_adj` needs `Partial_l BimodTensor E` to BE `TensorWith E` by conversion, which it is), **`bimodule_parametrized_adjunction : ParametrizedAdjunction BimodTensor`** in `Adjunction/Parameter.v`'s record with `pa_right` at `eq_refl`, and **`bimodule_hom_bifunctor : (BimodCat R S)^op ∏ ModR S ⟶ ModR R`** := #396's `parametrized_right_adjoint_bifunctor` — the arrow action of `hom_S(−, M)` in the bimodule variable is #396's mate, CONSUMED and not re-derived, while functoriality of the tensor in the bimodule variable is a PREREQUISITE of that theorem (`Partial_l` spends `bimap` at `(f, id)` and `(id, g)`) and is built here. Part (c): **`BimodTensorBimod E E' : Bimodule R T`** carries BOTH residual actions on ONE group (`btb_carriers_agree` at `eq_refl`, `bm_compat` by reflexivity on generators); **`bimodule_adjunction_composite`** is `Adjunction/Compose.v`'s `Adjunction_Compose` of the two part-(a) adjunctions, its unit and counit the DOUBLE generator and DOUBLE evaluation at `eq_refl`, Mac Lane's whiskered descriptions at `≈` only (both are `Qed`), their `eq_refl` forms refuted and pinned; **`tensor_assoc_iso : TensorWith E' ◯ TensorWith E ≈ TensorWith (BimodTensorBimod E E')`** in `Functor_Setoid`, every clause closing by reflexivity at double generators through the extensionality lemmas `ta_double_ext`/`_r`; and, because transporting an adjunction along a natural isomorphism of its LEFT adjoint existed nowhere in tree (the two neighbours run the other way; `Theory/Equivalence/Adjunction.v` transports along an equivalence of CATEGORIES), the general **`adjunction_along_left_iso : F ≈ F' → F' ⊣ G → F ⊣ G`** was built here (LOAD-BEARING transparent, `btba_to_is_transposed` at `eq_refl`) and moved verbatim to the end of `Theory/Adjunction.v` by #431, whose functor-category remark is its second consumer, whence `bimodule_tensor_bimod_adjunction` and, by `right_adjoint_iso`, **`bimodule_hom_composite_iso : HomS E ◯ HomS E' ≈ HomS (BimodTensorBimod E E')`** — the composite IS the adjunction of the tensor of the bimodules on both sides; both whole-record identifications are REFUTED and pinned. **Riehl 4.6.10 at her own LEFT-module handedness**: `LTensorWith E : RMod S ⟶ RMod R ⊣ LHomS E` (`bimodule_left_tensor_hom_adjunction`, unit and counit at `eq_refl`, a fourth instantiation of `BalTensor`) and `bimodule_left_tensor_preserves_colimits : PreservesAllColimits (LTensorWith E)` := `left_adjoint_preserves_colimits` (`Adjunction/Continuity.v`) with the right-module twin and the composite's `bimodule_tensor_bimod_preserves_colimits` — ALL colimits, strictly stronger than right exactness, the finite-colimit vocabulary of #546 not existing (`right exact` 0 declarations); Cor 4.6.9's additivity is NOT delivered. **Riehl's biclosed box at Mac Lane's handedness**: `HomAbBimod N M : Bimodule R S` (the group of Ab-homs `N → M` with left R-action through N's right action and right S-action through M's, `bm_compat` POINTWISE with no law consumed), `HomAbFunctor N : ModR S ⟶ BimodCat R S`, `hab_partial_adj N : Partial_r BimodTensor N ⊣ HomAbFunctor N` (the mirror hypothesis of `Adjunction/Parameter.v`), `bimodule_mirror_family`, **`bimodule_two_variable_adjunction`** (`mutually_right_adjoint` applied, an `AdjointOnTheRight`) and `bimodule_third_leg` with both hom-setoids written out — all three legs of Def 4.4.7 as inhabitants of the tree's own records. **Riehl's general three-ring form is NOT built and the ℤ route is closed off**: a right R-module is NOT a (ℤ,R)-bimodule at `eq_refl` (`bm_left` demands `RModObject Int_Ring`; pinned as typing refusals) and `Instance/Rng/Mod.v`'s `ZRestrict` is pinned at `RingObject@{Set Set Set}`; and her sentence "making Hom_R(M,N) into an R-T bimodule" is a MISPRINT for S-T, her own display landing it in `_S Mod_T`. Witness at `Int_Bimodule` (`Instance/Mod.v`): the tensor, the composite bimodule, both legs of the associator, the left action and both `HomAbBimod` actions COMPUTE by `eq_refl`, and `int_tensor_separates` keeps two generators apart by mapping OUT. Spend ledger: `bm_compat` spent exactly FOUR times (the two residual-action balances and the two translation actions), associativity only, `Rcomm`/`rig_mul_comm` zero below the header, no centrality. **Universes off BOTH binder and block, with an ISOLATING experiment that REFUTED the first attribution**: `BalTensor`, `RTensor`, `TensorWith`, `BimodCat`, `BimodTensor`, `LTensor`, `LTensorWith`, `BimodTensorBimod`, `bimodule_third_leg` and all three `*_preserves_colimits` carry NO equation; `HomSObj` carries four (`u2 = u6`, `u2 = u10`, `u2 = u11`, `u2 = u12`) and `HomS` and the adjunction those plus `u = u2`, the composite ten, `HomAbBimod` five, `tensor_assoc_iso` two (`adjunction_along_left_iso`'s one moved with it) — and the donor of `HomSObj`'s four is NOT `hom_ab`, NOT `AbEnriched`, NOT `RMod` and NOT their application, each measured to carry none at top level: the SAME body inside a `Section` whose `Context` binds the bimodule carries all four, so the donor is the SECTION VARIABLE (a Context fixes its universes once and the body then EQUATES where a parameter merely BOUNDS), confirmed against the file itself by `bimodule_third_leg` (top level, none) against `bimodule_two_variable_adjunction` (in a section, one); not claimed unavoidable, no repair attempted; no word-bounded `Set` in the block or binder of any of the 427 constants (440 before #431's move), the only `Set` tokens in the whole `About` dump being the motive sorts of the two auto-generated `_rec` eliminators. **That last clause is now false and the arithmetic around it was NOT re-measured.** In the PR "algebraic carriers are sets" (2026-09-17) `bs_eq` became a `Prop` inductive, which generates `_ind` and `_sind` only, so `bs_eq_rect` and `bs_eq_rec` no longer exist — the `print-assumptions` recipe gated both and stopped compiling until the two lines were removed, and the recipe carries a note saying why, keyed to the phrase "bs_eq_rect and bs_eq_rec were gated here". Consequently "the two auto-generated `_rec` eliminators" is at most one, and the 427/409/213 census, the "eight generated eliminators" reading and the 82/68 `eq_refl` counts are PRE-PR figures that no one has re-run by their own stated criterion (`Print Module` at five-space indent); what WAS re-measured here on 2026-09-17 is the `.glob` head count, 233 (`grep -c '^def '` 176, `'^prf '` 48, `'^rec '` 2, `'^proj '` 7) with `grep -c '_obligation_'` returning 0, a DIFFERENT criterion from the one the figures above use and not a correction of them. **427/427 constants closed under the global context** (an earlier revision said 440/440, 422, 198 and 220, before #431 moved the thirteen AdjunctionAlongIso constants to `Theory/Adjunction.v`; 409 `Print Module` entries at five-space indent — 192 `Definition`, 213 `Parameter`, and the 2 `Inductive` and 2 `Record` heads that WRAP onto their own line — plus 16 constructors and 2 `Build_*`; zero `Axioms:`), zero collisions over all 440 (427 now) (three avoided by construction: `rmod_hom_neg`, `HomFrom` — `Functor/Hom/Limit.v`'s — and `unit`), five `Defined` of which exactly TWO are load-bearing by flipping each alone (`bimodule_tensor_hom_adjunction`, `bimodule_left_tensor_hom_adjunction`; the third, `adjunction_along_left_iso`, moved to `Theory/Adjunction.v` with #431), 82 `eq_refl` occurrences (68 of them below the header; 83 and 69 before #431 moved [aali_to_is_transpose], one per `Example`) and exactly FOUR identifications at `≈` only, all four pinned (part (c)'s two whole-record comparisons, `tensor_assoc_iso` and `bimodule_hom_composite_iso`, whose sides are different `Functor` records, and the two whiskered descriptions, whose donors are `Qed`); closure 93 excluding the file (distinct `.v` in `coqdep -sort` minus the argument; marginals `Adjunction/Parameter` 11, `Adjunction/Compose` 2, `Adjunction/Additive` 1, `Adjunction/Continuity` 1, `Instance/Mod` 1, every other `Require` 0; `Instance/Mod/Extension.v` NOT required — it would add twelve modules (94 to 106, measured) through the Grothendieck stack and needs `CentralImage`; `Instance/Mod/Closed.v` NOT required). `Test/ProbeBimodule401.v` mirrors the target's `Require` list and carries **18 `Fail`s = 1 instrument + 17 negatives of THREE kinds told apart by the error TEXT** — 6 CONVERSION, 9 TYPING, 2 FORMABILITY (`AbEnriched` and `hom_ab` each forcing hom = proof in their binders; the latter fires at its `AbEnriched` argument and says so, the #340 trap) — each stripped ONE AT A TIME and compiled alone with its whole error read; guard coverage measured mechanically (50 identifiers inside the negatives, 40 also outside, the 10 exceptions exhaustively the keyword, one bound variable, the seven refuted declaration names and the instrument's absent name); rename-simulated 5/5 over the target constants a negative names, every break on a `Check` line. `make todo` grows by 28, ALL in the probe (18 `Fail` commands + 10 prose lines), the library file contributing ZERO — so the issue's "adds no new hits" box is not met as written and is disclosed. **The issue's "Current state" is stale on every count** (modules, the `Bimodule` record at `Instance/Mod.v`, tensors and `Adjunction/Parameter.v` all existed; only the category of bimodules, the right-module tensor and the hom-module with a residual action were absent, measured by shape), its suggested `Instance/Module/` directory does not exist (the file sits under `Instance/Mod/` with the tree's other module files), and its `Theory/Adjunction.v` citation is a `reflexivity` line inside `right_adjoint_iso`'s proof. NOT delivered: Riehl's general three-ring biclosed form; any right-exactness or finite-colimit vocabulary and Cor 4.6.9's additivity; the ONE-RING tensor-hom parametrized adjunction that `Adjunction/Parameter.v` defers "beside the module categories" — measured to compile in 22 lines of substance (its two requires cost this file +9 modules, 94 to 103), NOT shipped, #396's follow-on; any coherence for the bimodule tensor (no pentagon, no unit, no bicategory of rings and bimodules) and no naturality of `tensor_assoc_iso` in `E` or `E'`; any relation between `HomAbBimod` and `HomSObj`; limit preservation for the hom functors; nothing registered as an `Instance` beyond the two setoid instances the categorical structure needs, `bs_eq_Equivalence` and `BimodHom_Setoid`, both `#[export]` and resolvable downstream (measured)
- **Instance/Lie.v**, **Adjunction/Enveloping.v**: Mac Lane §IV.2 Exercise 3 (book p. 89, `maclane:IV.2:ex3`) — **the universal enveloping algebra is left adjoint to the commutator functor**, `enveloping_adjunction K : Adjunction (EnvelopingFunctor K) (CommutatorFunctor K)` over a commutative ring `K : CRng`. **THE EXERCISE'S OWN REVIEWER CHECK IS THAT THE ADJUNCTION MUST NOT APPEAL TO POINCARÉ–BIRKHOFF–WITT, AND THAT IS STRUCTURAL HERE RATHER THAN CAREFUL BOOKKEEPING**: the universal property is one five-case induction over `UEnvTerm` and one twenty-two-case induction over `uenv_eq`, and the strings `PBW`/`basis`/`filtration` occur nowhere below the header — measured against the header's extent, so every occurrence is inside it. **PBW is DEFERRED, neither proved nor assumed**, and what it would buy is listed as not claimed: injectivity of `uenv_insert`, `L ↪ U(L)`, faithfulness of `EnvelopingFunctor`, and `U(abelian) =` the polynomial algebra; `uenv_ut2_gens_distinct` is explicitly flagged as NOT an instance of PBW, separating two specific generators of one Lie algebra. **A SHARPER MEASUREMENT THE EXERCISE DOES NOT ASK FOR: NO LIE AXIOM IS CONSUMED ANYWHERE IN THE ENVELOPING FILE** — `lie_alt`, `lie_jacobi`, `lie_br_add_l`/`_r`, `lie_br_smul_l`/`_r`, `lie_antisym` and `lie_br_respects` occur ZERO times; only `lie_map_br`, the Lie MAP's bracket law, is spent, at exactly one line, where `exact (lie_map_br f x y)` IS the whole proof of the `uq_bracket` case. The header scopes that at its strength and explicitly declines the generalization. **ANTISYMMETRY IS DERIVED FROM ALTERNATING, AND THE ECONOMY IS MEASURED AT THE SIGNATURE RATHER THAN ASSERTED**: `Lib.v` sets `Default Proof Using "Type"`, so explicit `Proof using` clauses make `About` report what each direction spends — `lie_antisym_of_alt` needs BOTH additivity laws (expanding `[x+y,x+y]` splits each slot) while the converse `lie_alt_of_antisym` needs `TwoRegular` and NO additivity at all, which is why alternating is primitive and the two come apart in characteristic 2 — a DESIGN PRECEDENT rather than a new move, `Instance/Vect/TensorAlgebra.v` already running the identical alternating-primitive / antisymmetry-derived / char-2 argument for the exterior algebra, and like it this file discloses that NO COUNTEREXAMPLE IS BUILT. Correspondingly the record carries additivity in both slots but K-homogeneity in the FIRST only, `lie_br_smul_r` being a theorem. `aalg_central` is spent exactly once in each file. `lie_comm_jacobi` spends associativity of multiplication and commutativity of addition **and, inside `lie_comm_triple`, BOTH distributivity laws plus the negation calculus** — an earlier draft said "only" the first two and was wrong; what it genuinely spends NEITHER of is centrality nor commutativity of multiplication, both verified zero at the proof term. **THE `U(g)` ROUTE WAS CHOSEN ON A MEASUREMENT, AND IT CORRECTS THE ISSUE'S OWN PREMISE**: the issue warns that `Construction/Quotient.v` is a HOM-congruence quotient so an object-level quotient may have to be built — true of that module, but it does NOT follow that one must be built, since `Instance/Rng/Quotient.v` already carries `Ideal`, `QuotientRing`, `rquot_proj`, `rquot_proj_epic` and `rquot_med`. So quotienting `TensorAlg` was AVAILABLE, not blocked. What it would still have cost was measured: all FIVE in-tree `Ideal` inhabitants are closed-form membership predicates, so **no ideal generated by a set exists anywhere**, and the `Require` closure would grow from 53 modules to 66. Generators-and-relations — the idiom `TensorAlg` and `Instance/Rng/MonoidRing.v` already use — was taken instead; **no route-(a) version was built, so nothing claims it fails**. **211/211 constants closed under the global context** — 101 in Lie (99 `Print Module` names + the two `Build_*` constructors it omits) and 110 in Enveloping (63 source heads + 5 `UEnvTerm` and 22 `uenv_eq` constructors + 8 generated eliminators + 12 `Program` obligations) — each queried fully qualified, zero `Axioms:` lines. **Zero tree-wide collisions ACROSS DECLARATION HEADS**, which is the scope both file headers state and the scope that matters. Do NOT read that as "only two `Lie` hits exist": an unrestricted `\bLie\b` sweep returns FIVE lines in FOUR files — `Theory/Lawvere.v`, `Instance/Roster.v`, `Instance/Rng/Frac.v`, and `Structure/Group.v` twice — every one of them prose, none a declaration, and the repo's own `doc/plan/books/maclane/coverage/verified-IV-1.json` already lists the two `Structure/Group.v` ones. **11 `Fail` commands = 9 negatives in THREE kinds (5 conversion, 3 typing, 1 resolution) + 2 scope-free instrument checks**, each stripped ONE AT A TIME with the others left as `Fail`, compiled alone, and verified to fire at its own command; the resolution one reports `Cannot infer the implicit parameter Setoid of equiv … no type class instance found`. No formability negative in either file, and both headers say why. **NON-DEGENERACY GETS PAST THE ABELIAN CASE, which is the real risk here** since a commutative algebra has zero bracket and would witness nothing: `Lie_UT2` has `[E₁₁,E₁₂] = E₁₂` and `[E₁₂,E₁₁] = (0,−1,0)` both by `eq_refl` (UT2's hom-setoid is Leibniz on ℤ-triples), so `lie_ut2_not_abelian` closes by `discriminate`, and the enveloping side pairs `uenv_abelian_gens_commute` against `uenv_ut2_gens_do_not_commute` — every negative by mapping OUT, since no induction over a quotienting relation could yield one. **`UT2` is NOT the tree's only non-commutative `RingObject`** — `TensorRing` and `MonoidRing` are also proved non-commutative — and no total is given, because a NAME-ANCHORED declaration-head sweep (`Definition <name> : RingObject`) undercounts — the bare literal `: RingObject` does match the line, so only the anchored form misses it: `Theory/Algebra/Rig.v`'s `Int_Ring` — declared there and nowhere else, `Instance/Rng.v` merely using it hides behind a universe annotation between name and colon. What selects `UT2` is its COMPUTING hom-setoid. `uenv_universal_med` records that the mediator the universal property produces IS the fixpoint extension (which is why `uenv_universal` is `Defined`), and `uenv_unit_is_gen` that the unit sends a generator to itself. NOT delivered: PBW as above; no Lawvere-theory presentation; no `Instance/Lie.v` analogue of the free Lie algebra; nothing about Lie groups — `Instance/Rng/Frac.v`'s "Lie half" deferral is the Lie algebra of a Lie GROUP (§I.3), a different catalog item, and is NOT discharged; and no comparison between `AAlg` and `KAlg` beyond what `Instance/Rng/Algebras/Associative.v` already carries
- **Instance/Rg.v**, **Adjunction/Unitalization.v**: Mac Lane §IV.2 Exercise 4 (book p. 89, `maclane:IV.2:ex4`) + Riehl §4.7 (`riehl:4.7:construction-unitalization`, printed p. 180) — **the Dorroh extension freely adjoins an identity to a rng and is LEFT ADJOINT to the forgetful functor from unital rings to rngs**: `unitalization_adjunction : DorrohFunctor ⊣ Rng_Forget_Rg` (the issue's pinned name), over the new category **`Rg`** of rings not assumed to have a multiplicative identity. **THE ISSUE'S "Current state" IS STALE**: it says "there is no category of rings, unital or otherwise", whereas `Instance/Rng.v`'s `Rng := Ring` (UNITAL) has existed since #257; its own QA correction acknowledges this and fixes the name `Rg`, since `Rng` and `Theory/Algebra/Rig.v`'s `Ring`/`RingObject` are both taken. **THE DESIGN DECISION THAT PAYS FOR EVERYTHING IS BUILDING `RgObject` ON `AbObject` RATHER THAN ON `RigObject`** — SIX fields on top of `AbObject`'s four, against `RigObject`'s SEVENTEEN (measured; an earlier brief of mine said fifteen and a builder corrected it) — because it makes `Instance/Ab/Monoidal.v`'s ℤ-action `zsmul` apply **with no adapter**, which the Dorroh multiplication needs. `RgHom` extends `Instance/Ab.v`'s `AbHom` with one field, so preservation of negation is the EXISTING `ab_map_neg` cited rather than reproved. **ANNIHILATION IS A THEOREM HERE, NOT A FIELD** (`rg_mul_zero_l`/`_r` via `rg_cancel_idem`), because a rng has additive inverses where a rig does not — `Structure/Ring.v` is the internal precedent and its `bool_or_not_annihilating` is why `RigObject` must carry it — and it is LOAD-BEARING: `Rg_zero_hom`, hence initiality, does not typecheck without it. **★ THE PAYOFF BEYOND THE EXERCISE'S FOUR WORK ITEMS: `Rg_Zero : ZeroObject Rg`.** A rng homomorphism need not preserve 1, so the zero rng is BOTH initial and terminal, whereas `Rng` has ℤ initial (`Rng_Initial_Z`) and the zero ring terminal (`Rng_Terminal_zero`), which do not coincide. `Instance/Roster.v` had recorded that exact contrast as "NOT checkable here: the tree has no category of non-unital rings" — **that sentence and two more (`Instance/Rng.v`, `Instance/Roster.v`) are repaired in this commit** — and a fourth defect, of my own making, was caught only by audit: because the builder was told not to touch Roster.v and I edited it afterwards, the delivered `Instance/Rg.v` asserted in the PRESENT tense that Roster.v still carried the superseded sentence AND that "Roster.v is NOT edited by this file", both of which this very commit falsifies; the header now quotes that sentence explicitly as superseded text, and `Roster.v`'s replacement no longer credits `Rng_Rg_zero_object_contrast` with more than its type proves (its second component is the elementary "no unital hom from the zero ring to ℤ", the packaged `Rng_no_zero_object` being `Examples.v`'s) (the #360/A rule), and `Rng_Rg_zero_object_contrast` packages the two halves. `Rng_Forget_Rg` is pure reuse of `ring_ab` (`Instance/Rng.v`) and `Rng_Forget_Ab`'s fmap shape — all five object fields and the arrow action are `:=` with no tactic — proved Faithful (cheap, and the header says why rather than dressing it up) and, as a bonus, NOT Full, via the constant-zero map ℤ → ℤ. **THREE NEW LEMMAS, NOT THE TWO I BRIEFED**: `rg_zsmul_mul_l`/`_r` (the ℤ-action commutes with rng multiplication) AND `zsmul_mul` with `nat_smul_mul` beneath it — **the in-tree `zsmul` API has no multiplicativity in the SCALAR**, which `dorroh_mul_assoc` genuinely needs (verified BY ENUMERATION, and **the criterion has to be stated or the number moves**: declaration heads whose name BEGINS with `zsmul`/`nat_smul`, outside these files, number **23** — fifteen and eight, in two modules — while three further heads CONTAIN the token and are excluded as tensor-generator balance lemmas rather than action-API ones (`ts_gen_nat_smul_l`, `ts_gen_nat_smul_r`, `ts_gen_zsmul_balance`), making the contains-the-token figure **26**. `zsmul_add` gives additivity in the scalar and NONE of the 26 gives `zsmul (m*n) a ≈ zsmul m (zsmul n a)`, so the absence — the load-bearing half — does not turn on the criterion. The count took three passes: my commit first said 22, which is simply wrong; the delivered file said "sixteen and ten", which is RIGHT under the contains criterion it stated; and my first correction to 23 was right on the number but left the file's stated criterion contradicting it — the defect finding 3 was itself about. Naming the three exclusions is what makes it reproducible). Prior art disclosed rather than passed over: `Construction/Enriched/Ab.v`'s `zsmul_precomp`/`zsmul_postcomp` are the analogue of `rg_zsmul_mul_l`/`_r` — NOT of `zsmul_mul`, which the sentence's position in an earlier draft wrongly implied — one level up with a slicker proof; it was found after the sign-split proofs were written and is recorded, not substituted. **A `Set` PIN DEFEATED THE UNIQUENESS ROUTE I RECOMMENDED, AND THE MEASUREMENT IS EXACT**: `rng_from_Z@{u u0 u1} : ∀ R : RingObject@{Set Set Set}` — the TARGET ring, not merely ℤ — so composing with it would have confined the whole adjunction to `Set`-sized rngs; `rig_hom_preserves_zring` replaces it, and the pin is pinned as a FORMABILITY negative with `zring (Dorroh R)` as the discriminating control. Correspondingly `unitalization_adjunction`'s constraint block contains **no universe EQUATION at all** — every entry is `<` or `<=` — and `Rg_trivial@{o}` is built on `Instance/CMon/Biproduct.v`'s polymorphic `CMon_trivial@{o}` rather than on `Ab_trivial@{} : AbObject@{Set Set Set}` (measured: zero binders, the `Grp_trivial` family — a family #450 has since lifted for `Grp_trivial` itself, `About` reading `Grp_trivial@{p} : GrpObject@{p p p}`, while `Ab_trivial@{}` is unchanged), so `Rg_Zero@{u u0}` carries no `Set`. The transpose `(n,a) ↦ zring S n + f a` reuses `zring`/`zring_add`/`zring_mul` (`Instance/Rng.v`) and **deliberately avoids `Instance/Rng/Polynomial.v`'s `zring_central`**, which would add 15 modules; `zring_is_central` falls out of the zsmul bridge instead, and the donor is cross-referenced. **THE QA CORRECTION'S SUGGESTED PLACEMENT — put `Rg` in `Instance/Rng.v` — IS BLOCKED BY A DEPENDENCY CYCLE**, `Instance/Ab/Monoidal.v` requiring `Instance/Rng.v`; but read that honestly, since the cycle bites only because of `zsmul`, and a `Rg`-ONLY addition to that file WOULD have been possible. Require closure 52 modules; the `zsmul` reuse costs 7 (itself plus `Ab/Tensor`, four `Structure/Monoidal/*`, `Theory/Naturality`) against a duplicated 200-line ℤ-action API. **185/185 constants closed under the global context** — 101 in `Rg.v` and 84 in `Unitalization.v`, enumerated by splitting `Print Module` POSITIONALLY (it emits `Module` alone on a line with the qualified name wrapped to the next, so a name-anchored regex finds neither block) unioned with each `.glob`, less the four `Fail`-named probe entries the glob records and the environment never receives, plus the two `Build_*`; each queried fully qualified, zero `Axioms:`. **ONE COLLISION IN 185, AND IT IS A RECORD FIELD** — `rg_mul` against `Structure/Group/Representable.v`, which `Makefile` loads into the `print-assumptions` target's single scope. **A declaration-head sweep CANNOT see it** (`Instance/Rg.v`'s 53 declared heads and the NINE `rg_*` heads among `Structure/Group/Representable.v`'s 100 intersect in nothing — read "9" as the `rg_*` subset, not that file's total), which is the #361 lesson firing again; the mitigation is that all 28 gate entries for these two files are written FULLY QUALIFIED, following the `Powerset.Universal` precedent at `Makefile`, and no bare `rg_*` gate entry exists to be captured. **SIX negatives in THREE KINDS** (3 conversion, 1 typing, 2 formability), each stripped ONE AT A TIME with the others left as `Fail`, compiled alone, and read whole to confirm where it fires: the typing one reports a plain mismatch with NO `cannot unify`; the two formability ones end `Cannot enforce ah = ao` and `Cannot enforce Set = bh`. Strict-first grading: the Dorroh carrier, zero, one, add, neg and mul as written, `dorroh_insert` as `a ↦ (0,a)`, `arrow (dorroh_universal R)`, `fobj[DorrohFunctor]` and the `AUniversalArrow` mediator all hold by `eq_refl`, and `rig_iter_is_nat_smul`/`zring_is_zsmul_one` hold at LEIBNIZ `=`; the **unit of the produced adjunction is REFUTED at `eq_refl` and pinned** (`≈` holds by `reflexivity`), as is the mediator read out of `ump_universal_arrows`, whose donor is `Qed` — a documented in-tree fact, stated rather than fought. Non-vacuity is proved on both sides: `TwoZ_Rg` (the even integers) is proved not unital, not degenerate and not zero-multiplication, with `TwoZ_incl` injective and its image proved to be EXACTLY the evens (the builder added the missing half rather than weakening the word); `DorrohTwoZ` is proved unital with two distinct elements, the insertion proved not surjective, and four computing `eq_refl` Examples including one showing the multiplication is NOT componentwise. **`Rng_no_zero_object` ALREADY EXISTED** at `Structure/Kernel/Universal/Examples.v` — my brief asked for it to be proved, which would have been a false-absence claim; the underlying fact is restated under the fresh name `Rng_terminal_not_initial`, shadowing nothing, with the measured reason for restating rather than requiring (that file's transitive closure is **65** modules through Yoneda — adding 34 to `Instance/Rg.v`'s own 32 — which would land on every downstream consumer of `Rg`; an earlier draft said 23, which is that file's Require-LINE count and not a closure at all, the same category error as counting direct imports for a dependency cost). `make todo` grows by **24** lines (1405 → 1429), all `Fail`-family matches of that target's case-insensitive pattern and only SIX of them commands — a figure worth stating carefully, since the Unitalization builder measured 19 for its own file alone and `Instance/Rg.v` supplies the rest; zero TODO/FIXME/jww added. NOT delivered: no commutative variant and no `CRng`-level statement; no normal form, coefficient uniqueness or decision procedure for `Dorroh`; no comparison with `Instance/Rng/Free.v`'s free ring or with any tensor construction; no monad or comonad from the adjunction; no `Cocartesian Rg` or colimits of any kind; no naturality of any identification in `R`; and the TensorAlgebra faithfulness contrast the issue invites is stated as a proved faithfulness here against an OPEN deferral there (`Instance/Vect/TensorAlgebra.v`), not as a settled comparison
- **Instance/Monoid/Translation.v**: Mac Lane §IV.2 Exercise 5 (book p. 90, `maclane:IV.2:ex5`) — **a monoid is a group exactly when its left translations have right adjoints**, with the monoid viewed as a DISCRETE category on its elements and multiplication as a bifunctor. `mon_group_iff_left_translation_adjoints : MonInverses M ↔ LeftTranslationAdjoints M` and the right-handed twin. **THE CONVERSE — the half the exercise actually asks about and the issue's Definition of Done demands — IS PROVED**, not documented as failing: the INVERSE TRANSPOSE of the adjunction, at the SINGLE pair `x := R(1)`, `y := 1`, applied to the identity of `R(1)`, reads `a·R(1) ≈ 1` — so every element acquires a RIGHT inverse. Read that economy precisely, because an earlier draft of this bullet described a two-instantiation argument that is NOT what the file does: only the backward leg is consumed, at one pair of objects, on one identity, and the word "two-sided" occurs nowhere in the file, and right-inverses-everywhere is a group by `Construction/Deloop.v`'s pre-existing `mon_inverse_unique` — which IS that argument, so invoking it is a ONE-LINE citation rather than a chase — the lemma itself is six tactic lines, and an earlier draft called it two, and no prior "right inverses ⟹ group" over `MonObject` existed (swept; `Instance/Grp.v`'s `grp_mul_inv_r` is the mirror over a DIFFERENT record). **THE CONCLUSION IS THE PRE-EXISTING PREDICATE**, `Structure/Groupoid.v`'s `MonInverses`, with `GrpObject_of`/`MonInverses_of` already giving both passages — nothing is redefined. **★ THE FILE'S REAL CONTENT IS A FORK THE TREE HAD ALREADY DOCUMENTED, RESOLVED BY MEASUREMENT RATHER THAN CHOICE ★**: `Instance/Discrete.v`'s `DiscreteCat` has `hom x y := x = y` — Leibniz proofs under the strict-eq setoid `Morphism_equality` — which is what the issue's reviewer check names, but the FORWARD direction needs `a⁻¹·(a·x) = x` at Leibniz where `minv_l` gives only `≈`, and `Construction/Comma/Special.v` states the general form of that obstruction in terms, calling an isomorphism with `DiscreteCat` "not merely unproven here, it is unavailable" — the quoted phrase is predicated of the ISOMORPHISM, not, as an earlier draft had it, of the collapse `h ≈ h' → h = h'`, which is what such an isomorphism "would require". BOTH readings are therefore delivered: `MonDiscrete M := DiscreteSetoidCat …` (homs `≈`), over which the biconditional carries **NO hypothesis on M beyond being a monoid**; and `MonDiscreteStrict M := DiscreteCat (carrier M)`, meeting the reviewer's literal phrasing on the nose (`mon_discrete_strict_hom : (x ~> y) = (x = y)` by `eq_refl`). **THE COST IS ASYMMETRIC AND THAT IS THE FINDING**: over the strict category the CONVERSE is still free (`strict_left_translation_adjoints_MonInverses` takes no hypothesis) and only the FORWARD half needs `MonStrict` **and** `MonUIP` — the second of which my own brief missed, because `DiscreteCat`'s hom-setoid compares equality PROOFS, so both respectfulness obligations, both isomorphism laws and all four `Adjunction` naturality clauses are equations between proofs (`MonUIP_dec` discharges it by Hedberg for decidable carriers). The deciding measurement is pinned: `mon_inv_cancel_l` has type `… ≈ x` and does NOT typecheck as `… = x`, a TYPING negative with no `cannot unify`, against a passing `≈` control — stated at a VARIABLE monoid deliberately, since at Z/3 the strict form does hold. **THE BIFUNCTOR CLAUSE IS ANSWERED BY MEASUREMENT**: reading the translations off `MonMult` via `Functor/Bifunctor/Partial.v`'s `Partial_r`/`Partial_l` is CHEAPER (zero obligations against three apiece), and the direct records are shipped anyway precisely so the identification becomes a measured fact — `fobj` AND `fmap` agree with the partial applications at `eq_refl`, while the WHOLE-record equality is REFUTED and pinned, `Functor` having primitive projections with eta so the difference is localised to the three opaque law fields. **THE ONE-OBJECT ANALOGUE WAS ALREADY IN TREE AND IS RELATED, NOT DERIVED**: `Structure/Groupoid.v`'s `deloop_groupoid_iff` is "M is a group iff its DELOOPING is a groupoid" — elements as ARROWS — where this is the other categorification, elements as OBJECTS; the header says plainly that neither is claimed to follow from the other. **104/104 constants closed under the global context** (66 declared heads + 38 `Program` obligations; the file declares no `Record`/`Class`/`Inductive`, hence no record fields and no unlisted `Build_*`), **zero collisions**, under two criteria and — the point the count turns on — over two different SETS: the file's own sweep covers the **71 identifiers it introduces** (66 declared heads plus 5 `Fail`-declared probe names), while mine covers all **104 global constants** the module contributes (those same 66 heads plus the 38 `Program` obligations, which are real global names and can collide). Both are defensible; neither number means anything without its criterion, and an earlier draft gave **103** without one — a figure that was ALSO wrong, and wrong for a mundane reason worth recording: the name file had no trailing newline, so `wc -l` under-reported it by one while `grep -c .` gives 104. It contradicted this bullet's own "104/104 constants closed" two clauses later. Instrument-checked against `MonObject` and `MonInverses`, which must collide and do. **FIVE `Fail` commands = 4 negatives in TWO kinds (3 conversion, 1 typing) + a scope-free instrument check**, each stripped one at a time with the others left as `Fail`, compiled alone, and verified to produce exactly one `Error`. Universes: the headline carries **no universe equation and no `Set`**, the monoid's three levels only bounded; `MonDiscreteStrict` needs LOAD-BEARING explicit binders (unannotated, the same body collapses hom and proof); and the strict headline's `Set < _` is on an INTERNAL level (the auxiliary `Sets` of `Adjunction`, `DiscreteCat`'s homs being `Prop`-valued) that does not restrict the monoid — guarded by a probe section declaring all three monoid universes strictly above `Set`, where both headlines still elaborate. An engineering hazard is recorded: an obligation tactic `exact I` set INSIDE a section binding `I : MonInverses M` resolves `I` to the section variable and every obligation fails, where the same tactic at top level does not — measured both ways, and the witnesses are renamed `Inv` because of it. Non-vacuity both ways: (ℕ,+) refutes an adjoint for the single translation by 1 directly (`S (R 0) = 0`, by `discriminate`) as well as through the biconditional; Z/3 builds the adjunction and proves their OBJECT ACTIONS differ at `Z3_0` — `Z3_adjoints_differ` is an inequality of object values closed by `discriminate`, which entails the two functors differ but is not itself a functor inequality, and no functor inequality is stated, and discharges both strict hypotheses; Z/2 ships explicitly LABELLED degenerate, every element being its own inverse. NOT delivered: no equivalence or isomorphism between the two discrete categories; no necessity result for `MonStrict`/`MonUIP`, so neither is shown unavoidable; no strict `RightTranslation`; no unit/counit or triangle identities; no functor between `Deloop M` and `MonDiscrete M`; nothing about monoid homomorphisms; and no registered `Instance`
- **Adjunction/CokernelPair.v**: Mac Lane §IV.2 Exercise 10 (book p. 90, `maclane:IV.2:ex10`) — **the cokernel-pair functor is left adjoint to the equalizer functor**: `CokernelPair_Equalizer_Adjunction : CokernelPairFunctor ⊣ EqualizerArrowFunctor` (the issue's pinned name), with `CokernelPairFunctor : Arrow C ⟶ [Parallel, C]` under `HasPushouts C` and `EqualizerArrowFunctor : [Parallel, C] ⟶ Arrow C` under `HasEqualizers C`. **THE ISSUE'S "Current state" IS STALE**: it says there is no cokernel-pair construction at all, whereas `Theory/Morphisms/CokernelPair.v`'s `cokernel_pair f : IsPushout f f` has existed since #323 with its `ckp_*` accessor family; every one of nine of those is CONSUMED and none re-declared, and the raw `pushout_*` family beneath them is not touched. **What was genuinely absent is measured by TYPE SHAPE, and the criterion is stated**: no declaration head in the tree has type `Arrow _ ⟶ [Parallel, _]` or the reverse — the grep `'@?Arrow <ident> ⟶'` returns FOUR heads out of `Arrow` (`Arrow_dom`/`Arrow_cod`, `Construction/Displayed/Codomain.v`, the Set-pinned `Fun_of_Arrow : Arrow C ⟶ [_2, C]`) and the grep `'⟶ @?Arrow'` returns TWENTY-TWO lines into it — thirteen in `Construction/Arrow/Functor.v` and nine elsewhere, six of those nine being declaration heads (`Theory/Shapes.v`'s `Arrow_of_Fun`, `Construction/Comma/Diagram.v`'s `Comma_to_Arrow`, `Construction/Cylinder/Arrow.v`, `Instance/Cat/Pullback.v`'s `Slice_Arrow`/`Coslice_Arrow`, `Codomain.v`) — none of the twenty-six mentioning `Parallel`; the two `FreeWalkingArrow ⟶ FreeParallelPair` lines a looser grep also returns match on substrings, not categories. **Those counts moved TWICE**: my brief listed four heads, the delivered file said six out and ten in (counting the two substring artifacts IN and then, one paragraph later, disowning them), and an audit measured four and twenty-two; the criterion is now stated beside each number. The parallel-pair category is the FUNCTOR category `[Parallel, C]`, the site `Adjunction/Diagonal/Finite.v`'s `EqualizerFunctor : [Parallel, C] ⟶ C` already uses; a morphism of parallel pairs IS a `Transform`, its two non-identity naturality squares being the two squares. **THE REVIEWER CHECK — both functor laws PROVED, none `Program`-defaulted — is met and the spend is itemised**: each of the six laws (two functors × `fmap_respects`/`fmap_id`/`fmap_comp`) is a cheap branch plus ONE uniqueness appeal, the cokernel-pair side's three spending `ckp_med_unique` (`Theory/Morphisms/CokernelPair.v`) and the equalizer side's three the `uniqueness` of `Structure/Equalizer/Fork.v`'s `eq_desc`. The adjunction is built with `Theory/Adjunction.v`'s `Build_Adjunction'` (the hom-setoid isomorphism plus TWO naturality clauses; the class wants four). **THE UNIT AND COUNIT ARE NAMED, AS THE EXERCISE IS REALLY ABOUT THEM** — `cokernel_pair_unit A = (eq_desc-mediator of the arrow, id)`, Mac Lane's comparison of `f` into the equalizer of its cokernel pair, and `cokernel_pair_counit G` with components `(id, pushout-mediator of (G one, G two))`, the coequalizing comparison — **and their agreement with the class-produced `unit`/`counit` SPLITS BY COMPONENT rather than failing uniformly, which is finer than I predicted**: the unit's SECOND component and the counit's `ParX` component hold at `eq_refl` (`ckp_unit_snd_strict`, `ckp_counit_X_strict` — `nat_id`'s `fmap[ckp_pair A] id` reduces to `id` there), while the unit's FIRST component and the counit's `ParY` component are REFUTED at `eq_refl` and pinned, the residues exhibited: the class descends `id ∘ ar_mor A` where the named unit descends `ar_mor A`, and `parallel_leg ∘ id` against `parallel_leg`; `ckp_class_unit_agrees`/`ckp_class_counit_agrees` are the `≈` controls. **THE EXERCISE'S CONTENT LANDS AS A BICONDITIONAL**: `unit_iso_iff_regular : IsIsomorphism (cokernel_pair_unit A) ↔ RegularCokernelPair A`, where `RegularCokernelPair A := IsEqualizer (ckp_u A) (ckp_v A) (ar_dom A) (ar_mor A)` is a plain `Definition` — NO `RegularMono` class is introduced (the tree has only `RegularEpi`, `Structure/Regular.v`) — with `split_mono_regular` (a split monomorphism IS an equalizer of its own cokernel pair) and hence `split_mono_unit_iso` as the general theorem behind the positive witness. **Non-vacuity at `Sets` in BOTH directions**: the unit at the collapse `bool → 1` is proved NOT an isomorphism (`sets_collapse_unit_not_iso`, by mapping out), the unit at the inclusion `1 → bool` IS one (`sets_pick_unit_iso`), and that inclusion is itself proved NOT an isomorphism (`sets_pick_not_iso`) so the positive case is not degenerate — the equalizer of a cokernel pair in `Sets` being the IMAGE, this is the image factorization's epi part being invertible exactly for injections. **THE `HasEqualizers Sets` ROUTE WAS CHOSEN ON CLOSURE SIZE, AND MY BRIEF'S REASON WAS WRONG**: three routes exist — `Adjunction/GAFT/Sets.v`'s `Sets_HasEqualizers`, `Adjunction/Diagonal/Finite.v`'s `DiagSets_HasEqualizers`, and `Structure/Pullback/Reduction.v`'s `HasEqualizers_of_HasPullbacks_Terminal` fed `Sets_HasPullbacks` and `Sets_Terminal` — and the third is taken because on the tree's `coqdep` graph it adds **8** modules against the other two's 25 and 40 (file closure **49**, with neither GAFT nor Finite in it); I had claimed the GAFT route carries a `Set` pin, and it does NOT — measured, `Sets_HasEqualizers@{u u0}`'s block is `u0 < u` plus seven bounds with no `Set` anywhere; that file's disclosed pin belongs to `GAFT` itself, not to this constant. **The comparison with `EqualizerFunctor` I asked for CANNOT be delivered, and the file records the measurement rather than a weaker substitute**: the two take different hypotheses (`HasEqualizers C` here, `HasLimitsOfShape Parallel C` there) and even after bridging with `HasLimitsOfShape_HasEqualizers` the object actions are REJECTED at `eq_refl` — two independently CHOSEN equalizers — so what ships is the type-shape tie `eqa_dom_functor := Arrow_dom ◯ EqualizerArrowFunctor : [Parallel, C] ⟶ C` with its object action `eq_refl`. Universes measured off BOTH binder and block: `CokernelPair_Equalizer_Adjunction@{u u0 u1 u2 u3 u4 u5}` is over `C : Category@{u u0 u0}` — hom identified with proof by REUSING the level in the binder — while the constraint block carries **no equation at all** and **no `Set`**; the identification has FOUR donors each SUFFICIENT ALONE (`Arrow`, `[Parallel, _]`, `HasPushouts`, `HasEqualizers`, each rejected under `Constraint ch < cp` against accepted `x ~> y`/`id` controls), so `Arrow` is not "the" cause as an earlier brief said. **108/108 constants closed under the global context** — 92 declared heads plus the 16 `Program` obligations the `.glob` cannot see (it lists 94, the two extras being the NAMES of the two `Fail Example` commands); the file declares no `Record`/`Class`/`Inductive`, hence no fields and no `Build_*`. **SEVEN `Fail`s = 6 negatives in TWO kinds (2 conversion, 4 formability) + 1 scope-free instrument check**, each stripped one at a time with the others left as `Fail`, compiled alone, and verified to fire at its own command. **Zero collisions across all 92 heads** (my own sweep, heads and fields, instrument-checked against `Arrow` and `cokernel_pair`) — AFTER the builder found and renamed three away: `par_one`/`par_two` are taken TWICE (`Instance/Proset/Transform.v`, `Theory/Natural/Transformation/Arrows.v`) and became `parallel_leg1`/`parallel_leg2`, and `SetsEq` (`Adjunction/Diagonal/Finite.v`) became `SetsEqualizers`; also recorded there, two silent-failure traps in the sweep method itself — an unquoted `$VAR` in a zsh `for` does not word-split, and `rg` with no path argument reads the loop's stdin and swallows the rest of the name list — both of which produce a clean-looking zero. Two donor sentences this commit made stale are repaired (`Theory/Morphisms/CokernelPair.v`'s NOT-delivered entry and this index's own `CokernelPair.v` bullet), each now recording a division of labour rather than a gap. NOT delivered: no dual (the kernel-pair functor right adjoint to the coequalizer functor, `Structure/Regular.v`'s `kernel_pair` staying object-level); no `RegularMono` class and no characterization of regular monos beyond `unit_iso_iff_regular`; no statement about when the COUNIT is an isomorphism; no comparison of `EqualizerArrowFunctor` with `EqualizerFunctor` beyond the type-shape tie; no functoriality in `C`; and no witness at any category other than `Sets`
- **Adjunction/Fullness.v**: Mac Lane §IV.3 Exercises 3 and 6 (book p. 92, `maclane:IV.3:ex3`, `maclane:IV.3:ex6`) — **fullness of either adjoint makes the whiskered counit invertible, with the whiskered unit as inverse**: `whiskered_counit_iso_of_full : Full F + Full U → @Isomorphism ([C, D]) (U ◯ F ◯ U) U` (the issue's pinned name), forward leg `whiskered_counit` with component `fmap[U] (counit a)` and backward leg `whiskered_unit` with component `unit (U a)`, the SUM hypothesis being the faithful rendering of "either F or G full" with `_left`/`_right` readings as `:=` at `inl`/`inr`, and the other handedness `whiskered_unit_iso_of_full : @Isomorphism ([D, C]) (F ◯ U ◯ F) F` at the same strength — with the ROLE SWAP measured rather than assumed: on the `U ◯ F ◯ U` side the cheap hypothesis is `Full U`, on the `F ◯ U ◯ F` side it is `Full F`. **The law the tree lacked for an ARBITRARY adjunction is `unit_fmap_counit : η_{Ua} ∘ Uε_a ≈ id`** (the other law being the pre-existing triangle `fmap_counit_unit`; swept by SHAPE at `48756a39`, the only in-tree composite of a whiskered counit with a unit is that other triangle, `Theory/Equivalence/Monoidal.v`, and the reflective instance carries only the C-level split `s ∘ ε ≈ id` under full AND faithful), and the issue's reviewer check — "proved, not re-derived from the existing triangle identity" — is met TWICE by arguments sharing no step: `unit_fmap_counit_of_full_right` goes through the U-preimage of the unit and spends the OTHER triangle, `counit_fmap_unit`, one lemma down (an audit caught this bullet's first draft saying "never touches the triangle"), never `fmap_counit_unit`; `unit_fmap_counit_of_full_left` consumes `fmap_counit_unit` only as one of two inverses whose coincidence (`split_inverses_agree`, a two-sided splitting is unique — a one-line restatement of `Theory/Isomorphism.v`'s already-imported `comp_inverse_unique`, which the file's first draft claimed to have redeclared because the donor lived under `Structure/Groupoid.v`, a file that merely uses it) is the argument. Beneath both sits the reusable core `counit_split_mono_of_full_right : Full U → ∀ a, Section (counit a)` and `unit_split_epi_of_full_left : Full F → ∀ x, Retraction (unit x)`, each three steps (two rewrites and one `exact`) — naturality of ε (resp. η) at the chosen preimage plus one triangle identity — where the brief had prescribed a transpose comparison closed by injectivity of `⌊−⌋`, which is correct and for which the tree DOES carry the lemma, `Adjunction/Additive.v`'s `adj_to_inj`, outside this file's closure (the builder's report said it did not exist, and an audit caught this bullet repeating that against the `Adjunction/Additive.v` bullet above). **EXERCISE 3 TURNS ON ONE LETTER, AND THE PAGE IMAGE SETTLED IT AGAINST THE BUILDER**: the book PRINTS its triple as ⟨G, F, φ⟩ : X ⇀ A — read off the rendered scan, with the OCR layer agreeing — where the table's left column and Exercise 5 write ⟨F, G, φ⟩ and Theorem 1 and Exercise 6 write ⟨F, G, η, ε⟩, F first in every case; the issue's paraphrase "the left-listed functor is full" is therefore FAITHFUL to the print, and the builder's "verbatim" quote, which silently transcribed ⟨F, G, φ⟩ and on that basis "corrected" the issue, was itself wrong. Under §IV.1's convention the first-listed functor of a triple `X ⇀ A` is the LEFT adjoint, so the literal reading is `Full F`, where the monic hypothesis is IDLE — `unit_epic_of_full_left : Full F → Epic (unit x)` needs none, η_x being a split epi outright, and what the hypothesis then buys is `unit_iso_of_full_monic` (split epi + monic = iso; a data-producing constant, hence `Defined`); read with G the RIGHT adjoint — its role in Theorem 1 and Exercises 5 and 6, though the table's dual column does list G first as the left adjoint of the opposite adjunction A ⇀ X — the only reading under which the printed hypothesis does work, it is `unit_epic_of_full_monic : Full U → (∀ y, Monic (unit y)) → ∀ x, Epic (unit x)`, whose engine `unit_at_UF_of_full_right : η_{U(F x)} ≈ UF η_x` is exactly what Exercise 6 buys and whose monic hypothesis is consumed at the CODOMAIN of the two test arrows, never at x — which is why it must be assumed at every object. Whether ⟨G, F, φ⟩ is a misprint is not decidable from the page and is NOT decided: both readings ship, the header says which is the literal print and which carries the content, and neither is called "Mac Lane's". **THE WHISKER PACKAGING HAZARD IS REAL AND IT IS TYPING, NOT FORMABILITY**: `Transform` is a class applied to two functor RECORDS, and `U ◯ (F ◯ U)` and `(U ◯ F) ◯ U` agree on `fobj` and `fmap` but differ in their three law fields (distinct `Compose_obligation_*`), while `U ◯ Id` is not `U`; so `U ⊳ counit` lands at `U ◯ (F ◯ U) ⟹ U ◯ Id[C]` and `unit ⊲ U` at `Id[D] ◯ U ⟹ U ◯ F ◯ U`, neither ascribable at the isomorphism's type — the two transformations are hand-built at the one parenthesization `(U ◯ F) ◯ U`, identified with the whiskered forms COMPONENTWISE at `eq_refl` (`whiskered_counit_is_whisker_left`, `whiskered_unit_is_whisker_right`), and the record-level ascriptions are pinned as TYPING negatives whose error is a plain `has type … while it is expected to have type …` with no `cannot unify` and no universe clause. **The two in-tree instances the issue names are each a `:=` with no tactic**: `reflective_fmap_counit_IsIsomorphism` over `Construction/Reflective.v` at `inr` (the inclusion is full) and `equiv_fmap_counit_IsIsomorphism` over `Theory/Equivalence/Adjoint.v` at `inl` (`Equivalence_Full`), the produced inverse read back at `eq_refl` as the unit component in both; against the DONORS' own inverses the grading splits — the equivalence one agrees at `≈` (`equiv_fmap_counit_inverse_agrees`, the two being inverse to one arrow), the reflective one is refuted at `eq_refl` and is NOT delivered at `≈` either, because `reflective_counit_iso` (`Construction/Reflective.v`) produces DATA yet is closed with `Qed`, so neither leg reduces and no equation naming a leg's value is available by conversion (the isomorphism laws themselves of course hold, and an audit caught an earlier "no equation at all"); the donor is not modified, and the `≈` form is not claimed impossible. Non-vacuity separates this file's hypothesis from the "full and faithful" both instances happen to satisfy: `Full_Erase_of_ZeroObject` (a zero object makes `Erase C : C ⟶ 1` full, the zero morphism being the preimage), `zero_erase_adjunction` CONSUMING `Adjunction/Diagonal/Limit.v`'s `Initial_Erase_Adjunction`, and at `PointedSets` `Erase_PointedSets_not_Faithful` with `pointed_counit_not_IsIsomorphism` — a full, non-faithful right adjoint whose counit is not invertible though its U-image is — labelled DEGENERATE in the one respect that is: the target `1` makes Exercise 6's conclusion true there for a reason unrelated to the theorem. **Universes measured off BOTH binder and block, and the block alone gives the wrong answer twice**: every headline is over `C : Category@{u u0 u0}`, `D : Category@{u1 u2 u2}` — hom identified with proof by REUSING the level variable in the binder, no such equation in any block — with the blocks carrying the one equation `u0 = u2`; `split_inverses_agree@{u u0}`'s public block is EMPTY while its binder is over `Category@{u u0 u0}`. Attribution by probe: hom = proof has THREE donors each rejected ALONE under `Constraint ch < cp` against accepted hom and identity controls — `Section`, `Monic`, `Retraction` — and `Functor` is NOT one (`Du ⟶ Cu` elaborates there); `u0 = u2` needs neither `Compose` nor `Adjunction` nor `Full`, the mere presence of functors in BOTH directions sufficing (`Cu ⟶ Du` rejected under `dh < ch` while `Du ⟶ Cu` is accepted). **A `Set` PIN THE BUILDER'S REPORT DENIED WAS FOUND AND FIXED**: the report said no `Set` occurs in any binder or block of either file, but `Full_Erase_of_ZeroObject` written unannotated minimizes to `C : Category@{u Set Set}` and drags `zero_erase_fmap_counit_IsIsomorphism` with it — attributable to that definition's own minimization, since `zero_erase_adjunction` over the same `Erase C` stays free — and explicit binders `@{o h +}` over `Category@{o h h}` lift it (accepted at `Set < ch`); sections (A)–(D) carry no `Set`, and the two `PointedSets` lemmas carry the bound `Set < u` that instance's own declaration brings. **56/56 constants closed under the global context** — 44 glob heads (29 `def`, 15 `prf`; no `inst`/`proj`/`rec`) plus the 12 `Program` obligations `Print Module` lists and the glob cannot see; the file declares no `Record`/`Class`/`Inductive`, so there is no unlisted `Build_*`, and its one `Module HomSetAdj := Category.Theory.Adjunction` is an alias, not a constant — the gate carries the 44 heads permanently. Zero tree-wide collisions over all 56 environment names plus the alias and the probe's names, AFTER the builder found and renamed one: the obvious alias `Adj` collides with `Instance/Adj.v`'s category `Adj`, which the `print-assumptions` gate loads into the same scope. `Test/ProbeFullness368.v` carries **11 negatives of THREE kinds** — 4 FORMABILITY (the three hom = proof donors and the both-directions functor rejection, each beside controls at the very same levels), 2 TYPING, 5 CONVERSION (the two donor inverses, and the three record-level padding facts of the whisker hazard — associativity and both identity paddings — which the file's first draft called pinned when only the two ascriptions were, an audit finding) — plus a scope-free instrument check, each stripped ONE AT A TIME with the others left as `Fail`, compiled alone and its whole error read; every constant a negative names appears in a succeeding control. `make todo` grows by 18 lines, all `Fail`-family matches of its pattern and only 12 of them commands, zero TODO/FIXME/jww. Four engineering findings, each reproduced: a section-local `Notation "'η'" := (@unit C D F U A)` fails on use because the stored head's implicits are RE-INSERTED (`Illegal application`) — the notation must take the object argument, `'η' x`; `Theory/Morphisms.v`'s `retractions_are_epic` exports its two OBJECTS explicitly (its section declares `Variables x y : C`), so `retractions_are_epic f H` puts the morphism in an object slot; `Structure/Initial.v`'s `Notation "@Initial C"` captures `@Initial_Erase_Adjunction` and reports the missing reference `_Erase_Adjunction` (the `@Cocartesian_Initial_HasFiniteCoproducts` family); and with `Adjunction/Opposite.v` required a bare `D^op` on a CATEGORY elaborates as `Opposite_Adjunction D` — met while measuring the opposite route for the split-epi lemma, which is available but not a `:=` (`unit` of `Opposite_Adjunction A` IS `counit` of `A` at `eq_refl`, measured out of tree and pinned NOWHERE, but the passage still needs `Full_op` and `Retraction_of_op_Section`, a construction not a conversion), so the direct three-rewrite proof is kept. Two of the builder's own corrections were themselves wrong and are corrected in the header: the transcription above, and a line-number "fix" that moved the brief's correct `Theory/Adjunction.v` citations onto two `Qed.` lines. A fess audit of the first commit found eleven prose defects and no unsound step — three false claims (the donor location above, the `adj_to_inj` absence, and "four … pinned" for two), the "no equation at all" overreach, an unproved general sentence about the `Erase` witness's counit now labelled argued, and wording — all fixed in the amend. NOT delivered: Mac Lane §IV.3 Theorem 1 (G faithful ⟺ every ε_a epi; G full ⟺ every ε_a split monic, whose forward half `counit_split_mono_of_full_right` IS; hence full and faithful ⟺ ε iso), delivered since as `Adjunction/FullFaithful.v`; no CONVERSE to Exercise 6 and no adjunction with NEITHER adjoint full at which the law is refuted; no `≈` comparison with the reflective donor's inverse; the opposite-route identification unpinned; no Leibniz equality between the hand-built and whiskered transformations, only components; no `StrictCat` statement; no independence proof for `unit_iso_of_full_monic`'s two hypotheses and no adjunction with monic non-epi unit components; nothing registered as an `Instance`
- **Functor/Hom/Transfer.v**, **Adjunction/FullFaithful.v**: Mac Lane §IV.3 Theorem 1 with its Lemma and Exercise 5 (book pp. 90-92, `maclane:IV.3:thm1`, `maclane:IV.3:lem1`, `maclane:IV.3:ex5`) + Riehl §4.6 Lemma 4.6.11 and Exercise 4.6.ix (printed pp. 169/172, `riehl:4.6:lem11`, `riehl:4.6:exix`) — **a right adjoint is faithful exactly when every counit component is epi, full exactly when every counit component is a split mono, and fully faithful exactly when the counit is a componentwise isomorphism**, all six of Riehl's clauses as genuine biconditionals over an ARBITRARY `A : F ⊣ U` (not a subcategory inclusion, the issue's reviewer check; the two (iii)'s in their COMPONENTWISE reading — ε and η an isomorphism at every component, Mac Lane's own phrasing — while ε as an isomorphism IN the functor category is not delivered): `right_adjoint_faithful_iff_counit_epic : Faithful U ↔ (∀ a, Epic (ε a))` and `right_adjoint_full_iff_counit_split_monic : Full U ↔ (∀ a, Section (ε a))` (the issue's two pinned names; `Section` is the library's split mono, `Theory/Morphisms.v`, and NOT `Monic` — the other reviewer check, pinned as a TYPING negative), `right_adjoint_fully_faithful_iff_counit_iso : (Full U * Faithful U) ↔ (∀ a, IsIsomorphism (ε a))`, and the three unit-side duals `left_adjoint_faithful_iff_unit_monic`, `left_adjoint_full_iff_unit_split_epic`, `left_adjoint_fully_faithful_iff_unit_iso`, stated COVARIANTLY with no `^op` in any type; every one `Defined`. **THE ROUTE IS MAC LANE'S AND THE TRANSFER LEMMA IS CONSUMED, NOT CITED**: `hom_transfer f : [Hom a,─] ⟹ [Hom b,─]` (component `h ↦ h ∘ f`, his `f*`) with `hom_transfer_monic_iff_epic` and `hom_transfer_epic_iff_section` over an ARBITRARY category, proved in his three steps by consuming #369's pointwise characterisation (`Instance/Fun/Morphisms.v`'s four one-directional halves, by name) and `Instance/Sets.v`'s injectivity/surjectivity readings; the bridge to the adjunction is `fmap_to_adj_counit` (`Theory/Adjunction.v`), read as `counit_precomp_is_from_adj_fmap_U : g ∘ ε a ≈ ⌈fmap[U] g⌉`, and `transfer_at_counit_is_precomp` records at `eq_refl` that Mac Lane's displayed composite `A(a,c) → X(Ga,Gc) → A(FGa,c)` IS the component of `hom_transfer (ε a)` — so the transfer halves appear literally in the scripts of (i) both ways and (ii) backward, while (ii) forward CONSUMES #368's `counit_split_mono_of_full_right` (`Adjunction/Fullness.v`) with Mac Lane's route for it shipped alongside as `counit_section_via_transfer`; the two left inverses so produced are NOT compared, and in general cannot be, two left inverses of a non-epi being unrelated. **Exercise 5 is `adj_monic` READ AT THE OPPOSITE ADJUNCTION**, the issue's "visibly the same fact": `from_adj_epic_of_epic` is `constructor; intros` and one `exact` of `Theory/Adjunction.v`'s `adj_monic` at `Opposite_Adjunction F U A`, its two hypotheses the transports `Faithful_op` and `op_Monic_of_Epic` and every other step a conversion, with a direct transpose-chasing control beside it (the two are NOT compared — `Epic` is a one-field record whose field is a proof, with no setoid), and the backward half is `id` epic plus `⌈id⌉ = ε a` at `eq_refl` (the definition); the biconditional is `right_adjoint_faithful_iff_from_adj_epic`. **The dual clauses cost nothing, and that is measured**: `counit (Opposite_Adjunction F U A) x = unit A x` and its mirror hold at `eq_refl` (`op_counit_is_unit`/`op_unit_is_counit` — #368 measured this out of tree and pinned it nowhere), the transport is Duality.v's two quartets and `Functor/Opposite.v`'s four passages, dual (ii)'s forward half is #368's `unit_split_epi_of_full_left`, dual (iii)'s consumes `unit_iso_of_full_monic`, and `unit_retract_agrees` shows at `eq_refl` that the op route's right inverse for η IS #368's — the duals are #368's results transported, not parallel constructions. `Epic_Section_IsIsomorphism` repackages `Theory/Isomorphism.v`'s PRE-EXISTING `Epic_Section_Iso` (an object-level `Isomorphism`) as the predicate (iii) is stated with, consuming it for the one law `Section` does not carry — the brief had asked for a six-line reproof, which a grep showed would duplicate it. **`Construction/Reflective.v` is RE-DERIVED but cannot be RELATED, and the obstruction is a donor defect**: `reflective_counit_IsIsomorphism_general` is (iii) at `Incl C S` via `Incl_Faithful` (`Construction/Subcategory.v`) and `Full_Implies_Full_Functor`, using no property of the reflector, with `reflective_counit_Isomorphism_general` Mac Lane's "each object a ∈ A is isomorphic to F K a" and `reflection_counit_Epic` his "the counit of a reflection is always epi" from faithfulness alone; but `reflective_counit_iso` produces an `Isomorphism` (DATA) and is closed with `Qed`, so `to (reflective_counit_iso R x) = counit …` is refuted at `eq_refl` (pinned) and no `≈` comparison is available either, there being nothing to rewrite with; the donor is not modified. **PRIOR ART THE ISSUE MISSES, AND ITS ABSENCE CLAIM IS FALSE AT `418e970a`**: "a whole-tree inspection of every `Epic` occurrence finds none touching a counit" — `Instance/Coq/Monoid/Free.v`'s `adjunction_counit_epic (FU : Faithful U) (x : C) : Epic (counit x)` is the forward half of (i) for an ARBITRARY adjunction inside `Section AdjunctionCounit`, in the `Instance/Coq` layer; it is restated here by a different proof (through the transfer lemma) rather than consumed, and the probe `Check`s both at ONE type so the identity of the statements is machine-checked. The issue's donor citations are off throughout — for `adj_monic`, for `Full`/`Faithful` in `Theory/Functor.v`, and for every site it gives in Yoneda.v and Morphisms.v — and they are corrected in the header. **The relation of `hom_transfer` to `fmap[Curried_Hom A] f` is a three-rung ladder, not a yes/no**: VALUES agree at `eq_refl` (`hom_transfer_is_fmap_value`), the COMPONENT record does not (`SetoidMorphism`'s `proper_morphism` certificate is separately elaborated), and the whole `Transform` does not either (`naturality`/`naturality_sym` rebuilt) — both refuted and pinned, so the difference is located at the certificate rather than blamed on naturality alone — with `≈[[A, Sets]]` holding by `intros c h; reflexivity` (`hom_transfer_is_fmap`); the Yoneda readback `transform[hom_transfer f] a id ≈ f` is `≈`-only, the value being `id ∘ f` and `id_left` a law field, refuted strict and pinned. The Yoneda bijection is CITED, not routed through: `Yoneda_Embedding'` identifies object, hom and proof universes, and the lemma needs only `Curried_Hom` plus #369. `hom_transfer` is a `Program Definition` whose three obligations the library's tactic discharges entirely, so `Next Obligation` finds nothing and yet three named constants `hom_transfer_obligation_1..3` exist — a source sweep for that keyword undercounts by three. Universes measured off BOTH binder and block for all 45 constants: every constant is over `Category@{u u0 u0}` (hom identified with proof in the BINDER, no such equation in any block), the theorem file's headlines carry the ONE block equation `u0 = u2` identifying C's and D's hom universes with both object universes free, and NO `Set` anywhere; hom = proof has seven donors each rejected ALONE under `Constraint ch < cp` (`Epic`, `Monic`, `Section`, `Retraction` pinned in the probe; `Full`, `Faithful` and `Fun` measured out of tree) while `Functor` and `IsIsomorphism` are NOT donors; and `u0 = u2` is forced by functors in BOTH directions before any adjunction is formed (`Cu ⟶ Du` accepted, `Du ⟶ Cu` rejected under `ah < bh`, pinned), so `Adjunction` cannot be tested apart from `Functor` for it — the `MonoidObject`/`Monoidal` trap again. `Epic_Section_IsIsomorphism` is this pair's sharpest binder-versus-block trap: a four-entry block with no equation over a binder `Category@{u0 u1 u1}`. **45/45 constants closed under the global context** — 42 glob heads (Transfer 7 `def` + 6 `prf`; FullFaithful 27 `def` + 2 `prf`) plus the three obligations; no `Record`/`Class`/`Inductive`, so no `Build_*`; zero collisions over 64 names (heads, obligations, and the probe's 19 declared names including its five `Fail`-declared ones), instrument-checked at `Full`/`Monoid` = 3; the gate carries the 42 heads. `Test/ProbeFullFaithful367.v` mirrors both targets' full `Require` lists and carries **11 `Fail`s = 1 instrument + 7 numbered negatives in THREE kinds + 3 sibling formability `Fail`s**: 4 CONVERSION (the ladder's two rungs, the Yoneda readback, the opaque reflective donor), 1 TYPING (`Section (ε a)` against `Monic (ε a)`, a plain `has type … expected` with no `cannot unify`), 5 FORMABILITY (`Epic`/`Monic`/`Section`/`Retraction` under `ch < cp`, and `Du ⟶ Cu` under `ah < bh`) — each stripped ONE AT A TIME and compiled alone with the whole error read; the two target constants a negative names rename-simulated 2/2, and the first cut named `reflector`, `Incl`, `reflective_adj` and `Retraction` only inside a `Fail`, closed with `Check` guards before landing. Non-vacuity PROVED in both quadrants, both in the probe so the library closure stays lean: full-not-faithful is #368's `Erase PointedSets` — `pointed_counit_Section` by (ii) and `pointed_counit_family_not_Epic` by (i)'s contrapositive (NEW: #368 refutes only invertibility at one component), labelled DEGENERATE since the target `1` makes fullness hold for an unrelated reason; faithful-not-full is the free-monoid adjunction over `Coq` — `free_mon_forget_not_Full` evaluates the recovered homomorphism at the UNIT against `fun _ => [true]` and closes by `discriminate`, not by an empty hom-set, whence `free_mon_counit_family_not_Section` and `free_mon_counit_family_not_iso`. Read both at their strength: they refute the FAMILY `∀ a, Section (ε a)`, not a named component — a single ε_a may still split while U is not full — which corrected the brief's parenthesised "provably NO Section". `make todo` grows by 22, all in the probe (11 `Fail` commands, 11 prose lines). NOT delivered: no comparison of the two forward routes for (ii); no naturality statement (ε as an isomorphism IN the functor category would be `nat_iso_iff_pointwise` at `F ◯ U ⟹ Id`, not built); no contravariant twin of the transfer lemma and nothing about it at a target other than `Sets`; no `≈` comparison with the reflective donor, blocked as above; no generalization of `Construction/Localization.v`'s `unit_at_local_iso` (the issue's appended note, deliberately not acted on); nothing about reflection or creation and no `AdjointEquivalence` corollary; nothing registered as an `Instance`; and Duality.v's Section/Retraction quartet, like `Full_op`/`Full_of_op`, is read off the source as `Program Definition`s that REBUILD the record with one `exact`-discharged law obligation (only the Monic/Epic quartet and the two `Faithful` passages are record literals — a first draft of the header called all twelve "constructor applications with no proof content" and cited Duality.v for it, a sentence scoped to the Monic/Epic four; a fess audit caught that together with six lesser prose defects, among them "all six of Riehl's clauses" without the componentwise qualifier, all fixed before landing), with no `eq_refl` probe run on any of the twelve
- **Instance/Grp/Abelianize.v**, **Instance/Sets/Pointed/Free.v**, **Instance/Top/Discrete.v**, **Instance/CMon/Grothendieck.v**, **Instance/Grp/Completion.v**, **Instance/Mod/Extension.v**: Mac Lane §III.1 Exercise 3 (`maclane:III.1:ex3`, book p. 59) — universal arrows to the standard forgetful functors — together with Mac Lane §IV.2's concrete-counit clause (`maclane:IV.2:construction1`) and Riehl §4.1 Example 4.1.10 / §4.2 Exercise ii (`riehl:4.1:example10`, `riehl:4.2:exii`). **The issue's "Current state" is stale for the FIFTH consecutive issue** — it says "Absent. No Grp/Ab/Rng/Top/Set∗ categories exist in `Instance/`" and every one of them now does. **THE FOUR ROWS ARE DELIVERED AT THREE DIFFERENT STRENGTHS, AND THE DIFFERENCES ARE THE POINT.** (a) ABELIANIZATION is a full adjunction, and `Instance/Grp/Abelianization.v`'s pre-existing `abel_projection : Id[Grp] ⟹ Ab_to_Grp ◯ Abelianization_Functor` IS its unit — consumed, not rebuilt. Read the packaging precisely: `LeftAdjointFunctorFromUniversalArrows` PRODUCES its own left functor, so `abelianize_adjunction` is stated against `abelianize_left` and NOT against the pre-existing `Abelianization_Functor`; the two agree on objects by `eq_refl` (`abelianize_left_obj`) and on arrows up to `≈` (`abelianize_left_iso`), and the whole-record identification is REFUTED and pinned — they are not silently identified. (b) THE FREE RING ON AN ABELIAN GROUP NEEDED NO NEW FILE: #400's `Instance/Rng/Free.v` already carries `free_rng_ab_universal_arrow`, `free_rng_ab_AUniversalArrow` and **`free_rng_ab_unit` with `free_rng_ab_unit_is_insert` by `eq_refl`** — which IS this issue's stated obligation for that row — so it is CITED, exactly as #312's dependency note requires ("must reuse #400's construction rather than derive a second tensor algebra"). (c) THE DISCRETE TOPOLOGY IS THE ONE GENUINELY OBSTRUCTED ROW, AND THE OBSTRUCTION IS PRE-EXISTING RATHER THAN INTRODUCED HERE: `Instance/Top/Forgetful.v` records that a functor out of `Top` lands in a LIFTED `Sets` while a functor into `Top` comes from the unlifted one, so an `Adjunction` record "would need both functors to share ONE Sets, at levels o and h simultaneously, with o < h: the packaged triple is unformable at every universe assignment". So **no `Adjunction` and no `UniversalArrow` RECORD is claimed** — `Instance/Top/Discrete.v` delivers the universal property WRITTEN OUT (`discrete_universal`, a Type-valued `∃!`) plus Mac Lane's unit, and **`disc_unit` is literally `id`**: `disc_unit_target_strict` shows `Top_Forget (Top_Discrete A) = Setoid_Lift A` by `eq_refl` — NOT a new fact, `Instance/Top/Forgetful.v`'s `forget_discrete_on_objects` is the same proposition with the same proof, restated so the unit can be read without leaving the file — which is the right mathematical statement rather than a degeneracy (a discrete topology adds nothing to the underlying set). That is the usual reason one EXPECTS this left adjoint to be fully faithful, but neither `Full Top_Discrete` nor `Faithful Top_Discrete` is proved here or anywhere in tree, and the file's NOT-DELIVERED list says so. `disc_extend_is_adj_from` ties it to the donor's `discrete_adj` by `eq_refl`. (d) ADJOINING A BASEPOINT is a full adjunction, and the forgetful `PointedSets ⟶ Sets` did not exist — it is built here. **THE FOUR APPENDED CHECKBOXES ARE ALL DISCHARGED.** Riehl §4.2's route: `abelianize_adjunction_via_transform` rebuilds the SAME adjunction through `Adjunction_from_Transform` with both triangle identities proved by hand, and `abelianize_routes_agree` relates the two presentations. Mac Lane §IV.2's concrete counit: `abelianize_counit` with `abelianize_counit_iso`, stated on elements. Riehl §4.1's group completion **IN BOTH FORMS, AND THE TWO ARE PROVABLY DIFFERENT CONSTRUCTIONS**: `Instance/CMon/Grothendieck.v` does `CMon → Ab` by the pairs quotient `(a,b) ~ (c,d) iff ∃k, a+d+k ≈ c+b+k` — **the `+ k` is REQUIRED, since without it transitivity fails for a non-cancellative monoid** — while `Instance/Grp/Completion.v` does `Monoid → Group` by a PRESENTATION (the free group on the underlying setoid, quotiented by the normal closure of the multiplication relations) over `Grp_MonSets : Grp ⟶ MonSets` (`Instance/Rng/GroupRing.v`). **The pairs construction PROVABLY cannot serve as the second form**: `GrothendieckObject` yields an ABELIAN group for every input — a TYPING fact, since it is built as an `AbObject` and commutativity is inherited from `cmon_plus_comm`; the swap supplies `ab_neg`, i.e. group-ness, not commutativity, whereas the completion of the free monoid on two letters is the free group on two letters, which `Instance/Grp/Free.v`'s `free_group_two_generators_nonabelian` proves non-abelian — and `Instance/Grp/Completion.v`'s `completion_can_be_nonabelian` confirms the presentation route escapes that. **A HEADER CORRECTION LANDED IN THE SAME COMMIT AND NAMES ITS OWN BAD EVIDENCE**: `Instance/CMon/Grothendieck.v` had argued the second form was unreachable partly because "a forgetful `Grp ⟶ Mon` … does not exist", which is FALSE (`Grp_MonSets` is the functor, and an internal monoid in `(Sets, ∏)` IS an ordinary setoid monoid, so `MonSets` is a usable category of them); that reason is rewritten, while its OTHER reason — the abelian obstruction above — stands and is what forced a separate file. Riehl §4.1's extension of scalars is `Instance/Mod/Extension.v`, and **its hypothesis is WEAKER than the commutativity Riehl's setting would suggest, and is a VISIBLE explicit argument**: `extend_restrict_adjunction : ∀ R S φ (Hc : CentralImage φ), ExtendScalars φ Hc ⊣ Restrict φ`, where `CentralImage φ` asks only that every `φ r` be central in S, with `central_of_commutative` deriving it from commutativity and the single spend-site named in the header. Read "weaker" at exactly its proved strength: commutativity implies it, and the converse is NOT established — every `CentralImage` witness in tree comes from `central_of_commutative`, so no φ with central image into a NON-commutative target is exhibited and STRICTNESS is not claimed. the file also concedes that the object built is not the classical `S ⊗_R M` when R is non-commutative. **429 constants closed under the global context** across the six files (90 + 66 + 9 + 91 + 97 + 76, matching each file's own header). An earlier revision of this bullet said 327 by counting only glob-visible names (63 + 60 + 9 + 71 + 53 + 71 after filtering the `Fail`-named entries the environment never receives); that method is blind to the **102 `Program` obligations** the `.glob` does not record, and it contradicted the six headers it was describing. `Print Module` is the authority. `Test/ProbeUniversalArrows312.v` guards the cross-file boundaries from OUTSIDE the targets — an in-file `Fail` renames in lockstep with its own definition and so cannot detect a rename — with 3 negatives of 2 KINDS kept lexically apart (one FORMABILITY, that `Top_Discrete ⊣ Top_Forget` cannot be formed, against controls showing BOTH functors exist and are nameable so the negative is about the packaging; one CONVERSION, that the produced left adjoint is not the pre-existing functor on the nose), each stripped and its kind read off the error, plus a scope-free instrument check and probe-vacuity simulation **4/4**. NOT delivered: no `Adjunction` for the discrete row and no claim that its obstruction has any cause other than the donor's stratification, which is not re-derived here; no normal form or decision procedure for any of the three quotients; no comparison of the group completion with `Instance/Ab/Free.v`'s free abelian group (a different left adjoint); and the remaining rows of Riehl's fourteen-entry roster are owned by other issues and are neither claimed nor attempted
- **Instance/FdVect.v**: Finite-dimensional vector spaces and the matrix-category equivalence (Mac Lane §I.4 Ex. 6, Riehl Example 1.5.12) — `FieldObject` over commutative rings (total Proper `finv` with the law on nonzero elements only, `field_no_zero_divisors` derived; owned here per #258's QA-correction) with `Q_Field` and `Field_CRng`; `Vct_F F := RMod (field_ring F)`; `FdVectObject` carrying the chosen basis as a coordinate isomorphism (`fdv_coord`/`fdv_expand` with round trips and linearity — Zorn and decidable linear dependence both avoided, and since the library's ∃ is Type-valued, naming the witness is the same data, not a weakening); `FdVect F` IS Riehl's intermediate category of based spaces with arbitrary linear maps — the factorization's SECOND leg (based → bare fd spaces) is VACUOUS rather than omitted, the Type-valued ∃ making the unbased fd category `FdVect` again, and `FdVect_Forget` (into ALL F-modules, so NOT that leg and no surjectivity claim) is Full+Faithful, recording that carried coordinates behave as mere PROPERTY relative to arbitrary linear maps; `StdVect`/`std_basis` via Matr's `delta`; `Matr_to_FdVect` with the orientation pinned by discriminating eq_refl probes ([[1,2],[3,4]]·(5,6) = (17,39); the transposed reading `mat_apply_T` machine-checked to (23,34) and discriminated from it) and `fmap_comp` = Matr's associativity read one dimension down; `Faithful` + `Full` (`matrix_of g` = columns g(e_j), `matrix_of_sur` by basis expansion through `msum`) + `EssentiallySurjective` (`StdVect_iso` from the coordinate round trips; the instance `#[local]` — chosen-preimage choices stay out of the global database); **`FdVect_Matr_Equivalence` := `FF_ESO_Equivalence`**, axiom-free, with `equivalence_inverse_dim` (the quasi-inverse's object part IS `fdv_dim`, by eq_refl) and ℚ² acceptance incl. the independent `std_reindex` swap-matrix probe. Honest strength, disclosed in-file: the equivalence spends only COMMUTATIVITY of the base ring (twice on its path — `mat_apply_smul`, `matrix_of_sur`), so it would restate verbatim over any commutative ring via `Field_CRng`; `finv`/`field_one_neq_zero` are class content exercised by `finv_r`/`field_no_zero_divisors`/ℚ, not premises. Import-order gotcha recorded: `Coq.QArith.QArith` must precede `Category.Lib` (its `equiv` export shadows Setoid's)
- **Instance/FdVect/DoubleDual.v**: The double-dual natural isomorphism (Mac Lane §I.4 remark 2 + §II.2 construction 3, Awodey §7.5 Ex 7.12, Riehl §1.4 Ex 1.4.4(i)-(ii)) — `DualMod` (the hom-module V* = linear maps into `Ring_RMod`), `Dual : (Vct_F F)^op ⟶ Vct_F F` on ALL F-modules (§II.2's named artifact, no finiteness), `DoubleDual := Dual ◯ Dual^op` (StarAutonomous.v's shape), evaluation `eta` with **`double_dual_natural` : Id ⟹ DoubleDual at ANY dimension** — Riehl's scope delivered, not narrowed: only INVERTIBILITY needs finite dimension, and that is **`double_dual_iso`** (per-V `IsIsomorphism (eta V)` on FdVect via the dual basis, which IS `DualFdObj`'s own carried basis — `dual_basis_coord` identifies it with the coordinate functionals); `DualFd : (FdVect F)^op ⟶ FdVect F`; `fd_dual_pointwise_iso` (V ≅ V* pointwise, basis-dependent — Awodey's contrast, the naturality refutation living in NonNatural.v); header records the four uses of `field_comm` and why no `StarAutonomous` instance follows (no monoidal structure on `Vct_F` in tree when written; Instance/Mod/Closed.v's `RMod_SymMonClosed` supplies one since 2026-08-31, and #433 reads Structure/Monoidal/StarAutonomous.v's `dual` off it and proves it naturally isomorphic to `Dual` (`dual_iso359`) — the header paragraph is corrected in place; an earlier revision of this parenthetical called `dual` #359's); `rmod_hom_neg` added generically (Mod.v's Preadditive lacks negation). All axiom-free. **Instance/FdVect/NonNatural.v**: the negative half — a transformation `Id ⟹ DualFd` is not even well-typed (the variance clash IS Riehl's clean statement, recorded in the header), so the well-posed reading is the Character/NonNatural.v precedent: covariant through inversion on the iso-core, and `sigma_not_natural` refutes ONE isomorphism ℚ¹ ≅ (ℚ¹)* at the SINGLE pointwise square of the automorphism 2·(−) (2q ≈ ½q forces q ≈ 0, then σ collapses e₁ into 0 — Mac Lane's m² ≢ 1 obstruction over ℚ), with `sigma_categorical_not_natural` bridging from the categorical square by `exact` (DualFd's fmap IS precomposition definitionally) and `sigma_family_not_natural` the family form — whose premise, UNLIKE the character precedent's, is proven INHABITED and machine-checked to slot in (`family_premise_inhabited` repackages `fd_dual_pointwise_iso` at the FdVect hom-level, the homs being the same `RModHom`s, and `family_not_natural_applies` feeds it to the corollary): pointwise isos exist at every object, and what is refuted is exactly their naturality — the hypothesis consumed is ONE equation, the square at the single point (e₁, e₁). **Instance/FdVect/Tensor.v**: Riehl Ex 1.4.4(vii) (filed by doc/plan/books/qa/DECISIONS.md §4.1) — the diagonal square functor `TensorSq` (object `StdVect F (n²)`, morphisms by the Kronecker action through `fentry`/`hom_coords`, functoriality via the `fin_sum_split`/`fin_sum_pair`/`fin_sum_pair_mul`/`delta_pair` index kit over FinSet/Product.v's codec — upstreaming candidates beside `fin_sum_swap`), and **`diag_transform_zero`**: every `Id[FdVect F] ⟹ TensorSq` is the zero family, under explicit `1+1 ≉ 0` (the scaling square at 2·(−) gives 2x ≈ 4x and `finv` cancels the 2 — the first place #244's field layer is LOAD-BEARING), ℚ instance `diag_transform_zero_Q`, with `tsq_zero_transform` proving the zero family IS natural (the classification is of a nonempty collection); header contrasts `Collapse.v`'s abstract `no_cloning` (assumes a natural cocommutative diagonal in a compact closed category; neither implies the other)
- **Instance/FdVect/NoRightAdjoint.v**: Mac Lane §V.5 Exercise 2 (book p. 120, `maclane:V.5:ex2`) — **the dual functor on vector spaces has no right adjoint, proved for BOTH readings the tree has: `dual_functor_no_right_adjoint : CoordSpanProper F → ∀ Rt, Dual F ⊣ Rt → False` for Instance/FdVect/DoubleDual.v's `Dual F`, with the corollary `dual_not_left_adjoint_of_op : CoordSpanProper F → Dual F ⊣ (Dual F)^op → False`, and `dual359_no_right_adjoint` / `dual359_not_left_adjoint_of_op` for the functor the issue names, Structure/Monoidal/StarAutonomous.v's `dual` read at the line through Instance/Mod/Closed.v's `RMod_SymMonClosed` (`VctDual359`), by transport along the natural isomorphism `dual_iso359 : Dual F ≈ VctDual359`** (Theory/Functor.v's `Functor_Setoid`, fed to Theory/Adjunction.v's `adjunction_along_left_iso` of #431). CONDITIONAL on Mac Lane's own sentence `CoordSpanProper F` (some functional on F^ℕ is not a finite linear combination of the coordinate projections), and necessarily so: the premise is classically true for every field (Erdős–Kaplansky) but for F = ℚ independent of ZF + DC (in a model where every set of reals has the Baire property, Pettis' automatic continuity makes every additive `ℚ^ℕ → ℚ` a finite combination of coordinates), so docs/INHABITATION.md records it as a premise that will never get a constructive witness — the analysis is in the header, not formalized. The obstruction is exhibited on a CONCRETE colimit, as the issue's reviewer asks: the countable power `PowLine := ProdMod (fun _ => VctLine)` with its coordinates `vct_coord n`, a product in `Vct_F F` read as the colimit cocone `PowCocone` over the constant discrete diagram `LineDiagram : DiscreteCat nat ⟶ (Vct_F F)^op` (Structure/Limit/Comparison.v's annotated `DiscreteCat_Functor'`; Instance/Discrete.v's unannotated one pins `Set`, probe N4), `PowCocone_IsColimitCocone` by `prod_tuple`; its image under `Dual F` is the cocone of coordinate functionals, and the competing cocone `ZeroCocone` (apex the quotient `SpanQuot` of `Dual F PowLine` by the span `SpanCoords` of the coordinate functionals; `dual_coord_in_span`, the one use of `field_comm`, puts every leg in the span) receives two mediators, the quotient map and zero, that a colimit would identify — `dual_not_PreservesColimitCocone`, then Adjunction/Continuity.v's `left_adjoint_PreservesColimitCocone` (a colimit in `(Vct_F F)^op` is a product in `Vct_F F`; the issue's own "the dual of a direct sum is a product" describes the positive direction, corrected in the header). The UNCONDITIONAL half is Mac Lane's contrast: `dual_vct_AdjointOnTheRight : AdjointOnTheRight (Dual F) (Dual F)` via the involutive transpose `vct_transpose f := fmap[Dual F] f ∘ eta F x`, `dual_vct_Adjunction : (Dual F)^op ⊣ Dual F` and `dual_vct_Continuous : ContinuousFunctor (Dual F)` (RAPL) — the direction is pinned (probe N3: `Dual F ⊣ (Dual F)^op` is refused as an ascription). **The two duals**: `VctDual359 x` is the internal hom `HomMod x VctLine`, whose carrier, zero, sum and scalar action coincide with `DualMod`'s at `eq_refl`, so the identity on functionals is a module homomorphism each way (`to359`, `from359`, `iso359`); `dual`'s arrow action `dcur (eval' ∘ (id ⨂ f))` is an OPAQUE mediator (`ump_exponents_Mod` is `Qed`) and does not compute, so `dual359_fmap_pre` identifies it with precomposition by `dcur_uniq` on Instance/Mod/Tensor.v's `tensor_hom_ext` generators, by `reflexivity`; the two functors are NOT convertible (probe N1), not even objectwise (N5, the carriers accepted beside it). **Stale premises**: the issue's "no category of vector spaces, no dual functor" is FALSE twice (`Vct_F F := RMod (field_ring F)`, Instance/FdVect.v; `Dual`, DoubleDual.v, on ALL F-modules); it calls the dual-object functor "#359's" — `dual` is StarAutonomous.v's (2026-07-10) and #359 (Structure/Monoidal/Dual.v `dual_self_adjoint_on_the_right`, the probe's control) added the self-adjunction; DoubleDual.v's "no monoidal structure on `Vct_F F`" was stale since 2026-08-31 and is repaired in place, line-neutrally; over `FdVect F` the exercise is FALSE (`DualFd` is pointwise invertible by `double_dual_iso`), which is why everything runs in `Vct_F F`; the file sits beside `Dual` per the issue's placement clause (`Instance/Vect/DualNoRightAdjoint.v` was the suggestion). **Instance/Mod/Product.v** (new): the tree's first indexed product in an algebraic category — `ProdMod (V : I → RModObject R)`, `prod_proj`, `prod_tuple`, `prod_tuple_proj`, `prod_tuple_unique` (both `reflexivity` pointwise; `prod_proj_component` by `eq_refl`) and their assembly `ProdMod_IsIndexedProduct : IsIndexedProduct V ProdMod prod_proj` whose mediator IS `prod_tuple` (`prod_iprod_mediator`, `eq_refl`); 11 heads + 20 hand-closed `Qed` obligations, all 31 closed, one `Defined` (load-bearing under a flip), no `Set`, closure 43 (Structure/Limit/Product.v 7 and Instance/Mod.v 6 at the margins); the `HasIndexedProducts (RMod R)` instance and indexed coproducts are NOT built (before it, `RMod_Biproduct` and Instance/Ab/Coproduct.v's coproduct were binary, and the tree's `HasIndexedProducts` inhabitants — Sets', Cat's, StrictCat's, functor categories', `[_2, Sets]`'s and `_1`'s (Instance/Sets/Products.v, Instance/Cat/Limit.v, Instance/Fun/Terminal.v, Structure/Limit/Power/Adjunction.v) — include no algebraic category). **Universes**: no `Set` in any of the 41 heads; every head but `vct_zero_hom` carries the two equations identifying the three `FieldObject` levels (`u0 = u`, `u1 = u`, arriving with `Ring_RMod` and `Dual@{u u u _}` alike); stdlib caps by first carrier in file order in the header (`prod_rect` at `vct_zero_hom`, not at `VctLine`, as an earlier revision said). **58 constants closed under the global context, zero `Axioms:` lines** — 41 `.glob` heads (34 `def`, 7 `prf`) + 17 `Program` obligations (8 by hand under `idtac`, 9 by `cat_simpl`); the gate carries the 41 heads and Product.v's 11; six `Defined`, each LOAD-BEARING (flipped one at a time in a copy of the whole file: `op_discrete_cone` stops `PowCocone_IsColimitCocone`, `ZeroCocone` stops `dual_not_PreservesColimitCocone`, `PowCocone_IsColimitCocone` stops the mediator readback `pow_cocone_mediator_component`, `to359` and `from359` stop `dual_iso359`'s proof, `dual_iso359` stops the readback `iso359_to_component`); fourteen `Qed`; closure 103 excluding self (Adjunction/Right.v 18 at the margin, Comparison.v 5, Instance/Mod/Closed.v 2, Instance/Mod/Quotient.v 2, DoubleDual.v 1, Product.v 1, the other nineteen `Require`s 0, none droppable); zero name collisions (`SpanQuot`/`span_quot` and `modprod_*` chosen around Construction/PROP/Presentation.v's `quot` and Lib/Datatypes.v's `prod_setoid`). `Test/ProbeDualNoRightAdjoint433.v` mirrors the `Require` list plus Structure/Monoidal/Dual.v and Structure/Limit/Product.v and carries **6 refutation commands = 1 instrument + N1, N5 CONVERSION + N2-N3 TYPING (the unconditional statement does not ascribe; the self-adjunction's direction) + N4 UNIVERSE**, each stripped one at a time in a copy of the whole file beside its accepted control; seven `eq_refl` readbacks; guard coverage 25/19 with six exhaustive exceptions (identifier tokens inside the refutations / also named outside, comments stripped); rename-simulated 16 library names (listed in the header), every first break on a positive line; `make todo` grows by those 6 lines only (2266 → 2272 over 64b94901), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: the unconditional refutation; `HasIndexedProducts (RMod R)`; ⊕_I; the isomorphism of the two duals in any form but `Functor_Setoid`'s; a self-adjunction of `VctDual359` of its own; anything over `FdVect F`; no edit to Instance/FdVect.v, Instance/Mod.v, Instance/Mod/Quotient.v, Instance/Mod/Closed.v, Structure/Monoidal/StarAutonomous.v, Structure/Monoidal/Dual.v, Structure/Limit/Comparison.v or Adjunction/Continuity.v.
- **Instance/Matr/Determinant.v**: The determinant over a commutative ring, in full — first-column Laplace `det` over `Matr`'s setoid matrices with the `fin_delete`/`minor`/`pow_neg1` kit and explicit unfolding rules (`det_S`/`fin_sum_S` — plain `simpl` over-unfolds `fin_sum`), `det_id`, `det_map` (ring homs commute with the polynomial formula, negation by `RigHom_neg`), and the multiplicativity chain with NO symmetric-group enumeration: column multilinearity → first-ROW Laplace `det_row_expand` (making adjacent-equal-columns alternation a two-term cancellation, no involution pairing) → swap-negation → general alternation `det_alt` (NOT derived from det M ≈ −det M, which is vacuous in characteristic 2) → `det_perm` for EVERY index function (non-injective killed by decidable `fin_search`) → nested-sum product expansion → `det_mul`/`det_compose`; then both Laplace expansions (`det_transpose`, cycle-based `det_col_expand`, alien-cofactor `det_col_alien`) give the adjugate `adj` with `adj_mul_l`/`adj_mul_r` and invertibility both ways (`det_unit_of_iso`, `iso_of_det_unit`); eq_refl acceptance tests at 2×2/3×3 (det [[1,2],[3,4]] = −2, adjugate entries, det∘compose = product) and hypothesis-free ℤ exports; reusable ring-free index combinatorics (`fswap` kit, `fin_search`, and `all_fins` with completeness/NoDup — disclosed as the tree's third copy of the Fin enumeration alongside FinSet/Skeleton.v's `fin_enum` and FinSet/Pushout.v's `all_fin`, re-derived to avoid a cross-hierarchy dependency, with the NoDup helper renamed `map_FS_NoDup` to avoid Skeleton.v's name) via per-lemma `Proof using Type`; invertibility also in existential form (`invertible_of_det_unit`). Instance/Matr/GL.v assembles Mac Lane's §I.3–I.4 flagship: `UnitsOf : MonObject → GrpObject` (units-of-a-monoid ONCE — first-component setoid, swap-inverse; `mon_inverse_unique` makes that the right quotient) with `UnitsOf_map`, the units functor `(−)^* : CRng ⟶ Grp` at the multiplicative monoid, `GL_n : CRng ⟶ Grp` as `UnitsOf` of the endomorphism monoid `hom_monoid (Matr K) n` with entrywise functoriality, and `det_Transform : GL_n ⟹ (−)^*` — the naturality square `det_{K'} ∘ GL_n f ≈ f^* ∘ det_K` is `det_map`, Mac Lane's first example of a natural transformation, proven, with the determinant lemmas kept reusable for the matrix-equivalence exercise (`maclane:I.4:ex6`)
- **Instance/Sets.v**: Category of setoids (not strict sets!). Monos and epis are both characterized at one universe: `injectivity_is_monic`, and `surjectivity_is_epic` with its projections `surjective_implies_epic` / `epic_implies_surjective`, yielding `Sets_balanced` (monic + epic ⇒ iso) through the existing `bijective_is_iso`. The epi converse had been abandoned mid-proof for years on the belief that it inherited the subobject-classifier size obstruction; it does not. The classical probe does need a truth-value object with carrier `Type`, which is genuinely too big, but the **cokernel pair** `carrier B + carrier B` glued along the image tests the same thing and stays at the universe of the carriers (`Im`/`ckrel`/`CKSetoid`). The classifier obstruction proper is untouched and still real — Instance/Sets/Classifier.v remains cross-universe theorems rather than an instance, and no UNCONDITIONAL one-level instance is built anywhere (Instance/Sets/Classifier/OneLevel.v builds one under `Untruncate` or `DecImage`, and records a measurement, made in a scratch development against this worktree, that the classical instance compiles out of tree on exactly `classic` + `constructive_indefinite_description`, so an in-tree impossibility theorem is out of reach as an ARGUMENT rather than as a theorem) — and now also records `lifted_epic_implies_epic`/`epic_implies_lifted_epic`, i.e. that `Setoid_Lift` is inert for epicness. All constructive: `∃` is `sigT`, so preimages are data and no choice principle is used. `Instance/FinSet/Classifier.v`'s `finset_epic_iff_surjective` needs no substitute probe at all, `Ω := 2` being an honest object of the category, with acceptance tests including the negative `finset_const_true_not_epic`
- **Instance/Grp/TwoFunctors.v**: Mac Lane §I.3's exercise — two functors that share the identity object function and differ on arrows, distinctness proved in `Functor_StrictEq_Setoid` (the `StrictCat` hom-setoid), because `Cat`'s weak `Functor_Setoid` identifies naturally isomorphic functors and so cannot state it. The machine is `Functor/Twist.v`: for any family `α : ∀ x, x ≅ F x` with naturality NOT assumed, `Twist F α` is an endofunctor whose object function is literally `fun x => x`; it is strictly equal to `Id` exactly when α is natural (`Twist_natural_strict_id`, with the converse under UIP on objects), yet for `F = Id` it is ALWAYS naturally isomorphic to `Id` (`Twist_Id_weak_equiv`) — the strict/weak seam in one construction. Group witness: `S3` (nonabelian, semidirect presentation over a decidable carrier) with inner automorphisms `Grp_conj`/`Grp_conj_iso`, twisted over `GrpAt S3`, the one-object full subcategory of `Grp` on S3 (inclusion full, faithful, injective on objects), separated on the concrete morphism `S3_conj_r` at the element `S3_s`. The degenerate cases are PROVED degenerate rather than avoided by assertion: conjugation over an abelian group is the identity (`Grp_conj_abelian`, instantiated at `Z2`), and the canonical uniform family — inversion into the opposite group — is natural, so its twist is an endofunctor of the WHOLE of `Grp` with the identity object function that is strictly equal to `Id` (`Grp_op_twist_is_Id`). **The issue's own ask — a distinct pair over all of `Grp` — is NOT delivered**; no such family is constructed; the classical route needs a decision procedure for object equality, which costs `classic` plus `constructive_indefinite_description`. **Construction/Free/TwoFunctors.v** is the group-free companion (Fong–Spivak §3.3.2 Ex. 3.40): the free category on one arrow to the free category on two parallel arrows, the same object function, different images of the generator, distinctness again strict
- **Instance/Sets/Products.v**: Small products and coproducts in `Sets` — the tree's FIRST `HasIndexedProducts` instance at any category (dependent-function setoids `∀ i, F i` under pointwise `≈`) and the first `HasIndexedCoproducts` instance (sigma setoids `{ i : A & F i }`, two elements identified by an equality of indices transporting one to the other); both funext-free by the setoid discipline. The smallness side condition is carried by UNIVERSES, not by hypothesis: `Structure/Limit/Product.v`'s existing class is instantiated AS STATED, with no variant — `{A : Type}` is a universe PARAMETER of the class, not a quantifier over all universes — and `Sets_HasIndexedProducts@{u u0} : HasIndexedProducts@{u0 u0 u0 u0 u u0} Sets@{u0 u}` identifies all three of the class's index universes with `o` — the universe of the CARRIERS of `Sets`' objects, where the small sets live. NOT with the universe of `obj[Sets]`: `Sets@{o so} : Category@{so o o}`, so qua `Category` its objects live at `so`, strictly above `o`, and indexing by `obj[Sets]` is precisely the universe inconsistency named next. The condition genuinely bites, and precisely: a family indexed by the objects of the SAME `Sets` is a universe inconsistency, and the unpinned form typechecks only because Coq silently uses a STRICTLY SMALLER `Sets` for the index (`obj[Sets@{u0 u}] → obj[Sets@{u u1}]` with `u0 < u < u1`). The one index a class was previously refused for — the endomorphism-indexed product of `Theory/WeaklyInitial.v` — is shown ABSENT as an obstacle at `Sets`: `Sets_endo_iprod`/`Sets_endo_iprod_ump` build `∏_{u : P ~> P} P` WITH ITS UMP from this very instance, at the same `Sets@{u0 u}`, no universe growth. The general note stands unchanged and is quoted verbatim in the header, since for an arbitrary category the hom universe need not sit where the class puts the index. `HasIndexedCoproducts` is NEW vocabulary (`Structure/Limit/Coproduct.v`), defined as `@HasIndexedProducts (C^op)` in the house duality idiom with covariant accessors and no proof obligation of its own. Awodey §2.9 Ex 7(b) lands as `Sets_exponent_IsIndexedProduct`/`Sets_constant_iprod_exponent` (`X^I ≅ ∏_{i∈I} X` at the discrete setoid on the index). Non-degeneracy is checked rather than asserted: `Sets_iprod_bool`/`_icoprod_bool` recover the binary (co)products of `Instance/Sets/{Cartesian,Cocartesian}.v`, `Sets_iprod_nat`/`_icoprod_nat` give the stream and cons decompositions at an infinite index, the empty and singleton cases are PROVED and labelled degenerate, and `Sets_iprod_unit_pigeonhole` / `Sets_iprod_nat_no_pigeonhole` (plus coproduct duals) SEPARATE them — one and the same three-element pigeonhole statement, proved at a singleton index and refuted at `nat`. Header discloses the deferred Mac Lane §III.4 `Top` extension (optional, gated on #259, not a blocker)
- **Instance/Sets/Complete.v**: `Sets_Complete : @Complete Sets` — the FIRST `Complete`/`Cocomplete` instance in the library, hence the first concrete witness for a premise of GAFT/SAFT and `Comma_Complete`. The limit of `F : D ⟶ Sets` is the setoid of compatible families, a sub-setoid of `Sets_iprod_obj (fun d => F d)`; cone coherence IS the compatibility constraint. The header states plainly that this is a DIRECT construction and NOT a routing through "products + equalizers ⇒ complete" — that theorem is not in this tree (`Complete_HasEqualizers`, `Adjunction/GAFT.v`, runs the other way). Smallness again by universes: `Sets_Complete@{u u0} : Complete@{u u u u0}` puts both the diagram category's objects and its homs at `o`, the universe of the carriers of `Sets`' objects (`Sets@{o so} : Category@{so o o}`), strictly below where `obj[Sets]` lives. `Cocomplete Sets` is not provided BY THIS FILE — that sentence used to say it was not provided at all, which `Instance/Sets/Cocomplete.v`'s `Sets_Cocomplete` has falsified since, and the file's own header now says so. This is the premise that was MISSING, not the only one outstanding: `GAFT`'s other two are `PreservesImageLimit U`, discharged for EVERY right adjoint by the pre-existing `right_adjoint_PreservesImageLimit` (`Construction/Comma/Limit.v`), and `SolutionSet U d`, constructible — so with `Sets_Complete` in hand all three are met at once and `Adjunction/GAFT/Sets.v` runs `GAFT`; docs/INHABITATION.md moves the row up to *Witnessed results* accordingly. **EXTENDED for Mac Lane §V.1 Theorem 1 (#407): the SECOND, independent limit oracle, the one the book actually constructs.** `cone_apex F := fobj[ConePresheaf F] (terminal_obj Sets)` is the cone set (that file does NOT import `Construction/Elements.v`, so `SetsOne` is not in its scope, though the two are the same object) — `Structure/Cone.v`'s own presheaf at the terminal setoid, not a re-derived copy, which is the issue's reviewer check — with legs `cone_leg_at d` EVALUATION at `d` (`ν_j : σ ↦ σ_j`), the mediator `cone_set_med N` Mac Lane's `h` sending an element to the cone it induces, and `cone_set_IsALimit`/`cone_set_Limit`/`ConeSet_Complete` the packagings; commuting is `reflexivity` and uniqueness pointwise, with no choice, no funext and no decidability. **The discrete corollary lands against the compatible-family apex here** (`discrete_iprod_iso : Sets_limit_obj (DiscreteCat_Functor f) ≅ Sets_iprod_obj f`, consuming #254's `Sets_iprod_obj` rather than duplicating it), and Mac Lane's own sentence about the CONE SET is carried across in the sibling file — see there. **A UNIVERSE PIN, SCOPED BY MEASUREMENT RATHER THAN ASSERTED**: an earlier revision of the header said the whole discrete section is forced to `{A : Set}`; restating it at `{A : Type}` and compiling shows SIX constants accepted — `discrete_limit`, `discrete_inner_is_iprod`, `discrete_compatible`, `discrete_forget`, `discrete_pair` and the headline `discrete_iprod_iso` — and exactly FOUR refused, precisely those naming `Structure/Limit/Product.v`'s `iprod`/`iprod_proj`, whose `iprod@{u u0 u1} : ∀ {C : Category@{u1 Set Set}}` collapses `Sets`' carrier universe to the literal `Set` and, through `Sets_iprod_obj`'s `u <= u0` bound, carries the index down with it; both donors of `iprod`'s own pin (`DiscreteCat_Functor` and `Limit`/`IsALimit`) are pinned in the probe with `Cone` as the discriminating control
- **Instance/Sets/Cone.v**: Mac Lane §V.1 Theorem 1 and its remark (book p. 110) + Riehl §3.2 Definition 3.2.3 and Theorem 3.2.4 + Awodey §3.3 Example 3.14 + Seven Sketches §3.5.3 Theorem 3.95 — **the cone-set limit read as an adjunction, and the three concrete presentations the books give of it**. The apex is the sibling file's `cone_apex`; what is added here is the comparison, the naturality, and the identifications. **THE COMPARISON IS FREE AND THE FILE SAYS SO**: `cone_apex_iso : Sets_limit_obj F ≅[Sets] cone_apex F` is `limit_unique_iso` applied to the two limit witnesses, with `cone_apex_iso_legs` carrying BOTH leg families — the pair, not a bare `≅`, which is what makes it usable — so no new proof content is spent relating the two oracles; `cone_apex_iso_to_is_med` reads its forward leg back as the cone-set mediator at `eq_refl`, and that readback is disclosed as `limit_unique_iso`'s GENERIC behaviour instantiated rather than anything specific to the cone set. **THE REMARK IS THE ADJUNCTION, CONDITIONALLY AND EXACTLY**: `cone_transpose`/`cone_untranspose` with both round trips give `cone_hom_iso : Cone(X,F) ≅[Sets] Sets(X, Cone(1,F))` NATURAL in the apex (proved, not asserted — the issue's other reviewer check), packaged as `ConeSet_Natural_Iso` in `[Sets^op, Sets]` and as `ConeSet_Representable`; `ConeSet_HasLimitsOfShape` then feeds #353's `Diagonal_Limit_Adjunction` to give `ConeSet_Diagonal_Limit_Adjunction`, whose backward transposition, adjoint value, leg and counit read back at `eq_refl`. **WHICH ORACLE THE ADJUNCTION IS FED IS THE WHOLE CONTENT OF THAT CLAUSE, AND THE FILE REFUSES TO BLUR IT**: fed the PRE-EXISTING `Sets_HasLimitsOfShape` the same identification is not merely false but not even the same TYPE — a TYPING refusal, pinned with the cone-set form beside it as the control — so nothing here claims the book's bijection is the transposition of the pre-existing adjunction. **ONE GRADE IS `≈`-ONLY AND NO CAUSE IS ISOLATED**: the FORWARD transposition compared as a whole morphism with `cone_transpose` is refused; an earlier revision localized that to `Cone_Natural_Transform`'s `abstract`ed `cone_coherence` field, and measurement refutes the localization — the `ACone` round trip does return the leg family on the nose while rebuilding the whole record, but the negative's two sides differ ALREADY at a point `x : X` and again at the produced cone's leg, so the difference is not confined to one law field; `coneset_adj_to_is_transpose` is what holds. **THE THREE CONCRETE PRESENTATIONS**: Awodey's equalizer as the subsetoid `{x | f x ≈ g x}` (`sets_eq_obj`/`sets_IsEqualizer`, three one-line obligations, no choice and no decidability) with `existing_apex_is_limit_obj` pinning by `eq_refl` that the EXISTING instance's apex is instead the compatible-family setoid over the walking parallel pair — so the existing equalizer is neither Awodey's subset nor a cone set — and `concrete_vs_existing` the one-donor gap through `equalizer_unique`; **no fourth `HasEqualizers Sets` is added**, three library files already declaring one (`Sets_HasEqualizers`, `SetsEqualizers`, `DiagSets_HasEqualizers`), and `karoubi_IsEqualizer` records the idempotent case; Seven Sketches' tuple form as `tuple_obj`/`tuple_iso` with the projections proved to BE the legs; and its presentation-independence clause as `pres_iso`/`pres_iso_legs` — a diagram over a quotient of a quiver and its restriction along the projection have the same limit, so imposed path equations do not change it, which was statable in tree but never connected to limits. **`cone_apex_discrete_iprod_iso` IS THE ONE CONSTANT THAT MAKES MAC LANE'S DISCRETE SENTENCE A THEOREM ABOUT ITS OWN SUBJECT**: the sibling file proves "if J is discrete, Cone(∗,F) is just ∏ⱼ Fⱼ" of the COMPATIBLE-FAMILY apex, and the object the sentence names is the cone set; one `iso_compose` with `cone_apex_iso` carries it across, which an audit found missing after the quote had been shipped directly above a section that did not prove it. Riehl's display's FIRST isomorphism is `Theory/Universal/Element.v`'s appended `global_elements_natural : fobj[Curried_Hom Sets] SetsOne ≅ Id[Sets]` in `[Sets, Sets]`, five of its six clauses closed by the file's default obligation tactic (the tree-wide one; that file sets none) and the sixth the singleton's unique-inhabitant property; the neighbouring `setsone_is_terminal` is disclosed as holding by DELTA rather than by computation, `SetsOne` being DEFINED as the terminal object. **THE ISSUE'S "Current state" IS STALE ON ITS TWO LOAD-BEARING CLAIMS**: `@Complete Sets` is NOT uninhabited (`Sets_Complete` predates this work, which is exactly why a SECOND oracle needed comparing rather than building) and "no `Sets` equalizer instance exists" rests on grepping `Sets_Equalizer`, a name nothing uses, where `HasEqualizers Sets` has three inhabitants; its Verification block also pins `Print Assumptions Sets_cone_limit`, a name that occurs nowhere — the delivered name is `cone_set_Limit`. **16 `eq_refl` statements, FOUR `Defined` in `Cone.v`, of which exactly TWO are load-bearing — measured by flipping each ALONE to `Qed`: the two data obligations of `ConeSet_Representable`, whose next obligation `iso_from_to` then fails to unify against the rebuilt `ConeSet_Representable_obligation_*`, while the other two flip with both `Cone.v` and the probe still compiling and are kept `Defined` by the data convention only (an earlier revision of this bullet said one `Defined`) — closure 86 counting the file** (marginals `Adjunction/Diagonal/Limit` 14, `Instance/Sets/Karoubi` 5, `Theory/Diagram` 2, `Construction/Free/Quiver/Presented` 2, `Adjunction/GAFT/Sets` 2, `Structure/Limit/Unique` 1, every other `Require` 0), nothing depending on the file so that cost lands on no one; all 143 new constants are in the `print-assumptions` gate FULLY QUALIFIED and closed under the global context, the gate's `Axioms:` line count unchanged at 1. `Test/ProbeConeSets407.v` carries **12 refutation commands = 1 instrument + 11 negatives of FOUR kinds told apart by the error TEXT** — 7 CONVERSION, 2 FORMABILITY, 1 TYPING (the wrong oracle, a plain has-type mismatch), 1 RESOLUTION (`Cannot infer the implicit parameter Setoid of equiv`, there being no `≈` grade on objects; an earlier revision of this bullet mis-transcribed that string, which `Instance/Sets/Cone.v` and the probe itself both quote correctly) — each stripped ONE AT A TIME in a copy of the WHOLE file and compiled alone, a discipline that matters because a preamble-plus-command harness drops the enclosing `Section`'s `Context` and local `Universes`/`Constraint` declarations, so the command then refuses for a reason unrelated to the claim — an earlier revision of this bullet added a specific misclassification that an audit could not reproduce, and it is withdrawn rather than defended. `make todo` grows by 13, ALL in the probe, the library files contributing ZERO. NOT delivered: no `Cocomplete Sets` from this route and no colimit dual; no comparison of the two oracles' ADJUNCTIONS as records (only the apexes, through `cone_apex_iso`); the three concrete presentations are each compared with the compatible-family apex and NOT with each other; no functoriality of `cone_apex` in `F`; and nothing is registered as an `Instance`, a chosen limit oracle not being allowed to become globally resolvable
- **Instance/Sets/InverseLimit.v**: Mac Lane §V.1 construction 1 (book p. 109) + Riehl §3.1 Definition 3.1.21 and §3.2 Example 3.2.8 (printed pp. 88/95) — **the inverse limit of a tower of sets as the set of MATCHING STRINGS, with its projections, its universal property, and its identification with #407's cone set**. **THE INCREMENT IS THE PRESENTATION, NOT EXISTENCE, AND THE ISSUE'S OWN SURVEY IS STALE ON TWO OF ITS THREE COUNTS WITH THE THIRD ANSWERED RATHER THAN REFUTED**: re-measured at `067ee4eb`, `Sets_HasIndexedProducts` exists (`Instance/Sets/Products.v`, one of THREE UNCONDITIONAL inhabitants at a named category with `Two_Sets_HasIndexedProducts` and `One_HasIndexedProducts` — the criterion matters, a fourth constant `Instance/Fun/Terminal.v`'s `Fun_HasIndexedProducts` concluding `HasIndexedProducts` CONDITIONALLY on one), the compatible-family idiom is already in `Instance/Sets/Complete.v` AT ARBITRARY SHAPE rather than only at the end/wedge, and `Sets_Complete` therefore already supplies a `Limit F` for every `F : Omega^op ⟶ Sets` with no work at all — so what was absent is the tower's own description, compatibility at the SUCCESSOR STEPS ONLY, and the theorem that it agrees with the general condition, which quantifies over EVERY arrow of `Omega^op`, i.e. over every proof of an inequality. **That bridge is `tower_compat_all`**, the exact analogue of `Instance/Sets/Cone.v`'s `tuple_compat_paths` (generators → paths) one shape along — the only proof with content in sections (A)–(D) apart from `tower_obj`'s own `Equivalence` obligation, with section (H) adding two further inductions (`stream_trunc_compat`, `stream_trunc_respects`); counted by tactic, exactly FOUR scripts in the file exceed three tactics and every other is a single `exact` or `reflexivity`, so an "only new proof content in the file" claim would be false and is not made — `Instance/Sets/Cone.v` carried exactly that defect until an audit caught it. **Its successor case is DEFINITIONAL rather than a rewrite**: `Omega`'s composition is `le_t_trans g f` and `le_t_trans` recurses on its SECOND argument, so in `Omega^op` the composite `f' ∘ tower_step k` reduces by two iota steps to `le_t_S f'`, the very constructor the induction peels, and `fmap_comp` applies with no reindexing. Delivered: `tower_obj` with `tower_proj` and `tower_ext` (an element is determined by its coordinates), `tower_iso : Sets_limit_obj F ≅ tower_obj F` with BOTH legs the identity on the underlying family at `eq_refl` (the two presentations differ in what they CARRY, never in the string), **`Sets_tower_limit : IsALimit F (tower_obj F)`** and `Sets_tower_Limit` with the mediator, its commutation and its uniqueness as named constants, `limit_leg (Sets_tower_limit F) n = tower_proj F n` at `eq_refl`, and **`Sets_tower_limit_iso_cone_limit : tower_obj F ≅ cone_apex F`** — Mac Lane's own sentence that a matching string IS a cone from the one-point set — obtained from `limit_unique_iso` so that `Sets_tower_limit_iso_cone_limit_legs` carries BOTH leg families rather than leaving a bare `≅`. **`Cochain` STOPS BEING DEAD CODE**: `Construction/Chain.v` had been used in no file but its own (measured), and `cochain_inverse_limit`/`cochain_tower_limit` are its first consumers anywhere — **at a DONOR UNIVERSE PIN that is measured and disclosed rather than repaired**, `Cochain@{u u0}` being declared over `C : Category@{u0 Set Set}` (hom AND proof at the literal `Set`) where `Chain@{u u0 u1}` one line above it leaves its TARGET category FREE (`C : Category@{u u0 u0}`) — read that precisely: `Chain` is NOT free of `Set`, its printed type carrying `Omega@{u1 Set Set}`, so BOTH pin `Omega`'s hom and proof and what separates them is the target alone — and `Omega@{o h p}` itself is free in all three, so the consumer is confined to `Sets@{Set su}` while sections (A)–(D) carry no `Set` at all (`tower_obj@{…} : Omega@{u u0 u0}^op ⟶ Sets@{u0 u1} → obj[Sets@{u4 u2}]`); the pin is the donor's and is NOT claimed unavoidable. Riehl's §3.2 stream clause is delivered at its true strength: `StreamTower := Cochain (StreamF A)` is the classical tower `1 ← A ← A² ← …`, `stream_to_tower` sends a stream to its string of truncations with its coordinates read back at `eq_refl`, and it is **NOT proved invertible** — the inverse is a corecursive assembly plus two round trips up to bisimilarity — so nothing claims the inverse limit IS `Instance/Sets/Streams.v`'s `Stream_final`; the closure cost of reaching the streams is THREE modules (89 → 92, measured), so the deferral is about proof content and not dependencies. Non-vacuity is the constant tower, `Diagonal (Omega^op)` CONSUMED rather than rebuilt, with two provably distinct points (`const_string_separates`, by `discriminate` at coordinate 0) — THAT witness's transitions are identities, but section (H)'s `StreamTower` is genuinely lossy (two distinct stage-2 points share a stage-1 image), so the file does contain a lossy tower and an earlier revision of this bullet said otherwise; **what no witness has is an EMPTY inverse limit, and that phenomenon is not claimed**. **60/60 constants closed under the global context** (45 declared heads + 15 `Program` obligations, counted on `Print Module` — an earlier count of 48 was taken before the streams section existed, described the GATE rather than the module, and left all ten streams constants ungated; both are corrected here), all in the gate FULLY QUALIFIED, the gate's `Closed` count rising 5534 → 5594 with the `Axioms:` line count unchanged at 1; closure **92** excluding nothing (drop-one marginals `Instance/Sets/Cone` 46, `Instance/Sets/Streams` 3, `Construction/Chain` 1, every other `Require` 0); TWO `Defined` tokens, NEITHER load-bearing by flipping each alone to `Qed`, kept by the data convention that `Instance/Sets/Cone.v`'s `tuple_fwd`/`tuple_bwd` follow; 15 `eq_refl` statements, of which TWO — `tower_step_is_omega_step` and `inverse_limit_is_tower` — hold by DELTA and are disclosed in the file as guards against name drift rather than measurements (the `setsone_is_terminal` precedent), while `step_is_le_t_S` machine-checks the header's own mechanism claim by pinning `f ∘ tower_step k = le_t_S f` on the nose. `Test/ProbeInverseLimit408.v` carries **5 refutation commands = 1 instrument + 4 negatives of THREE kinds told apart by the error TEXT** — 2 CONVERSION (`tower_obj F` is neither `Sets_limit_obj F` nor `cone_apex F`, with the two isomorphisms beside them as controls), 1 TYPING (a covariant `Omega`-diagram is not a tower: a plain has-type mismatch with no `cannot unify` and no universe clause) and 1 FORMABILITY (the `Cochain` pin, with `Chain` at the SAME declared levels as the discriminating control) — each stripped ONE AT A TIME in a copy of the WHOLE file and compiled alone. `make todo` grows by **9, ALL in the probe**, the library file contributing ZERO. Two engineering findings: `Category.Functor.Opposite` opens `functor_scope`, so a bare `Omega^op` in an ASCRIPTION (where no expected type binds the scope) parses as `Opposite_Functor Omega` and dies naming neither culprit — `Open Scope category_scope` is the guard; and the `reflexivity _` that first stood in `const_string`'s compatibility witness elaborates on Rocq 9.1 but leaves the relation an unresolved implicit on Coq 8.19 AND 8.20, so it is a lemma closed by the TACTIC instead — measured on both legacy versions, where the base commit is green. (A second 'finding', that a dependent `Fixpoint` into `fobj[Cochain G] n` NEEDS its `match` annotated, was claimed and is REFUTED: the plain form compiles, the `Fixpoint`'s declared result type already fixing the return predicate.) NOT delivered: no cone-as-a-diagram-of-shape-`(ω+1)^op` reading (the library's generic `Cone` is used instead and no functor `(ω+1)^op ⟶ Sets` is formed, `Instance/Omega.v` carrying no `ω+1`); no generalization to an arbitrary limit ordinal (that file is nat-indexed only and the tree has no TRANSFINITE machinery — `Instance/Ordinal.v`'s `Ordinal n` is the FINITE ordinals, so the issue's own "no ordinal machinery" is too broad and is not repeated); nothing for a general target category, the construction reading elements; no inverse to `stream_to_tower`; and nothing registered as an `Instance`
- **Instance/Sets/Quotient.v**, **Instance/Sets/Quotient/Partition.v**, **Instance/Sets/Coequalizer.v**, **Instance/Sets/Coequalizer/Interconnect.v**: Mac Lane §III.1 construction 4 + §III.3 remark 2 and Ex 5, Awodey §3.4 Example 3.19 / §3.5 Ex 4, Seven Sketches §1.2.1 Def 1.21 and §6.2.4 Example 6.42 — quotient setoids and **`Sets_HasCoequalizers`, the tree's FIRST `HasCoequalizers` inhabitant at any category** (measured: only the class at `Structure/Coequalizer.v`, the DERIVED `HasCoequalizers_HasReflexiveCoequalizers`, and prose existed). **THE TWO PRESENTATIONS OF THE HYPOTHESIS ARE THE SAME ONE, AND THAT IS A THEOREM**: the issue asks separately for a quotient by a “setoid-respecting” relation and for one by a relation “coarser than `≈`”, and `coarser_iff_respects` proves them equivalent for an EQUIVALENCE relation, each direction consuming exactly one law (`coarser_respects` transitivity, `respects_coarser` reflexivity), so ONE construction `SetsQuotient A R HR` is built rather than two. Finer measurement: the quotient OBJECT needs only `Equivalence R`, and `SetoidCoarser` enters exactly at the PROJECTION. The house carrier discipline holds — same carrier, coarser `≈` — and `sets_quot_finest_eq : SetsQuotient A equiv A = A` closes by `eq_refl` on the **WHOLE RECORD**. The universal property is an `AUniversalElement (RespFunctor R)` over Theory/Universal/Element.v's class; **the Yoneda route REFUSES at `Sets`** (that file's donor restriction identifies object, hom and proof universes while `Sets@{o so} : Category@{so o o}`), pinned as two `Fail` probes with positive controls, so the Yoneda-free `ue_representation`/`AUniversalElement_of_repr` route carries `sets_quot_Representable`. **Mac Lane §III.3 Ex 5 lands ON THE NOSE**: `SetsRelObj X R` is the triples ⟨x,y,w⟩ with `w : R x y` compared by `≈` in the first two components only (the witness is data the equality ignores), and `sets_rel_IsCoequalizer` exhibits the quotient ITSELF — not something isomorphic to it — as the coequalizer of the two projections, with `sets_rel_coeq_iso` relating it to the generated-closure coequalizer by the identity on carriers both ways; it is NOT claimed to be a subobject of `X × X` (no map to the product is built and nothing is proved monic). **Seven Sketches' partition presentation hits a GENUINE UNIVERSE WALL, proved by probe rather than asserted**: a `Type@{o}`-valued class object does not exist at the carrier's level, `carrier A → Type@{o}` living at `Type@{o+1}`, pinned as `Fail Definition probe_parts_type_same@{o}` against a `Prop`-valued control at the same level and a `Type`-valued one a level up — and the same wall refutes the impredicative intersection Awodey's §3.5 Ex 4 uses, so THAT header claim is measured too. It is therefore delivered at THREE GRADED STRENGTHS rather than one: `sets_part_of` exists under the quotient's own hypotheses, is surjective, and separates UP TO TRUNCATION; `sets_partition_iso` is an isomorphism in `Sets` under `SquashStable`; and `partition_stability_is_the_conclusion` proves that hypothesis IS injectivity of `sets_part_of`, so no cheaper one exists (the `Instance/Grp/Epi.v` discipline) — with `prop_rel_squash_stable` discharging it for `Prop`-valued relations, which is the book's own setting, so Seven Sketches' identification is recovered with nothing extra. `SetsPartsProp` is the strict reading (a sub-setoid of Powerset.v's `Powerset_Prop_obj`, no representative); its evident inverse would be large elimination out of `Prop`, stated as unavailable BY THAT CANDIDATE with **no impossibility claimed**. The §6.2.4 circuit witness is EVALUATED — six ports, three wires, three terminals (one a lone unconnected port) — with the count PROVED rather than read off (`ic_terminal_cases` exhaustive AND `ic_terminal_distinct` pairwise), the carrier the port type by `eq_refl`, and the mediator produced by `coeq_desc` out of the delivered `IsCoequalizer` computing on closed ports; it is over `Sets` DELIBERATELY, since `FinSet` computes counts but has no registered `HasCoequalizers` instance (the inhabitant #405 later derived, `FinSet_derived_HasCoequalizers`, is a plain `Definition`), so a witness there would exercise a parallel construction — **nothing is claimed about `FinSet`**. Errata corrected in the file headers: the issue's “`rg -i 'coequalizer' Instance/` returns exactly two hits, both prose” is stale (22 hits in 3 files, including `Instance/Grp/Quotient/Colimit.v`'s REAL `quot_IsCoequalizer`/`normal_closure_IsCoequalizer` from #313) and its `Instance/Two.v` is off by four. Deferred: no `Cocomplete Sets`; no `HasCoequalizers FinSet` FROM THIS FILE — the tree's first such inhabitant is `FinSet_derived_HasCoequalizers` in Test/ProbeToposColimits405.v, obtained from Structure/Topos/Colimits.v's `topos_HasCoequalizers` at `FinSet_Topos` and NOT by a construction in `FinSet`; no functoriality of the quotient in `R` and no comparison with `Construction/Quotient.v`'s category quotient; no `RegularEpi` statement; no normal form for `coeq_rel`; and only the relation→parts direction of the Seven Sketches correspondence, partitions not being a first-class structure
- **Instance/Sets/Cocomplete.v**, **Instance/Sets/Chain.v**: Mac Lane §III.3 pp. 67-68 and §V.1 Exercise 8 (Riehl 2nd ed. §3.1 Example 3.1.26) — **`Sets_Cocomplete : @Cocomplete Sets`**, the tree's first HYPOTHESIS-FREE cocompleteness witness (the only other `@Cocomplete _` constants — `Proset_Cocomplete_of_all_joins`, `Cocomplete_equivalence_invariant`, `creates_colimits_Cocomplete` — each take one; that parenthetical held when written and is corrected here: since 2026-09-02 `Instance/Powerset.v`'s `Subsets_Cocomplete` takes none, the cocompleteness of a setoid's power set, and since #450 `Grp_Cocomplete_via_GAFT`, `Rng_Cocomplete_via_GAFT` and `SVariety_Cocomplete_via_GAFT` take none, each obtained by the adjoint functor theorem at the diagonal (CORRECTION, #454: and since #454 so does `Instance/Mod/Colimit.v`'s `RMod_Cocomplete_via_GAFT`, at every ring, by the same route), while "first" stands). It is the GENERAL small-colimit witness, `∀ (D : Category) (F : D ⟶ Sets), Colimit F` for EVERY shape, not merely the ω-chain reading the issue's title emphasises — V.1.8 demands the general statement. Construction: `Sets_icoprod_obj` (#254's sigma setoid) quotiented by `colim_rel`, a four-constructor inductive (fibre equivalence, diagram glue, symmetry, transitivity; reflexivity derived — the Coend.v shape), with the apex `SetsQuotient` of the first by the second. **`Sets_HasCoequalizers` is NOT consumed** — the layer beneath it is — so `Instance/Sets/Coequalizer.v`'s forecast about what a later cocompleteness proof would need is not what happened; and no "coproducts + coequalizers ⇒ cocomplete" reduction exists in tree in either variance, so this is a direct construction like its `Sets_Complete` mirror. **The universe block is FREER than that mirror, though read "earned" narrowly — what was earned is writing the annotation, and the mirror could have it too: the identification at `Sets_Complete` is ALSO minimization, its body accepting the same annotation in a scratch check, so nothing here says limits are inherently tighter than colimits**: `Sets_Cocomplete@{uc ud uo uso}` carries `ud <= uo` where `Sets_Complete@{u u0} : Complete@{u u u u0}` identifies them — the diagram category's OBJECTS may sit strictly below `Sets`' carrier universe. Two measured facts behind it: the four binders are written out DELIBERATELY (unannotated it minimizes to `ud := uo`, and the identification is minimization rather than a constraint — a declared `Constraint pd < po` instantiation is accepted), and the colimit leg is built DIRECTLY rather than as the donor composite `sets_quot_proj ∘ Sets_icoprod_inj`, because `Sets_icoprod_inj@{u u0}` identifies index and carrier universes and that identification was measured to propagate to the headline. The two legs are nonetheless proved equal at `eq_refl`. **No duality shortcut exists and that is measured, not assumed**: `Colimit F := Limit (F^op)` is definitional, but `F^op` lands in `Sets^op`, not `Sets` — what would give the colimit is `Complete (Sets^op)`, which IS `Cocomplete Sets` again. **NON-VACUITY IS PROVED IN BOTH DIRECTIONS, EACH AT ONE SHAPE — read that scope, since neither is a general non-collapse theorem and both headers refuse to say otherwise.** The quotient does not collapse: at `DiscreteCat bool` with a singleton over each object, `two_fibres_not_collapsed` separates the two fibres by mapping OUT through `colim_rel_separates` to `discriminate` (no induction on the relation could yield a negative), with the degeneracies excluded by proof — `two_disc_objects_distinct`, both fibre points NAMED and appearing in the theorem statement so each typechecks only because its fibre is inhabited, and `two_disc_hom_is_eq` recording at `eq_refl` that the shape's homs are index equality, which is why the discrete case is the sharp one. And the quotient does merge: a discrete shape has no connecting maps and so can witness nothing here, so `omega_stages_merged` / `omega_stages_apart_in_coproduct` (Chain.v) carry that half at the ω-shape. **What the two witnesses JOINTLY establish is that NEITHER endpoint describes `colim_rel` uniformly** — the ω pair refutes "always the coproduct's relation", the discrete witness refutes "always total" — a statement about the FAMILY. It is NOT that the relation sits strictly inside that interval at either shape; measured, it sits AT an endpoint at both (total at `OmegaPoints`, equal to the coproduct's relation at `TwoPoints`). An earlier revision of this bullet said the two ω constants "place `colim_rel` STRICTLY between" the two, which no in-tree theorem supports and which is refutable at both witnesses. The empty-shape reading is included and explicitly labelled a DEGENERATE control that does not stand alone. `Instance/Sets/Chain.v` gives the ω-reading as `Sets_Omega_Colimit` and `Sets_Chain_Colimit`, both bodies pure applications of `Sets_Cocomplete` — Riehl's checkbox asks for a `Colimit` over `Construction/Chain.v`'s shape rather than a bespoke construction, and that is what they are. SCOPE: the union characterisation is ONE-DIRECTIONAL — the sufficient half (`omega_colim_of_stage`) is proved, the NECESSARY half is not, because the converse needs a directedness merge comparing `fmap[G]` at two order proofs. Read that reason precisely: `Instance/Omega.v` itself declares `le_t` with no uniqueness lemma, but the lemma EXISTS in tree — `le_t_irr {m n} (f g : le_t m n) : f = g` at `Instance/Ordinal.v`, axiom-free by UIP on `nat`, in a file that requires `Omega` and reuses that same `le_t` — so the deferral is a SCOPE choice, not a missing lemma, and an earlier revision's "that would have to be proved first" was wrong. So the legs are NOT shown injective for inclusion-like chains and "the colimit IS the union" is not claimed. Also not delivered BY THESE FILES: no normal form for `colim_rel`, hence no decision procedure and no filtered-collapse theorem; no comparison with `Sets_HasCoequalizers`, `Sets_HasIndexedCoproducts` as the discrete case, `Instance/Sets/Pushout.v` or `SetsCoend`; no functoriality in F; no `AdamekData`, so `Theory/Adamek/Corollaries.v`'s chain gains a `Cocomplete` witness at `Sets` but stays conditional. `Sets_Chain_Colimit@{u}` is PINNED to `Sets@{Set u}`, attributed to `Construction/Chain.v`'s own result type fixing Omega's hom/proof universes at `Set` — inherited, not repaired, not claimed unavoidable. `Test/ProbeCocomplete329.v` guards the boundary with 3 negatives plus an instrument check, rename-simulated at 8/8 over the eight constants in the NEGATIVES — the audit found `Opposite` unguarded in the very expression whose `Opposite_Functor` a first run had already fixed, and found the whole-morphism triangle refutation measured but pinned nowhere; both are now shipped, and the previously claimed "13/13" matched no countable set, and records the probe-hygiene trap that spelling the duality negative as `Sets_Complete (D^op) (F^op)` is a FALSE PASS — `Functor/Opposite.v` opens `functor_scope`, so `D^op` parses as `Opposite_Functor D` and the command fails on notation rather than on the mathematics
- **Instance/Sets/Pullback.v**: Mac Lane §III.4 Exercises 1 and 6 (book p. 72) + Awodey §5.2 (construction and Example 5.9) + Riehl §3.2 Example 3.2.9 — pullbacks and kernel pairs in `Sets`. **`Sets_HasPullbacks : HasPullbacks Sets`** is the agreement sub-setoid of the product carrier with its projections and `∃!` mediator, and is the second inhabitant of `HasPullbacks` **FOR A CONCRETE CATEGORY** — read that qualifier, because unqualified the claim is false and this very commit refutes it: three generic conditionals inhabit `HasPullbacks C` (`HasPullbacks_of_Cartesian_HasEqualizers`, `HasPullbacks_op_of_HasPushouts`, `codomain_cleaving_pullbacks`), and the accompanying probe itself builds a second `HasPullbacks Sets` out of the first of them. `FinSet_Pullbacks` (`Instance/FinSet/Classifier.v`) was the only prior concrete one, and it lives in the finite SKELETAL model where objects are naturals; `Instance/Sets/Classifier.v`'s `sets_char_pullback` proves ONE square is a pullback, not that arbitrary cospans have them, and it sits one universe up. **THE ISSUE'S SUGGESTED SHORTCUT WAS MEASURED AND DECLINED, AND THE MEASUREMENT IS THE INTERESTING PART**: it proposes reading the instance off #326's `HasPullbacks_of_Cartesian_HasEqualizers` now that that has landed, and the derivation DOES elaborate — but the derived apex is **transparent and wrong**, not opaque: it reduces on the nose to `Sets_limit_obj (APair (f ∘ exl) (g ∘ exr))`, the compatible-family setoid over the walking parallel pair, so an element is a dependent function plus a constraint quantified over every arrow of `Parallel` rather than a PAIR (`Fail … DerObj = sets_pb_obj f g` gives `cannot unify`). Since `Sets_HasEqualizers := Complete_HasEqualizers Sets_Complete` is itself read off completeness, that was predictable; it is now measured. The build is therefore DIRECT, and #326's reduction is used the other way round, for the Riehl deliverable. That measurement is pinned in `Test/ProbeSetsPullback333.v` rather than in the file, because stating it needs `Adjunction/GAFT/Sets.v` and requiring that from the target would put the whole GAFT closure behind every consumer of `Sets` pullbacks. **Mac Lane Ex 6 in full**: `sets_kernel_pair_obj` records `Pull _ _ (kernel_pair f) = sets_ker f` at LEIBNIZ `=` (with `_fst`/`_snd` likewise), so `Structure/Regular.v`'s generic `kernel_pair` IS the relation setoid `{(x,x′) | f x ≈ f x′}` on the nose, and the equivalence-relation reading is given AS ARROWS — `sets_ker_refl : a ~> sets_ker f`, `sets_ker_sym : sets_ker f ~> sets_ker f`, `sets_ker_trans : sets_ker_comp f ~> sets_ker f` — with all six leg equations and `sets_ker_comp_is_pullback` by `eq_refl`. **Awodey Example 5.9** is `sets_preimage_IsPullback` (the pullback of a subset inclusion `V ↪ B` along `f` is `{a | f a ∈ V}`) together with the elementwise criterion `sets_preimage_criterion : FactorsThrough (sub_incl (sets_preimage f S)) t ↔ FactorsThrough (sub_incl S) (f ∘ t)` — kept `Defined` rather than `Qed` deliberately, since with `Qed` the downstairs factorization does not reduce and the witness evaluations fail. **Riehl's follow-on**, `sets_pullback_is_equalizer` with `sets_equalizer_is_pullback` back, is the pullback AS the equalizer of `f ∘ exl` and `g ∘ exr`. **Read that novelty precisely** — the issue calls this "nowhere stated in ANY category" and cites `Structure/Topos.v` as recording a known gap; BOTH are stale, and stale because of #326: that header now says the redundancy "is now a CHOICE rather than a gap" and that the reduction AND its converse "ARE formalized", naming `equalizer_of_pullback` (`Structure/Pullback/Reduction.v`). What is genuinely new here is the first INSTANTIATION at a concrete category — outside `Structure/` and `Test/` the only other `IsEqualizer` term is `matr_IsEqualizer_op` (`Instance/Matr/Coequalizer.v`), an op-read of a matrix COequalizer. Non-vacuity computes and is not degenerate: over the graph of `Nat.even`, the mediator evaluates (`sets_pb_snd (even_med 5) = false` by `eq_refl`); `even_med_is_ump` shows the `ump_pullbacks` mediator IS `sets_pb_med` on the nose but carries NO non-vacuity content, since the GENERAL equation at an arbitrary cospan also holds by `eq_refl` (measured) — so it would hold at degenerate cospans too, and the general form a consumer would want is NOT stated; `sets_ker evenM` relates 0 and 2 with `even_ker_not_diagonal` proving the relation is NOT the diagonal, and `(0,2)∘(2,4) = (0,4)` computes; the preimage side factors a non-constant map and carries it downstairs by the criterion, with `constThree_does_not_factor` as the negative. Universes are clean: `Sets_HasPullbacks@{u u0}` carries only `Sets`' own `u0 < u` — no identification, no `Set` — and even the concrete witnesses stay polymorphic by going through `eq_Setoid` (`Lib/Setoid.v`) rather than resolving `eq_equivalence` at an unannotated binder. 103/103 constants closed under the global context (95 glob `def|prf|inst|proj|rec` + 7 `_obligation_` constants that `Print Module` lists and the glob does not + `Build_SubsetOf`, which appears in neither). The probe carries **4 negatives of TWO kinds** — three CONVERSION with distinct causes (the derivation-route mismatch above; the pairing failing to round-trip strictly because stdlib `prod` has no definitional eta; and the equalizer round trip at MORPHISM level, which the target's header called measured while an earlier revision of the probe pinned it nowhere) and one TYPING (the bundled `Pullback` record carries its apex as data, so it is not applicable to an apex and two legs — `Illegal application`, a donor fact `Theory/Morphisms/CokernelPair.v` already pins for the dual) — plus an instrument check, each stripped and its kind confirmed, rename-simulated **10/10** on an unpadded denominator. NOT delivered: the `Top` half of Exercise 1, gated on #259 and recorded as an extension; no `Sets` pushout/pullback interaction; no stability or pasting results beyond what `Theory/Morphisms/Stability.v` already gives
- **Instance/Sets/Powerset.v**: The covariant and contravariant power-set functors on `Sets` (Mac Lane §I.3's first worked example of a functor, with the §II.2 inverse-image companion) over TWO carriers of subsets, because one carrier cannot do both jobs. (1) The PROOF-RELEVANT carrier `Powerset_obj X` — the `≈`-respecting `Type@{o}`-valued predicates under pointwise `↔`, i.e. the hom-setoid `Setoid_Lift X ~> PropSetoid` assembled from Instance/Sets/Classifier.v's donors with no new record — whose placement is honestly CROSS-UNIVERSE and carried in the types: `Powerset_obj@{o so} : SetoidObject@{o o} → SetoidObject@{so so}` with `o < so` (note `o` is the universe of the CARRIERS of `Sets`' objects, not the object universe, which is `so` — `Sets@{o so} : Category@{so o o}`), so `Powerset : Sets@{o so} ⟶ Sets@{so sso}` (direct image `∃ x, S x ∧ f x ≈ y`) and `Powerset_op : (Sets@{o so})^op ⟶ Sets@{so sso}` (inverse image `λ y, T (f y)`) are NOT endofunctors, and the singleton family is `Powerset_Singleton : Sets_Lift ⟹ Powerset` over the carrier-preserving lift. The consumer consequence is disclosed in the file: `@Monad Sets Powerset` and `FAlg Powerset` are BOTH rejected by the universe checker (`Theory/Monad.v` and `Construction/FAlg.v` each require an endofunctor), so the power-set monad (#466) and any initial-algebra reading (#750) cannot ride on this carrier and `Powerset_Singleton` cannot serve as a monad unit. (2) The TRUNCATED carrier `Powerset_Prop_obj X` — the same subsets valued in `Prop`, the direct-image existential truncated impredicatively by `Powerset_squash A := ∀ Q : Prop, (A → Q) → Q` — sits at the SAME universe as the carriers (its one cost is `Set < o`, since `Prop : Type@{Set+1}`), giving a genuine ENDOfunctor `Powerset_Prop : Sets ⟶ Sets` together with Riehl §1.4's singleton transformation in its own shape, `Powerset_Prop_Singleton : Id[Sets] ⟹ Powerset_Prop`, naturality proved; both `@Monad Sets Powerset_Prop` and `FAlg Powerset_Prop` are formable over it and are entered into the environment as `Powerset_Prop_Monad_statement`/`Powerset_Prop_FAlg` so the claim is type-checked rather than asserted (neither the monad nor an initial algebra is BUILT — #466 and #750 stay open). The trade-off is exactly proof relevance versus an endofunctor downstream consumers can use, and `Powerset_truncate : Powerset ⟹ Powerset_Prop_Lift` is the comparison from the first carrier to the second (`Powerset_Prop_Lift` is `Sets_Lift ◯ Powerset_Prop` assembled by hand, since `Compose` shares one hom universe across its three categories; two `reflexivity` lemmas check it IS that composite). SCOPE THE IMPOSSIBILITY — an earlier version of this entry said Riehl's `Id ⟹ P` is NOT STATABLE in this library, and that is RETRACTED both here and in the file's header. It is statable. The rejection holds only for the `Type@{o}`-valued carrier; the inference "`≈` is `Type@{o}`-valued, THEREFORE subsets are" is a non sequitur, since a `Prop`-valued predicate respects a `Type`-valued `≈` fine (`Type@{o} → Prop` is a `Prop` by impredicativity), and Instance/Sets/Classifier.v's obstruction is witness RECOVERY for a pullback UMP (its own note), which a power set does not owe. In-tree precedent for `Prop`-valued subsets at the carrier's own level: `Instance/Ens.v` and `Instance/Rel.v`. Funext-free by design and axiom-free. Non-vacuity is PROVEN rather than asserted, including the meta-point: `Powerset_injective_reflects` shows that along an injective map the direct image merges nothing, so an injective witness testifies to nothing; `Powerset_merges_fibre` is an iff (singletons merge EXACTLY on the fibres of `f`) with the corollary `Powerset_not_injective_on_subsets`; `Fin.t 2`/`Fin.t 3` witnesses show the direct image moves subsets (`Powerset_image_moves`), differs from the inverse image on the same input (`Powerset_direct_ne_inverse`), and cannot grow a singleton the way the inverse image does (`fin32_preimage_sng0` against `fin3_sub01_not_singleton`); and `Powerset_Prop_singletons_distinct` shows the truncation does not collapse the power set
- **Instance/Sets/NoAdjoint.v** (+ `Adjunction/Continuity/Finite.v`): Mac Lane §V.5 Exercises 1 and 4 (book p. 120, `maclane:V.5:ex1`, `maclane:V.5:ex4`) + Awodey §9.6's limit-preservation test for the non-existence of adjoints (`awodey:9.6:remark-adjoint-existence-tests`) — **`times_X_has_left_adjoint_iff_terminal : (∀ L, L ⊣ X × − → X ≅ 1) ∧ (X ≅ 1 → ∃ L, L ⊣ X × −)` in Sets (an instance of the generic `fixed_product_left_adjoint_iff_terminal` over any cartesian category with `1`) and `Sets_op_not_cartesian_closed (CC : @Cartesian (Sets^op)) (CL : @Closed (Sets^op) CC) : False` in the STRONG form quantified over every cartesian structure** — the tree's FIRST negative use of RAPL (its twelve other term-level consumers of `Adjunction/Continuity.v`'s RAPL/LAPC, in seven files, are all positive), the ONE novelty claimed: an earlier revision also claimed "the first no-adjoint result at `Sets`" and "the first stated as an iff", both FALSE — Construction/Slice/Adjunction.v `sets_slice_proj_no_left_adjoint` and `sets_coslice_proj_no_right_adjoint` are at `Sets`, and `slice_proj_left_adjoint_iff_terminal` is an "a left adjoint exists iff the object is terminal" biconditional of this very shape. **Stale premises**: "no impossibility result … no idiom for a functor lacking an adjoint" FALSE — twelve no-adjoint theorems in four shapes predate this (the declarations named `(no|not)_…adjoint` plus Adjunction/Choice.v `two_left_adjoint_proper`: Instance/Top/Image.v `nat_inf_no_left_adjoint`, Instance/Monoid/Translation.v, Adjunction/Choice.v, Instance/Proset/Galois.v, Instance/Powerset/Quantifier.v, Construction/Slice/Adjunction.v, Adjunction/Diagonal/Connected.v `eval_not_left_adjoint`, Instance/Top/Forgetful.v; an earlier revision counted seven; the issue's greps report 1 and 261 hits where the `.v` files have 12 and 786); "'not cartesian closed' only prose" FALSE — Instance/Fun/Closed.v `fun_not_cartesian_closed` is a theorem in the same strong form, so Exercise 4 is a second such result (Closed.v also has the pointwise one) and the first for `Sets^op`; the sites cited in Continuity.v are comments, with the constants below them; `prod_zero_r` (Structure/BiCCC.v) needs `Cartesian`+`Closed`+`Initial` only. Exercise 1 forward is a GENUINE consequence of limit preservation (`right_adjoint_preserves_terminal` → `fobj_one_iso : 1 ≅ X × 1` → `prod_one_r`); the converse `Id_adj_fixed_product : Id ⊣ fixed_product_functor X` is built directly from the pairing bijection `fp_adj_iso` through `Build_Adjunction'` (probe N1: `fixed_product_functor 1 = Id` is refused at `eq_refl`, so no transport along `X × − ≈ Id` would do). Exercise 4: `prod_zero_r` at the point makes the apex of `1 ×[CC] 0` subterminal in `Sets`, the two injections `pt_inl`/`pt_inr` of `OnePlusOne := 1 + 1` then agree, and across summands the coproduct's `≈` IS `False` (`pt_inl_inr_apart` at `eq_refl`); the scaffolding is `Sets_op_Initial := Sets_Terminal` and Cocartesian.v's notation (`@Cartesian (Sets^op) = @Cocartesian Sets` at `eq_refl` holds by notation expansion alone); the two-point set is `1 + 1`, since a morphism into the `bool`-carried setoid pins the CARRIER level of `Sets@{o so}` to `Set` (measured on a discarded draft). **Adjunction/Continuity/Finite.v** delivers Awodey's checkbox as four two-line compositions of RAPL with Structure/Limit/Comparison.v's bridges: `right_adjoint_preserves_terminal`, `right_adjoint_preserves_binary_products`, `left_adjoint_preserves_initial`, `left_adjoint_preserves_binary_coproducts` (the last two through `Opposite_Adjunction`; `InitialFunctor`/`CocartesianFunctor` are notations that do not parse in a return type, so they are spelled out; `About` prints only the first back under its notation) — a satellite of Continuity.v because Comparison.v requires Continuity.v. **Universes**: no equation and no `Set` in any constant of the Sets file (`Sets_op_not_cartesian_closed@{u u0}` with `u0 < u` only; `times_X_has_left_adjoint_iff_terminal@{u u0 u1 u2}` over `Sets@{u2 u1}` with `u2 < u1`); the four corollaries carry `u0 = u2`, `Adjunction`'s own hom identification (`Build_Adjunction'` has `h1 = h2`; probe N4 pins the underlying `Functor` constraint `h1 <= h2` — a functor may not go down the hom levels — as the adjunction's, not this file's); caps by `About` on the donors: `DiscreteCat_Functor'` (`EqdepFacts`, `JMeq`, `eq_ind`, `eq_ind_r`, `eq_rect_r`, `Logic_lemmas.equality`) into the corollaries and Exercise 1, `Build_Adjunction'` (`Logic_lemmas.equality`, `prod_rect`) into `Id_adj_fixed_product`, `Sets_Terminal` (`Logic_lemmas.equality`, `eq_ind`, `eq_ind_r`) into the point constants and `Sets_op_not_cartesian_closed`, `prod_zero_r` (`prod_rect`) and `Sets_Cocartesian` (`False_rect`, `sum_rect`) into `Sets_op_not_cartesian_closed`; `Sets_Cartesian` carries `prod_rect` (an earlier revision listed the two Sets structures as carrying none). **23 constants closed under the global context, zero `Axioms:` lines** — 15 `.glob` heads here (11 `def`, 4 `prf`) + 4 in Finite.v + `fp_adj_iso`'s 4 `Program` obligations; the gate carries the 19 heads; one `Defined` (`Id_adj_fixed_product`) LOAD-BEARING (flipped to `Qed`, the probe's transpose readback `to (adj …) f = to_X x △ f` stops), six `Qed`, Finite.v all `:=`; closure 68 excluding self (Functor/Product/Fixed.v 23 at the margin — it requires Instance/Grp.v, the price of reusing the tree's `X × −`; Finite.v 17; BiCCC.v 2; Cocartesian.v 1; fourteen `Require`s 0), Finite.v's own 39 (Comparison.v 12, Structure/Cocartesian.v 1); zero name collisions (`TwoPt` renamed to `OnePlusOne` away from Instance/Top/Wedge.v's). `Test/ProbeNoAdjoint432.v` mirrors the `Require` list and carries **5 refutation commands = 1 instrument + N1 CONVERSION (`fixed_product_functor 1 = Id`) + N2-N3 TYPING (the identity is the LEFT adjoint; the refutation is about `Sets^op`, not `Sets`, whose `Sets_Closed` is the control) + N4 UNIVERSE ("Cannot enforce ch = _ because ch < dh <= _")**, each stripped one at a time in a copy of the whole file beside its accepted controls; four `eq_refl` readbacks; guard coverage 20/17 with three exhaustive exceptions; rename-simulated over the nine library names the refutations use (`fixed_product_functor`, `Id_adj_fixed_product`, `Sets_op_not_cartesian_closed`, `Closed`, `Cartesian`, `Sets`, `Id`, `eq_refl`, `False`; an earlier revision listed the controls' `Category` and `iso_id` instead of `False`), every first break on a positive line; the two TYPING negatives are told apart from N1 by what is refused, not by the error text; `make todo` grows by those 5 lines only (2261 → 2266 over 39aa0ea5, master 1be50678 with #431), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: Awodey's concrete instances (the forgetful functor from posets, the free monoid); a right-hand adjunction transport (not needed); any positive closedness statement about `Sets^op`; the consolidation of `Prod_Functor` (Structure/Cartesian/Closed/Adjunction.v, `− × S`) with `fixed_product_functor_right` (surfaced); no edit to Adjunction/Continuity.v, Structure/Limit/Comparison.v, Functor/Product/Fixed.v, Structure/BiCCC.v or Instance/Sets/*.v.
- **Instance/Coq.v**: Category of Coq types and functions
- **Instance/Coq/Monoid/Free.v**: The free monoid and its universal property (Mac Lane §II.7 Cor 2; Awodey §1.7 construction + Props 1.9/1.10, §2.9 Ex 4, §7.5 Ex 7.7, §9.1, §9.9 Ex 2; Seven Sketches §5.2.3 Ex 5.24 — NINE catalog items in one file). Lists over X as an object of `Mon` at `Coq` (no new structure needed — `Coq_Monoidal` was already in tree), with `free_mmul_is_app`/`free_mone_is_nil` pinning concatenation and the empty word by `eq_refl`; the fold `free_ext` with `free_ext_unique`; **`free_monoid_universal`** (the ∃! statement — the issue's pinned name, resolving verbatim) packaged through `Theory/Universal/Arrow.v`'s `universal_arrow_from_UMP` as `free_monoid_universal_arrow`, and thence `free_monoid_adjunction : FreeMonoid ⊣ UMon` with its transposition (`free_monoid_transposition`/`_at`/`_inv`) and `free_monoid_unit_is_insert` by `eq_refl`. Awodey §7.5's insertion of generators is a genuine `Transform` (`insert_Transform : Id[Coq] ⟹ UMon ◯ FreeMonoid`) whose naturality square IS `free_fmap_generators` — "the induced homomorphism is determined by its action on generators" made literal — with `free_fmap_is_map` identifying the free functor's action with `List.map`. §2.9 Ex 4's `A`-Mon comma reading is delivered both ways (`free_monoid_initial` / `AMon_initial_universal_arrow`, with `eq_refl` round-trip checks), and Seven Sketches' two-generator computation identifies the pre-existing `ListMon` (Construction/Funny/Comparison.v) as an INSTANCE rather than a coincidence (`ListMon_hom_is_free_carrier`/`_id_is_free_unit`/`_compose_is_free_mul`, all `eq_refl`, the last pinning the diagrammatic argument order), with concrete words incl. `word_noncommutative` and folds into an XOR monoid. The `list_initial` bridge is `free_ext_is_fold`/`_is_initial_algebra_map`/`_is_the_unique_algebra_map` — the fold IS the initial-algebra map. **Awodey §9.9 Ex 2 is delivered SCOPED, and the scope is the honest content**: `adjunction_counit_underlying_retraction` (generic, hypothesis-free — `U ε` is split by the unit, one triangle identity) and `adjunction_counit_epic` (generic, under `Faithful U`), instantiated for the free monoid; what is NOT claimed is that the counit is a split epi IN `Mon`, because the evident section `m ↦ [m]` is not a homomorphism — the exercise's "every monoid is a quotient of a free one" is the surjection of UNDERLYING SETS, which is what is proved. This is new vocabulary: `adj_monic` (Theory/Adjunction.v) was the only statement of that family, with no epi counterpart. 72 named constants / 86 with obligations, all closed. **Theory/Universal/Arrow.v gains the uniqueness statement a CONSUMER wants**, and the file is precise about what was and was not already there (the issue's own QA note is inaccurate on this point and the file corrects it): `initial_unique_up_to_unique_iso` (Structure/Initial.v) DOES carry out the generic initial-object argument — but it lives in the comma `=(c) ↓ F` with its uniqueness clause ranging over COMMA isomorphisms and predicate `True`. Read down in D, where the object of interest is `arrow_obj` rather than the comma pair, that predicate would be FALSE: `arrow_obj` can have automorphisms (the free monoid on two generators has one exchanging them), and those are isomorphisms between the two universal objects that do NOT commute with the arrows. What is new is therefore the compatibility clause `fmap[F] (to i) ∘ arrow₁ ≈ arrow₂`, not initial-object uniqueness as such: `ua_med`/`aua_med` mediators with `_commutes`/`_unique`/`_id`/`_comp`, `universal_arrow_iso`(`_unique`) and `universal_arrow_unique`/`auniversal_arrow_unique`, specialised here as `free_monoid_unique`/`free_monoid_unique_iso` (Awodey Prop 1.10). The (ℕ,+)-on-one-generator consequence is NOT built — issue #802 owns it, cited
- **Instance/Rng/Algebras/Associative.v**, **Instance/Rng/MonoidRing.v**, **Instance/Rng/GroupRing.v**: Mac Lane §III.1 Ex 1's ring half — the ASSOCIATIVE (not necessarily commutative) K-algebra substrate and the monoid and group rings as universal arrows. **`AAlg K` is the category `Instance/Rng/Algebras.v` names in its own header as not built** ("the structure map is required to land in the CENTRE, a condition that is automatic exactly when the target is commutative … No centre-valued variant is built here"): `AAlgObject` carries exactly `aalg_ring`, `aalg_unit` and `aalg_central` — NO commutativity field, which is the whole point, since `KAlgObject`'s `kalg_comm` forces every object of `KAlg K` commutative and so `KAlg` can host NEITHER of the algebras below (`tensor_not_commutative` and `ext_anticomm_nontrivial` prove it rather than assert it). With `AAlg_Forget`, `AAlg_Forget_Mod : AAlg K ⟶ RMod (`1 K)`, the `AAlgLinear` ↔ module-hom passages, `KAlg_AAlg` full+faithful, and PROPERNESS PROVED: `UT2`, upper-triangular 2×2 over ℤ, is the tree's first closed NON-COMMUTATIVE `RingObject` — `Instance/Mod/Tensor.v` enumerates the closed ones as "all commutative" and defers a result for want of exactly such an object (whether `UT2` makes that collapse strict is NOT investigated, said so) — with `UT2_not_commutative`, `AAlg_not_all_commutative`, `UT2_not_in_KAlg`. `MonoidRing R M` is by generators and relations over an arbitrary `RingObject` and an arbitrary object of `Mon Sets`, with `mring_extend`/`mring_extend_unique` over the hypothesis `MRComm` (the images of R and M commute) that is a THEOREM rather than prose — `mring_comm_self` discharges it at the two insertions and `mring_extend_self` shows the extension is then the identity — then `zmring_universal_arrow : UniversalArrow M Rng_Forget_Mon` and `MonoidRingFunctor ⊣ Rng_Forget_Mon`, plus `mring_AAlg` (over commutative R, R[M] is an object of `AAlg R`). The group ring is Mac Lane's own unit-group phrasing and is the SAME OBJECT, not a second construction (`GrpRing G := ZMonRing (Grp_MonSets G)`): `Rng_Units : Rng ⟶ Grp` is new because GL.v's `Units_Functor` is `CRng`-only and ℤ[G] is not commutative, `Grp_MonSets : Grp ⟶ MonSets` is new outright, and the one real theorem is `monoid_hom_to_units` (a monoid hom out of a group's underlying monoid lands in the units), giving `grp_ring_universal_arrow` and `GroupRingFunctor ⊣ Rng_Units`. **A DONOR UNIVERSE PIN is disclosed and probe-guarded**: `Theory/Algebra/Rig.v`'s `rig_mult_monoid` is instantiable only at `RigObject@{Set Set _}`, so `Rig_Forget_Mon` and everything over it — `zmring_universal_arrow` included — is a statement about rings whose carrier AND `≈` live in `Set` — a class containing every concrete ALGEBRAIC ring in tree (`Int_Ring`, `Q_Ring`, `Zero_Ring`, `F2_Ring`, `FracRing`, `ZPoly`, `UT2`), the one closed `RingObject` EXCLUDED being `CRingOb X`, whose carrier is a hom-setoid of `Top` and so sits strictly above `Set` — and that led to the evaluation development being written at TOP LEVEL, an unannotated section variable's universes being fixed at `Context` (an annotated `Context (S : RingObject@{Set Set Set})` does work, so the top-level form is the simpler route rather than the only one); the restriction is the DONOR's, is NOT shown unavoidable, and is pinned in Test/ProbeAlgebras.v against a positive control at `Int_Ring@{Set Set Set}`. Strict attempts REFUTED and pinned: preservation of zero and of one by `mring_extend` is not definitional (both are SCALARS, so the fold returns the structure map's value), stated at a VARIABLE ring; every adjunction counit is `≈` only, `ump_universal_arrows` being Qed-opaque. Non-vacuity by mapping OUT into concrete algebras: `zmring_not_commutative`, `zmring_gen_not_scalar`, `zmring_nontrivial`, `zmring_gens_distinct`. NOT delivered: R[M] is not identified with the free module under convolution — `Instance/Mod/Free.v` proves `fv_normal_form` but disclaims coefficient CANONICITY, which convolution needs — and there is no `AAlg`-vs-monoid-objects-in-`RMod` comparison, no opposite algebras, no ⊗ of algebras, no repleteness of `KAlg_AAlg`
- **Instance/Vect/TensorAlgebra.v**: Mac Lane §III.1 Ex 1 + §IV.2 Ex 1 — the tensor and exterior algebras as universal arrows over `AAlg K`. `TensorAlg V : AAlgObject K` by generators and relations (the route is FORCED, and measured: `HasIndexedCoproducts` has no instance at `RMod R` (tree-wide it is inhabited only at `Sets` and, since #338, at `Cat`), and there is no `Cocartesian (RMod R)` (CORRECTION, #454: that was already false at Set carriers since Instance/Mod/Coproduct.v's `RMod_Cocartesian`, and since #454 `Instance/Mod/Colimit.v`'s `RMod_Cocartesian_via_GAFT` gives one above `Set`), so the graded `⊕ₙ V^⊗n` presentation over Instance/Mod/Tensor.v's BINARY tensor is unavailable without first building direct sums of modules), with `tensor_universal_arrow : UniversalArrow V (AAlg_Forget_Mod K)` in both encodings and §IV.2's left adjoint `TensorFunctor ⊣ AAlg_Forget_Mod K`. **Λ(V) is literally T(V)/⟨v·v⟩**: `et_eq` is `tt_eq` plus ONE generating EQUATION — seven constructors rather than twenty: the `tt_eq` embedding `ee_base`, the alternating relation `ee_alt` at GENERATORS, three congruences and two closure rules — so `ext_proj` is the identity on terms by `eq_refl`. **The defining relation is `v ∧ v ≈ 0`, NOT antisymmetry, and `ext_anticomm` is DERIVED from it** — the char-2 care of `Instance/Matr/Determinant.v`'s `det_alt`, cited in-header as the analogous discipline rather than the same degeneracy, the difference stated precisely: Determinant.v's vacuity is of the SELF-equation `det M ≈ −det M`, whereas `v∧w ≈ −(w∧v)` degenerates in char 2 to commutativity of generators, which is a CONDITION and not nothing. `AltLinear`, `AltFunctor V : AAlg K ⟶ Sets`, `ext_universal_element : AUniversalElement (AltFunctor V) (ExtAlg V)`, `ext_representation` (a natural iso in `[AAlg K, Sets]`), `ext_representable`, `ext_auniversal_arrow`. Strict: extension on generators and on scalars, the universal arrow IS the insertion, the free functor's object part, the adjunction unit, the universal element of `AltFunctor`, and the concrete computations (e₁∧e₂ = (0,0,0,1) against e₂∧e₁ = (0,0,0,−1)); REFUTED and pinned as `Fail` probes: preservation of zero and one by `tensor_extend`, for the same scalar reason as the monoid ring. Non-vacuity is by mapping OUT into concrete algebras (`UT2` and a hand-written 4-dimensional ℤ-algebra `Lam2`) since no induction on a quotienting relation can yield a negative — `tensor_not_commutative`, `tensor_gen_not_scalar`, `tensor_nontrivial`, `ext_nontrivial`, `ext_gen_not_scalar`, `ext_gens_distinct`, `ext_anticomm_nontrivial`, and `tensor_gen_square_not_zero` showing the exterior relation is REFUTABLE in T over the SAME module, so the quotient does something; both degeneracies (trivial base ring, zero module) are excluded by proof rather than assumption. **The QA scope fence is respected and stated**: §IV.2's left-adjoint packaging over GRADED ANTICOMMUTATIVE K-algebras and the non-faithfulness lemma are NOT delivered — no such category exists in tree — and no proof or comment below uses a grading, a target anticommutativity law, or any property of such a functor. Also NOT delivered: no grading, degree, normal form or coefficient uniqueness, hence no decision procedure, no dimension, no basis of Λ(V) and no finite generation; no symmetric algebra; no functoriality of Λ; no comparison of T(V) with Instance/Mod/Tensor.v's binary ⊗ (that needs degree); `Lam2` is a TARGET only and is not proved isomorphic to any `ExtAlg`
- **Instance/Ab/Free.v**, **Instance/Mon/Free.v**, **Instance/Rng/Free.v**: Mac Lane §IV.8 Exercise 2 (`maclane:IV.8:ex2`, book p. 104) — the free ring on a set as a composite adjunction TWO ways, with the two composites PROVED naturally isomorphic. **The issue's "Current state" is badly stale and the files say so rather than repeating it**: it claims "none of the three categories exists" and "not one of the six functors the exercise needs is defined", whereas `Grp` (`Instance/Grp.v`), `Ab` (`Instance/Ab.v`) and `Rng` (`Instance/Rng.v`) all existed, as did `Ab_Forget`, `Rng_Forget_Ab` (`Instance/Rng.v`), `Mon_Forget` and — decisively — the monoid ring WITH its adjunction (`MonoidRingFunctor`/`zmring_adjunction`, `Instance/Rng/MonoidRing.v`). **What was genuinely absent is narrower and was measured by shape, not by name**: the free abelian group on a setoid, the free ring on an abelian group, and a free monoid AT `Mon Sets`. **THE CENTRAL OBSTACLE IS A CATEGORY MISMATCH THE ISSUE DOES NOT ANTICIPATE, AND IT DEFEATS THE QA CORRECTION'S OWN INSTRUCTION.** That correction supersedes work items 1-2 with "consume #296's free monoid, do not rebuild" — but #296 landed at `MonCoq := @Mon Coq Coq_Monoidal` (`Instance/Coq/Monoid/Free.v`) while the monoid ring's right adjoint lands in `@Mon Sets Sets_Product_Monoidal`, and the two do not compose. Before this commit exactly THREE functors in the whole tree landed in `Mon Sets` and all three were forgetful (`Rig_Forget_Mon`, `Rng_Forget_Mon`, `Grp_MonSets`) — `FreeMonSets` below is the fourth and the first that is not, and there is no equivalence `Coq ≃ Sets`. Read that negative at its true width: "there is no functor at all between `Coq` and `Sets`" would be **FALSE** — `Trie_Functor : Coq ⟶ Sets` exists (`Instance/Shapes.v`) — and transport along an arbitrary functor pair would not preserve left-adjointness anyway, since the free monoid on a SETOID must identify `≈`-equal generators. `Instance/Mon/Free.v` therefore BUILDS the free monoid at `Mon Sets` (words compared letterwise up to `≈`, `free_mon_extend` a `Fixpoint` so the homomorphism laws close by `reflexivity`), which is the alternative site #400's own QA text names ("`Theory/Algebra/Monoid/Free.v` at `Mon(Sets)`") and never taken. It removes exactly the obstruction `Instance/Mon/Coproduct.v` records as the reason Awodey §3.2 Ex 5 went undelivered there — **that exercise is NOT attempted here and nothing is claimed about what else its proof would need**. **The free ring is by generators and relations and the route is FORCED**: the classical `⊕ₙ A^⊗ⁿ` needs `HasIndexedCoproducts Ab`, which is not built (`Instance/Ab/Coproduct.v` says so in terms). `fr_eq` has SEVENTEEN constructors with reflexivity DERIVED, and the presentation is deliberately irredundant — only TEN of `RigObject`'s twelve law fields are constructors, the two annihilation clauses being THEOREMS from distributivity and additive inverses, so the record is NOT a zero-obligation literal and two fields are paid for on purpose. **THE CRUX OF THE EXERCISE IS A MEASUREMENT, AND IT CAME OUT FINER THAN EITHER OBVIOUS GUESS.** With `RngUnderlyingAb := Ab_Forget ◯ Rng_Forget_Ab` and `RngUnderlyingMon := Mon_Forget ◯ Rng_Forget_Mon`, both `: Rng ⟶ Sets`: the OBJECT actions agree by `eq_refl` AND the ARROW actions agree by `eq_refl`, yet the whole records are NOT equal (the law fields are `Compose`'s obligations at different arguments) and **ascribing the monoid route's adjunction against the abelian route's right adjoint is REJECTED outright** — the `Adjunction` type mentions its right adjoint as a whole record. **No transport-along-a-natural-isomorphism lemma was needed, and none exists in tree** (`Theory/Equivalence/Adjunction.v` transports along an EQUIVALENCE OF CATEGORIES, a different thing; `right_adjoint_iso` runs the other way): the repair is pure FIELD COPYING, since every `Adjunction` field mentions the right adjoint only through `U y` and `fmap[U] f`, which agree definitionally, so the five field TYPES are convertible even though the two functor records are not — `free_ring_via_mon_adjunction_ab`, inert on transpose and unit by `eq_refl`. **`free_ring_composites_agree : free_ring_via_ab ≈ free_ring_via_mon` is a NATURAL ISOMORPHISM, not an object-level equality** — `≈` at `Sets ⟶ Rng` IS `Functor_Setoid`, a family of isomorphisms plus the coherence square — with `free_ring_composites_agree_unit` the unit statement the exercise also asks for. It is proved through a locally built `left_adjoints_agree` whose comparison is `⌈η'⌉`, with the triangle, both round trips and naturality from the hom-set laws alone; that local version is `Defined` **precisely so its components have names**, which is what `Theory/Adjunction.v`'s `left_adjoint_iso` could not give (it is `Qed`, so nothing reduces) — and `free_ring_comparison_is_agree_component` records by `eq_refl` that the named comparison IS the component, while `free_ring_composites_agree_via_left_adjoint_iso` feeds the same two adjunctions to the in-tree theorem so the claim is cross-checked against the library rather than asserted. **A UNIVERSE ASYMMETRY BETWEEN THE TWO ROUTES IS THE FILE'S OTHER REAL FINDING**: `Rig_Forget_Mon`'s source is `Rig@{u Set}`, so `Rng_Forget_Mon` inherits it and the MONOID route — and hence the comparison, which needs both routes at ONE `Rng` — is confined to rings whose homs live in `Set`, while the ABELIAN route leaves that universe free. Guarded by formability negatives (`Cannot enforce Set = uh`) against controls showing the abelian route's forgetful functors DO elaborate strictly above `Set`. **Read what the negatives fire on precisely, because the obvious reading is wrong and was corrected by audit**: they fire on the donor's literal `Set` meeting a RIGID declared level, NOT on the declared `Constraint` — deleting that `Constraint` leaves them failing with the identical message, so it is INERT for the negatives, and what it actually buys is the positive controls' "strictly above `Set`". The pin is the DONOR's, is not repaired, and is NOT claimed unavoidable. **76/76, 50/50 and 104/104 constants closed under the global context** (globs 80/53/110 minus 4/3/6 `Fail`-named entries, which the glob records but the environment never receives). `Test/ProbeFreeRing400.v` guards the three cross-file boundaries from OUTSIDE the target files — an in-file `Fail` renames in lockstep with its own definition and so cannot detect a rename — with 3 negatives of 2 KINDS kept lexically apart (2 conversion, 1 formability), each stripped and its kind read off the error, an instrument check, and probe-vacuity simulation **7/7** over every constant the negatives name (`RngUnderlyingAb`, `RngUnderlyingMon`, `free_ring_via_mon`, `free_ring_via_mon_adjunction`, `Rng_Forget_Mon`, `MonSets`, `Rng`) — an audit found `MonSets` VACUOUS on the first cut, named only inside a `Fail`, and the control that closes it was added for that reason. NOT delivered: no normal form or coefficient uniqueness for any of the three constructions, hence no decision procedure; no commutative variant of the free ring and no comparison with `Instance/Vect/TensorAlgebra.v`'s `TensorAlg` (which is over a MODULE, not an abelian group); no `HasIndexedCoproducts Ab`; no grading or degree; and no comparison functor between `Instance/Mon/Free.v`'s free monoid and `Instance/Coq/Monoid/Free.v`'s, the two living over different bases with nothing claimed to relate them
- **Instance/Ab/FreeNotContinuous.v**: Mac Lane §V.4 Exercise 3 (book p. 118, `maclane:V.4:ex3`) — **the free abelian group functor is not continuous: `FreeAb_not_continuous : ContinuousFunctor FreeAb → False` and `FreeAb_not_PreservesAllLimits`, the tree's first refutations of the all-shapes classes and its first refutation of `PreservesLimitCone`/`PreservesLimit` at a named functor** (Structure/Limit/Preservation/Separation.v already refutes cone-level preservation for a synthetic span functor, so "first discontinuity witness" is NOT claimed; non-preservation at a named functor as such is not first either — Instance/Top/Forgetful.v `indiscrete_image_not_colimiting` and Instance/Powerset.v `direct_image_not_meet_preserving` predate it, neither phrased with a class name; an earlier revision claimed it). ONE fact drives everything: the free abelian group on a point has two distinct endomorphisms, `id` and the zero map, because its generator is not zero (Instance/Ab/Free.v's `free_ab_gen_not_zero` at ℤ) — `free_ab_one_id_not_zero`. Three witnesses: **the empty limit** (`SetsPoint_empty_Limit`, `FreeAb_not_PreservesLimitCone_empty`, and the APEX-ONLY `FreeAb_not_PreservesLimit_empty`, the only shape reaching that level here — probe N5 pins the binary case, the countable apex case is unpinned — hence `FreeAb_not_PreservesAllLimits`); **a countable product in Mac Lane's shape** over `DiscreteCat nat` (`NatOneDiagram`, `NatOneCone_IsLimitCone` through `discrete_IsLimitCone_of_IsIndexedProduct`, the competing cone `NatCompeting` with legs `id` and the zero map, `FreeAb_not_PreservesLimitCone_countable_product`) — his SHAPE but not his mechanism: the finite-support argument needs a normal form for `FATerm` the tree lacks, so here the factors are the point, the product collapses and the legs coincide (the countability is inert: the same argument runs at any discrete shape with two or more objects); needs no indexed products in `Ab` (which has none); **the binary product through the canonical comparison map** (`FreeAb_binary_cmp := binary_comparison FreeAb BinOnes`, `FreeAb_binary_comparison_not_iso`, `FreeAb_not_PreservesLimitCone_binary_product` via `comparison_iso_of_PreservesLimitCone`, `FreeAb_not_CartesianFunctor` via `cartesian_functor_iff_comparison_iso`). The positive side of the same adjunction, one line each: `Ab_Forget_Continuous` (RAPL) and `FreeAb_Cocontinuous` (LAPC). **Stale premises**: "neither the functor nor its target exists" FALSE (Instance/Ab.v `Ab`, Instance/Ab/Free.v `FreeAb` and `free_ab_adjunction`, #400 — a term algebra `FATerm`/`fa_eq`, not finitely supported functions; work bullet 1 was already done and nothing is re-derived); the QA correction's `Construction/FreeAb.v` does not exist (the donor is Instance/Ab/Free.v, already over #256's `Ab`, no bridge needed); `Ab` has binary (bi)products, zero and terminal objects but no indexed coproducts (Instance/Ab/Coproduct.v, Instance/Rng/Free.v) and no indexed products or completeness either (measured: the tree's `HasIndexedProducts` instances are Sets', Cat's and StrictCat's — Instance/Sets/Products.v, Instance/Cat/Limit.v — none at `Ab`; an earlier revision cited the coproduct for the product half, and for the StrictCat one cited the `Definition`'s type rather than the instance; #433 re-measured the list and found three more, functor categories' and `[_2, Sets]`'s (Instance/Fun/Terminal.v) and `_1`'s (Structure/Limit/Power/Adjunction.v), still none at an algebraic category). **Universes**: no equation in any block; `Set` appears in exactly 12 of the 33 constants, from two donors — `_0 : Category@{u Set Set}` (Instance/Zero.v, declared bare) pins the eight empty-shape constants, and since `PreservesLimitCone` identifies shape hom with ambient hom the empty-route conclusion `FreeAb_not_continuous_via_empty` is stated for `FreeAb` at `Sets@{Set u} ⟶ Ab@{u Set}` only — which is why the HEADLINE `FreeAb_not_continuous` goes through the countable product (free levels, no `Set`); `Ab_trivial@{} : AbObject@{Set Set Set}` (Instance/Ab.v, monomorphic) pins `Ab_Terminal`/`Ab_Cartesian` and through them the four binary-witness constants (probes N6-N8: refused at a hom level above `Set` while `Ab` and `Ab_Forget` are accepted); `ab_zero_endo` is built directly, not as `Ab_zero_hom ∘ Ab_one`, so the core separation and the headline escape that pin; ℤ (`ab_int`) pins nothing; neither pin is repaired. **34 constants closed under the global context, zero `Axioms:` lines** — 33 `.glob` heads (23 `def`, 10 `prf`) plus 1 `Program` obligation; the gate carries the 33 heads; two `Defined`, `ab_zero_endo` LOAD-BEARING for the whole file (flipped to `Qed`, `ab_zero_endo_value` stops first and, with it removed, the core separation `free_ab_one_id_not_zero` and the probe's readback stop too; an earlier revision named only the two readbacks), `NatOneProduct` not; eleven `Qed` (the obligation, seven negations and three equational lemmas whose only consumers are refutations); closure 60 excluding self (Comparison.v 4 at the margin, Instance/Ab/Free.v 3, Instance/Ab/Coproduct.v 2, Instance/Sets/Cartesian.v 1, Instance/Zero.v 1, nineteen `Require`s 0; the binary witness accounts for 3 — an earlier revision said 8, charging Comparison.v to it, which witness 2 needs too — kept as the issue's "comparison map" bullet); zero name collisions. `Test/ProbeFreeAbNotContinuous430.v` mirrors the `Require` list and carries **9 refutation commands = 1 instrument + N1-N3 CONVERSION + N4-N5 TYPING (the cone-level refutations do not ascribe at `PreservesAllLimits`/`PreservesLimit` — the apex level is reached only at the empty shape) + N6-N8 UNIVERSE**, each stripped one at a time in a copy of the whole file beside its accepted controls; three `eq_refl` readbacks; guard coverage 37/29 with eight exhaustive exceptions; rename-simulated 15/15, every first break on a positive line; `make todo` grows by those 9 lines only (2246 → 2255 over 3a387cdb, master b0d0fa63 with #429), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: Mac Lane's finite-support mechanism; apex-level refutations at the product shapes (would need ℤ ≇ ℤ × ℤ in `Ab`; probe N5); `FreeAb 1 ≅ ℤ` (never written as ℤ — no such isomorphism exists in the tree); countable products in `Ab`; repair of either `Set` pin; no edit to Instance/Ab.v, Instance/Ab/Free.v, Instance/Zero.v, Structure/Limit/Comparison.v or Structure/Limit/Preservation.v.
- **Instance/Sets/Powerset/Universal.v**, **Instance/FinSet/Powerset.v**: Mac Lane §III.1 Ex 2 + Riehl §2.3 Example 2.3.6 — a universal element for the CONTRAVARIANT power-set functor, delivered TWICE because the two settings say different things. **`Powerset_Prop_op : (Sets@{o so})^op ⟶ Sets@{o so}`** is the contravariant power-set functor at ONE universe level, which `Instance/Sets/Powerset.v` (#227) did not have — its `Powerset`/`Powerset_op`/`Powerset_Prop_Lift` are all cross-universe and its one-level `Powerset_Prop` is COVARIANT — and the mechanism is that the PREIMAGE `λ y, T (f y)` introduces no quantifier, so unlike the direct image it needs no truncation; `Powerset_Prop_universal_element : AUniversalElement Powerset_Prop_op Powerset_Omega` inhabits the §III.1 clause over it, with `Powerset_representation` and `Powerset_Prop_Representable`. **Read the Sets result's strength precisely: it is THIN, and the file says so.** In this library a `Prop`-valued subset of A IS a map `A ~> Ω` — `carrier (Powerset_Prop_obj A) = (A ~{Sets}~> Powerset_Omega)` closes by `eq_refl` AT THE LEVEL OF TYPES — so the correspondence is nearly definitional and its entire mathematical content is the single equivalence `Powerset_squash (True ≈ P) ↔ P`. Hence the FinSet half, where the issue's own proposal has content and COMPUTES: `FinPowerset : FinSet^op ⟶ FinSet` with object action `n ↦ 2^n` and BOTH functor laws at **Leibniz `=`** on codes, `FinPowerset_universal_element : AUniversalElement FinPowerset_Sets 2`, and the capstone **`FinSet_Sub_powerset : Sub ≅[[FinSet^op, Sets]] FinPowerset_Sets`** — subobjects of a finite set are its subsets, naturally — plus `finpow_is_topos_Pow : @Pow FinSet FinSet_Topos n = finpow n` by `eq_refl` tying it to `Structure/Topos.v`'s internal power object (OBJECT actions only; the internal one has no arrow action). **Riehl's emphasis is enforced at the level of TYPES, not prose**: the universal element is the SUBSET `{⊤} ∈ P(Ω)` (`Powerset_truth_subset : carrier (Powerset_Prop_obj Powerset_Omega)`), NOT the point `⊤ ∈ Ω`, and putting the point in the element's position is a TYPE ERROR pinned as a `Fail` probe in both settings; the preimage description of the inverse is PROVED (`powerset_representation_to_is_preimage`, the forward leg IS the preimage by `eq_refl`; FinSet's `finpow_fmap_is_preimage`/`finpow_preimage_iff`/`finpow_preimage_of_truth`). The truncation story is measured rather than waved at: `Powerset_squash` appears nowhere in the FUNCTOR, but the universal ELEMENT does use it (the donor's `Powerset_Prop_singleton_pred` truncates uniformly) and at Ω it is INERT — `powerset_squash_prop_inert : ∀ P : Prop, Powerset_squash P ↔ P`, by instantiating the impredicative quantifier at `Q := P`. **The Yoneda route is REFUSED here and that is pinned**: `universal_element_yoneda` and `universal_element_representation` at `Sets^op` are genuine universe inconsistencies (Element.v's donor restriction identifies object, hom and proof universes), so both files use the Yoneda-FREE `ue_representation`/`AUniversalElement_of_repr` route throughout — with a positive control elaborating that very constant at a category whose three universes CAN be identified, so the refusal is a fact about `Sets` rather than about the constant. Deferred and disclosed: no `SubobjectClassifier Sets` instance (the subset→subobject round trip IS proved as `Powerset_subset_roundtrip`, the other composite is not, and **no impossibility is claimed** — the note is about a construction, not a theorem about the category; the other composite is exactly what Instance/Sets/Classifier/OneLevel.v's `Untruncate` buys, and its `Sets_Classifier` is the resulting instance, with `Powerset_Omega` as Ω and `Powerset_truth_point` as truth, both at `eq_refl` — the impossibility is still not claimed there either); no monad on `FinPowerset`; no comparison MORPHISM with the internal power object; no general-topos statement
- **Instance/Sets/Classifier/OneLevel.v**: Mac Lane §IV.9 construction 1 (book p. 105, `maclane:IV.9:construction1`) + Seven Sketches §7.2.2 displays (7.14)/(7.15) (printed p. 229, `7sketches:7.2.2:construction-subobject-classifier-set`) — **a genuine `@SubobjectClassifier Sets@{o so} Sets_Terminal` instance AT ONE UNIVERSE LEVEL, conditionally, in two shapes, plus the resizing characterisation with which it is inter-derivable (no round trip is built)**: `Sets_Classifier (U : Untruncate@{o})` with Ω := `Powerset_Omega` and truth := `Powerset_truth_point` (both `eq_refl`) and `Sets_Classifier_dec (D : DecImage@{o so})` with Ω := `BoolSetoid` — Seven Sketches' 𝔹 at level o, a polymorphic two-element setoid over `Lib/Setoid.v`'s `eq_Setoid` — and truth := `ptrue` (both `eq_refl`), with `Sets_Classifier_IEM` composing the first with `untruncate_of_IEM`. `Sets_Classifier` is the issue's Verification-block name, delivered verbatim. **THE DoD BOX IS ANSWERED AS ITS FIRST ALTERNATIVE, CONDITIONALLY, AND THE DISCLOSURE IS IN THREE PARTS THE HEADER STATES IN EXACTLY THOSE TERMS**: (i) NO unconditional axiom-free instance is built; (ii) NO impossibility theorem is proved; (iii) an in-tree impossibility proof is out of reach in a MEASURED sense — a scratch development compiled against the worktree builds `Sets_Classifier_IEM IEM_classical` and `Print Assumptions` reports EXACTLY `classic` and `constructive_indefinite_description` (Coq.Logic.ClassicalEpsilon), so an in-tree impossibility theorem would refute a classically valid statement — labelled an ARGUMENT, not a theorem, with the classical instance deliberately not shipped and that stdlib module required by no tree file. **THE CONVENTION SWAP IS STATED RATHER THAN ABSORBED**: Mac Lane's ψ_S is 0 ON the subset with 0 the value "truth", while Seven Sketches, the tree's `FinSet_Classifier` (`fin_true`) and this file put `true` ON it; the two differ by the swap 0 ↔ 1, an automorphism of the truth object, and this file follows the tree — read that swap as one of NAMES, since `fin_true` is `Fin.F1`, the first element of `Fin.t 2`, and so is Mac Lane's 0 as a numeral (#403's measurement). **THE TWO HYPOTHESES ARE EQUIVALENT, NOT MERELY BOTH IMPLIED BY EXCLUDED MIDDLE, AND THAT WAS A PREDICTION MEASUREMENT CONFIRMED**: `DecImage_iff_IEM` pairs `DecImage_of_IEM` with `IEM_of_DecImage`, the latter applying the decider to `subone_incl P` at the point `ttt` of the terminal setoid — `sets_in_image (subone_incl P) ttt` IS `∃ _ : P, ttt ≈ ttt`, so `projT1` gives P and the other branch gives `P → False`; `untruncate_of_IEM` runs the other hypothesis, while `Untruncate → IEM` is NOT built and is not claimed. **WHY A NEW FILE, MEASURED**: `Instance/Sets/Powerset.v` and `Instance/Sets/Powerset/Universal.v` BOTH Require `Instance/Sets/Classifier.v`, so the instance whose Ω is `Powerset_Omega` cannot live there — the cycle-forced sibling, on `Structure/Limit/Indexed/Hom.v`'s precedent — and the module short name is `OneLevel` rather than `Small` because `Theory/Size.v` already declares `Class Small` (ten files mention the word) and the gate loads many modules into ONE scope (the #390 `Sound` hazard). **THE RESIZING CHARACTERISATION IS THE ENGINE, NOT A BY-PRODUCT**: `Record SmallClassifierExt@{o} : Type@{o+1}` (Ω at level o, a point t, an assignment `sce_w : Type@{o} → carrier Ω`, `sce_intro`/`sce_elim` identifying `w P ≈ t` with P, and `sce_ext`) with `classifier_iff_small : (SubobjectClassifier Sets → SmallClassifierExt) * (SmallClassifierExt → SubobjectClassifier Sets)` over an arbitrary `Sets@{o so}` — `Sets_Classifier` is built THROUGH `classifier_of_small` while `Sets_Classifier_dec` is a direct `@Build_SubobjectClassifier` over `char_of_dec`/`char_of_dec_pullback`/`char_of_dec_unique` (and `classifier_of_small (small_of_IEM E)` is a THIRD classifier, refuted equal to `Sets_Classifier_dec (DecImage_of_IEM E)` by the probe's negative 9), and the forward direction's `sce_ext` spends `char_unique` twice (through `subone_pt_classifies`) and `Structure/SubobjectClassifier.v`'s `char_respects` once (itself derived there from `char_pullback` and `char_unique`, among others), with `subone_respects` supplying the SubObj equivalence of two propositions' subobjects of 1. **THE BOOK'S RULE IS PROVED, AT THE GENERAL LEVEL AND AT BOTH INSTANCES**: `classifier_of_small_char_iff : (char m M b ≈ truth ttt) ↔ sets_in_image m b` — Mac Lane's (1) with the swap and Seven Sketches' ⌜m⌝ rule — with `sets_classifier_char_iff` a `:=` of it and `sets_classifier_dec_char_iff` a `:=` of `char_of_dec_iff`. **"ONE STORY" IS MEASURED STRICT-FIRST AND COMES OUT AT TWO GRADES, WITH NO THIRD**: the VALUE identification `char (Sets_Classifier U) m M b = Powerset_squash (char_setoid m b)` holds at `eq_refl` (as do `= Powerset_squash (sets_in_image m b)` and `char_setoid m b = sets_in_image m b`), so the one-level characteristic map IS the truncation of `Instance/Sets/Classifier.v`'s cross-universe one and the two files are one construction seen at two levels; the RECORD identification, statable only one universe up through the new `sets_squash_mor : PropSetoid ~> Setoid_Lift Powerset_Omega`, is REFUTED at `eq_refl` (a CONVERSION refusal, the two `proper_morphism` certificates being separately elaborated) and holds at `≈` as `bridge_record_equiv`; and there is NO whole-`Transform` grade because no `Transform` occurs on either side — said, not silently omitted. **THE CLASSIFICATION THEOREM IS NOT STATABLE AT `Sets` AND THAT IS PINNED WITH ITS CONTROL**: `classifier_classifies@{u u0 u1}` is over `C : Category@{u u0 u0}` with the BOUND `u <= u0` (objects ≤ homs) while `Sets@{o so} : Category@{so o o}` has o < so, so it, `Sub_classifier_natural` and `Sub_Representable` are each refused as universe inconsistencies while `char_reindex` IS accepted at the SAME `Sets` for the SAME variable classifier; what survives are the two ROUND TRIPS, `sets_char_roundtrip` and `sets_pullback_roundtrip`, both `:=` terms of `Structure/SubobjectClassifier.v`'s own constants over `Instance/Sets/Pullback.v`'s **`Sets_HasPullbacks`, NAMED in both statements**, with NO second pullback structure built anywhere in the file. Read the QA checkbox exactly: its subject, the classifying square, CANNOT be stated against a `HasPullbacks` — the class field `char_pullback` is an `IsPullback` predicate and the class takes only two arguments (pinned as the probe's TYPING negative) — so `Sets_HasPullbacks` is consumed where the theory does need it, in the two round trips, and nothing is rebuilt. **NON-VACUITY WITHOUT ANY GLOBAL HYPOTHESIS, and the per-mono data is the PRIMITIVE rather than a corollary**: `char_of_dec m M (d : SetsDec m)` takes the decidability of ONE mono's image — no global hypothesis in its type — with `char_of_dec_pullback` (the classifying square against `one`) and `char_of_dec_unique` proved for it directly, so `Sets_Classifier_dec` is that data at every mono and `char (Sets_Classifier_dec D) m M = char_of_dec m M (D u x m M)` holds by `eq_refl`; the concrete witness is `dec_false : 1 ~> BoolSetoid` (the constant `pfalse`), whose image membership is decidable outright, whose characteristic map is NEGATION — `char_false pfalse = ptrue` AND `char_false ptrue = pfalse`, both `eq_refl`, so it MOVES both points and is not the generic subobject in disguise — with `ptrue_neq_pfalse` by `discriminate` and `dec_false_not_iso` refuting invertibility; `point_monic` supplies the `Monic` argument for ANY morphism out of the terminal setoid. **FinSet IS UNCONDITIONAL AND COMPUTES**: `finset_classifier_classifies n := classifier_classifies n` at `FinSet_Classifier` (a `:=` with no tactic — the theorem IS statable there, `FinSet`'s objects being naturals), `finset_classifier_omega_is_two`, and Seven Sketches' ⌜m⌝ evaluated on a closed injection `1 ↣ 2` at BOTH points (`fin_true` at the included one, `fin_false` at the other, both `eq_refl`) — Exercise 7.16's shape. **A SURPRISE THE BRIEF DID NOT ANTICIPATE, PINNED**: the two routes from `IEM` to a classifier — `classifier_of_small (small_of_IEM E)` and `Sets_Classifier_dec (DecImage_of_IEM E)` — agree on ALL THREE DATA fields at `eq_refl`, Ω, `char` AND the truth MORPHISM (not merely its value), while the WHOLE RECORDS are refused, so what differs is EXACTLY the two LAW fields `char_pullback` and `char_unique`; my own guess that the truth morphisms would differ as records was refuted by measurement. **Universes measured off BOTH binder and block over all 92 constants: NOT ONE of the 92 constraint blocks contains a universe EQUATION** (every entry is `<` or `<=`), and NINETEEN carry `Set`, ALWAYS as a strict lower bound and never as an equation — THIRTEEN as `Set < o` from the Powerset route, isolated to `Powerset_Omega`, whose own block is exactly `Set < o`, and the other SIX (`Set < u1` ×5, `Set < u2` ×1) in the unannotated section-(K) `FinSet` block, from `FinSet` and not from `Powerset_Omega`, inherited from `Powerset_Prop_truth` (`Prop : Type@{Set+1}`) and NOT from `Powerset_squash`, whose block is EMPTY, a discriminating pair — so the ENTIRE `BoolSetoid` route (`Sets_Classifier_dec`, `char_of_dec`, `dec_truth`, `small_of_IEM`, `DecImage`) is `Set`-free, `Untruncate`, `IEM` and `SmallClassifierExt` have EMPTY blocks, and `Sets_Classifier@{o so}`'s type is `@SubobjectClassifier Sets@{o so} Sets_Terminal` with `Set < o` and `o < so` and nothing identified; explicit `@{o so}` binders sit on every top-level definition of sections (B)–(J) — the six section-(K) `FinSet` constants carry none — and are measured NOT load-bearing (the unannotated clones of `Sets_Classifier` and `DecImage` elaborate at the same two-universe shape, so minimization pins nothing and the binders buy presentation), the one algebraic sort `Type@{o+1}` (the file's only `+1`) having precedent for algebraic record sorts at `Instance/Sets.v` (`Type@{max(o+1,p+1)}`) and `Theory/OGraph.v` (`Type@{max(o,h+1,p+1)}`). **92/92 constants closed under the global context with ZERO `Axioms:` lines** — the 89 entries `Print Module` emits at five-space indent (69 `Definition`, 18 `Parameter` — `Print Module`'s rendering of the eighteen `Qed`-closed constants, a display convention and not an axiom — 1 `Record`, 1 `Inductive`) plus `ptrue`/`pfalse` on the `Inductive`'s continuation line and `Build_SmallClassifierExt` after the `Record`'s `:=`, each queried FULLY QUALIFIED, and all 92 in the `make print-assumptions` gate as a NEW block (Makefile–5477, 95 lines, 8476 bytes — a new block rather than an append, to keep each `sh` argument small), the gate's `closed` count rising by exactly 92 (4906 → 4998) with the `Axioms:` line count unchanged at 1; `Print Assumptions Sets_HasPullbacks` was ALREADY gated at Makefile and is not duplicated. **14 `Defined` tokens, ELEVEN load-bearing by flipping each ALONE to `Qed`** (only `IEM_of_DecImage`, `sce_pullback` and `char_of_dec_pullback` survive the flip, and all three are kept `Defined` because they produce data); 18 `:= eq_refl` statements; closure **70** modules excluding self, with drop-one marginals `Instance/Sets/Powerset/Universal` 19, `Instance/Sets/Pullback` 12, `Instance/FinSet/Classifier` 2 and every other Require 0 — of the seventeen Requires SIXTEEN are textually load-bearing, only `Structure/Pullback` arriving through `Structure/SubobjectClassifier`; `Instance/FinSet/Subsets.v` is NOT required (its closure is 99 against this file's 70), so `FinSet_Omega_is_two` is cited by path and the one-line `eq_refl` restated under a prefixed name. **ONE COLLISION WAS FOUND AND RENAMED BEFORE LANDING**: `finset_omega_is_two` is already declared, with the identical statement, at `Test/ProbePowersetUniversal.v` — a name the brief's own FREE list did not cover — hence `finset_classifier_omega_is_two`; no declaration collides with any of the 92 names, swept whole-word over every `.v` and instrument-checked at `Full` and `Monoid` — the only whole-word hits outside the file are the prose pointers this same change writes into `Instance/Sets/Classifier.v` and `Instance/Sets/Powerset/Universal.v`. `Test/ProbeClassifier402.v` mirrors the target's full seventeen-Require list plus `Structure/SubobjectClassifier/Natural.v` and carries **10 refutation commands = 1 instrument + 9 negatives of THREE kinds told apart by the error TEXT** — 6 FORMABILITY (the three not-statable theorems, the `SubObj` carrier, `PropSetoid` as an `obj[Sets]`, and the naive squash elimination, whose error is the predicted `Cannot enforce o <= Prop`), 1 TYPING (`@SubobjectClassifier Sets Sets_Terminal Sets_HasPullbacks` → `Illegal application (Non-functional construction)`, no universe clause and no `cannot unify`, against the accepted two-argument form) and 2 CONVERSION (the bridge's record grade and the two IEM routes, each with its `eq_refl`/`≈` controls beside it) — each stripped ONE AT A TIME, compiled alone and its whole error read, with guard coverage measured mechanically (49 identifiers inside a refutation command, 37 also outside, the 12 exceptions exhaustively four keywords, two bound variables, three local universe binders, the two `Fail Example` names that never enter the environment, and the instrument's absent name) and rename-simulated **8/8** over the target constants the negatives name, every break landing on a `Check` control line. `make todo` grows by **12, ALL in the probe** (10 refutation commands + 2 header prose lines naming the token), the library file contributing **ZERO** — so the issue's "adds no new hits" box is not met as written and the header says so. Three prose sites are corrected in the same commit and each was FALSE or incomplete: `Instance/Sets/Classifier.v`'s "no other route is available here" (now naming the conditional instance, with its heading changed to "WHY ONE LEVEL DOES NOT SUFFICE UNCONDITIONALLY" and its `PropSetoid` paragraph amended to record that the truncated `Powerset_Omega` serves at one level once the truncation inverts), `Instance/Sets.v`'s two classifier sentences, and `Instance/Sets/Powerset/Universal.v`'s "not settled here" paragraph (now settled CONDITIONALLY, with its own "NO IMPOSSIBILITY IS PROVED" left true); `Theory/Sheaf.v`'s classifier mention was inspected and left, being about a Grothendieck topos. NOT delivered: no unconditional axiom-free instance and no impossibility theorem; no proof that either hypothesis is underivable (only that no axiom-free in-tree inhabitant exists, with `IEM` cited as what would give both) and no passage `Untruncate → IEM`; no naturality, `Sub_classifier_natural` being unstatable at `Sets` — the same `u <= u0` bound as `classifier_classifies`, and the same one that a PRESHEAF category CAN satisfy, so #403's Instance/Fun/Classifier.v does state both there (`fun_classifier_classifies`, `fun_sub_classifier_natural`) at its re-annotated second reading, while both stay refused at its minimized `Fun_Classifier`; no `ElementaryTopos Sets` IN THAT FILE and no power objects there — #404's `Instance/Sets/Topos.v` assembles `Sets_Topos` on this file's `Untruncate` (or `DecImage`, or `IEM`) and gets `Pow` with it, `@Pow Sets (Sets_Topos U) b` being `Powerset_Prop_obj b` at `eq_refl`; no functor `FinSet ⟶ Sets`, so the FinSet section relates to the `Sets` sections in prose only; **the two conditional instances' truth objects are NOT compared — no `BoolSetoid ≅ Powerset_Omega` is built, under `IEM` or otherwise**; no third grade for the bridge; and nothing is registered as an `Instance`, neither the hypotheses nor a chosen classifier being allowed to become globally resolvable
- **Theory/Sieve.v**, **Instance/Fun/Pullback.v**, **Instance/Fun/Classifier.v**: Mac Lane §IV.9 construction 2 and remark 1 (book pp. 105-106, `maclane:IV.9:construction2`, `maclane:IV.9:remark1`) + Awodey §8.8 printed pp. 210-211 (`awodey:8.8:def-sieve`, `awodey:8.8:construction-omega-sieves`) — **the subobject classifier of a set-valued functor category, by sieves, CONDITIONALLY on #402's `Untruncate`, in TWO universe readings, the second of which carries the classification theorem itself**. `Fun_Classifier (UT : Untruncate) : @SubobjectClassifier ([C^op, Sets]) (Functor_Category_Terminal Sets_Terminal)` for an ARBITRARY `C` at the levels the presheaf category itself forces (objects at or below homs, homs above `Set`), with Ω := `Sieve_Presheaf C` and truth := `sieve_truth C` both read back at `eq_refl` and `char` read back as `char_nat`; `Fun_Classifier_IEM` at `untruncate_of_IEM`; and the covariant reading Mac Lane actually writes, `Fun_Classifier_cov {D} := @Fun_Classifier (D^op)`, a `:=` with NO tactic and NO transport, since `(D^op)^op = D` holds by `eq_refl` here. **THE PER-CONSTANT HYPOTHESIS LEDGER IS THE FILE'S SHARPEST FACT, AND IT IS MEASURED AT THE SIGNATURES**: `char_nat` and `char_square` (the classifying square COMMUTES) carry NO hypothesis at all — Section Char's Context is `{C} {E} {U} (theta)` and neither `Monic` nor `Untruncate` occurs in either type, so the square commutes for an arbitrary transformation, monic or not; **`presheaf_char_unique` carries NONE EITHER** — Awodey's "the reader should verify … a UNIQUE classifying morphism", which the issue flags as the real work, is the CHEAP half here, and what pays for it is the sieve read as a SUBPRESHEAF (`h_sub_char` feeds the pullback's own universal property `sieve_subpresheaf d S`, the presheaf of the arrows into `d` lying in the sieve, with its cone `sieve_cone` into `E`, and reads the witness OUT of the mediator, so no truncation is eliminated in it), while the Awodey §8.8 appended checkbox — `sieve_incl`/`sieve_incl_Monic`/`sieve_subobject`, that subpresheaf embedded in `Curried_CoHom C d` — is a chain NOTHING else consumes (measured: deleting the three leaves the file compiling), so it ships as a standalone reading of Awodey's clause and is decorative for the classifier; only `presheaf_char_pullback` needs both, `Monic` through #369's `presheaf_monic_iff_injective` at THREE named sites on the way to it (the mediator `char_med`'s respectfulness obligation, its two naturality obligations since `Transform` carries `naturality` and `naturality_sym`, and `presheaf_char_pullback`'s own uniqueness clause — the issue's reviewer check; four USES, a fifth in section (I)'s `small_pb`) and `Untruncate` EXACTLY ONCE, in `med_img`. **WHETHER `Untruncate` IS NECESSARY IS NOT SETTLED**; what is measured is that the naive elimination `fun P h => h P (fun p => p)` is refused with "Cannot enforce o <= Prop" — #402's wall verbatim, at the same level — against `powerset_squash_prop_inert` as the accepted control. **THE THREE-PART DISCLOSURE, following #402's**: (i) NO unconditional axiom-free instance is built; (ii) NO impossibility theorem is proved; (iii) an in-tree impossibility proof is out of reach in a MEASURED sense — a scratch development compiled against this worktree builds `Fun_Classifier (untruncate_of_IEM IEM_classical)` and `Print Assumptions` reports EXACTLY `constructive_indefinite_description` and `classic`, so such a theorem would refute a classically valid statement; an ARGUMENT, not a theorem, and the classical instance is not shipped. **WORK ITEM 2 IS DELIVERED, AND IT TOOK A HAND-ANNOTATED SECOND READING — the sentence "classifier_classifies is not statable for presheaf categories" is FALSE, and the precise one has three parts**: the theorem's BOUND `u <= u0` (objects ≤ homs) is what #402 records as unsatisfiable at `Sets`; at a presheaf category it IS satisfiable, so (a) the theorem is FORMABLE at an ABSTRACT classifier of `[C^op, Sets]`, (b) it is REFUSED at the built `Fun_Classifier`, whose elaboration minimizes the presheaf category's hom universe below its object universe ("Cannot enforce … because … <= … < …", pinned), and (c) a redefinition under declared universes with `so <= OO <= HH` DOES escape — section (I)'s `PShSmall := [Cs^op, Sets@{o so}] : Category@{OO HH HH}` carries `Fun_Classifier_small`, and **`fun_classifier_classifies` is `classifier_classifies` applied to it over `Fun_HasPullbacks Sets_HasPullbacks`**, an isomorphism in `Sets` between the subobjects of a presheaf and the maps into Ω. The escalation was measured in four steps and each refusal quoted: the constant cannot be re-typed; every TYPE involved IS formable there; the already-built `char_square` DOES inhabit the inflated field; `presheaf_char_pullback` rebuilds but `presheaf_char_unique` does not, its `IsPullback` argument's two universe readings being refused as interconvertible (measured at the argument; the field where they visibly differ is `ump`, which quantifies over the ambient objects and hom-types) — so the two uniqueness lemmas are RESTATED in that section and nothing else is. **NATURALITY COMES WITH IT AND WAS NOT EXPECTED TO**: `Sub_classifier_natural` carries the same bound, so `fun_sub_classifier_natural` and `fun_Sub_Representable` are that theorem and its representability corollary applied — measured formable at an abstract classifier, refused at `Fun_Classifier`, accepted at `Fun_Classifier_small`; there is still NO naturality statement for `Fun_Classifier` itself. The two round trips are delivered for the minimized reading (`fun_char_roundtrip`, `fun_pullback_roundtrip`, both `:=` of `Structure/SubobjectClassifier.v`'s own constants, both NAMING `Fun_HasPullbacks Sets_HasPullbacks` in their statements — a plain `Definition`, so nothing resolves it silently). **THE "CONVENTION" IS A MATTER OF NAMES, AND THE FILE SAYS SO**: every classifier puts ITS truth value on the subobject (the pullback square says so), so what varies between the books is only the NAME — Mac Lane's 0 (§IV.9 construction 1), Seven Sketches' `true`, `FinSet_Classifier`'s `fin_true`, which is `Fin.F1`, the first element of `Fin.t 2`, and so IS Mac Lane's 0 as a numeral (the "swap 0 ↔ 1" #402 records is between the books' names, and its OneLevel.v header now says so), #402's `ptrue`/`Powerset_truth_point` with no numeral at all; here Ω(c) is a set of sieves whose truth point is the total sieve and carries no numeral either — an element of the subobject has the total characteristic sieve (`char_square`, hypothesis-free) and the converse is the pullback clause, where `Untruncate` is spent. **THE ARROW-SHAPE CASE IS PROVED, NOT ASSERTED** (the issue's other reviewer check): at `C := _2^op`, so that the presheaf category IS `[_2, Sets]` (`(_2^op)^op = _2` by `eq_refl`), the three sieves on `TwoX` and two on `TwoY` are pairwise ≉-distinct and `jmap := fmap[Sieve_Presheaf (_2^op)] TwoXY` carries total ↦ total, mid ↦ total, empty ↦ empty (`j_total`, `j_mid`, `j_empty`), so it IDENTIFIES the first two and SEPARATES the first from the third (`j_identifies`, `j_separates`) — Mac Lane's `j0 = 0, j1 = 0, j2 = 2`, whose fibres {0, 1} and {2}, together with the page's 0 ON the subset (the subobject's elements have the total characteristic sieve), FORCE the dictionary total ↔ 0, {TwoXY} ↔ 1, ∅ ↔ 2 — the numeral enters only through the codes `twoX_code`/`twoY_code`, which send the total sieve to `Fin.F1`, the same element `fin_true` is; and the identification with his EXPLICIT object is the theorem **`mac_dictionary : twoY_code Em (jmap S) = macj (twoX_code Em S)` for EVERY sieve S**, over `MacOmega : _2 ⟶ FinSet` whose `fobj TwoX = 3`, `fobj TwoY = 2` and `fmap TwoXY = macj` all hold at `eq_refl`. **THE PRINTED PAGE CARRIES A MISPRINT and it is recorded rather than corrected away**: the text says "j : {0,1,2} → {0, 1}" while the diagram's corner is "{0,2}" and the formula in the same sentence is "j0 = 0, j1 = 0, j2 = 2", so the codomain is the two-element {0, 2}. The COUNTS are constructively unavailable — a sieve on `TwoX` is a pair of `Prop`s under mutual implication, so "exactly three" is as strong as two-valuedness of `Prop` — and are delivered under `IEM` as `twoX_three_sieves`/`twoY_two_sieves`, the SAME conditionality as the general instance. **`Instance/Fun/Pullback.v` IS ITEM 3 AND WAS GENUINELY ABSENT**: before it no `HasPullbacks`, `Pullback` or `IsPullback` for ANY functor category existed anywhere (Instance/Fun/Morphisms.v said so in its own prose), and `Definition x : HasPullbacks ([_2, Sets]) := _.` is a RESOLUTION refusal pinned with `Fun_HasPullbacks Sets_HasPullbacks` as the control. The apex-pinned `Fun_IsPullback` comes FIRST and is the general one (an arbitrary `D`, no `HasPullbacks` in its type); the chosen pointwise pullback is then `Fun_Pullback_Functor` with two natural projections, `Fun_Pullback := is_pullback_pullback (Fun_IsPullback …)` so the mediator is constructed ONCE, and `Fun_HasPullbacks` a plain `Definition` on `Fun_HasIndexedProducts`' precedent (whose own header calls that an UNTESTED PRECAUTION, and this file repeats the qualification). **FIVE `eq_refl` readbacks there, and the fifth REFUTED A PREDICTION**: the pullback object, both projections and `pullback` of the class read back on the nose as expected, and so does the MEDIATOR (`fun_pullback_med_strict`) — the brief expected only `≈` on the ground that the bundled record reaches its universal property through `is_pullback_pullback`, and measurement says otherwise, `is_pullback_pullback` being a field repackaging and `Build_Transform'` transparent; the `≈` form is kept beside it as the shape a consumer rewrites with. **THE ISSUE'S "Current state" IS STALE ON FOUR COUNTS, each measured**: `ls Instance/Fun/` has SEVEN satellites, not one; the terminal object of a functor category is #339's `Functor_Category_Terminal` (with `Two_Sets_Terminal` beside it, so "not even a Terminal instance" is stale twice); the pointwise mono characterisation is #369's; and "the only `SubobjectClassifier` instance is `FinSet_Classifier`" has been stale since #402's two conditional `Sets` ones. What IS absent, and is what these files supply, is any sieve at all (`rg -in 'sieve'` returns two prose lines), any classifier for a functor category, and any pullback instance for one. **WHY THREE FILES**: `Theory/Sieve.v` is LEAN — closure **16**, a SUBSET of both `Theory/Sheaf.v`'s 18 and `Construction/Localization.v`'s 23 measured as sets with EMPTY difference, so either could reuse it later at zero module cost, and NEITHER IS REWIRED HERE; `Instance/Fun/Pullback.v` is a general `[C, D]` result with no `Sets` in it (closure 22); `Instance/Fun/Classifier.v` is the `Sets`-valued assembly (closure **105**, marginals Instance/Fun/Terminal 29, Instance/Sets/Classifier/OneLevel 14, and Theory/Sieve, Structure/SubobjectClassifier/Natural, Instance/Fun/Morphisms and Instance/Fun/Pullback 1 each, EVERY other Require 0 — `Instance/FinSet` included, which is why `MacOmega` lives in the library file). **THE PROP-VALUED MEMBERSHIP IS FORCED, NOT CHOSEN**: a `Type@{o}`-valued sieve type sits at `o+1`, so it is not the carrier of an object of `Sets@{o so}` — "Cannot enforce o < o because o = o", pinned with the `Prop`-valued `Sieve` as the control — which is why the characteristic sieve must truncate with `Powerset_squash` and pay for it with `Untruncate`. **Universes measured off BOTH binder and block over all 161 constants**: `Fun_Classifier@{u u0 u1}` is over `C : Category@{u1 u u}` with `Untruncate@{u}` — C's hom, C's proof and `Sets`' CARRIER universe all one level, IN THE BINDER — while its block carries NO equation at all, only bounds, and `Fun_Classifier_small@{co o so OO HH u u0}` carries none either; `Fun_HasPullbacks` is the same trap read the other way, its binder filling four hom/proof slots with one level while its block has no equation and no `Set`, where SEVENTEEN of Pullback.v's 24 constants DO carry `Fun`'s `u0 = u2` in the block. THIRTY-SEVEN of the 161 carry a block equation (Pullback 17, Classifier 20 — Section Char's FOURTEEN carrying `u1 = u2`, E's and U's `Sets` universes, bound on SEPARATE Context lines and equated by `Context (theta : U ⟹ E)`, handed to every constant by discharge, `Efmap_resp` included though its own type never mentions `U`, where the same body without `theta` carries no equation and Section Pullback, with the same `theta` but `{E U}` bound on ONE line, carries none either, its identification sitting in the binder; Section Unique's THREE, whose `u1 = u2` is a different identification under the same spelling, `u2` being a universe `Sieve_Presheaf` brings in through `h` and the `IsPullback` Context `HP` equates with the `Sets` level (all measured out of tree, none pinned); plus section (I)'s three; Sieve.v ZERO); SEVENTY-FOUR carry a word-bounded `Set`, ALWAYS as the strict lower bound `Set < _` and never as an equation, and Pullback.v carries none. The two BINDER identifications are the presheaf category's and each is guarded: `ch = cp` has TWO INDEPENDENT donors, `Opposite` and `Fun` (the second refused with no `^op` in the command at all) while `@Functor Cu Cu` and `@Functor Cu Sets` are ACCEPTED at those very levels, so `Functor` is NOT a donor; `ch = o` is `Fun`'s in both variances, with `Cv^op` and both functor types accepted as controls; and `u0 = u2` is `Fun`'s too, `@Functor Cw Dw` being accepted where `[Cw, Dw]` is refused. C's OBJECT universe is only BOUNDED, which is Awodey's "for any SMALL category C". **161/161 constants closed under the global context with ZERO `Axioms:` lines** — 22 + 24 + 115, counted as the entries `Print Module` emits at five-space indent (the `Program` obligations, invisible to the `.glob`, among them) plus `Build_Sieve`, which it lists only after a `:=`; every one queried FULLY QUALIFIED, and all 161 are in the `make print-assumptions` gate as a new block of 12,900 bytes, whose `closed` count rises by exactly 161 (4998 → 5159) with the `Axioms:` line count unchanged at 1. **`Defined` tokens: 0 / 4 / 7, of which 4 and 4 are LOAD-BEARING**, measured by flipping each ALONE to `Qed` and compiling the result alone (all four of Pullback.v's; `sieve_sub_map`, `sieve_subpresheaf`, `sieve_incl` and `sieve_cone` in Classifier.v, while `med_img`, `presheaf_char_pullback` and `small_pb` survive the flip and are kept `Defined` because they produce data). Statements closed by `:= eq_refl`: 0 / 5 / 14. **Zero DECLARATION collisions** over the 188 names this change introduces, swept whole-word over all 893 other `.v` files and instrument-checked at `Full` (97) and `Monoid` (94) — the only hits are the three prose pointers this same change writes into Theory/Sheaf.v and Instance/Fun/Morphisms.v — after TWO REAL COLLISIONS were found and renamed away before landing (`efmap_comp` and `efmap_id` are FIELDS of `Construction/Enriched.v`, which the gate loads into one scope). `Test/ProbeFunClassifier403.v` mirrors the union of the three `Require` lists and carries **17 refutation commands = 1 instrument + 16 negatives of FOUR kinds told apart by the error TEXT** — 10 FORMABILITY, 2 RESOLUTION (`Cannot infer this placeholder`), 3 TYPING (one `Illegal application`, two plain has-type clauses with no cannot-unify and no universe clause), 1 CONVERSION (`sieve_subpresheaf c (total_sieve c) = fobj[Curried_CoHom C] c`, both sides `Check`ed as objects of the same category) — each stripped ONE AT A TIME, compiled alone and its whole error read; guard coverage measured mechanically (67 identifiers inside a refutation command, 55 also outside, the twelve exceptions exhaustively the keyword, three bound variables, the seven names DECLARED inside a refutation command and the instrument's absent name); rename-simulated **11/11** with every break on a `Check` line of the guard block and none inside a refutation command, after ONE VACUOUS GUARD was found that way and closed (`sieve_incl_Monic` was named only inside its negative, so renaming it left the probe compiling; the guard block was extended after the run, and every one of the eleven has its `Check` in the shipped file). `make todo` grows by **18, ALL in the probe** (1983 → 2001), the three library files contributing ZERO — so the DoD box "make todo adds no new hits" is NOT met as written and is disclosed in all three headers. NOT delivered: no unconditional axiom-free instance and no impossibility theorem; no proof that `Untruncate` is necessary and no `DecImage` variant; no `@SubobjectClassifier ([_2, FinSet]) _` instance (it would need the shape-`_2` converse of the mono characterisation and a three-way characteristic map, neither built) and no bijection of sieves with `Fin.t 3` without `IEM`; no CONVERSE for `Fun_IsPullback` and no separating example either; no pushouts, equalizers or completeness for `[C, D]`; no `ElementaryTopos` for any presheaf category IN THAT FILE — the exponentials are Instance/Fun/Exponential.v's `Functor_Category_Closed` (#718) and #404's `Instance/Fun/Topos.v` joins the two into `Presheaf_Topos`, conditionally on #402's `Untruncate`, which this file's `Fun_Classifier` takes as its hypothesis, `Instance/Fun/Closed.v` refuting the inheritance of cartesian closure in general; no naturality for `Fun_Classifier` itself; no comparison of the two readings at any strength (the probe pins that they do not even share a type); no isomorphism between the subpresheaf of the total sieve and the representable; no Grothendieck topology, coverage or sheaf reading, and Theory/Sheaf.v and Construction/Localization.v are NOT rewired, only pointed at this work in prose; no passage from a subobject of the representable BACK to a sieve (`sieve_subobject` runs one way, so Awodey's "as a subobject of the representable" is an embedding, not an identification); no comparison of `Sieve_Presheaf` at a poset with a lower-set construction, no cosieve and no lattice of sieves; and nothing is registered as an `Instance` except `Sieve_Setoid`, which `SieveObj` needs
- **Instance/Grp/Quotient.v**, **Instance/Grp/Quotient/Isomorphism.v**, **Instance/Grp/Quotient/Colimit.v**: Mac Lane §III.1 construction 5 + Ex 4, Awodey §4.2 (factor group, Thm 4.4, Cor 4.5) and Riehl §3.1 Example 3.25 — quotient groups and the isomorphism theorems BY UNIVERSALITY. **`NormalSubgroup` is exactly the five facts `Instance/Grp/Abelianization.v` had named as sufficient** (≈-saturation, unit, closure under product and inverse, normality), so `CommutatorNS` packages that file's pre-existing `InCommutator` with ZERO new proof and the refactor removes duplication rather than adding a layer. `QuotientGrp`/`quot_proj` is the setoid quotient (carrier unchanged, relation coarsened — no new carrier), `KillsFunctor N : Grp ⟶ Sets`, and `quot_universal_element` is the §III.1 universal element, with `quot_universal_elem_is_proj` and `quot_universal_med_is_quot_med` recording by `eq_refl` that the class's element IS the projection and its mediator IS `quot_med`. Awodey Thm 4.4 is the biconditional `hom_theorem`, and `KernelNS` proves the kernel normal. **Since the PR "algebraic carriers are sets" (2026-09-17) `QuotientGrp`'s equality is the propositional truncation `quot_equiv N a b := inhabited (quot_rel N a b)`, and three biconditionals of these files hold up to that truncation** — an earlier revision of this bullet stated them untruncated: `quot_proj_kernel` and `psi_kernel_is_meet` now conclude `↔ inhabited (…)`, `S3_proj_kernel_is_A3` is restated with the first, and the memberships of `NmodM` and `MeetNS` became `inhabited (sub_mem N a)`, FORCED because a `Subgroup`'s saturation must turn `inhabited (quot_rel M a b)` plus membership of `a` into membership of `b`. Each untruncated direction is kept whole and separately named — `quot_proj_kills` was already there and `psi_kernel_of_meet` is new — `quot_rel` and its eleven lemmas, `sub_mem`, `InNormalClosure`, `normal_closure_least`, `GrpImage` and `GrpSurjective` are all unchanged, and measured: `NmodM` and `MeetNS` have no consumer outside their own file and the three biconditionals are consumed only through the direction that survives untruncated, so nothing in tree reads a lost witness. **The universality accounting is honest and graded rather than uniform** — the file states which theorem is what: the THIRD is a pure chase (its mediator lemmas inspect no element; its one element-touching step is the definitional transfer `third_precompose_kills`, proof term `` `2 x a Ha ``), the FIRST derives its isomorphism universally but must read a preimage out of an image-membership witness because the image is DEFINED elementwise (it is DATA, a sigma — nothing chosen, and no coset object exists anywhere in these files), and the SECOND is a corollary of the first composed with two identity-carrier repackagings which are inverse pairs rather than instances of `universal_element_iso` — the comparisons for the FIRST and THIRD come from `universal_element_iso` alone while the SECOND does NOT — it is the first's comparison composed with those two hand-built pairs, so the quantifier is over two of the three and not all — and `first_isomorphism_to_is_quot_med`/`_from_is_image_med` show BOTH legs of the first comparison ARE the two mediators by `eq_refl`. Uniqueness is PROVED for the first and third (`first_isomorphism_unique`, `third_isomorphism_unique`); the second has none, being a composite. Colimit.v gives Awodey's coequalizer presentation and **the tree's FIRST concrete cokernel** — `IsCokernel` occurs only in `Structure/Kernel.v` and `Structure/Abelian.v`, and every inhabitant there is conditional on `Abelian`, which has no instance — with `InNormalClosure` and its leastness giving Riehl's `normal_closure_IsCokernel`, reusing `Structure/Kernel.v`'s definition rather than restating it. **A DONOR UNIVERSE PIN is disclosed**: `Grp_trivial`/`Grp_Zero` (`Instance/Grp.v` — an earlier revision of this bullet named two other sites, correct until the PR "algebraic carriers are sets" (2026-09-17) grew the file, and the readback it quoted, `GrpObject@{u Set u}`, is now `GrpObject@{u u Set}`, the record's universe roles having permuted to `(aux, carrier, proof)` so that the pinned slot is the THIRD rather than the middle) carry a single universe binder where the record needs more, pinning one argument to `Set` even though the donor `unit_setoid@{t u}` is polymorphic in exactly it — so **every cokernel and coequalizer-against-zero statement about `Grp` is confined to `GrpObject@{Set Set Set}`**, the whole of Colimit.v; the pin is `Instance/Grp.v`'s, is NOT repaired here, and `Quotient.v`/`Isomorphism.v` are proved free of it by a probe section instantiating them strictly above `Set`. **CORRECTION (#450): THE PIN IS LIFTED** — #450 wrote out `Instance/Grp.v`'s zero-object universes in place (`Grp_trivial@{p} : GrpObject@{p p p}`, `Grp_zero_hom@{u p}`, `Grp_Zero@{u p}`), and re-read by `About`, `normal_closure_IsCokernel@{u u0}` is over `GrpObject@{u0 u0 u0}` and `quot_IsCoequalizer@{u u0 u1}` over `GrpObject@{u1 u1 u1}`, with no binder instantiated at `Set` and the one `Set` in each block a lower bound (`Set < u`, `Set < u0`); so Colimit.v's statements hold at every carrier level, and `Test/ProbeGrpQuotient.v`'s zero-morphism negative is the control `control_big_zero`. It also forced per-lemma binders in Colimit.v, a section `Context` making the group's universe rigid. Non-vacuity throughout on `S3` (`Instance/Grp/TwoFunctors.v` — note `S3_Grp` at `Structure/Groupoid.v` is Deloop's DIFFERENT `GrpObject` record and is not an object of `Grp`): `A3` proper and nontrivial, the quotient proved not to collapse, `S3_refl_sub` a subgroup proved NOT normal so the second theorem's plain-subgroup generality is exercised, and the normal closure proved strictly larger than the subgroup. Four strict attempts REFUTED and pinned as `Fail` probes (three since #450, the fourth — the zero morphism refused above `Set` — now a control; `grep -c '^Fail' Test/ProbeGrpQuotient.v` 4 → 3), including that `SubgroupGrp (KernelNS h) = Grp_kernel h` converts on carriers but not as whole records. Deferred: no `HasCokernels`/`HasCoequalizers` instance for `Grp` (since #450 `Grp_HasCoequalizers_via_GAFT : HasCoequalizers Grp` exists in `Instance/Grp/Colimit.v` — a `Definition`, not a registered instance, and non-computing — and its coequalizer of `(f, 0)` is compared with this file's quotient by `Grp_coequalizer_agrees_normal_closure`; no `HasCokernels` is built); no correspondence/lattice theorem, index, Lagrange or Jordan-Hölder; no naturality of the isomorphisms; no universal-ARROW packaging, structurally — `KillsFunctor` is the subfunctor of `Hom(G,−)` cut by the killing condition, not the shape `d ↦ Hom(c, S d)` that `universal_element_arrow_subsumption` needs
- **Instance/Grp/Congruence.v**: Mac Lane §II.8 Ex 2 + Awodey §4.2/§4.5 Ex 1 — group congruences ARE normal subgroups, and the quotient category of a delooping IS the delooping of the quotient group. **The delooping was already in tree and this file does NOT rebuild it**: `Instance/Grp/Free.v`'s `grp_deloop_monoid`/`grp_deloop`/`grp_deloop_IsGroupoid` deliver the one-object groupoid of an `Instance/Grp.v` group, built incidentally en route to the free group, and Free.v's own header already documents the TWO RECORDS NAMED `GrpObject` — `Construction/Deloop.v`'s, layered on `MonObject` with both unit and both inverse laws as fields, versus `Instance/Grp.v`'s flat one carrying only the left-handed laws — so everything here is stated over the existing `grp_deloop` and no third delooping is created. The record conversion IS supplied, by REUSE rather than a fresh literal: `Structure/Groupoid.v`'s `MonInverses`/`GrpObject_of` exist precisely to separate having-inverses from the bundling, so `grp_deloop_GrpObject := GrpObject_of (grp_MonInverses H)` and **`deloop_GrpObject_agrees : Deloop (grp_deloop_GrpObject H) = grp_deloop H` by `eq_refl`** — a guarantee a hand-built literal could not have given. (`Instance/Rep.v`'s `grp_mon` is not a donor for two reasons: it drops the inverse, and it is `Program`-built with `Qed` obligations, so it would not convert.) The correspondence is a genuine bijection of setoids, **`ns_cong_iso : NS_Setoid ≅[Sets] Cong_Setoid`**, with `congruence_normal_subgroup` as the issue's named alias; `ns_rel N ttt ttt f g = quot_rel N f g` by `eq_refl`, so EVERY #313 lemma about `quot_rel` is a lemma about this congruence, and all four `HomCongruence` fields of `ns_congruence` are #313's `quot_rel_of_equiv`/`_sym`/`_trans`/`_mul` applied verbatim with zero new proof. **Stronger than the exercise asks**: `congruence_iff_normal` holds for an ARBITRARY `Subgroup S` — `HomCongruence (sub_rel S) ↔ S` normal — so the correspondence is with normal subgroups EXACTLY, not merely defined on them. The quotient corollary is `deloop_quotient_iso : deloop_quotient N ≅[StrictCat] grp_deloop (QuotientGrp N)`, both functors identity on objects and arrows, with the `≅[Cat]` reading derived and labelled weaker. **Since the PR "algebraic carriers are sets" (2026-09-17) `deloop_quotient` is built on a TRUNCATED congruence** `ns_prel := inhabited ∘ ns_rel`, and that was not a convenience: `QuotientGrp`'s equality became `inhabited (quot_rel …)` in the same PR, so without the matching truncation `deloop_quotient_iso` would not merely be unproved but FALSE in one direction, a functor `grp_deloop (QuotientGrp N) ⟶ deloop_quotient N` having to eliminate a `Prop` into the `Type`-valued `quot_rel`. With both truncated the headline is unchanged in statement and unchanged in strength — still `≅[StrictCat]`, and re-measured at `eq_refl` on objects, arrows, identity and composition, of which the probe guards the last three and not the object leg. `ns_rel`, `ns_congruence` and the whole congruence dictionary are UNTOUCHED, the new `ns_prel_is_truncation` records the relationship at `eq_refl`, and `cat_lift` goes through a new `deloop_pkills`; the "`ns_rel N ttt ttt f g = quot_rel N f g` by `eq_refl`" sentence above therefore still holds word for word. **Awodey §4.5's specialization is real but its provenance is disclosed rather than dressed up**: the category-level homomorphism theorem exists unnamed as `Construction/Quotient.v`'s `QuotientLift`/`QuotientLift_factors_strict`/`QuotientLift_unique`, and the file records the deflating measurement that `QuotientLift_unique`'s proof is `exact HG` — in a quotient category the projection is the identity on morphisms, so the uniqueness clause IS its own hypothesis restated, which is exactly why the group-level uniqueness costs nothing; the route consumes exactly TWO proofs about `N` — #313's `kills_descends` and `quot_rel_refl`, the latter supplying the two functor-law fields of `quot_to_deloop`/`deloop_to_quot` at four sites — plus `Build_GrpHom'`'s `grp_map_unit_from_mul` as a second group-theoretic step; an earlier draft said `kills_descends` was the ONLY one, which over-read. `kernel_ns` gives Awodey §4.2's kernel-is-normal its CATEGORICAL provenance — kernels are normal BECAUSE `FunctorKernel_Congruence` says a functor's kernel congruence is a congruence — with zero group-level obligations, while #313's `KernelNS` is cited and left alone. Six strict attempts REFUTED and pinned, the sharpest being that `deloop_quotient N = grp_deloop (QuotientGrp N)` converts on exactly FOUR of the ten fields a `Category` literal supplies (`obj`, `hom`, `id`, `compose`), the other six being built twice. Non-vacuity over #313's S3 (`Instance/Grp/TwoFunctors.v` — note there are THREE S3 presentations in tree and `Structure/Groupoid.v`'s `S3_Grp` is Deloop's different record): `A3_relates_rotation` merges, `A3_separates_reflection` separates, `S3_refl_sub_no_congruence`. Deferred: no congruence lattice or correspondence theorem for subgroups of G/N; no functoriality of `N ↦ ns_rel N`; no comparison with `CongClosure`; **no monoid analogue** (`cong_ns` spends both inverse laws, and nothing is claimed about congruences on a delooped monoid); no Leibniz inequality of the two categories (measured, not proved)
- **Instance/Mod/Quotient.v**, **Instance/Mod/Quotient/Isomorphism.v**, **Instance/Rng/Quotient.v**, **Instance/Rng/Quotient/OneSided.v**: Mac Lane §III.1 Ex 5-6 — quotient modules and quotient rings BY UNIVERSALITY, the module/ring analogue of #313's quotient groups. **The normality layer behaves ASYMMETRICALLY between the two halves, and that asymmetry is the mathematical content.** On the module side there is deliberately NO `NormalSubmodule`: `Record Submodule` carries membership plus FOUR laws, and `smod_normal` — `ns_conj`'s statement transcribed additively, `∀ t a, mem a → mem ((t+a) − t)` — is a THEOREM of an arbitrary submodule. **Commutativity removes TWO fields, not one**: normality, and closure under negation, the latter because the ring's `(−1)` gives `smod_neg = (−1)·a`; the same economy makes `Ideal` five laws with `idl_neg` derived. And the `ns_conj` ↔ two-sidedness correspondence is NOT one-to-one — in `Grp` normality is spent TWICE (`quot_rel_mul` AND `quot_rel_inv`), while in `Rng` two-sidedness is spent ONCE (`rquot_rel_mul` only), additive commutativity handling the negation congruence. **One-sidedness is REFUTED, not assumed**: `Instance/Rng/Quotient/OneSided.v` exhibits `E11Left : LeftIdeal UT2` (the column ℤ·E₁₁ in the upper-triangular 2×2 ring) and `ut2_left_ideal_not_mul_congruence` refutes **the congruence itself** — the property `rquot_rel_mul` establishes and the construction actually consumes — where #313's `S3_refl_sub_not_normal` refutes the closure condition. **Read that comparison precisely — an audit corrected an earlier overclaim here**: the group side ALSO carries a congruence-level refutation at the same subgroup, `Instance/Grp/Congruence.v`'s `S3_refl_sub_no_congruence` (landed by #301), so it is NOT the case that the group side leaves an inference to the reader; what is actually different is only that the ring refutation is DIRECT where the group one goes through `congruence_iff_normal`, and that it lands on the MULTIPLICATION congruence specifically, the clause one-sidedness cannot supply. `ut2_e11_not_right_absorbing` and `E11Left_is_not_an_Ideal` separately refute the dropped closure, and the positive half at the SAME ring is `StrictUpper`, a proper nontrivial two-sided ideal whose quotient does not collapse and **is commutative though UT2 is not**. Both universal elements are over `Theory/Universal/Element.v`'s class with the underlying arrow the projection and the extracted mediator the direct one, both by `eq_refl`. **The three module isomorphism theorems are GRADED, not claimed uniform**, following #313's discipline: the THIRD is a pure universal-property chase (its mediator lemmas destructure nothing; the only element computation is `TmodS`'s saturation obligation, a prerequisite to STATING the theorem); the FIRST derives its comparison from `universal_element_iso` but `mod_image_med` DESTRUCTURES an element, reading the preimage out of the image witness (data, so nothing is chosen), and its uniqueness clause is proved; the SECOND is a corollary composed with `mod_surjective_image_iso` and `mquot_congr`, which ARE hand-built inverse pairs with round trips — degenerate, identity legs, but not instances of `universal_element_iso` — and has no uniqueness clause. **No proof anywhere manipulates a coset**, the setoid quotient forming no coset object, and the header says that is a property of the presentation. **`RModQuotient` is RECONCILED rather than left as a second unshared construction**: `ImageSubmod f : Submodule N`, `rmod_quotient_relations_agree` a biconditional at every pair, and `RModQuotient_is_quotient_by_image : RModQuotient f ≅[RMod R] QuotientMod (ImageSubmod f)` with both legs the identity on elements — the two relations being `{a & x ≈ y + f a}` versus `{a & x − y ≈ f a}`, NOT convertible (pinned as a `Fail` probe), the shuffle between them being the biconditional's whole content. **`AbQuotient` is deliberately NOT unified, and the dependency argument runs OPPOSITE to the one #313 recorded**: `Instance/Ab.v` is strictly UPSTREAM of `Instance/Mod.v` (Mod requires Ab, and `RModQuotient` is literally built on `AbQuotient`), so routing it through a submodule quotient would INVERT an existing dependency — the reason not only still applies but applies with more force, and the honest unifier would be an `AbSubgroup` interface in `Instance/Ab.v` of which `Submodule` is the extension, recorded but not built. `Instance/Ab/Subtract.v` is the shared subtraction calculus, factored so no ring file depends on a module file for it and labelled an upstreaming staging post rather than the first word (`ab_neg_invol` is knowingly its THIRD copy in tree). **A name-collision hazard was found and fixed**: 34 names collided with `Instance/Grp/Quotient/Isomorphism.v`, and since the gate Requires both in one scope, `Print Assumptions first_isomorphism_theorem` would silently have audited the WRONG constant — the module side is renamed `mod_*`/`ImageSubmod`, and zero collisions remain among the four quotient files. Non-vacuity over ℤ throughout (2ℤ proper and nontrivial; 2ℤ ⊃ 4ℤ with the middle quotient proper AND nontrivial for the third theorem; 2ℤ and 3ℤ incomparable with `1 ∈ 2ℤ+3ℤ` for the second) and the ring UMP EXERCISED, the mediator ℤ/6ℤ → ℤ/2ℤ being the class-produced one by `eq_refl`. Deferred: the RING isomorphism theorems (the image of a ring hom is a SUBRING, not an ideal, needing a `Subring` interface the issue does not ask for); no sum/product/intersection of ideals, no principal/prime/maximal ideals, no CRT; no identification of ℤ/2ℤ with `F2_Ring`; no `HasCokernels`; no submodule lattice, correspondence theorem, modular law or exact sequences; no naturality of any isomorphism
- **Instance/Mod/Limit.v**: Mac Lane §V.7 p. 128 (PDF p. 137, `maclane:V.7:construction1`, the premise "K-Mod is small-complete") and §V.1 Theorems 2–3 one level up, issue #449 — **`RMod_Complete : Complete (RMod R)`: limits of modules are created from abelian groups**. `RMod_Forget_Ab R : RMod R ⟶ Ab` (`Instance/Mod.v`) STRICTLY creates every limit from `Ab_Complete` (`Instance/Ab/Limit.v`), by the template of that file one level up: given a limiting cone `L` in `Ab` over `RMod_Forget_Ab ◯ K`, the scalar action on its apex is the mediator `mlim_smul r` of the cone whose legs are `r·` after the legs (`mlim_smul_cone`), the four module laws are proved from the limit's uniqueness clause alone (`mlim_smul_distr_l/_r`, `mlim_smul_assoc`, `mlim_smul_one`, by `mlim_ext`), the action is the UNIQUE one making every leg a module map (`mlim_smul_unique`, Mac Lane's Theorem 2 for the action — the group is inherited on the nose, not lifted), the lifted cone `mlim_cone` lies over `L` at `eq_refl` (`mlim_over_obj`, `mlim_over_legs`), is limiting (`mlim_created`), and a module cone whose image is limiting is limiting (`rmod_reflects`). Packaged with Structure/Limit/Creation.v's classes exactly as `Ab` is — `RMod_Forget_Ab_StrictlyCreatesLimit`, `_CreatesLimit`, `_StrictlyCreatesLimits`, `_creates_limits : CreatesAllLimits`, `_reflects_limits`, `_lifts_limits` — with the corollaries `RMod_Complete := creates_limits_Complete …`, `RMod_Forget_Ab_creates_continuous : ContinuousFunctor (RMod_Forget_Ab R)` and `_PreservesAllLimits`, and for the underlying-SET functor `RMod_Forget_creates_continuous : ContinuousFunctor (RMod_Forget R)` through a bridge `rmod_abcone_of`, needed because `Instance/Mod.v` builds `RMod_Forget` as its own record: `RMod_Forget R = Ab_Forget ◯ RMod_Forget_Ab R := eq_refl` is REFUSED ("cannot unify") and so is ascribing the composite's continuity at `ContinuousFunctor (RMod_Forget R)` ("cannot unify `Cone.Cone (RMod_Forget R ◯ K)` and `Cone.Cone (Ab_Forget ◯ RMod_Forget_Ab R ◯ K)`"), while `fobj`/`fmap` agree at `eq_refl` (`rmod_forget_obj_agrees`, `rmod_forget_map_agrees`), which is what makes the bridge sound. **Computed readbacks, all `eq_refl`** (the analogue of `Instance/Ab/Limit.v`'s `AbComputed`, plus the one that section has no counterpart for): the created limit's carrier IS `Sets_limit_obj (Ab_Forget ◯ (RMod_Forget_Ab R ◯ K))` (`rmod_complete_carrier`) — a dependent pair of a family `∀ d, carrier (K d)` with its compatibility proof, equivalence pointwise on the family — and plus, zero, negation, the scalar action (`rmod_complete_smul`, no `Ab` counterpart) and the legs compute componentwise (`rmod_complete_plus/_zero/_neg/_leg`). The continuity constant is the CREATION-derived one and presupposes only `Sets_Complete` through `Ab_Complete`; it is NOT routed through `Instance/Mod/Coextension.v`'s `coex_adjunction : RMod_Forget_Ab ⊣ Coextension` (RAPL), the choice that keeps it usable as a GAFT premise without circularity (the distinction `Instance/Ab/Limit.v` draws for `Ab_Forget_Continuous`). **NOT DELIVERED**: no `HasIndexedProducts (RMod R)` or `HasEqualizers (RMod R)` instance — deliberately: `creates_limits_Complete` needs neither, so `Instance/Mod/Product.v`'s declined universe negotiation stays declined; no colimits or cocompleteness in this file (CORRECTION, #454: an earlier revision said "no colimits or cocompleteness" without the qualifier; since #454 `Instance/Mod/Colimit.v`'s `RMod_Cocomplete_via_GAFT` obtains every colimit within its shape discipline from `GAFT` at the diagonal over this file's `RMod_Complete`, and the file's header carries the same correction); no comparison of the created product with `Instance/Mod/Product.v`'s `ProdMod`; no monadicity (nothing says `RMod R` IS an Eilenberg–Moore category over `Ab`); no whole-record uniqueness `M = LimitMod` (only the action, `mlim_smul_unique`); right modules only through `ModR R := RMod (Ring_op R)` (`Instance/Mod.v`), not spelled out; `Instance/Ab/Limit.v`'s four-lemma duplication with `Instance/Grp/Limit.v` is neither repaired nor extended (this file borrows `absets_limit_ext`). **Measured**: 62 `.glob` heads (46 `def`, 16 `prf`) plus 6 `Program` obligations (four of them appearing in `strings … .vo` only under a junk digit prefix, stripped not dropped; `Print Module` cross-checks to the same 68) — 68 `Print Assumptions`, 68 "Closed under the global context", gated by `make print-assumptions`; 3 `Defined`, 22 `Qed` by token; two `Defined`s load-bearing by flipping to `Qed` in a copy of the whole file (`mlim_created` stops the strict-creation clause of `RMod_Forget_Ab_StrictlyCreatesLimit`; that instance stops `rmod_lift_apex`), while `rmod_reflects` is NOT load-bearing (flipped, the file compiles) and is kept `Defined` only to mirror `ab_reflects`. Universes by `About`: no `Set` anywhere (0 word-bounded hits over the 68 `About` blocks); `RMod_Complete@{u u0 u1 u2 u3 u4} : ∀ R : RingObject@{u u3 u4}, Complete@{u u u u0}` with `u < u0`, `u < u1` and NO equation — `Ab_Complete@{u u0 u1} : Complete@{u u u u0}` one ring parameter wider; 33 constants inherit `u = u2` (shape object = ring carrier universe) from `Complete@{u u u u0}`'s own quantification over shapes with coinciding universes, entering at `mlim_sets_limit`, attributed by an isolation `Example` in the same `Context` (which already carries `u = u3` and the strict constraints RMod's own constants carry, `Instance/Mod/Product.v`); no annotation written. Closure 53 files by iterated `coqdep` not counting the file itself (`Instance/Mod.v` 6 at the margin, `Instance/Ab/Limit.v` 1). Zero name collisions over all other `.v` and `.glob` files (`find|xargs`, instrument-checked on `Ab_Complete`). Boundaries in `Test/ProbeModLimit449.v` (instrument check; 53 positive `Check` controls and the 14 readbacks restated as `eq_refl` `Example`s; F1 the composite equation `RMod_Forget R = Ab_Forget ◯ RMod_Forget_Ab R` at `eq_refl`, F2 the composite's continuity ascribed at `RMod_Forget`, F3 `ContinuousFunctor` ascribed at `PreservesImageLimit`, F4 the scalar-action triangle at an ABSTRACT limit at `eq_refl` — all four CONVERSION, each paired with the term or the ≈-strength that IS available; every `Fail` stripped alone in a copy of the whole file).
- **Instance/Mod/Spanning.v**: Mac Lane §V.7 p. 128 (PDF p. 137, `maclane:V.7:construction1` — "it suffices to consider the bilinear β which span C, i.e. do not factor through a proper submodule of C" — and `maclane:V.7:ex3`'s "spanned by the elements a ⊗ b" with its base-change clause), issue #449 — **spanning bilinear maps, submodules as subobjects of `RMod R`, and the solution set for `Bilin V V'`**. (1) The `Submodule ↔ SubObj (RMod R)` bridge, absent before: `smod_subobj S : SubObj W` (`sub_dom`/`sub_mono` at `eq_refl` `SubmoduleMod S`/`smod_incl S`, built with `@Build_SubObj` explicitly) and `subobj_smod m : Submodule W`, the image submodule of `sub_mono m` (`Instance/Mod/Quotient.v`'s `ImageSubmod`), with the round trips at their true strength — `sub_le` both ways (`smod_subobj_image_le/_ge`, packaged `smod_subobj_image_equiv`) and membership inter-derivable but NOT `eq_refl` (`smod_round_to/_from/_iff`; probe N1 pins the refusal). (2) `MGen P`, the submodule generated by a `Type`-valued predicate, INDUCTIVE (base/resp/zero/plus/smul; negation DERIVED via `smod_neg`, `Quotient.v`), packaged `SubGenMod P` with `mgen_incl`, `mgen_least` by recursion, and — what #448 left unstated for varieties — leastness in the SUBOBJECT order, `mgen_sub_le_least : sub_le (smod_subobj (SubGenMod P)) (smod_subobj S)` for `S ⊇ P`. (3) Spanning in TWO forms proved equivalent: elementwise `RBilinearSpanning β := ∀ w, MGen (rbl_image β) w` and #448's categorical `Spanning (Bilin V V') (global_element β)` (`Adjunction/SpanningArrow.v`), `rbilinear_spanning_iff` as two named functions — forward, a subobject through which β factors bilinearly has an image submodule containing `rbl_image β`, hence all of `MGen`, so its mono is surjective as well as injective (`rmod_monic_injective`, `Instance/Mod.v`) and splits (`rmod_split`), which IS `sub_le sub_top m`; backward, corestrict β to `SubGenMod (rbl_image β)` (`rbl_gen_corestrict`) and read the splitting off the conclusion; neither direction consumes `sub_le_top_iso`/`spanning_forces_top`. (4) **The elementary tensors span**: `tensor_gen_spanning : RBilinearSpanning tensor_gen` by induction on `MTerm` in five cases (the `tensor_hom_ext` shape, `Instance/Mod/Tensor.v`), and `tensor_gen_spanning_categorical` in the `Spanning` form through (3) — so `(TensorMod V V'; (global_element tensor_gen; _))` is an inhabitant of `SpanningArrowsOutOf (Bilin V V') SetsOne`. (5) `RMod_HasWidePullbacks`, the tree's SECOND `HasWidePullbacks` instance and its first at an algebraic category (apex the submodule of `Instance/Mod/Product.v`'s `ProdMod` on which the family agrees), which **DOES NOT discharge `spanning_solution_set`'s hypothesis, for a universe reason measured and attributed**: `About` reads `RMod_HasWidePullbacks@{u u0 u1 u2 u3} : ∀ R : RingObject@{u u2 u3}, HasWidePullbacks@{u u u0 u} (RMod@{u0 u1 u3 u2 u} R)` with `u < u0` — the class's INDEX universe is the module-CARRIER universe `u`, strictly below `u0` where `RModObject R` (a record over a `SetoidObject`) and hence `SubObj W` and `FactoringFamily` live — and the application is refused with "universe inconsistency: Cannot enforce <carrier universe> = <SubObj universe>"; probe N3 isolates the donor as `ProdMod` (index ≤ carrier), so annotation cannot help. This is the module-side twin of the wall `Adjunction/SpanningArrow.v` records for `Sets_HasWidePullbacks`. (6) `Bilin_PreservesWidePullbacks : PreservesWidePullbacks (Bilin V V')`, proved against an ARBITRARY `IsWidePullback` through the element calculus `rmod_by_element` (`Instance/Mod/Representable.v`), which is what keeps its index universe free — a bilinear map into the wide pullback is exactly a compatible family of bilinear maps into the members. (7) The Lemma-2 solution set, CONDITIONAL on `HWP : HasWidePullbacks (RMod R)` for the reason in (5): `tensor_solution_set X := spanning_solution_set (Bilin V V') HWP (Bilin_PreservesWidePullbacks V V') X` and `tensor_esols := esols_of_sols …`, with `esol_index = SpanningArrowsOutOf (Bilin V V') SetsOne`, `esol_obj`, `esol_elem` at `eq_refl`. Mac Lane's p.-128 solution set WITHOUT wide pullbacks — corestriction to the generated submodule — is `Instance/Mod/TensorAFT.v`'s `tensor_esols_direct`, unconditional, and `tensor_esols_same_index` there pins at `eq_refl` that the two routes are indexed by the same type. (8) Exercise 3, spanning: `bal_gen_spanning` — every element of `Instance/Mod/Bimodule.v`'s balanced tensor `BalTensor` lies in the subgroup `ABGen` generated by the `bal_gen n m`, by induction on `bsum`. (9) Exercise 3, base change along `phi : S ~{Rng}~> R`: the comparison `bal_change : BalTensor (over S, on the restricted modules) ~{Ab}~> BalTensor (over R)` sends generators to generators at `eq_refl` (`bal_change_gen`), is SURJECTIVE (`bal_change_surjective`, by induction on `bsum`) and IS `bal_med` of the R-balanced `bal_gen` read as S-balanced (`bal_change_is_med`, by `bal_med_unique`). **NOT DELIVERED**: the kernel of `bal_change` (nothing exhibits it as generated by `(a·r) ⊗ b − a ⊗ (r·b)`, nothing shows the comparison ever an isomorphism); any non-degeneracy witness for ex3 (the tree has no non-commutative ring, `Instance/Mod/Tensor.v`); smallness of the solution set's index (a bare `Type`, inherited from `Adjunction/GAFT.v`); the wide-pullback route at `RMod` for the universe reason in (5); a PROPER-RESTRICTION witness — nothing exhibits a bilinear map that is NOT spanning, so the restriction Mac Lane's argument turns on is not shown to cut anything down, and the solution set is Mac Lane's in SHAPE and in nothing that bounds it. **Measured**: 80 `.glob` heads (40 `def`, 19 `prf`, 10 `constr`, 8 `scheme`, 2 `ind`, 1 `inst`) plus 26 `Program` obligations (enumerated from `strings … .vo` with the junk prefix stripped and cross-checked by `Print Module`; `ImageSubmod`'s three, which the census also shows, belong to `Quotient.v` and are not counted) — 106 `Print Assumptions`, 106 "Closed under the global context", gated; 16 `Defined`, 37 `Qed` by token; two `Defined`s load-bearing by flipping in a copy of the whole file (`WPullSub` stops `wpull_mod_commutes`; `bal_change_bil` stops `bal_change_gen`'s `eq_refl`), the other fourteen flipped individually AND together leave the file compiling. Universes by `About`: no `Set` in the universe block or printed type of any declared head or obligation, the only `Set` in the module being the auto-generated `MGen_rec`/`ABGen_rec` motives; `smod_subobj`, `subobj_smod`, `mgen_sub_le_least` and `RMod_HasWidePullbacks` carry no equation (and the module carrier universe strictly below two others, `smod_subobj` printing them as `u1 < u`, `u1 < u0`, the other three as `u < u0`, `u < u1`); `Bilin_PreservesWidePullbacks` carries `u = u0 = u1`, attributed to `rmod_by_element` (which reports exactly those two while `Bilin`, `wide_pullback_jointly_monic` and `IsWidePullback` report none); `bal_change` identifies the three universes of BOTH rings — the five equations are the SECTION BINDER's, not any constant's: a trivial `Example` in a Section carrying only `{T R : RingObject}` reports none, adding `Context (phi : T ~{Rng}~> R)` alone reports all five, the two `RModObject` binders add none, and `RestrictObj`, `RigHom_op`, `Ring_op`, `Rig_op` each report none (an earlier revision attributed them to `bal_left_T`/`bal_right_T` and to `Rng`'s hom by halves; a verifier's staged isolation refuted that). Traps met: the Rocq-9-only anonymous-constructor `(a; b)` term twice (closed with `exact`/`existT`); `reflexivity` inside an `existT` elaborating to Leibniz `eq`; `MGen`'s predicate is an explicit Section variable, so `mgen_base (P := …)` is refused ("Wrong argument name") — the constructors take it positionally; `bs_eq` constructors need `@` at the intended ring. Zero DECLARATION-head collisions over all other `.glob` files (the names are used, not redeclared, in `Instance/Mod/TensorAFT.v` and the probes; an earlier revision's `grep -rlw` criterion no longer returns zero in the PR tree for exactly that reason). Closure 162 files by iterated `coqdep` not counting the file itself, 107 more than `Instance/Mod/Tensor.v`'s 55 — the delta being 39 `Structure/`, 25 `Instance/`, 15 `Adjunction/` (`GAFT.v`, `SpanningArrow.v`, `Representability/Sets.v`, `SAFT.v` and the adjunction stack under them), 14 `Theory/`, 7 `Functor/` and 7 `Construction/` files (`comm -13` of the two sorted closures). Boundaries in `Test/ProbeModSpanning449.v` (its N1 the submodule round trip at `eq_refl`, CONVERSION, paired with `smod_round_iff`; N2 the exported wide-pullback instance refused by `spanning_solution_set`, UNIVERSE, paired with the same application accepted under an abstract `HasWidePullbacks` hypothesis passed explicitly; N3 the `ProdMod`-at-`SubObj` donor control, UNIVERSE, paired with `ProdMod` over a bare `Type`; every `Fail` stripped alone in a copy of the whole file) and `Test/ProbeModTensorAFT449.v` (N1 the direct covering family refused by `representability_theorem` and N2 `GAFT_from_spanning` refused at `RMod R`, both UNIVERSE; N7 `mgen_base (P := …)` refused — the predicate is positional; the readback `c_same_index` with `HWP` passed explicitly).
- **Instance/Mod/TensorAFT.v**: Mac Lane §V.7 p. 128 (PDF p. 137, `maclane:V.7:construction1`, the tensor product of modules from the adjoint functor theorem; `maclane:V.7:ex3`, the balanced tensor over a noncommutative ring by the same route), issue #449 — **the two premises of Mac Lane's argument are DELIVERED and the theorem is applied CONDITIONALLY, for a universe reason measured and attributed**. The premises, both unconditional and Set-free: `Bilin_continuous : ContinuousFunctor (Bilin V V')` (`Instance/Mod/Tensor.v`'s bilinear-maps functor preserves EVERY limiting cone — a bilinear map into a limit of modules is a compatible family of bilinear maps into its members, proved ELEMENTWISE through a four-lemma `Sets` element calculus `slim_elem`/`slim_elem_leg`/`slim_ext`/`slim_family_cone` over the limits `Instance/Mod/Limit.v` creates, at an ARBITRARY limiting cone, so no transport is consumed) and `Bilin_PreservesImageLimit := Continuous_PreservesImageLimit Bilin_continuous` (`Construction/Comma/Creation.v` must stand in the term: `ContinuousFunctor` does not ascribe where the theorem asks for `PreservesImageLimit`, probe F3 of `Test/ProbeModLimit449.v`), NOT routed through the existing tensor's representability — that route, `preserves_image_of_representable` at `tensor_repr_of_UE`, is CIRCULAR (it derives the AFT's hypothesis from the object the AFT is meant to construct; the distinction `Instance/Ab/Limit.v` and `Instance/Grp/FreeAFT.v` draw) and probe N6 pins that the delivered constant is a DIFFERENT term. Mac Lane's COVERING FAMILY — a solution set in shape, with no smallness — `tensor_esols_direct : ElementSolutionSet (Bilin V V')`, UNCONDITIONAL and without wide pullbacks: index `SpanningArrowsOutOf (Bilin V V') SetsOne` (the spanning bilinear maps out of V × V' with their codomains), covering an arbitrary β by its corestriction to the submodule generated by its image (`Instance/Mod/Spanning.v`'s `rbl_gen_corestrict`), which spans (`rbl_corestrict_spanning`), followed by the inclusion — `esol_index`/`esol_obj`/`esol_elem` at `eq_refl`, and `tensor_esols_same_index` pins at `eq_refl` that this p.-128 route and `Spanning.v`'s Lemma-2 route (conditional on wide pullbacks) are indexed by the SAME type. **The application**: `tensor_via_AFT (E : ElementSolutionSet (Bilin V V')) : Representable (Bilin V V') := representability_theorem (Bilin V V') RMod_Complete Bilin_PreservesImageLimit E` — CONDITIONAL on `E`, because feeding `tensor_esols_direct` is REFUSED: `representability_theorem`'s `ElementSolutionSet`'s INDEX universe is tied to the shape universe of the `Complete` it is handed, `RMod_Complete : Complete@{u u u u0}` makes that the ring's CARRIER universe, and Mac Lane's index is a sigma over `obj[RMod R]`, which sits STRICTLY ABOVE the carriers (`RModObject` is a record over a `SetoidObject`): "universe inconsistency: Cannot enforce <index universe> = <carrier universe> because <carrier universe> < <index universe>" (probe N1). This is a wall of the same FAMILY as the one `Instance/Grp/FreeAFT.v` measures for the free group (Mac Lane's literal index refused there too), but NOT the same wall, and the distinction is now a fact of the tree rather than a verifier's experiment. **Promoted from the experiment this bullet used to record.** An earlier revision quoted the refusal with a literal `Set` in it — `ElementSolutionSet@{u_obj Set u_Sets u7}`, "Cannot enforce Set = <object universe> because Set < <object universe>", "the theorem pins it to `Set`", and probe N2's `RMod_Complete R : Complete@{Set Set Set u}` against `Complete@{u_obj u_obj Set u_obj}` — and added that "a verifier applied #1309's `DiscreteCat_Functor` repair in a full copy, rebuilt, and the refusal REMAINS at `RMod R` and at `Ab` with the literal `Set` gone". That repair was APPLIED in tree at `Instance/Discrete.v` in the PR "algebraic carriers are sets" (2026-09-17), so the `Set`-free refusal quoted above is the current one and every `Set`-carrying quote here is history. The cause is unchanged: `Sets_Complete : Complete@{u u u u0}` fixes the shape universe at the carrier universe while any subobject-shaped index sits strictly above it; three further repairs (an annotated `ElementSolutionSet`, an abstract `Complete (RMod R)` in place of `RMod_Complete`, an annotated ring) are refused the same way, so #1309 does not lift this one; `GAFT_from_spanning` at `RMod R` is refused for the same reason (probe N2), so no adjunction `⊣` appears in the file. The size arithmetic behind all of this is stated once, authoritatively, in the size note in the header of `Structure/Complete.v`. The only in-tree inhabitants of the hypothesis are CIRCULAR and labelled so at every use (`tensor_esols_from_tensor`, index `unit`, read off `TensorMod`; `tensor_via_AFT_from_tensor`), the `Instance/Grp/FreeAFT.v` precedent. **Comparison, for EVERY `E`**: `tensor_AFT_iso : repr_obj (tensor_via_AFT E) ≅ TensorMod V V'` by `Functor/Representable.v`'s `repr_unique_iso` against `tensor_repr_of_UE := Representable_of_UniversalElement tensor_UniversalElement` (the DIRECT route `Theory/Universal/Element.v`, whose representing object is `TensorMod V V'` at `eq_refl`, `tensor_repr_of_UE_obj`), with the uniqueness clause `tensor_AFT_iso_universal`, the element-level `tensor_AFT_elem_iso`/`_commutes`/`_unique`, `tensor_AFT_iso_carries_elem` (the AFT's universal bilinear map carried along the iso IS `tensor_gen` at ≈) and `tensor_AFT_isos_agree`; the issue's verification name `module_tensor_universal` is an alias of `tensor_AFT_ue` (the universal element the theorem produces), pinned definitional by the probe's `c_alias_is_ue`; the object comparison is `≅`, NOT `eq_refl` (probe N5) — the AFT object is a comma-category limit, `TensorMod` a quotient of `MTerm`. **Exercise 3 by the same route over `Ab`**: `BalBiadd N M : Ab ⟶ Sets` (object part `Instance/Mod/Bimodule.v`'s `BalBiadditive`), `bal_universal_element : AUniversalElement (BalBiadd N M) (BalTensor N M)` from `bal_med`/`bal_med_unique` with `ue_obj`/`ue_elem` at `eq_refl`, `BalBiadd_continuous` (elementwise over `Ab_Complete`'s created limits), `bal_esols_direct` (unconditional; it needed four pieces of `Ab` vocabulary absent from the tree — `ABGenSub`, `AbImageSub`, `ab_split`, `abg_subobj`), `bal_tensor_via_AFT (E) : Representable (BalBiadd N M)` CONDITIONAL for the same universe reason (probe N3), and `bal_AFT_iso : repr_obj (bal_tensor_via_AFT E) ≅ BalTensor N M` with its uniqueness and element clauses. **The Set pin, measured**: exactly 21 of the 111 constants carry a word-bounded `Set` — the two AFT applications and everything downstream, the alias `module_tensor_universal` included — and the other 90 (element calculus, both continuity results, both direct solution sets, the `Ab` vocabulary, all 33 obligations) carry none; `tensor_via_AFT` reads `RingObject@{Set u8 u8}` (the theorem's own pin on the carrier plus its identification of the ring's 2nd and 3rd universes), and `tensor_AFT_iso` reads `RingObject@{Set Set Set}` because `tensor_repr_of_UE` adds the 1st = 3rd identification the EXPLICIT tensor carries (`Instance/Mod/Closed.v` names `tensor_med` as that donor; `TensorMod`/`tensor_gen` carry none) — attributed by isolation, `tensor_esols_direct` reading `RingObject@{u u0 u1}` with no identification; probe N4 pins that a ring declared above `Set` is refused. **NOT DELIVERED**: the unconditional application (above); any adjunction; smallness of the index (a bare `Type`); the `eq_refl` object comparison; a concrete witness at a named ring (the tree still has no non-commutative ring, `Instance/Mod/Tensor.v`); a repair of the #1309 artifact; the kernel of `Spanning.v`'s base-change comparison. **Measured**: 78 `.glob` heads (58 `def`, 20 `prf`) plus 33 `Program` obligations (20 of them visible in `strings … .vo` only under a junk digit prefix, stripped not dropped; `Print Module` cross-checks to the same 33) — 111 `Print Assumptions`, 111 "Closed under the global context", gated; 8 `Defined`, 50 `Qed` by token; two `Defined`s load-bearing by flipping in a copy of the whole file (`tensor_esols_direct` stops `tensor_esols_direct_index`; `bal_esols_direct` its twin), the other six flipped individually and together leave the file compiling. Closure 169 files by iterated `coqdep` not counting the file itself (7 more than `Instance/Mod/Spanning.v`'s 162). Zero DECLARATION-head collisions over all other `.glob` files (`find|xargs`, instrument-checked on `tensor_gen` and `Ab_Complete`); the names are USED, not redeclared, in `Test/ProbeModTensorAFT449.v` and in `Instance/Mod/Spanning.v`'s header prose. Boundaries in `Test/ProbeModTensorAFT449.v` (instrument check; positive controls for every constant a negative names and nine `eq_refl` readbacks; N1–N4 UNIVERSE, N5–N6 CONVERSION, N7 the positional-argument refusal of `Spanning.v`'s `MGen`; every `Fail` stripped alone in a copy of the whole file). **CORRECTED ON 2026-09-17 BY THE PR "ALGEBRAIC CARRIERS ARE SETS", AND THE CORRECTION INVERTS THE HEADLINE.** Everything above about the CONDITIONALITY is now history and is kept as history: the application is UNCONDITIONAL. `tensor_via_AFT : ∀ {R} (V V' : obj[RMod R]), Representable (Bilin V V')` takes no `E`, and so do `tensor_AFT_iso`, `tensor_AFT_iso_universal`, `tensor_AFT_ue`, `module_tensor_universal` and the four `bal_*` twins; every conditional form survives VERBATIM under an `_of_esols` name (five `tensor_*`, four `bal_*`, statements byte-identical to the pre-PR ones), so no theorem was lost and the sentence "the application is CONDITIONAL on `E`" should be read against `tensor_via_AFT_of_esols`. The mechanism is the next bullet. Three further corrections to the text above, each measured rather than smoothed: (a) the paragraph headed "**The Set pin, measured**" — 21 of 111 constants carrying a word-bounded `Set`, `tensor_via_AFT` reading `RingObject@{Set u8 u8}`, `tensor_AFT_iso` reading `RingObject@{Set Set Set}`, probe N4 — is entirely stale in both halves: the `Set` came from the unannotated `DiscreteCat_Functor` and was removed by the same PR (N4 turned over), and the file now holds **176 constants** rather than 111 (measured 2026-09-17: 136 `.glob` heads — `grep -c '^def '` 94, `^prf ` 27, `^rec ` 2, `^proj ` 13 — plus 40 `Program` obligations invisible to that census), all "Closed under the global context"; (b) `tensor_AFT_isos_agree` and `bal_AFT_isos_agree` DID change statement, their right-hand sides now naming `tensor_AFT_iso_of_esols`/`bal_AFT_iso_of_esols`, which is forced and semantically empty but was unrecorded — the other seven `E`-taking constants (`*_elem_iso`, `*_elem_commutes`, `*_elem_unique`, `*_iso_carries_elem`) are genuinely unchanged; (c) the first item of "**NOT DELIVERED**", "the unconditional application", IS delivered, while every other item of that list still stands — `tensor_esols_direct` is STILL refused as an index for the universe reason quoted above (N1, unchanged), and is now the SUBJECT of the file's section 10 rather than a dead end. (d) Three citations into `Instance/Grp/FreeAFT.v` in the text above drifted when that file grew by 338 lines in the same PR, and one of them inverted:, cited as where the free group's wall is measured, now opens the paragraph "WHAT THE NEW READING DOES AND DOES NOT ESCAPE" and so describes the LIFTED reading rather than the wall;, cited for the circular/non-circular distinction, still draws it but now with the non-circular side inhabited (`free_group_via_GAFT` UNCONDITIONAL and non-circular, `free_group_via_GAFT_from_adjunction` kept beside it); and the site cited as the precedent for "the only in-tree inhabitants of the hypothesis are CIRCULAR" no longer supports that sentence at either file — the tensor's own `tensor_esols_prop` and `bal_esols_prop` are unconditional and non-circular, and the circular pair `tensor_esols_from_tensor`/`bal_esols_from_tensor` is kept for comparison only.
- **Instance/Mod/TensorAFT.v** (sections 3A, 7A and 10), **Instance/Rng/AFT.v**, **Instance/Grp/FreeAFT.v** (all extended), **Test/ProbeRngAFTProp.v**: **the adjoint functor theorem APPLIED, with no hypothesis beyond the ring or the setoid — the tensor product of modules, the balanced tensor, the free rings and the free group** (the PR "algebraic carriers are sets", 2026-09-17, its payoff). Every one of `tensor_via_AFT`, `tensor_AFT_iso`, `tensor_AFT_iso_universal`, `tensor_AFT_ue`, `module_tensor_universal`, `tensor_via_AFT_maclane`, the four `bal_*` twins, `free_rng_ab_via_GAFT`, `free_ring_via_GAFT` and `free_group_via_GAFT` is now obtained from `representability_theorem` / `GAFT` with no solution-set argument, and none of them is circular. **THE ONE IDEA, and it is an arithmetic one**: index the solution set by the `Prop`-VALUED CONGRUENCES on the term model rather than by a Σ over the category's objects. `CongIdx` (`Instance/Mod/TensorAFT.v`'s `IsCongruence`, seven `Prop` fields — reflexivity, symmetry, transitivity, containment of `mt_eq`, and compatibility with `+`, with negation and with the scalar action, which is every operation of `RModObject` that takes an argument), and its siblings `BalCongIdx`, `RngCongIdx` (`Instance/Rng/AFT.v`) and `FGCongIdx` (`Instance/Grp/FreeAFT.v`). Measured, and this single inequality is the whole difference: on `CongIdx@{u u0 u1 u2 u3 u4}` the ONLY bound relating the ring's carrier `u0` to the index `u4` is `u0 ≤ u4`, so `u4 := u0` is expressible, where `tensor_esols_direct`'s Σ over `obj[RMod R]` carries `u0 < u12` — strictly above. Read as element solution sets: `tensor_esols_prop` at `ElementSolutionSet@{u2 u4 u3 u}` with `u4 ≤ u`, against `tensor_esols_direct` at `@{u12 u0 u16 u12}` with `u0 < u12`. `carrier ≤ index` is what makes the index sit where `RMod_Complete` puts its shape objects; the brief that started this work said "index bounded by the carrier", which reads backwards. **TOTALITY AND THE FACTORISATION, both measured, not assumed**: the covering is `∀ c (x : K c), { i & { t & fmap[K] t (esol_elem i) ≈ x } }` at an ARBITRARY object and an arbitrary element, and `tensor_esols_prop` discharges it by `exists (ker_idx beta), (ker_med beta)` closing by `reflexivity` — the kernel congruence `pequiv (ev β x) (ev β y)`, which IS a congruence precisely because the target module carries `cmon_prop`, so the whole construction is the field regime of the bullet above cashed in. The factorisation is ON THE NOSE, pinned at Leibniz equality: `ker_factors … = rbl_map beta v w := eq_refl`, and likewise `bal_ker_factors` and `rng_ker_factors`. **NO CIRCULARITY, checked three independent ways** (a grep of `TensorMod` over the construction's own line ranges, the fresh `QMod_gen_spanning` standing where `tensor_gen_spanning` would have, and a `Print`-based transitive closure from the seven unconditional heads that never reaches `TensorMod`, `tensor_universal_element` or `tensor_UniversalElement`): the chain consumes the TERM MODEL — `MTerm`, `mt_eq`, `tensor_med_fun`, `tensor_med_respects` — and not the tensor object, whose only appearance is in the downstream COMPARISON `tensor_AFT_iso`. The free group is the same story one layer over: `Grp_Forget_solution_set_prop` is the `Prop`-valued congruences on `FGWord X`, covering every `X → U G` through the kernel of `free_grp_extend` into `G` — a congruence because `G` carries `grp_prop` — and `free_group_via_GAFT`'s STATEMENT is byte-identical to the circular one it replaces, at SIX universes against the circular solution set's nine and FIVE against the circular application's ten. **APPLIED AT CONCRETE OBJECTS, compiled by the verifier rather than copied from the probe**: `tensor_via_AFT`, `tensor_AFT_ue`, `module_tensor_universal` and `tensor_via_AFT_maclane` at `Int_RMod`; `tensor_AFT_iso` read back against the term-model tensor as `repr_obj … ≅ TensorMod Int_RMod Int_RMod`; the same three at a SECOND ring `Ring_RMod Q_Ring` and a THIRD `Ring_RMod (Ring_op Int_Ring)`; the balanced twin at `Ab` over ℤ and ℤ^op with `bal_AFT_iso … ≅ BalTensor`; `free_ring_via_GAFT` at `bool_setoid_object` and `unit_setoid_object`, `free_rng_ab_via_GAFT` at `ring_ab Int_Ring`; the free group at `SetOne` and `TwoLetters`. **THE ONE NEW SIDE CONDITION IS `Set < carrier`, and it is attributed rather than explained away**: it is the sort of `Prop` (`Set+1`) reaching the index universe. Measured slot by slot with the ring's carrier slot named the same in both, `tensor_via_AFT` carries `Set < u2` on `RingObject@{u10 u2 u11}` where `tensor_via_AFT_of_esols` carries no bound on that slot at all. It costs nothing in practice — every application above has its carrier inferred strictly above `Set`, and `Constraint Set < <carrier>` is ACCEPTED — and at `Ab` there is NO such cost whatever, `bal_tensor_via_AFT` compiling over a ring whose carrier slot is declared `= Set`. For the free group it bites only at a NAMED object: `` `1 free_group_via_GAFT TwoLetters `` (the `bool` setoid) is refused while `SetOne` is not, and while `Grp_Forget_solution_set_prop TwoLetters` and the circular application both elaborate — so the condition belongs to the APPLICATION, not to the congruence index, which is exactly what the sharpest measurement shows on the tensor side too (`tensor_esols_prop V V'` ALONE is accepted at a `Set` carrier; only feeding it to the theorem is refused). **`Adjunction/SpanningArrow.v` was widened in the same commit**, a DELETION of its hand-written `Set`: `GAFT_from_spanning` goes from eight universes with `Set = oA` (objects AND homs in `Set`) to **thirteen with no literal `Set` and not one `Set` constraint** in the whole block, statement, `Proof using` and one-line `exact` untouched. **Nothing was lost and nothing deleted**: the nine conditionals above, and the nine CIRCULAR discharges (`tensor_esols_from_tensor`, `tensor_via_AFT_from_tensor`, `bal_esols_from_tensor`, `bal_tensor_via_AFT_from_tensor`, the three `*_solution_set_from_adjunction` and the two applications built on them), are all KEPT, each re-headed "THE CIRCULAR DISCHARGE, KEPT FOR COMPARISON; A REMOVAL CANDIDATE FOR JOHN" with the date and the PR name, and two new `*_via_GAFT_from_adjunction` names were ADDED so the circular applications survive under names that say what they are. Removal is not free and the file says so: `Test/ProbeGrpFreeAFT442.v` and the tensor probe's N6 are statements ABOUT them. **ONE FORCED STATEMENT CHANGE, with its measured reason**: the five `_mon` clauses of `Instance/Rng/AFT.v` are now stated about `free_ring_via_GAFT_from_adjunction`, because `free_ring_via_mon` is `Functor@{u Set Set u …}` while the non-circular application carries `Set < carrier` and the attempt is refused with "Cannot enforce Set = u0 because Set < u0"; widening `free_ring_via_mon` belongs to `Instance/Rng/Free.v`'s own commit. **Measured on 2026-09-17**: `Instance/Mod/TensorAFT.v` 176 constants (136 `.glob` heads by `grep -c '^def '`/`'^prf '`/`'^rec '`/`'^proj '` = 94/27/2/13, plus 40 `Program` obligations), `Instance/Rng/AFT.v` 62 (57 heads + 5 obligations), `Instance/Grp/FreeAFT.v` 50 (41 heads + 9 obligations) — all reported "Closed under the global context" by the phase's own census, 127 `Print Assumptions` for the first two and 23 for the third, zero `Axioms:` lines, and every new declaration head covered by a new gate line. **NOT DELIVERED, and these are the honest residue**: `GAFT_from_spanning` at `RMod R` is STILL refused even after the widening, with a new message re-quoted from a stripped whole-file copy — "The term \"RMod R\" has type \"Category@{u_obj u_hom u_hom}\" while it is expected to have type \"Category@{u_obj' u_hom' u_hom'}\" (universe inconsistency: Cannot enforce u_hom = u_hom' because u_hom < u_obj <= u_hom')", i.e. the widened theorem wants objects at or below homs and `RMod R` has homs strictly below objects — so no adjunction `⊣` appears in the tensor file and the two wide-pullback hypotheses remain uninhabited; `Instance/Variety/Spanning.v` needed NO adaptation and `variety_solution_set` stays CONDITIONAL; the object comparison is still `≅` and not `eq_refl`; Mac Lane's subgroup-generated family is still not BUILT, so #1309 stays open; and the tensor file's "Zero DECLARATION-HEAD COLLISIONS" scan and its `Require`-closure figure of 169 were NOT re-run for the names this work adds, which the file's own header says in terms
- **Instance/Mod/Watts/Unconditional.v**, **Instance/Mod/Watts.v**, **Functor/Representable/Additive.v**, **Instance/Mod/HomTensor.v**, **Instance/Mod/Cogenerator.v**, **Instance/Mod/WellPowered.v**, **Instance/Mod/Colimit.v** (probe `Test/ProbeWatts454.v`): Mac Lane §V.8, printed pp. 131–132 (PDF pp. 140–141; `maclane:V.8:thm-watt`, `maclane:V.8:ex2`, `maclane:V.8:ex3`), read from the printed page, issue #454 — **Watt's theorem in the book's contravariant form with NO hypothesis beyond the book's own two: every additive `T : (RMod R)^op ⟶ Ab` that takes small colimits to limits is naturally isomorphic, as an Ab-valued functor, to `Hom_R(−, A)` (`watts_theorem_unconditional`), obtained through the GENERAL adjoint functor theorem — a stated deviation from the issue's "derived from SAFT"; beside it Mac Lane's own SAFT route as closed conditionals (the text form over `Untruncate`, Exercise 3 over `Untruncate` and a cogenerator), Exercise 2(a) unconditionally over any ring, Exercise 2(b) as the book's conditional with metatheorems showing its premises classical, the additive upgrade of a representation, and the size facts about `RMod R` the SAFT route consumes**. **THE HEADLINE (Unconditional.v)** is `watts_theorem_unconditional`, not the name the issue's Verification block prints: `Print Assumptions watts_theorem` names Watts.v's CONDITIONAL SAFT form below, a Π over `U : Untruncate@{c}` at `RingObject@{c c c}`, whose "Closed" is a conditional's. `watts_theorem_unconditional T AF contT : { A : RMod R & CoHomAb (RMod_AbEnriched R) A ≅ T }` in `@Fun ((RMod R)^op) Ab`, for `R : RingObject@{a c p}`, `AF : AdditiveFunctor T` and `contT : ContinuousFunctor T` (cone-level preservation at shapes `Category@{c c c}`, so every small colimit of modules goes to a limit of groups — the book's "takes small colimits to limits", the header's reading of the `Complete` shape discipline). Its `Sets`-level core is `RModop_continuous_representable K contK : Representable K` for EVERY continuous `K : (RMod R)^op ⟶ Sets`: `Adjunction/Representability/Sets.v`'s `representability_theorem` (§V.6 Theorem 3, whose proof is `Adjunction/GAFT.v`'s comma-initial step) fed `Complete_op_of_Cocomplete (RMod_Cocomplete_via_GAFT R)` and the explicit element-wise solution set `RModop_esols`, the Eilenberg–Watts construction read in solution-set form — one fixed coproduct `ew_Cop` of copies of the free module on one generator `ew_Gen`, one copy per element of `K(ew_Gen)`; for each module `M` and `x ∈ K(M)` the colimit of ONE multi-span diagram (the new thin shape `MultiSpan`, drawn by `ms_diagram`); the compatible family lifted to one element by the new generic `sets_limit_point`; and the member `Cop/N` with `N` a `PSubmodule` kernel, so that the index sits at `Type@{c}`, the carrier universe `representability_theorem` pins. The covering map is DATA (`ew_t_at` at `eq_refl`): no choice is made and no truncation is eliminated. Additivity enters only through `watt_ab_iso`, so "plus additivity" is kept verbatim. **The deviation, and its measured reason**: SAFT at `(RMod R)^op` asks co-well-poweredness of `RMod R` at the pin, which the tree has only under `Untruncate`, and its cogenerator `RModop_Cogenerator` exists only at `RingObject@{c c c}`; the route here needs neither. **Strengths**: at `eq_refl`, `watts_theorem_unconditional_obj` (the module IS the object `representability_theorem` produces for `Ab_Forget ◯ T`), `ew_t_at` and `ew_ev_g0`, and the iso's two components ARE the representation's (`watt_ab_iso_to_at`, `watt_ab_iso_from_at`); up to ≈ the covering and the natural iso; up to ≅ the witnesses. **Universes** (`About`, the header's readbacks): the ring's three universes stay APART and neither headline carries an equation; the only side condition is `Set < c`, measured genuine (deleted from `RModop_continuous_representable`'s binder in a copy of the whole file, it is re-inferred), and the probe refuses the theorem and its ℤ witness at a `Set` carrier (N24, N25); the only equations in the module (c = u2, c = u4, on the continuity hypothesis's own instance) sit on `RModop_esols` and nine section constants. **Non-vacuity**: `watts_unconditional_CoHomAb_obj` (T := `CoHomAb A` for every ring and module, the module returned ≅ A), `RModop_hom_representable_obj` (the Sets-valued hom functor), `watts_unconditional_at_Z` (at `Int_Ring@{c p a}`, the three universes distinct), and in `Instance/Mod/Watts.v`'s section 6 the witness that is not a hom functor of `RMod R`: `watts_unconditional_coext` at `HomAbForget R A` (M ↦ Hom_Ab(U M, A), continuous because `RMod_Forget_Ab` is a LEFT adjoint, `coex_adjunction`), the module returned ≅ `CoextObj R A`, and ≅ Exercise 2(a)'s `HomZL R_R A` by `watts_unconditional_coext_2a`, at `RingObject@{c c c}`, which `CoextObj` imposes. **MAC LANE'S ROUTE, CONDITIONAL (Watts.v).** `watts_theorem T AF contT U`, the issue's Verification name: `continuous_Set_functor_representable` (#453's SAFT corollary) at C := `(RMod R)^op` over `RMod_Cocomplete_via_GAFT`, `RModop_Cogenerator` and `RMod_CoWellPowered_untruncate U`, then `watt_ab_repr`; `U : Untruncate@{c}` is spent once, in that co-well-poweredness, and the ring is at `RingObject@{c c c}`, forced at `RModop_Cogenerator` (the probe's N17, peeled to N18 and to `Ring_RMod` at N11). `watts_theorem_adjoint` is the book's first step (`SAFT_wellpowered` at T itself) and `watts_theorem_Fz` its last, C ≅ F ℤ in `RMod R` with ℤ read as `FreeAb SetsOne`, through `ab_hom_Z_iso : HomAb Ab_AbEnriched (FreeAb SetsOne) ≅ Id[Ab]` (Ab(ℤ, X) ≅ X natural in X, no hypothesis). The text form here is STRICTLY WEAKER than the headline: `watts_theorem_via_unconditional` has `watts_theorem`'s statement and is `watts_theorem_unconditional` with `U` carried and not consumed; the SAFT route is kept for the issue's Reviewer line, not for strength, and Unconditional.v's header lists which of Watts.v's constants the headline subsumes (not `watts_theorem_adjoint`, `watts_theorem_Fz`, the two readbacks or the Exercise 3 family). Exercise 3, covariant: `watts_ex3 T AF contT G U` over `Untruncate` and a hypothesis `G : Cogenerator (RMod R)`, the ring's universes apart; `watts_ex3_QZ` takes `G` from Exercise 2(b), so that its premises are the book's "Q/Z … is known to be an injective cogenerator of Ab", taken as hypotheses, together with `U`, and `watts_ex3_QZ_cogenerator` shows at `eq_refl` that the injectivity half is carried and not consumed. Witnesses, each inheriting `U`: `watts_ex3_forget` (T := `RMod_Forget_Ab R`, module ≅ `Ring_RMod R`, refused at `eq_refl`, N16) and `watts_theorem_coext` (module ≅ `CoextObj R A`, and ≅ `HomZL R_R A`). `watt_at_forget : HomAb (RMod_AbEnriched R) (Ring_RMod R) ≅ RMod_Forget_Ab R` needs nothing: Exercise 3's conclusion at the forgetful functor, reached by the additive upgrade without SAFT. **THE ADDITIVE UPGRADE (Additive.v).** `HomAb AC A0 : C ⟶ Ab`, the Ab-valued hom functor of a locally propositional Ab-enriched C (values `Adjunction/Additive.v`'s `hom_ab`, arrow action at `eq_refl`), and `CoHomAb`, `HomAb` at `C^op` through `AbEnriched_op` and `LocallyPropositional_op`; both additive, both continuous (`HomAb_continuous`, `CoHomAb_continuous`, one definition each, consumed by both Watts files); and `watt_ab_iso AC T AF Rp : HomAb AC (repr_obj Rp) ≅ T` in `@Fun C Ab` for T additive and Rp a representation of `Ab_Forget ◯ T`, stated once for an arbitrary C so that the covariant and contravariant forms are two instantiations of one constant. `LocallyPropositional_op` is a `Definition`, so at `C^op` it is passed explicitly (N1 pins the resolution refusal). **EXERCISE 2(a) (HomTensor.v), unconditional, over any ring, no commutativity.** `tensor_homZ_adjunction B : TensorBF B ⊣ HomZLF B` (hom_Z(B ⊗_R A, G) ≅ hom_R(A, hom_Z(B, G)), hom_Z(B, G) with the left action induced by B's right one), its mirror `homZ_tensor_adjunction A` for the right-module leg, the outer terms `hom_hom_swap`/`hom_hom_swap_iso` (argument exchange at `eq_refl`) with `hom_hom_swap_natural` (natural in G), and `hom_tensor_adjunction`, the issue's name, carried to #449's AFT tensor along `bal_AFT_iso` by `adjunction_along_left_iso`. At B := R: `ring_unit_iso` (R ⊗_R A ≅ A, both components at `eq_refl`), `homzl_ring_adjunction` and `homzl_coext_iso` against `Instance/Mod/Coextension.v`'s `coex_adjunction`, groups and actions agreeing at `eq_refl`; the record identification `HomZL R_R G = CoextObj R G` at `eq_refl` is refused (N2), and still refused with every `Qed` of both files flipped. **EXERCISE 2(b) (Cogenerator.v), the book's conditional.** `QZ_injective_cogenerator R HI HC : Injective (RMod R) (CoextObj R QZ) * Cogenerator (RMod R)` from `HI : Injective Ab QZ` and `HC : QZ_family_cogenerates`, at `R : RingObject@{r r r}` only (as are `_via_2a`, `_ML` and Watts.v's `watts_ex3_QZ`, by `Check` with universes printed, 2026-09-24): `CoextObj`'s identification of the ring's three universes, which Cogenerator.v's header records for the theorem and `_via_2a` and Watts.v's for `watts_ex3_QZ`; through the unconditional transfers `right_adjoint_injective` and `right_adjoint_cogenerator` along `coex_adjunction`; `_via_2a` is Mac Lane's route through Exercise 2(a) at B := R; `QZ_injective_cogenerator_ML` is the exercise at the book's own ≠ definition of a cogenerator (`ML_cogenerates`, §V.7); `QZ_cogen_prod` shows SAFT's `cogen_prod` accepts the conditional cogenerator at `RMod_Complete R`. **The premises are classical, not false**: the closed metatheorems `QZ_cogenerates_Ab_DNE` (`HC` ⇒ ∀ P, ¬¬P → P), `QZ_injective_WLEM` (`HI` ⇒ the informative ∀ P, (¬P) + (¬¬P)), `cogenerator_stable_DNE` (with `_Z`: any cogenerating family of `Ab` whose members' equality is ¬¬-stable yields DNE), `coext_QZ_cogenerates_DNE` and `coext_QZ_injective_WLEM` (the exercise's conclusion at R = ℤ costs the same) refute nothing; they show the premises unprovable without an axiom unless double-negation elimination (respectively the informative weak excluded middle) is. What is constructive about ℚ/ℤ: `QZ_divisible`, `zchar_determined` and `QZ_extends_along_mul`, Baer's condition at the ideals of ℤ. Also new: `Ab_to_ZMod : Ab ⟶ RMod Int_Ring`, each group with its own `zsmul` action (readbacks at `eq_refl`), and the unconditional but LARGE `RMod_Cogenerator_large`, refused by `cogen_prod` (N4). **RMOD SIZE FACTS (WellPowered.v) AND COCOMPLETENESS (Colimit.v).** `RMod R` is well-powered ONE UNIVERSE UP with no hypothesis (`RMod_WellPoweredAt_up`, `Instance/Mod/Spanning.v`'s round trip) and co-well-powered likewise (`RMod_CoWellPoweredAt_up`, `RMod_CoWellPowered_up`, indexed by codomain, map and `Prop`-level surjectivity, at `c < w` and `a <= w`), both refused at the pin (N6, N8); at the pin under `Untruncate` (`RMod_WellPowered_untruncate`, `RMod_CoWellPowered_untruncate`, over the `Prop`-membership `PSubmodule`, at `Set < c`), the refusals without it pinned as N9 (a sort) and N10 (an elimination). `RMod_ring_separates`, `RMod_Generator` and `RModop_Cogenerator` are unconditional at `RingObject@{c c c}` only, forced by `Instance/Mod.v`'s `Ring_RMod` as typed (N11). `RMod_Cocomplete_via_GAFT : Cocomplete@{c so c o} (RMod R)` on #450's template (`Prop`-valued cocone congruences on `Instance/Mod/Free.v`'s `FVTerm`), with the finite bridges and non-vacuity at ℤ above `Set` (`RMod_coproduct_via_GAFT_nontrivial`); its `Set < c` is `GAFT`'s (N14; `Structure/Complete.v`'s size note carries an ADDENDUM (#454)), and the comparisons with the `Set`-pinned zero module and direct sum are refused on universes (`Instance/Mod/Colimit.v`'s header; N15 pins the first). **Measured.** ONE new block of the `print-assumptions` gate, 469 names by counting its `Print Assumptions` lines (113 of them `Program` obligations, by the substring `_obligation_`; per module 38 Additive.v, 159 HomTensor.v, 100 Cogenerator.v, 36 WellPowered.v, 39 Colimit.v, 27 Watts.v, 70 Unconditional.v), so `grep -c 'Print Assumptions' Makefile` goes 9541 → 10010, and `make print-assumptions` passes with every one "Closed under the global context"; the block carries every constant `Print Module` lists for the seven modules (record constructors aside) including the four eliminators `MSObj_rect`, `MSObj_ind`, `MSObj_rec` and `MSObj_sind` of Unconditional.v's inductive `MSObj` (compared name by name against a `Print Module` of the seven modules). `Untruncate` is a hypothesis (a Π-type), not an axiom (docs/AXIOMS.md). `rocqchk -o` over `Instance/Mod/Watts.v`'s closure reports "Modules were successfully checked". The development files add no `make todo` hit; the probe adds 35, exactly its lines opening with the refutation keyword (2551 → 2586, the Makefile's pipeline counted by `wc -l`, measured 2026-09-24). `Test/ProbeWatts454.v` carries those 35 refutations — the name-absence instrument and N1–N34, N28–N30 pinning the other side of the equations N7, N19 and N20 pin and N31–N34 Watts.v's refusals of `HomAbForget_continuous`'s body in that header's own form, classified in its header by the error each one's copy of the whole file stops with once the keyword is stripped — beside positive controls, under the seven targets' full import list, with guard `Check`s naming the targets' constants. Coq 8.19.2 and 8.20.1: a 250-file dependency closure containing all seven development files, built by `coq_makefile` with the nix-store toolchains and Equations 1.3, make rc=0 on each, with the 132 constants of Additive.v, Watts.v and Unconditional.v closed on both and the same universe-equation profile as on 9.1 (Unconditional.v's header); the probe, at its 35 refutations, compiles on both over that closure, each stripped copy refused on both inside the stripped command (its header). And the flake's `nix build .#category-theory_8_19`/`_8_20` of the committed revision passes (rc=0, all 1063 files compiled on each; the PR records the revision). **NOT DELIVERED, AND DEVIATIONS.** Exercise 3 without its hypotheses, and the SAFT route to the text form without `Untruncate`, are re-homed as their own issue, #1326, per the maintainer's decision. The path is `Instance/Mod/`, the tree's module directory, not the issue's `Instance/Module/` (a deviation in path only). The unconditional theorem is through GAFT, not SAFT; the SAFT form the Reviewer line names is delivered only as a conditional. Exercise 3, the covariant form, has no unconditional form in tree: it is delivered only over `Untruncate` and a cogenerator of `RMod R` (for `watts_ex3_QZ`, the book's ℚ/ℤ premises), the hypotheses the SAFT route consumes; the only unconditional cogenerator in tree, `RMod_Cogenerator_large`, is large, and no route other than SAFT is attempted. Nothing in tree proves either hypothesis NECESSARY: the one condition PROVED classical is Exercise 2(b)'s ℚ/ℤ premise (the DNE/WLEM metatheorems), and `RMod R` well-powered at the pin without `Untruncate` is neither built nor refuted. Exercise 2(b) is only the book's conditional: ℚ/ℤ is not proved injective or cogenerating in `Ab`, by the metatheorems it cannot be without an axiom, and no instance under `IEM` or `Untruncate` is built (Baer's criterion needs Zorn); no small unconditional cogenerator of `RMod R` or `Ab`. Exercise 2(a) is not natural in the FIXED module (no bifunctor `ModR R ∏ RMod R ⟶ Ab`, no `ParametrizedAdjunction`), and its isomorphisms are bijections of hom-setoids, not of hom-groups. The representing module is not identified with T(R) carrying the Eilenberg–Watts action; no naturality in T; no Eilenberg–Watts tensor form; the additivity premise is redundant in principle (a continuous T preserves finite products) and is not eliminated. `RMod R` well- or co-well-powered at the pin without a hypothesis: neither built nor refuted. No equivalence `Ab ≃ RMod Int_Ring`. No `Instance` is registered. **Surfaced for the maintainer, not moved or removed**: `sets_limit_point`, a general fact about `Sets`, and `MultiSpan`/`MSObj`/`MSHom`/`ms_diagram`, a general thin shape, sit in `Instance/Mod/Watts/Unconditional.v` and are relocation candidates, as is `Ab_to_ZMod : Ab ⟶ RMod Int_Ring`, a general functor, in the Exercise 2(b) file `Instance/Mod/Cogenerator.v`, which seven headers now cite for it; nine `Require`s of `Instance/Mod/Watts.v` (`Lib.Setoid.Propositional`, `Functor.Hom`, `Functor.Hom.Limit`, `Structure.Limit`, `Structure.Complete`, `Structure.WellPowered`, `Structure.Preadditive`, `Instance.Sets.Powerset`, `Instance.Rng`) and its `Generalizable All Variables` are unused: each `Require` was measured removable alone, and all ten lines removed together from a copy of the committed file compile (`coqc` rc=0, measured 2026-09-24; the `About` output of the file's 27 constants, normalised for qualification and whitespace, is unchanged). They are left in place for the maintainer; removing them would also shorten the probe's import list, which copies Watts.v's. **Corrections in place**, to prose #454 falsified or found false during it (each marked "an earlier revision said …", "CORRECTION, #454" or "since #454", nothing deleted), in the nineteen existing `.v` files #454 edits, every edit inside a comment (their comment-stripped text compared with master's, file by file, identical for all nineteen): the `Ab_to_ZMod` absences in `Instance/Ab/Free.v`, `Instance/Mod/BaseChange.v`, `Instance/Mod/Coextension.v`, `Instance/Mod/Closed.v`, `Instance/Mod/Monoidal.v`, `Instance/Mod/Quotient.v` and `Instance/Mod/Tensor.v`; the RMod well-poweredness gaps in `Structure/WellPowered.v`, `Structure/WellPowered/Counterexample.v` and `Instance/Grp/WellPowered.v`; `Instance/Mod/Limit.v` (colimits); `Instance/Ab/Limit.v` (its GAFT hypotheses consumed); `Instance/Sets/Cogenerator.v` (cogenerators of other categories); `Adjunction/Representability/Sets.v` (witnesses beyond `Sets`); `Adjunction/SAFT/Characterization.v` and its `Examples.v` (the witness list); `Adjunction/Additive.v` (the one-variable Ab-valued hom functors `HomAb`/`CoHomAb` now exist, `adj_hom_ab_iso` still not restated between them); `Instance/Mod/Representable.v` (the Ab-level reading of its representation, `watt_ab_iso`, instantiated at the forgetful functor by Watts.v's `watt_at_forget`); `Instance/Mod/Coextension.v`'s two further notes, the reason it gave for not applying `adj_hom_ab_iso` (an absent `AbEnriched (RMod R)`, false BEFORE #454, `Instance/Mod/Bimodule.v`'s `RMod_AbEnriched` supplying it) and Ab(ℤ, X) ≅ X by `ab_hom_Z_iso` at `FreeAb SetsOne`, with its bullet on `Int_Ring`'s group standing; `Structure/Complete.v`'s ADDENDUM (#454); in this file, beyond the bullets that repeat those, the bullets of `Construction/Reflective/Colimit.v` (the `_Cocomplete_via_GAFT` count), `Adjunction/SAFT/InitialObject.v` (cogenerators of other categories), `Structure/Projective.v` (Baer's condition at the ideals of ℤ), `Instance/Sets/Cocomplete.v` (GAFT cocompleteness witnesses) and `Instance/Vect/TensorAlgebra.v` (a `Cocartesian (RMod R)` absence false BEFORE #454, by `Instance/Mod/Coproduct.v`'s `RMod_Cocartesian`); and the docs/INHABITATION.md rows that repeated them.
- **Instance/Discrete.v**: Discrete categories, and Mac Lane §I.2's "a discrete category is determined by its set of objects" — `DiscreteCat A` (objects `A`, morphisms equality proofs) with `DiscreteCat_Discrete` one way, and the reconstruction the other in Instance/Discrete/Reconstruct.v: `Discrete_iso : Discrete C → DiscreteRigid C → C ≅[StrictCat] DiscreteCat obj[C]`, with the `Cat` reading alongside. `DiscreteRigid` is the extra hypothesis and it is stated INTRINSICALLY — the comparison functor is faithful (`DiscreteRigid_Faithful`) — with UIP on objects and decidable object equality as sufficient conditions (`DiscreteRigid_UIP`, `DiscreteRigid_dec`, the latter by Hedberg, axiom-free). It is not an artifact of the proof: `DiscreteCat`'s homset is strict `eq` on equality proofs, so a functor into it must identify parallel proofs. The file also addresses `Structure/Discrete.v`'s long-standing "Equality is too much here" caveat with the iso-robust `DiscreteUpToIso` (subsingleton homs, all arrows invertible), and then shows the two notions are genuinely independent rather than merely differently phrased: `Discrete_DiscreteUpToIso_forces_UIP` proves that `∀ C, Discrete C → DiscreteUpToIso C` would entail UIP for *every* type, and `Indiscrete_bool_Discrete_absurd` with `Indiscrete_DiscreteUpToIso` exhibits a category that is discrete-up-to-iso but not `Discrete`. Functor categories OVER discrete shapes are Instance/Fun/Discrete.v (Mac Lane §II.4): transformations over a discrete domain are bare families with naturality free (`Discrete_Transform`, with the hom-setoid isomorphism `Discrete_hom_iso : (F ⟹ G) ≅[Sets] (∀ a, F a ~> G a)`), `[DiscreteCat A, B] ≅[Cat] PiCat (fun _ => B)` for ANY index type, the finite power `Fun_Discrete_power : [DiscreteCat (Fin.t n), B] ≅ Pow B n` by Fin-peeling entirely at PiCat level (`Pow` carries a trailing terminal factor as its nullary case — n B-factors ∏ `1` — with the literal `B ∏ B` shape exhibited at n = 2 via `prod_one_r`), the power-set computation `Fun_Discrete_bool_subsets` (functors into `DiscreteCat bool` up to natural iso = characteristic functions `X → bool`, the constructive decidable-subsets reading), the axiom-free Cantor diagonal `cantor_predicates`/`cantor_bool` with `Fun_Discrete_no_surjection` connecting it to the correspondence — the size half of Mac Lane's remark, with the universe-sized half rendered structurally by the library's universe discipline — and Seven Sketches 3.53's morphism half `One_hom_iso` (hom-setoid iso in Sets, any target, instantiated at Sets; the object half `[1, C] ≅ C` is Theory/Shapes.v's `One_Fun_iso`, cited not rebuilt)
- **Instance/Cat.v**: Category of categories
- **Instance/Fun.v**: Functor categories [C, D]
- **Instance/Fun/Eval.v** (+ **Instance/Fun/Eval/Cat.v**): Mac Lane §V.3's opening construction (book p. 115, `maclane:V.3:construction1`) + Awodey §8.1's `ev_C` (`awodey:8.1:construction-evaluation-functor`) — **the evaluation functor `Eval p : [P, X] ⟶ X` in its light home, the two-variable evaluation `EvalBi : [P, X] ∏ P ⟶ X` with `Eval p` its partial application, and evaluation as a functor `EvalFunctor : P ⟶ [[P, X], X]`**. **Stale premises**: the issue's "the fixed-object evaluation functor is nowhere materialised" is FALSE — Adjunction/Diagonal/Connected.v's `EvalAt` IS it (recorded NEW in that file's bullet here, now corrected in place), and Functor/Hom/Yoneda/Natural.v `YoEvalAt` and `YoEval` (two-variable, at Sets, the other arrow order) and Theory/Shapes.v `One_Eval` are its special cases; the issue's hit list is wrong in two of four entries (Kan/Extension.v is a comment, CMon.v is `CMon_Forget`); the transpose `[J ∏ P, X] ≅ [J, [P, X]]` exists as Instance/Cat/Exponential.v's `Cat_exp_prod_l` (cited as `eval_transpose`, not rebuilt); Functor/Bifunctor/Partial.v's `Partial_l`/`Partial_r` exist. `Eval` is annotated `@{jo jh co ch}` exactly as `EvalAt` and carries `EvalAt`'s constraint set (`jh < u`, `jo <= jh`, `jh <= u0`, `co <= u0`, `jh = ch`); its two data fields agree with `EvalAt`'s at `eq_refl` under that annotation (probe), the RECORDS do not convert (N1: opaque `Program` law fields). `EvalBi` uses `Cat_Closed`'s arrow order `fmap[K] f ∘ transform[σ] p`, so the satellite's `CatEval := @eval Cat Cat_Cartesian Cat_Closed P X` — Instance/Cat/Cartesian/Closed.v's unnamed evaluation, named — agrees with it in both fields at `eq_refl` (`CatEval_obj`, `CatEval_map`); it is built directly because THE `Cat` ROUTE COLLAPSES UNIVERSES: the five `CatEval*` constants carry `u0 = u1`, `u0 = u5`, `u0 = u6` (P's and X's object and hom levels one universe), while `EvalBi` restated at the same `P X : Cat` binder carries NO equation and at arbitrary categories only Instance/Fun.v's `jh = ch` — the collapse is `Cat_Closed`'s, not the binder's, measured by `About`, not a refutation (the route typechecks). **Partial application agrees** on objects at `eq_refl` (`eval_partial_obj`) and on arrows at `≈` ONLY (`eval_partial_map`; `Partial_l`'s action is `bimap s id`, carrying the `fmap id` the functor law removes — N2, the reason `yo_eval_at_map` is at `≈` too), packaged as `eval_partial_iso : Eval p ≅ Partial_l EvalBi p` in `[[P, X], X]` with identity components; the other partial is the functor itself (`eval_partial_r_obj` at `eq_refl`, `eval_partial_r_map` at `≈`, N3). **Naturality in the object**: `Eval_nat f : Eval p ⟹ Eval q` with component `fmap[H] f`, `EvalFunctor` with `EvalFunctor_obj`/`_map` at `eq_refl` — the GENERAL form is new, the construction appears at one object of one category as Functor/Representable/Functorial.v's `wit_ev_tau` over the delooping of (ℕ, +) and Sets, whose component converts with `Eval_nat`'s at `eq_refl` (probe), and `EvalFunctor` has no precedent (a tree-wide sweep for a functor into `[[_, _], _]` returns only this file); an earlier revision said "genuinely absent before". In the probe (the identified constants sit in Adjunction/, Functor/Hom/Yoneda/ and Theory/Lawvere/, layers no Instance/Fun/ file requires): `YoEvalAt` and `One_Eval` in BOTH fields at `eq_refl` (an earlier revision recorded `One_Eval` on objects only), and Awodey's `ev1` re-expressed as `p424_ev1_via_Eval := Eval (law_of_nat 1) ◯ Incl`, both data fields at `eq_refl` — the Awodey checkbox asks that `ev1` itself be re-expressed "rather than left as a parallel hand-written definition", so it is NOT met (`ev1` untouched; the twin shows the re-expression is exact; the landed-file edit is surfaced; an earlier revision called the box met); the issue's pinned verification name `Eval_partial_application` is not declared, the agreement being `eval_partial_obj`/`_map`/`_iso`. **The `EvalAt` decision is surfaced, not taken**: aliasing edits Connected.v under its exact annotation and re-checks `EvalAt_retracts`, `colimit_is_EvalAt` and `Test/ProbeConnected378.v`; removal deletes working code; both constants remain. **The name `Eval` and the vernacular**: `Eval compute in …` and `Check Eval` still work, but `Definition f p := Eval p.` at a body head is a PARSE error (Coq reads `Eval <red_expr> in <term>`); `(Eval p)`, `@Eval P X p` and tactic-mode `exact (Eval p)` work — consumers must parenthesise; renaming is surfaced for John. **Universes**: every Eval.v constant — the 17 `Program` obligations included — carries exactly one equation, `jh = ch` (`Fun` identifies source and target hom levels); no word-bounded `Set`, no `JMeq`/`EqdepFacts`/`eq_rect_r`; exactly four constants carry `prod_rect` bounds (`eval_partial_iso`, `EvalFunctor`, `EvalFunctor_obj`, `EvalFunctor_map`) and the carrier is NOT the passage through `[[P, X], X]`, which carries none (an earlier revision said it was; the donor is not isolated); the satellite's `Cat`-routed constants carry the collapse above and `Basics.compose`/`eq_rect`/`prod_rect` bounds, `eval_transpose@{u u0}` at `Cat`'s single level with no equation. **34 constants closed under the global context, zero `Axioms:` lines** — 17 `.glob` heads (Eval.v 9 `def` + 2 `prf`; Cat.v 5 `def` + 1 `prf`) plus Eval.v's 17 `Program` obligations the glob cannot see; the gate carries the 17 heads, fully qualified (an earlier revision said "17/17"); no `Defined`, eight `Qed` tokens in Eval.v and one in Cat.v; closure 19 excluding self for Eval.v (Instance/Fun.v 5 at the margin, Functor/Bifunctor/Partial.v 1, seven `Require`s 0) and 33 for Cat.v (Instance/Cat/Exponential.v 8, Eval.v 1, fourteen 0; Instance/Fun/Discrete.v and Action.v are the precedents for requiring Instance/Cat/*); zero name collisions. `Test/ProbeFunEval424.v` carries the two files' `Require` lists plus the identified constants' and **4 refutation commands = 1 instrument + 3 CONVERSION negatives** (`Eval p = EvalAt p` "cannot unify" beside the two `eq_refl` field controls; `fmap[Eval p] s = fmap[Partial_l EvalBi p] s` beside the object control, `eval_partial_map` and `eval_partial_iso`; the `Partial_r` twin beside `eval_partial_r_obj`/`_map`) — each stripped one at a time in a copy of the whole file; guard coverage 23 identifier tokens inside / 17 also named outside (comments stripped) with six exhaustive exceptions; rename-simulated 7/7 (`Eval`, `EvalBi`, `EvalAt`, `Partial_l`, `Partial_r`, `YoEvalAt`, `CatEval`; module paths excluded), every first break on a positive line; `make todo` grows by the probe's 4 refutation commands only (2218 → 2222), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: preservation or creation of limits by evaluation (#425/#426); an alias or removal of `EvalAt`/`YoEvalAt`/`One_Eval` (surfaced); replacing `ev1`'s body (the Awodey box, surfaced); a `Cat`-level two-variable evaluation without the collapse; a rename of `Eval` (surfaced); no edit to Connected.v, Yoneda/Natural.v, Shapes.v, Lawvere/Sets.v, Functorial.v or Bifunctor/Partial.v.
- **Instance/Fun/Limit.v**: Mac Lane §V.3 Theorem 1 and Corollary 1 (book pp. 115-116, `maclane:V.3:thm1`, `maclane:V.3:cor1`) + Awodey §8.5 Proposition 8.7 and its Corollary + Riehl §3.4 Proposition 3.4.9 — **limits in a functor category are computed pointwise: the pointwise limit functor, its limiting cone, evaluation preserving every pointwise limit, `Complete X → Complete [P, X]`, and `Complete [C^op, Sets]`**. Over `D : J ⟶ [P, X]` and `L : ∀ p, Limit (Eval p ◯ D)` (#424's `Eval`): `plim_obj`/`plim_leg` with joint monicity of the legs (`plim_jointly_monic`); the arrow part `plim_map f` as the mediator of the cone `plim_cone_at f` (legs `fmap[D j] f ∘ plim_leg p j`), with Mac Lane's uniqueness `plim_map_unique`; **`PointwiseLimit : P ⟶ X`** (each functor law one joint-monicity argument), `plim_nat`, **`PointwiseLimitCone : Cone D`**, and **`PointwiseIsLimitCone`** — uniqueness in `[P, X]` IS pointwise uniqueness, an equation of transformations reducing componentwise — packaged as the issue's pinned **`Functor_Category_pointwise_limit : Limit D`**; seven readbacks at `eq_refl` (the diagram at p, the object action, the leg components, the image cone's apex and legs, the mediator's components, the packaged cone). **Preservation**: `Eval_pointwise_IsLimitCone p` (the image of the pointwise cone under `Eval p` is the chosen limit cone in apex and legs, limiting with no coercion) and **`Eval_PreservesLimitCone p : PreservesLimitCone D (Eval p)`** for an ARBITRARY limiting cone over a diagram WHOSE EVALUATIONS HAVE LIMITS — the hypothesis `L` stays; a limit in `[P, X]` that is not pointwise is not covered — by `limitcone_transport` along `FCone_iso` of `limitcone_iso` (the construction alone yields only the canonical cone), `Eval_PreservesLimit`. **Corollary 1**: `Fun_HasLimitsOfShape` in the unfolded form (it ascribes at Adjunction/Diagonal/Limit.v's `HasLimitsOfShape` downstream — probe — so the file does not pay that import, +25 files over its shipped `Require`s), the pinned **`Functor_Category_Complete : Complete X → Complete [P, X]`**, `Eval_ContinuousFunctor`, `Eval_PreservesAllLimits` — all four ANNOTATED AT THE DEFINITION because written bare each minimizes P's object universe onto its hom universe (probe N2: the bare body refused under `Constraint jo < jh`, "Cannot enforce jh = jo", beside the annotated form accepted; a section-level `Universe` declaration does not lift it). **Presheaves** (Awodey's Corollary, Work item 3, its "once the Sets witness lands" blocker long discharged): `Presheaf_Complete C : Complete [C^op, Sets]`, `Presheaf_Eval_PreservesAllLimits`, `Fun_Sets_Complete`, from `Sets_Complete`. **Stale premises**: "no `Terminal` on `[C, D]`" is false (Instance/Fun/Terminal.v `Functor_Category_Terminal`, #339; `Fun_HasIndexedProducts`, `Functor_Category_Cartesian`, `Fun_HasPullbacks` too) — the GENERAL shape and any preservation statement were the absences; "no `Complete` inhabitant" false: a declaration sweep of the non-Test/ tree finds at least thirteen constants concluding `@Complete`, unconditional witnesses and premise-carrying transformers together (`Sets_Complete`, `ConeSet_Complete`, `Grp_Complete`, `Arrow_Complete`, `Comma_Complete`, `PiCat_Complete`, `EM_Complete`, `reflective_Complete`, …); "the evaluation functors do not exist" false (`EvalAt`; `Eval`, whose marginal closure cost here is 2 against 103 through `EvalAt`, both over the shipped `Require` list); Construction/Product/Limit.v's `PiCat_Complete` has term for term the same shape (an analogy, not an instance — `PiCat` is not `[DiscreteCat I, X]`). Work item 4's prose repointed LINE-NEUTRALLY at five files (Instance/Fun.v, keeping the colimit half attributed to the nLab; Instance/Fun/Cartesian.v; Structure/Complete.v — the monadic clause's words are `EM_Complete`'s and untouched; Structure/Cartesian/Product.v; and Instance/Fun/Terminal.v, whose NOT DELIVERED list denied the general theorem and any preservation statement — corrected in place to the measured statements). **Universes**: the 22 constants of the section carry exactly `u0 = u2`, `u0 = u4`, `u0 = u6`, `u2 = u4` — J's hom = P's hom = X's hom = `[P, X]`'s hom (`Fun`'s identification plus `Limit`'s shape-hom = ambient-hom binder applied twice; probe N3 pins the latter: at a shape with homs strictly below X's, the diagram and its `Cone` are accepted and its `Limit` refused, "Cannot enforce ch = ih") with the three object universes FREE; the four annotated corollaries carry only `jh = ch`; the three presheaf constants sit at a category with one level for objects, homs and proofs (the two `Complete` ones at `C : Category@{u0 u0 u0}` concluding `Complete@{u0 u0 u0 u}`, inherited from `Sets_Complete@{u u0} : Complete@{u u u u0}`); no word-bounded `Set`, no `JMeq`/`EqdepFacts`/`eq_rect_r`. **40 constants closed under the global context, zero `Axioms:` lines** — 29 `.glob` heads (24 `def`, 5 `prf`) plus 11 `Program` obligations the glob cannot see; the gate carries the 29 heads, fully qualified; one `Defined` (`PointwiseIsLimitCone`, LOAD-BEARING: flipped to `Qed` the file compiles but the probe's `p425_mediator` readback is refused), sixteen `Qed` tokens; closure 37 excluding self (Instance/Sets/Complete.v 12 at the margin, Instance/Fun/Eval.v 2, eleven `Require`s 0); zero name collisions. `Test/ProbeFunLimit425.v` mirrors the `Require` list plus Adjunction/Diagonal/Limit.v and carries **4 refutation commands = 1 instrument + N1 CONVERSION + N2 UNIVERSE + N3 UNIVERSE** — each stripped one at a time in a copy of the whole file, each beside its accepted controls; `p425_hlos`/`p425_LimitFunctor` (the `HasLimitsOfShape` ascription and the limit functor on `[P, X]`) as positive controls; guard coverage 19 identifier tokens inside / 16 also named outside (comments stripped) with three exhaustive exceptions; rename-simulated 6/6 (`FCone`, `PointwiseLimitCone`, `limit_cone`, `Limit`, `Eval`, `Functor_Category_pointwise_limit`; module paths excluded), every first break on a positive line; `make todo` grows by the probe's 4 refutation commands only (2222 → 2226), so the issue's "adds no new hits" box is not met as written (disclosed). NOT delivered: the colimit dual and evaluation preserving colimits (#715, superseding Work item 2's `Cocomplete` clause — not one colimit line); creation of limits (#426); a `HasLimitsOfShape`-typed statement inside the file; a comparison with `Fun_iprod` or the cartesian products at their shapes (so Terminal.v's products are NOT tied to the preservation statement); no edit to Instance/Fun/Eval.v, Structure/Limit.v, Structure/Limit/Preservation.v or Instance/Sets/Complete.v.
- **Instance/Fun/Creation.v**: Mac Lane §V.3 Theorem 2 (book p. 116, `maclane:V.3:thm2`) + Riehl §3.4 Proposition 3.4.9 (`riehl:3.4:prop9`; the joint-creation clause is the analogue of her Exercise 3.4.v, which concerns the projections of a product of categories and is `PiCat_JointlyCreateLimit`'s) — **pointwise limits as CREATION: UNDER THE HYPOTHESIS that the pointwise limits exist, restriction along the object-discrete inclusion `[P, X] ⟶ [|P|, X]` creates limits; unconditionally, the evaluation functors jointly create them — both as transports of #425's Theorem 1, not parallel proofs**. `ObjCat P := DiscreteCat (obj P)` at P's own levels, `DiscInc : ObjCat ⟶ P` through Structure/Limit/Comparison.v's annotated `DiscreteCat_Functor'`, **`Res : [P, X] ⟶ [ObjCat, X] := Induced DiscInc`** (Theory/Kan/Extension.v), five `eq_refl` readbacks (`res_obj`, `res_obj_at`, `res_map`, `eval_res_obj`/`eval_res_map`: evaluating after restricting IS evaluating, in both fields). **Reflection** (the new content, unconditional — #425 has only the converse, which takes `L`): for `M : Cone D` with every `FCone (Eval p) M` limiting, `ptw_of_cone`, `ptw_apex_readback`/`ptw_leg_readback` at `eq_refl`, `ptw_map_is_fmap` (the mediator between adjacent pointwise limits is M's own arrow action, by `plim_map_unique`), `ptw_apex_iso` (identity components), `ptw_cone_iso`, **`Fun_pointwise_reflect : IsLimitCone M`** = `limitcone_transport` of `PointwiseIsLimitCone` along it. **Joint creation** (the analogue of Riehl Ex 3.4.v): `ev_lift` (#425's pointwise cone from the given family), `ev_lift_apex`/`ev_lift_leg` at `eq_refl`, `ev_lift_over`, **`Fun_Eval_JointlyCreateLimit : JointlyCreateLimit D (fun p => Eval p)`** (the cone RECORDS do not convert — probe N1). **Creation along `Res`** UNDER THE HYPOTHESIS `L : ∀ p, Limit (Eval p ◯ D)`: `res_comp_cone`, `res_ump_nat`, `Res_preserves`, `res_coneiso_at`, **`Res_CreatesLimit L : CreatesLimit D Res`** and its alias under the issue's name `discrete_inclusion_creates_limits` (ANNOTATED at the definition: bare over `{J P X : Category}` it minimized P's object universe onto its hom universe and J's hom onto it, `{J : Category@{u2 u3 u3}} {P : Category@{u3 u3 u3}}`), `res_limit`, and `creation_apex`/`creation_leg` at `eq_refl`: the limit `creates_limit_lift` derives IS `Functor_Category_pointwise_limit`. The hypothesis stays because `creates_lift` must read the components of ONE limiting cone in `[|P|, X]` as limiting cones in X, whose constructive proof patches the apex family at one index and needs decidable equality on `obj P` (Construction/Product/Limit.v made the same call: `Fst_`/`Snd_PreservesLimitCone` for the binary product, only `pi_reflect`/`PiCat_JointlyCreateLimit` for `PiCat`); a hypothesis-free version over `ObjDecEq P` is a follow-on. **Corollaries** through Structure/Limit/Creation.v's general theorems: `Res_CreatesAllLimits`, `Res_Complete : Complete [P, X]` (`creates_limits_Complete` with `Functor_Category_Complete` at the discrete shape), `Res_continuous`. **Strictness**: `strict_self := self_lift M`; Riehl's STRICT creation is NOT delivered — it would need an `eq` between functor records `DiscreteCat (obj P) ⟶ X` (probe N3; N2: a restricted functor is not the discrete functor on its own object map). **Stale premises**: "the creation vocabulary is absent" false since #406 (Structure/Limit/Creation.v `CreatesLimit`, `creates_limit_lift`, `StrictLift`, `self_lift`, `JointlyCreateLimit`); "no discrete subcategory on the objects and no inclusion" TRUE as stated (0 hits) but Comparison.v's `DiscreteCat_Functor'` and Instance/Fun/Discrete.v's `Fun_Discrete_PiCat` exist — the latter Set-PINNED in the target (it uses the unannotated `DiscreteCat_Functor`; "Cannot enforce Set = …" at a general X), so Riehl's route through `PiCat_JointlyCreateLimit` is not usable and the reflection is proved directly; `Induced` is not where the issue puts it; the Riehl note's "no evaluation functor" and "Instance/Fun/ has exactly one file" false (`Eval`, `EvalAt`; fourteen files besides this one). **Universes**: `Res@{jo jh co ch u u0 u1}` carries only `jh = ch` and leaves P's OBJECT level free — at `Constraint smallh < bigo` (objects above homs) `ObjCat Pw`, `Res Pw Xw` and `[Pw, Xw] ⟶ [Pw, Xw]` are accepted, and seven constants share the freedom (`ObjCat`, `DiscInc`, `Res`, `res_obj`, `res_obj_at`, `res_map`, `strict_self`); the other 27, every one reaching `Eval` through its definition (21 name it in the statement), inherit `jo <= jh`, whose FIRST CARRIER is the type `[P, X] ⟶ X` itself (`Fun`'s hom universe sits above P's objects and `Functor` identifies source and target hom universes) — probe N5 refuses the bare type `[Pw, Xw] ⟶ Xw` ("Cannot enforce smallh = … because smallh < bigo"), N6 `@Eval Pw Xw`, N4 `Functor_Category_Complete Pw Xw`; the 20 section constants with J carry #425's four hom equations, the nine over `{P X}` alone `u0 = u2` only, the annotated alias `jo <= h` and no equation; stdlib bounds `jo <= eq.u0`, `jo <= Logic_lemmas.equality.u0`, `jh <= eq_ind.u0`, `jh <= eq_ind_r.u0` (first carrier `ObjCat`, from `DiscreteCat`) and `jh <= eq_rect_r.u1`, `jh <= EqdepFacts.eq_sigT_sig_eq.u2`, `JMeq.u0 <= JMeq.u1` (first carrier `DiscInc`, from `DiscreteCat_Functor'`) on the 21 constants outside the reflection and joint-creation sections (they reach `ObjCat` or `Res` through their definitions), none on those sections' 13; ten constants also inherit bounds into a non-local `Projections` module from Structure/Limit/Creation.v; no word-bounded `Set`. **43 constants closed under the global context, zero `Axioms:` lines** — 34 `.glob` heads (33 `def`, 1 `prf`) plus 9 `Program` obligations (`ptw_apex_iso` 6, `res_comp_cone` 1, `res_ump_nat` 2); the gate carries the 34 heads, fully qualified; six `Defined`, only `Res_CreatesLimit` LOAD-BEARING (flipped to `Qed`, `creation_apex` is refused; the other five flip with library and probe unchanged and stay transparent as data); seven `Qed` tokens; closure 52 excluding self (Structure/Limit/Comparison.v 11 at the margin — the cost of `DiscreteCat_Functor'`, whose relocation into Instance/Discrete.v is surfaced, not done — Instance/Fun/Limit.v 9, Structure/Limit/Creation.v 1, Theory/Kan/Extension.v 1, twelve `Require`s 0); zero name collisions. `Test/ProbeFunCreation426.v` mirrors the `Require` list and carries **7 refutation commands = 1 instrument + N1, N2, N3 CONVERSION + N4, N5, N6 UNIVERSE**, each stripped one at a time in a copy of the whole file beside its accepted controls; guard coverage 32 identifier tokens inside / 26 also named outside (comments stripped) with six exhaustive exceptions; rename-simulated 13/13, every first break on a positive line; `make todo` grows by those 7 lines only (2226 → 2233), so the issue's "adds no new hits" box is not met as written (disclosed). Two forward references in Instance/Fun/Limit.v repointed line-neutrally (an earlier revision named the wrong one of the two; re-measured). NOT delivered: the colimit half (not one colimit line; no issue carries it — #715 is pointwise colimits, not creation); Riehl's strict creation; a hypothesis-free `CreatesLimit D Res`; the `Fun_Discrete_PiCat` route; no edit to Instance/Discrete.v, Structure/Limit/Comparison.v, Structure/Limit/Creation.v, Theory/Kan/Extension.v or Instance/Fun/Discrete.v.


- **Instance/Fun/Terminal.v**: Mac Lane §III.5 Ex 5 — the POINTWISE terminal object and pointwise INDEXED products of a functor category, the nullary and infinitary halves of an exercise whose binary half was already `Functor_Category_Cartesian` (`Instance/Fun/Cartesian.v`). **`Functor_Category_Terminal (T : @Terminal D) : @Terminal ([C, D])`** — the constant functor at `1` with the unique natural transformation into it, registered as an `#[export] Program Instance` on the binary case's precedent, and named so because **issue #718 (Awodey §8.7, exponentials of presheaves) records that it DEFERS the terminal presheaf to this issue** — a fact about the issue tracker, not about the tree: no in-tree consumer exists yet, the probe being the only importer, and that is stated rather than implied. **`Fun_HasIndexedProducts : HasIndexedProducts D → HasIndexedProducts ([C, D])` at an ARBITRARY index `Type`**, built ELEMENTARY-FIRST — `Fun_iprod`/`Fun_iprod_proj`/`Fun_IsIndexedProduct` at the apex-pinned `IsIndexedProduct`, then `Build_HasIndexedProducts` — with **no `Limit`, no `Cone` and no `DiscreteCat` occurring anywhere in the file**, deliberately (see the universe correction below). It is a plain `Definition` and NOT an `Instance`, a disclosed design call rather than an oversight: it carries a premise of its own class, so registering it would let resolution unify an unknown category with `Fun ?C ?D` and recurse, where the leaf `Sets_HasIndexedProducts` is safe to register. **A UNIVERSE ATTRIBUTION THIS INDEX HAD CARRIED WRONGLY IS CORRECTED HERE, AND THE CORRECTION IS MEASURED THREE WAYS**: `DiscreteCat_Functor` ALONE does NOT pin the ambient category — `probe_functor_only` has `C : Category@{u0 u2 u2}`, FREE, the functor fixing only the SHAPE at `DiscreteCat@{u Set Set}` — and `Cone` is NOT a co-donor either, `probe_cone_of_discrete` being free at `C : Category@{u1 u2 u2}`; the ambient `Set` pin needs a SECOND donor that identifies the shape's hom and proof with the ambient's, giving `probe_limit_of_discrete` at `C : Category@{u1 Set Set}`. **It takes BOTH.** Read the `Cone` half NARROWLY: the RECORD is innocent, but that licenses no claim that `IsALimit`/`Limit` are the ONLY other donors, and they are not — `cone_leg` (`Structure/Limit/Preservation.v`) and `IsLimitCone` identify them the same way, so CONE VOCABULARY is among the donors even though the record is not; `Structure/Limit/Initial.v`'s own bullet already warns that an `ACone` control rules out only `ACone`/`Cone`, and an earlier revision of this bullet ran that control and drew the broader conclusion anyway. The practical conclusion every affected bullet draws — do not route an indexed product through the discrete presentation — is UNCHANGED; only the `Construction/Coproduct/Indexed.v`/`Instance/Cat/Coproduct.v` bullet stated the mechanism as one donor, and that is the one fixed in the same commit — an earlier revision of THIS sentence misnamed it as `Structure/Bicartesian/Matrix.v`'s, which does not mention `DiscreteCat_Functor` at all; `Functor/Hom/Limit.v`'s named both donors and was right, and `Structure/Limit/Product/Finite.v`'s cites the composite and is sound. **A SECOND MEASUREMENT LESSON, and it inverts the house rule**: `Functor_Category_Terminal@{u u0 u1 u2 u3 u4}`'s constraint block is SIX BOUNDS WITH NO EQUATION, yet its binder reads `C : Category@{u2 u4 u4}` and `D : Category@{u3 u4 u4}` — hom identified with proof in BOTH and the two identified with each other, expressed by REUSING the level variable. Reading the block alone reports "no identification" and is WRONG; both must be read. That identification is `Fun`'s, and `Fun` does more than its four sibling donors: **FIVE donors identify hom with proof INDEPENDENTLY** — `Terminal`, `Cartesian`, `HasIndexedProducts`, `IsIndexedProduct` and `Fun` — each rejected under `Constraint uh < up` against controls naming a hom and an object family at those very levels, so no single one is "the" cause. What stays FREE is worth as much: `Constant_Functor@{co ch cp dro dh dp}` identifies NOTHING (five bounds, `Functor`'s own block verbatim) and `Constant_Terminal_Functor` keeps **C's hom and proof APART** — which is why the constant functor is built by hand rather than cited from `Functor/Diagonal.v`, `fobj[@Diagonal D C] d` being REJECTED at `C : Category@{co ch cp}` with `ch < cp` ("Cannot enforce cp = dh") against the hand-built functor as a passing control at the same levels; both records are compared in-file anyway, agreeing on BOTH actions at `eq_refl` while the whole records are refuted, `Diagonal` being a `Program Instance` with opaque law fields. `Fun_iprod` keeps C's and D's hom levels DISTINCT and leaves the INDEX universe only BOUNDED, never identified; the file's only EQUATION is `Fun`'s `u0 = u2`, carried by EIGHT constants rather than the one an earlier revision of this bullet named — `Fun_IsIndexedProduct` plus `fun_terminal_obj`, `fun_terminal_fmap`, `fun_terminal_one_component`, `fun_iprod_tuple`, `constant_functor_diagonal_obj`, `constant_functor_diagonal_fmap` and `ctl_bool_iprod_obj`, every constant whose statement is at `[C, D]`; it is the SAME equation each time so the conclusion survives, but the sentence read as a count; and **the general theorems carry no `Set` at all**, the witnesses' `Set` being the SHAPE's (`Instance/Two.v` declares `TwoHom : … → Set`). **The binary comparison is an ISOMORPHISM and NOT `eq_refl`, and the reason is mathematical rather than presentational**: `HasIndexedProducts D` and `Cartesian D` choose their objects INDEPENDENTLY, so `fobj[Fun_iprod HP (bool_fam F G)] c = fobj[F × G] c` is refuted (pinned in-file with controls) and `Fun_bool_iprod_iso` with BOTH leg families is the strongest available; `cartesian_bool_IsIndexedProduct` supplies the generic half — in ANY cartesian category the binary product IS an indexed product over `bool`. By-product: **`iprod_jointly_monic`** — the legs of an indexed product are jointly monic — which is **NOT new, contrary to an earlier revision of this bullet**: `Structure/Bicartesian/Matrix.v`'s `iprod_ext` is the same statement with essentially the same proof, and the novelty claim rested on a search for the NAME `jointly_monic`, the exact trap this index warns about. What IS true, and is the honest reason for restating it, is that `iprod_ext`'s type carries a spurious `IsIndexedCoproduct` argument inherited from its section, so it cannot be applied here without supplying an unrelated coproduct. It is also NOT "the only place `HasIndexedProducts D` is spent" — that class supplies the object, the projections and the UMP, and the lemma takes `IsIndexedProduct` rather than the class; what the lemma carries is the ROUTING, every functor law and naturality square going through it. Its natural home is `Structure/Limit/Product.v` rather than here. The nullary and infinitary halves are ONE phenomenon: `terminal_empty_IsIndexedProduct` / `Fun_empty_iprod_terminal` show the terminal functor IS the pointwise product over an empty index, stated over an arbitrary `A` with `A → False` rather than a named empty type **so that no index universe is pinned**. Prior art is disclosed rather than restated: the pointwise-inheritance principle is already prose in FOUR places — `Structure/Cartesian/Product.v`, twice in `Instance/Fun.v`, and `Instance/Fun/Cartesian.v` quoting the nLab — and all four are cited. Holding at `eq_refl`: the terminal functor's object action, arrow action and the component of its unique arrow; the pointwise product's object action and its projections' components; the class-extracted mediator IS `fun_iprod_tuple`; and both actions of `Constant_Functor` against `fobj[Diagonal]`. Non-vacuity is proved, not gestured at: `two_pick true` and `two_pick false` are two provably DISTINCT transformations INTO a functor of `[_2, Sets]` while arrows OUT into the terminal one are unique, so terminality is a real asymmetric constraint; and the pointwise product has carrier `∀ _ : bool, bool` by `eq_refl`, two provably inequivalent elements, computing projections, and a COMPUTING mediator. **72/72 constants closed under the global context.** `Test/ProbeFunTerminal339.v` pins the boundary with **6 negatives, all FORMABILITY** — the five independent donors plus the `Diagonal` pin — each stripped and confirmed a genuine universe inconsistency naming the declared levels, plus an instrument check and the three-way `Set`-pin separation as `About`-able definitions; rename-simulated **7/7** unpadded against an instrument-checked baseline, after two vacuous guards were found and fixed (`_` is a word character, so `Check @Fun_HasIndexedProducts` does NOT guard the bare `HasIndexedProducts`). NOT delivered: no `Limit`-shaped corollary and no general "limits in `[C,D]` are pointwise"; no colimits or equalizers (pullbacks are now Instance/Fun/Pullback.v's `Fun_HasPullbacks`, #403, and exponentials Instance/Fun/Exponential.v's `Functor_Category_Closed`, #718); no `Cartesian` or `Complete` for `[C, D]`; no functoriality or naturality of `Fun_iprod` in the family or the index; no `HasIndexedProducts D → Terminal D` (it would need a chosen empty index type, which would pin an index universe — the emptiness is a hypothesis instead); no preservation statement for the evaluation functors `[C, D] ⟶ D`; and no separation proof anywhere, so nothing is called "strictly weaker"
- **Instance/Fun/Closed.v**: Mac Lane §IV.6 Exercise 5 (book p. 98, `maclane:IV.6:ex5`) + Awodey §8.7 (book pp. 207-208, the unnumbered remark that the pointwise formula does not define an exponential of presheaves) — **cartesian closure is NOT inherited by functor categories, proved rather than remarked, and the objectwise exponential formula gives the wrong OBJECT**. Mac Lane's exercise reads in full: "5. Show that A cartesian closed need not imply A^J cartesian closed." **BOTH HALVES RUN ON ONE ENGINE, and the engine is where the content is**: in a cartesian closed category `f ↦ curry (f ∘ exr)` sends a morphism `P ~> Q` to a POINT `1 ~> Q^P`, and that assignment is INJECTIVE — `uncurry_curry` undoes the transpose and `exr : 1 × P ~> P` is SPLIT EPI, its section the pairing of `one` with the identity (`exr_fork`), so it left-cancels; note this uses NO unitor isomorphism, `prod_one_l` appearing nowhere in the file — an isomorphism there would advertise a bijection, and only left-cancellability is proved or needed. Specialised to `[J, A]` where the INDEX category has an INITIAL object `j0`, naturality at the unique arrow `j0 ~> j` forces a transformation out of the constant-terminal functor to be determined by its component at `j0`, so **`Hom_{[J,A]}(1, E)` INJECTS into `Hom_A(1_A, E j0)`** (`fun_const_point`/`fun_const_point_inj`, then `fun_hom_point`/`fun_hom_point_inj`). An INJECTION is what is proved and all either half needs; no bijection is claimed. **(1) MAC LANE.** `A := FinSet` (cartesian closed by `FinSet_Closed`, `Instance/FinSet/Closed.v`), `J := Omega` (initial object by `Omega_Initial`, `Structure/Limit/Initial.v`). `Hom_FinSet(1, n)` has AT MOST `n` elements (`finset_point_eq` is injectivity at `Fin.F1` and nothing more; the lower bound is neither needed nor proved), so closure would make every hom-set of `[Omega, FinSet]` finite; but `TwoFun` — constant at the object `2`, arrow action `two_map (le_t_n) = id` and `two_map (le_t_S _) = const F1` — admits the infinite family `Alpha k` (naturality reduces to `α_m F1 = F1`, which every member satisfies; `Alpha_distinct` separates them at component `S k` on `Fin.FS Fin.F1`). Pigeonhole then gives **`fun_not_cartesian_closed (CC : @Cartesian ([Omega, FinSet])) : @Closed _ CC -> False`** — quantified over an **ARBITRARY** cartesian structure, not merely the pointwise one, which is the honest reading of "not cartesian closed"; the engine takes the product structure as a parameter and supplies the terminal object itself, so the strengthening cost nothing. `fun_pointwise_not_cartesian_closed` is the pointwise corollary, kept so the hypothesis is visibly non-vacuous for want of a cartesian structure. **There is NO pigeonhole lemma in tree** — the ingredients are `fin_enum`/`fin_enum_length`/`fin_enum_full`/`fin_enum_nodup` (`Instance/FinSet/Skeleton.v`) and the nearest existing statement is an `assert` sealed inside `fin_bijection_index`'s `Qed` — so `fin_no_nat_injection` is built locally from those plus stdlib `List.NoDup_incl_length`, with a `Fin.to_nat`/`Fin.to_nat_inj` restriction step the nat-indexed family requires. **(2) AWODEY.** `J := _2^op`, whose initial object IS `Two_Terminal` read in the opposite category (`Initial C` is notation for `@Terminal (C^op)` and `(C^op)^op` is `C`, so `TwoOpInitial := Two_Terminal` typechecks by conversion with no transport). `PresheafP` is one-element at `TwoX` and EMPTY at `TwoY`; `PresheafQ` is constant at a two-element setoid. `Hom(P, Q)` has at least two elements (`PQNat true`/`PQNat false`, distinct by `PQNat_distinct`), so by the engine the true exponential's value at `TwoY` has at least two points, while the objectwise candidate `Q(TwoY)^{P(TwoY)}` — the exponential IN `Sets` of a two-element setoid by the empty one — is a subsingleton (`objectwise_candidate_subsingleton`, two of its points being maps out of the empty setoid). Hence **`awodey_pointwise_not_exponential`**: no `@Isomorphism Sets` between the two. **READ ITS STRENGTH AND ITS SCOPE PRECISELY, because it is neither weaker nor the same as Awodey's own sentence.** Awodey's stated reason is that the objectwise assignment "is not functorial"; read literally, "there is no contravariant action on it" is FALSE FOR THE FAMILY AT HAND, which does admit one — though not because every family does, an empty-valued family over `_2` admitting none. What is proved here is different in kind and stronger where it matters: the objectwise VALUES are already wrong, so no action whatsoever on them can produce the exponential — the argument uses only that the two objects are isomorphic. Nothing here formalizes the sentence "it's not functorial". The theorem is also **CONDITIONAL on `[_2^op, Sets]` being cartesian closed**, which is #718's presheaf result — since landed as Instance/Fun/Exponential.v's `Functor_Category_Closed`, which that file's new section G instantiates at `_2` so that `awodey_pointwise_not_exponential_unconditional` now stands beside the conditional theorem with no hypothesis at all, at the `Set` pin `_2` brings. **UNIVERSES, measured first and reported with their attribution**: `[Omega, FinSet]` elaborates free of any new `Set`, but **`[_2^op, Sets]` elaborates ONLY at `Sets@{Set _}`** — the carrier universe pinned to the literal `Set` — attributed to TWO CO-NECESSARY donors, `_2` (`Category@{u Set Set}`) and `Fun` (which identifies its source and target hom universes): `[Omega, Sets]` leaves `Sets`' carrier free, so `_2` is necessary, and the bare functor TYPE `@Functor (_2^op) Sets` is formable at the very levels where the CATEGORY `[_2^op, Sets]` is not, so `Fun` is necessary too — the second half measured out of tree and NOT pinned; `Sets` is measured NOT to be a donor. That **RESTRICTS THE CONTENT** of the Awodey half to presheaves valued in small setoids; it is disclosed and is NOT claimed unavoidable. `TwoFun` NEEDS explicit universe binders: written unannotated it minimizes and is then rejected as an object of the very `[Omega, FinSet]` its section elaborates. **PRIOR ART — the issue's "the only structure lemma for functor categories in the tree" is FALSE**: `Functor_Category_Terminal` (`Instance/Fun/Terminal.v`) and `Fun_HasIndexedProducts` both exist, as do `Thin_Fun` (`Instance/Proset/Closure.v`) and `Fun_IsGroupoid` (`Construction/Deloop/Transform.v`); the header states its sweep criterion and lists what the sweep also returns of a different shape. `Instance/Fun.v`'s standing prose claim is corrected in the same commit — it now hedges the positive nLab clause, records that completeness of D may not simply be dropped and points here for the counterexample, and names BOTH `Instance/Fun/Cartesian.v` (binary products) and `Instance/Fun/Terminal.v` (terminal object, indexed products) as what IS inherited pointwise. **43/43 constants closed under the global context**, zero `Axioms:` lines — 34 source-declared plus 9 `Program` obligations, of which `TwoFun` contributes THREE though only one needed a `Next Obligation`, so a source sweep sees none of them; counted from whitespace-flattened `Print Module`, and **the count needed care twice over**: a line-anchored sweep of that output harvests the wrapped module head, and a name file written without a trailing newline makes `while read` silently skip its last entry (`grep -c .` = 43 where `wc -l` = 42), which is how a first pass reported 42. Exactly ONE `Defined` (`PMap`) against 22 `Qed`, and it IS load-bearing — flipped to `Qed`, `PresheafP`'s obligations stop. `Test/ProbeFunClosed392.v` carries **8 `Fail`s = 1 instrument + 7 negatives of THREE KINDS told apart by the error TEXT** — 2 FORMABILITY (both ending `universe inconsistency: Cannot enforce Set = …`), 3 CONVERSION (each ending `cannot unify`), and 2 TYPING (a plain `has type … while it is expected to have type …` with NO `cannot unify`; note the two formability errors ALSO carry a has-type clause, so the universe clause is what discriminates) — each stripped ONE AT A TIME and compiled alone with its whole error read, and every constant a negative names also appears in a passing `Check` outside all of them — the only names inside a `Fail` and nowhere outside being the three `Fail Definition` heads, which never enter the environment, and the instrument's deliberately-absent name. `make todo` grows by 14, **all in the probe and none in the library**. NOT DELIVERED, and the scope-out is deliberate rather than an omission: the positive companion IN ITS GENERAL FORM (D complete and cartesian closed, C small ⟹ `[C, D]` cartesian closed) is NOT attempted; its presheaf case is #718's and landed in Instance/Fun/Exponential.v rather than here, on a measured closure asymmetry (81 against 62, twenty modules this file needs and the exponential does not), this file gaining only the section G that consumes it; no end or limit formula for exponentials in a functor category; no characterisation of which pairs `(J, A)` do give cartesian closure — ONE pair is exhibited and nothing is claimed beyond it; `FinSet` is NOT proved incomplete, that being an inference relative to the nLab theorem rather than a theorem here; nothing about local cartesian closure or the classical "`A^𝟚` is cartesian closed iff A is locally cartesian closed"; and nothing is registered as an `Instance`
- **Instance/Fun/Exponential.v**: Awodey §8.7 display (8.5), Proposition 8.13 and the first clause of Theorem 8.14 (book pp. 208-209, `awodey:8.7:def-presheaf-exponential`, `awodey:8.7:prop13`, `awodey:8.7:thm14`) + Riehl §4.4 Lemma 4.4.11 (printed pp. 152-153, `riehl:4.4:lem11`), with her §6.5 Exercise 6.5.iii (printed p. 248, `riehl:6.5:exiii`) cited as the density route and not delivered — **exponentials of presheaves: `[C^op, Sets]` is cartesian closed**. `Functor_Category_Closed C : @Closed ([C^op, Sets]) (@Functor_Category_Cartesian (C^op) Sets Sets_Cartesian)`, an `#[export] Program Instance` for an arbitrary `C : Category@{o h h}`, whose exponential `PshExp P Q` is the display-(8.5) formula and nothing else — `PshExp_obj P Q c` IS `[[[C^op, Sets]]](Curried_CoHom C c × P, Q)`, read back at `eq_refl` by `exp_obj_is_display85`, the arrow action precomposition with `first (fmap[Curried_CoHom C] h)`, and BOTH functor laws PROVED in a hand-built `Program Definition` over separate object and arrow actions rather than inherited YoNat-style (`fmap_id` alone is closed by the default tactic; measured, a third `Next Obligation` reports none remaining) — with `presheaf_exp_iso : @Isomorphism Sets ([[[C^op, Sets]]](X × P, Q)) ([[[C^op, Sets]]](X, PshExp P Q))` (Proposition 8.13, Riehl's (4.4.12)) natural in X in BOTH directions (`to_exp_natural_X` costs one appeal to `naturality_sym` of the precomposed transformation, `from_exp_natural_X` closes by `reflexivity`), `presheaf_terminal C := Functor_Category_Terminal Sets_Terminal` CONSUMING #339 (readback `eq_refl`), `Functor_Category_Closed_cov` giving Riehl's covariant `Set^C` as a `:=` at `C^op` with no tactic and no transport since `(C^op)^op = C` holds by `eq_refl` — annotated like the instance so that it is delivered at the same universes (unannotated it minimizes to `Category@{u0 u0 u0}` and is refused where the instance is accepted; measured, the annotated form pinned as a control) — and the class readbacks `exponent_obj_is_PshExp`, `curry_is_to_exp`, `uncurry_is_from_exp` at `eq_refl`. **THE ROUTE IS THE DIRECT ONE, AND THE CHOICE IS MEASURED RATHER THAN STYLISTIC**: Riehl's explicit transposition formulas — `to_exp alpha c x` has component `hp ↦ alpha d (fmap[X] (fst hp) x, snd hp)` and `from_exp beta` component `xp ↦ (beta c (fst xp)) c (id, snd xp)` — with the two round trips spending exactly `fmap_id` of X (`from_to_exp`) and the naturality of beta plus one identity law (`to_from_exp`, Riehl's "by the Yoneda lemma"), NO Yoneda constant anywhere: neither `Functor/Hom/Yoneda` module is required and neither is in the file's 62-module closure; and a route through the lemma would have NARROWED the theorem, since `Yoneda_Lemma` and `yoneda_natural_pre` are declared over `Category@{u0 u0 u0}` and are refused at a `C` whose objects sit strictly below its homs, where the display-(8.5) object, `PshExp`, `presheaf_exp_iso` and the instance are all accepted (both refusals and seven controls pinned). Awodey's own route is NOT taken: his Proposition 8.10 IS in tree as `Theory/Density.v`'s `presheaf_density` (cited by path, not required), his Lemma 8.12 is absent, and `CCC_prod_preserves_colimits` cannot supply it because it takes a `Closed` structure as a HYPOTHESIS — read off the types, not a compiled experiment. **UNIVERSES, off BOTH binder and block**: `Functor_Category_Closed@{o h so u} : ∀ C : Category@{o h h}, Closed@{so h u}` with block `h < so, h < u, o <= h, o <= so` plus stdlib bounds — hom = proof in the BINDER, the donors' doing (`Opposite` and `Fun` each refuse `ch < cp` alone, with no reference to the other, while `@Functor C Sets` is accepted there, so `Functor` is NOT a donor), the OBJECT universe free of the hom universe but BOUNDED by it, and that bound `o <= h` IS Awodey's "for any small category C": it is NEW relative to the presheaf category itself (`[C^op, Sets]` and `presheaf_terminal` are both formable at a `C` whose objects sit strictly ABOVE `Sets`' carriers, pinned as controls) and its mechanism is `Transform : Type@{max(o1,p2)}` — a set of natural transformations is a family indexed by the source's objects — the bound Construction/Elements/Kan.v's `KanNat`/`KanCone` carry with the same attribution and the same refusal to call it unavoidable. `PshExp` taken ALONE lands in a possibly LARGER `Sets` (its block carries `u0 <= u3`, not an equation), and it is `presheaf_exp_iso`'s STATEMENT that collapses the levels: pinned as a discriminating pair, `PshExp Pb Qb` ACCEPTED at `o < co` and the same term ASCRIBED at its arguments' `Sets` refused, then the iso and the instance. **The universe annotations are load-bearing JOINTLY, measured one at a time**: with no binder list, `C` bare and `Sets` bare the body minimizes to `Category@{u0 u0 u0}` and is refused where the shipped instance is accepted (pinned); with `Category@{o h h}` on `C` kept and `Sets@{h so}` dropped the object universe stays free, and so does it with `Sets@{h so}` kept and `C` bare — either alone blocks the collapse; the trailing `+` is required (an unbound universe without it — for the STATEMENT, not `Program`: the type alone is refused the same way and takes four further levels). ZERO word-bounded `Set` in the binder or block of any of the 46 constants; the twelve constants of the `C := 1` section (`one_exp_to`, `one_from_comp`, `one_exp_from`, `one_exp_iso` and their obligations) carry the block equations `u = u2`, `u0 = u3`, `u1 = u4` (the two `one_from_comp` constants the first two only), reported and NOT attributed, the other thirty-four carrying none. **Non-vacuity in both directions**: at `C := 1`, `one_exp_iso` recovers `Sets_Closed`'s exponential (its backward leg needs a `match` on the object, `poly_unit` having no eta here); at `C := _2`, Instance/Fun/Closed.v's new section G — `Psh2_Closed := Functor_Category_Closed _2` — discharges the hypothesis #392's section F was stated over, so `awodey_pointwise_not_exponential_unconditional` and `pq_point_distinct_unconditional` carry none, at the `Set` pin `_2` brings (`Psh2_Closed : Closed@{u Set u}`, block `Set < u`, inherited). Strict versus `≈`, measured strict first: FIVE `:= eq_refl` statements, the whole count; exactly ONE identification at `≈` only, Riehl's counit formula `ev_c(γ, y) = γ_c(id_c, y)` (`eval_is_at_identity`), refuted at `eq_refl` and pinned, the residue one identity law (`id ∘ id ≈ id`, an instance of `id_left` and of `id_right` alike) because `eval'` is `uncurry' id` and `nat_id`'s component is `fmap[−] id`; Awodey's `y h × 1_P` is spelled `first` and NOT `split _ id`, the two elaborating at one type and refuted convertible (pinned), `first`'s identity factor leaving no `fmap[P] id` residue in the pointwise-cartesian presheaf category (two `eq_refl` controls; WHY it reduces is not isolated). **50/50 constants closed under the global context** — 46 from `Print Module Category.Instance.Fun.Exponential` FLATTENED (27 declared heads + 19 `Program` obligations, of which only 14 have a written `Next Obligation`, `PshExp`, `presheaf_exp_iso`, `one_exp_to` and `one_exp_iso` each generating more than are written) plus Closed.v's four — all in the gate FULLY QUALIFIED as a new 4457-byte block, the gate's closed count 5271 → 5321 with the `Axioms:` line count unchanged at 1; FOUR `Defined` tokens, ALL load-bearing by flipping each alone to `Qed`; zero collisions over the 46 names, Closed.v's four and the probe's declared names, the only whole-word hit outside the three files being the prose pointer this change writes into Instance/Fun.v (the short module name `Exponential` is shared with the gate-loaded Instance/Cat/Exponential.v — legal, and nothing is referenced unqualified); closure 62 excluding self against Instance/Fun/Closed.v's 83 (81 before its new Require), the placement decided on that measured asymmetry — twenty modules in Closed.v's closure and not this one's, the chains beneath Instance/FinSet with its Skeleton satellite, Instance/Omega, Structure/Limit/Initial and Instance/Two/Monoidal. `Test/ProbePresheafExp718.v` mirrors the target's Require list plus the two Yoneda modules, Closed.v and Two.v and carries **15 refutation commands = 1 instrument + 14 negatives of THREE kinds told apart by the error text** — 9 FORMABILITY (the smallness bound at the display-(8.5) object, the ascribed `PshExp`, the iso and the instance; `Opposite` and `Fun` each alone; `Yoneda_Lemma`, `yoneda_natural_pre` and the unannotated clone), 2 CONVERSION (`first` versus `split _ id`; the counit formula at `eq_refl`), 3 TYPING (`PshExp P Q` ascribed covariantly, a plain has-type mismatch; the iso ascribed with `PshExp Q P`; the objectwise family `c ↦ Q(c)^{P(c)}`, refused as "a product" where a presheaf is expected — pinning the SHAPE of the objectwise data only, the objectwise VALUE being refuted by section G) — each stripped ONE AT A TIME and compiled alone with its whole error read (fifteen rc=1), guard coverage measured mechanically (56 identifiers inside a refutation command, 49 also outside, the seven exceptions exhaustively the keyword, the five names a refutation command declares and the instrument's absent name), rename-simulated 3/3 in place with every break on a `Check` line. `make todo` grows by 16, ALL in the probe, the three library files contributing ZERO — the DoD box not met as written, disclosed. The issue's "Current state" is stale on three counts, each re-measured (`ls Instance/Fun/` had nine files; #339's `Functor_Category_Terminal` existed and is consumed; its ten `Closed` sites omit Instance/Theory/Lindenbaum.v's `Lind_Closed`, whose type wraps onto a second line, and count Instance/Comp.v's `Algs_Closed`, which sits inside a block comment) and four of its cited donor lines drifted. NOT delivered: no naturality in P or Q (`PshExp` is not exhibited as a bifunctor); no Lemma 8.12 and no density proof; no `ElementaryTopos` for a presheaf category IN THIS FILE — #404's `Instance/Fun/Topos.v` joins this instance with #403's classifier into `Presheaf_Topos`, consuming `Functor_Category_Closed` unchanged; nothing about the Yoneda embedding preserving products and exponentials (Theorem 8.14's second clause, Awodey's §8.9 Exercise 4, a separate item); no general target D in place of `Sets`; no comparison with `Cat_Closed`; and nothing registered as an `Instance` except `Functor_Category_Closed`
- **Instance/Fun/Topos.v**, **Instance/Sets/Topos.v**: Mac Lane §IV.10 remark 1 (book p. 107, `maclane:IV.10:remark1`) + Appendix §App.1 (book pp. 289-290, `maclane:App.1:remark1`) + Awodey §8.8 Definition 8.16 and Proposition 8.17 (printed pp. 210-211, `awodey:8.8:prop17`) + Seven Sketches §7.3 with its footnote 5 (printed pp. 231-232, `7sketches:7.3:remark-presheaf-topos`) — **`Sets` and every presheaf category assembled as ELEMENTARY TOPOSES, conditionally, with the one conditional field named**. Seven bundles: **`Presheaf_Topos@{o h so} (C : Category@{o h h}) (UT : Untruncate@{h}) : ElementaryTopos ([C^op, Sets@{h so}])`** (the issue's pinned name) with `Presheaf_Topos_IEM`, and the Seven Sketches footnote-5 checkbox as **`Copresheaf_Topos C UT := Presheaf_Topos (C^op) UT`** with its `_IEM` twin — a `:=` with NO tactic and NO transport, `(C^op)^op = C` holding by `eq_refl` (`psh_op_invol`) — and **`Sets_Topos@{o so} (U : Untruncate@{o})`**, `Sets_Topos_dec (D : DecImage)`, `Sets_Topos_IEM`. **EVERY BUNDLE UNFOLDS TO A RECORD LITERAL OF FIVE IN-TREE DONORS AND NOTHING ELSE** — three are written as one (`Presheaf_Topos`, `Sets_Topos`, `Sets_Topos_dec`) and the other four are a `:=` of one of those, the two `_IEM` readings through a sixth donor, `untruncate_of_IEM`: `Functor_Category_Terminal`/`Functor_Category_Cartesian`/`Fun_HasPullbacks`/`Functor_Category_Closed`/`Fun_Classifier` on the presheaf side (#339, #403, #718 consumed, none rebuilt) and `Sets_Terminal`/`Sets_Cartesian`/`Sets_HasPullbacks`/`Sets_Closed`/`Sets_Classifier` on the setoid side, so `Instance/Fun/Topos.v` contains NO tactic, NO `Program`, NO obligation and NO `Qed` at all, and only §B of the Sets file proves anything. **EXACTLY ONE FIELD IS CONDITIONAL, AND IT IS MAC LANE'S OWN AXIOM 8**: the classifier, resting on `Instance/Sets/Classifier/OneLevel.v`'s `Untruncate` (or `DecImage`, or `IEM`), none of which has an axiom-free in-tree inhabitant — measured by TWO sweeps, 36 declaration heads mentioning one of the three as a HYPOTHESIS and FIVE having one as a CONCLUSION, all five being PASSAGES between them — and **NO IMPOSSIBILITY IS PROVED**, the naive elimination's refusal being cited to `Test/ProbeClassifier402.v` rather than duplicated. So the honest verdict is NEITHER of the issue's two branches: a one-level instance does exist, conditionally. **THE STRONGEST READBACK IS THAT MAC LANE'S POWER OBJECT IS DEFINITIONAL AT `Sets`**: `@Pow Sets (Sets_Topos U) b = Powerset_Prop_obj b` by `eq_refl` — his "P b, the set of all subsets s of b" (App.1 Axiom 9, quoted verbatim) IS the truncated power set, because `Powerset.v` and `Cartesian/Closed.v` build the same record and `Powerset/Universal.v` makes `Powerset_Omega` BE `Powerset_Prop_truth`; on the DECIDABLE route the power object is the DECIDABLE power set instead, so the same equation is REFUSED there while its carrier IS `SetoidMorphism b BoolSetoid` at `eq_refl` (refutation and control both pinned). **THE PRINTED PAGE IS INTERNALLY INCONSISTENT AND BOTH HALVES ARE QUOTED RATHER THAN RECONCILED**: p. 290 writes "ψ(y) = 1 or 0 according as the element y of b is or is not in the subset a" and, two sentences later, "ε_b(x, s) = 0 if the element x of b is in the subset s and equal to 1 otherwise" — membership is 1 for ψ and 0 for ε on one page — and the issue's paraphrase silently corrected it; the headers quote both and resolve neither, pointing at #402/#403's name-of-truth notes. App.1's four clauses land as: the terminal object's carrier `poly_unit` at `eq_refl`; the pullback of `a → 1 ← b` isomorphic to the product WITH its leg equations (`pullback_over_one_is_product`, the `eq_refl` forms refuted — the apex is a SIGMA carrying the agreement witness, so `pbone_fst = exl` is not even well typed, a TYPING refusal beside three CONVERSION ones); Ω and truth at `eq_refl` on both routes; and the classifying rule as `app1_char_rule_U`/`_dec`, `:=` of OneLevel's own `sets_classifier_char_iff`/`_dec_char_iff`. **READ App.1's LAST CLAUSE AT ITS TRUE STRENGTH, WHICH AN AUDIT NARROWED**: `eval (s, x) = s x` holds at an ARBITRARY second object of `Sets` — a `Sets_Closed` fact, not a topos fact, machine-checked by closing the shipped Example as an instance of the general one — so the two Examples are ONE fact at the two routes' Ω, what their TYPES pin is already pinned by `app1_pow_carrier`, and the word MEMBERSHIP would be an over-read: nothing relates `s x` to `sets_in_image`, to `char` or to the classifying rule, and the header says so. **WHAT THE FLAGSHIPS BUY, AND WHERE THEY ARE FIRSTS**: `Pow`, `power_object_monadic`, `topos_finitely_complete`, `topos_HasEqualizers` and `topos_has_finite_colimits` (#405) all elaborate at BOTH toposes — at `[C^op, Sets]` giving what would be the FIRST `Initial`, `Cocartesian`, `HasCoequalizers`, `HasPushouts` and `HasEqualizers` at ANY functor category (measured by three sweeps: a NAME sweep returning nothing, a SHAPE sweep returning nothing with `@Cartesian ([` as the instrument, and a reading of every declaration head of those five classes tree-wide), at `Sets` giving SECOND inhabitants beside `Sets_Initial`/`Sets_Cocartesian`/`Sets_HasCoequalizers`/`Sets_HasPushouts`/`Sets_HasEqualizers` — and they live in the PROBE, not the library files, on a measured closure ground (+25 and +27 modules). **NO AGREEMENT PROOF IS BUILT** between the derived legs and the existing `Sets` ones; they are ASCRIBED at those instances' own types so the types are seen to line up, and that is all. **THREE THEOREMS ARE REFUSED AT BOTH TOPOSES AND THAT IS WHY `FinSet_Topos` KEEPS ITS ROW**: `classifier_classifies`, `relations_iso` and `Sub_classifier_natural` all carry objects ≤ homs, which neither `Sets@{o so}` (o < so) nor a presheaf category satisfies — FIVE of those six universe refusals are pinned, with `Pow` at those toposes as the accepted control; the sixth, `Sub_classifier_natural` at `Sets_Topos`, is measured and pinned NOWHERE, which is disclosed rather than glossed — so docs/INHABITATION.md's witness column gains nothing from either new bundle, and `Presheaf_Topos_small` does not rescue it: the small reading is BLOCKED, and at the CLOSED field specifically, FOUR of the five fields fitting `PShSmall` and the exponential not (the obstruction sits at `presheaf_exp_iso`, whose hom-setoids live in `Sets@{o so}` while the lifted reading needs a larger one), with `fun_classifier_classifies` cited as the standalone stand-in and NOTHING claimed impossible. **Universes measured off BOTH binder and block over all 67 constants: NOT ONE CONSTRAINT BLOCK IN EITHER FILE CONTAINS AN EQUATION** — and in `Instance/Fun/Topos.v` that is true only because the readback `Section`'s `Context` is ANNOTATED, an audit having found that left bare it hands the block equation `u0 = u1` to every readback that USES that context, sixteen of the nineteen, the three exceptions being the whole-bundle ones, which bind their own `C` and `UT` (the `Untruncate` universe identified with C's hom universe, which the four bundles carry in their BINDER instead); the fix was measured both ways and the file says so where the reader looks. C's hom and proof are identified IN THE BINDER with no block equation saying so, the OBJECT universe is BOUNDED by the hom universe (`o <= h`) and never identified — that bound IS Awodey's "for any small category C", and it has **EXACTLY TWO DONORS, EACH SUFFICIENT ALONE** (`Functor_Category_Closed` and `Fun_Classifier`, each refused in a command where the other does not appear, against three accepted controls at the same levels, and neither is the #340 already-refused-argument trap) — `Set` occurs ONLY as a strict LOWER bound over all 67, with **`Sets_Topos_dec` carrying no `Set` anywhere** and reaching `Sets@{Set su}` where `Sets_Topos` is refused "Cannot enforce Set < Set", and the annotation on the four bundles buys PRESENTATION and not strength (the unannotated bodies elaborate at the same reading up to renaming, and `Copresheaf_Topos` does NOT minimize where a standalone alias would — the #718 lesson). **`_2` IS NOT A WITNESS SHAPE AND THE EXCLUSION IS THE CLASSIFIER'S**: four of the five donors are accepted at `_2` and `Fun_Classifier` alone is not (`Set < u` from its own declaration), so the shape witnesses are `_1`, `Ordinal 2`, `Parallel` and `Roof`. Strict-first ledger: **TWENTY `eq_refl` statements in `Instance/Fun/Topos.v` and TWENTY-TWO in `Instance/Sets/Topos.v`**, with exactly FOUR identifications at `≈` only, all four in the Sets pullback section, and four strict refutations pinned beside them — but they are two different fours, and only ONE of the pinned four (`exl ∘ pbone_to_prod ≈ pbone_fst`) is the strict form of one of the `≈` lemmas, the other three refuting the OBJECT identification and `pbone_fst = exl`; among the twenty are THREE WHOLE-BUNDLE readbacks (audit-added, strictly stronger than the two field ones they sit beside) showing the covariant and contravariant bundles are the SAME TERM at opposite categories, in both directions, and the `IEM` pair with them. **67/67 constants closed under the global context**, all gated FULLY QUALIFIED in a new Makefile block (the gate's closed count rises by exactly 67, the `Axioms:` line count unchanged at 1), counted three ways that agree — `Print Module` flattened, the `.glob`, and a source sweep — which they can because NEITHER FILE USES `Program`, so there are zero obligation constants. TWO `Defined` tokens, NEITHER load-bearing (each flipped alone to `Qed` with rc=0), kept by the data convention. Closures 108 / 74 / 151 excluding self. `Test/ProbeToposInstances404.v` carries **19 refutation commands = 1 instrument + 18 negatives of FOUR kinds told apart by the error TEXT** (11 FORMABILITY, 4 CONVERSION, 2 TYPING, 1 RESOLUTION — the last being that the bundle does NOT resolve by typeclass search, `Untruncate` being a `Definition` and not a `Class`, so registering it would be inert; measured both with and without `Existing Instance`, where the unconditional components still resolve to the DIRECT instances), each stripped ONE AT A TIME and compiled alone, guard coverage measured mechanically (65 identifiers inside refutation commands, 55 also outside, the 10 exceptions exhaustive), rename-simulated 6/6 with zero vacuous guards. `make todo` grows by 23, ALL in the probe, the two library files contributing ZERO — so the DoD box is not met as written and is disclosed. Prose corrections made in the same commit, each measured: `Structure/Topos.v`'s "the Sets story is deliberately CROSS-UNIVERSE theorems, not an instance" (now naming both conditional bundles and scoping what they do and do not reach), `Adjunction/GAFT/Sets.v`'s "the tree's only inhabitant of that class" (rewritten LINE-NEUTRALLY, one line for one line with the amendment APPENDED after the last definition, because six in-tree citations name `Sets_HasEqualizers` and a growing header would have moved it), two docs/INHABITATION.md rows, and docs/AXIOMS.md's item 1. **THE PUBLISHED SURVEY CRITERION WAS ITSELF DEFECTIVE AND IS REPAIRED**: both headers had offered `rg -n ':\s*ElementaryTopos' -g '*.v'` as the ground for "`FinSet_Topos` is the only inhabitant", and that pattern is LINE-ANCHORED, so it cannot see a head whose type wraps — which is all three bundles of `Instance/Sets/Topos.v`; the criterion now says "declaration heads whose CONCLUSION is `ElementaryTopos`, swept comment-stripped ACROSS LINE BREAKS", under which the base commit returns three and the shipped tree ten. NOT delivered: no unconditional topos on either side and no impossibility theorem; no `Presheaf_Topos_small`; no comparison between the two toposes and none among the three `Sets` bundles (no `BoolSetoid ≅ Powerset_Omega`, under `IEM` or otherwise); no agreement proof for the derived colimits and no `Complete`/`Cocomplete` from the topos structure; no sheaf reading, slice topos, Lawvere–Tierney topology, internal logic or Giraud; nothing about Yoneda, representables or density; NOTHING COMPUTES TO A NUMERAL at the presheaf topos, which is structural rather than an omission (sieve membership is `Prop`-valued and truncated, so Ω carries no decidable code and there is no analogue of FinSet's `Pow 2 = 4`); and nothing is registered as an `Instance`, all seven bundles being plain `Definition`s on `Instance/FinSet/Topos.v`'s precedent
- **Instance/Fun/Morphisms.v**: Mac Lane §IV.3 printed p. 91 (`maclane:IV.3:remark1`) + Awodey §7.10 Ex 6 (`awodey:7:ex6`) — **monos and epis in a functor category are pointwise**, at the two strengths the printed sentence actually states. For an ARBITRARY target `D`, componentwise monic (epic) implies monic (epic) in `[C, D]` — `pointwise_monic_is_monic`/`pointwise_epic_is_epic`, in a section that never mentions `Sets`, each proof one cancellation applied at the component with NO rewriting, because `[C, D]`'s composition is componentwise (`nat_compose`, `Theory/Natural/Transformation.v`) and its hom-setoid pointwise (`Transform_Setoid`), both definitionally. The BICONDITIONALS `sets_functor_monic_iff_pointwise`/`sets_functor_epic_iff_pointwise` are at `D := Sets`, which is exactly where the page puts them — the clause "for B = Set" governs the whole "if and only if", read off the page image (the raw `pdftotext` of the scan renders τ as "I", `B^C` as "Be" and both Roman numerals as "111", so the quotation is from the image and not the OCR). **The converses are proved by Mac Lane's own route, the pointwise (co)limit computation, and the two functors it needs had to be BUILT**: `KerPair θ : C ⟶ Sets` with object action `x ↦ sets_ker (θ x)` (`Instance/Sets/Pullback.v`) and projections `KerFst`/`KerSnd`, where naturality of θ is exactly what lands the arrow action back in the kernel pair (`ker_map_ok`), and `CokerPair θ` with `x ↦ CKSetoid (θ x)` (`Instance/Sets.v`) and injections `CkLeft`/`CkRight`, naturality carrying an image witness forward (`ck_Im_map`). Left cancellation at `ker_agree` — whose proof is the sigma's second projection — IS injectivity of each component, closed by `injectivity_is_monic`; right cancellation at `ck_agree_nat`, read at `b`, gives the preimage exactly as `epic_implies_surjective` does one level down. **Read what is NOT consumed**: `sets_ck_IsCokernelPair`'s universal property is never spent, only the commuting square `ck_agree`, so `Instance/Sets/CokernelPair.v` is not required and the closure stays at 39 modules — measured, as is the 74 that reusing `Instance/Fun/Terminal.v`'s `Constant_Functor` would cost, which is why a five-line witness-local `const_fun` is declared with the duplication disclosed. No universal property is claimed for either functor in `[C, Sets]`, and no `HasPullbacks`/`HasPushouts` instance at a functor category is built HERE — the pullback half has since landed as Instance/Fun/Pullback.v's `Fun_HasPullbacks` (#403), the pushout half still nowhere; none was declared at `bbddaee2`, by name searches and by a wider sweep of every file mentioning both a functor category and a pullback, pushout or kernel-pair token, whose only hits are limits of a DIAGRAM into a category (`Adjunction/Diagonal/Finite.v`'s `PullbackFunctor : [Roof^op, C] ⟶ C`) and the arrow-category `CokernelPairFunctor` of `Adjunction/CokernelPair.v`. **Awodey's isomorphism half is a predicate on a GIVEN θ**: `nat_iso_iff_pointwise : IsIsomorphism θ ↔ ∀ x, IsIsomorphism (θ x)`, both directions `Defined`, with `componentwise_iso` the backward construction whose inverse `nat_inverse` has component `two_sided_inverse (H x)`. The pre-existing `Functor_Setoid_Nat_Iso` (`Instance/Fun.v`) is the EXISTENTIAL form `F ≅ G ↔ F ≈ G` and cannot ask about a particular θ; the two are related at `eq_refl` on the `transform` FUNCTIONS of both legs (`equiv_iso_to_is_theta`, `equiv_iso_from_is_nat_inverse`, pointwise and as whole functions), while neither whole leg nor the whole isomorphism record is Leibniz-equal — all three refuted and pinned, the donor building both legs with `abstract`ed naturality proofs (`Instance/Fun.v`) — so the difference is confined to opaque LAW fields (each leg's two naturality fields and the record's two inverse laws) and touches no data; the file's first draft located it in the inverse laws alone, and an audit measured the legs. The monomorphism DECISION is stated at its proved strength: componentwise monic ⟹ monic for every `D`; the converse is PROVED at `Sets`, and the header argues without proving that the same argument runs wherever the kernel pair is computed pointwise; at an arbitrary `D` it is neither proved nor refuted, no countermodel being built, so the file does not say "false in general". The presheaf corollaries `presheaf_monic_iff_pointwise`/`presheaf_epic_iff_pointwise` are `:=` of the `Sets` biconditionals at `C^op`, and `presheaf_monic_iff_injective`/`presheaf_epic_iff_surjective` spell out the setoid reading the subobject-classifier work will cite. Universes measured off BOTH binder and block: every general constant is over `C : Category@{u u0 u0}`, `D : Category@{u1 u2 u2}` — hom identified with proof in the BINDERS — with the one block equation `u0 = u2` and both object universes free; attributed by probe to `Fun` (rejected under `Constraint ch < dh` against accepted hom controls) and independently to `Monic`/`Epic` (rejected under `mh < mp`), while `IsIsomorphism` is read off the binder and NOT separately probed. `Set` is carried by exactly 20 of the 74 constants, all in the witness section (16 named constants and 4 of their obligations), inherited from `Instance/Two.v`'s `TwoHom : … → Set` through `Fun`; sections A–D are `Set`-free, both new `Sets`-morphisms writing their `proper_morphism` certificate by hand (the `Theory/Universal/Element.v` hazard). Non-vacuity over `[_2, Sets]`: `two_pick_nat` is monic and provably not epic, `two_collapse` epic and provably not monic, BOTH refutations routed through the converse halves so section B is exercised, the obstruction exhibited at the component `TwoX` by `discriminate`; `two_arrow_Monic`/`_Epic` over `[_2, _2]` exercise the general direction at a non-`Sets` target — all three witness transformations between CONSTANT functors, labelled degenerate for naturality. **74/74 constants closed under the global context** (52 glob heads + 22 `Program` obligations from `Print Module`; no `Record`/`Class`/`Inductive`, so no `Build_*`; no `Fail` in the library file), the gate carrying the 52 heads; zero collisions over all 95 names the two files introduce, after `two_pick` (taken by `Instance/Fun/Terminal.v`) was renamed `two_pick_nat`. `Test/ProbeMorphisms369.v` carries **13 negatives of THREE kinds** — 7 CONVERSION (each functor preserves identities only at `≈`, and the cause is DISCRIMINATED rather than guessed: at the constant-functor witnesses `fmap[P] id` IS `id` and `fmap_id` IS `reflexivity` by `eq_refl` and the law still fails applied to an element, so the blocker is the rebuilt sigma-over-pair and sum-`match`, which have no definitional eta — an audit refuted the first draft's opaque-`fmap_id` explanation; the whole-record comparison above; and its two whole-leg refinements), 1 TYPING (`Monic` in `[C, Sets]` against the pointwise family are different types, a plain mismatch with no `cannot unify`), 5 FORMABILITY (`Fun`, `Monic`, `Epic` and the two general theorems each rejected at hom levels declared apart) — plus an instrument check, each stripped ONE AT A TIME with its whole error read, rename-simulated 9/9 over the target constants the negatives name, with a `Check` guard block for the rest and for the two donor constants that occur only inside a `Fail`; `make todo` grows by 21, all in the probe. Two of the issue's donor citations are stale, for `injectivity_is_monic` and for `surjectivity_is_epic`. NOT delivered: no converse and no countermodel at a general `D`; no universal property for, functoriality in θ of, or identification of `KerPair`/`CokerPair` with `Structure/Regular.v`'s `kernel_pair` or `Theory/Morphisms/CokernelPair.v`'s `cokernel_pair`; no Yoneda route and no measurement of one; no subobject lattice for presheaves — the CLASSIFIER is now Instance/Fun/Classifier.v's `Fun_Classifier` (#403), which cites this file's `presheaf_monic_iff_injective` at three sites (the mediator's respectfulness, its naturality, the universal property's uniqueness clause) and a fourth in its re-annotated `Fun_Classifier_small`; nothing about split, regular or effective epis or balancedness of `[C, D]`; and no witness at a shape other than `_2` or with a non-constant functor
- **Instance/Rep.v**: Linear representations as the functor category (Mac Lane §II.4 construction 3, p. 41; Awodey §7.1's linear-representation reading) — `RepObject K G` over ANY `RingObject` K (nothing below spends commutativity — disclosed) and Instance/Grp.v's `GrpObject`, with the action in the ISSUE-LITERAL form `rep_hom : GrpHom G (UnitsOf (hom_monoid (RMod K) V))` — "a homomorphism into the automorphisms", composed entirely from in-tree pieces (GL.v's `UnitsOf`, Deloop.v's `hom_monoid`) — and the endomorphism-valued working form derived at ZERO cost (`monhom_of_rep`/`rep_of_monhom`, both round trips `reflexivity`; invertibility automatic, inverse = action of g⁻¹). The TWO-GrpObject situation (Deloop.v's vs Grp.v's identically-named records) is bridged by `grp_mon : GrpObject → MonObject` and disclosed in the header. Intertwiners as morphisms; category `Rep K G`; **`Rep_Fun_equiv : [Deloop (grp_mon G), RMod K] ≅[Cat] Rep K G`** — note `≅[Cat]` IS equivalence of categories, Cat's hom-setoid being `Functor_Setoid` (MSet_Fun_equiv's §II.4-construction-2 architecture one door down: a functor is its object-image + action monoid-hom via the `functor_of_hom_monoid` spine, a natural transformation over one object IS a single intertwiner; `rep_of_functor_act`/`functor_of_rep_fmap` definitional). Awodey's degeneracy delivered general-first as ONE statement at three instantiations: `thin_functor_endo_trivial` (in a thin category ANY functor kills ANY endomorphism — neither group nor one-object needed), `thin_group_functor_trivial`, `proset_group_functor_trivial` (at `Proset` via `proset_thin`, the Poset := Proset erratum cited, real posets covered through `PosetAsCategory`; the Proset conclusion degenerates to `True` — disclosed, the Proset/Limit.v pattern). Witness generalized beyond the brief: `sign_rep : RModObject K → RepObject K Z2` for ANY module (negation is always a self-inverse module endomorphism, `rmod_neg_hom`), non-vacuity at `Int_RMod` at BOTH strengths — pointwise (`sign_rep_moves_one` 1 ↦ −1, `sign_rep_nontrivial`, `sign_rep_not_trivial_rep`) and the categorical one a reader expects from "different representations": **`sign_not_iso_trivial`**, no isomorphism in `Rep Int_Ring Z2` between the sign and trivial representations (any intertwiner out of the sign rep vanishes, and 0 ≠ 1); `ab_neg_neg` proved here (Ab.v lacks it — upstreaming candidate). No commutativity, no finiteness, no character theory; FdVect-valued reading stays with #237's file — all scoped in the header
- **Instance/Field.v**: The category of fields (Riehl §1.2 Exercise 1.2.iii + §1.6 Example 1.6.15's field clause) — `Field` over #244's `FieldObject` with ring homs as morphisms (`finv` preservation a THEOREM on nonzero arguments, `field_hom_finv` — at 0 both values are junk and nothing is claimed), `Field_Rng`/`Field_Forget` faithful. The mono determination CONSTRUCTIVELY STRATIFIED per the Grp/Epi.v discipline: `field_hom_distinct` — no field hom identifies distinct elements — is UNCONDITIONAL and axiom-free (Riehl's argument verbatim: f(x−y)·f((x−y)⁻¹) ≈ 1 against `field_one_neq_zero`), while the strict cancellation form and hence `Monic` need ¬¬-STABILITY of `≈` in the source (`FieldStable`; under `f a ≈ f b` the record's guarded `finv_l` yields only `¬¬(a ≈ b)` — `stability_is_the_conclusion` proves the hypothesis IS the conclusion restated, so no cheaper price exists); `field_dec_stable` discharges it from decidable equality, making **`Q_hom_monic` unconditional for every hom out of ℚ** — every object in the tree, ℚ being the only `FieldObject`. `Q_endo_id`: every ring endomorphism of ℚ is the identity (`rng_from_Z_unique` twice + denominator cancellation). The stability analysis is now PROVED, not asserted: `FieldStableAtZero` (¬¬-stability at zero only) is inter-derivable with `FieldStable` and suffices for cancellation (`field_stable_at_zero_suffices`) — LOCATING the obstruction exactly at `finv_l`'s guard. **`F2_Field`** (the two-element field on bool; "characteristic 2" is definitional, `F2_ftwo_zero` by `reflexivity`) makes the category disconnected in both directions (`no_hom_Q_F2`, `no_hom_F2_Q` — no homomorphisms between different characteristics) and delivers Riehl 1.6.15's field clause CONSTRUCTIVELY: **`Field_no_initial`/`Field_no_terminal`** (+ the object-level `IsInitialObj`/`IsTerminalObj` forms) — each a single `field_hom_nonzero` application through the named element `ftwo := 1+1`, no case split, no decidability, no stability consumed (the negative guard on `finv_l` is exactly what the proof holds). All 76 constants Closed (50 source + 2 Faithful instances + 24 Program obligations, enumerated by `Print Module` per the AXIOMS.md methodology)
- **Instance/Roster.v**: Mac Lane's roster of standard large categories (§I.1 construction 2, §I.2 construction 12; Awodey §1.4; Riehl §1.1 Examples 1.1.3/1.1.4) — the single point of reference: 74 Examples exhibiting every roster entry the tree realizes with its evident forgetful functor (Sets/Coq/Ens/EnsT/EnsV/FinSet/Set_f/PointedSets/Part/Par/Rel/CMon/Grp/Ab/Rig/Rng/CRng/RMod/ModR/Bimodule/Vct_F/FdVect/Field/Rep/Matr/Mon_Sets/Proset/Pos/Cat/StrictCat/QuiverCategory/Simplex/Metacategory/Top+satellites/HausdorffSpaces/CompactHausdorffSpaces/Top_pointed/Toph/Toph_pointed/Algs/Group-variety), with **`Mon_Sets := @Mon Sets Sets_Product_Monoidal`** finally naming the internal-monoid category at `Sets` (`Rig_Forget_Mon` already landed there — the reconciliation is a typecheck, `roster_Rig_Forget_Mon`). A dedicated "Initial and terminal objects across the roster" section delivers Riehl §1.6 Example 1.6.15: 22 Examples citing the existing instances (the algebraic entries coincide into zero objects; Sets/Cat differ; Top differs AND has provably no zero object; Field has NEITHER — cross-referenced to Field.v's constructive refutations), with the unital-ring discrimination disclosed (ℤ-initial + zero-ring-terminal exhibited; the Rng-vs-non-unital-Rg non-full-subcategory contrast awaits #362). The axiom footprint is MEASURED per constant (Print Module methodology, obligations included: 96 constants, 90 Closed) and corrected the obvious guesses twice: Top's roster entries are ALL Closed (the reals enter only with the homotopy quotients — 4 `roster_Toph*` constants carry the two reals axioms) and Comp.v's `Algs`/`GroupOp` are Closed (funext is spent in `GroupEq`'s naturality alone — 2 variety constants carry it); the six exceptions are segregated at section tails. Out-of-scope disclosures in the issue's style, with "no third case" as the file's stated contract: cHaus's monadicity story (subcategory exhibited; Forgetful.v stratification + no ultrafilters) (CORRECTION, #455: the stratification does not make the Stone–Čech adjunction unformable, its left adjoint's domain being the higher `Sets` itself, and that adjunction is refuted under informative excluded middle at `CompHaus`'s objects at or above its hom universe, at the instances Instance/Top/StoneCech/Refutations.v's COVERAGE records (the `Top` slot at `h`, the proof slot at `c`, the Hausdorff proof's separating opens at the points' universe `o`); Roster.v's note carries the correction), ringed spaces (per-leg Sheaf predicate), Man, Meas/Measure-a.e. (flagged as a natural FUTURE setoid target — Quotient.v machinery ready), ℕ-recursive-functions (Lambda is STLC, different subject), chain complexes, full first-order Model_T (equational fragment = Algs + Lawvere Models), SIMPLE GRAPHS (only quivers in tree; the clause's interest recorded — a terminal simple graph would need a loop, forbidden by irreflexivity), Awodey's ℝ-and-continuous-maps + open-subsets-of-ℝ categories (R_Top/OpenSub exist as objects/spaces; neither roster CATEGORY assembled, cited in prose so no reals dependency is added), and the derivative-as-Jacobian functor (riehl:1.3:example2's one homeless clause, same root cause as Man). The Poset/Proset erratum stated sharply: `Poset` TAKES an `Antisymmetric` hypothesis and DISCARDS it
- **Instance/Proset/Transform.v**: Natural transformations into a preorder (Mac Lane §I.4 ex. 4) — the existence criterion `(S ⟹ T) ↔ ∀ c, S c ≤ T c` as an `iffT` with both round trips, uniqueness up to `≈`, and hence thinness of `[C, Proset P]`; the pointwise order on functors makes that functor category a proset, identified with it both as `≅[Cat]` and as the sharper `≅[StrictCat]` (identity on objects, so `eq_refl` discharges the object leg); at the naturals this is `[ℕ≤, ℕ≤] ≅[Cat] MonotoneProset`, the preorder of monotone maps ordered pointwise (`≅[Cat]` only — the strict version would need funext for the rebuilt functor-law fields; the on-the-nose content is isolated in `monotone_of_endo_of_monotone` and `endo_of_monotone_of_endo`). The dual is refuted: two transformations out of `(ℕ, ≤)` into `Parallel` proved distinct in the transformation setoid (`proset_out_not_unique`, Seven Sketches Exercise 3.58)
- **Instance/Lambda/**: Full lambda calculus formalization
- **Instance/Proset/Limit.v**: the preorder dictionary made a theorem — in the thin category `Proset P` an object is a limit of a diagram of ANY shape exactly when it is a greatest lower bound of that diagram's objects, and dually for colimits and joins (`proset_limit_iff_glb`, `proset_colimit_iff_lub`, both at a pinned apex), with `Cartesian`/`Cocartesian`/`Terminal`/`Initial`/`HasPushouts` constructors from meet/join/top/bottom data and their converses, the bicompleteness biconditionals `proset_Complete_iff_all_meets` and `proset_Cocomplete_iff_all_joins` (the join half assembled by the adversarial verifier of an earlier revision from the file's own pieces, then landed), and equivalence-invariance of `Complete`/`Cocomplete`; duals are the primal statements read at `op_rel R` (`IsLUB := IsGLB (op_rel R)`), so `lub_unique` IS `glb_unique` at the reversed preorder. The thin-category degeneration is tracked explicitly in the header — every `≈` obligation is `True`, the content is the apex, the legs, and the mediating arrow — and guarded by refutations (`nat_glb_not_4`, `Nat_no_Terminal`, `discrete_two_no_initial`, `discrete_two_no_cartesian`) beside the computing witness `nat_product_3_5 : nat_meet 3 5 = 3%nat` on (ℕ, ≤). No lattice vocabulary is introduced; that is left to `Structure/Lattice.v`.
- **Instance/Theory/Lindenbaum.v**: Mac Lane §IV.6 Exercise 2 (book p. 98, `maclane:IV.6:ex2`) — **the sentences of an elementary theory, preordered by entailment, form a CARTESIAN CLOSED category, with product conjunction and exponential implication**: `Lind T := Proset (entails_PreOrder T)` with `Lind_Terminal`, `Lind_Cartesian` and `Lind_Closed`. The syntax `Sent V` is deliberately MINIMAL — an atom former, truth, conjunction, implication, nothing else — which is exactly what the exercise needs, and the header says in terms that quantifiers, equality, signatures and substitution are OUT OF SCOPE and that `Sent` is NOT proposed as a model of a first-order theory; a theory is a bare set of sentences `Theo V := Sent V -> Prop` with no deductive closure, consistency or inhabitation required. **THE DESIGN DECISION IS THAT THE DEDUCTION THEOREM IS A CONSTRUCTOR**: `Entails T` is an inductive family **in `Prop`** — which is what lets `Instance/Proset.v`'s `Proset` accept it, that donor taking a stdlib `relation A` — with nine natural-deduction rules, `ent_curry` (`T, p ∧ q ⊢ r` gives `T, p ⊢ q → r`) among them, where a Hilbert presentation would make it a metatheorem needing an induction; the header records the choice, and **no rule is claimed underivable from the rest** — that would need a countermodel apiece and none is built. **WHICH RULES THE FOUR STRUCTURES CONSUME IS MEASURED BY A DROP EXPERIMENT, NOT READ OFF THE SOURCE** (each constructor deleted in turn, the file truncated before the soundness section, recompiled, first unresolved site recorded): `ent_refl` and `ent_cut` ARE the preorder (hence `id` and `compose`), `ent_top` is `one`, `ent_pair`/`ent_fst`/`ent_snd` are `fork`/`exl`/`exr`, `ent_curry` is `to exp_iso` and `ent_eval` is consumed through `lind_uncurry` — so the four structures need **exactly EIGHT of the nine**, and the ninth, `ent_ax`, is consumed by NONE of them (sections A–D compile unchanged without it), its whole job being the two-theory separation. **EVERY ONE OF THE 14 `Program` OBLIGATIONS IS FREE**, discharged by the default tactic with no proof written: thinness (`lind_thin` is `fun _ _ _ _ => I`, `Proset`'s hom-setoid being `fun _ _ => True`) makes uniqueness of the transpose vacuous, and it is NOT proved. The one derived rule with content is `lind_uncurry`. **THE BRIEF'S EXPECTATION ABOUT `eval` WAS HALF WRONG AND THE FILE MEASURES IT**: `curry` IS `ent_curry` on the nose (`lind_curry_is_rule`, `eq_refl`), but `eval` is NOT `ent_eval` — `Structure/Cartesian/Closed.v` defines `eval' := uncurry' id`, so it computes to `lind_uncurry (ent_refl …)` and agrees with `ent_eval` only up to `≈` by thinness, refuted at `eq_refl` and pinned with an `≈` control beside it; the terminal, product and exponential identifications are `eq_refl` (`lind_terminal_is_top`, `lind_product_is_and`, `lind_exponent_is_imp`). **THE REVIEWER CHECK NEEDS A NON-DERIVABILITY PROOF AND BOTH HALVES SHIP**, over `V := bool`: `ent_soundness` is the induction over derivations against `sdenote` (`s_top ↦ True`, `s_and ↦ /\`, `s_imp ↦ ->`), whose only case consuming the hypothesis on `T` is `ent_ax`; `not_entails_empty` refutes `Entails T_empty (sv true) (sv false)` by soundness at the separating valuation then `discriminate`, while `entails_mp` derives it at `T_mp` — and that derivation is CATEGORICAL, pairing the identity with itself and uncurrying the axiom through `lind_uncurry`, so modus ponens is read off the cartesian closed structure rather than assumed. **THE SEPARATION IS THEN STRENGTHENED BEYOND EXHIBIT-AND-REFUTE**: `no_full_functor_empty_to_all` shows NO full functor whatever exists from `Lind T_empty` to `Lind T_all`, independently of any chosen comparison, whence `no_equivalence_empty_to_all` through `Equivalence_Full` — the two categories are not equivalent, which is the strongest reading of "two different axiom sets give two different categories"; the pair of `Theory/Equivalence` requires costs 4 modules (closure 23 → 27), of which `FullFaithful`'s own marginal cost is 1 — dropping it alone gives 26 — the other 3 being `Theory/Equivalence`'s, which is required anyway for `EssentiallySurjective` (`Theory/Equivalence.v`, consumed at `Lindenbaum.v`). **THE `Props` COMPARISON IS MEASURED GRADE BY GRADE AND TWO GRADES ARE REFUTED**: `LindSound T val HT : Lind T ⟶ Props` has all three functor laws free and preserves the terminal object, the product and the exponential **on the nose** at Leibniz equality of objects (`sound_terminal`/`sound_product`/`sound_exponent`, each `eq_refl`), is **Faithful only VACUOUSLY** — and the vacuity is COMPILED rather than asserted, `lind_any_Faithful` proving every functor out of `Lind T` faithful because `fmap_inj`'s conclusion is `True`, with `LindSound_Faithful` literally that term — and is **NOT Full** (`sound_not_Full`: fullness would return a derivation `not_entails_empty` refutes, so it is completeness with respect to one valuation, strictly stronger than the exercise asks) and **NOT essentially surjective** (`sound_not_EssentiallySurjective`, through `sound_misses_False`: every `sdenote val_true p` is inhabited, so nothing in the image is isomorphic to `False`), hence not an equivalence. **Read its quantifier precisely: `LindSound` is a FAMILY INDEXED BY VALUATIONS, not one functor** — at the empty theory every valuation qualifies vacuously — and both refutations are at the specific `val_true`, nothing being claimed for arbitrary valuations. **Universes measured off BOTH binder and block over all 81 constants: NOT ONE constraint block contains a universe EQUATION**, every entry being `<`, `<=` or a stdlib bound, while the hom = proof identification sits ENTIRELY IN THE BINDER — `Lind@{u u0 u1} : … -> Category@{u0 u u}`, with `Terminal@{u0 u}`, `Cartesian@{u0 u}`, `Closed@{u1 u0 u}` and `Functor@{u1 u u u0 u u}` below it — so reading the block alone reports no identification and is wrong; the identification is INHERITED from `Proset` and is pinned by two formability negatives, the second of which names `Proset` with **no `Lind` in the command at all**, which is what makes the attribution discriminate. `sdenote@{u}`'s block is LITERALLY EMPTY; `Set` appears in 67 of the 79 blocks and ALWAYS as the strict lower bound `Set < …` (from `Prop : Type@{Set+1}` through `Theo`), with **zero `Set =` anywhere and nothing pinned to `Set`**; `Lind`'s own hom universe `u` occurs in no constraint of its block, the hom type being `Prop`-valued so the object universe does not bound it; `Lind_Closed` adds the one strict `u0 < u`, which is `Sets`' own declaration through `exp_iso`. **81/81 constants closed under the global context** — 68 `Print Module` names (which wraps the `Inductive Entails` head onto its own line, so a line-anchored sweep of its output returns 67 and misses exactly that name) plus the 13 inductive constructors, each queried fully qualified, zero `Axioms:` lines — of which the gate carries all 81. Zero collisions over 97 names (instrument-checked at `Full` = 93 files and `Monoid` = 91), after FOUR were renamed away before landing: `denote` (`Instance/AST.v`), `soundness` (`Instance/Lambda/Sound.v`), `Weaken` (`Theory/Metacategory/General.v`), and `Sound`, which is a MODULE short name the `print-assumptions` gate loads into one scope. Closure **27** excluding self (probe 28); two `Defined` tokens, of which **exactly one is load-bearing** by flipping each ALONE to `Qed` — `entails_weaken`, whose flip leaves the target compiling and BREAKS THE PROBE's `eq_refl` readback, while `entails_mp` flips with no effect anywhere measured and is `Defined` by the data convention only. An ENGINEERING FINDING recorded in-file: `Lib.v` sets `#[export] Set Uniform Inductive Parameters` development-wide, so inside `Inductive Sent (V : Type)` the name `Sent` already MEANS `Sent V` and writing `s_var : V -> Sent V` is rejected with `Illegal application (Non-functional construction)` — an error that names the parameter but neither the flag nor the cause, reporting `Sent` as a non-functional term applied to `V` without saying that `Sent` has already absorbed it. `Test/ProbeLindenbaum390.v` mirrors the target's `Require` list and carries **9 `Fail`s = 1 instrument + 8 negatives of THREE kinds told apart by the error TEXT** — 3 FORMABILITY (ending in a universe clause; one of them, a `Type`-valued entailment offered to `Proset`, READS like a typing mismatch but closes with `Cannot enforce … <= Prop`, and is classified by its tail rather than its look), 2 TYPING (no universe clause and no `cannot unify`), 3 CONVERSION — each stripped ONE AT A TIME, compiled alone and its whole error read, with **65 `Check` commands** naming every constant any negative mentions outside every `Fail`, and rename-simulated **12/12** — the nine `Fail`s name 18 LIBRARY-LEVEL identifiers — probe-local names (`TEnt` and the five `probe390_*`) and bound variables aside — of which SIX are DONORS that cannot be renamed in the target (`Category`, `Proset`, `Relation_Definitions.relation`, `product_obj`, `exponent_obj`, `eval`), so 12 is the renameable set; renaming each in the TARGET ONLY (a whole-file rename is a no-op by construction and gives a false verdict) breaks the probe at all twelve sites, every one a `Check` and none a `Fail`. `make todo` grows by 11, all in the probe (9 `Fail` commands + 2 header prose lines), zero in the target. NOT delivered: no first-order syntax, so Mac Lane's "elementary theory" is realised at PROPOSITIONAL strength only; **no Lindenbaum–Tarski ALGEBRA** — `Lind T` is a preorder and not a poset, inter-derivable sentences being isomorphic objects rather than equal ones, with no quotient and no `Skeleton`, so the classical Heyting algebra is not obtained; NOT shown bicartesian closed (the syntax has no `⊥` and no `∨`, so no `Initial` and no `Cocartesian`, unlike `Instance/Props.v`, which has both) — a scope choice rather than an obstruction; no completeness, fullness of the `Props` comparison being refuted and not repaired, with no investigation of whether quantifying over all valuations recovers it; no universal property, `Lind T_empty` not being exhibited as the free cartesian closed category on `V` and no comparison made with `Construction/FreeMonoidal.v` or the PROP machinery; **`T ↦ Lind T` is NOT a functor** — `LindWeaken` exists for a given inclusion of theories but no identity or composition law for it is proved, so there is no functor from a category of theories; the Hilbert-system remark in the header is PROSE, no Hilbert calculus being built and the agreement of the two presentations therefore unverified; no independence results; and no decidability, cut elimination or normal form for `Entails`.
- **Instance/FinSet/Skeleton.v**: The skeleton equivalence of Mac Lane CWM §I.4 — `Set_f`, finite setoids with a fully faithful (but not injective-on-objects, hence not sub-) functor to `Sets`, whose objects CARRY their counting (a chosen `θ_X : X ≅ Fin.t n` in `Sets`, data not an assertion, so the cardinal functor needs no choice principle); the inclusion `FinSet_Incl` shown full+faithful+essentially surjective and the equivalence obtained through `FF_ESO_Equivalence` rather than by hand, with `Card` (Mac Lane's `#`, `# f = θ_Y ∘ f ∘ θ_X⁻¹`) agreeing with the produced quasi-inverse up to conversion. Awodey §7.8's asymmetry is proved in both directions: `Card ◯ FinSet_Incl` is the identity by `eq_refl` on both maps with the unit's components identity at every object, while `FinSet_Incl ◯ Card` is only naturally isomorphic to it — witnessed at `two_swap`, the canonical `Fin.t 2` carrying the transposition as its chosen bijection. Skeletality (`FinSet_skeletal : m ≅ n → m = n`, by an enumeration/counting argument, the tree's first statement about isomorphisms of `FinSet`) and Fong–Spivak §3.2.5 setf_cardinality as a complete isomorphism invariant (`setf_cardinality_iso_invariant`, `setf_cardinality_complete`); `parity_two` — `nat` under equality of parity — is an object of setf_cardinality 2 with an infinite carrier
- **Instance/FinSet/Limit.v**: Mac Lane §V.1 Exercise 4 (book p. 112, `maclane:V.1:ex4`) — **`FinSet` is finitely complete, with a NATIVE, COMPUTING equalizer**. `FinSet_HasEqualizers : HasEqualizers FinSet` is the counted sub-object of agreement in Instance/FinSet/Classifier.v's codec: predicate `finset_eq_pred f g x := fin_eqb (f x) (g x)`, object `FinSet_equalizer_obj := fin_countP _`, inclusion `fin_select _`, mediator `fin_rank _` (`FinSet_equalizer_med`), the universal property `FinSet_IsEqualizer` closed by `fin_select_sat`/`fin_select_rank`/`fin_select_inj` with `fin_eqb_eq`/`fin_eqb_refl` — strictly simpler than `FinSet_Pullbacks` (no pair codec); before this file NO constant in the tree had type `HasEqualizers FinSet` (grep for the phrase: 0). `FinSet_FinitelyComplete : @FinitelyComplete FinSet` is the QUANTIFIED statement of Structure/Limit/Finite.v (a `Limit` for every functor out of every `FiniteCategory`-witnessed shape, the issue reviewer's reading, not a bundle of shapes), as ONE application of #417's `finitely_complete_from_generators FinSet_Terminal FinSet_Cartesian FinSet_HasEqualizers` — FinSet's OWN numeral-`1` terminal, numeral-`m * n` products and the native equalizer. **THE GENERAL MACHINERY COMPUTES AT `FinSet`**: seven `eq_refl` examples — five at one pair `3 ⇉ 2` agreeing at two points (object `2`, both inclusion values, the class's readback, the mediator), and the two headline conversions — the terminal object recovered from the EMPTY finite limit, `terminal_obj (FinitelyComplete_Terminal FinSet_FinitelyComplete)`, IS `1` by `eq_refl` (`FinSet_FinitelyComplete_terminal_computes`), and the finite limit over the SHAPE `Parallel` at `APair f g`, built by `finite_limit` from three finite products and two equalizers, IS `2` (`FinSet_FinitelyComplete_parallel_computes`); both MEASURED against the pullback route (`finitely_complete_of_pullbacks_terminal FinSet_Pullbacks FinSet_Terminal`, `HasEqualizers_of_HasPullbacks_Terminal FinSet_Pullbacks`), which is REFUSED at `eq_refl` on both (the topos-derived `topos_HasEqualizers` at `FinSet_Topos` being the same term, read back at `eq_refl` in the probe), `lazy` leaving a `fin_countP` over `unique_obj (FinSet_Pullbacks_obligation_2 …)` — the first computing witness of finite completeness in the tree, correcting the Finite.v bullet above. Relation to what existed: Test/ProbeFinite417.v carried `FinSet_FinitelyComplete` at probe strength by the pullback route; it is renamed there `FinSet_FinitelyComplete_pb` (the only collision over the 19 names: one definition, three uses) and its N10 note now names the route. The issue's "Current state" is stale on three re-measured points: `FiniteCategory` exists (#417); Structure/Topos.v, Structure/Regular.v and Structure/Pullback.v all now say the generator reduction IS formalized, so the only "disclosure" edits are TWO line-neutral sentences, Structure/Pullback.v and the like-worded Structure/Topos/Monadic.v, each now pointing its scoped-out general claim at Finite.v's `FinitelyComplete`; the standing premise was "no equalizer", and `qualizer` under Instance/FinSet* was one prose mention (Subsets.v). Instance/FinSet/Pushout.v is NOT required (it re-declares eleven Classifier.v codec names at top level; measured list in the header). **Universes off BOTH binder and block over all 19**: ten carry an EMPTY universe binder; `FinSet_equalizer_med@{u}` one free level; `FinSet_HasEqualizers@{u u0 u1 u2 u3} : HasEqualizers@{u u0} FinSet@{u u0 u1 u2}` and `FinSet_IsEqualizer` carry exactly `Set < u1`, `FinSet`'s own bound (`FinSet@{u u0 u1 u2} : Category@{u u0 u0}` with `Set < u1`, measured), inherited; `FinSet_FinitelyComplete@{u … u10} : FinitelyComplete@{u u0 u1 u2 u3}` carries thirty-two constraints (`Set < u4`, `u1 < u`, `u2 < u`, stdlib caps); NO universe equation in any of the 19 blocks; exactly SEVEN blocks carry a word-bounded `Set`, every one the strict lower bound `Set < _` inherited from `FinSet`, none a pin. **19/19 constants closed under the global context, zero `Axioms:` lines** (10 `Definition`, 1 `Lemma`, 1 `Instance`, 7 `Example`, no `Program`), all gated fully qualified; one `Defined`-terminated proof (`FinSet_IsEqualizer`), whose flip to `Qed` leaves the library and every conversion example green and breaks only the probe's universe control (the instance's universe instance drops from five levels to four) — kept `Defined` by the data convention; closure 73 excluding self, Finite.v's 66 dominating (dropping it leaves 36), Classifier.v costing 4, the other eleven `Category.*` `Require`s 0 (thirteen in all). `Test/ProbeFinSetLimit415.v` mirrors the `Require` list and carries **6 refutation commands = 1 instrument + 5 negatives of THREE kinds told apart by the error text** — 2 CONVERSION (the pullback route's equalizer object against `2` and its terminal against `1`, each beside the native `eq_refl` and a `terminal_unique` isomorphism), 2 TYPING (`FinSet_HasEqualizers : HasEqualizers Sets`, plain has-type; `FinSet_FinitelyComplete : Complete FinSet`, has-type with a binder-comparing unification clause, beside `Complete_FinitelyComplete`), 1 UNIVERSE (`FinSet_HasEqualizers@{_ _ Set _ _}`, "Cannot enforce Set < Set", beside the bare instance) — each stripped one at a time in a copy of the whole file; readbacks at `eq_refl` of the class's object, inclusion and universal property, of `FinSet_FinitelyComplete` against its `:=`, and counts `0` (never agree) and `3` (equal maps); guard coverage 19 inside / 15 outside under the plain tokenization, the four exhaustive exceptions the sort `Set` (named only by the universe negative), two refuted `Example` names and the absent name; rename-simulated 4/4 with every break on a control `Example`, none inside a refutation command; `make todo` grows by the probe's 6 refutation commands only. NOT delivered: `Complete FinSet` (false as stated, not refuted); a native coequalizer or `FinitelyCocomplete FinSet` beyond the #417 probe's pushout route; any comparison of `finite_limit`'s chosen products or equalizer with `FinSet_Cartesian`/`equalizer` beyond the two computing examples; a bridge to `FinSet_Topos`'s generators; closed formulas `fin_countP (fun _ => true) = n` or `FinSet_equalizer_obj f f = m`; naturality of the mediator; nothing registered as an `Instance` except the hypothesis-free `FinSet_HasEqualizers`
- **Instance/Sets/Pointed.v**: `Set_*`, the category of pointed setoids and basepoint-preserving maps (Mac Lane §I.7) — the one-point object is a genuine `ZeroObject` (`zero_coincide := iso_id`); monic ⟺ injective (probed with a two-point object, since the singleton is the zero object and detects nothing) and epi ⟺ surjective (a pointed cokernel-pair gluing `Y + Y` along the image by a closed-form relation — no inductive closure, no decidability, no choice: `pt Y ≈ f (pt X)` is exactly what makes the right leg pointed); Mac Lane's Proposition I.7.2 with its constructive statuses separated — **`pointed_balanced` (invertible ⟺ monic ∧ epi) is unconditional in both directions**, while monic ⇒ split monic needs decidable image membership and epi ⇒ split epi needs decidable equality plus an enumeration (in Set "every epi splits" IS the axiom of choice, so the hypotheses are the honest content, not a formalization gap). Satellites: the `Part` equivalence proving `Instance/Coq/Par.v`'s header prose **for the setoid variant** — that header is about `Par` over `Type`, the theorem about `Instance/Sets/Par.v`'s `Part` over setoids (`Part_to_Pointed` full and faithful unconditionally; essential surjectivity scoped to a global basepoint-decidability hypothesis via the apartness sub-setoid, and the header's "not isomorphic" half is not formalized at all, Pointed/Part.v); `Pointed_Coslice_iso : PointedSets ≅[Cat] Coslice Sets SetsOne`, turning Construction/Slice.v's header prose into a theorem — note that `≅[Cat]` in this library IS equivalence of categories (Instance/Cat.v), so this is NOT Awodey §1.6's on-the-nose isomorphism, which would have to be stated in `StrictCat` and is obstructed by a function-extensionality step the library declines (Pointed/Coslice.v); and the finite witness discharging both decidability packs with computed retractions and sections (Pointed/Finite.v). The two splitting hypotheses are shown NECESSARY in-tree, not merely cited: dropping either yields informative excluded middle (`unconditional_monic_split_gives_IEM`, `unconditional_epic_split_gives_IEM`)
- **Instance/Top.v**: The category of topological spaces — spaces as setoids of points with a predicate-lattice topology (`IsOpen` over ≈-respecting predicates, arbitrary unions as dependent sums, the load-bearing `open_const`), continuous maps, `Top : Category`; discrete/indiscrete constructors with the constructive disclosure that indiscrete opens are the UNIFORM predicates (the `{∅, X}` presentation is not union-closed constructively); one-point terminal and empty initial, with `Top_no_zero_object` proving the absence of a zero object; monic ⟺ injective by point probes and the unconditional epi ⟺ surjective by an explicit cokernel-pair space (closed-form relation, no inductive closure, no decidability, no choice, in-universe), with the textbook two-point indiscrete probe recorded separately as decidability-scoped per the constructive-status question; Seven Sketches covers (`Covers`, `covers_nothing`); Hausdorff and compact-Hausdorff full subcategories, with the separation predicate exercised both ways (`Bool_Discrete_Hausdorff`, `TwoPoint_Indiscrete_not_Hausdorff`) and continuity shown load-bearing (`indiscrete_to_discrete_id_not_continuous`). Closed sets in Instance/Top/Closed.v: a closed set carries its complementing open as DATA with the setoid comparing the open (the constructive rendering of Riehl 1.3.14(iv)/1.4.4(v), disclosed in the header), the thin-category duality `Opens X ≅[Cat] (Closeds X)^op`, and the complementation natural isomorphism `OpensF ≅ ClosedsF` between the open- and closed-set functors `Top^op ⟶ Sets`, naturality being that preimages commute with complements; the degeneracy of the open-comparing setoid is disclosed and priced exactly — a predicate-comparing setoid is equivalent to double-negation elimination (`pred_comparison_forces_DNE`), with the classical correspondence recovered under that explicit hypothesis — and Mac Lane's §II.2 presheaf of continuous real-valued functions in Instance/Top/Presheaf.v: the real line `R_Top` as a ball space (metric topology, radius-as-data), open subspaces `OpenSub U` over `Opens X` with a properness-carrying topology (axiom-free), with the subspace-topology identification machine-checked (`sub_ext_recovers`/`sub_open_of_open`: subspace opens = opens of X contained in U), and `ContinuousPresheaf X : Presheaf (Opens X) Sets` by restriction-as-precomposition, with constant sections and the global-sections identification `global_sections_iso : Γ(X) ≅ C(X,ℝ)`; Theory/Sheaf.v's `Presheaf` alias is universe-generalized so a small site can take values one level up; per-constant axiom audit in docs/AXIOMS.md (29 closed, 21 carrying the two stdlib reals axioms, none needing LUB). Mac Lane §II.3 Ex. 5 rides the same infrastructure (Instance/Top/ContinuousRing.v): the pointwise algebra of continuous real-valued maps — the algebra-of-continuous-functions gap the Presheaf work flagged — with continuity of pointwise + and · exhibited as unions of finite intersections of ball preimages (the only shape an abstract topology admits; product radius shrunk by the local bound, capped at 1), `CRingOb X : RingObject` = C(X), precomposition a ring homomorphism, and `ContinuousRingFunctor : Top^op ⟶ Rng`; Awodey §7.2's functoriality-from-representability is NOT deliverable in-tree — an internal ring object ℝ needs products in Top, which don't exist yet — so the carrier is structurally the hom-setoid (`rig_setoid` IS Presheaf.v's `Maps_to_R`) but operations are pointwise from R's scalar ring and the functor laws are checked by hand, the obstruction disclosed in the header with general representable lifting deferred to #341; 38 constants: 36 carrying the two stdlib reals axioms, 2 (`CRing_precomp`'s zero/one-preservation obligations) `sig_forall_dec` alone (AXIOMS.md, measured). The underlying-set functor and its adjoint triple Discrete ⊣ Forget ⊣ Indiscrete (Mac Lane §V.9, Riehl Example 4.1.6, Seven Sketches 7.28/7.29) live in Instance/Top/Forgetful.v, delivered across a machine-verified universe stratification the header proves out: Top's homs are strictly above its points and every `Functor` pins codomain hom-level ≥ domain's, so the forgetful functor into the points-level `Sets` is elaborator-rejected and the packaged `Adjunction` records are unformable at every level. Delivered instead, Classifier.v-style: `Top_Forget : Top@{h o} ⟶ Sets@{h so}` via `Setoid_Lift` (with `Top_Forget_Faithful`), `Top_Discrete`/`Top_Indiscrete : Sets@{o so} ⟶ Top@{h o}` at the natural level, and the two adjunctions as cross-universe transposition isomorphisms `discrete_adj`/`indiscrete_adj` — identity on underlying maps, the transposes being exactly the in-tree `out_of_discrete_continuous`/`into_indiscrete_continuous` (so Seven Sketches Ex 7.29 costs no separate obligation) — with all eight naturality squares, packaged as `discrete_forget_indiscrete_triple`; RAPL/LAPC shape-wise with raw vertex-legs-coherence competitors (`Forget_preserves_limit_cone`/`Forget_preserves_colimit_cocone` — the limit-cone predicates `IsLimitCone`/`IsColimitCocone` and the `Cocone` record pin shape-hom = ambient-hom (bare `Cone` only imposes ≤), a second disclosed stratification that also blocks `FCocone`/`PreservesColimitCocone` across the gap); and Exercise 2 (`maclane:V.9:ex2`) at two strengths — `indiscrete_image_not_colimiting` (the indiscrete image of the coproduct `1+1=2`, taken over `DiscreteCat bool` since `Two_Discrete`'s hom level is pinned at Set; the would-be mediator to `Bool_Discrete` is the non-continuous indiscrete→discrete identity, refuted through `singleton_true_open`) and `indiscrete_no_right_adjoint`, which quantifies over the adjunction-shaped data any right adjoint would provide (object map + A-natural transposition bijection — strictly less than functoriality) and still concludes False
- **Instance/Top/CompHaus.v**, **Construction/Subcategory/Creation.v**: Mac Lane §V.1 Exercise 2 (book p. 112, `maclane:V.1:ex2`) — **compact Hausdorff spaces, the bijection theorem under an explicit decidability hypothesis, the truncated product topology as the measured furthest point of Tychonoff, and the creation question stated — with a new generic lemma that a full subcategory closed under limits is created into by its inclusion**. THREE PREMISES OF THE ISSUE WERE STALE, re-measured: `Top`, `IsCompact`, `IsHausdorff`, `CompactHausdorffSpaces` (Instance/Top.v, whose own header scoped this theory out) and `CreatesLimit` (#406) all exist, so `CompHaus : Category := CompactHausdorffSpaces` is an ALIAS (`eq_refl` readback) with `CompHaus_Incl` (`Full`, `Faithful`) and `CompHaus_Forget := Top_Forget ◯ CompHaus_Incl` (`Faithful`). **THE BIJECTION THEOREM**: for `f : X ~{Top}~> Y` with a setoid inverse, `X` compact, `Y` Hausdorff, `inverse_open_of_decidable U HU (dU : ∀ x, U x + ¬U x) : IsOpen Y (fun y => U (g y))` — the classical argument covers `X` by `U` and the `f`-preimages of Hausdorff-separating opens around `f c` for `c ∉ U`, which DECIDES membership in `U` (`sep_cover_covers`; `nbhd_inside` uses `dU` a second time, deciding `U (g y')` before the contradiction), takes `IsCompact`'s finite subcover as a list, and intersects the matching separating opens (`nbhd`); `List.In` is eliminated only into `False`; no double-negation elimination is needed (the Hausdorff ⟹ T0 worry of Instance/Top/Kolmogorov.v does not arise) — then `inverse_continuous_of_decidable (dec : all opens decidable) : Continuous Y X g` and `compact_hausdorff_bijection_iso dec : X ≅[Top] Y`. CAVEAT disclosed, not claimed necessary: `dec` for all opens holds for no INHABITED in-tree space (`Discrete_Top`'s opens are all the `≈`-respecting predicates, Instance/Top.v; the empty `Empty_Top`, Instance/Top.v, satisfies it vacuously and instantiates both theorems, measured by an audit); the per-open lemma is the form with non-vacuous content, applied in the probe at the one-point space. **THE TRUNCATED PRODUCT**: `Top` has no products (the natural product-openness predicate is refused at the points' level, Test/ProbeCoproduct.v N9); the `Powerset_squash` route Instance/Top/Coproduct.v named but did not try IS tried: `sq_open W := W respects ≈ ∧ ∀ p ∈ W, squash (box datum inside W)` fits at `Type@{o}` and `Top_sq_product X Y : TopSpace@{o}` closes all five fields, with continuous projections `Top_sq_fst`/`Top_sq_snd` (points read back at `eq_refl`); WHAT STOPS, compiled in the probe: the pairing's continuity would eliminate the squash into the `Type`-valued `IsOpen Z` (refused, "Cannot enforce o <= Prop"; elimination into `True` accepted), and a union over all boxes has its index at `o1 > o` (refused; a points-indexed union accepted) — so no universal property is ESTABLISHED here (the two routes tried are refused; it is not shown that every encoding is refused — Instance/Top/Coproduct.v records the same over-read in an earlier draft) and Tychonoff is not attempted (expected behind the same wall by the same argument, asserted rather than compiled); a measured refinement of Coproduct.v's note. **CREATION**: the pinned `CreatesLimit K CompHaus_Forget` IS FORMABLE for any shape (Instance/Top/Forgetful.v's note generalized its stratification claim to `Top_Forget` by name — false there, since `Top_Forget` lands in `Sets@{h so}` at `Top`'s hom level; corrected in place, line-neutrally, in this commit) — its PROOF is not delivered (`creates_lift` needs a compact Hausdorff apex = Tychonoff + Hausdorff of the lift; `creates_reflect` is the fork wall). New generic donor `Construction/Subcategory/Creation.v`: `ClosedUnderLimits S := ∀ J K N, IsLimitCone N → sobj C S vertex_obj[N]`; `sub_lift` (same apex, same legs, `eq_refl` readbacks), `sub_cone_iso` (identity), `sub_ReflectsLimitCone` via `ff_reflect_ump` (inclusion full + faithful, the Reflective/Limit.v route), `sub_CreatesLimit K : CreatesLimit K (Incl C S)`, `sub_CreatesAllLimits`, `sub_Complete : Complete C → Complete (Sub C S)` (Mac Lane V.4 Thm 2, second half) — applied at `CompactHausdorff_Subcategory` as the CONDITIONALS `CompHaus_Incl_CreatesLimit closed K` and `CompHaus_Complete_of closed HT`, both hypotheses unwitnessed (`Top` has no limits beyond the terminal object; no `Top_Complete`) (CORRECTION, #455: at the shapes the adjoint functor theorems use, `HT` is REFUTED, not merely unwitnessed: under informative excluded middle `Complete Top@{h o}` is false at every shape universe at or above its hom universe (`Top_not_complete_IEM`), and so is `Complete CompHaus` at every shape universe at or above its hom universe (`CompHaus_not_complete_IEM` and `CompHaus_not_complete_IEM_below`, Instance/Top/StoneCech/Refutations.v), at the instances that file's COVERAGE records: the `Top` slot at `h`, the proof slot at `c`, the Hausdorff proof's separating opens at the points' universe `o`; below the hom universe neither is refuted under `IEM`, the index forms applying there given an index, and `closed` stays unwitnessed). **Universes off BOTH binder and block over all 57**: ZERO word-bounded `Set`; `Top_sq_product@{o o1}` with the single bound `o < o1`; the bijection constants carry `u3 = u1` (the two list universes; five constants, `nbhd_inside` printing it as `u1 = u3`) and `u9 = u8` (the open's level is the carrier's; two), the other forty-three of the 48 none; Creation.v's `sub_lift`/`sub_lift_apex`/`sub_lift_leg`/`sub_cone_iso`/`sub_CreatesLimit` carry five equations — `u0 = u11` (ambient hom = shape hom, the `IsLimitCone` pin, inherited) and `u0 = u13` (ambient hom = `Sub` hom, imposed by `Compose` in `Incl C S ◯ K`) and `u5 = u12`, `u6 = u14`, `u9 = u10` (the closure hypothesis consumed at the diagram's universe instance) — `sub_ReflectsLimitCone` the first two, `ClosedUnderLimits`/`sub_CreatesAllLimits`/`sub_Complete` none. **57/57 constants closed under the global context, zero `Axioms:` lines** (48 + 9; no `Program`), all gated fully qualified; seven `Defined`-terminated proofs, FIVE load-bearing by flipping each alone (`Top_sq_product` breaks the library at `sq_fst_continuous`'s statement, `sq_fst_map`/`sq_snd_map` inside the projections' continuity proofs; `compact_hausdorff_bijection_iso` the probe's forward-map readback; `sub_lift` its own readbacks), `sq_fork_map` and `sub_cone_iso` kept `Defined` by the data convention; closure 52 excluding self (Creation.v costs 10, Powerset.v 3, Forgetful.v 1, the other sixteen `Require`s 0), Creation.v's own 35; zero collisions over the 57 names (`sep_`-prefixed cover constants). `Test/ProbeCompHaus413.v` mirrors the `Require` list and carries **5 refutation commands = 1 instrument + 4 negatives of THREE kinds told apart by their messages** (N1 and N2 both say "universe inconsistency"; "expected to have type Prop" against "because o < …" separates them) — N1 SORT/TYPING (squash elimination into `IsOpen Z`, "has type Type … expected Prop", clause "Cannot enforce o <= Prop"), N2 UNIVERSE (the all-boxes union's index above `o`), N3 TYPING (`CompHaus_Forget : CompHaus ⟶ Top`, plain has-type), N4 TYPING (the isomorphism without `dec` has a function type) — each stripped one at a time in a copy of the whole file; the POSITIVE formability of `CreatesLimit K CompHaus_Forget`; readbacks at `eq_refl` (alias, inclusion, projections, forward map, lift's apex and legs); one-point non-vacuity of the per-open lemma; guard coverage 31 inside / 29 outside under the plain tokenization, the two exhaustive exceptions the absent name and one bound variable; rename-simulated 7/7 with every break on a `Check` line, none inside a refutation command; `make todo` grows by the probe's 5 refutation commands only. NOT delivered, in the issue's terms: (a) Tychonoff at any generality (not even the binary product's universal property); (c) the proof of creation by the forgetful functor; (d) `CompHaus_Complete` unconditionally (CORRECTION, #455: at the adjoint functor theorems' shapes it is refuted under informative excluded middle, at the instances Refutations.v's COVERAGE records, not merely absent; the Stone–Čech bullet below); a `Cartesian Top`; compactness of a subset; the closed-map form of the bijection theorem; any necessity result for `dec`; any witness of `ClosedUnderLimits`, or an inhabited witness of `dec` for all opens; nothing registered as an `Instance`; Stone–Čech and the ultrafilter monad stay as Instance/Roster.v leaves them (CORRECTION, #455: no longer so for Stone–Čech. The left adjoint to `CompHaus_Forget` is refuted under informative excluded middle, at the instance with `CompHaus`'s objects at its hom universe and at one with them above it (`StoneCech_adjunction_refuted_IEM`, `StoneCech_adjunction_refuted_IEM_above`), each at the instances Refutations.v's COVERAGE records; the left adjoint to `CompHaus_Incl` is not refuted and holds classically, only its adjoint-functor-theorem route being vacuous, at those instances, and #1329 carries it; the universal arrows at finitely enumerable sets and Exercise 4 at the level of universal arrows are Instance/Top/StoneCech.v's; Roster.v's note carries the correction. The ultrafilter monad stays as Roster.v leaves it)
- **Instance/Top/StoneCech.v**, **Instance/Top/StoneCech/Refutations.v** (probe `Test/ProbeStoneCech455.v`): Mac Lane §V.6, printed p. 125 (PDF p. 134; `maclane:V.6:construction-stone-cech-discrete`), and §V.8 Construction 1 and Exercise 4, printed pp. 131–132 (PDF pp. 140–141; `maclane:V.8:construction1`, `maclane:V.8:ex4`), with Riehl Example 4.7.12 (printed p. 178, `riehl:4.7:example12`), issue #455 — **a PARTIAL deliverable, by the maintainer's decision of 2026-09-24: the closed constructive pieces of Stone–Čech and of Exercise 4, and closed METATHEOREMS showing why the adjoint-functor-theorem routes are vacuous over the tree's `Top` encoding; the constructions proper are re-homed to two filed follow-ups, #1328 (re-encode `Top` and `CompHaus` with a Prop-valued topology, homs at the points' universe) and #1329 (Stone–Čech proper, on #1328's encoding or by a direct construction)**. **THE ISSUE'S TWO HEADLINE NAMES ARE NOT CONSTANTS, FOR TWO DIFFERENT REASONS**, so the Verification block's `Print Assumptions StoneCech_discrete` and `Print Assumptions StoneCech` name nothing. The §V.6 statement, a left adjoint to `CompHaus_Forget`, is REFUTED under informative excluded middle (`IEM`, `Instance/Sets/Classifier/OneLevel.v`; a hypothesis, never an axiom, and classically true): `StoneCech_adjunction_refuted_IEM` at the instance with `CompHaus`'s objects at its hom universe, the one `GAFT_CompHaus_only_complete` produces (the refutation applied to that constant's own output is accepted, the probe's `p455_refutes_gaft_output`), and `StoneCech_adjunction_refuted_IEM_above` at an instance whose object universe lies above the hom universe, each at the instances Refutations.v's COVERAGE records: the `Top` slot at the hom universe `h`, the proof that a space is compact Hausdorff at the object universe `c`, and the separating opens of the Hausdorff proof (`CompHaus`'s fourteenth and fifteenth slots) at the points' universe `o`, where `Bool_CH` and `Point_CH` put them, from `Discrete_Hausdorff@{h o}`; with those opens below `o` the adjunction's type is formed and every refutation is refused (the probe's N22, beside its control at `o`). The §V.8 statement, a left adjoint to the inclusion `CompHaus_Incl`, is NOT refuted, and classically it holds: the refutation needs, in the left adjoint's domain, an object as large as the arrow collection of `CompHaus`, and the domain `Top@{h o}` has its points at `o`, strictly below `h` (that βX of a space whose points lie in a universe stays in that universe is a meta-argument about the set-theoretic model with choice, not a theorem here). Only its adjoint-functor-theorem ROUTE is vacuous, at the instances COVERAGE records; for that name the obstacle is a missing proof, #1329's. **WHY: THE UNIVERSE OF TOP'S HOMS.** `Instance/Top.v` puts the homs of `Top@{h o}` strictly above the points (`o < h`), continuity quantifying over the codomain's `Type`-valued opens, so the objects (`TopSpace@{o} : Type@{o+1}`) and the arrows of `Top` and of its full subcategory `CompHaus` all fit in `Type@{h}`: the category is small relative to its own hom universe. The adjoint functor theorems consume completeness at shapes no smaller than the homs (`GAFT` takes `Complete@{h h h cobj}`; #453's `SAFT_wellpowered` takes `Complete@{so so h o}` under `h <= so`), and there Freyd's argument (`Structure/Complete/Freyd.v`) applies: the two points of the two-point space are parallel arrows that evaluation at the point tells apart constructively, so there is no power of `Bool_CH` indexed by the arrows once the arrows carry an index, and `IEM` supplies one (`canonical_ArrowIndex` of decidable object equality). This inverts `Structure/Complete.v`'s size note: there the solution sets are the wall; here they are free (`taut_sols`) and completeness is. The probe measures the way out #1328 takes: over `p455_TopP`, a minimal record with Prop-valued opens, `p455_ContHom@{o}` lands in `Type@{o}` under a closed binder with an empty constraint block, while `ContinuousMorphism X Y` ascribed at `Type@{o}` is refused ("Cannot enforce <1> <= o because o < <1>", N6). That completeness then becomes the classical question (Tychonoff) it is in the book is an expectation no constant tests. **DELIVERED, CONSTRUCTIVE (StoneCech.v).** `Discrete_Hausdorff`: every discrete space is Hausdorff, at every universe, used in place of `Instance/Top.v`'s `Point_Hausdorff`, which is monomorphised at a `Set` carrier (`About`: `IsHausdorff@{u Set Set Set} Point_Top@{Set}`; N7; that file's comment carries the correction). `FinEnum A` (a list meeting every `≈`-class) with `Discrete_Compact_of_FinEnum` and `FinEnum_of_Discrete_Compact`: THE DISCRETE SPACE ON `A` IS COMPACT IF AND ONLY IF `A` IS FINITELY ENUMERABLE, in `IsCompact`'s data sense; `nat_not_FinEnum` and `Discrete_nat_not_compact` show the restriction is real; `Bool_CH`, `Point_CH` and `Discrete_CH X FX` are the resulting objects of `CompHaus`, at every carrier universe. **The finite §V.6 case**: `StoneCech_finite X FX : UniversalArrow (Setoid_Lift@{o h} X) CompHaus_Forget` for every finitely enumerable setoid, βX being `X` discrete, the unit the identity and each map extending as itself (built by `universal_arrow_from_UMP`; the setoid is lifted because `CompHaus_Forget` lands in the `Sets` one universe up), with `eq_refl` readbacks `StoneCech_finite_obj` (the universal object IS `Discrete_CH X FX`) and `StoneCech_finite_unit` (the unit IS `sc_fin_unit X FX`). A wrong-value control has to be chosen with care: at `bool` the equation with `Bool_CH` is ACCEPTED, `Bool_CH` being `Discrete_CH` at `bool_FinEnum` up to conversion, so the probe pins the one-point space (N1, refused at the carrier) and `Discrete_CH` at a reversed second enumeration (N2; both enumerations end `Qed`, so N2 pins that the object carries the enumeration it was built from). **The reduction**: `CompHaus_Forget_PreservesImageLimit`, with no hypothesis, the forgetful functor being represented by the one-point space; `taut_sols`, the tautological solution set (every object with every arrow), legal as GAFT's solution set when the objects sit at the hom universe (at `h < c` GAFT refuses it, N9); so `GAFT_CompHaus_only_complete : Complete@{h h h h} CompHaus → { F : Sets@{h s} ⟶ CompHaus & F ⊣ CompHaus_Forget }` (`h < s`) has one hypothesis left and needs no double-power-set bound — a conditional whose premise is refuted under `IEM`, kept as the measured reduction and not as a result. **Exercise 4 at the level of universal arrows**: over `CompHaus_Forget`, `unit_injective_of_separated` and its converse `separated_of_unit_injective`, unconditional, and `unit_injective_of_dec` (the separating map `sc_chi` into `Bool_CH`), applied to `StoneCech_finite` at `bool` (`StoneCech_finite_bool_injective`); over the inclusion, the issue's pinned name `StoneCech_unit_injective_completely_regular`, stated PARAMETRICALLY — for any compact Hausdorff `K` whose maps separate the points of `X : Top@{h o}` (`KSeparated K X`), the unit of ANY `UniversalArrow X CompHaus_Incl` is injective up to `≈` — with its converse in two strengths: `KSeparated_of_unit_injective_local` (`K` separating the points of the universal object), witnessed at `CompHaus_Incl_universal K` by `KSeparated_of_unit_injective_local_at_CompHaus`, and `KSeparated_of_unit_injective` (`K` separating the points of EVERY compact Hausdorff space), with no witness. The only universal arrow at `CompHaus_Incl` in the tree is `CompHaus_Incl_universal`, the identity at a space already compact Hausdorff (readback `CompHaus_Incl_universal_obj` at `eq_refl`), so the pinned name's one application, `StoneCech_unit_injective_at_CompHaus` with `KSeparated_self`, is at an identity unit and carries no new content; the book's `K = [0,1]` is not an object of `CompHaus` in tree, and the issue's note that complete regularity is unnecessary at the level of universal arrows is read in the header as an interpretation no constant shows. **REFUTED (Refutations.v).** Completeness: `CompHaus_not_complete` (an `ArrowIndex` at shape `s` ⇒ `¬ Complete@{r s h c} CompHaus`, through `freyd_no_separated_pair`); under `IEM`, `CompHaus_not_complete_IEM` (`c <= s`, `h <= s`) and `CompHaus_not_complete_IEM_below` (at every `s >= h`, the shapes below the objects included), which together reach every shape universe at or above the hom universe, and so every shape `SAFT_wellpowered` accepts at the inclusion, at the instances Refutations.v's COVERAGE records (the `Top` slot at `h`, the proof slot at `c`, the Hausdorff proof's separating opens at the points' universe `o`), and `CompHaus_not_complete_IEM_above`, GAFT's own shape `Complete@{h h h c}` at `c > h`; `Top_not_complete` and `Top_not_complete_IEM` for `Top` at every `h <= s`. The index above the hom universe is `CompHaus_ArrowIndex_of_Top`, an `ArrowIndex` of `CompHaus` at the hom universe transported from one of `Top` along the full inclusion: an arrow of `CompHaus` is a `Top` arrow paired with `True`, so the index needs `Top`'s objects (at `o+1 <= h`) and not `CompHaus`'s Σ-objects. The adjunction: `large_universal_arrow_refuted` (no universal arrow from the arrow-index set `KSet AI` to `CompHaus_Forget`: the unique extensions of the maps K → bool, read back at the unit, would give a surjection K → (K → bool), which `freyd_cantor_bool` refutes) and the two `IEM` forms above; universal arrows at SMALL sets are untouched (`StoneCech_finite`), the refutation needing a set at the hom universe, which `Sets@{h s}` contains. Vacuity: `GAFT_CompHaus_IEM_vacuous`, `SAFT_Incl_vacuous` and `SAFT_Incl_IEM_vacuous` pair each conditional with the refutation on ONE completeness hypothesis, and two wider pairs follow the wider refutations, `GAFT_CompHaus_IEM_vacuous_above` (GAFT at `CompHaus_Forget` with the objects above the homs, where `taut_sols` no longer fits, over any solution sets, against `CompHaus_not_complete_IEM_above`) and `SAFT_Incl_IEM_vacuous_below` (SAFT at the inclusion at every shape it accepts, the shapes below the objects included, against `CompHaus_not_complete_IEM_below`); `CompHaus_WellPowered` is `trivial_small CompHaus`, and `SAFT_CompHaus_Incl` is SAFT at the inclusion with well-poweredness discharged, three hypotheses left (completeness, `PreservesImageLimit`, a `Cogenerator`), which forces `c = h`, `c <= h` from `trivial_small`'s index at the hom universe and `h <= c` from `CompHaus_Incl`'s own block (`About` of each). `SAFT_CompHaus_Incl` is a conditional at more instances than COVERAGE records: it is accepted with the proof slot below the objects' universe and with the Hausdorff opens below the points', where every refutation is refused (measured by #455's review), and there its completeness premise stays classically false, a meta-argument. With neither `IEM` nor an index: `big_inj` and `big_inj_injective` inject `obj[CompHaus] → bool` into the points of one space, the power of `Bool_CH` over all objects, from `Complete@{r s h c} CompHaus` alone (the local definition `C := CompHaus` ties the index to the category and the block carries `c <= s`; without it the two elaborate at separate instances, N18); in a set-theoretic model whose points' universe is a Grothendieck universe V_κ there are at least κ objects and no such injection, so completeness is false there — a meta-argument, the tree having no cardinal arithmetic. The cogenerator premise: `CompHaus_cogenerator_stable_DNE` (any `Cogenerator CompHaus` whose members have ¬¬-stable equality of points ⇒ ∀ P : Prop, ¬¬P → P, through `YP_CH P`, the two-point discrete space with equality `x = y ∨ P`, compact Hausdorff with no hypothesis) and `CompHaus_point_separator_stable_DNE` (the same for one `K` meeting `KSeparated_of_unit_injective`'s premise), the `CompHaus` analogues of #454's `cogenerator_stable_DNE`: they refute nothing classically (`[0,1]` meets both premises, by Urysohn) and show that an axiom-free such family or space with stable points, the interval among the candidates, would prove double-negation elimination. **Strengths**: at `eq_refl` the three readbacks; `UniversalArrow` records; `∃!` up to `≈` for the preservation; `≈` on points, never `=`, in all of Exercise 4; data both ways in the compactness characterization (`FinEnum` and `IsCompact` are `Type`-valued); `False` for the refutations; pairs whose second component is `False` for the vacuity forms; Leibniz equality of boolean families for `big_inj_injective`; ∀ P : Prop, ¬¬P → P for the two DNE theorems. **Universes** (`About`, the headers' readbacks): `Discrete_Hausdorff@{h o}` and `StoneCech_unit_injective_completely_regular` under `o < h`; `StoneCech_finite` with `o < h` and `h` below the `Sets` level; `GAFT_CompHaus_only_complete` with all four completeness universes `h` and `h < s`; `CompHaus_not_complete_IEM` with `c <= s` and `h <= s`; `Top_not_complete_IEM` with `o < h <= s`. A word-bounded `Set` occurs in none of the readbacks the headers list, and one binder is load-bearing: without it `bool_FinEnum` elaborates at `FinEnum@{Set} bool_setoid_object@{Set Set}`, and `Bool_CH`'s body built from that binderless lemma is refused above `Set` (N8). **Measured.** 74 constants in the two files, every one "Closed under the global context" by its fully qualified name and every one in ONE new block of the `print-assumptions` gate (`grep -c 'Print Assumptions' Makefile` 10010 → 10084); neither file uses `Program`, and `Print Module` lists exactly the declared names. StoneCech.v has seven `Defined` proofs (by token), four load-bearing, each flipped alone to `Qed` (`sc_pt_cone`, `sc_pres_map`, `sc_chi`, `CompHaus_Incl_universal`), three kept as data; Refutations.v's `sc_phi_map` is load-bearing (flipped alone, `large_universal_arrow_refuted` is refused). Closure excluding self: 67 modules for StoneCech.v and 153 for Refutations.v (each direct `Require` dropped alone; `Instance/Top/CompHaus.v` and `Instance/Sets/Classifier/OneLevel.v` the dearest, 11 and 15 at the margin). Neither file `Require`s the standard-library reals (a grep of their `Require` lines; the one textual "Reals" in StoneCech.v is inside its header, naming `I_Top`'s axiom), so docs/AXIOMS.md's nine reals-importing development files stay nine. `Test/ProbeStoneCech455.v`, registered after `Test/ProbeWatts454.v` under the targets' full import list, carries 23 refutations, the name-absence instrument and negatives classified in its header by the error each stripped copy of the whole file stops with, beside 81 positive controls and guard `Check`s naming the targets' constants; the development files add no `make todo` hit, and the probe adds 23, exactly its lines opening with the refutation keyword (against 2586 at master 4eb62968). Coq 8.19.2 and 8.20.1: as the probe's header records, the targets and the probe compile against the Nix-store builds of this library, and each stripped copy is refused there at its Rocq 9.1.1 line and character range. **STALE PREMISES OF THE ISSUE, re-measured.** "No topological categories exist — no `Top`, no `CompHaus`" (and, in its Riehl section, "there is no category of spaces"): both exist, `Instance/Top.v` (#259) and `Instance/Top/CompHaus.v` (#413). "SAFT itself is never applied to any concrete category" (docs/INHABITATION.md): its `SAFT_wellpowered` row records applications at `Subsets Y`, at `Indiscrete`, at `Sets` under `Untruncate` and at `(RMod R)^op` (#454). "#413 … creates limits, so `CompHaus` is complete": #413 formed `CreatesLimit K CompHaus_Forget` without proving it, and completeness at the theorems' universes is refuted under `IEM`. The issue's case-insensitive search also matches unrelated words ("ConstOne", "milestone"); StoneCech.v's header states a narrowed pattern and the four files it finds, every hit a comment. **NOT DELIVERED.** The §V.8 adjunction at `CompHaus_Incl`, not refuted and classically true, by a direct construction (ultrafilters) or by a non-vacuous route: #1329, which also carries the §V.6 case over #1328's encoding. The re-encoding itself: #1328. Stone–Čech at an infinite set or at a non-discrete space; Tychonoff at any arity; Urysohn's lemma; Mac Lane's double-power-set solution set (not needed with the objects at the hom universe; above it `GAFT` would need a solution set other than `taut_sols`, and none is built); Riehl's closure-of-the-image description. The interval: `Instance/Top/Interval.v`'s `I_Top` carries `ClassicalDedekindReals.sig_forall_dec` and `functional_extensionality_dep` (`Print Assumptions I_Top`) and is shown neither compact nor Hausdorff in tree, so it is not an object of `CompHaus`, and neither the SAFT route with the interval cogenerator (the issue's Reviewer line), even as a conditional, nor the interval form of Exercise 4 is built; nothing ungated was added. A refutation of completeness with neither an index nor `IEM`; completeness at shape universes below the hom universe, which every `IEM` refutation here excludes (`h <= s`; the index forms apply there given an index) and the adjoint functor theorems do not consume; any refutation at the instances COVERAGE leaves out (the proof slot below the objects' universe, the Hausdorff opens below the points', or the `Top` slot below the homs with the objects above them at a shape below the objects), where the statements stay classically false (a meta-argument); the cardinal argument of `big_inj` as a theorem; a witness of the strong converse's premise; the discrete case as a composite with a packaged `Discrete ⊣ Forget` on `Top` (`Instance/Top/Forgetful.v`: the two functors live on different `Sets`); no `Instance` is registered. **Corrections in place**, each marked CORRECTION (#455), every edit inside a comment: `Instance/Top.v` (`Point_Hausdorff`'s `Set` pin), `Instance/Top/CompHaus.v` (its (D), `HT` refuted at the theorems' shapes and not merely unwitnessed; its NOT DELIVERED, the Stone–Čech adjoint) and `Instance/Roster.v` (the packaged record for the Stone–Čech adjoint IS formable, and refuted); in this file the `Instance/Top/CompHaus.v`, `Instance/Roster.v` and `Adjunction/SAFT/Characterization.v` bullets. docs/INHABITATION.md's `GAFT`, `SAFT_wellpowered` and `Hausdorff_Subcategory` rows carry additions marked Since #455, not corrections.
- **Instance/Top/Kolmogorov.v**: Mac Lane §IV.3 Exercise 4(b) (book p. 92, `maclane:IV.3:ex4`) — **T₀-spaces form a full reflective subcategory of `Top`**, delivered as `T0_Reflective_in_Top : Reflective T0_Subcategory` (`T0_Full`, `T0_reflector`, `T0_adj`), with `Ord`'s clause (a) the sibling above and nothing shared between them. **THE FILE'S SUBSTANCE IS A UNIVERSE OBSTRUCTION, MEASURED AND DESIGNED AROUND RATHER THAN PAPERED OVER.** The obvious indistinguishability relation — "x and y lie in exactly the same opens", quantified over EVERY open — does not fit this `Top`: `TopSpace@{o}` (`Instance/Top.v`) carries `top_carrier :> SetoidObject@{o o}`, so a space's `≈` is valued in `Type@{o}`, while `IsOpen` quantifies over predicates `X → Type@{o}`, so a relation over all of them lands at a level strictly above `o`; building a `SetoidObject@{o o}` on it is refused with a genuine universe inconsistency, pinned as the probe's headline formability negative against two positive controls at the SAME declared levels. So the "same carrier, coarser `≈`, same opens" design of #371 and of clause (a) is NOT available for `Top` at one universe, and what is delivered instead is `SameOpens@{s o | s < o +} X x y := ∀ U : X → Type@{s}, IsOpen X U → (U x ↔ U y)` — indistinguishability tested against the opens valued strictly BELOW the space's level (every `Prop`-valued open is such a `U`, and both witnesses separate their points with one; the trailing `+` is load-bearing across versions — a closed declaration compiles on Rocq 9.1 but Coq 8.19/8.20 reject its six inferred `prod`/`sigT` bounds, which the three-version nix build caught) — with the quotient `KolmogorovQuotient X` keeping the SAME carrier (`kq_carrier_strict`, `eq_refl`), coarsening `≈` to `SameOpens X` (`kq_equiv_strict`, `eq_refl`), and taking as opens exactly the opens of `X` that RESPECT that relation (`kq_open`), so that `open_proper` holds for the coarser `≈`; the circle is broken by `small_open_is_kq_open` — a level-`s` open of `X` respects the relation AUTOMATICALLY, since the relation says in so many words that `x` and `y` agree on it — which is why `KolmogorovQuotient_T0` is three lines and the mediator out of the quotient is continuous (a `Type@{o}`-valued open of the T0 target pulls back to a respecting open by the target's own `open_proper`). Classically, with small `Prop`-valued opens, `kq_open` IS the topology of `X`. Read `IsT0 X := ∀ x y, SameOpens X x y → x ≈ y` at its strength: agreement on the level-`s` opens is a WEAKER hypothesis than agreement on all (`SameOpensAll_SameOpens` is the one-way passage), so this `IsT0` is a STRONGER requirement, and the restriction sits visibly in the statement; `KolmogorovQuotient : TopSpace@{u} → TopSpace@{u}` is a genuine ENDOfunction on one and the same `Top`, which is what makes the `Reflective` record formable at all. `IsOpen (KolmogorovQuotient X) U = IsOpen X U` is refuted at `eq_refl` and pinned with the carrier agreement as the discriminating control, so the difference is located in the `IsOpen` field alone; `KolmogorovQuotient_T0` is NOT definitional, correcting the brief. Route as in #371: `kolmogorov_universal` (the `∃!`, mediator = the same map on points, continuity from the target's `open_proper`), `universal_arrow_from_UMP` → `LeftAdjointFunctorFromUniversalArrows` → `AdjunctionFromUniversalArrows`, nothing re-derived; `t0_reflect_iso` instantiates `reflective_counit_iso`, at the two-point discrete space as `bool_reflect_iso` (`KolmogorovQuotient Bool_Discrete = Bool_Discrete` refuted, the two `IsOpen` fields being different terms). Unit: `t0_unit_is_proj` POINTWISE at `eq_refl`, `≈` as a `ContinuousMorphism` record (the composite-unit cause of #371). **Hausdorff at its true strength**: `Hausdorff_T0_nn : IsHausdorff X → SameOpensAll X x y → ¬¬(x ≈ y)` — a DOUBLE NEGATION, stated against the UNRESTRICTED relation, for two independent measured reasons: the argument refutes apartness rather than producing equality (double-negation elimination for the carrier's `≈` is not available), and `IsHausdorff`'s separating opens are valued at `Type@{o}`, not among the level-`s` opens `SameOpens` tests — so NOTHING here derives `IsT0` from `IsHausdorff`, and `Bool_Discrete_T0` is proved directly from one `Prop`-valued open. Witnesses both ways: `Bool_Discrete` T0; `TwoPoint_Indiscrete` NOT T0 (its opens are the uniform predicates) with its quotient identifying the pair by the written-out witness; the MIXED `Tri_Top` (three points, opens the predicates that cannot tell two of them apart) whose quotient merges that pair and keeps `Tri_l` apart from the third point (`Tri_r`'s apartness follows by symmetry and transitivity and is not stated) — the first negative PROVING the relation at every open from `tri_open`'s two components, the second INSTANTIATING it at one `Prop`-valued open, both closing by `discriminate` on the discrete carrier, neither mapping out. Prior art measured at `d658518e`: the issue's "there is no `Instance/Top.v`" is false (#259), and no T0 predicate, indistinguishability relation or Kolmogorov quotient existed (`kolmogorov`/`indistinguish`/`specializ` return no topological hit; the token `T0` occurs three times, all Mac Lane's unrelated "whence T0 = 0"); the three quotient-topology precedents (`Instance/Top.v`'s `CP_open`, `Instance/Top/Wedge.v`'s `wedge_open`, `Instance/Top/Pushout.v`'s `tp_open`) glue POINTS and are neither needed nor required, this quotient changing no points. 76/76 closed under the global context (72 glob entries ∪ 73 `Print Module` names = 76: the three `Tri` constructors glob-only, the four eliminators `Print Module`-only; no `Program`, no `Record`), the file requiring none of the reals-carrying `Top` files; universes: no constraint-block equation, the only `Set` tokens the sort of `Inductive Tri : Set` and its eliminator's motive, `Top@{u u0} : Category@{u u u}` and `Subcategory`'s identification inherited and guarded; closure 30 (transitive in-project `.vo` deps excluding the file; `Tri_Top` built directly rather than as a `Sum_Top` because that adds nothing to it); zero collisions after five probe names colliding with `Test/ProbeTorsionFree371.v` were prefixed `k372_`; `Test/ProbeKolmogorov372.v` carries 7 `Fail`s (2 formability, 3 conversion, 1 typing, 1 instrument), each stripped one at a time, rename-simulated 7/7. NOT delivered: no T1/T2 chain beyond `Hausdorff_T0_nn`, no sober spaces; no specialization PREORDER as an object of `Ord` — the natural bridge between the two clauses, built in neither file; no space exhibited separating `SameOpens` from `SameOpensAll`, so the level restriction is not shown strict; no categorical-quotient reading (no coequalizer, the projection not shown epic); no pointed variant; no idempotent monad
- **Instance/Top/FundamentalGroupoid.v**: The fundamental groupoid π(X) — objects are the points of X, arrows are homotopy classes rel endpoints of paths, composition is reparametrized concatenation and inversion is path reversal; `FundamentalGroupoid X : Category`, `fundamental_groupoid_is_groupoid`, `fundamental_group X a` as the vertex group, and Riehl Corollary 1.5.14 as `fundamental_group_basepoint_independent` — derived THROUGH the connected-groupoid structure theorem, not by an independent conjugation argument. All four category and groupoid laws are instances of ONE construction, `straight_homotopy` H(t,s) = p((1−s)·φ(t) + s·ψ(t)), reducing to four pointwise identities over the reparametrizations of `Instance/Top/Interval.v` (`rf_assoc` = min(2t, t+1/4, t/2+1/2)); well-definedness on classes (`path_concat_respects`) is separate, pasting two homotopies across the square. `Instance/Top/Interval.v` supplies [0,1] and the square over the STANDARD LIBRARY REALS through `BallSpace`/`BallTop` (radius is DATA, which discharges Top's Type-valued union axiom with no choice), plus `paste_arrow`; its `Rcases`/`Rlin` are deliberately GLOBAL `Ltac`s rather than `Lib/Tactics.v` residents, since moving them would hand the reals' axioms to the whole tree — disclosed in the header, along with the fact that no `.glob` name sweep sees an `Ltac`. **These, with Instance/Top/Presheaf.v, Instance/Top/ContinuousRing.v, Instance/Top/Homotopy.v and Instance/Top/LoopSpace.v, together with the three metric-space files (`Instance/Met.v`, `Instance/Met/Extended.v`, `Instance/Met/Completion.v`), are the only nine DEVELOPMENT files in the tree that import the reals (`Test/ProbeMet.v` imports them too, so an unrestricted `rg` returns ten)**, and the axiom price is measured PER CONSTANT, not sampled: at most two stdlib axioms (`sig_forall_dec`, `functional_extensionality_dep`), plus a third (`sig_not_dec`) on exactly EIGHT constants running through the least-upper-bound property — `gval_endpoints`, `f_endpoints`, `interval_to_discrete_constant_dec` with its two-point corollary `interval_to_discrete_constant`, and `no_path_true_false`, `Bool_Discrete_not_pathconnected`, `Bool_Discrete_pi_not_connected`, `Bool_Discrete_loops_trivial`. Two is a CEILING, not an invariable charge: of Interval.v's 160 constants 1 is closed (`rf_id`), 34 carry only `sig_forall_dec`, 125 carry both; of FundamentalGroupoid.v's 113 — the 108 in the `.glob` PLUS five `FundamentalGroupoid_obligation_*` that no glob sweep sees — 1 is closed, 1 carries one, 103 carry two, 8 carry three; all enumerated in docs/AXIOMS.md. Non-vacuity separates two topologies on ONE set: `Bool_Discrete` and `TwoPoint_Indiscrete` share `bool_setoid_object` (`bool_carriers_agree` by `eq_refl`) and π tells them apart, the discrete half resting on `interval_to_discrete_constant_dec` — every continuous map from [0,1] into ANY discrete space with DECIDABLE equality is constant. The decider is a hypothesis, so no axiom is incurred; it is stated with `sum` rather than `sumbool` and supplies a `Prop` shadow of `≈`, both forced by this library's `≈` being `crelation`-valued while `completeness` needs a `Prop`. Limits disclosed: NO space with nontrivial π₁ is exhibited (both witnesses have trivial vertex groups at EVERY base point, both ∀-quantified and proved), so the pair is a contrast in CONNECTEDNESS and nothing else; π₁(S¹) ≅ ℤ needs covering-space theory; connectedness of [0,1] in the general sense (no separation by disjoint nonempty opens) is still unproven; and Riehl Remark 1.5.15 is formalized COMPONENTWISE only. docs/INHABITATION.md carries the row and the note
- **Instance/Top/Homotopy.v**: Homotopy categories and pointed spaces (Mac Lane §I.7 constructions 5+7, pp. 25-26; §II.8 remark 1; Riehl §1.1 Ex 1.1.iv) — homotopy of continuous maps WITHOUT products in Top: the cylinder-as-object is UNIVERSE-OBSTRUCTED (measured by probe, recorded in the header: opens live one universe above points, and the product-rectangle condition quantifies over opens of X, so `Cyl X` is a `TopSpace` only one level up — the Forgetful.v stratification met from the other side), so `Homotopy f g` is a RAW-CERTIFICATE record (map + properness + joint-continuity certificate stated as the pointwise rectangle condition + endpoint equations), Type-valued DATA over the Type-valued `HomRelT` — Riehl clause (ii)'s presentational point resolved in the strong direction, every congruence clause a CONSTRUCTION on witnesses. The four `HomCongruence` fields: constant track, time reversal, concatenation (`homotopy_trans` — paste_arrow's seam argument REDONE at certificate level, three-way `total_order_T` with opens intersected and radii halved at the seam; paste_arrow itself needs a BallSpace domain an arbitrary X lacks), and the DIAGONAL composition `H1(H2 x t, t)` (no cylinder functoriality, no transitivity needed — the outer certificate hands out an open of the middle space, exactly what the inner consumes). **`Toph := Quotient Top homotopic`** over the NAMED `homotopy_congruence` (the II.8 checkbox: the in-tree hom-congruence quotient, not a bespoke category), `TophProj` full. **`Top_pointed`** (pointed spaces, hom-setoid INHERITED from Top on underlying maps — restating it would sit a universe too low, and inheritance makes based clauses hand hypotheses straight to unbased ones) is CLOSED under the global context — pointed spaces mention no interval; **`Toph_pointed`** over `based_homotopy_congruence`, with basedness THROUGHOUT the track (`bhtpy_base` quantifies over every t, per the reviewer note). Witnesses: the straight-line contraction (`interval_id_homotopic_const` with `interval_id_not_const` — Toph identifies what Top separates, Mac Lane's "arrows are not functions" moral), based variant, and the capstone BEYOND the issue: homotopy equivalences ARE Toph's isomorphisms both ways (`homotopy_equivalence_Toph_iso`/`Toph_iso_homotopy_equivalence` — repackagings, the dividend of building Toph as a Quotient) with **`interval_iso_point_in_Toph`** against `interval_not_iso_point_in_Top` — the interval is a point in Toph and provably not in Top. Circle/disc non-monic example deferred-disclosed (needs genuine algebraic topology); the finer rel-endpoints `ArrowHomotopy` of FundamentalGroupoid.v explicitly distinguished. Axiom footprint measured per constant in docs/AXIOMS.md (17 Closed incl the whole `Top_pointed` spine / 60 with exactly the two-axiom ceiling / ZERO `sig_not_dec` — completeness never touched); no docs/INHABITATION.md row — unlike the π-development's conditional notes, the flagships here are unconditional with in-tree witnesses; the equivalence/iso passages are mutually inverse on the nose (`homotopy_equivalence_round`/`Toph_iso_round`, Leibniz round trips)
- **Instance/Top/LoopSpace.v**: Loop interchange and the abelian fundamental group of a topological group (Mac Lane §II.5 Exercises 4+6) — the second in-tree consumer of Theory/EckmannHilton.v, and the classical one. `HTopMonoid` (a `TopSpace` with a unit point, `Proper` multiplication, STRICT unit laws, and joint continuity as the two-open rectangle certificate — Top has no products, the Homotopy.v accommodation) is EXACTLY what the exercises consume, so the theorem is stated at H-space-with-strict-unit strength; `TopGroup` extends it (inverse + laws + `tg_inv_cont`, literally continuity of inversion) so Mac Lane's named object exists, with the honest note that nothing on the path to the theorem uses the inverse. `mult_arrow` develops the pointwise product ONCE over an arbitrary domain `TopSpace` — at `I_Top` it multiplies paths, at `Sq_Top` homotopies, and it mentions no reals at all; `loop_mult` with `loop_mult_respects` and strict unit laws lifted by FundamentalGroupoid.v's existing `pointwise_homotopic`. Exercise 4 is `loop_interchange : (α ∗ β) · (γ ∗ δ) ∼ (α · γ) ∗ (β · δ)` — a COMPUTATION, not surgery: both sides split at 1/2 identically, so pointwise equality plus `pointwise_homotopic` closes it. Matching Exercise 4's displayed law variable-for-variable assigns f := pointwise product, g := concatenation (the swapped assignment is NOT false — interchange is self-dual under swapping f and g with the middle arguments transposed, and would deliver the same conclusions from the f-side), so π₁-abelian comes out as the g-side corollary `eh_g_comm`: `pi1_topgroup_abelian : mon_op p q ≈ mon_op q p` at `fundamental_group G (htm_unit G)` (pinned name, over `HTopMonoid`), `pi1_TopGroup_abelian` the Mac Lane reading, `loop_concat_comm` the loop-level form, plus `loop_mult_concat_agree`/`loop_mult_comm`/`loop_mult_assoc` read off the same instantiation, and the unmisreadability anchors `pi1_mon_op_is_concat`/`pi1_mon_unit_is_const` by `eq_refl`. Witness: `R_TopMonoid`/`R_TopGroup` ((ℝ,+) with ε/2 certificates) and `pi1_R_abelian`, disclosed as exercising the hypotheses only (π₁(ℝ) trivial; no in-tree space has nontrivial π₁). Axiom footprint per constant in docs/AXIOMS.md: of 52 constants, 23 CLOSED — the ENTIRE abstract spine, both records, all projections, `open_of_nbhds` (an upstreaming candidate: a predicate with an open neighbourhood at each point is open) and all of `mult_arrow` — 27 at the two-axiom ceiling, 2 `sig_forall_dec` alone, ZERO `sig_not_dec`; `pi1_topgroup_abelian` itself carries exactly the two. Ω(G,e) as a SPACE (compact-open) is out of scope, disclosed
- **Instance/Met.v**: Mac Lane §III.1 construction 3 + Seven Sketches §2.3.3 Definition 2.51 — metric spaces, their completion as a UNIVERSAL ARROW, and extended metric spaces, across `Instance/Met.v` (the category), `Instance/Met/Extended.v` (the `[0,∞]` variant) and `Instance/Met/Completion.v` (the construction). `MetricSpace` carries Seven Sketches' four axioms with NON-NEGATIVITY DERIVED rather than assumed, so it has one field fewer than the book's ℝ≥0-codomain presentation; morphisms are ISOMETRIES, which is what the catalogued item asks for ("metric-preserving functions (necessarily injective)"), and Mac Lane's parenthetical is proved — `isometry_injective`, with `Met_all_Monic` as its categorical shadow. The header records that Mac Lane uses a DIFFERENT category one chapter later (§IV.3's reflective-subcategory example, over uniformly continuous maps) and that no comparison between the two is built. **The codomain is stdlib ℝ and that is forced, not chosen**: the completion's distance is `lim d(xₙ,yₙ)`, so the codomain must contain the limits of its own Cauchy sequences — ℚ does not, so a ℚ-valued `Met` is not closed under the very construction being formalized and the universal arrow would be unstatable. Two halves are kept apart: `cdist_spec` and `cdist_unique` MACHINE-CHECK that the distance is the limit and that any real the distances converge to IS it (so the limit is not one admissible distance among several), while the claim that ℚ lacks such limits is ARGUED and labelled as argued — `Search "irrational"` over the loaded stdlib returns nothing, so there is no result to lean on. `CMet` is the complete full subcategory with `CMet_Incl` full and faithful; the completion is by Cauchy sequences with **the modulus carried as DATA** (the tree's `∃` is `sigT`), which is precisely what makes the diagonal completeness argument choice-free — extracting thresholds from `Prop` existentials would be countable choice. `Completion_AUniversalArrow` is the headline, with `completion_unique` giving uniqueness up to unique isomorphism CARRYING the leg equation, obtained from `Theory/Universal/Arrow.v`'s `auniversal_arrow_unique` rather than by hand. **A pre-existing tree defect was surfaced in the attempt**: `Structure/UniversalProperty/Universal/Arrow.v`'s `UniversalArrowIsUniversalProperty` — the intended route — is not instantiable at any of the tree's LARGE concrete categories, `Sets` and `Coq` and the wanted `CMet_Incl` alike, each rejected by a universe inconsistency; it is NOT uninstantiable in general, since it elaborates at `_1`, `_2` and `Cat`, so the defect is a size boundary rather than a blanket failure. It has no consumer anywhere in the tree, so this is the first attempt to give it one; the obstruction is not this development's and is pinned as a probe so its repair gets noticed. Non-vacuity is proved on both sides: `Harmonic_not_MComplete` (an explicit modulus with no limit — and since `MComplete` is the WEAKER Bishop-style property, refuting it is the STRONGER negative, so the space is classically incomplete too) and `eta_Harmonic_not_surjective` (a point of the completion outside the embedding's image). The extended-metric box is DELIVERED: `Rinf`, `ExtMetricSpace` on the same four axioms, both passages, and `metric_iff_finite_extended` stated over a FIXED carrier (comparing records with different carriers would need a contentless transport). Strengths measured strict-first — `R_Metric_dist`, the metric↔extended round trip on the metric side, and `Completion_carrier` close by `eq_refl`; the three that do NOT are pinned as rejection probes with positive controls, the sharpest being that `cdist (eta a) (eta b) = dist a b` is not definitional because it needs uniqueness of limits. Axiom cost measured PER CONSTANT across all 184 (docs/AXIOMS.md): mentioning `R` is free, real arithmetic costs `sig_forall_dec` (+ `functional_extensionality_dep`), and only `R_complete` adds `sig_not_dec` — which in `Met.v` is exactly two constants and in `Extended.v` is none. **These files are deliberately NOT in the `print-assumptions` gate**, which fails on any axiom but the ZX `Phase` parameters; none of the six pre-existing reals files is in it either. Nothing here computes (`cdist` is a `proj1_sig` of `R_complete`), so the acceptance tests are structural rather than numeric. Distinct from `Construction/Karoubi/Universal.v`'s `CauchyComplete := IdempotentsSplit`, which is categorical Cauchy completeness and unrelated; both headers say so. Deferred: the completion as a FUNCTOR and the §IV.3 reflection `Completion ⊣ CMet_Incl` (both machine-checked to elaborate with no further obligation, guarded in the probe file, but left off the library surface since the issue stops at the universal arrow), no metric topology or `Met ⟶ Top`, no short/uniformly-continuous morphism variants, no limits or monoidal structure on `Met`, and no unification with the Lawvere/Cost-enriched reading
- **Instance/Proset/Galois.v**: Galois connections are adjunctions between preorders (Mac Lane §IV.5 Theorem 1) — `GaloisConnection RA RB` (two monotone maps plus the biconditional `f a ⊑ b ↔ a ≤ g b`), the bridge BOTH ways (`GaloisAdjunction`, `GaloisOfAdjunction`), and `galois_round_trip` showing the two maps survive the round trip ON THE NOSE. That round trip is the load-bearing check, not a formality: over a thin target the coherence fields carry nothing, so it is what pins down that the bridge transports the Galois data rather than discarding it. **Thinness is now a usable hypothesis**: `Thin C` was previously available only as the trivial hom-setoid buried inside `Proset` plus an ad-hoc `two_thin`, so it could be neither assumed nor concluded; `proset_thin`, `thin_side_condition` and `proset_side_conditions_vacuous` make the vacuity a REUSABLE LEMMA — in a thin category every one of `Adjunction`'s four naturality fields and `Adjunction_Transform`'s two triangle identities is an equation between parallel morphisms, hence free. Non-vacuity is witnessed concretely by `nat_shift_galois`: on ℕ, truncated SUBTRACTION is left adjoint to ADDITION. NOTE the orientation — "add k ⊣ subtract k" is FALSE (`gal_from` would read `a ≤ b − k → a + k ≤ b`, refuted at `k=5, b=0, a=0` where truncation makes the hypothesis hold and the conclusion not); `lia` rejected it. **Erratum in #380**: `Poset` and `Proset` are the SAME category — `Instance/Poset.v` is `Definition Poset ... := Proset P`, DISCARDING antisymmetry, so `poset_is_proset` holds by `eq_refl`, anything proved for `Proset` holds for `Poset` by delta-reduction, and antisymmetry is NOT recoverable from the category; a "mutually related ⇒ equal" corollary therefore needs it as a separate hypothesis (`mutual_le_to_eq`).
- **Instance/Grp/Galois.v**: Mac Lane §IV.5 construction 1 (book p. 96, `maclane:IV.5:construction1`) with Theorem 1's displays (1) and (2) from p. 95 — **the Galois connection of a group acting on a set, and it is ANTITONE, which is Mac Lane's own `Q^op`**: display (1) reads `L p ≥ q in Q iff p ≤ R q in P` with `L : P → Q^op`, so `group_action_galois : GaloisConnection (@subset_le U) (op_rel (@subset_le G))` (the issue's pinned name) takes `Instance/Proset/Galois.v`'s COVARIANT record at the REVERSED inclusion on `P(G)` (`Instance/Proset/Limit.v`'s `op_rel`, proved the same function as stdlib's `Basics.flip` by `eq_refl`) — the tree's FIRST antitone inhabitant, the three prior ones at named relations (`nat_shift_galois`, `image_preimage_galois`, `finpow_image_preimage_galois`; `GaloisOfAdjunction` and `galois_of_unit_counit` are parametric over arbitrary relations) all being covariant; the covariant ascription is pinned as a TYPING negative (a plain `has type … expected`, no `cannot unify`). `stab X` is his `L X` and `fixed S` his `R S`, over #382's `Powerset_Prop_obj` with memberships at `eq_refl`; his middle term "σ·x = x for all σ ∈ S and x ∈ X" is `FixesAll X S`, and `subset_le S (stab X) = FixesAll X S` holds at `eq_refl` (`stab_transpose_strict`) while the other side is one quantifier swap (`fixed_transpose_iff`), which is the whole biconditional. **THE ONE DEVIATION FROM THE BOOK IS FORCED AND LOCATED**: `≈` is `Type`-valued while a member of `Powerset_Prop_obj` must be a `Prop`, so the fixing condition is `Powerset_squash (act A s x ≈ x)` — the same impredicative truncation `Powerset_Prop_image` uses — inert over any `Prop` goal (`powerset_squash_prop_inert`), which every elimination below is and both witnesses' `eq_Setoid` carriers make it. **THE REVIEWER CHECK LANDS AS AN INHABITANT OF THE EXISTING RECORD**: `stab_Subgroup X : Subgroup G` (`Instance/Grp/Quotient.v`), `sub_mem` the stabiliser at `eq_refl`, its four laws `stab`'s own respectfulness, `act_unit`, `sact_op`, and `sact_op` with `grp_mul_inv_l`; nothing dual is claimed for `fixed S`, a subset of `U`. **DISPLAY (2) IS DELIVERED GENERALLY, over an ARBITRARY `GaloisConnection`** (section A, which mentions no group, action or power set and keeps its two type parameters at SEPARATE universes): `gal_lrl_below`/`gal_lrl_above`/`gal_rlr_below`/`gal_rlr_above` are `:=` terms over the donor's `gal_unit` and `gal_counit` — CONSUMED, not restated — two of them being the unit/counit itself and two one monotonicity step away, so **each half of (2) needs only ONE of the two preorders**, which the signatures record; `GalClosed_l`/`GalClosed_r`, every image closed, and `gal_closed_l_iff`/`gal_closed_r_iff` (closed exactly when mutually related to an image — the sharpest reading a preorder without antisymmetry supplies); no closure-operator vocabulary existed (no composite `gal_l (gal_r (gal_l _))` anywhere). Instantiated: `stab_fixed_stab`/`fixed_stab_fixed` at the carriers' `≈` (mutual inclusion IS `equiv` there, `subset_le_antisym`), the `eq_refl` forms refuted and pinned; `ClosedG`/`ClosedU` with **`closed_G_iff`/`closed_U_iff`** — a subset of `G` is closed exactly when it IS a stabiliser, a subset of `U` exactly when it IS a fixed-point set — and `closed_G_Subgroup`, Mac Lane's "L X is the subgroup" read at every closed subset. **TWO PACKAGINGS, NEITHER BRIDGED TO THE OTHER**: `group_action_adjunction : StabFunctor ⊣ FixedFunctor` is #380's `GaloisAdjunction` applied at `Proset stab_PreOrder_G`, whose objects and homs are those of `(Subsets G)^op` at `eq_refl` (`galois_PG_obj`/`galois_PG_hom`) while the WHOLE categories are refuted and pinned — NOT at `id`/`compose`, which agree at `eq_refl` too and are probe controls, but at the `homset` record, whose `Equivalence` witness is `Proset`'s opaque `Program` obligation at swapped arguments (an audit refuted the draft's `id`/`compose` attribution); and `group_action_AdjointOnTheRight : AdjointOnTheRight StabOp FixedOp` is Mac Lane's own `L : P → Q^op`, `R : Q^op → P` typing in `Adjunction/Right.v`'s class — the first group action there — delivered because its `Require` costs THREE modules (129 against 126, measured by dropping it), where the brief had guessed thirty. **`Instance/Grp.v`'s `Z2` IS UNUSABLE, and for a universe reason, not the reducibility one the brief asked about**: it is declared `Z2@{u} : GrpObject@{u Set u}`, pinning its relation universe to the literal `Set`, while `Subsets` demands `SetoidObject@{o o}` with `Set < o` — pinned as a FORMABILITY negative (`Cannot enforce Set = …`) beside `Z2` and `grp_setoid Z2` as controls. **CORRECTION (#450): `Z2` IS USABLE NOW** — #450 lifted it in place (`About`: `Z2@{p} : GrpObject@{p p p}` over `bool_setoid@{u} : Setoid@{u u} bool`), the probe's Negative 8, `Subsets (grp_setoid Z2)`, is a control, and `GalZ2` is kept as it is. So the witnesses are built on `Lib/Setoid.v`'s polymorphic `eq_Setoid`: `GalZ2` (`bool` under `xorb`) acting on two points by `xorb` — `stab {true}` IS the trivial subgroup (`galois_stab_true_trivial`, at `≈`), `fixed {true}` is EMPTY, `{true}` is closed NEITHER as a subset of `G` nor of `U` (both by `discriminate` after eliminating the truncation), a closed pair is exhibited and the action moves a point; and, because a FREE action has only the two trivial stabilisers, the Klein four-group `GalV4` on `bool * bool` acting through its FIRST coordinate, whose `stab {true}` is proved both PROPER and NON-TRIVIAL (`galois_v4_stab_proper`/`_nontrivial`, one element omitted and one non-unit element contained — **no cardinality is computed anywhere**). **Universes measured off BOTH binder and block, all 77 heads**: section (C) onward binds `G : GrpObject@{o o gu}` and `A : MSetoidAction@{o o gu gu gu o o gu} (grp_mon G)` — the identification of carrier and relation levels is the DONORS', not this file's — FOUR of them each sufficient alone at levels declared apart, `Powerset_Prop_obj` (`Instance/Sets/Powerset.v`, met first in section C), `subset_le`, `subset_le_preorder` and `Subsets` (`SetoidObject@{o o}`, `Instance/Powerset.v`), measured out of tree — and the LAST is guarded: at levels declared apart `grp_setoid Gq`, `sact_setoid Aq`, `carrier (grp_setoid Gq)` and `grp_mon Gq` are accepted while `Subsets (grp_setoid Gq)` and `Subsets (sact_setoid Aq)` are rejected (`Cannot enforce qh = qo`); ZERO universe EQUATIONS in any block and no `Set` in any INSTANCE; 59 blocks carry `Set` only as the strict LOWER bound `Set < o` (`Powerset_Prop_obj`'s, `Prop : Type@{Set+1}`), the other 18 — all of section (A) and the witness data — none. **91/91 constants closed under the global context** — 77 glob heads (54 `def`, 23 `prf`) plus the **14 `Program` obligations** of section (I)'s three `Program Definition`s (three each for `StabOp`/`FixedOp`, eight for the right-adjoint pair), which the builder's report said did not exist and `Print Module` lists; the gate carries all 91 fully qualified. Zero collisions over the 77 names (instrument `Subgroup`/`GaloisConnection` ≥ 2; `stab`/`fixed`/`fixes` occur elsewhere only as prose or as local binders). Closure **129** excluding self (probe 130): `Instance/Rep.v` costs 8 (it is where `grp_mon` lives), `Adjunction/Right.v` 3, `Instance/Grp/Quotient.v` 3, `Instance/Powerset.v` 2, every other `Require` 0. **A PRIOR-ART CORRECTION TO THIS INDEX**: `Instance/Grp/Free.v`'s `grp_deloop_monoid` is a SECOND `Instance/Grp.v`-`GrpObject → MonObject` bridge — cheaper by 3 and a plain `Definition` where `grp_mon` is a `Program` one — and the two are NOT convertible (`grp_mon H = grp_deloop_monoid H` rejected with `cannot unify`, measured out of tree, unpinned), so an `MSetoidAction` is over one or the other; `grp_mon` is kept as the bridge this index names, and the `Construction/Free/Groupoid.v` bullet's "the only in-tree bridge" is corrected in the same commit. Nine `Defined` tokens, **ALL load-bearing** — each flipped to `Qed` alone stops a named `eq_refl` readback or witness (`stab`→`stab_mem`, …, `GalV4Act`→`galois_v4_stab_forces`); 15 `eq_refl` Examples; zero lines over 80; `make todo` grows by 13, all in the probe; the issue's suggested `Instance/Group/Galois.v` names a directory that does not exist, and its "Current state" is stale on all four counts (`Grp`, `MSetoidAction`, `Subgroup` and the powerset preorder all exist and are consumed, none rebuilt). `Test/ProbeGalois381.v` mirrors the target's full `Require` list and carries **8 `Fail`s = 1 instrument + 7 negatives of THREE kinds** told apart by the error text — 1 TYPING, 3 CONVERSION (the two `eq_refl` idempotences, the whole-category comparison with its obj/hom/id/compose `eq_refl` controls beside it), 3 FORMABILITY (the two `Subsets` level rejections and the `Z2` `Set` pin; since #450 the count is 7 = 1 instrument + 6 negatives, FORMABILITY being the two `Subsets` level rejections only, `grep -c '^Fail' Test/ProbeGalois381.v` 8 → 7) — each stripped ONE AT A TIME with the whole error read, every constant a negative names guarded outside a `Fail` (12/12, donors included; CORRECTION (#450), re-measured by listing the global names inside the `Fail` commands with comments stripped, universe instances dropped, and `eq_refl`, the instrument's absent name and section binders excluded: at the parent commit that is 13 names with the record field `carrier` and 12 without it, the figure matching the count without it, and since #450 `Z2` is named by no negative, so 11/11, or 12/12 with `carrier`, every one still named outside a `Fail`), rename-simulated 4/4 over the target constants (`group_action_galois`, `stab`, `fixed`, `stab_PreOrder_G`), each rename leaving the target compiling and breaking the probe at a `Check` line. NOT delivered: nothing about fields or the fundamental theorem of Galois theory (the tree has no automorphism group of a field); no lattice on the closed elements; no orbits, orbit-stabiliser, transitivity or freeness vocabulary; no normality criterion for `stab_Subgroup`; no functoriality or naturality in `G`, `A` or `U`; no comparison with #382's image/preimage connection or with `Adjunction/Right.v`'s two antitone witnesses; no bridge or equation between the two adjunction packagings; no `AdjointOnTheLeft` reading; nothing registered as an `Instance`
- **Instance/Powerset.v**, **Instance/Powerset/Subobject.v**, **Instance/FinSet/Subsets.v**, **Instance/Top/Image.v**: Mac Lane §IV.5 construction 2 (book p. 96, `maclane:IV.5:construction2`) + Awodey §9.4 Example 9.12 and §9.9 Exercise 4 + Seven Sketches §§1.2.2/1.4.3 — **the power set of a SETOID as a thin category under inclusion, with the direct image LEFT ADJOINT to the inverse image**: `Subsets X := Proset (subset_le_preorder X)` over `Instance/Sets/Powerset.v`'s `Powerset_Prop_obj X`, the ≈-respecting Prop-valued predicates — that carrier and BOTH operations, `Powerset_Prop_image` and `Powerset_Prop_preimage` (`Instance/Sets/Powerset/Universal.v`), pre-existed, so the issue's "the carrier is missing" is STALE on three counts and nothing is redefined — with `image_preimage_galois f : GaloisConnection (@subset_le X) (@subset_le Y)` and **`image_preimage_adjunction f : DirectImage f ⊣ InverseImage f`** by CONSUMING #380's `GaloisFunctor_l`/`GaloisFunctor_r`/`GaloisAdjunction` (both object actions and their agreement with the pre-existing functors at `eq_refl`; `image_galois_round_trip` at Leibniz `=` on the WHOLE record). **THE NAME `Subsets` IS FORCED**: `Powerset` is declared twice already (`Instance/Rel/Dagger.v`, bool-valued over a bare `Type`, and `Instance/Sets/Powerset.v`, the cross-universe functor), and `make print-assumptions` loads many modules into ONE scope, so the issue's `Print Assumptions Powerset.` cannot be satisfied under that name. **THE ORDER LIVES ON THE TRUNCATED CARRIER, AND THE REASON IS A SORT REJECTION, PINNED**: inclusion of `Instance/Sets/Powerset.v`'s proof-relevant `Powerset_obj` is `Type@{o}`-valued and so not a stdlib `relation` at all (`Cannot enforce … <= Prop` — FORMABILITY, not the typing error a first draft of the probe called it), and the same wall keeps the homs of `Opens X` out of `Proset` and `GaloisConnection`, which is why `nat_inf_no_left_adjoint` quantifies over `Adjunction` directly; the route is `Powerset_squash`'s impredicative elimination into the Prop `subset_le`. Unit and counit are Mac Lane's two inclusions at TYPE level (`adj_unit_has_incl_type`/`adj_counit_has_incl_type` typecheck by conversion and say only that the unit inhabits the inclusion's type — an audit found the Leibniz `unit S = unit_incl S` holds at `eq_refl` once the two transposes and the two inclusions are closed `Defined`, so the identification is blocked by the file's own `Qed`s and NOT shipped, and a draft calling the ascription "the strongest statement available" was FALSE; `subsets_equiv_is_True` is `eq_refl`); meets and joins at ARBITRARY index (`subset_inter_IsGLB`/`subset_union_IsLUB`, the union needing NO truncation) give `Subsets_Cartesian`/`Cocartesian`/`Terminal`/`Initial`/`Complete`/`Cocomplete` through `Instance/Proset/Limit.v`'s biconditionals; preservation is delivered BOTH ways where both routes exist — `inverse_image_preserves_meets` direct (two lines, no `Set`) and `_via_RAPL` read off `right_adjoint_preserves_limit` at `Proset_Limit`, `direct_image_preserves_joins` and `_via_LAPC`, with `inverse_image_meet_routes_agree`/`direct_image_join_routes_agree` checking that each pair inhabits ONE type — while `inverse_image_preserves_joins` is DIRECT ONLY (the adjoint proof needs f^*'s RIGHT adjoint, the dual image, which is #384's) and the direct image provably does NOT preserve meets (`direct_image_not_meet_preserving`, at `Universal.v`'s reused `powerset_fin2`/`powerset_const0`, the `eq_refl` form refuted and pinned). **A `Set` PIN IS INHERITED AND LOCATED, NOT INTRODUCED**: `Subsets_Complete : Complete@{u0 u0 Set o}` is stated against `Subsets@{o Set} X` because `Proset_Limit` routes through `Instance/Discrete.v`'s unannotated `DiscreteCat_Functor` (`DiscreteCat@{u Set Set}`) while `IsALimit` identifies the shape's hom-and-proof universe with the ambient's — pinned as a formability negative (`Cannot enforce Set = u`) with `Subsets_Cartesian`, which never meets the discrete diagram, as the discriminating control accepted at `Set < u`; `Subsets@{o u}` itself relates its object and hom levels NOT AT ALL (block exactly `Set < o` plus two stdlib bounds), `subset_union`'s index universe is unconstrained, the two RAPL/LAPC routes carry the `Set < u2`/`Set < u4` residue and the direct ones do not, and no definition in the four files introduces a `Set` of its own. Awodey §9.9 Ex 4 rides `Instance/Proset/Monotone.v`'s `MonotoneFun` (`image_MonotoneFun`/`preimage_MonotoneFun`), `Functor_of_monotone` of each agreeing with the Galois functor on OBJECTS at `eq_refl` and the whole records refuted and pinned, the cause located: `Functor_of_monotone` is a `Program Definition` whose three law fields are its own opaque obligations, so the difference touches neither data field. **The subobject reading cannot go through `Proset` at all** (`Theory/Subobject.v`'s `sub_le` is a `sigT`), so `Instance/Powerset/Subobject.v` states it elementarily: `sub_reindex_monotone` in ANY category with chosen pullbacks — measured absent at `9455ac5f`, the token `sub_le` occurring nowhere in `Theory/Subobject/Functor.v`, whose `sub_reindex_respects` is the weaker `Proper` — `sub_image` in `Sets` from `Instance/Sets/Image.v`'s `Sets_Image` with `sub_image_respects` as an `#[export] Instance` on that donor's precedent, BOTH transposition directions packaged as `sub_image_reindex_iffT` (the backward one is where monicity of `sub_mono v` is spent, through `injectivity_is_monic`'s backward leg), and the bridges to #311's passages at `≈` — NOT at `eq_refl`, the two truncated existentials being bracketed differently, pinned. **The computing half is `Instance/FinSet/Subsets.v`**: `FinSubsets n : Category@{Set u u}` with a LITERALLY EMPTY constraint block (`finpow_le` and `finpow_image` carry no universes at all) over the `finpow n` codes, with Seven Sketches' three counts 1/2/4 at `eq_refl`, the orders EXHIBITED — all five inclusions of the four-element case inhabited and BOTH incomparabilities refuted, so `FinSubsets 2` is provably not a chain — `FinSubsetsToSubsets n` Full into `Subsets (Powerset_Prop_fin_object n)` — order reflection is what fullness spends — and Faithful VACUOUSLY, the source being thin so every functor out of `FinSubsets n` is faithful by a one-line proof, which the header now says (essential surjectivity NOT claimed: it would decide an arbitrary Prop), `FinSet_Pow_one : Pow 1 = 2` completing `Instance/FinSet/Topos.v`'s family (the open-`n` form is `Instance/FinSet/Powerset.v`'s `finpow_is_topos_Pow`, cited not duplicated), the INTERNAL order `internal_subseteq n : Pow n × Pow n ~{FinSet}~> Ω` proved EQUIVALENT to the external `finpow_le` (`internal_subseteq_spec`) — the general-topos form NOT delivered for a measured reason, no constant of type `Ω × Ω ~> Ω` existing anywhere in the tree — and Example 118 EVALUATED: `finpow_preimage` IS `Instance/FinSet/Powerset.v`'s `finpow_map` at `eq_refl`, `finpow_image` and `finpow_dual` are new, six `eq_refl` Examples at inputs where the three operations VISIBLY differ (at {0} the dual is EMPTY where the image is {0}), `finpow_image_preimage_galois` for Example 117's left half, the dual-image ADJUNCTION left to #384. **Awodey's topological caveat is PROVED in general and delivered positively at a witness**: `Opens_preimage g : Opens X ⟶ Opens Y` (object action `Instance/Top/Closed.v`'s `open_preimage` at `eq_refl`; no functor in the tree had it, `OpensF` landing in `Sets`), `NatInf_Top` (`option nat` with `None` at infinity, a predicate open exactly when it contains a tail whenever it contains infinity), **`nat_inf_no_left_adjoint : ∀ L, L ⊣ Opens_preimage nat_inf_point → False`** by transposing twice through the punctured opens, and `nat_inf_preimage_adjunction : Opens_preimage nat_inf_point ⊣ nat_inf_right_adjoint` — the GENERAL right adjoint, the interior as a union over ALL opens, being NOT FORMABLE in this `Top`, since `OpenSet X : Type@{u}` sits one level above the points (`u0 < u`) while `open_union` indexes at the points' level, pinned (`Cannot enforce X <= o because o < X`) against the same union at a small index as the control. **196/196 constants closed under the global context** (64 + 14 + 83 + 35, the last being 21 glob heads plus 14 `Program` obligations; no file declares a `Record`/`Class`/`Inductive`, so there is no `Build_*`), of which the gate carries the 182 heads; zero collisions over 216 names after ONE was renamed away before landing (`galois_round_trip` is `Instance/Proset/Galois.v`'s own lemma); closures 87/65/99/24 excluding self, the probe's 115 — `Instance/Proset/Limit.v` costs file 1 twenty-two of its 87 (65 without it, 63 without it and `Adjunction/Continuity.v`, which costs nothing while Limit is present; a draft's 56/31 was consistent with itself and with nothing measured) and is kept because restating `IsGLB` would be a lookalike of a donor, and the optional `Instance/Rel/Dagger.v` bridge, at 11 more, is declined; five of the FIFTEEN `Defined`s are load-bearing by flipping (`subset_inter`, `subset_union`, `sub_image_map`, `sub_transpose_from_map`, `finpow_to_subset`), the other ten `Defined` for uniformity, a line-anchored count reporting thirteen because two sit inline after one-line scripts. `Test/ProbePowerset382.v` mirrors the union of the four `Require` lists and carries **9 `Fail`s = 1 instrument + 8 negatives in THREE kinds told apart by the error text** — 4 FORMABILITY (two SORT rejections and two LEVEL rejections; the tail alone does not separate the two shapes), 3 CONVERSION, 1 TYPING (the swapped handedness `InverseImage f ⊣ DirectImage f`, a plain mismatch with no `cannot unify` and no universe clause) — each stripped ONE AT A TIME with the whole error read, every constant a negative names appearing in a control outside any `Fail`, rename-simulated 10/10 over the target constants the negatives name; `make todo` grows by 13, all in the probe, zero TODO/FIXME/jww. Engineering findings recorded in-file: the record-literal-with-holes idiom `{| morphism := fun p => _ ; proper_morphism := _ |}` does NOT keep `p` in scope for the deferred goal, which is why `sub_image_witness` and `sub_transpose_from_eq` exist as separate constants; `Bool.orb_true_iff` cannot be destructed into a `Type`-sorted `∃` over `Fin.t` where the `sumbool` `Bool.orb_true_elim` can; and an italicised book title ending `Theory*)` inside a header comment silently CLOSES the comment. A fess audit of the first commit found five prose defects and no unsound step — the closure figures, the `Defined` count, `TopSpace`'s seven fields miscounted as six, the vacuous `Faithful` undisclosed, and the "strongest available" clause — all fixed before landing. NOT delivered: the dual image and `f^* ⊣ ∀_f` (#384), the Boolean connectives (#383), the group-action Galois connection (#381, which consumes this `Subsets`), the general-topos internal ⊆, the general right adjoint on opens and any frame or locale structure, the `FinSubsets n ≅ Subsets _` isomorphism, the link from `finpow_subseteq` to `sub_le` on `SubObj n`, naturality of any identification, and the antisymmetric quotient — the objects of `Subsets X` are bare predicates, so ≈-equal subsets are distinct isomorphic objects, a proset and not a poset, which is Mac Lane's own word
- **Instance/Powerset/Quantifier.v**: Mac Lane §IV.5 construction 4 (book pp. 96–97, `maclane:IV.5:construction4`) + Awodey §9.4 Ex 9.12 and §9.6 (`awodey:9.4:example12`, `awodey:9.6:remark-rapl-distributivity`), Seven Sketches Ex 1.117 (`7sketches:1.4.3:example117`) and Riehl Examples 4.1.8/4.1.9, Exercise 4.1.ii and §4.5 (`riehl:4.1:example8`, `riehl:4.1:example9`, `riehl:4.1:exii`, `riehl:4.5:construction-quantifier-adjoint-triple`) — **the quantifiers as the two adjoints of substitution on a power set, `∃_f ⊣ f^* ⊣ ∀_f`**, as `exists_substitution_adjunction f : DirectImage f ⊣ InverseImage f` and `substitution_forall_adjunction f : InverseImage f ⊣ DualImage f` (the issue's two pinned names) packaged as `quantifier_triple`. **THE EXISTENTIAL IS THE DIRECT IMAGE BY DEFINITION**: the first adjunction is #382's `image_preimage_adjunction` under the pinned name (`:=`, read back at `eq_refl`), so Mac Lane's "P_* S is the direct image of S under the projection P" holds at a general `f` with no comparison. **THE DESIGN CRUX IS THAT BOTH ADJUNCTIONS ARE STATED AT THE SAME `InverseImage f`**, which #380's `GaloisAdjunction` cannot do because it manufactures its own two functors: `GaloisFunctor_l … preimage_dual_galois = InverseImage f` is REFUTED at `eq_refl` — `fobj` and `fmap` agree, the three law fields are separate opaque `Program` obligations — and pinned, so the donor route would have made "f^* has both adjoints" a statement about two different `f^*` records; a new general `proset_adjunction_at` (an adjunction between prosets at two GIVEN functors, from the bare biconditional, its six `Program` obligations closed by the default tactic since the target is thin) is what carries the right half. The dual image `Powerset_Prop_dual f S := {y | ∀ x, f x ≈ y → S x}` — Awodey's `f_*(U) = {b | f^{-1}(b) ⊆ U}`, Riehl's `f_!`, Fong–Spivak's `f_*`, Mac Lane's `P_#` — needs NO truncation (a `∀` of `Prop`s is a `Prop`), membership at `eq_refl`; `preimage_is_precompose` records at `eq_refl` that #382's `Powerset_Prop_preimage f T` IS Riehl's `Δ_f p := p ∘ f`; `preimage_dual_galois` and `DualImage`. **THE UNIT AND COUNIT ARE THE TWO INCLUSIONS AT LEIBNIZ `=`** (`adj_unit_is_forall_incl`/`adj_counit_is_forall_incl`, `eq_refl`) — strictly better than #382's ascription-only `adj_unit_has_incl_type`, and exactly what that file's own note predicted for transparent donors — and `dual_galois_round_trip` returns #380's `GaloisOfAdjunction` at Leibniz `=` on the WHOLE record. **THE FOUR BOOKS DISAGREE ABOUT `f_*` AND ABOUT `f_!`, TWO WAYS EACH** (Riehl's `f_*` is the direct image, which Mac Lane writes `P_*` at the projection; Awodey's and Fong–Spivak's `f_*` is the dual image, Awodey writing `im(f)` for the direct; Riehl's `f_!` is the DUAL image, the reverse of the usual topos convention, while Fong–Spivak's `f_!` is the direct image — an earlier draft said "three ways", which an audit corrected), so the file uses no starred or shrieked name and says so. **MAC LANE'S OWN SITE, the first projection `P : U × V → U`**, is delivered at his literal formulas over `Sets_Cartesian`'s product (`ProdSetoid U V`, measured to land at `SetoidObject@{o o}`): `proj_exists S := {x | ∃ y, S (x, y)}` (stdlib `ex`, NO truncation — a brief of mine said the truncation was forced, which was FALSE), `proj_forall S := {x | ∀ y, S (x, y)}`, `cylinder Xs := Powerset_Prop_preimage proj_fst Xs` with `cylinder_mem` at `eq_refl`; p. 97's two displayed biconditionals as `iffT`s (`proj_exists_transpose`, `proj_forall_transpose`), "the largest subset X of U such that the cylinder on X is wholly contained in S" as `proj_forall_largest` (stated plainly to BE the second biconditional plus its unit), the two `GaloisConnection`s at his formulas (`proj_galois_exists`/`proj_galois_forall`), and the identifications with the general operators at `≈` ONLY (`proj_exists_is_image`, `proj_forall_is_dual` — the `eq_refl` forms refuted and pinned: `ex` over `V` against a truncated sigma over `U × V`, and different quantification carriers for `∀`). **BECK–CHEVALLEY HOLDS AT WHOLE-SUBSET LEIBNIZ EQUALITY, WHICH THE BRIEF PREDICTED WOULD BE REFUTED**: along the reindexing `cyl_reindex V g : U' × V → U × V`, `beck_chevalley_exists`/`beck_chevalley_forall` close by `eq_refl` — a first-cut negative that did NOT fire and was turned into a positive with what is measured kept apart from what is explained: `SetoidMorphism` has eta and the `proper_morphism` certificate is NOT irrelevant (two arbitrary proofs do not convert, measured), so a typechecking `eq_refl` forces both certificates to converge; WHY they converge (`cyl_reindex` reduces at a literal pair) is an explanation, the one flip experiment breaking all six Beck–Chevalley statements at once and so isolating nothing — an audit's finding. **AWODEY §9.6 LANDS AS THEOREMS ABOUT ADJUNCTIONS, NOT PROSE**: general `gal_r_preserves_glb`/`gal_l_preserves_lub` over an arbitrary `GaloisConnection` (nothing in tree related `IsGLB` to `gal_` — measured), `dual_image_preserves_meets` directly AND as the general lemma applied with no adapter (`_via_galois`, the two routes inhabiting one type), #382's `inverse_image_preserves_joins` re-derived as the COROLLARY its own comment said it wanted, and the contrapositives **`exists_not_right_adjoint : (∃ L, L ⊣ DirectImage powerset_const0) → False`** and **`forall_not_left_adjoint : (∃ R, DualImage powerset_const0 ⊣ R) → False`** — each turns the hypothesised adjunction into a Galois connection through `GaloisOfAdjunction`, applies the preservation lemma at the pair meet or join, and contradicts #382's `direct_image_not_meet_preserving` or the new `dual_image_not_join_preserving`. **RIEHL'S `Ω^X` IS `Subsets X` ON THE NOSE**: `predicates_are_subsets` and `pointwise_is_subset_le` at `eq_refl` (a predicate on `X` IS an element of `Powerset_Prop_obj X`, and the pointwise order IS `subset_le`), so Exercise 4.1.ii's identification is definitional here and no functor category `[X, Ω]` is formed (`X` is a setoid, not a category — said in terms); Example 4.1.9's triple over `!_X` is `Delta_X := InverseImage (quant_bang X)` (constant predicate at `eq_refl`), `exists_X`, `forall_X`, with `riehl_exists_delta`/`riehl_delta_forall` `:=` the general adjunctions; the truth-value object is **`subsets_one_Props : Subsets quant_one ≅[Cat] Props`** — an EQUIVALENCE of thin categories, `≅[Cat]` being `Functor_Setoid`, the object types provably different (pinned) — costing NOTHING in closure — not because `Instance/Cat.v` lies inside `Instance/Props.v`'s closure (it does not; that closure is 19 modules), as an earlier draft said, but because it already lies inside those of `Instance/Sets/Powerset/Universal.v`, `Instance/Proset/Limit.v` and `Instance/Powerset.v`, all three required anyway. Seven Sketches' clause in its own words: `apples_buckets_bicondition` and `dual_vacuous` (empty buckets count). **NON-VACUITY CORRECTED A PREDICTION OF MINE**: along #382's constant map at 0 on the two-point setoid, the dual image of `{0}` is `{1}`, NOT empty — the fibre over 1 is EMPTY so 1 enters vacuously, only the fibre over 0 is everything (`dual_const0_sng0_at_1`, `dual_const0_sng0_not_at_0`); `exists_ne_forall_at_const0` separates the two adjoints at one input; over `fin2 × fin2` the diagonal has `proj_exists` everywhere and `proj_forall` nowhere while the row `{(0,0),(0,1)}` has `proj_forall = {0}`. **Universes measured off BOTH binder and block, all 100 constants**: `X Y : SetoidObject@{o o}` is the DONORS' identification — `Powerset_Prop_obj` imposes it first, `subset_le`, `subset_le_preorder` and `Subsets` each alone — pinned at `Powerset_Prop_obj` with carrier/`is_setoid` accepted at the same declared levels (the #340 trap: a probe aimed at `Powerset_Prop_dual` would fail at its `Powerset_Prop_obj` argument and measure nothing); NO universe equation anywhere; 83 blocks carry `Set` only as the lower bound `Set < o` (`Powerset_Prop_truth`'s), 17 are `Set`-free (all of the general section including its six obligations, and the seven constants that mention `Sets` but no power set); #382's RAPL/LAPC `Set` pin is NOT inherited. **100/100 constants closed under the global context** — 86 glob heads (65 `def`, 21 `prf`) plus 14 `Program` obligations (six for `proset_adjunction_at`, three each for the two `Props` functors, two for `subsets_one_Props`), the gate carrying all 100 fully qualified; zero declaration collisions over 113 names (`cylinder` occurs in seven other files, all as prose); closure **89** excluding self (probe 90; `Instance/Powerset.v`'s own 87 dominates — marginal costs `Instance/Powerset.v` 2, `Instance/Sets/Cartesian.v` 1, every other `Require` 0); 8 `Defined` tokens of which SIX are load-bearing (flipped one at a time) and two kept for data (`quant_refl` and the written-out one of `subsets_one_Props`'s two obligations); 17 `eq_refl` statements; zero lines over 80; `make todo` grows by 12, all in the probe. `subset_le_antisym` already exists at `Instance/Grp/Galois.v` and is deliberately NOT required (that module's closure is 129 against 89), its one use written inline. Two engineering findings: the stdlib `Equivalence`/`Relation_Definitions` imports the donors need SHADOW `Category.Lib.Setoid.equiv`, so explicit occurrences are written `@Category.Lib.Setoid.equiv` and term-mode reflexivity goes through `quant_refl` (the `≈` notation is unaffected); and a `Definition` inside a `Section` whose BODY mentions a section variable makes every later lemma demand `Proof using … f`, so the two operators are declared at top level with `f` explicit, as the donors are. The issue's "Current state" is stale (#382 IS the left half; `Instance/FinSet/Subsets.v`'s `finpow_dual` evaluations exist and defer only the adjunction here). `Test/ProbeQuantifier384.v` mirrors the target's full `Require` list and carries **7 `Fail`s = 1 instrument + 6 negatives of THREE kinds** (4 CONVERSION, 1 TYPING — the reversed handedness, a plain `has type … expected` — 1 FORMABILITY), each stripped ONE AT A TIME with the whole error read, 16/16 guard coverage (four `Check` controls added after a first cut named `Subsets`, `Props` and `obj` only in prose), rename-simulated 8/8 over the target constants the negatives name. NOT delivered: the finite-code dual-image adjunction for `finpow_dual`; the slice-level `Σ_f ⊣ f^* ⊣ Π_f` (#387's; `Construction/Slice/Pullback.v` has `Bang_Functor`/`Star_Functor` and a commented stub, no `Π_f`); hyperdoctrines, Beck–Chevalley for a general pullback square, Frobenius reciprocity; naturality of any identification in `f`, `g` or the setoid; record-level comparison of `proj_galois_exists` with `image_preimage_galois`; any monad from either adjunction; the Boolean connectives (#383); nothing registered as an `Instance`
- **Instance/InnerProduct/Galois.v**: Mac Lane §IV.5 Exercise 1 (book p. 97, `maclane:IV.5:ex1`) — **the orthogonal complement as a self-Galois connection on a power set, with `LS = RS` at `eq_refl`**. The book's inner-product space is replaced by the one thing the connection consumes, a SYMMETRIC `≈`-respecting relation on a setoid — `PerpRel X` (`perp : X → X → Prop`, `perp_sym`, and a one-implication `perp_respects` whose biconditional `perp_respects_iff` is derived, the converse following from symmetry of `≈`) — the issue's own recommended scope, and the header says why symmetry is right even for a Hermitian form (`⟨x,y⟩ = 0` is symmetric though the form is not) and why nothing demands irreflexivity (characteristic-two isotropic vectors are admitted). `perp_set S := {x | ∀ s, S s → perp s x}` over `Instance/Sets/Powerset.v`'s `Powerset_Prop_obj X` needs NO truncation (`perp` is already `Prop`-valued), with `perp_set_mem` at `eq_refl`; `perp_transpose : S ⊆ perp_set T → T ⊆ perp_set S` is a `:=` term (one quantifier swap plus `perp_sym`), and **`perp_galois : GaloisConnection (@subset_le X) (op_rel (@subset_le X))`** (#380's record at the reversed inclusion on the right, the #381 ANTITONE pattern) is a record literal with no tactic whose `gal_to` and `gal_from` are that one term at swapped arguments — **`perp_galois_l_is_r : gal_l perp_galois = gal_r perp_galois` holds at `eq_refl`**, Mac Lane's "LS = RS" literally, with `perp_galois_l_is_perp_set` and `perp_to_is_from_swapped` likewise. Display (2) is #381's section (A) APPLIED, not restated: `perp_unit`/`perp_counit` are `:=` of `gal_unit`/`gal_counit` (`Instance/Proset/Galois.v`) and agree at `eq_refl` (`perp_unit_is_counit`), `perp_triple : perp_set³ S ≈ perp_set S` (the `eq_refl` form REFUTED and pinned), `ClosedPerp S := perp_set (perp_set S) ⊆ S` IS `GalClosed_r` AND `GalClosed_l` at this connection at `eq_refl` (the two halves of #381's vocabulary coincide because the two maps do), every `perp_set` closed, and **`closed_perp_iff : ClosedPerp S ↔ ∃ T, perp_set T ≈ S`** — a subset is closed exactly when it IS an orthogonal complement. Two packagings, neither bridged to the other: `perp_adjunction : PerpFunctor ⊣ PerpFunctor_r` through #380's `GaloisAdjunction` (both object actions at `eq_refl`; the two FUNCTORS do not share a type, so `LS = RS` does not lift from the maps to the functors — pinned as a TYPING negative), and `perp_AdjointOnTheRight : AdjointOnTheRight PerpOp PerpOp` in `Adjunction/Right.v`'s class with both legs one term at `eq_refl` (`perp_aor_to`/`perp_aor_from`), the THIRD inhabitant of that class in tree whose two slots COINCIDE, after `Powerset_AdjointOnTheRight` (`Right.v`) and `Structure/Monoidal/Dual.v`'s `dual_self_adjoint_on_the_right` — Structure/Topos/Power.v's `pow_self_adjoint`, the power-object functor of an elementary topos, is a FOURTH, added later — and, re-measured by shape at that later commit, the NINTH inhabitant of the class overall, the other five — `Id_AdjointOnTheRight`, the `Chain3` witness, `Adjunction/Parameter.v`'s `mutually_right_adjoint`, `Instance/Mod/Bimodule.v`'s `bimodule_two_variable_adjunction` and `Instance/Grp/Galois.v`'s `group_action_AdjointOnTheRight` — having distinct slots; an earlier draft of this bullet called it the FIFTH self-adjoint one, which an audit refuted against the file header's own enumeration (`Instance/InnerProduct/Galois.v`). **THE NAME CLASH IS DISCLOSED AND AVOIDED**: `Theory/Orthogonality.v`'s `Class Orthogonal … (e : a ~> b) (m : x ~> y)` with notation `e ⫫ m` is the LIFTING orthogonality of a factorization system, unrelated; no name here is spelled `Orthogonal`, `orthogonal` or `⫫`, and at the base commit no `.v` file mentions `perp`, an inner product or an orthogonal complement (measured). **The witness is ℤ² with the dot product under `Lib/Setoid.v`'s `eq_Setoid`, axiom-free** — Mac Lane's Hilbert space is DISCLOSED as out of reach (no reals-free inner-product structure in tree, and one over the stdlib reals would cost the two reals axioms for nothing the exercise asks), and `bool × bool` over F₂ was set aside as a poorer illustration — its isotropic `(1,1)` lies in the complement of its own singleton, though the same statements are provable there (an audit measured it, refuting an earlier draft's claim that they would be refuted; and `zz_e1_not_self_perp` is a ONE-point statement, the origin lying in the complement of `{(0,0)}` over ℤ as well): `perp_set {(1,0)} ≈ the y-axis` (`≈` only — two different `SetoidMorphism` records, the `eq_refl` form refuted and pinned), `(1,0) ∉ perp_set {(1,0)}`, `{(1,0)}` is NOT closed (its double complement contains `(2,0)`), the y-axis IS closed (proved THROUGH `closed_perp_iff`, not recomputed), `perp_set (y-axis) ≈ x-axis`, the two axes provably differ — every predicted value in the brief measured true this time. Universes off BOTH binder and block over all 63 `Print Module` names: `X : SetoidObject@{o o}` is the donors' identification (`Powerset_Prop_obj` first, `subset_le`/`subset_le_preorder`/`Subsets` each sufficient alone — #384's measurement, cited not re-run), the `PerpRel` formability negative fires at the ARGUMENT, and the record's own three fields force NOTHING — the same fields declared over `SetoidObject@{o1 o2}` are accepted at levels declared apart (an audit measured it out of tree) — so its `@{o}` binder is a declaration choice aligning it with `Powerset_Prop_obj`, the donor whose rejection is pinned as `Test/ProbeQuantifier384.v`'s negative 6 (an earlier draft said the negative measured an inherited shape); ZERO universe equations in any block; `Set` only ever as a strict lower bound (`Set < o` ×30, `Set < u` ×5, `Set < u0` ×4 and `Set < wo` ×4 across the 43 `Set`-mentioning blocks, the last thirteen all in the ℤ² section, the first `Powerset_Prop_obj`'s; an earlier draft said ×41, which an audit recounted). **64/64 constants closed under the global context** — 52 glob heads (38 `def`, 10 `prf`, 3 `proj`, 1 `rec`) plus 11 `Program` obligations `Print Module` lists and the glob cannot see, plus `Build_PerpRel`, which neither lists; zero collisions over all 64 names (instrument `Subsets` = 11 files); closure 130 excluding self (probe 131); 3 `Defined` tokens, ALL load-bearing (each flipped to `Qed` alone breaks a readback), 14 `Qed`s; 13 `eq_refl` statements; zero lines over 80; `make todo` grows by 12, all in the probe — so the issue's "`make todo` adds no new hits" box is not met as written, the probe contributing exactly the `Fail`-family lines every probe in this tree contributes and the target none; the `print-assumptions` gate carries all 64 fully qualified, `Build_PerpRel` included after an audit found it the one omitted. `Test/ProbePerp385.v` mirrors the target's `Require` list and carries **7 `Fail`s = 1 instrument + 6 negatives of THREE kinds told apart by the error text** — 3 TYPING (the covariant `GaloisConnection subset_le subset_le` ascription, the `AdjointOnTheLeft` reading, `PerpFunctor = PerpFunctor_r`), 2 CONVERSION (`perp_set³ S = perp_set S`, the ℤ² complement against the axis record), 1 FORMABILITY — each stripped one at a time with the whole error read, rename-simulated 9/10 over the target constants the negatives name, `PerpRel` being inconclusive by that method (renaming a record the target itself uses breaks the target) and `Check`-guarded instead — the builder's 10/10 counted it, and my re-run separates it. Three header defects the builder found and fixed before delivery: a consumption list naming two #381 lemmas never used (`gal_rlr_below/above`; the six actually applied are named), an "eight lemmas" count corrected to six, and the missing fifth `AdjointOnTheRight` inhabitant. NOT delivered: no inner product, bilinear form, vector space or analysis; no closed SUBSPACES (the closed elements are closed subsets in the Galois sense — identifying them with closed linear subspaces needs linear structure); no antisymmetric quotient (`Subsets X` is a preorder, two `≈`-equal complements being distinct isomorphic objects); no lattice of closed elements and no orthomodularity; no naturality in `X` or `P`; no functoriality of `perp_set` in the relation; no `AdjointOnTheLeft` refutation (neither proved nor refuted); nothing registered as an `Instance`
- **Construction/Reflective/FixedPoints.v**, **Instance/Proset/Galois/FixedPoints.v**: Mac Lane §IV.5 Exercise 2 (book p. 97, `maclane:IV.5:ex2`) + Riehl §4.2 Corollary 4.2.10, Lemma 4.2.11 and Exercise 4.2.iv (printed pp. 142-143, `riehl:4.2:cor10`, `riehl:4.2:lem11`, `riehl:4.2:exiv`) — **the fixed points of an ARBITRARY adjunction form an equivalence, and at a Galois connection between posets that equivalence is Mac Lane's bijection between the two sets of closed elements**. For any `A : F ⊣ U` (the tree's `Adjunction (F : D ⟶ C) (U : C ⟶ D)`, so `D` carries the unit and `C` the counit — the two letters exchange roles against Riehl's, said in the header), `UnitFixed A : Subcategory D` and `CounitFixed A : Subcategory C` are the full subcategories spanned by the objects where the unit, resp. the counit, is invertible (`shom` the terminal predicate, `Construction/Reflective/Idempotent.v`'s `MLocal_Subcategory` pattern, with `UnitFixed_Full`/`CounitFixed_Full`); the two closure facts the issue records as missing are `counit_iso_of_unit_iso` (two-sided inverse `fmap[F] (unit x)` — one law is the triangle identity `counit_fmap_unit` verbatim, the other holds because that arrow is invertible in its own right) and `unit_iso_of_counit_iso` (inverse `fmap[U] (counit c)`, free law `fmap_counit_unit`); the restricted functors `FixedL`/`FixedR` and `fixed_adjunction : FixedL ⊣ FixedR` are built from the ambient hom-set isomorphism through `Build_Adjunction'`; and **`adjunction_fixed_point_equivalence : EquivalenceOfCategories (FixedL A)`** (the issue's pinned name) is Lemma 4.2.11 through `fixed_AdjointEquivalence`, whose two invertibility clauses are the ambient objects' own inverses carried into the subcategory by the terminal `shom`, with `fixed_point_equivalence_swap` the other handedness. **THE HYPOTHESIS-FREEDOM IS VERIFIED AT THE SIGNATURE**: sections (A) and (B) bind `C D F U` and `A` and nothing else — no idempotency, no fullness, no smallness — which is the exercise's closing question answered YES. **THE ESSENTIAL-IMAGE CLAUSE IS DELIVERED AT ITS EXACT STRENGTH, AND THE WEAKER READING IS REFUTED**: `unit_fixed_iff_image` says the unit is invertible at `x` exactly when `x ≅ U c` for a COUNIT-FIXED `c` (`counit_fixed_iff_image` dually; both `Defined`, the forward witness `F x` with `counit_iso_of_unit_iso`, the backward direction by naturality of the unit at the isomorphism); the one-sided "`x` is isomorphic to `U c` for SOME `c`" is refuted at the free-monoid adjunction in the probe (`probe_image_of_U_not_unit_fixed`: `list bool` IS `U (F bool)` at `eq_refl`, yet `free_monoid_unit_never_iso` shows NO object of `Coq` is unit-fixed there, a two-sided inverse `k` giving `k nil :: nil = nil`), while in a thin category the two readings coincide, which is why the exercise can state the unqualified version for posets. **WHAT IDEMPOTENCY BUYS IS REFLECTIVITY, NOT THE EQUIVALENCE** (the issue's fifth work item): `unit_fixed_reflective_of_idempotent : IdempotentMonad … → Reflective (UnitFixed A)` is `Idempotent_Reflective` transported along **`unit_fixed_is_mlocal : UnitFixed A = MLocal_Subcategory (Adjunction_Induced_Monad A)`, which holds by `eq_refl` on the WHOLE `Subcategory` record** — `Monad/Comparison.v` supplies `ret` as the adjunction's unit and both records use the terminal `shom` — so no transport appears. **THE COMONAD SIDE (work item 1) IS ONE DEFINITION AND ONE MEASUREMENT**: `IdempotentComonad W H := @IdempotentMonad (C^op) (W^op) H` (`Existing Class`, the `Comonad := @Monad (C^op) (M^op)` idiom — the identifier had zero occurrences at the base commit, the issue's one accurate absence claim), `WLocal_Subcategory H := op_subcategory (@MLocal_Subcategory (C^op) (W^op) H)`, and because `op_subcategory (op_subcategory S) = S` holds by `eq_refl` (`wlocal_op_involution` — `Subcategory` is a primitive-projection record and `op_subcategory` rebuilds its four fields by eta), the dual **`Idempotent_Coreflective : IdempotentComonad W H → Coreflective (WLocal_Subcategory H)`** is `Idempotent_Reflective` at the opposites with no tactic and no transport. Its converse is delivered in the op form `Coreflective_IdempotentMonad_op` and NOT as an `IdempotentComonad`, for a TYPING reason pinned as negative 2 (the error reads `has type Monad … expected Comonad`, no `cannot unify`): the class asks for an endofunctor presented as `W^op`, and the endofunctor a coreflection induces on `C^op` is `Incl ◯ reflector`, which has no such presentation. `wlocal_sobj_strict` (membership IS invertibility of `extract` read in `C^op`, `eq_refl`) and `wlocal_obj_iff` (the covariant reading, a biconditional and NOT an equation, the two inverse laws being exchanged — negative 4). **STRENGTHS MEASURED STRICT FIRST: NINE `eq_refl` identifications** — the restricted unit and counit ARE the ambient ones on the carrier AND on the whole sigma (`fixed_unit_strict`/`fixed_counit_strict`/`fixed_unit_whole`/`fixed_counit_whole`), `wlocal_op_involution`, `unit_fixed_is_mlocal`, `wlocal_sobj_strict`, `fixed_op_unit_is_counit` (the opposite adjunction's unit IS this one's counit, same term and same type) and `counit_fixed_op_strict` — against which `CounitFixed A = op_subcategory (UnitFixed (Opposite_Adjunction F U A))` is REFUTED (negative 3, the two `sobj` fields being `IsIsomorphism` in `C` and in `C^op`), so the counit side is built directly rather than read off the unit side. **ONE `Defined` OF ELEVEN IS LOAD-BEARING, measured by flipping each alone to `Qed`**: only `fixed_adjunction`, whose `Qed` form stops `fixed_unit_strict` with `cannot unify projT1 unit and unit`; the other ten are kept `Defined` by the data convention only. Two inverse-calculus lemmas (`fixed_IsIso_along`/`_comp`) RESTATE `Theory/Equivalence/Adjoint/Compose.v`'s and on a measurement — requiring that module takes the closure from 37 to 52 (its witness section drags in `Theory/Equivalence/Strict`, `Instance/One` and `Instance/Discrete/Reconstruct`) — and, unlike the originals' load-bearing explicit binders, are written unannotated, so they identify hom with proof (`fixed_IsIso_along@{u u0 u1}` over `Category@{u u1 u1}`), which costs nothing here since every consumer is over an `Adjunction` that identifies them anyway; the third, `fixed_IsIso_of_iso`, duplicates nothing there — it runs OPPOSITE to `Theory/Isomorphism.v`'s `IsIsoToIso`, and `IsIsomorphism (to _)` occurs nowhere else in the tree, so it is new. **THE POSET FILE IS A SPECIALISATION, NOT A RE-PROOF, AND THE ONE LINE IS THE MEASUREMENT**: `galois_fixed_point_equivalence := adjunction_fixed_point_equivalence (GaloisAdjunction PA PB G)`, a `:=` with no tactic over #380's record — refuting the issue's "no Galois connection exists in-tree", stale since #380 — and what the file adds is the DICTIONARY, `unit_fixed_iff_closed_r : IsIsomorphism (unit a) ↔ GalClosed_r G a` (a reverse arrow of `Proset` IS a proof of the relation, so the `Prop` is literally the `two_sided_inverse` field and both inverse laws are `I`) with `counit_fixed_iff_closed_l` dually, both `Defined`, and the ELEMENTWISE reading Mac Lane asks for, which needs ANTISYMMETRY as an explicit `Antisymmetric` hypothesis in the shape `Instance/Proset/Galois.v`'s `mutual_le_to_eq` consumes (#380's erratum: `Poset` IS `Proset` with antisymmetry discarded): `closed_r_eq : GalClosed_r G a ↔ gal_r G (gal_l G a) = a` ("{p | p = RLp}"), `closed_r_iff_image_eq` ("equals {p | p = Rq for some q}", from #381's `gal_closed_r_image` plus antisymmetry), and Mac Lane's bijection `closed_r_to_l`/`closed_l_to_r` with `closed_round_r`/`closed_round_l` closing both round trips on the FIRST projection at Leibniz `=` — the whole-sigma round trip is NOT claimed (`GalClosed_r` is `Prop`-valued and no proof irrelevance is taken; negative 5), and `p = RLp` is not a conversion at a variable closed element (negative 6, the reason the hypothesis exists). Riehl's Corollary 4.2.10 is `gal_lrl_eq : gal_l (gal_r (gal_l a)) = gal_l a` and `gal_rlr_eq`, from #381's four closure inequalities plus antisymmetry exactly as his proof sentence runs; his direct-image illustration is `image_preimage_image`/`preimage_image_preimage` at the power set's `≈` (Leibniz equality of two `Powerset_Prop_obj` elements would need both extensionalities), and **the two "need not be equalities" witnesses ALREADY EXIST** — `Instance/Powerset.v`'s `unit_not_iso` and `counit_not_iso` — so they are cited, a correction to this issue's own plan, which asked for them to be built. Mac Lane's "group of automorphisms of a field" is NOT delivered (the tree carries no automorphism group of a field — measured, a scoped search of `Instance/Field.v`, `Instance/Field/` and `Instance/Rng/` returning nothing); the available reading is the group-ACTION connection, `group_action_fixed_point_equivalence` applying the general theorem to #381's `group_action_adjunction`, whose `closed_G_iff`/`closed_U_iff` already answer "what are these sets" (stabilisers, fixed-point sets). **The computing witness is `nat_shift_galois 2`**: closed on the left exactly when `2 <= a` (`nat_shift_closed_r_iff`), EVERY element closed on the right (`nat_shift_closed_l_all`), `1` not closed and `5` closed, and the restricted left adjoint carrying the fixed object `5` to `3` at `eq_refl` (`nat_shift_FixedL_obj`) — the exercise's bijection here is `{a | 2 <= a}` against all of `nat`, the left side a PROPER subset. Two further probe witnesses bracket the general theorem: the identity adjunction, where EVERY object is unit- and counit-fixed (`probe_id_unit_fixed`), and the free-monoid one, where NONE is unit-fixed and the counit at `FreeMon bool` is not invertible either (`free_monoid_counit_not_iso`, by injectivity of a would-be section separating the words `[nil]` and `nil`) — so the parenthetical the issue's checkbox appends to Corollary 4.2.10, that `F G F ≅ F` "in general" through `F η ≈ (ε F)^{-1}`, holds AT A UNIT-FIXED OBJECT (it IS `counit_iso_of_unit_iso`) and is refuted for the CANONICAL comparison at an arbitrary adjunction; whether some other natural isomorphism exists there is neither proved nor refuted. **Universes measured off BOTH binder and block**: every section constant is over `C : Category@{u u0 u0}`, `D : Category@{u1 u2 u2}` — hom identified with proof in the BINDER — with the one block equation `u0 = u2` (42 of the 56 blocks carry it and no other equation occurs; both object universes free), all `Adjunction`'s own (its block carries `h1 = p1`, `h1 = h2`, `h1 = p2`), guarded by two FORMABILITY negatives under `Constraint uh < up` with the category's homs, identities and functors in both directions accepted as controls — `Adjunction` (negative 7) and, with no adjunction in the command, `Subcategory` (negative 8); the sharpest binder-versus-block trap is `WLocal_Subcategory@{u u0 u1}`, whose block is LITERALLY EMPTY while its binder reads `Category@{u1 u u}`; no word-bounded `Set` in the general file, and exactly FIVE of the poset file's 27 constants carry a word-bounded `Set`, always as a strict LOWER bound and never as an equation (`Set < u` at the two image/preimage identities, `Set < u` together with `Set < u1` at the two closedness constants, `Set < u9` at `group_action_fixed_point_equivalence`, all `Powerset_Prop_obj`'s, attributed by #382 and #384), the `nat` witnesses carrying none — a `Set` pin the plan predicted and measurement refuted. `Set Default Proof Using "All"` in the poset file, the `Construction/Reflective/Idempotent.v` precedent. **56/56 + 27/27 constants closed under the global context** (54 glob heads plus the two `Program` obligations of `fixed_adj_iso`; 27 heads and no `Program`; no `Record`/`Class`/`Inductive` in either file, so no `Build_*`), the gate carrying all 83 fully qualified; zero collisions over 96 names after one was renamed before landing (`op_unit_is_counit` is `Adjunction/FullFaithful.v`'s, hence `fixed_op_unit_is_counit`); closures 37 / 139 / 162 (transitive in-project `.vo` deps excluding self; the poset file's `Instance/Grp/Galois` costs 5, and the two adjunction witnesses live in the probe because `Instance/Coq/Monoid/Free.v`'s own closure is 67, of which 44 lie outside the general file's); 13 `Defined` tokens; `make todo` grows by 9, all in the probe. `Test/ProbeFixedPoints386.v` mirrors the union of both `Require` lists and carries **9 `Fail`s = 1 instrument + 8 negatives of THREE kinds** — 2 TYPING (the equivalence is not one of `F`; the comonad-class converse), 4 CONVERSION, 2 FORMABILITY — each stripped one at a time with the whole error read, every constant a negative names guarded outside a `Fail` by one of sixteen `Check @…` controls, rename-simulated 18/18 with every rename breaking the probe at a `Check` line — the four constants the poset file consumes (`adjunction_fixed_point_equivalence`, `UnitFixed`, `FixedL`, `FixedR`) having to be renamed in BOTH library files for the simulation to reach the probe at all. One engineering note, recorded in the probe: `Instance/Coq/Monoid/Free.v` declares `MonCoq`/`UMon` as `#[local] Notation`s, which do not export, hence the probe's own `MonC`/`UMonC`. NOT delivered: `Coreflective (CounitFixed A)` (its membership reads `IsIsomorphism` in `C` where the op route reads it in `C^op`); any comparison of `WLocal_Subcategory` with `Comonad/Duality.v`'s `Adjunction_Comonad` (built through the opaque `Adjunction_Monad`, so its `extract` does not reduce); the automorphism-group-of-a-field instance; no antisymmetric quotient (the two "sets" are subtypes of a preorder); no naturality of any identification; no Eilenberg-Moore or co-Eilenberg-Moore reading; no lattice on the closed elements; nothing registered as an `Instance`
- **Instance/Pos.v**: `Pos`, the category of posets and monotone maps (Awodey §1.4) — the STRUCTURED-SETS reading, whose OBJECTS are posets, as distinct from `Instance/Poset.v` which turns ONE poset into a thin category. Neither subsumes the other. `Instance/Poset.v` cannot be reused as the donor: its `Poset` is `Definition Poset ... := Proset P`, DISCARDING antisymmetry, so a `Poset` value *is* a `Proset` value and the structure has to be bundled — which `PosetObject` does. It carries a field `Instance/Poset.v` lacks — `pos_le_respects`, the order being compatible with the carrier's own `≈` — which is an object WELL-FORMEDNESS condition, not consumed by any proof below (the file compiles without it); it is kept for the same reason `Instance/CMon.v` carries `cmon_plus_respects`. `pos_antisym` concludes `x ≈ y`, not Leibniz equality — that is what makes this the category of POSETS (the category itself compiles without it), and a disclosed deviation from Awodey's set-level statement. Morphisms are `MonoHom` (a `SetoidMorphism` plus monotonicity), compared pointwise up to `≈`; Awodey's two closure facts (identities monotone, composites monotone) are the discharged obligations of `mono_hom_id`/`mono_hom_compose`. Plus `Pos_Forget : Pos ⟶ Sets`, and the bridge to the other reading — `PosetAsCategory` sends a poset to its thin category and `MonotoneAsFunctor` a monotone map to the induced functor. NOTE #641's "alternative" (realize `Pos` as a full subcategory of `Cat` on "the skeletal thin categories `Poset P`") was not taken, for the reason the issue itself gives — it calls the direct construction the recommended principal artifact. The subcategory is in fact constructible in a few lines (`Construction/Subcategory.v`'s `sobj : C → Type` takes any `Type`-valued predicate, needing no equality on `Category` values), and "skeletal thin" does NOT misdescribe its family — skeletality is what antisymmetry buys and strictly excludes preorders. What is genuinely deferred is the AGREEMENT between the two presentations, which needs the carrier setoid and order recovered from a skeletal thin category.
- **Instance/Ord.v**, **Instance/Ord/Poset.v**: Mac Lane §IV.3 Exercise 4(a) (book p. 92, `maclane:IV.3:ex4`) — **the category `Ord` of ALL preorders (Mac Lane's Preord) and its full reflective subcategory of partial orders**, delivered as `Poset_Reflective_in_Ord : Reflective Pos_Sub` (`Construction/Reflective.v`'s record: `Pos_Sub_Full`, `Poset_reflector`, `Poset_adj`). **THE ISSUE'S "Current state" IS STALE IN ONE DIRECTION AND RIGHT IN THE OTHER**: it says "both ambient categories are missing", whereas `Instance/Pos.v`'s `Pos` (the category whose OBJECTS are posets, #641) and `Instance/Top.v`'s `Top` (#259) both exist at `d658518e`; what was genuinely absent is a category of ALL preorders — `rg -i 'PreorderObject|ProsetObject|OrdObject|OrdHom'` returned zero hits, and the only in-tree "preorder as a category" is `Instance/Proset.v`'s `Proset P`, which turns ONE preorder into a thin category — which is exactly the reviewer check on this issue, and it is delivered as CONSTRUCTIONS rather than prose: `Ord_obj_are_preorders : obj[Ord] = OrdObject` and `OrdAsCategory_obj_are_points : obj[OrdAsCategory P] = carrier …` both by `eq_refl`, their identification refuted in the probe, and the one-way passage `OrdAsCategory`/`OrdHomAsFunctor` mirroring `Instance/Pos.v`. **The name is `Ord` because the issue pins `Poset_Reflective_in_Ord`**, and the tree's own prose had anticipated "an Ord category"; the two sentences this makes false — `Instance/Proset.v` ("does not exist yet; it is #786") and `Instance/Roster.v` ("Mac Lane's Preord is `Proset`", which was never right) — are corrected in the same commit, and #786 (Seven Sketches' preorder/Bool-category equivalence) now consumes `Ord` rather than building it. `OrdObject` is `PosetObject` (`Instance/Pos.v`) MINUS `pos_antisym`, field for field, and `OrdHom` is `MonoHom` — deliberately, so that the comparison with the pre-existing `Pos` is cheap: `OrdObject_of_Poset` drops the field, `Poset_of_antisym` supplies it, BOTH object round trips close by `eq_refl` on the WHOLE record (primitive-projection eta), and **`Pos_Posets_strict_iso : @Isomorphism StrictCat Pos Posets` is an ISOMORPHISM OF CATEGORIES** (`Posets := Sub Ord Pos_Sub`), not merely an equivalence — one `eq_on_obj` family is `fun P => eq_refl` outright, the other needs one `destruct` because an object of `Posets` is a stdlib `sigT`, which `Lib.v`'s `Set Primitive Projections` does not cover, so `(`1 x; `2 x) = x` is not definitional (pinned, with the SAME statement proved by `posets_pos_posets_obj` as the control, so the negative measures the missing eta and nothing else). The reflection is the #371 design: `PosetReflection P` has the SAME carrier and the SAME order (both `eq_refl`) and differs from `P` in ONE field, its `≈` coarsened to `ord_le P x y ∧ ord_le P y x` — a setoid quotient with no equivalence class, no quotient axiom and no choice principle — so antisymmetry of the reflection is the DEFINITION of its `≈`, the projection is the identity on points, and the only new argument is the mediator's respectfulness (monotonicity twice, then the TARGET's antisymmetry, the one place membership in the subcategory is spent); the route is `universal_arrow_from_UMP` (`Theory/Universal/Arrow.v`) → `LeftAdjointFunctorFromUniversalArrows` → `AdjunctionFromUniversalArrows`, nothing re-derived, and `poset_reflect_iso` instantiates `reflective_counit_iso`. Reviewer check on the unit at its true strength: `poset_unit_is_proj` POINTWISE at `eq_refl`, `≈` as a morphism record (the class unit is the composite `fmap[Incl] id ∘ reflection_proj`), the same grade #371 and `Instance/Mod/Free.v` have. Witnesses: `Chaos2` (`bool` under the TOTAL relation, not antisymmetric by discrimination, its reflection merging the two points), `NatLe` (antisymmetric, so `natle_reflect_iso` applies and the reflection is INERT — `PosetReflection NatLe = NatLe` refuted at `eq_refl` while isomorphic, the two differing only in the setoid field), and the MIXED three-point `MixOrd` merging exactly one pair and keeping the top apart by direct computation of the relation. **A false pass caught and recorded rather than quietly fixed**: `Fail Example : obj[Ord] := OrdAsCategory P` SUCCEEDS, because Coq's `reverse_coercion` silently accepts the ascription through `OrdObject`'s coercion to `Type`; the shipped negative compares the two object TYPES instead. Universes: NO constraint block of the 122 constants carries an equation; `Ord@{…} : Category@{u u4 u4}` identifies hom with proof in the BINDER, with a binder and block character-for-character identical to `Pos`'s (measured, hence inherited from the template); `Subcategory`'s identification is guarded by one formability negative (`Reflective` cannot be tested apart from its `Subcategory` argument, the #340 `MonoidObject` trap); 113 of the 122 carry `Set` in a universe instance or constraint block, always as a BOUND, attributed by a discriminating experiment to the `Prop`-valued order (the same two-field record with `Type` in place of `Prop` carries none). 122/122 closed under the global context (Ord 64 + `Build_OrdObject`/`Build_OrdHom` invisible to both glob and `Print Module` — which WRAPS the `Record` keyword and misses `OrdHom` on a line-anchored sweep — and Poset 56 (40 glob heads + 12 `Program` obligations + 4 `MixPt` eliminators)); closures 20 and 32 (transitive in-project `.vo` deps excluding the file); zero collisions; `Test/ProbeOrd372.v` carries 7 `Fail`s (4 conversion, 1 typing, 1 formability, 1 instrument), each stripped one at a time, rename-simulated 12/13 with `Ord` itself `Check`-guarded (a whole-word rename rewrites the module path). NOT delivered: no passage from a thin category back to an `OrdObject`, no bridge to `Instance/Proset/Skeletal.v`'s `Proset_Skeleton` (the thin-category shadow of this reflection), no relation to `MonotoneFun`/`TwoPreorder`, no limits or completeness for `Ord`, no idempotent monad, no coreflection, and no specialization preorder of a space — the natural bridge to clause (b), built in neither file
- **Instance/Sets/SubobjectLattice.v**: Mac Lane §V.7 def 2 / Riehl 4.7.9 / Seven Sketches §1.2.1 (book pp. 126, 177, 8) at `Sets`, issue #445 — **the data `Theory/Subobject/Lattice.v`'s constructions are conditional on, supplied**: `Sets_zero_monic` (so `sub_bot` exists at every object; proved directly rather than through `Structure/BiCCC/Strict.v`), `Sets_HasImages` (`Instance/Sets/Image.v`'s factorization read as an `ImageOf`, nothing reproved; the ONE new piece is `im_least`, whose comparison sends an image point to `g` of its STORED preimage and whose RESPECTFULNESS is where injectivity of the competing mono is spent — the BACKWARD leg of `Instance/Sets.v`, exactly as `Instance/Powerset/Subobject.v` spends it), and **`Sets_HasWidePullbacks`, the FIRST inhabitant of `Structure/Pullback/Wide.v`'s class in the tree** (the apex is the setoid `{ a : ∀ i, carrier (A i) & ∀ i j, f i (a i) ≈ f j (a j) }` compared pointwise, the `wide_pullback_commutes` field IS a point's second component; the class instance was NOT refused on universes — `About` gives `Sets_HasWidePullbacks@{o so} : HasWidePullbacks@{o o so o} Sets@{o so}` with block `o < so, o <= compose.u0/u1/u2, o <= Projections.u0/u1, o <= ID.u0`, the index universe at `o`, the CARRIER level, exactly where `Instance/Sets/Products.v`'s header puts it for `Sets_HasIndexedProducts`; indexing by `obj[Sets@{o so}]` at the SAME instance is refused, `cannot ensure that Type@{so} is a subtype of Type@{o}`, and a freely-universed version elaborates at a different instance, so the refusal is not to be read as more). The Wide.v bullet's "declared but UNINHABITED" clause and Wide.v's own NOT-DELIVERED sentence were corrected in place in the same commit. **Fong and Spivak's worked family lands at ≈ on `SubObj`, not at membership-equivalence**: with `Sets_nat := Sets_discrete nat` and `fs_seg n := {k | 1 ≤ k ≤ n}`, `fs_union_is_positives : fs_union ≈ fs_pos` and `fs_inter_is_empty : fs_inter ≈ fs_bot`, both assembled from mutual `sub_le` through `sub_equiv_iff_mutual`, so each is an ISOMORPHISM OF DOMAINS COMMUTING WITH THE MONOS (`Instance/Powerset/Subobject.v`'s Prop-valued bridge is neither required nor used); the union is `sub_wide_join_via` at `Sets_IsIndexedCoproduct`, the intersection `sub_wide_intersection` at index 0; **the content sits in opposite halves of the two proofs** — `im_least` gives the union's easy half and exhibiting a positive `k` inside `fs_seg k` (by `Nat.le_refl`) is where the family's being INCREASING is spent, while `sub_bot_least` gives the intersection's easy half and the hard one is that `fs_seg 0` is empty, so ONE empty member empties the intersection. An independent verifier proved `fs_seg 3 ≉ fs_bot` and `fs_pos ≉ fs_seg 3`, so the setoid genuinely discriminates and neither theorem is vacuous. `fs_join_bot_left_unit` instantiates the unit law ∅ ∪ X ≅ X at `fs_seg 3`. **Strengths, measured**: `Sets_image_least_map`, `Sets_ImageOf` and `Sets_WidePullback` MUST be `Defined` — each flipped to `Qed` in turn stops the file, at the `im_least` field, at `fs_pos_to_union` and at `fs_inter_to_bot`; `fs_inter_le_bot` compiles as `Qed` and is `Defined` for uniformity. **53/53 constants closed under the global context** — 43 `.glob` heads plus the TEN `Program` obligations no source sweep sees (four of which `strings … .vo` returns ONLY with a digit-prefixed artifact name, `4seg_obj_obligation_1` for `seg_obj_obligation_1`, so the census must strip the prefix rather than discard the line); no `funext` and no stdlib axiom anywhere. The example is `Set`-pinned and the machinery is not: `fs_union_is_positives@{u}` carries `Set < u` while none of `Sets_zero_monic`/`Sets_HasImages`/`Sets_HasWidePullbacks` mentions `Set`; the inherited `Category@{u u0 u0}` collapse is `Theory/Subobject.v`'s (see the Lattice.v bullet). Boundaries pinned in `Test/ProbeSubobjectLattice445.v`, both CONVERSION: the union is ≈ the positive naturals and NOT equal on the nose (N10, `sub_dom fs_union = pos_obj` refused), and the empty-index wide pullback is terminal only up to ≅ (N11, `Sets_wpull_obj` at `I := False` against `terminal_obj` refused, its carrier being the setoid of empty families with a vacuous coherence proof rather than `poly_unit`). NOT delivered: no `IsIntersection`/`IsUnion` witnesses built here (the universal properties are Lattice.v's), no lattice or order class on `SubObj x` (the `Type`-versus-`Prop` wall `Instance/Powerset/Subobject.v` documents), no binary `sub_meet` at `Sets` though `Instance/Sets/Pullback.v` supplies the pullbacks, no right unit law, no distributivity or Heyting structure, no preservation statements
- **Instance/FinSet/Subobject.v**: the same three clauses at skeletal `FinSet`, where they **COMPUTE**, issue #445. `FinSet_HasImages` is the image by the counted-subset codec of `Instance/FinSet/Classifier.v` — hit predicate `fin_existsb (fun a => fin_eqb (f a) k)`, object `fin_countP`, mono `fin_select` monic by `fin_select_inj`, factor `fin_rank`, the same shape as `Instance/FinSet/Limit.v`'s `FinSet_IsEqualizer` one construction over. **`im_least` is what forces the choice of decision procedure**: the comparison must send the q-th selected image point to `g` of SOME preimage, and a boolean test cannot produce one — `fin_existsb_sound` can, because it returns the least witness as DATA and ends in `Defined` for exactly this consumer; no injectivity of the competing mono is spent, `FinSet`'s hom-setoid being pointwise Leibniz equality. **Fong and Spivak Exercise 1.11 made concrete, merging against tagging, by `eq_refl` with no `vm_compute` fallback**: with u = {0,1} and v = {1,2,3} inside the 4-element ambient object (each the count-codec subobject of an explicit boolean predicate, so monicity is `fin_select_inj` and the domain is a count that reduces), `sub_dom u = 2`, `sub_dom v = 3`, `sub_dom (sub_join u v) = 4` (MERGING — the count of the hit predicate of the copairing out of `Fin.t (2+3)`, evaluated), `Coprod 2 3 = 5` (TAGGING), and `sub_dom (sub_meet u v) = 1` through `FinSet_Pullbacks` over the encoded pairs in `Fin.t (2*3)`; both computed numbers are pinned by their neighbours being REFUSED by conversion (`Test/ProbeSubobjectLattice445.v` N8 `= 5%nat` against the union, N9 `= 2%nat` against the meet), which is what makes the positive `eq_refl`s evidence that the codecs reduced. An independent verifier checked the numbers are not coincidences of the ambient size — {0} ∪ {1} = 2, {0} ∩ {1} = 0, {0,1} ∩ {0,1,2} = 2, {0,1} ∪ {0,1,2} = 3, all `eq_refl` in a scratch file — and that `ex11_u`/`ex11_v` select exactly the stated elements. The exercise's arithmetic, asserted nowhere at these instances before: `Pow 3 = 8` and `2 × 3 = 6` (`2 + 3 = 5` being `ex11_coproduct_five`, not restated), in the style of `Instance/FinSet/Topos.v`. **THE ISSUE'S PRIOR-ART CLAIM IS CORRECTED IN THE HEADER RATHER THAN REPEATED**: it says the `fin_tabulate`/`fin_apply` codec elements "are never listed", and MEASURED that is too strong — nine membership bits are already in tree (`Instance/FinSet/Subsets.v`; `Instance/FinSet/Powerset.v`); what is genuinely absent is narrower: every one of those nine names its code by `fin_tabulate` of a characteristic function, NOT ONE by its POSITION in `Fin.t (finpow n)`, and at n = 2 only two of the four codes are read out. `powcode2_0`–`powcode2_3` are the four positions and the eight `powcode2_i_at_j` Examples the complete bit table (position 0 ↦ {0,1}, 1 ↦ {0}, 2 ↦ {1}, 3 ↦ {}), with `finpow_two_is_Pow` pinning `finpow 2` to `Structure/Topos.v`'s `Pow` at `FinSet_Topos` by `eq_refl`. Seventeen `eq_refl` Examples in all (`grep -c ':= eq_refl'`). **46/46 constants closed under the global context**, no `Program` obligations, no stdlib axiom; `About FinSet_HasImages` carries the single-line block `Set < u1`, inherited from `FinSet` itself (objects are `nat`). `finset_image_least` is the only `Defined` and compiles as `Qed`, so its transparency is for uniformity — the Examples read `finset_image_obj`/`finset_image_mono`, never `im_least`. NOT delivered: no `HasWidePullbacks FinSet` (the count codec does not obviously extend to an arbitrary index type — the wide case is at `Sets` only), no `IsMeet`/`IsJoin` witnesses, no lattice or order instance on `SubObj n`, no comparison with `Instance/FinSet/Subsets.v`'s `finpow_le` or with any orthogonal factorization system, and no comparison ARROW between the union and the coproduct — only their object sizes are compared
- **Instance/Sets/QuotObj.v**: Mac Lane §V.7 def 3 (book p. 126, `maclane:V.7:def3`) at `Sets`, issue #446 — **Mac Lane's "where epis are onto" clause, in both directions and with NOTHING assumed**: `Sets_quot_epi_surjective` (the epi of a quotient object is a surjection) and `Sets_quot_of_surjection` (every surjection presents one), both `Instance/Sets.v`'s `surjectivity_is_epic` read through the accessors, both its legs included; no stability or decidability hypothesis, `≈` being `Type`-valued so the backward leg RETURNS the preimage — exactly the clause `Instance/Grp/QuotObj.v` cannot have. `SetsQuotient_QuotObj` packages `Instance/Sets/Quotient.v`'s same-carrier-coarser-`≈` quotient with its projection, the surjectivity witness being `Equivalence_Reflexive` and nothing else; the `SetoidCoarser` argument is a FOURTH argument and is not optional, the projection not existing without it. **The COIMAGE as an `ImageOf` in `Sets^op`** (`Sets_CoimageOf`; `Sets_HasCoimages` its class form, an `#[export] Instance` so that `HasImages (Sets^op)` resolves — an audit found the first draft a plain `Definition`, which resolution cannot see; universes allow it, `HasImages@{so o} Sets@{o so}^op`): every field of `Theory/Subobject/Lattice.v` turns over, `im_sub` a quotient object of the DOMAIN by the kernel relation `f a ≈ f b` (whose object-level twin is `Instance/Sets/Pullback.v`'s `sets_ker`, the kernel pair), `im_least` saying the coimage is COARSER than every quotient through which f factors. **One expectation was wrong and the file records it**: `im_least` is NOT `sets_quot_med`'s job — that mediator maps OUT of a quotient, while `im_least` needs a map INTO the coimage and must therefore CHOOSE a preimage; the choice is free (surjectivity is data) and is well defined into the coimage though not into X. **The join transports in one line**: `Sets_quot_join := quot_join q r` at `Sets_Cartesian` (`Instance/Sets/Cartesian.v`, which IS a `@Cocartesian (Sets^op)` on the nose) and `Sets_HasCoimages` — a first draft said the join needed "a product instance not assembled here", which an audit refuted by compiling the line, so it is landed and the false obstruction withdrawn; no pair of quotients has its join computed. **A worked binary MEET at the strongest available reading**: `Sets_quot_meet_parity_mod3 : quot_meet Sets_Q_parity Sets_Q_mod3 ≈ Sets_Q_total`, the meet being the PUSHOUT of the two epis (`Instance/Sets/Pushout.v`), proved WITHOUT touching the apex — both sides are glbs of the same two-element family and `Theory/Subobject/Lattice.v`'s `IsIntersection_unique` identifies them — with `Sets_quot_meet_collapses` reading the same fact off the apex itself; the only arithmetic is `Sets_meet_collapse` (m ~ m+3 mod 3, m+3 ~ m+1 in parity, hence consecutive, hence all), and BOTH invariant equations are `eq_refl`, `nat_tri` being a three-step fixpoint in place of `Nat.modulo` for exactly that reason; `Sets_Q_total_below` puts the total quotient BELOW every quotient of `NatDiscrete` (it factors through every one — a first draft's comment had this direction inverted and is corrected in place). **The BOTTOM quotient object at every inhabited setoid, and the measurement of `quot_bot`'s hypothesis**: `Sets_one_epic_of_inhabited` discharges `Epic (one : X ~> 1)` from a point in three lines with nothing assumed, so `Sets_quot_bot` is the coarsest quotient X ↠ 1 of every inhabited setoid, and `Sets_one_not_epic_empty` refutes the hypothesis at the empty setoid (`Sets_pick true`/`Sets_pick false` agree after ∅ → 1 for free and differ at `ttt`) — `Epic one` is exactly inhabitedness in `Sets`, object-dependent and not rare; the theory file's header had claimed this discharge "lives with the witnesses" before any had been landed, which the integration audit caught, and it was landed instead of retracted. **33 constants, every one `Print Assumptions`-closed** (33 `.glob` heads, no `Program` so `strings … .vo` returns no obligation; an earlier revision of this bullet said 29, before the bottom-object constants); 5 `Defined` / 7 `Qed`, of which exactly TWO are REQUIRED transparent — `sets_coimage_med` (flipped to `Qed` it stops the file at `sets_coimage_least`'s `change`, "Not convertible") and `Sets_pick` (flipped, `Sets_one_not_epic_empty`'s `discriminate` reports "Not a discriminable equality") — the other three recompiling green; closure 74 files COUNTING the file itself (73 without it), by iterated `coqdep` and cross-checked against `.Makefile.coq.d` (an earlier revision of this bullet said 75, from a script that counted `Lib.v` twice under two spellings of its path; corrected during #447's integration); no name occurs elsewhere in the tree (swept over all `.glob`, instrument-checked on `sub_le` — a first draft's `sets_ker` collided with `Instance/Sets/Pullback.v` and was renamed). Universes `@{o so}` with `o < so`, the `Instance/Powerset/Subobject.v` idiom; the `Category@{u u0 u0}` collapse is `Theory/Subobject.v`'s and `Sets@{o so} : Category@{so o o}` satisfies it on the nose; no `Set` pin. Boundaries in `Test/ProbeQuotObjWitnesses446.v` (N4 the meet is ≈ and not `=`, N5 `QuotObj X` is not `SubObj X`, N6 the covariant setoid spelling ELABORATES at a concrete category but is not `eq_refl` — all CONVERSION). NOT delivered: no worked join and no lattice laws restated at `Sets`; no top quotient object (the bottom one is delivered at inhabited setoids only, and `Sets_Q_total_below` is likewise specific to an inhabited carrier since its constant map must name an element); no indexed meet; no relation between `Sets_coimage` and `Instance/Sets/Image.v`'s image; no dual of `sub_le_unique`; no `eq_refl` readback anywhere in this file — the computing half is `Instance/FinSet/QuotObj.v`
- **Instance/Grp/QuotObj.v**: Mac Lane §V.7 def 3 (book p. 126, `maclane:V.7:def3`), the group illustration, issue #446 — **the quotient objects of a group ARE the G/N**, and the issue's "the library having no group theory of that kind" is measured STALE: `Instance/Grp/Quotient.v` already carried `Subgroup`, `NormalSubgroup`, `QuotientGrp`, the projection with its epicness and `KernelNS`, and `Instance/Grp/Quotient/Isomorphism.v` the first isomorphism theorem with its triangle; this file spends those donors and adds no group theory. One direction is a line, `grp_quot_of_normal N := mk_quot (quot_proj N) (quot_proj_epic N)`, and the order comes with it: `grp_quot_le_of_included` — N ⊆ N' gives G/N' ≤ G/N, the COARSER quotient being the LOWER one, with the identity function as mediator and its respectfulness clause exactly the inclusion. **The converse is where the constructive cost sits and the TYPE says so**: `grp_quot_is_quot_by_kernel` takes `GrpSurjective (quot_epi q)` (`Instance/Grp/Epi.v`) and gives `q ≈ grp_quot_of_normal (KernelNS (quot_epi q))` through `iso_compose (surjective_image_iso e Hs) (first_isomorphism_theorem e)`, with `grp_quot_kernel_triangle` showing the comparison carries the projection to e — which is what makes it an equivalence OF QUOTIENT OBJECTS and not a bare isomorphism of codomains, packaged by `Theory/Subobject/Quotient.v`'s `quot_equiv_of_cod_iso` (first proved HERE during the parallel build and lifted at integration). Surjectivity is NOT available from `Epic`: "surjective implies epic" is unconditional but the converse is the double-negation elimination that file's header argues, the unconditional theorem being `grp_epic_image_dense`, so the `Epic`-only reading is the separate CONDITIONAL `grp_quot_is_quot_by_kernel_stable` over `GrpImageStable` — and `stability_is_the_conclusion` shows that for an epimorphism that hypothesis IS the conclusion restated, so the conditional form buys nothing free. Non-vacuous at A3 ⊴ S3 (`Instance/Grp/Quotient.v`), nonabelian with a proper nontrivial normal subgroup: `grp_quot_S3_A3` and the order inhabitant `grp_quot_S3_trivial_le_A3` carry the new content, `grp_quot_S3_A3_not_collapsed` being a labelled verbatim re-export of `S3_mod_A3_not_collapsed`. **10 constants, every one `Print Assumptions`-closed**, no `Program` and so no obligations; 1 `Defined` / 5 `Qed`, the `Defined` recompiling green as `Qed` and kept transparent because `quot_le` is data; closure 94 files COUNTING the file itself (an audit found the four #446 bullets mixing conventions — the theory bullet excludes the file, the witness bullets include it — so each now says which); zero collisions. Everything is at ≈ — `QuotObj` inherits `SubObj`'s setoid (`Theory/Subobject.v`) and has no Leibniz antisymmetry, so no statement here is or could be an `eq_refl` readback. NOT delivered: no correspondence theorem (only the cheap direction of the order; the converse would need the mediator's action on members recovered from the factorization); no lattice, `Instance/Grp/Pushout.v` supplying pushouts but nothing identifying the pushout of two projections with the quotient by the join of the two normal subgroups; no `ImageOf` at `Grp^op` though `Isomorphism.v`'s `factorization` exists; no enumeration of the quotient objects of any finite group
- **Instance/FinSet/QuotObj.v**: Mac Lane §V.7 def 3 (book p. 126, `maclane:V.7:def3`) at skeletal `FinSet`, where the meet **COMPUTES**, issue #446. The onto clause is both ways and UNCONDITIONAL from `Instance/FinSet/Classifier.v` — no stability hypothesis, unlike `Grp`, the probe there being the classifier's own characteristic map into Ω = 2 over a decidable finite search — and `finset_no_quot_of_const` records the other side positively as a labelled verbatim re-export of refutation for the constant map 1 → 2, because a refutation probe on `mk_quot f _` would report an uninstantiated evar and not the mathematical fact. **`quot_meet` at FinSet is the union-find pushout of `Instance/FinSet/Pushout.v`, and it reduces**: with the two surjections 4 ↠ 3 that identify {0,1} and {1,2}, `finset_quot_meet_two : quot_cod finset_q_meet = 2%nat := eq_refl` — no tactic, no `vm_compute` fallback — with `finset_quot_meet_merges_0_2` and `finset_quot_meet_merges_1_2` reading the identifications off the EPI of the meet at `eq_refl`, `finset_quot_meet_separates_0_3` refuting the collapse by `discriminate`, and both factors pinned at 3 so the meet is visibly a coarsening of each; `Test/ProbeQuotObjWitnesses446.v` N1 refuses `= 3%nat` against the meet and N2/N3 the two neighbours, all CONVERSION, which is what makes the positive `eq_refl`s evidence that the codecs reduced. Leibniz equality is the right relation for every one of these, the claims being about DATA (a natural number, an element of `Fin.t 2`) and FinSet's hom-setoid being pointwise Leibniz in any case; nothing about a `QuotObj` itself is claimed at `=`. **Coimages, where balancedness shows up as an identity of terms rather than as a theorem**: `FinSet_CoimageOf`/`FinSet_HasCoimages` (the latter an `#[export] Instance`, so `HasImages (FinSet^op)` resolves — a first draft was a plain `Definition`) at `FinSet^op` take the coimage of f to BE the image object of `Instance/FinSet/Subobject.v` with the image FACTOR as its epi and the image MONO as the map to the codomain, so `finset_coimage_commutes` is `finset_image_commutes` VERBATIM with no transport at all; only `im_least` does work, and only because it must choose preimages under a competing epi, `fin_rank_resp` (`Instance/FinSet/Pushout.v`) discharging `fin_rank`'s proof argument from decidable equality of booleans so that no proof irrelevance and no axiom enters. Two coimage cardinalities pinned at `eq_refl` (3 for the collapsing map, 1 for a constant). **24 constants, every one `Print Assumptions`-closed** — the union-find pushout, the rank/select codec and the classifier probe all stay axiom-free — no `Program` and so no obligations, and **0 `Defined` / 6 `Qed`: nothing here needs transparency**, every computing readback going through `Definition`s and record projections rather than a proof term. Closure 91 files COUNTING the file itself, of which `Instance/FinSet/Regular.v` contributes exactly ONE and is required only for `fin3_cases`. Zero collisions after two sweep rounds: a first draft named the codomain elements `fin3_0`/`fin3_1`/`fin3_2` and restated `fin3_cases`, and the sweep found `Structure/Limit/Power/Hom.v` and `Instance/FinSet/Regular.v`, so the elements are written with bare constructors and the case analysis is required rather than duplicated. Universes: the `Set <` bounds are FinSet's own (its objects are `nat`) and the `SubObj` hom-equals-proof collapse is `Theory/Subobject.v`'s, neither introduced here. NOT delivered: no join, no lattice laws, no wide meet; no Bell-number count and no enumeration of the quotient objects of n; **no claim that `FinSet_HasCoimages` and `FinSet_HasImages` assemble a factorization system, and no general "coimage ≅ image" theorem** — here the two SHARE an object by construction, which is a fact about this presentation of the image and not a proof of balancedness; no `Decategorify` reading as a partition; no timing claim
- **Instance/Sets/Generator.v, Instance/Grp/Generator.v, Instance/Ab/Generator.v**: Mac Lane §V.7 def 4 / Awodey §7.2 def-generator / Riehl 4.7.7 and E.4 (book pp. 127, 154, 177, 257), issue #447 — **the concrete separators, against `Structure/Generator.v`'s `IsSeparator`/`Generator`**. `Sets_unit_separates` (global elements distinguish setoid maps: instantiate the hypothesis at the constant map `sets_elem x` out of `Instance/Sets.v`'s `unit_setoid_object` and evaluate at `ttt`), `Grp_free_one_separates` (`FreeGrpObject unit_setoid_object` — Awodey's free group on one generator, via `Instance/Grp/Free.v`'s `free_grp_extend` at the constant map and `free_grp_extend_generators`) and `Ab_free_one_separates` (`FreeAbObject unit_setoid_object` — **Mac Lane's "the integers generate Ab", with his ℤ replaced by the free-on-one-generator object representing the same functor**), each packaged as `Sets_Generator`/`Grp_Generator`/`Ab_Generator` through `Generator_of_separator` (whose `{C}` must be given explicitly, the object argument being a coercion target). **The Ab proof is ONE `exact` and the Grp proof needs two `rewrite`s, and the difference is a measured strength of the donors**: `Instance/Ab/Free.v`'s `free_ab_extend_generators` is an `eq_refl` `Example` (the extension IS the fold `fa_eval` over the inductive `FATerm`), while `Instance/Grp/Free.v`'s is only at ≈ (the extension is `fmap` of a functor through `FreeGroupoidFunctor`, whose value on a one-letter word comes from a `Qed`-opaque lemma). **THE ISSUE'S PRIOR-ART CLAIM IS CORRECTED IN THE HEADER RATHER THAN REPEATED**: #447 says there is "NO definition of a separating set / separator / generating family anywhere", and measured that is STALE — `Theory/Concrete.v` already carries `Class Separator` (the same cancellation law), **BOTH directions of the Awodey equivalence with faithfulness of the representable** (`Concrete_of_Separator`, `Separator_of_Faithful`), `WellPointedCategory`, the `#[export] Instance Sets_Separator` proving the terminal object separates `Sets` and `Sets_empty_not_Separator`; so the Awodey checkboxes "the terminal object generates Sets" and "generator ⟺ `Faithful [Hom C,−]`" were discharged before this issue, and what these files add is the same content in the frozen vocabulary plus the singleton reading, the `Generator` packages, and the Grp and Ab witnesses. The two vocabularies are bridged both ways by `Structure/Generator/Concrete.v`'s `IsSeparator_of_Separator`/`Separator_of_IsSeparator` (one line each, no proof content), and `Sets_terminal_separates_from_Concrete` makes the staleness disclosure machine-checked. **The singleton and the terminal object are TWO statements**, `@terminal_obj Sets Sets_Terminal = unit_setoid_object := eq_refl` being refused `cannot unify "1" and "unit_setoid_object"` (CONVERSION — is a bare record over `Lib/Setoid.v`'s `unit_setoid`, a `Program` record whose setoid field is `Instance/Sets.v`'s `Unit_Setoid`, the identity with THAT record being accepted at `eq_refl` as the probe's positive control), so `Sets_terminal_separates` — the issue's pinned name — is proved three ways and all three are landed: DIRECT via `sets_elem_term` (the delivered constant), TRANSPORT along `Sets_terminal_unit_iso` through `Structure/Generator.v`'s `separator_iso`, and by unpacking the prior art; no claim that the three terms are equal, `IsSeparator` landing in `Type`. **The non-example is a THEOREM and the header says why a `Fail` cannot express it**: `Sets_empty_not_separates` refutes separation by the initial object (`Instance/Sets.v`), stated at `@terminal_obj (Sets^op) Sets_Initial` — a different term from `Theory/Concrete.v`'s `empty_setoid_object`, so neither subsumes the other — and is delivered TWICE, polymorphically through `Sets_two_arrows` and as `Sets_empty_not_separates_pick` through `pick_true`/`pick_false`, the second pinned at object universe `Set` because `About pick_true` shows `Instance/Sets.v` already minimized THAT constant's object universe to `Set`. **19 constants, every one `Print Assumptions`-closed** — 11 + 3 + 3 `.glob` heads plus the two `Program` obligations `sets_elem_obligation_1`/`sets_elem_term_obligation_1`, which `strings … .vo` returns (one under a junk digit prefix, stripped; `Print Module` lists the same two as `Parameter` and nothing else) — **and no stdlib axiom in the `Grp` or `Ab` layer either**. 1 `Defined` / 4 `Qed` in the Sets file, 1/1 and 1/1 in Grp and Ab, counted by token (the Grp and Ab `Defined.` close a one-line `Proof.`, so a line-start count would miss them — an audit caught a first draft saying 0/1): `grp_point`, `ab_point`, `sets_elem` and `sets_elem_term` are all REQUIRED transparent (each flipped in turn stops its proof — `grp_point`'s at the `assert` of the generator equation, the `ab_point` message being "The term "Hk (free_ab_extend (ab_point x)) (fa_gen ttt)" has type … while it is expected to have type "f x ≈ g x""), while `Sets_terminal_unit_iso` recompiles green as `Qed` and stays `Defined` because it is data. Closures 22 / 56 / 45 files NOT counting each file itself, by iterated `coqdep` (23 / 57 / 46 with it); `Theory/Concrete.v` and `Structure/Generator/Concrete.v` add exactly two files to the `Sets` closure. Zero collisions over all `.glob` (instrument-checked on `sub_le`); `sets_const`, `sets_point`, `Sets_Separator` and `Separator` were all found taken and avoided. **Universes: annotated `@{o so}` throughout and the annotations are load-bearing** — unannotated, `Sets_unit_separates` reads `IsSeparator@{u u Set} unit_setoid_object@{Set Set}`, a `Set` pin on the object universe; annotated it reads `IsSeparator@{so so o} unit_setoid_object@{o o}` with `o < so`, matching `Sets_Separator@{o so}`. The `p = h` collapse on every statement is `Structure/Generator.v`'s (`About IsSeparator` gives `Category@{u0 u1 u1}`) and is faithful to its model, `About Cogenerator` (`Adjunction/SAFT.v`) and `About Separator` giving the same. A `Program` trap is recorded in the `Grp` and `Ab` headers: under the universe annotation `Program Definition grp_point` does not register the constant before its obligation is met and the next proof reports "The variable grp_point was not found in the current environment", so both points are built by `refine`. **Integration removed the witness half's own copies of the iso-transport (`separator_along_iso` with its helper `iso_cancel_right`, itself a re-proof of `Theory/Equivalence/Monoidal.v`'s `iso_cancel_spine_ft`) and of the Concrete bridge pair**, keeping the theory half's `separator_iso` and `Structure/Generator/Concrete.v`; the headers record the removal. Boundaries in `Test/ProbeGeneratorWitnesses447.v` (N1 the singleton is not the terminal object; N2 separation at `=`; N3 the two composites swapped, the coseparator's clause; N4 `IsSeparator Ab ab_int` from the free-abelian-group theorem; N5 the singleton proof against the terminal-object statement — all CONVERSION). NOT delivered: no `FreeAbObject unit_setoid_object ≅ ab_int` (`Instance/Ab/Free.v`) — and the reason is NOT a missing ℤ-action, `zsmul` with `zsmul_add`/`zsmul_hom` being at `Instance/Ab/Monoidal.v` (a first draft of the Ab header said the tree had only the ℕ-scaled `nat_smul`, which an audit refuted); what is open is the isomorphism itself and its two inverse laws by `FATerm` induction, so "ℤ separates Ab" is NOT a theorem here; no isomorphism `FreeGrpObject unit_setoid_object ≅ Int_Plus_Grp` either — ℤ IS in `Grp`, as `Construction/Deloop/Functors.v`'s `Int_Plus_Grp` (a first draft of this bullet and of the Grp header said no ℤ group object existed, from a sweep that saw only the tactic-mode `: GrpObject.` constants and missed every `:= {| … |}` one; the integration audit found it, and the correction is recorded in place), the five abelian names being the `AbObject := ring_ab Int_Ring` side; no generating families of more than one object; no smallest-separator claim; no size conditions (Giraud's clause (vi)).

## Applied Programming (Theory/Coq/)
Bridges pure theory with practical Coq programming:
- Applicative functors proven via monoidal functors
- Traversable functors with laws
- Monad transformers with coherence conditions — `MonadTransformer` and `MonadTransformerLaws` are in **Monad/Transformer.v**, NOT under `Theory/Coq/`; they are listed here because they belong to this applied layer thematically
