# Flagship inhabitation

This document records, for the library's headline results, whether the
distinctive hypothesis of each result is satisfied by a concrete object
constructed inside the library, or whether the result stands as an
axiom-free *conditional* whose hypothesis no in-tree object yet meets.

Read this scope precisely.  Every result named below is proven, and (per
`docs/AXIOMS.md`) the audited ones are free of axioms.  The question here is
a different and complementary one: is the theorem *about* something the
library actually builds?  A theorem quantified over `Complete C` is a real
theorem whether or not any `Complete` category is exhibited; but a reader
calibrating how much the library demonstrates should know which results come
with a model and which do not.  Proving a theorem parametrically and
instantiating it in-tree are distinct achievements, and this table keeps
them apart.

Two cautions apply throughout.  First, "no in-tree witness" is not
"unsatisfiable": in most cases a model exists in ordinary mathematics and
simply has not been formalised here.  Second, a witness is not always
merely absent for want of effort — the library's universe discipline can
make a particular concrete instantiation impossible, as the cospan row
below shows, so the fact that a premise is inhabited in mathematics does not
guarantee it can be inhabited in this development.

**The size wall, in brief.** The one universe obstruction that recurs in the
adjoint-functor rows below has a single authoritative statement, the size note
in the header of `Structure/Complete.v`; this paragraph and the next are an
echo of it, and any disagreement between them is to be resolved in that file's
favour. The arithmetic is that a `Prop`-valued relation on a carrier in
`Type@{u}` is itself a type in `Type@{u}`, while a `Type`-valued one lands in
`Type@{u+1}`. This library's `≈` is `Type`-valued and stays so, because in
`Cat` an `F ≈ G` is a family of isomorphisms and truncating it would discard
data. So the congruences on a carrier, and with them the family of quotient
presentations of a term model that Mac Lane's §V.7 solution-set argument
produces, sit one universe above that carrier.

Against that, `Adjunction/GAFT.v` applies its completeness hypothesis to the
discrete category on the solution-set index, which identifies the index
universe with the shape-object universe of `Complete`; the equalizer of all
endomorphisms in the same proof pushes that universe up to the ambient hom
universe; and `Sets_Complete` caps it at the carrier universe, a shape `Ab` and
`RMod` inherit through limit creation. A solution set indexed by a Σ over the
OBJECTS of a concrete algebraic category therefore sits one universe too high,
and so does one indexed by `Type`-valued congruences. The escape routes — a
universe lift, inductive cumulativity, an SProp-valued `≈`, a `Prop`-truncated
kernel, propositional resizing, index-free large completeness — were each
compiled and refused, and are enumerated in the size note. The resolution this
library adopts is `PropEquiv` (`Lib/Setoid/Propositional.v`) on the object
records of the concrete algebraic categories; the rows below say, per result,
how far that has been carried.

An earlier revision of this paragraph described that resolution as a plan. It is
landed. Since 2026-09-17 the object records of `CMon`, `Ab`, `RMod R`, `Grp`,
`Rig`, `Ring`, `Rng` and `Rg` carry the property (`cmon_prop`, `rig_prop`,
`grp_prop`), so the congruences on their term models are `Prop`-valued and
carrier-sized, and the adjoint-functor rows below record the consequence in the
only form that matters here: the tensor product of modules, the balanced tensor,
the free rings and the free group are now obtained from `representability_theorem`
and `GAFT` with no hypothesis beyond the ring or the setoid, and none of those
applications is circular. Since #450 both lists are longer, as the ADDENDUM
(#450) to item 4 of that size note records. The categories include the setoid
variety `SVariety E` of `Instance/Variety/Free.v`, for every operation
signature and set of equations, whose `SetoidOpAlgebra` record carries
`soa_prop`. The consumers include three more files: `Instance/Grp/Colimit.v`
and `Instance/Rng/Colimit.v` apply `GAFT` at the diagonal functor of every
shape with homs at the carrier universe and objects at or below it (the shape
discipline `Cocomplete@{u u0 u u1}`), with solution sets
(`Diagonal_Grp_solution_set`, `Diagonal_Rng_solution_set`) indexed by
`Prop`-valued cocone congruences on a term model, the kernel of a cocone being a
`Prop` because its target carries `grp_prop` or `rig_prop`, and obtain
`Grp_Cocomplete_via_GAFT` and `Rng_Cocomplete_via_GAFT`;
`Instance/Variety/Colimit.v` does the same for `SVariety E` with two
congruence-indexed solution sets,
`Diagonal_SVariety_solution_set` at the diagonal and
`SVariety_Forget_solution_set` at the forgetful functor, whose kernels are
`Prop`s because the target carries `soa_prop`, and obtains
`SVariety_Cocomplete_via_GAFT` and Mac Lane's free algebra
`Free_Variety_via_GAFT`. The side condition is the same `Set < carrier`, and at
`Grp`, `Rng` and `SVariety E` it is measured to be `GAFT`'s: the three diagonal
solution sets elaborate at `Set` carriers, and so does `GAFT` applied to every
argument but the solution set, while `Grp_colim_via_GAFT`, `Rng_colim_via_GAFT`
and `SVariety_colim_via_GAFT` are refused there, `GAFT` putting the index AT the
carrier universe and the index sitting strictly above `Set`
(`Test/ProbeAlgColimit450.v`'s N4 and N5 and `Test/ProbeVarietyColimit450.v`'s
N4, with those accepted applications as controls). Since #454 a fourth consumer
is measured the same way, as the size note's
ADDENDUM (#454) records: `Instance/Mod/Colimit.v` applies `GAFT` at the
diagonal into `RMod R` for every ring, with `Diagonal_RMod_solution_set`
indexed by `Prop`-valued cocone congruences on `Instance/Mod/Free.v`'s term
model, and obtains `RMod_Cocomplete_via_GAFT`, its `Set < carrier` again
`GAFT`'s (`Test/ProbeWatts454.v`'s N14). And
`Instance/Mod/Watts/Unconditional.v`'s `RModop_esols`, the element-wise
solution set behind the unconditional Watt theorem, is indexed by the
`Prop`-membership submodules of ONE fixed module paired with an element, at the
carrier universe `representability_theorem` pins, and not by a Σ over the
objects of `RMod R`. What the resolution does NOT
do is lift the wall for a solution set indexed by a Σ over the OBJECTS of such a
category: Mac Lane's own
`Subgroup G` family is still not built, `tensor_esols_direct` is still refused as
a direct premise, and `GAFT_from_spanning` is still refused at `RMod R`. Those
three are the honest residue, and they are recorded per row. Since #450 a
fourth of the same kind is measured: `Instance/Variety/Spanning.v`'s
`variety_solution_set`, indexed by a Σ over the objects of `SVariety E`, is
refused by `GAFT` even with the real `SVariety_Complete` and
`SVariety_Forget_continuous` as its other arguments (that file's header quotes
the refusal), where the congruence-indexed `SVariety_Forget_solution_set` is
accepted by the same call.

## Witnessed results

Each result here is instantiated by a concrete object constructed in the
library, so the "Closed under the global context" report on it certifies a
result about something the library actually contains.

| Result | Distinctive premise | In-tree witness |
|--------|---------------------|-----------------|
| `classifier_classifies`, `relations_iso` | an `ElementaryTopos` | `FinSet_Topos` (`Instance/FinSet/Topos.v`); at `Sets` the theorem is not even STATABLE — its own construction builds a setoid whose carrier is `SubObj x`, forcing objects ≤ homs, while `Sets@{o so} : Category@{so o o}` has o < so (a universe inconsistency, pinned in `Test/ProbeClassifier402.v` against `char_reindex` as the accepted control) — so `FinSet_Topos` remains the only in-tree witness for THESE TWO CONSTANTS, and the reason is now sharper than a missing instance: since #404 the tree has TWO further assembled toposes, `Sets_Topos` (`Instance/Sets/Topos.v`) and `Presheaf_Topos` (`Instance/Fun/Topos.v`), and BOTH `classifier_classifies` and `relations_iso` are REFUSED at each of them — four universe inconsistencies, all four pinned in `Test/ProbeToposInstances404.v` with `Pow` at those toposes as the accepted control — so this row's witness column gains nothing from them; `Instance/Sets/Classifier/OneLevel.v` supplies a conditional `SubobjectClassifier Sets`, and `Instance/Fun/Classifier.v` a conditional one for presheaf categories — where the theorem IS formable at an abstract classifier of `[C^op, Sets]` (unlike at `Sets`), is refused at the built `Fun_Classifier` (whose elaboration minimizes the presheaf category's hom universe below its object universe), and DOES apply to the re-annotated `Fun_Classifier_small`, giving `fun_classifier_classifies` and its naturality upgrade `fun_sub_classifier_natural`; all three measurements are pinned in `Test/ProbeFunClassifier403.v` |
| `power_object_monadic`, `topos_has_finite_colimits`, `topos_finitely_complete` (`Structure/Topos/{Monadic,Colimits}.v`) | an `ElementaryTopos` (NO further premise: the theorems are unconditional over the class, and their universe instances leave the ambient object universe FREE and carry no `Set`) | `FinSet_Topos` (`Instance/FinSet/Topos.v`), the only UNCONDITIONAL assembled topos of the tree's three — the other two being `Sets_Topos` (`Instance/Sets/Topos.v`, CONDITIONAL on `Untruncate`, or on `DecImage`, or on `IEM`) and `Presheaf_Topos` (`Instance/Fun/Topos.v`, CONDITIONAL on `Untruncate`; `Copresheaf_Topos` is the covariant reading), neither premise having an axiom-free in-tree inhabitant, so this row's "NO further premise" column stays true of the THEOREMS and must not be read as saying the two new witnesses are unconditional.  All three of this row's constants DO elaborate at both, and their colimit legs are exhibited in `Test/ProbeToposInstances404.v`: at `Sets` the derived initial object, binary coproduct, coequalizers, pushouts and equalizers are all SECOND inhabitants, ascribed at `Sets_Initial`/`Sets_Cocartesian`/`Sets_HasCoequalizers`/`Sets_HasPushouts`/`Sets_HasEqualizers`' own types so the types are seen to line up, with NO agreement proof built; at `[C^op, Sets]` all five would be the FIRST inhabitant of their class at any functor category, measured by three sweeps whose criterion `Instance/Fun/Topos.v`'s header states.  At FinSet the derived initial object, binary coproduct and pushout agree with the computable ones up to `≅`; the `eq_refl` form is refused for the initial object and that one refusal is pinned in `Test/ProbeToposColimits405.v` with its cause NOT isolated (the derived object is `Crude_Inverse` at the terminal algebra, a chain that never consumes `PowF_ReflectsIsos`), while the coproduct and pushout readbacks are delivered at `≅` only — and there the derived `HasCoequalizers FinSet` is the first such inhabitant the tree has ever had |
| `lambek`, `lambek_final` | an initial `F`-algebra / final `F`-coalgebra | `list A` (`Instance/Coq/Lists.v`), `nat` (`Instance/Coq/Nat.v`), streams (`Instance/Sets/Streams.v`) |
| `monadic_creates` | a `Monad` with its Eilenberg–Moore adjunction | `Id_Monad` (`Monad/Strong.v`) |
| `mate_iso` | a `Bicategory` | `Cat` as a bicategory (`Instance/Cat/Bicategory.v`) |
| `markov_all_deterministic_iff_cartesian` | a `Markov` category | `Markov_of_Cartesian` on any cartesian category (`Structure/Monoidal/Markov.v`) |
| `GAFT_from_initials` | a family of comma-initial objects | `InternalProductFunctor` (`Adjunction/GAFT/Examples.v`) |
| `tensor_via_AFT`, `tensor_AFT_iso`, `tensor_AFT_ue`, `module_tensor_universal`, `tensor_via_AFT_maclane` and the four `bal_*` twins (`Instance/Mod/TensorAFT.v`, #449 — Mac Lane §V.7 p. 128, the tensor product of modules from the adjoint functor theorem, and Exercise 3's balanced tensor over `Ab`) | an `ElementSolutionSet (Bilin V V')` (resp. `ElementSolutionSet (BalBiadd N M)`) at the index universe `representability_theorem` demands; the other two premises, `RMod_Complete` (`Instance/Mod/Limit.v`) and `Bilin_PreservesImageLimit` (resp. `Ab_Complete`, `BalBiadd_PreservesImageLimit`), were already unconditional | **WITNESSED AND UNCONDITIONAL since 2026-09-17**, and this row sat in the CONDITIONAL table below until then; it is moved rather than rewritten, and what it used to say is recorded here. It said: "conditional-only in the honest sense: Mac Lane's own solution set IS in tree and unconditional (`tensor_esols_direct`, `bal_esols_direct` — the spanning bilinear maps, covering by corestriction to the generated submodule) but is REFUSED as the premise for a universe reason (its index is a sigma over the objects of `RMod R`, one level above the carrier universe at which `RMod_Complete` offers its shape objects)", and that "the only in-tree inhabitants of the premise are the CIRCULAR `tensor_esols_from_tensor`/`bal_esols_from_tensor`, read off `TensorMod`/`BalTensor`". Both sentences were true when written and both are now superseded by the PR "algebraic carriers are sets". The premise is discharged by `tensor_esols_prop` and `bal_esols_prop`, indexed by the `Prop`-VALUED congruences on the term module (`CongIdx`, `BalCongIdx`): a `Prop`-valued relation on a carrier is carrier-sized, so the only bound relating the two is `carrier ≤ index` and the index can be taken AT the carrier universe, exactly where `RMod_Complete` puts its shape objects — against `tensor_esols_direct`'s `carrier < index`, which is the one inequality that separates them. Every bilinear map into a module `W` factors through the term module modulo the kernel `pequiv (ev β x) (ev β y)`, a congruence precisely because `W` carries `cmon_prop`, and the factorisation is on the nose (`ker_factors … = rbl_map beta v w` at `eq_refl`). Instantiated at THREE rings and one Ab-pair, compiled by the verifier rather than copied from the probe: `Int_RMod` (with `tensor_AFT_iso : repr_obj … ≅ TensorMod Int_RMod Int_RMod`), `Ring_RMod Q_Ring`, `Ring_RMod (Ring_op Int_Ring)`, and the balanced twin at ℤ over ℤ^op with `bal_AFT_iso … ≅ BalTensor`. Mac Lane's OWN family is not discarded: `tensor_spanning_SmallUpToIso` proves it small up to isomorphism through the same congruence index — his §V.7 Lemma 2 — and `tensor_via_AFT_maclane` feeds it to the theorem through `resize_up_to_iso` (`Adjunction/GAFT/Resize.v`). Read the residue precisely: `tensor_esols_direct` is STILL refused as a direct premise, the nine conditional `*_of_esols` forms survive byte-identical so nothing was lost, the object comparison is `≅` and not `eq_refl`, and ONE side condition is new — `Set < carrier`, the sort of `Prop` reaching the index universe — which costs nothing in practice (every application above infers its carrier strictly above `Set`) and costs nothing at all at `Ab`, where `bal_tensor_via_AFT` compiles over a ring whose carrier slot is declared `= Set` |
| `free_group_via_GAFT` (`Instance/Grp/FreeAFT.v`), `free_rng_ab_via_GAFT` and `free_ring_via_GAFT` (`Instance/Rng/AFT.v`) | a `SolutionSet` for `Grp_Forget`, `Rng_Forget_Ab` and `Rng_Forget` at an index universe `GAFT` accepts; the other premises (`Grp_Complete`/`Rng_Complete` and the continuity results) were already unconditional and non-circular | WITNESSED AND NON-CIRCULAR since 2026-09-17, where all three had been read off the very adjunctions Freyd's theorem is meant to produce — see the `GAFT` row below, which carried that disclosure from #442 until this PR. Each now consumes a `*_solution_set_prop`: the `Prop`-valued congruences on the free object's term model, covering every hom through the kernel of the evaluation into the target, which is a congruence exactly because the target carries `grp_prop` / `rig_prop`. The statements are byte-identical to the circular ones they replace, so nothing downstream moved; measured, `Grp_Forget_solution_set_prop` carries SIX universes against the circular solution set's nine and `free_group_via_GAFT` FIVE against the circular application's ten, and on `FGCongIdx` the bound relating carrier to index is `carrier ≤ index`, not `<`. The circular solution sets are KEPT and re-headed as removal candidates, each with its own application preserved beside it under a `*_via_GAFT_from_adjunction` name. Instantiated at `SetOne` and `TwoLetters` for the group, `bool_setoid_object` and `unit_setoid_object` for the free ring, and `ring_ab Int_Ring` for the free ring over `Ab`. The new cost is `Set < carrier`, and it bites at a NAMED object rather than in general: `` `1 free_group_via_GAFT TwoLetters `` is refused while `SetOne` is not, and while `Grp_Forget_solution_set_prop TwoLetters` and the circular application both elaborate. NOT delivered: Mac Lane's own `Subgroup G`-indexed family, which the congruence index replaces rather than resizes, so #1309 stays open on that count; and the five `_mon` clauses of `Instance/Rng/AFT.v` are now stated about `free_ring_via_GAFT_from_adjunction`, `free_ring_via_mon` being `Set`-pinned in a way the non-circular application refuses |
| `Grp_Cocomplete_via_GAFT`, `Rng_Cocomplete_via_GAFT`, `SVariety_Cocomplete_via_GAFT` and the finite colimits read off them (`Instance/Grp/Colimit.v`, `Instance/Rng/Colimit.v`, `Instance/Variety/Colimit.v`, #450 — Mac Lane §V.7 Exercises 1, 2 and 4, §IX.1 Exercises 1 and 2), with `Free_Variety_via_GAFT` | a `SolutionSet` for the diagonal `Δ[J]` at every diagram, at an index universe `GAFT` accepts (for `Free_Variety_via_GAFT`, one for `SVariety_Forget`); the other premises are the unconditional `Grp_Complete`, `Rng_Complete` and `SVariety_Complete` and, as the preservation input, `Adjunction/Diagonal/Limit.v`'s `Diagonal_continuous` at the diagonal and `Instance/Variety/Limit.v`'s `SVariety_Forget_continuous` for `Free_Variety_via_GAFT` | WITNESSED and non-circular, with no hypothesis beyond the variety's signature and equations: `Diagonal_Grp_solution_set`, `Diagonal_Rng_solution_set` and `Diagonal_SVariety_solution_set` index the `Prop`-valued cocone congruences on a term model, a cocone being covered by the kernel of its extension because the target carries `grp_prop`, `rig_prop` or `soa_prop`, and `SVariety_Forget_solution_set` does the same at the forgetful functor. At concrete objects: the AFT coproduct of `Z2` with itself is not a subsingleton, `¬ ∀ a b, a ≈ b` (`Grp_free_product_via_GAFT_nontrivial`), and at #440's `CommEq`, an `EqSignature` built without extensionality, the variety's coproducts and coequalizers are closed constants (`CommMagma_Cocartesian`, `CommMagma_coequalizers`). Downstream, `Instance/Grp/Colimit/Presentation.v`'s `Grp_Cocartesian_of_coequalizers`, whose premise is `HasCoequalizers Grp`, is instantiated at that premise's one in-tree inhabitant, `Grp_HasCoequalizers_via_GAFT` (`Grp_Cocartesian_via_riehl`). The side condition is `Set < carrier`, and it is `GAFT`'s: the diagonal solution sets elaborate at `Set` carriers, and so does `GAFT` applied to every argument but the solution set, while `Grp_colim_via_GAFT`, `Rng_colim_via_GAFT` and `SVariety_colim_via_GAFT` are refused there (`Test/ProbeAlgColimit450.v`'s N4 and N5, `Test/ProbeVarietyColimit450.v`'s N4). The shape discipline is `Cocomplete@{u u0 u u1}`: shape homs AT the carrier universe, so `Two_Discrete`, whose homs sit at `Set`, is refused as a shape. NOT witnessed: anything at `Set`-level carriers, a nontrivial ring above `Set`, and any colimit that computes, `GAFT` being `Qed`. Since #454 the row's witnessed, non-circular reading extends to `RMod R` for every ring, with the same caveats (nothing at `Set`-level carriers, no colimit that computes): `Instance/Mod/Colimit.v`'s `RMod_Cocomplete_via_GAFT`, with `Diagonal_RMod_solution_set` indexing the `Prop`-valued cocone congruences on `Instance/Mod/Free.v`'s term model (a cocone's kernel is a `Prop` because the target module carries `cmon_prop`), its finite bridges (`RMod_Cocartesian_via_GAFT`, `RMod_HasCoequalizers_via_GAFT`, `RMod_Initial_via_GAFT`), and a nontrivial module colimit above `Set` (`RMod_coproduct_via_GAFT_nontrivial`: the AFT coproduct of ℤ with itself at `Int_Ring@{c c c}` is not a subsingleton); its `Set < c` is again `GAFT`'s (`Test/ProbeWatts454.v`'s N14), and the comparison with `Instance/Mod.v`'s `Set`-pinned zero module is refused (N15). At `(RMod R)^op` it is the completeness both Watt routes consume (the rows below) |
| `coproduct_injections_meet_trivial`, `coproduct_injections_pullback_zero` (`Theory/Subobject/Disjoint.v`, #450 — Mac Lane §V.7 Exercise 1, second half; nLab "disjoint coproduct") | a `ZeroObject`, binary coproducts (`Cocartesian`) and `HasPullbacks` in one category | `Grp`, at the coproduct the adjoint functor theorem produces: `Grp_free_product_meet_trivial` (`Instance/Grp/Colimit.v`) with `Grp_Zero` and `Grp_HasPullbacks`, the bottom subobject's domain being the trivial group at `eq_refl` (`Grp_free_product_meet_bottom_is_trivial`), and the coproduct itself nontrivial at `Z2` (`Grp_free_product_via_GAFT_nontrivial`); statable above `Set` only since #450 wrote out `Grp_Zero`'s universes in `Instance/Grp.v`. Not witnessed in a category without a zero object: in `Sets` the retraction of `inl : x ⟶ x + y` the proof uses does not exist when `x` is empty and `y` is not, and the strict-initial route is not built |
| `Cospan_Hypergraph`, `spider_collapse`, `spider_frobenius` | `HasPushouts` on a base whose objects fit its homs | `FinSet_HasPushouts` (`Instance/FinSet/Pushout.v`) over `FinSet`; see the cospan note below |
| `ZX_Cat` | the three `Phase` parameters | supplied by a user; see `docs/AXIOMS.md` |
| `LawvereTheory`, `CopyDiscard` supplies | — | `FinSet_Lawvere`, the Kleisli comonoid supplies |
| `CreatesLimit`, `StrictlyCreatesLimit`, `creation_preserves_limit` | a functor that creates limits | `EM_Forget` for any monad (`Monad/Eilenberg/Moore/Limit.v`), instantiated with no remaining hypothesis at `Id_Monad` over `Coq` with `Coq_Terminal` downstairs (`Monad/Eilenberg/Moore/Limit/Examples.v`); also `Id[C]` via equivalences (`Theory/Equivalence/Creation.v`), and `comma_proj2` (`Construction/Comma/Creation.v`) under the same `Complete C` premise as the `GAFT` row |
| `connected_deloop_equiv`, `connected_vertex_moniso` | a connected groupoid (`IsGroupoid C` + `Connected C`); `connected_iff_deloop_equiv` needs only `IsGroupoid C`, since it CONCLUDES connectedness in one direction | `Bool_Wide := WideDeloop Bool_Xor_Grp bool` (`Structure/Groupoid/Connected.v`): two objects, eight arrows, vertex group Z/2, with the equivalence instantiated at both objects (`Bool_Wide_structure`, `Bool_Wide_structure_false`) and the vertex-group isomorphism at `Bool_Wide_vertex_moniso` (`Structure/Groupoid/Basepoint.v`). A second witness: `FundamentalGroupoid TwoPoint_Indiscrete` is connected, and `TwoPoint_Indiscrete_inclusion_equivalence` instantiates the structure theorem there. The hypothesis is also shown necessary: `Two_Discrete_no_deloop_equivalence` refutes the conclusion for the disconnected two-object discrete groupoid. For `connected_vertex_moniso` read the fundamental-groupoid note below: the premise is witnessed, the conclusion is not yet exercised |
| `FundamentalGroupoid`, `fundamental_groupoid_is_groupoid`, `fundamental_group_basepoint_independent`, `fundamental_group_inclusion_equivalence` | `PathConnected X` for the last two (the first two need only a `TopSpace`) | `TwoPoint_Indiscrete_pathconnected` (`Instance/Top/FundamentalGroupoid.v`), the indiscrete topology on two points, with the base-point isomorphism instantiated at `TwoPoint_Indiscrete_basepoint_iso`. The premise is shown to be a real restriction rather than a formality: `Bool_Discrete_pi_not_connected` refutes connectedness of π for the DISCRETE topology on the same setoid of points (`bool_carriers_agree` holds by `eq_refl`), so π is reading the topology and not the underlying set. See the fundamental-groupoid note below for what is still missing |
| `conjugation_iso` (conjugation is a group isomorphism of vertex groups) | a groupoid with a NONABELIAN vertex group, without which the automorphism is the identity | `Deloop S3_Grp` (`Structure/Groupoid.v`) — the tree's first nonabelian group, added with this work; `S3_conjugation_not_identity` exhibits conjugation moving an element |
| `Inversion_iso` (a groupoid is isomorphic to its opposite, in `StrictCat`) | `IsGroupoid C` | `core_is_groupoid C` for every `C`; `Deloop Z3_Grp` for a case where the morphism map is not the identity (`Z3_inversion_not_identity`) |
| `Curry_Adjunction`, `Curry_Representable` | a cartesian closed category | `Sets` via `Sets_Closed` (`Instance/Sets/Cartesian/Closed.v`), instantiated with computing counit and a concrete transposed arrow in `Instance/Sets/Cartesian/Closed/Adjunction.v` |
| `pointed_monic_split`, `pointed_epic_split` | decidable image membership / decidable equality + an enumeration | `PointedBool`, `PointedThree` (`Instance/Sets/Pointed/Finite.v`), with the retraction and section computed by `reflexivity` |
| `Grp_injectivity_is_monic`, `Grp_Cartesian`, `Grp_Op` | a nontrivial group | `Z2` on `bool` (`Instance/Grp.v`), with `Monic_in_Grp_is_not_vacuous` and the kernel shown non-degenerate |
| `fixed_product_functor`, `fixed_product_transform` | a `Cartesian` category, and an `f : H ~> K` that is not invertible | `Grp` via `Grp_Cartesian`, instantiated as `Grp_fixed_product`/`Grp_fixed_product_transform` (`Functor/Product/Fixed.v`); the zero endomorphism `Grp_Z2_zero` of `Z/2` witnesses non-degeneracy — `Grp_Z2_zero_not_iso` and `Grp_fixed_product_transform_not_id` |
| `grp_not_epic_of_witness`, `grp_epic_iff_surjective` | a homomorphism together with an element proved outside its image. For the biconditional, `GrpImageStable` on top — but under `Epic f` that premise is EQUIVALENT to its own conclusion (`stability_is_the_conclusion`), so it is not an independent hypothesis | Two witnesses, deliberately not interchangeable. `grp_two_incl`, the inclusion of Z/2 as a factor of Z/2 x Z/2 (`Instance/Grp/Epi.v`), exhibits the missing element, the decidability of image membership, and the non-identity of both the action and the twist — but its image is NORMAL, so the equivariance step degenerates there and the whole image acts as the identity (`grp_two_incl_image_acts_trivially`). `grp_two_sym3`, Z/2 into the symmetric group of a three-letter setoid, supplies the NON-NORMAL image at which that step has content (`grp_two_sym3_image_acts_nontrivially`) |
| `Hausdorff_Subcategory`, `CompactHausdorff_Subcategory` | a Hausdorff / compact space | `Bool_Discrete_Hausdorff` (two points, with `TwoPoint_Indiscrete_not_Hausdorff` refuting the indiscrete twin) and `Point_Compact` (`Instance/Top.v`) |
| `skeleton_inclusion_is_equivalence`, `skeletons_are_isomorphic`, `skeletons_isomorphic_iff_equivalent` | a `Skeleton C` — a full subcategory with one chosen representative per isomorphism class, and its uniqueness | `Indiscrete_bool_Skeleton` (`Theory/Skeleton/Separation.v`), a category that is NOT skeletal whose skeleton is exhibited on the nose as `1` (`Indiscrete_bool_skeleton_is_One`); plus `Skeleton_of_Skeletal`, applied in tree at `FinSet` (`FinSet_Skeletal`, `FinSet_Skeleton`) and at any `Poset` (`Proset_Skeleton`), and available at `1`, `2` and `DiscreteCat A` from `One_Skeletal`, `Two_Skeletal` and `DiscreteCat_Skeletal` |
| `kernel_aue`/`aue_kernel`, `kernel_representation`, `kernel_iff_representable` (`Structure/Kernel/Universal.v`) | a kernel, i.e. an `IsKernelOf` inhabitant, in a category with zero morphisms | `ab_kernel_IsKernel` (`Structure/Kernel/Universal/Examples.v`) — **the tree's first concrete `IsKernel` inhabitant**. `Instance/Ab.v` built `AbKernel` with its inclusion but proved no universal property of it, and the only other `IsKernel` occurrences are fields and hypothesis binders of `Structure/Abelian.v`, whose class has no instance (see the `image_mediator_epic` row below). Non-vacuity is proved, not assumed: the witness is `ab_parity : ℤ ⟶ Z/2`, whose kernel is the even integers, PROPER and nontrivial (`ab_parity_kernel_has_two`, `ab_parity_kernel_proper`), with `ab_parity_med_computes` evaluating the unique factorization of doubling at 3 to 6 by `eq_refl`. NOTE the scope: the bundled `kernel_universal_element_iso` does NOT reach `Ab` — it requires `u <= u0` while `Ab@{u u0}` is declared `u0 < u` — so the witnessed statements are the universe-free passages and the representability form, not the bundled setoid isomorphism |
| `comma_StrictlyCreatesLimit`, `comma_CreatesLimit_at`, `comma_CreatesProducts`, `comma_IsIndexedProduct`, `comma_equalizer_at` (`Construction/Comma/Creation.v`, #438) | `PreservesLimitCone (Gdiag K) U` at the ONE base diagram of the diagram being created — the per-diagram weakening of `PreservesImageLimit` — and for `comma_CreatesProducts` the all-shapes form | discharged with NO hypothesis at all when `U = Id`, for every category: `Id_PreservesImageLimit` (`Construction/Slice/Creation.v`) is `right_adjoint_PreservesImageLimit` at `adj_id`, so `coslice_comma_StrictlyCreatesLimit`, `coslice_comma_CreatesAllLimits` and `coslice_comma_proj_CreatesAllLimits` are unconditional theorems about an arbitrary `C`; more generally every right adjoint supplies the premise. `Coslice_Complete` and `coslice_comma_Complete` additionally consume `Complete C`, witnessed at `Sets` by `Sets_Complete` (`Instance/Sets/Complete.v`), and `Test/ProbeCommaCreation438.v` instantiates `Coslice_Complete` there. The products clause takes the indexed product DOWNSTAIRS in `C` and its `U`-image as two hypotheses, the product upstairs in the comma being the conclusion; at `U = Id` the two hypotheses coincide, so it too is discharged. The shape-indexed classes `comma_CreatesAllLimits` and its instance `comma_CreatesProducts` are provable, axiom-free and NOT vacuous: a CONSTANT discrete diagram is formable over a generic `C` and the class applies to it (`Test/ProbeCommaCreation438.v`'s `p438_const_creates`). What has no inhabitant over a generic `C` is a discrete diagram at an arbitrary FAMILY: any functor out of `DiscreteCat A` that eliminates the shape's `x = y` into a hom pins both categories to `Set`, `DiscreteCat_Functor` included, and the refusal persists even at `C = D = Sets` with `U = Id` (probe n2) |
| `Coslice_Proj_StrictlyCreatesLimit`, `Coslice_Proj_CreatesAllLimits`, `Coslice_Complete_direct` (`Construction/Slice/Creation.v`, #438) | none — Riehl §4.7's exercise done directly for the projection `Coslice_Proj : c ̸co C ⟶ C` | UNCONDITIONAL: the step that consumes preservation in the comma case is the passage to the `U`-image of a limit, and with `U` the identity that passage is the given limiting cone itself, so no hypothesis is taken and the theorem holds for every category and every object. `Coslice_Complete_direct` consumes only the `Complete C` that is its conclusion's subject. Strictness holds for the PLAIN projection and NOT for the transported `comma_proj2 ◯ Coslice_to_Comma` (probe n3, n4); n6 records that the two are different functors |
| `GAFT_iff`, `solution_set_of_adjunction` (`Adjunction/GAFT.v`, #436) | for the biconditional, `Complete C`; for the necessity direction, an adjunction `F ⊣ U` and nothing else | witnessed at `Sets` — `GAFT_iff_at_Sets_Id` reads the biconditional at `Id : Sets ⟶ Sets` on `Sets_Complete`, and `Sets_Id_has_left_is_GAFT_at_Sets_Id` shows its reverse half IS the application `Adjunction/GAFT/Sets.v` already had, at `eq_refl`. The necessity direction needs no witness of its own: `solution_set_of_adjunction` builds a solution set for EVERY right adjoint, so the third premise of `GAFT` now has the same general in-tree supply the second has had through `right_adjoint_PreservesImageLimit`, and at `adj_id` the two agree with the hand-built `Sets_Id_SolutionSet` on index, member and arrow. What is NOT witnessed is any category but `Sets`, and the biconditional inherits `GAFT`'s pin of both hom universes to `Set` (probe n6) where the necessity direction does not |
| `fp_snd_StrictlyCreatesLimit`, `comma_proj2_creates_second` (`Instance/Cat/Creation.v`, #439) | for Exercise 3, that the left functor STRICTLY creates the limits of the projected diagram and the bottom one preserves them; for Exercise 4, `ContinuousFunctor U` | both discharged in tree with nothing assumed at the coslice cospan: #438's `Coslice_Proj_StrictlyCreatesLimit` is unconditional and supplies the first, and `Id_ContinuousFunctor` — `right_adjoint_Continuous` at `adj_id` — supplies the second for EVERY category, so `coslice_comma_proj2_creates_via_pullback` and `coslice_comma_Complete_via_pullback` are unconditional theorems. This route is a second proof, not a strengthening — but measured against the right constant: it is of EQUAL strength to #438's `comma_CreatesAllLimits` (whose `PreservesImageLimit` premise is interderivable with `ContinuousFunctor U`, in both directions, in `Construction/Comma/Creation.v`, and whose conclusion is likewise `CreatesLimit`), and weaker in both directions only than #438's `comma_StrictlyCreatesLimit`. An earlier revision of this row made the comparison against `comma_CreatesAllLimits`, which has neither the per-diagram premise nor the strict conclusion it was credited with. Not witnessed: Exercise 3 at any cospan but the coslice one, and no pullback square in `Cat` — `HasPullbacks Cat` is still open, and `Instance/Cat/Pullback.v`'s refutation is at the `Indiscrete bool` cospan, not at this apex (an earlier revision of this row said it "refutes this apex there") |
| `sub_meet_is_glb`, `sub_meet_IsMeet`, `sub_join_is_lub`, `sub_join_IsJoin`, `sub_meet_absorb`/`sub_join_absorb`, `sub_join_bot` (`Theory/Subobject/Lattice.v`, #445) | `HasPullbacks C` for the meet; `Cocartesian C` + `HasImages C` for the join; `Initial C` with `Monic zero` for the bottom | `FinSet` carries all of them and COMPUTES — `FinSet_Pullbacks` (`Instance/FinSet/Classifier.v`), `FinSet_Cocartesian` (`Instance/FinSet.v`), `FinSet_HasImages` and `FinSet_zero_monic` (`Instance/FinSet/Subobject.v`), with `sub_dom (sub_join u v) = 4` against `Coprod 2 3 = 5` and `sub_dom (sub_meet u v) = 1` at `eq_refl`; `Sets` carries them too — `Sets_HasPullbacks` (`Instance/Sets/Pullback.v`), `Sets_Cocartesian` (`Instance/Sets/Cocartesian.v`), `Sets_HasImages` and `Sets_zero_monic` (`Instance/Sets/SubobjectLattice.v`), where the unit law is read at `fs_seg 3` as `fs_join_bot_left_unit` |
| `sub_wide_intersection_IsIntersection`, `sub_intersection` (`Theory/Subobject/Lattice.v`, #445) | a `WidePullback` of the family's monos / `HasWidePullbacks C` — a class `Structure/Pullback/Wide.v` declared and left uninhabited until this issue | `Sets_HasWidePullbacks` (`Instance/Sets/SubobjectLattice.v`), the class's first inhabitant, at index universe `o` (the carrier level); exercised on Fong–Spivak's increasing family, whose intersection is proved ≈ the empty subobject (`fs_inter_is_empty`) and whose union, through `sub_wide_join_via` at `Sets_IsIndexedCoproduct` and `Sets_HasImages`, is proved ≈ the positive naturals (`fs_union_is_positives`) — both at the `SubObj` setoid, not at membership-equivalence. `FinSet` has NO `HasWidePullbacks` instance (the count codec does not obviously extend to an arbitrary index type) |
| `quot_meet_is_glb`, `quot_meet_IsMeet`, `quot_join_is_lub`, `quot_join_IsJoin`, `quot_meet_absorb`/`quot_join_absorb` (`Theory/Subobject/Quotient.v`, #446 — the #445 lattice transported to quotient objects at `C^op`) | `HasPushouts C` for the meet; `Cartesian C` + `HasCoimages C` (notation for `@HasImages (C^op)`) for the join | `FinSet` — `FinSet_HasPushouts` (`Instance/FinSet/Pushout.v`), `FinSet_Cartesian`, `FinSet_HasCoimages` (`Instance/FinSet/QuotObj.v`) — where the meet COMPUTES: `quot_cod (quot_meet q r) = 2` at `eq_refl` for two surjections 4 ↠ 3; `Sets` — `Sets_HasPushouts` (`Instance/Sets/Pushout.v`), `Sets_Cartesian`, `Sets_HasCoimages` (`Instance/Sets/QuotObj.v`) — with the parity/mod-3 meet proved ≈ the total quotient and `Sets_quot_join` the one-line join |
| `grp_quot_is_quot_by_kernel` (Mac Lane's G/N illustration, `Instance/Grp/QuotObj.v`, #446) | a quotient object of a group whose epi is SURJECTIVE (`GrpSurjective`) | every `grp_quot_of_normal N`, whose epi `quot_proj N` is surjective by construction; exercised at A3 ⊴ S3 (`grp_quot_S3_A3`), nonabelian with a proper nontrivial normal subgroup. The `Epic`-only form is the CONDITIONAL row below |
| `quot_bot`, `quot_bot_least` (`Theory/Subobject/Quotient.v`, #446) | `Epic (one : x ~> 1)` — the arrow to the terminal object is epic | `Sets_one_epic_of_inhabited` (`Instance/Sets/QuotObj.v`): at every INHABITED setoid, in three lines with nothing assumed, giving the bottom quotient object `Sets_quot_bot`; the hypothesis is exactly inhabitedness there — `Sets_one_not_epic_empty` refutes it at the empty setoid — so it is OBJECT-DEPENDENT rather than rare, and in every thin category it is automatic. An earlier revision of this table listed the pair as conditional, "not discharged in tree", and the theory file's header claimed a discharge that had not been landed; both were caught by an audit and the discharge landed at integration. `one_epic_of_costrict` is the formal dual of `zero_monic_of_strict` and its hypothesis is near-vacuous |
| `generator_jointly_faithful`, `jointly_faithful_generator`, `separator_faithful`, `faithful_separator`, `gen_of_cog`/`cog_of_gen` (`Structure/Generator.v`, `Structure/Generator/Dual.v`, #447) | a generating (separating) family `Generator C` / a separator `IsSeparator c` | `Sets_Generator` and `Sets_terminal_Generator` (`Instance/Sets/Generator.v`: the singleton and the terminal object, two non-convertible objects, the second also read off `Theory/Concrete.v`'s `Sets_Separator` through the bridge), `Grp_Generator` (`Instance/Grp/Generator.v`: the free group on one generator, via `free_grp_extend`), `Ab_Generator` (`Instance/Ab/Generator.v`: the free abelian group on one generator — Mac Lane's ℤ up to an isomorphism not in tree); the non-example `Sets_empty_not_separates` shows the condition is not vacuous. No `Cogenerator` of a concrete category is exhibited, so the dual reading is witnessed only through the bridge. **CORRECTION (#452)**: that sentence no longer holds. The bridge is applied, `Instance/Sets/SpecialInitial.v`'s `setsop_cog` being `cog_of_gen Sets_Generator`, a cogenerator of `Sets^op` (a grep for `cog_of_gen` over the comment-stripped `.v` files outside `Test/` finds it applied to a concrete generator there and nowhere else; CORRECTION (#454): no longer "nowhere else", the same grep over the comment-stripped `.v` files outside `Test/` now also finding `Instance/Mod/WellPowered.v`, whose `RModop_Cogenerator` is `cog_of_gen (RMod_Generator R)`, a cogenerator of `(RMod R)^op` from `RMod_Generator`, R as a module over itself, both unconditional and at `RingObject@{c c c}` only), and cogenerators of a concrete category are exhibited directly: `Instance/Sets/Cogenerator.v`'s `Sets_Cogenerator_untruncate` and `Sets_Cogenerator_IEM`, small and conditional on `Untruncate` and `IEM` (the conditional row below), and its unconditional but large `Sets_Cogenerator_large`, besides `Adjunction/SAFT/InitialObject/Examples.v`'s cogenerators of the thin `Subsets X` and `Indiscrete`. Since #454 also `Instance/Mod/Cogenerator.v`'s unconditional but large `RMod_Cogenerator_large`, refused by `cogen_prod` as `Sets_Cogenerator_large` is (`Test/ProbeWatts454.v`'s N4), and its conditional `QZ_Cogenerator` at `Ab` and `QZ_injective_cogenerator` at `RMod R` (the conditional row below) |
| `WellPowered`, `CoWellPowered`, `wellpowered_complete_has_intersections` (with `wellpowered_complete_has_intersections_prop`, `wellpowered_complete_intersection_all`, `wellpowered_complete_least_subobject`) and `cowellpowered_cocomplete_has_cointersections` (`Structure/WellPowered.v`, #451 — Mac Lane §V.8) | a `WellPowered C` witness AT THE PIN: the subobjects of every object indexed at or below the hom universe, with the exhaustiveness clause `wp_to_from`; for the consequence, `Complete C` besides (for the dual, `CoWellPowered C` and `Cocomplete C`) | well-poweredness at the pin: `trivial_small` (every category whose objects sit at or below its homs), `Groupoid_WellPowered` and its instances `DiscreteCat_WellPowered`, `Indiscrete_WellPowered` and `DiscreteCat_types_WellPowered` (objects strictly above homs), their co-well-powered twins, `Classifier_WellPowered` (any category with a subobject classifier over pullbacks), and at a concrete category, unconditionally, `FinSet_WellPowered` (`Instance/FinSet/WellPowered.v`, the index read back at `eq_refl` as `Fin.t n → Fin.t 2`). The consequence with BOTH premises discharged: `Indiscrete_types_has_intersections` (`Structure/WellPowered.v`, at `Indiscrete@{o j j} Type@{j}`, objects above homs but degenerate, one subobject per object) and `Subsets_has_intersections`/`Subsets_intersection_all` (`Instance/Powerset/WellPowered.v`, at the powerset lattice of any setoid, over `Subsets_Complete_free`), non-degenerate (`Subsets_intersection_all_not_top`) but with the trivial well-poweredness of objects at or below homs. No unconditional instance has both objects above homs and a non-degenerate subobject lattice; at `Sets` the consequence is CONDITIONAL (the `Sets_WellPowered_untruncate` row below), and `FinSet`, well-powered unconditionally, carries no `Complete` instance (it is finitely complete and lacks infinite products, as `Instance/FinSet/WellPowered.v` records), so the theorem does not apply there. The dual `cowellpowered_cocomplete_has_cointersections` is applied nowhere in tree (a grep for its name over the `.v` files finds `Structure/WellPowered.v` and `Test/ProbeWellPowered451.v`, which pins it). That the pin excludes something is itself witnessed: `Structure/WellPowered/Counterexample.v`'s `AntichainTop_not_WellPowered` refutes it at a category built with no axiom, closed under the global context (see docs/AXIOMS.md on its use of the stdlib's Hurkens theorem) |
| `complete_wide_pullback`, `Complete_HasWidePullbacks` (`Structure/Pullback/Wide/Complete.v`, #451 — wide pullbacks from completeness, by §V.2's products and equalizers) | `Complete C` | witnessed as the `GAFT` row below records (`Sets_Complete` and nine more unconditional `Complete` instances, besides #451's own `Indiscrete_Complete` and `Subsets_Complete_free`, which that row notes); the construction is EXERCISED in tree through `Structure/WellPowered.v`'s `complete_intersection` at `Indiscrete_Complete` and at `Subsets_Complete_free` (the row above), and since #452 by a library constant at `Sets_Complete` too (`Test/ProbeWellPowered451.v`'s controls having already applied it there), conditionally on `Untruncate`, where `Instance/Sets/SpecialInitial.v`'s `sets_special_initial_wellpowered` reads its initial object back at `eq_refl` as that `complete_intersection`. `Complete_HasWidePullbacks` itself is a `Definition`, not an `Instance`, and no library constant applies it to a concrete category; at `Sets_Complete` and at `SVariety_Complete E` it is accepted in scratch files (the latter reported closed under the global context), as the satellite's header and `Adjunction/SpanningArrow.v`'s corrections record, and in tree only by `Test/ProbeWellPowered451.v`'s controls `p451_sets_hwp_from_complete` and `p451_svariety_hwp`, both reported closed under the global context. Since #453 `complete_wide_pullback` is also applied by `Adjunction/SAFT/Characterization.v`'s `Complete_HasSubobjectWidePullbacks`, and through it at `Subsets_Complete_free Y` by `Adjunction/SAFT/Characterization/Examples.v`'s `subsets_inverse_image_thm2`; `Complete_HasWidePullbacks` itself is still applied by no library constant (a grep of its name over the comment-stripped `.v` files outside `Test/` and `doc/` finds its defining file only) |
| `special_initial_object` (with `special_initial_object_least`, `special_initial_object_wellpowered`, `special_initial_IsInitialObj` and the uniqueness lemma `least_sub_arrows_agree`; `Adjunction/SAFT/InitialObject.v`, #452 — Mac Lane §V.8 Theorem 1, Riehl Lemma 4.7.11) | `Complete C` and a cogenerating family `Cogenerator C` (`Adjunction/SAFT.v`), with the intersection of ALL subobjects of `cogen_prod comp G`, or equivalently a least subobject of it; the other forms take instead `HasClassIntersections C` (every ≈-closed class of subobjects has an intersection, the book's hypothesis read over classes) or `WellPowered C` | witnessed UNCONDITIONALLY at three categories, every application closed under the global context: the powerset lattice `Subsets X` of any setoid and `Indiscrete@{o j j} Type@{j}` (`Adjunction/SAFT/InitialObject/Examples.v`'s `Subsets_special_initial_empty`, `Subsets_special_initial_bot` and `Indiscrete_types_special_initial`, `Subsets_special_initial` itself taking the cogenerator as an argument; through the well-powered corollary with every premise supplied in tree and the least subobject COMPUTED as `Structure/WellPowered.v`'s wide-pullback `complete_intersection`; `Subsets X`'s object is provably empty, and at a pointed `X` the top subset has no arrow into it, while `Indiscrete`'s is degenerate, every object being initial; both categories are THIN, so every family cogenerates them, the empty one included, and `cog_separates` does no work), and `Sets^op` (`Instance/Sets/SpecialInitial.v`'s `setsop_special_initial`, over `Complete_op_of_Cocomplete Sets_Cocomplete` and the small, genuine cogenerator `cog_of_gen Sets_Generator` of a category that is not thin; measured against Examples.v's two, it is the only unconditional witness that is not thin). THE CIRCULARITY CAVEAT, as that file's header states it: at `Sets^op` the least subobject handed to the theorem, `setsop_L`, is BUILT FROM the known initial object (`Sets_Terminal`'s singleton), and the object reads back at `eq_refl` as that singleton, so this witness exercises completeness, the cogenerator product, `cogenerator_canonical_monic` with `cog_separates`, the pullbacks of the existence half and the equalizer argument of the uniqueness half, and not the intersection; at `Subsets X` the intersection is computed but separation does no work, and no unconditional witness does both. At `Sets` the theorem is CONDITIONAL on `Untruncate` or `IEM` (the conditional row for `Instance/Sets/Cogenerator.v`). Applied at NO concrete category in tree: `special_initial_object_small` (at `Subsets X` it is accepted in a scratch file only, as the theorem's header records), `special_initial_object_wellpowered_at`, and the Freyd comparisons `special_vs_freyd`, `freyd_of_cogenerator` and `special_vs_freyd_of_cogenerator` (none of the five names occurs in the comment-stripped code of Examples.v or SpecialInitial.v, the only library files that require the theorem's module; `Test/ProbeSpecialInitial452.v` uses them in controls and refutations; `special_vs_freyd` and `freyd_of_cogenerator` are accepted at `Sets` under `Untruncate` in a scratch file, closed under the global context) (CORRECTION, #453: two claims of this sentence no longer hold. Examples.v and SpecialInitial.v are not the only library files that require the theorem's module: `Adjunction/SAFT/Characterization.v` and `Adjunction/SAFT/Characterization/Cover.v` require it too, four files outside `Test/` and `doc/` by a grep for the `Require` line against two at master 9613c9a5. And `special_initial_object_wellpowered_at` is applied: `Characterization.v`'s `SAFT_comma_initial` applies it in every comma `=(d) ↓ U`, and through `SAFT_wellpowered` it reaches the comma categories of concrete functors in `Adjunction/SAFT/Characterization/Examples.v` — of `InverseImage f` on `Subsets Y` and of `Id` on `Indiscrete` unconditionally, of `Id` and `HomFrom A` on `Sets` under `Untruncate` (the `SAFT_wellpowered` row below); `special_initial_object_least` is applied the same way through `SAFT_thm2_initial`, at the commas of `InverseImage f` by `subsets_inverse_image_thm2`. The other four names still occur in the comment-stripped code of no library file but `Adjunction/SAFT/InitialObject.v`, by a grep over the `.v` files outside `Test/` and `doc/`); `special_initial_object_book` is applied at `Sets` only, under `Untruncate`. At `Sets^op` both well-powered corollaries are refused with the only co-well-poweredness of `Sets` in tree, `Sets_CoWellPoweredAt_up`, one universe up (the probe's N15 and N16) |
| `SAFT_wellpowered`, `SAFT_wellpowered_iff` (`Adjunction/SAFT/Characterization.v`, #453 — Mac Lane §V.8 Corollary, Awodey Remark 9.35), with the `GAFT` route to the same conclusion, `SAFT_cover_wp` (`Adjunction/SAFT/Characterization/Cover.v`) | `Complete C`, a cogenerating family `Cogenerator C` and #451's `WellPowered C`, with `PreservesImageLimit U` for the functor; no covering datum | witnessed UNCONDITIONALLY at two categories, every application closed under the global context (`Adjunction/SAFT/Characterization/Examples.v`): the powerset lattice `Subsets Y` at `InverseImage f` (`subsets_inverse_image_saft`, over `Subsets_Complete_free Y`, `Subsets_Cogenerator_empty Y` and `Subsets_WellPowered Y`) and `Indiscrete@{o j j} Type@{j}` at `Id` (`indiscrete_saft`, the well-powering's index at `Set`). Both are THIN (`Indiscrete` degenerate), so `cog_separates` does no work, and at both the continuity premise is read off a known adjunction (`image_preimage_adjunction f`, `adj_id`), so the conclusion was known — `subsets_inverse_image_saft_iso` identifies the constructed left adjoint with `DirectImage f`. Where separation does work the witnesses are CONDITIONAL on `Untruncate`: at `Sets@{o so}`, with `Sets_Complete`, `Sets_Cogenerator_untruncate Un` and `Sets_WellPowered_untruncate Un`, `sets_id_saft` (and `sets_id_saft_cover` through `GAFT`) at `Id`, circular likewise (`Sets_Id_PreservesImageLimit` is read off `adj_id`; `sets_id_saft_iso` identifies the adjoint with `Id`), and `sets_hom_saft` at `HomFrom A`, whose continuity `hom_ContinuousFunctor` is proved directly — the one witness whose premises are not read off a known adjunction, its left adjoint not identified. No unconditional witness at a category that is not thin among the candidates `Examples.v`'s header measures: without `Untruncate`, `Sets_WellPoweredAt_up` sits one universe up and is refused at the pin (`Test/ProbeSAFT453.v`'s N11–N13), `Sets^op` is co-well-powered only one universe up (N25, N29), and the unconditional large `Sets_Cogenerator_large` is refused by both routes (N15, N16). `SAFT_wellpowered_iff` itself is applied at no concrete category (a grep of its name over the comment-stripped `.v` files outside `Test/` and `doc/` finds its defining file only); its necessity half is `right_adjoint_PreservesImageLimit`, general. `SAFT_cover_wp` is applied at `Id[Sets]` under `Untruncate` only (`sets_id_saft_cover`). **CORRECTION (#454)**: "where separation does work the witnesses are CONDITIONAL on `Untruncate`: at `Sets@{o so}`" is no longer the whole list. `Instance/Mod/Watts.v` applies `SAFT_wellpowered` at the NON-thin `(RMod R)^op` under one `Untruncate@{c}`, at `RingObject@{c c c}`, over `Complete_op_of_Cocomplete (RMod_Cocomplete_via_GAFT R)`, `RModop_Cogenerator R` — whose `cog_separates` is `RMod_ring_separates`, evaluation at the ring's generator, which does work — and `RMod_CoWellPowered_untruncate U`: generically in T as `watts_theorem_adjoint`, and at a concrete functor through `continuous_Set_functor_representable` at `watts_theorem_coext` (the `continuous_Set_functor_representable` row below). Still conditional: without `Untruncate`, `RMod R`'s co-well-powering sits one universe up and is refused at the pin (`Test/ProbeWatts454.v`'s N8), as `Sets^op`'s is, so "no unconditional witness at a category that is not thin" still holds of this theorem. The corollary it serves there, representability of a continuous `K : (RMod R)^op ⟶ Sets`, holds UNCONDITIONALLY by another route, `Instance/Mod/Watts/Unconditional.v`'s `RModop_continuous_representable`, through `representability_theorem` (the general adjoint functor theorem's comma-initial step) with an explicit element-wise solution set, and not through this theorem (the `watts_theorem_unconditional` row below) |
| `saft_colim`, `saft_cocomplete` (`Adjunction/SAFT/Characterization/Corollaries.v`, #453 — Riehl Corollary 4.7.13: a category satisfying the special adjoint functor theorem's hypotheses is cocomplete) | `Complete C`, `Cogenerator C` and `WellPowered C`; cocompleteness is delivered at shapes `Category@{jo h h}` with `jo <= h` | witnessed UNCONDITIONALLY at `Subsets Y` (`subsets_saft_cocomplete`) and at `Indiscrete@{o j j} Type@{j}` (`indiscrete_saft_cocomplete`), both thin (`Indiscrete` degenerate), as in the row above, and at `Sets@{o so}` under `Untruncate` (`sets_saft_cocomplete`), all in `Adjunction/SAFT/Characterization/Examples.v` and closed under the global context. `sets_saft_cocomplete` adds no new content, `Instance/Sets/Cocomplete.v`'s `Sets_Cocomplete` needing no `Untruncate`; `subsets_saft_cocomplete` is stated at `Subsets@{o u} X`, where `Instance/Powerset.v`'s `Subsets_Cocomplete` is stated at `Subsets@{o Set} X` and the record is not cumulative (`Examples.v`'s header). The corollary is refused at `h < jo` (`Test/ProbeSAFT453.v`'s N19), the theorem sharing `C`'s hom universe with `[J, C]`'s, and its colimit-datum universe is the homs, inherited from #353's `Diagonal_left_adjoint_HasColimits` (N20) |
| `watts_theorem_unconditional`, `RModop_continuous_representable`, `RModop_esols` (`Instance/Mod/Watts/Unconditional.v`, #454 — Mac Lane §V.8, Watt's theorem in the book's contravariant form, through `representability_theorem` rather than the special adjoint functor theorem) | an additive `T : (RMod R)^op ⟶ Ab` continuous in the cone sense (for `RModop_continuous_representable`, any continuous `K : (RMod R)^op ⟶ Sets`), at a ring with `Set < c`; no `Untruncate`, no cogenerator, no co-well-poweredness | WITNESSED UNCONDITIONALLY, every witness closed under the global context: T := `CoHomAb (RMod_AbEnriched R) A` for every ring and every module A, continuous by `Functor/Representable/Additive.v`'s `CoHomAb_continuous`, the module returned ≅ A (`watts_unconditional_CoHomAb_obj`); the `Sets`-valued hom functor (`RModop_hom_representable_obj`); ℤ with the ring's three universes distinct (`watts_unconditional_at_Z`, at `Int_Ring@{c p a}`); and a functor that is not a hom functor of `RMod R`, M ↦ Hom_Ab(U M, A) (`Instance/Mod/Watts.v`'s `HomAbForget R A`, continuous because `RMod_Forget_Ab` is a left adjoint, `coex_adjunction`), where `watts_unconditional_coext` identifies the module returned with `CoextObj R A` and `watts_unconditional_coext_2a` with Exercise 2(a)'s `HomZL R_R A`, at `RingObject@{c c c}`, which `CoextObj` imposes. Each witness's representing module is known in tree independently (`HomAb_representable`, `coext_representable`), so the witnesses exercise the premises and identify the module rather than find a new one. NOT witnessed: a ring whose carrier sits at `Set` (the theorem and its ℤ witness are refused there, `Test/ProbeWatts454.v`'s N25 and N24); the covariant form, Exercise 3, which has no unconditional statement in tree (the `watts_ex3` row below) |
| `watt_ab_iso`, `watt_ab_repr`, with `HomAb`, `CoHomAb`, `HomAb_continuous` and `CoHomAb_continuous` (`Functor/Representable/Additive.v`, #454 — the "plus additivity" step of Mac Lane's proof of Watt's theorem) | an additive `T : C ⟶ Ab` on a locally propositional Ab-enriched `C`, and a representation of `Ab_Forget ◯ T` in `Sets` | WITNESSED with no hypothesis at functors that are not hom functors: `Instance/Mod/Watts.v`'s `watt_at_forget` (C := `RMod R`, T := `RMod_Forget_Ab R`, the representation `Instance/Mod/Representable.v`'s `rmod_representable` carried along the identity-component `RMod_Forget_equiv`: `HomAb (RMod_AbEnriched R) (Ring_RMod R) ≅ RMod_Forget_Ab R`, at `RingObject@{c c c}`) and `ab_hom_Z_iso` (C := `Ab`, T := `Id[Ab]`, the representation from `free_ab_adjunction`: Ab(ℤ, X) ≅ X natural in X, ℤ := `FreeAb SetsOne`); and at `(RMod R)^op`, the representation coming from `representability_theorem`, in the unconditional Watt theorem above. `watt_ab_iso_HomAb` and `watt_ab_iso_at_Ab` feed it a hom functor's own representation, which exercises the hypotheses without testing them. The contravariant reading passes `LocallyPropositional_op` explicitly, resolution being refused (`Test/ProbeWatts454.v`'s N1) |

## Conditional results (no in-tree witness of the distinctive premise)

Each result here is proven, but its distinctive hypothesis has no inhabitant
anywhere in the library, so no concrete object exercises it.  These are
honest conditionals — "given such-and-such structure, the following holds" —
and nothing proven elsewhere secretly depends on their being inhabited.

| `field_every_monic` (`Instance/Field.v`) | the blanket premise `∀ F : FieldObject, FieldStable F` (¬¬-stability of `≈` in every source field) | uninhabited as a blanket: no uniform discharge exists, and `stability_is_the_conclusion` shows the per-square instance is the conclusion restated. The PER-OBJECT form is discharged where it matters — `field_dec_stable` from decidable equality gives `Q_Field_stable`, so `Q_hom_monic` (every hom out of ℚ is monic) is unconditional, covering every `FieldObject` in the tree; the unconditional apartness form `field_hom_distinct` needs no hypothesis at all |

| Result | Distinctive premise | Status of the premise |
|--------|---------------------|-----------------------|
| `GAFT` (solution-set form) | `Complete C` | witnessed at `Sets` — `Sets_Complete` (`Instance/Sets/Complete.v`) and, since #329, `Sets_Cocomplete` (`Instance/Sets/Cocomplete.v`), the first hypothesis-free inhabitant of either class; the theorem is stated for an arbitrary `C`. An earlier revision of this cell added "and no OTHER category has one"; measured against the tree on 2026-09-12 that is false on four counts, all unconditional (no `Complete` premise of their own): `Grp_Complete` (`Instance/Grp/Limit.v`), `Presheaf_Complete` and `Fun_Sets_Complete` (`Instance/Fun/Limit.v`) and `Subsets_Complete` (`Instance/Powerset.v`) — since #443 two more, `Ab_Complete` (`Instance/Ab/Limit.v`) and `Rng_Complete` (`Instance/Rng/Limit.v`), both by strict limit creation over `Sets`, and since #444 a seventh, `SmallOrd_op_Complete` (`Instance/Ordinal/Large.v`), the reversed order on the large ordinals — bringing the unconditional count to SEVEN. **#444 also sharpens what this row means.** The library's `Complete` carries a smallness side condition — the diagram shape's object universe strictly below the ambient's, spelled out in terms in the header of `Instance/Sets/Complete.v` — and `Adjunction/GAFT/Necessity.v`'s `Complete_Initial` consumes the COLLAPSED instance instead, i.e. large-completeness. Measured: `Complete_Initial` is REFUSED by `Sets_Complete`, `Grp_Complete`, `Ab_Complete` and `SmallOrd_op_Complete` alike, and ACCEPTED by exactly one witness, the thin `Subsets_Complete`. That is Freyd's collapse (`Structure/Complete/Freyd.v`) showing up as a universe constraint, and it is why Mac Lane's §V.6 counterexample has to live at small-completeness: `Instance/Ordinal/Large.v` inhabits `Complete` at the small instance and REFUTES it at the collapsed one over one and the same category (`SmallOrd_op_not_large_complete`), with `SmallOrd_self_limit` as the control showing the collapsed pairing is not uninhabitable in general — criterion: `grep -E '(Definition|Instance) [A-Za-z_0-9]*Complete '` over the tree outside `Test/`, keeping those whose type is `@Complete <category>` with no `@Complete` argument, which excludes the transported `Arrow_Complete`, `Coslice_Complete`, `Res_Complete`, `Product_Complete` and `reflective_Complete`. Since #442 GAFT is also APPLIED at `Grp` (`free_group_via_GAFT`, `Instance/Grp/FreeAFT.v`), on `Grp_Complete` and `Grp_Forget_continuous`, and since #443 twice more over rings (`free_rng_ab_via_GAFT` at `Rng_Forget_Ab` and `free_ring_via_GAFT` at `Rng_Forget`, `Instance/Rng/AFT.v`), on the new `Rng_Complete`. An earlier revision of this cell continued "ALL THREE applications' SOLUTION SETS are derived from the very adjunctions the theorem is meant to produce, so each is a genuine instantiation and none is an independent existence proof; every file says so in capitals". Measured on 2026-09-17, after the PR "algebraic carriers are sets": NONE of the three is derived that way any more. `free_group_via_GAFT` consumes `Grp_Forget_solution_set_prop` (`Instance/Grp/FreeAFT.v`) and the two ring applications consume `Rng_Forget_Ab_solution_set_prop` and `Rng_Forget_solution_set_prop` (`Instance/Rng/AFT.v`), each the `Prop`-valued congruences on the relevant term model, covering every hom through the kernel of the evaluation into the target — a congruence exactly because the target's object record now carries `grp_prop` / `rig_prop`. Each statement is byte-identical to the circular one it replaces; each circular solution set is KEPT beside it, re-headed as a removal candidate, with its own application preserved under a `*_via_GAFT_from_adjunction` name so the contrast stays visible at every use site. So all three ARE independent existence proofs now, and the GAFT row of this table no longer carries the disclosure it carried since #442. The one new cost is `Set < carrier`, which bites at a named object rather than in general: `` `1 free_group_via_GAFT TwoLetters `` is refused where `SetOne` is not. What #443 does remove is a SECOND circularity that was there for `Ab`: `Ab_Forget_Continuous` (`Instance/Ab/FreeNotContinuous.v`) is RAPL applied to the free/forgetful adjunction, so GAFT's second hypothesis was circular at `Ab` too; `Ab_Forget_creates_continuous` (`Instance/Ab/Limit.v`) comes from limit creation and is not. No circular hypothesis remains at any of the three instances. Two earlier readings are corrected in place rather than deleted. The first ended "which is what `#1309` records"; #1309's repair — annotating `DiscreteCat_Functor` (`Instance/Discrete.v`) so that no `Set` is minimized into `GAFT`'s statement — was APPLIED on 2026-09-17 in the PR "algebraic carriers are sets", so the `Set` pin #1309 was filed against is gone, and it was never what kept these solution sets circular. The second said "what keeps them circular is the size wall of the preamble: the index the honest solution set wants sits one universe above the shape-object universe `GAFT` demands. A later commit of this PR lifts that for the free group, through `PropEquiv`-indexed congruences; until it lands, all three remain read off the very adjunctions the theorem is meant to produce." That commit landed, and it lifted all three rather than one: the congruence index is carrier-sized because the congruences are `Prop`-valued, so it sits AT the shape-object universe instead of one above it, the bound relating carrier to index being `carrier ≤ index` and not `carrier < index`. What is still NOT built is Mac Lane's own `Subgroup G`-indexed family, which the congruence index replaces rather than resizes; #1309 stays open on that count. This cell read "no `Complete`/`Cocomplete` instance exists in-tree" until #329: the `Cocomplete` half was falsified by that issue, the `Complete` half had already been stale since `Sets_Complete` landed. Re-measured by the criterion above on 2026-09-23, the unconditional count is NINE, not SEVEN: the seven above, #449's `RMod_Complete` (`Instance/Mod/Limit.v`), which the count had not picked up, and #450's `SVariety_Complete` (`Instance/Variety/Limit.v`), for every signature and set of equations. #451 adds two constants with no `Complete` premise that the criterion's pattern does not match, because in their declarations `Complete` is followed by a universe binder or by a suffix: `Structure/WellPowered.v`'s `Indiscrete_Complete` (every indiscrete category on a type, given a point of it) and `Instance/Powerset/WellPowered.v`'s `Subsets_Complete_free` (`Instance/Powerset.v`'s `Subsets_Complete` restated with its hom level free, so the same category as one already counted). Neither is in the NINE. #452 adds a third that the pattern does not match, `Instance/Sets/SpecialInitial.v`'s `setsop_comp : Complete (Sets^op)`, which is `Construction/Product/Limit.v`'s `Complete_op_of_Cocomplete` applied to `Sets_Cocomplete` (that constant's first application at a concrete category, by a grep of its name over the comment-stripped `.v` files outside `Test/`); it is not in the NINE either. A pattern widened to accept those spellings also matches pre-#451 constants whose premises are other than `Complete` (`Instance/Cat/Limit.v`'s `StrictCat_Complete`, over uniqueness of identity proofs and dependent function extensionality, for one), which the parenthetical reading of "unconditional" above does not settle, so no widened total is given. Since #450 `GAFT` is also applied at the DIAGONAL `Δ[J]` of `Grp`, `Rng` and `SVariety E`, for every shape within `Cocomplete@{u u0 u u1}` (shape homs AT the carrier universe, shape objects at or below it), and at `SVariety_Forget` (`Free_Variety_via_GAFT`); the row for `Grp_Cocomplete_via_GAFT` above records those witnesses, which give the tree's first `Cocomplete` inhabitants at algebraic categories (before #450 the pattern `'@?Cocomplete *\(?(Grp|Rng|Ring|Ab|CMon|RMod|Rg)'` over the `.v` files hit only a prose line of `Instance/Ab/DirectedColimit.v`, as `Instance/Grp/Colimit.v`'s header records). Since #453 `GAFT` is also applied by `Adjunction/SAFT/Characterization/Cover.v`'s `SAFT_cover_wp_at` and `SAFT_cover_wp`, to the solution sets `saft_solution_set_at` built from a PROVED covering datum over completeness, continuity and a cogenerating family, indexed by a well-powering that `GAFT`'s pin bounds, and at a concrete category by `Adjunction/SAFT/Characterization/Examples.v`'s `sets_id_saft_cover`, at `Id[Sets]` under `Untruncate`, circular as `GAFT_at_Sets_Id` is (its continuity is read off `adj_id`); without `Untruncate`, `GAFT`'s index pin refuses the one-universe-up well-powering of `Sets` there (`Test/ProbeSAFT453.v`'s N12) |
| `initial_iff_weakly_initial_family_complete` (`Theory/WeaklyInitial.v`, #435) | `Complete C` and `HasEqualizers C` | witnessed at `Sets` — `Sets_Complete` and `Sets_HasEqualizers` (`Adjunction/GAFT/Sets.v`) — with the FULL round trip in `Theory/WeaklyInitial/Sets.v`: `Sets_Initial` → the singleton family `Sets_wif` → Freyd's construction → `Sets_initial_recovered`, canonically isomorphic to the original (`Sets_roundtrip_iso`); the derived singleton family's index and member read back at `eq_refl`. The per-family form `initial_iff_weakly_initial_family` needs its two products and the equalizer supply; the necessity direction `weakly_initial_of_initial` needs nothing at all |
| `SAFT` | `Cogenerator` + `SubobjectIndex` + `SubobjectCover` (the covering datum) | none of the three is inhabited. Since #437 `SAFT` IS applied, exactly once and conditionally, by `saft_representable` (`Adjunction/Representability/Sets.v`), which inherits all three premises and so has no witness either; this cell read "`SAFT` is never applied" until then, and the same sentence in `Adjunction/GAFT/Sets.v` and `Instance/Sets/Complete.v` is corrected in place. **CORRECTION (#451)**: an earlier revision of this row gave the premise column as "`SolutionSet` + `Cogenerator` + `SubobjectIndex`"; `SolutionSet` is what `SAFT_solution_set` BUILDS, and the third datum `SAFT` takes is `SubobjectCover`. And "none of the three is inhabited" is false of `SubobjectIndex`: `Adjunction/SAFT.v`'s `empty_SubobjectIndex` inhabits it at every object of every category — it has no exhaustiveness clause, so it is not well-poweredness (`SubobjectIndex_not_exhaustive`) — and `Adjunction/SAFT/WellPowered.v`'s `WellPowered_SubobjectIndex` supplies it from any well-poweredness witness, `Sets_WellPowered_untruncate` under `Untruncate` among them. `SubobjectCover` is worse than uninhabited: `Adjunction/SAFT/Sets.v`'s `SubobjectCover_Id_Sets_absurd` REFUTES it at `Id[Sets@{h cobj}]` with `Set < h`, for every completeness witness, cogenerating family and index, although classically the hypotheses of the special adjoint functor theorem hold there, so `SAFT` and `saft_representable` are inapplicable at the identity of `Sets`; the datum is taken over the one product `cogen_prod comp G`, where the classical argument's product depends on the object `d`. No `Cogenerator` at `Sets`, or at any other concrete category, is built in tree (a grep for `Cogenerator` over the `.v` files finds it only as a hypothesis, in prose, and in `Adjunction/SAFT.v`'s record and `Structure/Generator/Dual.v`'s generic bridges); `Structure/Generator/Dual.v`'s `cog_of_gen` gives one at `Sets^op` from `Instance/Sets/Generator.v`'s `Sets_Generator` — `cog_of_gen Sets_Generator : Cogenerator (Sets^op)` is accepted in a scratch file and reported closed under the global context — but is applied to it nowhere. **CORRECTION (#452)**: three claims of this cell no longer hold. "None of the three is inhabited" is false of `Cogenerator` as well: `Instance/Sets/Cogenerator.v` builds two SMALL cogenerators of `Sets` under `Untruncate` or `IEM` (`Sets_Cogenerator_untruncate`, `Sets_Cogenerator_IEM`) and an unconditional LARGE one (`Sets_Cogenerator_large`, which `cogen_prod` at `Sets_Complete` refuses on universes), `Adjunction/SAFT/InitialObject/Examples.v` builds cogenerators of the thin `Subsets X` and `Indiscrete`, and `Instance/Sets/SpecialInitial.v`'s `setsop_cog` IS `cog_of_gen Sets_Generator`, so the sentence "No `Cogenerator` at `Sets` … applied to it nowhere" is superseded. "Exactly once" has not held since #451: `Adjunction/SAFT/WellPowered.v`'s `SAFT_of_WellPowered` applies `SAFT` too, as conditionally (a grep for the word `SAFT` over the comment-stripped `.v` files outside `Test/` finds, besides the definition, its `Require` lines and two module-qualified field names, those two applications and no other). And under one `U : Untruncate` every premise of `SAFT` at `Id[Sets]` but `SubobjectCover` is now inhabited in tree: `SAFT` applied to `Sets_Complete`, `Sets_Id_PreservesImageLimit`, `Sets_Cogenerator_untruncate U` and `WellPowered_SubobjectIndex (Sets_WellPowered_untruncate U)` is accepted in a scratch file at `Sets@{h cobj}` with `Set < h`, closed under the global context, as `Adjunction/SAFT.v`'s header records, and `SubobjectCover_Id_Sets_absurd_at_SAFT` refutes the covering datum for that very tuple. The object-level half of SAFT, the special initial-object theorem, has witnesses of its own (the `special_initial_object` row above), and `SAFT` is not re-shaped (#453). (CORRECTION, #453: the parenthetical forecast a re-shaping that did not happen. `SAFT` is kept unchanged, and the special adjoint functor theorem over the classical hypotheses was built beside it instead — `Adjunction/SAFT/Characterization.v`'s `SAFT_wellpowered`, with no covering datum, and `Adjunction/SAFT/Characterization/Cover.v`'s `SAFT_cover_wp`, whose covering datum `SubobjectCoverAt` is taken over a product depending on `d` and PROVED by `saft_cover_at` from completeness, preservation and the cogenerating family at every well-powering, `wp_trivial`'s included. At one tuple at `Id[Sets]` under `Untruncate`, `Adjunction/SAFT/Characterization/Examples.v`'s `sets_id_cover_separation` inhabits the new datum at every object and refutes `SubobjectCover`; see the `SAFT_wellpowered` row above and the `SAFT_thm2` row below. #453 adds no application of `SAFT` itself: the grep described above, re-run over the comment-stripped `.v` files outside `Test/` and `doc/`, still finds the same two.) The row stays conditional |
| `SAFT_thm2`, `SAFT_iff` (`Adjunction/SAFT/Characterization.v`, #453 — Mac Lane §V.8 Theorem 2) | `HasSubobjectWidePullbacks C` (every family of subobjects of an object, indexed at a universe at or above the objects, has a wide pullback) together with `PreservesWidePullbacks U`, besides `Complete C`, `Cogenerator C` and `PreservesImageLimit U` | placed among the conditionals, not above, because its distinctive premises are inhabited together at a THIN category only, and circularly, with no witness at a non-thin category in tree, even conditionally: `Adjunction/SAFT/Characterization/Examples.v`'s `subsets_inverse_image_thm2` applies `SAFT_thm2` at `InverseImage f` on `Subsets Y`, `HasSubobjectWidePullbacks` by `Complete_HasSubobjectWidePullbacks (Subsets_Complete_free Y)` (the objects of `Subsets Y` sit at or below its shape universe) and both preservation premises read off `image_preimage_adjunction f`, the adjunction whose left adjoint the theorem then builds, so it shows the premises satisfiable together and constructs no adjoint not already known; it is closed under the global context and is the one application of `SAFT_thm2` at a concrete category (a grep of the name over the comment-stripped `.v` files outside `Test/` and `doc/` finds `Characterization.v` and `Examples.v`). At every category that is not thin the theorem is a parametric conditional: completeness supplies `HasSubobjectWidePullbacks` at indices at or below its shape universe (`Complete_HasSubobjectWidePullbacks`), which in the adjoint functor theorems' regime `so < o` lies below the objects where `least_index` lives, and the same term asked at the objects' universe is refused (`Test/ProbeSAFT453.v`'s N22–N24, N24 at `Sets@{o so}` with `Sets_Complete`; N26 refuses handing `least_index` to `HW` at an index below the objects); no other route to the premise at the objects' universe was attempted, and none from `WellPowered` and `Complete` exists in tree (the first clause of #453's Work item 3, a stated deviation). `PreservesWidePullbacks` alone is inhabited widely — `right_adjoint_PreservesWidePullbacks` at every right adjoint, and #449's `Bilin_PreservesWidePullbacks` — so the premise that bites is the first. `SAFT_iff` is applied at no concrete category (the same grep finds `Characterization.v` only) |
| `continuous_Set_functor_representable`, `continuous_Set_functor_representable_iff`, `continuous_Set_functor_representable_at` (`Adjunction/SAFT/Characterization/Corollaries.v`, #453 — Mac Lane §V.8 Corollary, "every continuous functor K : A → Set is representable", and Riehl's Epilogue criterion with well-poweredness in place of intersections) | `Complete C`, `Cogenerator C` and `WellPowered C` (for `_at`, well-poweredness at the one object `saft_prod K comp G SetsOne`), with `PreservesImageLimit K` for a `K : C ⟶ Sets` | witnessed only CONDITIONALLY, at `Sets@{o so}` under `Untruncate`: `Adjunction/SAFT/Characterization/Examples.v`'s `sets_id_repr_saft` at `Id` (circular, `Sets_Id_PreservesImageLimit` being read off `adj_id`) and `sets_hom_repr_saft` at `HomFrom A` (continuity by `hom_ContinuousFunctor`, proved directly), both closed under the global context; no `Sets`-valued functor on `Subsets Y` or on `Indiscrete` is handed to it. `continuous_Set_functor_representable_iff` and `continuous_Set_functor_representable_at` are applied at no concrete category (a grep of each name over the comment-stripped `.v` files outside `Test/` and `doc/` finds `Corollaries.v` only). The first supersedes `Adjunction/Representability/Sets.v`'s `saft_representable`, which is kept (the `SAFT` row above). **CORRECTION (#454)**: "witnessed only CONDITIONALLY, at `Sets@{o so}`" is no longer the whole list, and the Corollary's STATEMENT now holds unconditionally at one non-thin category by another constant. `Instance/Mod/Watts.v`'s `watts_theorem_sets` applies it at `(RMod R)^op` under one `Untruncate@{c}`, at `RingObject@{c c c}`, and at a concrete functor `watts_theorem_coext` does so at `HomAbForget R A` (M ↦ Hom_Ab(U M, A), continuous by `HomAbForget_continuous`, `RMod_Forget_Ab` being a left adjoint), the representing object ≅ `CoextObj R A` (`watts_theorem_coext_obj`; the representation was known in tree, `coext_representable`); `watts_ex3_sets` applies it at `RMod R` under `Untruncate` and a cogenerator taken as a hypothesis, at the forgetful functor as `watts_ex3_forget` (object ≅ `Ring_RMod R`). All inherit `Untruncate`. The statement "every continuous K : A → Set is representable" holds UNCONDITIONALLY at A := `(RMod R)^op`, for every ring, as `Instance/Mod/Watts/Unconditional.v`'s `RModop_continuous_representable` (the `watts_theorem_unconditional` row above), which does not use this constant. `_iff` and `_at` are still applied at no concrete category (the same grep, re-run on the #454 tree, finds `Corollaries.v` only) |
| `left_adjoint_of_representable_Sets` (`Adjunction/Representability/Sets.v`, #437) | `HasSetsCopowers C` — a copower of each object by each SETOID, i.e. by an object of `Sets` | NOT inhabited, and #366's copowers cannot supply one: every copower in tree (`Structure/Limit/Power.v` from #321, with #366's `Structure/Limit/Power/Adjunction.v`) is indexed by a bare `Type` and so yields plain functions out of the index, where a `Sets`-valued adjunction needs morphisms respecting `≈` (`Test/ProbeRepresentability437.v`'s N5 pins the type mismatch); no inhabitant is built here by any other route either, and none is shown impossible. By #348's `adjunction_iff_pointwise_representable` the hypothesis IS "the represented functor has a left adjoint", so the converse of §V.8 Ex 1 is conditional by construction. The rest of #437 is unconditional: `representability_theorem` and `representability_iff` are witnessed at `Sets` twice over (`Sets_points_repr`, `Sets_Id_repr`, and `Sets_Id_repr_of_adjoint` by the forward direction of Ex 1) |
| `Sets_Classifier`, `Sets_Classifier_dec` (`Instance/Sets/Classifier/OneLevel.v`) | `Untruncate` (every impredicative truncation at level o inverts) / `DecImage` (every mono's image is decidable) | no axiom-free in-tree inhabitant of either; both follow from informative excluded middle at level o (`IEM`), and `DecImage` is equivalent to it (`DecImage_iff_IEM`); the classical instance compiles out of tree on exactly `classic` + `constructive_indefinite_description` (re-measured against the shipped `Sets_Classifier_IEM`) and is not shipped; the unconditional instance is neither built nor refuted |
| `Fun_Classifier`, `Fun_Classifier_small` (`Instance/Fun/Classifier.v`) | `Untruncate` (every impredicative truncation at level o inverts) | no axiom-free in-tree inhabitant; it follows from informative excluded middle at level o (`IEM`), and `Fun_Classifier_IEM` is that instantiation; the classical instance compiles out of tree on exactly `classic` + `constructive_indefinite_description` (re-measured against the shipped `Fun_Classifier`) and is not shipped; the unconditional instance is neither built nor refuted. The hypothesis is spent EXACTLY ONCE, at a single line of `med_img`, the mediator both `presheaf_char_pullback` and section (I)'s `small_pb` route through; `presheaf_char_unique` and `char_square` need neither it nor `Monic`, and whether it is NECESSARY is not settled. The arrow-shape counts (`twoX_three_sieves`, `twoY_two_sieves`) are conditional on `IEM` for the same reason the instance is |
| `Sets_WellPowered_untruncate`, `Sets_wellpowered_intersection`, `Sets_intersection_all_empty` (`Instance/Sets/WellPowered.v`, #451) | `Untruncate@{o}` (every impredicative truncation at level o inverts) | no axiom-free in-tree inhabitant, exactly as for `Sets_Classifier` above: the pinned witness IS `Structure/WellPowered.v`'s `wp_of_classifier` at `Sets_Classifier U`. Unconditionally `Sets` is well-powered and co-well-powered only ONE UNIVERSE UP (`Sets_WellPoweredAt_up`, `Sets_CoWellPoweredAt_up`, indices at `o+1`), and offering the first where the pin is expected is refused ("Cannot enforce o = so because o < so", quoted in the file); `Grp` likewise, one universe up only (`Grp_WellPoweredAt_up`, `Instance/Grp/WellPowered.v`, refused at the pin with "Cannot enforce o = h because h < o"). The unconditional pinned form at `Sets` is neither built nor refuted, and co-well-poweredness at the pin is not attempted even under `Untruncate` |
| `Sets_Cogenerator_untruncate`, `Sets_Cogenerator_IEM` (`Instance/Sets/Cogenerator.v`, #452 — the first `Cogenerator`s of `Sets`, both SMALL, indexed by `unit`) and the special initial-object theorem at `Sets`: `sets_special_initial_untruncate`, `sets_special_initial_wellpowered`, `sets_special_initial_book`, `sets_special_initial_IEM` and `sets_special_initial_IEM_at_Set` (`Instance/Sets/SpecialInitial.v`, #452), and with them `Adjunction/SAFT/InitialObject.v`'s `special_initial_object_book`, whose premise `HasClassIntersections` is inhabited by a library constant only here (`sets_special_initial_book`; at `Subsets X` the term `special_initial_object_book (Subsets_Complete_free X) (Subsets_Cogenerator_empty X) (wellpowered_class_intersections (Subsets_WellPowered X) (Subsets_Complete_free X))` is accepted with no hypothesis in a scratch file, closed under the global context) | `Untruncate@{o}` / `IEM@{o}` (at `sets_special_initial_IEM_at_Set`, `IEM@{Set}`) | no axiom-free in-tree inhabitant of either, exactly as for `Sets_Classifier` above. What the hypothesis supplies is a SMALL cogenerator, indexed at `Sets_Complete`'s shape universe where `cogen_prod` needs it: `Sets` HAS an unconditional `Cogenerator`, `Sets_Cogenerator_large` (every object, each tested by its identity), but its index lies strictly above that universe, and `cogen_prod` at `Sets_Complete` refuses it on universes (`Test/ProbeSpecialInitial452.v`'s N13 and N14, labelled P_large_cog). The `Type`-valued truth-setoid route to an unconditional small cogenerator is refused on universes as well (N11 and N12, labelled P_type_truth), a refusal of one construction and not an impossibility proof; the unconditional small cogenerator is neither built nor refuted. `Sets_terminal_not_cogenerates` shows the condition is not vacuous at `Sets`. Of the witnesses, `sets_special_initial_wellpowered` and `sets_special_initial_book` COMPUTE the least subobject as an intersection (`Structure/WellPowered.v`'s `complete_intersection`, and `Instance/Sets/WellPowered.v`'s `Sets_wellpowered_intersection` read as `HasClassIntersections`) and meet `Sets_Initial` only through `initial_unique`; `sets_special_initial_untruncate` and the two `IEM` ones take `sub_bot`, whose domain is `Sets_Initial`'s empty setoid, and rediscover it at `eq_refl` (the circularity caveat of the `special_initial_object` row above). `special_initial_object_small` at `Sets` is refused (N10), the objects of `Sets@{o so}` living above `Sets_Complete`'s shape universe |
| `RoundTrip_Equivalence` | a `SplitCleaving` of the required shape | never inhabited in that shape |
| `beck_monadicity` | `CreatesUSplitCoequalizers` composed from the engine | never assembled; `Id` is shown monadic by a direct proof (`Monad/Monadicity/Examples.v`), bypassing the coequalizer machinery |
| `Pointwise_LocalRightKan` | `∀ b, Limit (X ◯ comma_proj2)` over `=(b) ↓ F` | no in-tree inhabitant; `Structure/Limit/Kan/Pointwise.v` is a leaf module, nothing instantiates it |
| `Pointwise_LocalLeftKan` | `∀ b, Colimit (X ◯ comma_proj1)` over `F ↓ =(b)` | same: the dual premise is likewise never inhabited |
| `image_mediator_epic` | an `Abelian` category | no `Abelian` instance; `CMon` cannot serve, since `Additive` requires additive inverses |
| the `Sheaf` development | a `Site` | no `Site` instance; the development is abstract throughout |
| `StarAutonomous` | a `SymMonClosed` category | the BASE is now INHABITED — `Instance/Mod/Closed.v`'s `RMod_SymMonClosed` makes `RMod R`, for a commutative `R`, the tree's first `SymMonClosed`, so the parametric results over that class (notably `Structure/Monoidal/Dual.v`'s self-adjoint dual functor) now have a concrete model. `StarAutonomous` ITSELF remains uninhabited: it additionally needs a dualizing object with the double-dual map invertible, and nothing here supplies one. The double-dual FIELDS (`star_double_dual`/`star_natural`) now have a concrete inhabitant PATTERN: `Instance/FdVect/DoubleDual.v` proves `double_dual_natural` (naturality of evaluation over ALL F-modules) and `double_dual_iso` (invertibility per FINITE-DIMENSIONAL object) by the same `dual ◯ dual^op` shape; no `StarAutonomous` instance follows from it, since invertibility of the canonical double-dual map is exactly what a dualizing object would have to supply. It is NOT recorded here as the first NON-CARTESIAN closed monoidal witness: that phrase needs a proof that the tensor is not the categorical product, `RMod R` does have binary products (the direct sum, via `Instance/Mod/Coproduct.v`'s `RMod_Cartesian`), and no such separation is proved — both new file headers and the commit that lands them say so. Note the parenthetical that once stood here — that the module tensor product was deferred — is now stale: it is `Instance/Mod/Tensor.v`'s `TensorMod`, and `Instance/Mod/Monoidal.v` gives it a full symmetric monoidal structure |
| `pointed_part_equivalence` | the GLOBAL basepoint decidability `∀ Z, DecidablePt Z` | uninhabited: finiteness discharges only the per-object form (`PointedBool`, `PointedThree`); the global form is classically automatic but follows from no finite witness. The functor's full-and-faithfulness (`Part_to_Pointed_Full`/`_Faithful`) is unconditional |
| `Regular`, `Distributive`, `Additive`, `localization_universal` | the corresponding class | abstract-by-design; no in-tree instance |
| `Regular_HasImages`, `HasImages_of_OFS` (`Theory/Subobject/Lattice.v`, #445) | a `Regular C` instance, or an `OFS E MonoClass` — an orthogonal factorization system whose right class is the monos | neither is inhabited anywhere in the tree — the `Regular` row above, and the only other `OFS E MonoClass` in tree, `Structure/Abelian.v`'s `Abelian_OFS`, sits under an `Abelian` context that no instance discharges; the join half of the subobject lattice is therefore witnessed NOT through these but through the two DIRECT `HasImages` instances, `Sets_HasImages` (`Instance/Sets/Image.v`'s factorization read as an `ImageOf`) and `FinSet_HasImages` (the counted-subset codec), which is the route the Witnessed table records |
| `grp_quot_is_quot_by_kernel_stable` (`Instance/Grp/QuotObj.v`, #446) | `GrpImageStable (quot_epi q)` — double-negation elimination on the image of the epi, which `Instance/Grp/Epi.v` shows is the constructive gap between `Epic` and `GrpSurjective` | uninhabited in general and, for an epimorphism, equivalent to its own conclusion (`Instance/Grp/Epi.v`'s `stability_is_the_conclusion`); the surjective form above is the one with a witness |
| `Category_SpanMonoid`, `Category_monoid_iso` (`Theory/Category/Monoid.v`) | `HomRigid C` | no in-tree category is given a `HomRigid` witness; `HomRigid_of_ObjUIP` + Hedberg would supply one for any category with decidable object equality, but the application is never made — and the necessity theorem `arrow_mul_respects_forces_UIP` shows the premise cannot be discharged uniformly |
| `creates_limits_Complete`, `EM_Complete` (§V.4 Thm 2, completeness half) | `Complete` on the base category | witnessed at `Sets` only, the same status as the `GAFT` row (see that row for the correction history); the per-diagram creation results above are witnessed and are what the development exercises |
| `dual_functor_no_right_adjoint`, `dual_not_left_adjoint_of_op` and their transports to the #359 reading of the dual, `dual359_no_right_adjoint`, `dual359_not_left_adjoint_of_op` (`Instance/FdVect/NoRightAdjoint.v`) | `CoordSpanProper F` — a functional on `F^ℕ` outside the span of the coordinate projections | NO constructive witness is POSSIBLE, not merely absent: the premise is classically true for every field (Erdős–Kaplansky) but, for F = ℚ, independent of ZF + DC — in a model where every set of reals has the Baire property, every additive map `ℚ^ℕ → ℚ` is continuous (Pettis) and so a finite combination of coordinates — so unlike the rows above it will never get an in-tree inhabitant without a choice principle (analysis in the file's header, not formalized); the positive half (`dual_vct_AdjointOnTheRight`, `dual_vct_Continuous`) is unconditional |
| `reflective_Cocomplete` (`Construction/Reflective/Colimit.v`, #434) | `Cocomplete C` on an ambient category carrying a `Reflective` subcategory | no ambient of an in-tree reflection (`Ab`, `Top`, `Ord`) is shown cocomplete, so at a concrete reflection the theorem stays a `Check`ed conditional — the same status as `reflective_Complete`'s; the per-diagram form `reflective_colimit` needs only the one ambient colimit, and its specialisation `reflective_terminal_shape_colimit` (any shape with a terminal object, by `terminal_Colimit`) needs no hypothesis at all — `Test/ProbeReflectiveColimit434.v` instantiates it at the torsion-free reflection of `Ab` over `Ordinal 2` (`p434_torsionfree_colimit`, apex at `eq_refl`) |
| `spanning_solution_set`, `factors_through_spanning`, `GAFT_from_spanning` (`Adjunction/SpanningArrow.v`, #448 — Mac Lane §V.7 Lemma 2) | `HasWidePullbacks A` (intersections of arbitrary families of subobjects) together with `PreservesWidePullbacks G` (the functor carries them to wide pullbacks); `GAFT_from_spanning` adds `Complete A` and `PreservesImageLimit G` | neither: the tree's only `HasWidePullbacks` instance, `Instance/Sets/SubobjectLattice.v`'s `Sets_HasWidePullbacks`, is universe-wise unusable here (its index lives at `Sets`' hom universe, `FactoringFamily` one level up), and NO in-tree functor is shown to preserve wide pullbacks, so no `SolutionSet` is produced through the Lemma for any concrete functor. Mac Lane's Remark 1, the underlying-set functor of a variety, is witnessed DIRECTLY instead: `Instance/Variety/Spanning.v`'s `variety_solution_set X : SolutionSet (SVariety_Forget E) X` builds the spanning arrow by hand (the generated subalgebra `SubGen` of the image), `SVariety` having no wide pullbacks in tree, so the general Lemma is not what produces it — and `variety_solution_set` is still CONDITIONAL, `Instance/Variety/Spanning.v` having needed no adaptation in the work described next. **Two things changed on 2026-09-17, in the PR "algebraic carriers are sets", and neither inhabits this row's premise.** First, `GAFT_from_spanning` was WIDENED: its section's hand-written `Set` annotation was deleted, taking it from eight universes over `{A : Category@{oA Set Set}} {X : Category@{oX Set Set}}` with `Set = oA` to thirteen with no literal `Set` in the type and not one `Set` constraint in the block, statement and proof term untouched. That removed an artifact, not a wall: the theorem is still refused at `RMod R`, now with the message "Cannot enforce u_hom = u_hom' because u_hom < u_obj <= u_hom'" — the widened form wants objects at or below homs, where `RMod R` has homs strictly below objects — and `HasWidePullbacks A` and `PreservesWidePullbacks G` remain uninhabited, so this row's status column stands. Second, Mac Lane's §V.7 Lemma 2 SMALLNESS clause, which the `Adjunction/SpanningArrow.v` delivery never attempted (`FactoringFamily` is a bare `Type`), is now a theorem at one instance: `Instance/Mod/TensorAFT.v`'s `tensor_spanning_SmallUpToIso` proves the spanning bilinear maps out of `V × V'` small UP TO ISOMORPHISM through the `Prop`-valued congruence index, and `tensor_via_AFT_maclane` feeds that to `representability_theorem` through `resize_up_to_iso` (`Adjunction/GAFT/Resize.v`). Read the boundary exactly: what Lemma 2 discharges there is SMALLNESS, not `GAFT_from_spanning`'s four hypotheses, and nothing of it reaches `FactoringFamily`. **#450 adds two facts that bear on this row without inhabiting its premise.** `SVariety E` now HAS binary pullbacks — `FinitelyComplete_HasPullbacks (Complete_FinitelyComplete (SVariety_Complete E))` is accepted and closed, as `Adjunction/SpanningArrow.v`'s correction records — but still no `HasWidePullbacks`; and `variety_solution_set` is refused by `GAFT` even with the real completeness and continuity (`Instance/Variety/Spanning.v`'s header quotes the refusal), Mac Lane's §V.6 route to the free algebra being inhabited instead through the congruence-indexed `SVariety_Forget_solution_set` (`Free_Variety_via_GAFT`, `Instance/Variety/Colimit.v`). **CORRECTION (#451)**: three phrases above are stale. "The tree's only `HasWidePullbacks` instance" was already stale once #449's `RMod_HasWidePullbacks` (`Instance/Mod/Spanning.v`) landed, and #451 adds a general producer, `Structure/Pullback/Wide/Complete.v`'s `Complete_HasWidePullbacks`, a `Definition` and not an `Instance`. "`SVariety` having no wide pullbacks in tree" and "still no `HasWidePullbacks`" no longer hold: `Complete_HasWidePullbacks (SVariety_Complete E)` inhabits `HasWidePullbacks (SVariety E)`, closed under the global context (measured in a scratch file; in tree that term is only `Test/ProbeWellPowered451.v`'s controls `p451_svariety_hwp` and `p451_n22_hwp_by_search_both`, closed by the same measure). None of them discharges this row's premise: `RMod_HasWidePullbacks` on universes (#449), and `Complete_HasWidePullbacks` at `Sets_Complete` and at `SVariety_Complete E` is refused by `spanning_solution_set` on universes (scratch files, quoted in `Adjunction/SpanningArrow.v`'s corrections, and pinned as `Test/ProbeWellPowered451.v`'s N20 and N21), its index sitting at `Complete`'s shape-object universe, the carrier, where `FactoringFamily` lives one level up. So the status "neither" stands, and `variety_solution_set` is still built directly. **CORRECTION (#453)**: "NO in-tree functor is shown to preserve wide pullbacks" and "`HasWidePullbacks A` and `PreservesWidePullbacks G` remain uninhabited" were false of the second premise before this issue: #449's `Bilin_PreservesWidePullbacks : PreservesWidePullbacks (Bilin V V')` (`Instance/Mod/Spanning.v`, proved directly) inhabits it at one functor. Since #453 `Adjunction/SAFT/Characterization.v`'s `right_adjoint_PreservesWidePullbacks` inhabits it at EVERY right adjoint, a library constant applying it at `InverseImage f` on `Subsets Y` (`Adjunction/SAFT/Characterization/Examples.v`'s `subsets_inverse_image_thm2`), and `Adjunction/SpanningArrow.v`'s header records `spanning_solution_set` at `Id[Sets]` with it, over an abstract `HasWidePullbacks Sets`, accepted in a scratch file. The FIRST premise, `HasWidePullbacks A` at the universe `FactoringFamily` asks, still has no in-tree witness at `Sets`, `SVariety E` or `RMod R`, so the status "neither" reads "not the first, at any concrete category checked", no `SolutionSet` is produced through the Lemma for a concrete functor, and the row stays conditional |
| `watts_theorem`, `watts_theorem_sets`, `watts_theorem_adjoint`, `watts_theorem_Fz` and the witness `watts_theorem_coext` (with `watts_theorem_coext_obj`, `watts_theorem_coext_obj_2a`) (`Instance/Mod/Watts.v`, #454 — Mac Lane's route to Watt's theorem through the special adjoint functor theorem, the one the issue's Reviewer line names) | `Untruncate@{c}`, spent once in `Instance/Mod/WellPowered.v`'s `RMod_CoWellPowered_untruncate`; the ring at `RingObject@{c c c}`, forced by `RModop_Cogenerator` (`Test/ProbeWatts454.v`'s N17 and N18) | no axiom-free in-tree inhabitant of `Untruncate`, as for `Sets_Classifier` above; the route's other premises are unconditional (`RMod_Cocomplete_via_GAFT`, `RModop_Cogenerator`), and `RMod R`'s unconditional co-well-powering sits one universe up and is refused at the pin (N8). The CONCLUSION of the text form is nevertheless inhabited unconditionally, by the witnessed `watts_theorem_unconditional` row above: `watts_theorem_via_unconditional` derives `watts_theorem`'s statement from it with `U` carried and not consumed, and `watts_unconditional_coext` and `watts_unconditional_coext_2a` are the unconditional counterparts of `watts_theorem_coext_obj` and `watts_theorem_coext_obj_2a`. `watts_theorem_sets` is subsumed only at the instance whose second `Representable` universe is `RMod`'s object universe, and the readbacks `watts_theorem_obj` and `watts_theorem_to_at` are not subsumed (Unconditional.v's header). Not subsumed, and so conditional only: `watts_theorem_adjoint`, the left adjoint of T the book's proof builds, which only the special adjoint functor theorem builds here, and `watts_theorem_Fz`, the book's C ≅ F ℤ (ℤ read as `FreeAb SetsOne`) |
| `watts_ex3`, `watts_ex3_sets`, `watts_ex3_QZ` and the witness `watts_ex3_forget` (with `watts_ex3_forget_obj`) (`Instance/Mod/Watts.v`, #454 — Mac Lane §V.8 Exercise 3, the covariant form) | `Untruncate@{c}` (spent in `RMod_WellPowered_untruncate`) and a `Cogenerator (RMod R)`; for `watts_ex3_QZ`, Exercise 2(b)'s premises `Injective Ab QZ` and `QZ_family_cogenerates` in place of the cogenerator | no in-tree inhabitant of either premise. `Untruncate` as above; the only unconditional `Cogenerator (RMod R)`, `Instance/Mod/Cogenerator.v`'s `RMod_Cogenerator_large`, is large, and `cogen_prod` refuses it at `RMod_Complete R` (`Test/ProbeWatts454.v`'s N4); the small one on the book's premise is the `QZ_injective_cogenerator` row below, whose premises are classical, so `watts_ex3_QZ`'s hypotheses have no in-tree inhabitant and no inhabitant of all of them is constructible axiom-free unless double-negation elimination is. No unconditional covariant form is stated (`Instance/Mod/Watts/Unconditional.v`'s NOT DELIVERED), and nothing in tree proves either premise NECESSARY for one: the SAFT route consumes both and no other route is attempted, the ℚ/ℤ premise is the one condition PROVED classical, and `RMod R` well-powered at the pin without `Untruncate` is neither built nor refuted (`Instance/Mod/WellPowered.v`). Exercise 3's CONCLUSION at the forgetful functor holds with no hypothesis, `watt_at_forget` (the `watt_ab_iso` row above), by the additive upgrade and not by the special adjoint functor theorem |
| `QZ_injective_cogenerator`, `QZ_injective_cogenerator_via_2a`, `QZ_injective_cogenerator_ML`, `QZ_Cogenerator` and `QZ_cogen_prod` (`Instance/Mod/Cogenerator.v`, #454 — Mac Lane §V.8 Exercise 2(b)) | the book's "known" fact about ℚ/ℤ, taken as hypotheses: `HI : Injective Ab QZ` and `HC : QZ_family_cogenerates` (for `_ML`, `HC` in Mac Lane's own ≠ form, `ML_cogenerates`) | PROVABLY CLASSICAL, with no in-tree inhabitant. The same file's closed metatheorems derive double-negation elimination from `HC` (`QZ_cogenerates_Ab_DNE`; `cogenerator_stable_DNE` for any cogenerating family of `Ab` with ¬¬-stable equality) and the informative weak excluded middle from `HI` (`QZ_injective_WLEM`), and `coext_QZ_cogenerates_DNE` and `coext_QZ_injective_WLEM` show that the exercise's CONCLUSION at R = ℤ costs the same; they refute nothing, and show the premises unprovable without an axiom unless those principles are provable. No instance under `IEM` or `Untruncate` is built (Baer's criterion needs Zorn). For `_ML` the injectivity premise costs the same, while its ≠-form cogenerating premise lies outside the metatheorems' argument and is not shown classical. The transfers `right_adjoint_injective`, `right_adjoint_cogenerator` and `right_adjoint_ML` are unconditional and are applied at `coex_adjunction`; what is constructive about ℚ/ℤ is `QZ_divisible`, `zchar_determined` and `QZ_extends_along_mul` (Baer's condition at the ideals of ℤ), beside `Instance/Ab/Character/Finite.v`'s results for finite groups |
| `RMod_WellPowered_untruncate`, `RMod_CoWellPowered_untruncate` (`Instance/Mod/WellPowered.v`, #454 — Mac Lane §V.8's size conditions at `RMod R`, the premises of the special adjoint functor theorem's route to Watt's theorem) | `Untruncate@{c}`, turning a truncated preimage into data (a subobject's domain; a preimage under an epi) | no axiom-free in-tree inhabitant, as for `Sets_WellPowered_untruncate` above. Unconditionally `RMod R` is well-powered and co-well-powered ONE UNIVERSE UP (`RMod_WellPoweredAt_up`; `RMod_CoWellPoweredAt_up` and `RMod_CoWellPowered_up`, indexed by codomain, map and `Prop`-level surjectivity, at `c < w` and `a <= w`), and both are refused where the pin is expected (`Test/ProbeWatts454.v`'s N6 and N8); without `Untruncate` the pinned constructions are refused at a sort and at an elimination (N9, N10). The unconditional pinned form is neither built nor refuted. Unconditional, and so not in this section: `RMod_ring_separates`, `RMod_Generator` and `RModop_Cogenerator` (at `RingObject@{c c c}` only, N11; the generator row above) |

### The cospan universe note

`Cospan_Hypergraph` and the spider results are witnessed exactly when a
`HasPushouts` instance exists on a base category whose object universe sits
at or below its hom universe.  The only `HasPushouts` instance in the tree
is `Sets_HasPushouts` (`Instance/Sets/Pushout.v`), and it cannot serve: a
cospan's hom carries an apex *object*, so `CospanCat` requires
objects ≤ homs, whereas `Sets` places its objects one universe *above* its
homs.  The failure is structural, not an annotation defect —
`CospanCat Sets HP` reports a universe inconsistency for any `HP`.

The category that does fit is skeletal `FinSet`, whose objects are natural
numbers and therefore sit below its homs.  `FinSet_HasPushouts`
(`Instance/FinSet/Pushout.v`) supplies the instance, and
`Cospan_Hypergraph` together with both spider results now instantiate over
`FinSet` — each reported "Closed under the global context" — which is why
they appear in the witnessed table above.

### The skeleton choice note

The `Skeleton` record is inhabited in-tree, but it is not dischargeable
uniformly: "every category has a skeleton" is equivalent to the axiom of
choice, a caveat `Theory/Equivalence.v` has recorded since long before this
development, and `Theory/Skeleton.v` accordingly never produces a
`Skeleton C` for an arbitrary `C`.  Two things keep the packaging honest.
The uniqueness clause is stated at the level of `Sub`'s objects rather than
their carriers, and that strengthening is necessary rather than convenient:
`skeleton0_skeletal_forces_UIP` shows the carrier-level weakening cannot
yield skeletality of the subcategory without entailing UIP for every type,
by a free-loop-space countermodel over `DiscreteCat`, while
`skeleton0_is_skeletal_carrier` shows the carrier statement itself still
holds.  And every witness supplied is a category whose representatives can
be named explicitly — `Indiscrete bool` chooses `true`, a skeletal category
chooses itself — so no witness in the table smuggles a choice principle.

## Maintaining this table

When a new headline result is added, record here whether its distinctive
premise is witnessed in-tree, and by what.  When a previously conditional
premise gains an instance, move its row up.  The intent is that a reader can
tell at a glance which results are demonstrated over a concrete model and
which are proven parametrically and await one.

### The fundamental-groupoid note

Two limits of the π₁ development belong here rather than only in the file
headers, because the gap between them is exactly what this document exists to
keep visible.

**No space with a nontrivial fundamental group is exhibited.**  Both witnesses
above have trivial vertex groups at *every* base point, and both facts are
proved rather than assumed (`Bool_Discrete_loops_trivial`,
`TwoPoint_Indiscrete_loops_trivial`).  So
`fundamental_group_basepoint_independent` is witnessed in the strict sense
this document uses — its premise is met by an object the library builds, and
that premise is shown to be a genuine restriction — while the isomorphism it
produces happens to run between two trivial groups.  Reaching a nontrivial one
means building S¹ and computing π₁(S¹) ≅ ℤ, which needs covering-space theory;
nothing in the tree attempts it.  The witnessed pair is a contrast in
*connectedness* and in nothing else.

**A witnessed premise is not an exercised conclusion.**
`connected_vertex_moniso` is instantiated at `Bool_Wide`
(`Bool_Wide_vertex_moniso`), which satisfies both hypotheses; but `WideDeloop`
fixes the hom type uniformly, so the two vertex groups being compared are the
*same* monoid, and the resulting type is already inhabited by the identity
maps — `Bool_Wide_vertex_moniso_trivial` records exactly that, with no appeal
to connectedness or to the structure theorem.  The fundamental-groupoid
witness improves on this in one respect (the two vertex groups then have
different carriers, so the identity does not typecheck) and not in the other
(both groups are trivial).  A witness that made the conclusion informative
would need a connected groupoid whose vertex group both depends on the object
and is nonabelian; `S3_Grp` supplies such a group but nothing in the tree
spreads it over objects non-uniformly.

## Maintaining this table

When a new headline result is added, record here whether its distinctive
premise is witnessed in-tree, and by what.  When a previously conditional
premise gains an instance, move its row up.  The intent is that a reader can
tell at a glance which results are demonstrated over a concrete model and
which are proven parametrically and await one.
