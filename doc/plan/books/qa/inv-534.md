# INV-534 — Decision 6.4: the "quoted string that does not exist" on issue #534

**Question.** The plan (`doc/plan/books/qa/plan-tail-sections.md:718`) instructs: replace
`should share the resulting lemma rather than proving it twice` on #534. That exact substring
is not in #534's live body, so the edit was delivered as an appended correction block instead
(`doc/plan/books/qa/judgement-calls.md:529`, `doc/plan/books/qa/remediation.json` item `[351]`,
whose `note` reads `the 'share the lemma' instruction on #534 is impossible and must not
propagate [in-place match failed; delivered as a block]`).

**Answer.** The sentence exists. The plan's quote failed to match for one reason only: the live
body is hard-wrapped at ~80 columns and a newline falls between `rather` and `than`. Verified
against the live body (`gh issue view 534 --json body`, fetched 2026-08-01; issue `updatedAt`
`2026-08-01T12:51:00Z`, state OPEN):

| probe | result |
| --- | --- |
| `should share the resulting lemma rather than proving it twice` | **ABSENT** |
| `should share the resulting lemma rather` | PRESENT (body line 72, at end of line) |
| `than proving it twice` | PRESENT (body line 73, at start of line) |
| same quote after collapsing all whitespace runs to one space | **PRESENT** |

No CRLF in the body; the break is a bare `\n`.

---

## 1. The sentence the plan actually meant — EXACT verbatim

It is the final sentence of the `### Also covered by` (Riehl §E.5) append, occupying body lines
69–73. Reproduced byte-exactly, including its hard line breaks (note `…` U+2026, `⊔` U+2294,
`×` U+00D7, and the straight ASCII double quotes around the #547 quotation):

```
assumed enrichment. That derivation is #547's first Work item ("derive
invertibility of the canonical comparison `a ⊔ b → a × b` from the abelian
axioms … feeding `bicartesian_preadditive` without assuming it"), so the two
issues together cover both routes and should share the resulting lemma rather
than proving it twice.
```

The offending clause proper is `so the two\nissues together cover both routes and should share
the resulting lemma rather\nthan proving it twice.`

## 2. Replacement — and a hazard in the plan's literal instruction

**Hazard.** The plan's find string ends where the replacement text begins: swapping only
`should share the resulting lemma rather than proving it twice` →
`cover both routes under disjoint hypotheses; …` yields
`…so the two issues together cover both routes and cover both routes under disjoint
hypotheses; …` — "cover both routes" twice in one clause. The find string must be widened to
include `cover both routes and `.

**Second consideration.** The appended block carries two assertions the plan's replacement
wording does not: the disjointness rationale (`#534 proves can_comparison_iso ASSUMING the
Preadditive enrichment; #547 must prove the same conclusion BEFORE any enrichment exists.
Neither can invoke the other.`) and `No edge in either direction.` The verdict record
(`doc/plan/books/qa/all-verdicts.json:804`) ends `Assert no dependency edge in either
direction.`, so that content is wanted. Deleting the block while applying only the short
swap would drop it. Option A folds it into the paragraph; Option B is the minimal swap for a
coordinator who would rather keep the block.

### Option A — RECOMMENDED (in-place fix, block removable without loss)

FIND (exact, occurs exactly **1×** in the live body):

```
axioms … feeding `bicartesian_preadditive` without assuming it"), so the two
issues together cover both routes and should share the resulting lemma rather
than proving it twice.
```

REPLACE (wrapped to ≤80 cols to match the surrounding paragraph, which runs 70–80):

```
axioms … feeding `bicartesian_preadditive` without assuming it"), so the two
issues together cover both routes under disjoint hypotheses; name the two
lemmas distinctly and cross-cite, rather than attempting to share one. #534
proves `can_comparison_iso` ASSUMING the `Preadditive` enrichment; #547 must
prove the same conclusion BEFORE any enrichment exists. Neither can invoke the
other. No edge in either direction.
```

### Option B — minimal literal swap (keeps the block)

FIND (exact, 1×):

```
issues together cover both routes and should share the resulting lemma rather
than proving it twice.
```

REPLACE:

```
issues together cover both routes under disjoint hypotheses; name the two
lemmas distinctly and cross-cite, rather than attempting to share one.
```

Under Option B the `### Scope increment (QA audit)` block must **stay** (it alone then carries
the disjointness rationale and the no-edge assertion), and #534 still half-repeats itself.

## 3. The appended correction block to REMOVE (Option A only) — EXACT text

Body lines 75–81, preceded by the blank line 74 and followed by blank line 82. Delete the
heading, the paragraph, and exactly **one** of the two surrounding blank lines so a single blank
line separates the Riehl paragraph from the `<!-- catalog: … -->` trailer. Byte-exact string to
delete (leading `\n` included, trailing `\n` included — verified to occur exactly **1×**):

```
### Scope increment (QA audit)

The clause instructing that #534 and #547 *should share the resulting lemma rather than proving
it twice* is superseded by: *cover both routes under disjoint hypotheses; name the two lemmas
distinctly and cross-cite, rather than attempting to share one.* #534 proves
`can_comparison_iso` ASSUMING the `Preadditive` enrichment; #547 must prove the same conclusion
BEFORE any enrichment exists. Neither can invoke the other. No edge in either direction.
```

This is character-identical to `remediation.json` item `[351]`'s `block` field, so it can be
matched programmatically from that record rather than retyped.

## 4. Simulated result

Option A find/replace + block deletion applied to the live body was simulated; both patterns
matched exactly once, and the resulting tail is:

```
axioms … feeding `bicartesian_preadditive` without assuming it"), so the two
issues together cover both routes under disjoint hypotheses; name the two
lemmas distinctly and cross-cite, rather than attempting to share one. #534
proves `can_comparison_iso` ASSUMING the `Preadditive` enrichment; #547 must
prove the same conclusion BEFORE any enrichment exists. Neither can invoke the
other. No edge in either direction.

<!-- catalog: {"ids": ["maclane:VIII.2:thm2", "maclane:VIII.2:ex1", "awodey:3.2:prop11", "riehl:E.5:def-biproduct"], "deps": []} -->
```

The catalog trailer stays last and untouched (`"deps": []` — consistent with "no edge in either
direction"). Full proposed body written for inspection to
`/private/tmp/claude-501/-Users-johnw-src-category-theory-master/ccf260aa-13c7-4435-8156-ea832592c6d7/scratchpad/issue534-proposed.md`.
No GitHub write was performed; this agent is read-only.

## 5. Adjacent defect noticed, NOT part of decision 6.4

#547's own `### Scope increment (QA audit)` block was appended as *instructions to an editor*
rather than as an applied edit. Its live body reads:

```
### Scope increment (QA audit)

On its own physical line in `## Dependencies`:

> 
- Related (NOT blocking): #534 proves `IsIsomorphism (can_comparison x y)` in a `Preadditive` category with the biproduct (`can_comparison_iso`, `Structure/Semiadditive.v`). …
```

— note the literal `On its own physical line in `## Dependencies`:` and the stray `> ` line.
The cross-cite Option A promises therefore exists on #547 only in imperative form, in the wrong
section. Flagged for the coordinator; not remediated here.
