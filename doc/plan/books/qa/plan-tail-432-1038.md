EqProp` is the hard one and must not be assumed cheap:** the hom-setoid is `cospan_equiv` (`Construction/Cospan/Category.v:78-81`), a Σ-type carrying the apex isomorphism as DATA, and the legs do not determine it, so `heq_elim : heq f g → f ≈ g` is a Prop-to-Type elimination. Over FinSet it is discharged by a decidable finite search over `Fin.t a -> Fin.t b` (the enumeration codecs of `Instance/FinSet/Closed.v` are the donor). If that proves disproportionate, record in the header that Work item 3 routes around `Presented_factor` instead — e.g. via `InterpF`/`interp_unique` on the free prop plus an explicit quotient argument — and say which route was taken.` DoD box: `- [ ] \`CospanCat FinSet\` either carries \`PROP\` + \`HomEqProp\` + \`ObjDecEq\` so \`Presented_factor\` applies, or the header records the route taken around it.`

| Issue | Edit (continued) |
|---|---|
| **#920** | Replace the escape-clause DoD line `- [ ] All six bulleted claims are stated; each is either proved **or explicitly recorded as blocked on a named prerequisite issue**, with nothing left as prose` with `- [ ] All six bulleted claims are stated and proved — the groupoid clause against #248's \`IsGroupoid\` and the local-smallness clause against #253's predicate, both of which this issue's dependencies guarantee are available — with nothing left as prose`. Keep both edges. **Also** trim `blocked ones included` from the Reviewer-checklist phrase `confirm each of the six claims appears explicitly, blocked ones included`, or it re-licenses what the hardened DoD forbids. |
| **#987** *(UNVERIFIED, LOW)* | Append to the §6.1 Example 6.1.7 block: `- [ ] (from Riehl §6.1) The header records Riehl's caveat that the same discussion covers group-sets, group-spaces, based group-spaces and group-objects in any category, but that in a target with few limits or colimits the two adjoints need not exist — stated alongside the (co)completeness hypothesis this issue already carries.` |
| **#991** | Reword the appended §6.5 checkbox (do NOT add the #1034 edge): `- [ ] (from Riehl §6.5) Exhibit the nerve/homotopy-category adjunction as an instance of #1034's presheaf-adjunction theorem — … — OR, if #1034 has not landed, record in the header that the instance-of-#1034 reading is deferred to #1034`, plus `- Related (NOT blocking): **#1034** owns the presheaf-adjunction theorem the appended §6.5 checkbox asks this adjunction to instantiate; applying it at \`Cat\` presupposes the \`Cocomplete Cat\` proved here, so no dependency edge is asserted.` |
| **#1014** | Add immediately after the existing cocompleteness box: `- [ ] The descent step cites #434 rather than re-deriving it; whichever of #1014/#1016 lands first owns the proof.` Contrast precedent verified: **#1011**'s overlap block does carry checkboxes. |

---

### GROUP E — Fix wrong or stale citations, clause maps and factual claims

**E1. Two issues order FALSE theorems — these are the highest-priority E items.**

| Issue | Change |
|---|---|
| **#916** | Work item 2 `"Prove that \`Nat(Id, (−)₊)\` has exactly one element, the \"inject\" transformation \`x ↦ Some x\`…"` → `2. Prove that \`Nat(Id, (−)₊)\` has exactly TWO elements — the injection \`x ↦ Some x\` and the constant family \`x ↦ None\` — and prove both are natural. The count comes from probing with maps out of a singleton (\`Instance/Sets.v:248\` \`Sets_Terminal\`): naturality forces \`α_X (x) = fmap f (α_1 ∗)\` for \`x : 1 ⟶ X\`, and \`(1)₊\` has exactly two elements.` Work item 4 `(a singleton setoid, resp. the empty setoid)` → `(a two-element setoid, resp. the empty setoid)`. DoD → `- [ ] \`Nat(Id, (−)₊)\` is proved to be a two-element setoid, exhibiting both elements (the injection and the constant-\`None\` family)`. Verification: rename `nat_id_to_addpoint_unique` → `nat_id_to_addpoint_two`. **Leave the `Nat((−)₊, Id)` half untouched.** Riehl asserts no count. (Yoneda: `Nat(Id,F) ≅ F(1) = option(1)`.) |
| **#929** | **The stated conclusion is false as written and the DoD is unprovable.** Work item 4: replace the clause after the enumeration with `…and conclude through the Yoneda embedding (\`Functor/Hom/Yoneda.v:253\` \`Covariant_Yoneda_Embedding\`) that \`Nat(ob, mor) ≅ Cat(_2, _1)\` has exactly ONE element (the identity-assigning transformation) and \`Nat(mor, ob) ≅ Cat(_1, _2)\` exactly TWO (domain and codomain) — three natural transformations between \`ob\` and \`mor\` in total, which is Riehl's count. State each as an isomorphism of setoids with a one-, resp. two-element setoid, so the counts are theorems rather than existence facts plus prose.` DoD → `- [ ] Exactly one natural transformation \`ob ⟹ mor\` and exactly two \`mor ⟹ ob\` are proved, as isomorphisms with a one- and a two-element setoid derived through the Yoneda embedding (three in total between \`ob\` and \`mor\`, which is Riehl's statement)`. Verification: `Print Assumptions nat_ob_mor_three.` → `Print Assumptions nat_ob_mor_one.` and `Print Assumptions nat_mor_ob_two.` Update the Reviewer-checklist phrase to name both counts. Verified: `Functor/Hom/Yoneda.v:253` is `Covariant_Yoneda_Embedding : ∀ A B : C, Copresheaves [Hom B,─] [Hom A,─] ≊ A ~> B`. |

**E2. `Structure/Group.v:112` → `:109` — one defect, three issues.** In **#617**, **#620** and **#635**, change the Current-state citation `Structure/Group.v:112` to `:109`. (`:112` is the field `groupobject_is_monoid : MonoidObject grp;`; `Class GroupObject (grp : C) := {` is at `:109`. Sister issue #612 already cites `:109`.) On **#620** additionally rewrite Work item 1: `"- Instantiate a group object in \`Cat\` using the in-tree \`GroupObject\` at \`Cat_Cartesian\`"` → `- Instantiate a group object in \`Cat\`: \`GroupObject\`'s context is \`@CartesianMonoidal C\` (\`Structure/Group.v:107\`), so first obtain \`@CartesianMonoidal Cat\` via \`Structure/Monoidal/Internal/Product.v:435\` \`CC_CartesianMonoidal\` from \`Cat_Cartesian\` (\`Instance/Cat/Cartesian.v:39\`) and \`Cat_Terminal\` (\`Instance/One.v:54\`), then instantiate \`GroupObject\` over it.` Add `Structure/Monoidal/Internal/Product.v` (`CC_CartesianMonoidal`) to In-tree donors. **CONFIRMED by compilation** — `Fail Check (fun g : Cat => @GroupObject Cat Cat_Cartesian g).` succeeds as a `Fail`; `Check (fun g : Cat => @GroupObject Cat Cat_CartesianMonoidal_probe g).` elaborates. `@CartesianMonoidal Cat` is the correct fully-applied form; `@CartesianMonoidal Cat _` is an "Illegal application".

**E3. Remaining citation / clause-map fixes.**

| Issue | Change | Verified? |
|---|---|---|
| **#435** | `printed pp. 251–253, PDF pp. 260–261` → `… PDF pp. 260–262` | **head-plan dup**; unverified LOW |
| **#452** | `Theory/WeaklyInitial.v:119` → `Adjunction/SAFT.v:119` | **head-plan dup**; CONFIRMED here |
| **#463** | `(printed p. 189, PDF p. 210; item \`riehl:5.2:example6\`)` → `(printed p. 189, PDF p. 209; …)`. Riehl offset is printed = PDF − 20; #465 and #471 cite the same example correctly at PDF 209 | unverified LOW |
| **#487** | Replace `The syntactic variety category \`Algs\` (\`Instance/Comp.v:268\`, over \`Type\`) exists but is entirely disconnected from any monad, \`Set^T\`, or comparison functor` with `\`Instance/Comp.v:151\` defines \`Algs\`, the category of algebras of a signature WITHOUT equations, and \`Instance/Comp.v:268\` defines the record \`Algebra S E\` (a type, not a category) — so the variety category \`⟨Ω,E⟩-Alg\` is nowhere formed (this is #440's deliverable), and nothing connects any of it to a monad, \`Set^T\`, or a comparison functor.` The dropped trailing parenthetical was ALSO wrong (the free-algebra universal property IS formalized: `Instance/Comp.v:108` `induced_hom`, `:116` `from_free_unique`) | CONFIRMED |
| **#491** | `(Strict.v:43, Compose.v:55, Product.v:28, Braided.v:43)` → `` (`Functor/Structure/Monoidal/Strict.v:43`, `.../Compose.v:55`, `.../Braided.v:43`, and `Structure/Monoidal/Product.v:28`) `` — `Functor/Structure/Monoidal/Product.v` does not exist | unverified LOW |
| **#497** | `Riehl, Epilogue §E.2, the statement of the Mac Lane–Kelly coherence theorem for` → `Riehl, *Category Theory in Context*, 2nd ed., Epilogue §E.2, …` | unverified LOW |
| **#520** | `Construction/ColouredPROP.v:108` → `:109` (`:108` blank; `:109` = `Class ColouredPROP : Type := {`). `Construction/PROP.v:68` is correct. **Also** the same one-word "2nd ed." fix as #497, on #520's second Riehl append (`Riehl, Epilogue §E.2, the corollary drawn from symmetric coherence:`) | unverified LOW |
| **#552** | `` (`Structure/Regular.v:25`, `:81`) `` → `` (`Structure/Regular.v:79`–`:81`, the `regular_stable` field) `` — `:25` is blank; the field runs `:78`–`:81` | unverified LOW |
| **#563** | `` (the only `κ`/`kappa` hits are Freyd `κ`-categories in `Structure/Premonoidal/Freyd.v` and a bound cone variable in `Structure/Limit/Weighted.v`) `` → `` (the `κ`/`kappa` hits are all unrelated: Freyd `κ`-categories in `Structure/Premonoidal/Freyd.v`, a bound cone variable in `Structure/Limit/Weighted.v`, the Funny-tensor-to-product comparison functor `κ = FunnyToProduct` in `Construction/Funny/Comparison.v:14` and `Construction/Funny/Closed.v:33`, and continuation variables in `Instance/Lambda/Eval.v`; no morphism anywhere has a colimit as source and a limit as target) `` | unverified LOW |
| **#568** | `PreservesColimit` (`Structure/Limit/Preservation.v:206`) → `:196` | unverified LOW |
| **#570** | `doc/plan/books/ledger.tsv` lines 790–791 `PARTIAL` → `ABSENT`; #570's Current state first word `Partial.` → `Absent.` Rule from `schemas.md`: `"**PARTIAL requires a PROVED statement covering part of the item. A never-instantiated class does not qualify.**"` All four cites verified (`Structure/Wedge.v:38,61`, `Theory/Dinatural.v:51`, `Structure/Cartesian/Closed.v:75`). **Confirm the row numbers before editing — J23.** | unverified LOW |
| **#590** | `Awodey, *Category Theory*, §9.6, Corollary 9.17 —` → `Awodey, *Category Theory* (2nd ed.), §9.6, Corollary 9.17 —` (+ the F4 blank-line fix) | unverified LOW |
| **#606** | `Structure/Monoidal/Braided/Proofs.v:507` → `:506` (`:507` blank; `:506` is ``Context `{S : @SymmetricMonoidal C}.``). `Structure/Monoidal/Braided.v:132` → `:131` (`:132` is the field `braid {x y} : x ⨂ y ~> y ⨂ x;`). Other citations exact — do not touch | unverified LOW |
| **#623** | Current-state parenthetical → `(\`Instance/Cat/Cartesian.v:39\` provides \`Cat_Cartesian\` and \`Instance/One.v:54\` provides \`Cat_Terminal\`, and the generic \`Structure/Monoidal/Cartesian.v:49\` \`Cartesian_Monoidal\` (over \`Structure/Monoidal/Internal/Product.v:54\` \`CC_Monoidal\`) turns those into \`@Monoidal Cat\` directly — the instance is simply never named, and \`Enriched\` is instantiated only at \`Sets\` and \`Two\`)` | CONFIRMED (probe compiled) |
| **#658** | `(2nd ed., Oxford Logic Guides 49)` → `(2nd ed., Oxford Logic Guides 52)` in the `## Source` line. **GATED ON J5** — the #679 REFUTED verdict shows "2nd ed. (OLG 52)" is itself campaign-wide wrong | unverified LOW |
| **#667** | Replace `There is no category of topological spaces in-tree, so the Top instance cannot yet be formed; \`Instance/Poset.v\` provides Pos but carries no ordered-group object.` with `There is no category of topological spaces in-tree, so the Top instance cannot yet be formed; and there is no category **Pos** either — \`Instance/Poset.v:116\` builds a *single* poset as a thin category (\`Definition Poset ... : Category := Proset P\`) and the \`[Pos]\` reference at \`:21\` is dangling — so the ordered-group case is blocked on #641, and additionally needs a \`@Cartesian\`/\`@CartesianMonoidal\` structure on **Pos** that no issue currently schedules; scope it as a follow-up rather than a required leg if that structure does not land.` (final clause is the verdict's addition: `Structure/Group.v:107` opens `Context \`{@CartesianMonoidal C}` before `:109 Class GroupObject`) | ADJUSTED |
| **#671** | In the `### Also covered by` §8.8 block, opening citation line → `` Awodey, *Category Theory* (2nd ed.), §8.8 (Topoi), Remark 8.18, printed page 211 (PDF page 220), item `awodey:8.8:remark18` (multi-part — leg 1/2, the membership criterion; the topos-level adjoint-triple leg is recorded against #387) ``. Leave the rest. (`ledger.tsv:1340/:1341` record the two legs.) **Out-of-scope note, recorded not proposed:** #387's copy of the same block cites `printed p. 210, PDF p. 219`, disagreeing with the inventory's 211/220 | CONFIRMED |
| **#679** | **REFUTED — no edit.** See §2a and J5 | — |
| **#688** | `Construction/PROP/Presentation.v:48` → `:180` (`:48` is header prose; `Definition PresentedCat : Category :=` is at `:180`). **Leave `Construction/PROP/Universal.v:603`** — verified as `Theorem interp_unique : ∀ m n (t : Term S m n),` | CONFIRMED |
| **#690** | Same: `Construction/PROP/Presentation.v:48` → `:180`. **Leave `Instance/Comp.v:240`** — verified as `Record EqSignature (S : OpSignature) := {` | CONFIRMED |
| **#698** | Current state `There is no category of monoids over \`Sets\` and no category of groups.` → `There is no *named* category of monoids over \`Sets\` — \`Theory/Algebra/Monoid/Hom.v:83\`'s \`Mon\` instantiates at \`Sets\` via \`Instance/Sets.v:283\`'s \`Sets_Product_Monoidal\`, but nothing in-tree names that instance — and there is no category of groups.` | CONFIRMED |
| **#722** | `ledger.tsv` line 1323, note column: `MULTI-PART leg 2/2: **the higher-order-logic/quantifier reading** -> #722` → `MULTI-PART leg 2/2: every slice of a topos is again a topos -> #722`. The stray phrase belongs to `awodey:8.8:remark18` (`8-duplicates.json:85`), ledgered at 1340 (#671) and 1341 (#387). **Line 1339 (#405) is correct** — optionally sharpen `the topos-properties list` to `every topos has finite colimits` | CONFIRMED |
| **#725** | Third Current-state bullet → `Lower sets, down-sets and downward-closed subsets appear only as header prose — \`Construction/Slice.v:81\` ('For a poset P, the slice P/p is the principal down-set of p'); there is no definition, lemma or instance. (\`Theory/Sheaf.v:117\`, \`Instance/Fun.v:63\` and \`Construction/Day.v:111\` are the three cocompletion-prose sites recorded in the next bullet, not lower-set sites.)` | unverified LOW; the substantive claim is independently true |
| **#740** | Replace all six occurrences of `Exercise 10.6.7` with `§10.6 Exercise 7` (Source, Background, Current state, Work to be done, and the two DoD checkboxes). The issue's own id is `awodey:10:ex7`; pagemap's NUMBERING-CONVENTIONS fixes the scheme | unverified LOW |
| **#772** | Opening line → `Riehl, *Category Theory in Context*, 2nd ed., Epilogue §E.2, the roster of examples of symmetric monoidal categories,`. Clause map `(The roster's other clauses are recorded on #490,\n#388, and the new Riehl E.2 chain-complex issue.)` → `(The roster's other clauses: clause (a), finite products, is already in force in-tree as \`CC_SymmetricMonoidal\` and needs no issue; clause (b) is on #490, clause (c) on #388, clause (d) on #1011.)` | unverified LOW |
| **#779** | `with the arrow table at :395` → `:396` (`396:Program Definition ThreeArrows : Metacategory := {|`; `:395` blank; `:413` correct) | unverified LOW |
| **#784** | Donor entry `Instance/Two.v:38,122,134` → `Instance/Two.v:122,134`; if the top-element lemma was intended, add `Construction/Enriched/Two.v:38` (`two_from_top`) | unverified LOW |
| **#785** | Replace **both** occurrences of `Instance/Two.v:38` with `Construction/Enriched/Two.v:38`. Current-state sentence → `…takes reflexivity from \`eid\` through \`Construction/Enriched/Two.v:38\` (\`two_from_top : TwoHom TwoY z → z = TwoY\`)…`; in In-tree donors merge `38` into the existing `Construction/Enriched/Two.v:60,65,71,104,113,131,165` (→ `:38,60,…`) and delete the standalone `Instance/Two.v:38` entry (that line is essay prose) | CONFIRMED |
| **#786** | Both occurrences of `Instance/Proset.v:20` → `:19` (Current state + DoD). #803 already cites `:19` | unverified LOW |
| **#827** | In the In-tree-donors paragraph replace `` `Structure/Monoidal/Internal/Cocartesian.v`-style cocartesian-to-monoidal assembly `` with `` `Structure/Monoidal/Internal/Product.v:54` (`CC_Monoidal`) composed with `Construction/Opposite/Monoidal.v:92` (`Monoidal_op`) for the cocartesian-to-monoidal assembly, with `Instance/FinSet/Lawvere.v:41-42` as the in-tree precedent for exactly this pairing ``. **Do not create `Structure/Monoidal/Internal/Cocartesian.v`** — that directory contains only `Product.v` | CONFIRMED |
| **#832** | `` (`Construction/PROP/Tietze.v:265`, `:288`) `` → `` (…, `:301`) ``. `:288` is `RelabelFunctor_Braided`; `RelabelFunctor_Symmetric` is at `:301`. The In-tree-donors line cites only `:265` — no change | unverified LOW |
| **#851** | **Label correction, NOT a demotion.** Replace `` Depends on: #520 (coherence for symmetric monoidal categories, and\nproduct/coproduct as symmetric tensor — the cartesian-tensor vocabulary). `` with `` Depends on: #520 — the free-standing *symmetric strict monoidal* class (#520's Seven Sketches §4.4.3 checkbox), which is the notion Example 5.71's conclusion names; the tree has no such bundling outside `Construction/PROP.v:68` and `Construction/ColouredPROP.v:109`, both of which pin their objects. NOT consumed from #520: the symmetric coherence theorem, and the cocartesian symmetric tensor — the *cartesian* symmetric monoidal structure this issue needs is already in tree as `CC_SymmetricMonoidal` (`Structure/Monoidal/Internal/Product.v:314`). `` **Keep trailer `["#771","#786","#520","#500"]` and native 851←520** | CONFIRMED |
| **#874** | Replace `` `vertex_obj` occurs 27 times (cone apices, in `Theory/Equivalence/Limit.v`, `Instance/Cones*.v`, `Structure/Limit/*.v`, `Construction/Comma/Limit.v`) `` with `` `vertex_obj` occurs 104 times across 19 files (cone apices, throughout `Structure/Cone*.v`, `Structure/Limit/*.v`, `Instance/Cones*.v`, `Adjunction/Continuity.v`, `Structure/Pullback/Limit.v`, `Structure/UniversalProperty/Limit.v`, `Construction/Comma/Limit.v` and `Theory/Equivalence/Limit.v`) ``. The note's other two counts are correct | unverified LOW |
| **#883** | **ADJUSTED — do not paste the auditor's text, it asserts a falsehood.** Replace `"(clause (iv) only — the colimits clause of that remark is recorded on #405)"` with `(clause (iv) only). Full clause map for the five-property remark, whose authoritative record is #405's body: (i) all limits → #405, which carries the added checkbox 'An elementary topos is finitely complete: derive equalizers from products and pullbacks' — this is NOT already a theorem, since \`Structure/Topos.v:112\`'s \`ElementaryTopos\` carries terminal + products + pullbacks only as DATA and the file header discloses the pullback/equalizer reduction as unformalized; (ii) all colimits → #405; (iii) cartesian closure and (v) the subobject classifier → already axioms of the class (\`topos_closed\`, \`topos_classifier\`), so no issue is needed; (iv) epi-mono factorization → this issue. The remark's corollary 'since Set is a topos, all five hold for Set' is recorded on #404 and is blocked by the absence of any \`ElementaryTopos Sets\` instance.` | ADJUSTED |
| **#888** | `` `Construction/Arrow.v:110`'s `Arrow := Id[C] |> Id[C]` `` → `` `Construction/Arrow.v:110`'s `Arrow {C : Category} : Category := (Id[C] ↓ Id[C])` `` — the comma notation is `↓`, not `\|>` | unverified LOW |
| **#918** | `§2.2 Corollary 2.2.8, printed p. 65 (PDF p. 85)` → `printed p. 64 (PDF p. 84)`. (`riehl/inventory/inventory-2.json` records `riehl:2.2:cor8` at book_page 64 / pdf_pages [84]; 65/85 is `riehl:2.2:example9`, as #929 cites it) | CONFIRMED |
| **#930** | `returns **exactly two hits**, both prose in header comments` → `returns four hits, none of them a definition of the notion: two prose comments (\`Theory/Sheaf.v:80\`, \`Construction/Localization.v:101\`) and the section delimiters \`Section SubFunctor.\` / \`End SubFunctor.\` in \`Theory/Subobject/Functor.v\`, which house the SUBOBJECT functor \`Sub : C^op ⟶ Sets\` — a different construction whose name the new \`Subfunctor\` class must not be confused with.` | unverified LOW |
| **#931** | Replace the non-reproducing command with `\`rg -in 'PreservesMonos' -g '*.v'\` → 0 hits; \`rg -n 'Monic' Functor/\` → 0 hits, so there is no \`PreservesMonos\` predicate and \`Monic\` is never applied to a functor's action. The only nearby assertions are \`Theory/Adjunction.v:311\` \`adj_monic\` (a *faithful left adjoint* transposes a mono) and the passing remark at \`Construction/Reflective/Idempotent.v:137\`; neither concerns representable or hom-functors.` **Write the searches with `-g '*.v'`** — an unscoped `rg` now hits the untracked `doc/plan` artifacts. `adj_monic` is at `:311`, not `:310` | CONFIRMED |
| **#945** | `\`rg 'Slice_Proj\|slice projection'\` returns 0 hits` → `\`rg 'Slice_Proj'\` returns 0 hits and the only \`slice projection\` hit is prose (\`Instance/Coq/Comonad/Env.v:79\`)` | unverified LOW |
| **#955** | `` `toggle` (`:492`) `` → `` `toggle` (`:493`) ``. The other three cites (`:449`, `:479`, `:485`) verify exactly | unverified LOW |
| **#959** | `Theory/Equivalence/Limit.v:353` → `:350` (`Hlegs` declared at `:350`; `:353` is a comment). The companion cite `:401` for `ff_reflects_limit` is correct | unverified LOW |
| **#976** | Source: `§4.4 "Adjoint functors and duality":` → `§4.4 "Contravariant and multivariable adjoint functors":`. Nothing else changes — every page number (152/172, 155/175, 172/192) re-verified against `inventory-4.json` | CONFIRMED |
| **#978** | `§4.5 "Contravariant adjunctions" / locally cartesian closed material:` → `§4.5 "Locally cartesian closed categories":`. Nothing else in the paragraph changes (all three page numbers verify) | CONFIRMED |
| **#1005** | `(printed pp. 206–207, PDF pp. 226–228)` → `(printed pp. 206–208, PDF pp. 226–228)` **if** the lemma runs onto printed p. 208 (consistent with #1006/#1007 putting Exercises 5.5.v–vii on printed 208–209), otherwise `(printed pp. 206–207, PDF pp. 226–227)` | unverified LOW |
| **#1016** | `This obligation appears on **both #1014 and #1014**,` → `both #1014 and #1016` (the mirror block on #1014 already reads correctly) | CONFIRMED |

---

### GROUP F — Contract / format

**F1. Body must END with the trailer** — move the block above the trailer; collapse the stray multi-newline gap.

- **#490** — move the whole `### Clause (a) of Riehl's E.2 roster — the named instances (added on audit)` section (heading, three-paragraph rationale, the `- [ ]` checkbox, and the closing "This mirrors the treatment the sibling leg #388 …" sentence) to sit immediately BEFORE the `<!-- catalog: … -->` line; collapse the double blank line. ids/deps unchanged. *(Only issue in its 35-issue batch containing `\n\n\n`.)*
- **#526** *(unverified LOW)* — same move for the `### Same-file peer (NOT a dependency)` block, above `<!-- catalog: {"ids":["maclane:VII.9:def-pointed"],"deps":["#259"]} -->`; collapse the triple newline.
- **#559** — move the `### Dropped clauses from Riehl §3.8 Example 3.8.8 (added on audit)` block (heading through `**Related (NOT blocking): #957** … before #957's item 3 lands.`) verbatim to before the trailer; collapse three blank lines to one. **1210 characters currently sit after the trailer, including two `- [ ]` DoD increments.** Trailer arrays unchanged.
- **#589** *(unverified LOW)* — move the block from `### The reindexing functor this issue's functoriality step needs (added on audit)` through `…cross-reference the appends already imply.` to immediately before `<!-- catalog: {"ids": ["maclane:X.3:thm1", "maclane:X.3:def2", "riehl:6.2:thm1"], "deps": []} -->`, one blank line apart. **Also** prefix the note's final line with `- ` so it reads `- Related (NOT blocking): #1021 — no dependency edge is asserted here; the link is a cross-reference the appends already imply.`
- **#657** — move the `### Same-file peer (NOT a dependency)` block (through `**They must not be worked in the same parallel wave.**`) to before the trailer; collapse the double blank line. Block content matches `serialize-groups.json` (`Instance/Sets/Choice.v`, [657, 981], creator 657).
- **#809** — cut the block from `### CONSUME, DO NOT RE-CREATE the category of elements (added on audit)` through `…it does not remove a duplicated instruction.` and re-insert immediately BEFORE `<!-- catalog: {"ids": ["7sketches:3.5.3:remark3.100", "riehl:2.4:def13"], "deps": ["#345", "#337"]} -->`, one blank line each side; collapse the three consecutive blank lines. **Keep the block's substance** — `serialize-groups.json` lists `Construction/Elements.v` for `[345, 809, 1021]` with creator 345. (#809 is the only body in its 35-issue batch with content after the trailer.)
- **#918** — move the trailer to the very end, after the `### Also covered by` block; collapse the triple newline before that block. **Trailer text depends on the Group A demotion order:** write `<!-- catalog: {"ids":["riehl:1.5:example11","riehl:2.2:cor8"],"deps":["#712"]} -->` if the #231 demotion is applied (it should be); use `"deps":["#712","#231"]` only if that demotion is rejected. Applying the finding's literal text without this care silently re-adds the #231 edge.
- **#972** — move the whole `### SHARED CONSTRUCTION with #1028 (added on audit)` block (heading through `` Recorded in `doc/plan/books/graph/serialize-groups.json`. ``) to immediately before `<!-- catalog: {"ids":["riehl:4.1:example15"],"deps":["#907","#248","#299"]} -->`; collapse the two stray double blank lines. **Preserve the block verbatim** — the `- [ ] Build the zig-zag localization **once**.` checkbox and the `Related (NOT blocking): **#1028**.` line are both correct. *(This is where `file_chapter.py` itself inserts appended blocks; while the trailer is not last, `file_chapter.py:224` falls to the `else` at `:231` and appends **without updating `ids`**.)*
- **#1011 / #1014 / #1016** also carry post-trailer "(added on audit)" content, rated LOW/no-action by the verdict — **J24**.

**F2. Missing trailer ids.**
- **#918** → add `"riehl:2.2:cor8"` (`ledger.tsv` has two rows for #918; `riehl/issue-map.json` maps both ids to 918).
- **#1006** → `<!-- catalog: {"ids":["riehl:5.5:exv","riehl:5.5:exvi","riehl:5.5:example7"],"deps":["#320","#366","#484"]} -->`. `ledger.tsv:2782` reads `riehl:5.5:example7\tABSENT\t#1006\t10\tpart: clause (iii): ev : C^J -> C^{ob J} is monadic for C with coproducts and J small`; `issue-map.json` has `"riehl:5.5:example7@1006": 1006`; the body already homes it. Siblings #465, #474, #993 all carry the id; #1006 is the only ledger-rowed coverer omitting it.

**F3. Trailer `deps` out of sync** (body and native already agree).
- **#732** → `"deps": ["#387", "#730"]` → `["#387", "#730", "#731"]`. Native is `[387,731,730]`; #731 genuinely supplies `Slice_of_Slice`/`Slice_Over_Terminal`.
- **#807** → `"deps":["#705","7sketches:3.2.1:example3.13"]` → `["#705","7sketches:3.2.1:example3.13","#742"]`. Body and native both carry 742; the only three-surface disagreement in its batch.
- **#1021** → `"deps":["#345",]` → `"deps":["#345"]` (delete the trailing comma). **It is the only trailer in its batch that `json.loads` rejects**: *"Illegal trailing comma before end of array: line 1 column 72 (char 71)"*.
- **#649** — see Group B (`["#296"]` → `["#296","#648"]`).
- All Group A/B trailer edits, listed inline there.
- **#953 + #956/#963/#964/#968 normalisation — BLOCKED on the deps-array convention (§5).** Concrete resolutions, from `riehl/issue-map.json`: `#953 → ["#259","#458","#951"]`; `#956 → ["#220","#954"]`; `#963 → ["#259","#220","#954"]`; `#964 → ["#259","#951"]`; `#968 → ["#563","#954"]` (both item ids resolve to 954, so the array de-duplicates). `ids` arrays and body text untouched either way. *(unverified LOW)*

**F4. Blank-line hygiene.**
- Stray double blank after `### Also covered by`: **#590**; **#637** (both occurrences, `### Also covered by\n\n\n` → `\n\n`) *(both unverified LOW)*. The 11 occurrences across #432/#435/#436/#437/#440/#441/#443/#444/#453 are **exactly head-plan F4 — no new action.**
- **#871** *(unverified LOW)* — collapse the double blank line after `## Dependencies`; insert a blank line between `` > `doc/plan/books/graph/serialize-groups.json`; do NOT convert it into blocked-by edges. `` and the `<!-- catalog: … -->` trailer.
- **#921** *(unverified LOW)* — collapse the double blank line after `## Dependencies` (see also F8f).

**F5. One dependency per physical line; hedges split off.**
- **#482** *(unverified LOW)* — split the one-line section into ``Depends on: #393 (`maclane:IV.7:def-map-of-adjunctions`) — the general maps-of-adjunctions notion, of which this comparison is the identity-on-`X` specialization, supplying the derived `M ∘ ε' ≈ ε ∘ M`.`` + a new line with no issue numbers: `` Otherwise uses the in-tree Beck engine (`CreatesUSplitCoequalizers`/`Beck_Inverse`) and `EM_Comparison`. ``
- **#553** *(unverified LOW)* — split into ``Depends on: #545 (`maclane:VIII.3:def4`) (short exact sequences — the objects of `Ses A`).`` + `- Related (NOT blocking): the morphism-of-SES notion is reused by the short five lemma (#554) and the snake lemma (#555).`
- **#585** — replace the whole `## Dependencies` body with:
  ```
  Depends on: #334
  - Related (NOT blocking): #436 (Freyd's adjoint functor theorem — the stronger-hypothesis route, already filed). RAPL/LAPC are present in-tree (`Adjunction/Continuity.v`).
  - Note on #334: it creates `Structure/Limit/Initial.v` and supplies `IsALimit F (F initial_obj)`, which discharges criterion (ii) in the forward half of Theorem 2 (the adjunction alone yields only an initial object of `x ↓ G`).
  ```
  Trailer (`"deps": ["#334"]`) and native relation unchanged.
- **#627** *(unverified LOW)* — replace the two lines ``Depends on: #626 (`maclane:XII.4:def3`)`` and ``Depends on: #626 (`maclane:XII.4:def4`)`` with the single line ``Depends on: #626 (`maclane:XII.4:def3`, `maclane:XII.4:def4`)``. **Leave the trailer** (resolver de-duplicates; `blocked_by` = 626,283).
- **#648** — single line → two: `Depends on: #296 (MacLane II.7 — the free monoid and its universal property).` / `- Related (NOT blocking): the group/ring/vector-space cases of Example 2.3 additionally require categories the library does not yet have.`
- **#649**, **#652** — split, per Groups B and A.
- **#719** — split the first Dependencies line at the semicolon into `Depends on: #239 (the currying adjunction \`(− × B) ⊣ (−)^B\` itself — the two functors and the \`Adjunction\` record, in \`Structure/Cartesian/Closed/Adjunction.v\`)` and `Depends on: #682 (the UNIT of that adjunction and the global-points bijection only — #682 explicitly defers the adjunction assembly to #239)`. Both edges genuine and already harvested; no trailer or native change.
- **#780** — **corrupted section, highest-priority format fix.** The raw body has the #745 line spliced into the MIDDLE of the #382 line, stranding `adjunction — the inclusion-ordered carrier).` on its own physical line. Replace with exactly:
  ```
  Depends on: #382 (the powerset preorder and the direct-image/inverse-image adjunction — the inclusion-ordered carrier).
  Depends on: #745 (the interior comonad and closure monad on the subsets of a space — it lands in the same `Instance/Powerset.v` and must be in place before this issue extends that file).
  ```
  and move the hedge to its own line after all Depends-on lines: `` - Note: #382, #745 and this issue all write `Instance/Powerset.v`; the chain 382 → 745 → 780 serialises them, so extend the file rather than re-creating it. `` **No trailer/native change** (all three surfaces agree; `blocked_by(745)` = 727,463,382 and `blocked_by(780)` = 771,799,382,389,685,745).
- **#883** — replace `Depends on: #405 — the sibling clause of the same topos-properties remark (colimits) is that issue's obligation; this issue deliberately does not restate it.` with two lines: `` Depends on: #405 — `Structure/Regular.v:66`'s `Class Regular` requires `regular_coeq` (coequalizers of kernel pairs), so `Topos_Regular : ElementaryTopos C → Regular C` cannot be proved until finite colimits in a topos exist. `` / `- Related (NOT blocking): #405 also owns the colimits clause of the same topos-properties remark; this issue deliberately does not restate that clause.` No trailer/native change.
- **#921** *(unverified LOW)* — replace the blockquote `> **Related, NOT a dependency (corrected on audit):** #225 (the simplicial category Delta) is referenced for the header CONTRAST only. …` with the standard single line placed before the Depends-on line: `- Related (NOT blocking): #225 (the simplicial category Delta) — referenced for the header CONTRAST only; previously mis-filed as a \`Depends on:\` line, which made the resolver assert a blocker the prose denied.` (`Related (NOT blocking)` is the exact string `REVERSE_CUE` recognises; the blockquote form is invisible to it.) **J25 also applies to #921.**
- **#944** — replace `- Depends on: #730 (the dependent product as the right adjoint to base change — related reading, distinguished in the header)` with `- Depends on: #730 (the dependent product as the right adjoint to base change — supplies \`Σ_!\` and \`Π_!\`, which the appended Riehl §4.5 item identifies with this issue's dependent sum and product along \`X → 1\`)`. Amend the contradicted DoD line `- [ ] The header distinguishes this dependent product from the base-change right adjoint of #730` → `- [ ] The header relates this dependent product to the base-change right adjoint of #730, and the appended §4.5 identification is proved (not merely a distinction)`. Append to Work item 5: `…and prove the identification the appended §4.5 item requires, rather than only recording the distinction.`
- **#1017** *(unverified LOW)* — replace the body `None in catalog. Related: #590 supplies the global adjoint form of the same bijection, and this issue's local form should be stated so that #590's assembly consumes it.` with two lines: `None blocking.` / `- Related (NOT blocking): **#590** supplies the GLOBAL adjoint form of the same bijection. The arrow runs the other way — this issue's local form should be stated so that #590's assembly consumes it. No dependency edge is asserted.` **Merge with the Group C #1017 edit**, which appends a second Related line for #347.
- **#437** — replace the hedged `Depends on: #348 …` line (**match on its text; the verdict places it at body line 69, not 71 — edit by text match, not line number**) with exactly: ``Depends on: #348 (`maclane:IV.1:cor2`) — owns and creates the new module `Adjunction/Representability.v`, which this issue extends; consume its `AUniversalArrow` ⟷ `UniversalArrow` packaging lemma rather than re-deriving it.`` Keep native `blocked_by` 348 and trailer `"#348"`. **Do NOT add the serialize-groups entry — see §5.**

**F6. Bare / unresolvable references → issue numbers** (no edge implied unless stated).
- **#433**, **#446**, **#458** — **head-plan F6, no delta** except: on **#458**, if the optional Related line is added, write the real id — ``#416 (`maclane:V.2:thm1`)`` — not the head plan's ellipsis `maclane:V.2:...`.
- **#437**: `the §V.4 issue` → `#428`. **#453**: both `the §V.6 comma-creation issue` → `#438`. **#455**: all three `the §V.1 CompHaus issue` → `#413`. **#459**: `the §V.9 Top-cocompleteness issue` → `#458`. *(head-plan dups)*
- **#456**: `the §V.5 non-existence issue` and `the §V.5 non-existence\nidiom` → `#432's obstruction lemma`; add `` - Related (NOT blocking): #432 (`maclane:V.5:ex1`) — packages the adjoint-obstruction lemma; `Adjunction/Continuity.v:223` already supplies the positive direction, so the contrapositive can be taken locally if #432 has not landed. `` **No `blocked_by`.** *(head-plan dup)*
- **#465**: `(the Riehl §5.6 Theorem 5.6.5 issue)` → `(#1008)`.
- **#487**: `(see the Riehl §5.5 lattices issue)` → `(#1003)`.
- **#546**: `(new Riehl E.4 definition issue)` → `(#1013)`.
- **#561** *(unverified LOW)*: `Depends on: #440` → ``Depends on: #440 (`maclane:V.6:construction-variety`) (the variety category `Alg_τ` — the general algebraic setting of `maclane:IX.1:remark2`, of which `Grp` is an instance).`` No trailer or native change; the edge is real, only its label was missing.
- **#590** *(unverified LOW)*: append to the Seven Sketches checkbox `— that witness is #407 (completeness of \`Sets\` by cone sets), which rests on #254 (indexed products/coproducts in \`Sets\`).` and add on its own line in Dependencies: `` - Related (NOT blocking): #407, #254 (the `Complete Sets` witness the Seven Sketches clause waits on; the Mac Lane statement itself is parametric in a complete `A`, so no edge is asserted). `` **No edge.**
- **#591**: `That counterexample is filed as its own issue in the Riehl Chapter 6 batch.` → `That counterexample is filed as **#1019**.`; `(recorded against the group-theoretic induction issue) and Exercise 6.1.iv (in the counterexample issue)` → `(recorded against **#987**) and Exercise 6.1.iv (on **#1019**)`. Add as the last line of Dependencies: `- Related (NOT blocking): #1019, #987. No edge is asserted — #1019 already depends on this issue, so the reverse would create a cycle.` (`blocked_by(1019) = [591,599,589]` verified.)
- **#611** *(unverified LOW)*: `see the §XI.3 strictification issue` → `see #609, which owns the primary (free-monoid-on-objects) route`; `the main strictification result of §XI.3` → `… (#609)`; append after ``Depends on: #610 (`maclane:XI.3:ex1`)`` the line `- Related (NOT blocking): #609 — same theorem statement by a deliberately independent route; do not merge.` Trailer deps unchanged.
- **#756** *(unverified LOW)*: `they are filed as their own issue (Seven Sketches 6.2.1, "Deciding initiality in small concrete categories")` → `they are filed as #860 (Seven Sketches 6.2.1, "Deciding initiality in small concrete categories"), which depends on this issue`. (`filed-issues.tsv` and the ledger split both confirm #860, whose trailer already declares `deps ["#756"]`.)
- **#890** *(unverified LOW)*: add on its own line in Dependencies, after the #268 line and before the first `### Also covered by`: `` - Related (NOT blocking): #416 (MacLane V.2, limits from products and equalizers) — the textbook route from the colimit-preservation form to the equalizer form; `Structure/Equalizer/Fork.v` already suffices in-tree, so this issue does not wait on it. `` Do not touch the trailer or native relations.
- **#984**: `the Riehl §4.1 chain-of-adjoints issue` resolves to **#970**, but the ordinal family is **#224's** — handled in Group C; do not write `#970` as the builder.
- **#998**: `the double power set monad of the continuation-monad issue for §5.1` → `the double power set monad of #994`.
- **#1025**: Work item 2 opening `2. Over #935's vector spaces:` → `` 2. Over #258's `Vect_k` and #305's free ⊣ forgetful adjunction: ``.

**F7. Verification placeholders and missing/unrunnable targets.**
- **#533** *(unverified LOW)*: `` `coqc -R . Category <path>` compiles standalone. `` → `` `coqc -R . Category Structure/Preadditive/ZeroObject.v` compiles standalone (or `coqc -R . Category Structure/Preadditive.v` if the results are added in place). ``
- **#538** *(unverified LOW)*: same placeholder → `` `coqc -R . Category Instance/AbGroup/InfiniteBiproduct.v` compiles standalone. ``
- **#544** *(unverified LOW)*: same placeholder → `` `coqc -R . Category Structure/Abelian/Image.v` compiles standalone (or `coqc -R . Category Structure/Abelian.v` if the coimage is added in place). ``
- **#602** *(unverified LOW)*: `` - `coqc -R . Category` on the density-coend file compiles from a clean tree. `` → `` - `coqc -R . Category Structure/Coend/Density.v` compiles from a clean tree. `` and tighten the Work sentence to `` Suggested home: `Structure/Coend/Density.v` (new; alternatively fold into `Theory/Kan/Density.v` alongside the §X.6 density notion built by #600). ``
- **#909** — insert as the first line of `## Work to be done`, immediately after the paragraph beginning `"This issue is the *categorical spine*"`: `` Suggested module: `Theory/Topology/Brouwer.v`. `` (The Verification block already names that path, which appears nowhere else in the body; every batch sibling #905–#914 carries a module line.)

**F8. Section-internal defects.**
- **F8a. #704 — `## Dependencies` physically mangled.** Current block, verbatim:
  ```
  Depends on: #311 (MacLane III.1: A universal element for the contravariant
  Depends on: #227 — creates `Instance/Sets/Powerset.v`; coordinate the module layout so the covariant and contravariant halves coexist.
  power-set functor)
  Depends on: #428 (MacLane V.4: Hom-functors are continuous) — the book's stated
  route for Exercise 1
  ```
  Replace the whole block with:
  ```
  ## Dependencies

  None.

  - Related (NOT blocking): #227 (MacLane I.3: the covariant power-set functor) — designated `creator` of `Instance/Sets/Powerset.v` in `doc/plan/books/graph/serialize-groups.json` (claimants 227, 466, 704, 750, 871, 1005). This issue's Work item 1 builds the same `≈`-closed-predicate carrier, so land whichever comes first and have the other reuse it; must not share a parallel wave.
  ```
  plus `` - Related (NOT blocking): #311 — the same functor at `FinSet`, where the universe obstruction does not bite; nothing here consumes it. `` and `` - Related (NOT blocking): #428 (Hom-functors are continuous) — the book's own route to Exercise 1; this issue takes the `exp_coprod` route instead and only records the alternative in the header. `` Trailer → `"deps": []`, **`ids` preserved verbatim as all six**: `["awodey:7.5:construction-sets-double-dual", "awodey:7:ex9", "awodey:7:ex1", "awodey:9:ex5", "riehl:1.3:example7", "riehl:4.4:exiii"]`. Run the three `blocked_by` deletions (311, 428, 227). *(unverified LOW, additionally:)* insert a blank line between `` > `P X`. `` and `` Suggested module: `Instance/Sets/Powerset.v`. `` (GFM lazy continuation currently swallows the module line into the blockquote); collapse the double blank line after `## Work to be done`.
- **F8b. Stray `None.` inside a populated Dependencies block — #865:** delete the single line `None.` immediately following `` Depends on: #320 (MacLane III.3: Indexed coproducts) — it owns the indexed-coproduct file this issue must not rebuild. `` Leave the blank line and the SCOPE blockquote.
- **F8c. #639** *(unverified LOW)* — delete the leading line `` None (builds on the in-tree `SubobjectClassifier`, which is already present). ``, leaving the `Depends on: #683 — **serialization / name-ownership only, NOT a content prerequisite.** …` line as the section's only content. Trailer `"deps": ["#683"]` and `blocked_by = 683` already agree. (The #639/#653 `Structure/BooleanAlgebra.v` collision is already registered with creator 653 — no change.)
- **F8d. Body must not open with a redundant H1** — *(unverified LOW)* **#640, #641, #642, #643, #644, #645, #646, #647** (the whole Awodey Ch1 filing block; verified mechanically as the only 8 whose first line equals `# ` + title). Delete the first two lines (H1 + the blank line) from each so each begins with `## Source`. **#645 also gets the Group D clause-partition line — apply both in one edit; #643 also gets a Group B edge; #647 also gets the Group D bound edit.**
- **F8e. Blockquote inside `## Dependencies` → end of `## Work to be done`.** Move, unchanged, in **#843** (`> **SCOPE (added on audit).** …`, placed immediately after the In-tree-donors paragraph), and cosmetically in **#827** (`> **Verification note folded in on audit …**`), **#829** (`> **SCOPE (added on audit — two-thirds of the original Work section belonged to #824).**`) and **#832** (`> **Precision correction (added on audit).**`). After the #843 move, `## Dependencies` contains the three surviving `Depends on:` lines (#221, #839, #826) **PLUS the Group A `- Related (NOT blocking): #789 …` line — the Related-line convention is legitimate and must not be stripped.** *(Severity: uniformity/readability, MEDIUM-minus, not a proven resolver bug.)*
- **F8f. #921** — see F5 (blockquote → standard Related line) and F4 (blank line).

**F9. `make todo` DoD line — bulk substring swap across 19 issues.** In each of **#566, #567, #568, #569, #570, #571, #572, #573, #574, #575, #576, #577, #578, #579, #580, #581, #582, #583, #584** replace the substring `` `make todo` clean. `` with `` `make todo` adds no new hits. `` in the final DoD checkbox. No other text on that line changes. Verified: `make todo` on the current worktree prints 89 lines of pre-existing hits (`./Structure/Pullback.v:254:(* jww (2017-06-01): TODO *)`, `./Structure/Closed.v:154:(* jww (2018-10-05): TODO`, `./Structure/Discrete.v:29:  (* jww (2017-06-02): Equality is too much here. *)`, plus `Fail`/`Abort`/`admit` matches from `Test/`, `Theory/Metacategory.v`, `Theory/Kan/Extension.v:433-438`), so the box can never be ticked. The split is exactly 19 (#566–#584, `clean.`) / 16 (#585–#600, `adds no new hits`), machine-checked. **Only #566 is in a finding set — J26.**

**F10. Confusable module path — #529** *(unverified LOW, J27)*: change `Instance/Pointed/Sets.v` → `Instance/Sets/Pointed/Smash.v` in both the Work line and the Verification line, and append to the donor list `` (distinct from #261's `Instance/Sets/Pointed.v`, which builds the category itself; this issue uses the in-tree `Par`/`Coslice 1 Sets` model instead and asserts no edge) ``.

---

### GROUP H — `doc/plan/books/graph/serialize-groups.json`

All path-keyed entries follow the `Construction/Fractions.v` (#972/#1028) precedent; `check_graph.py` reads only the `issues` key. Construction-keyed (non-path) entries follow head-plan Group H items 1 and 4.

| # | Action | Entry |
|---|---|---|
| **H1** | Add | `{"module": "Structure/Biproduct/Finite.v", "issues": [336, 536], "creator": 536, "reason": "SERIALIZE_ONLY: the finite matrix calculus is scoped on both, under \`Structure/Bicartesian/Matrix.v\` (#336) and \`Structure/Biproduct/Finite.v\` (#536). Hypotheses differ (#336 bicartesian + ZeroObject; #536 HasBiproducts + zero object), so no precedence edge is asserted."}` |
| **H2** | Add | `{"module": "Construction/FreeAb.v", "issues": [430, 540], "creator": 540, "reason": "SERIALIZE_ONLY: the free abelian group on a set and its universal property are scoped on both, under \`Construction/FreeAb.v\` (#540) and \`Instance/Ab/FreeNotContinuous.v\` (#430)."}` *(paired with the Group B edge 430→540)* |
| **H3** | Add | `{"module": "Instance/Top/Opens.v", "issues": [268, 685], "creator": 685, "reason": "SERIALIZE_ONLY: the poset of opens of a space is built twice under two paths — #268 as the inclusion-ordered thin category \`Open X\` in \`Instance/Top/Presheaf.v\`, #685 as the lattice/frame of opens in \`Instance/Top/Opens.v\`. Invisible to check_collisions.py, which matches paths. Whichever lands first owns the carrier; #727 consumes it.", "rationale": "#727 declares both as blockers and never says which owns O(X)."}` |
| **H4** | Add | `{"module": "Instance/FinSet/Groupoid.v", "issues": [956, 963], "creator": 956, "reason": "SERIALIZE_ONLY: both build Fin_iso, the groupoid of finite sets and bijections, under different proposed paths (#956 Instance/FinSet/Groupoid.v; #963 offers to build it inside Instance/Top/Configuration.v). Invisible to check_collisions.py. #956 is the canonical builder; no precedence edge is asserted because #963 needs only Fin_mono."}` |
| **H5** | Add | `{"module": "Instance/Field.v", "issues": [307, 971], "creator": 971, "reason": "SERIALIZE_ONLY: the SAME category of fields and the SAME characteristic-obstruction non-existence argument under two module paths (#971 Instance/Field.v; #307 Instance/Dom/Fractions.v \\"if not already present\\"). Invisible to check_collisions.py. No precedence edge is asserted; #971 is the canonical builder (#1038 already cites it as such)."}` |
| **H6** | Add — **REQUIRED companion to the #1002→#481 demotion, not optional** | `{"module": "Structure/Limit/Reflection.v", "issues": [481, 1002], "creator": 481, "reason": "SERIALIZE_ONLY: #481 creates the file for coequalizer reflection; #1002 would add two ReflectsIsos lemmas to it if it exists. No precedence; edge removed on audit."}` Without it, with the edge gone both `blocked_by` sets become empty on a path both bodies name inside a scanned section, and **invariant 4 fires**. |
| **H7** | **AMEND head-plan Group H item 4 — do not create both** | Replace the non-path key `"Grp (construction, not a path: Instance/Grp.v vs Instance/Variety.v)"` with `{"module": "Instance/Grp.v", "issues": [255, 440], "creator": 255, "reason": "SERIALIZE_ONLY: the same category built two ways under two paths (Instance/Grp.v vs Instance/Variety.v) — invisible to check_collisions.py, which matches paths. #255 owns the name; #440 builds its variety presentation under a different name and the comparison equivalence falls to whichever lands second. No dependency edge is asserted."}` *(Every existing entry's `module` holds a real path and the precedent keys on ONE of the two. **Note the tension with H9/H10/H12/H14, which are deliberately non-path — see §5.**)* |
| **H8** | Add | `{"module": "Instance/Rel.v", "issues": [262, 830, 854], "creator": null, "reason": "SERIALIZE_ONLY: three issues edit one existing file with no supply/consume relation between them. No dependency edge is asserted.", "rationale": "#262 adds the converse R† and Faithful Relation_Functor; #830 fixes the composition-order comment at :33 and resolves the dead block at :96-157 while building a separate Instance/FinRel.v; #854 (Rel_R, the prop of relations over a rig) also edits this file's header."}` *(#814 is NOT a member — it consumes only the bare category and edits no file.)* |
| **H9** | Add (construction-keyed) | `{"module": "Graph (construction, not a path: Instance/Parallel/Graphs.v vs Instance/Graph.v)", "issues": [705, 926], "creator": 705, "reason": "SAME IDENTIFIER under two paths — invisible to check_collisions.py. #705 Work item 1 names its category \`Graph\` in Instance/Parallel/Graphs.v; #926 names a different category \`Graph\` in Instance/Graph.v. No dependency edge is asserted.", "rationale": "The two cannot be imported together; #926 must rename (e.g. SimpleGraph). See J10."}` |
| **H10** | Add (construction-keyed; **module key is a placeholder — J28**) | `{"module": "<numeric substrate: Instance/Poset/Numeric.v \| Instance/ExtReal.v \| Instance/Poset/Reals.v>", "issues": [759, 967, 1022], "creator": 759, "reason": "SERIALIZE_ONLY: the SAME real-number carrier built under three different module paths. No precedence; no dependency edge asserted. NOTE this group is invisible to check_collisions.py.", "rationale": "#759 builds ℤ/ℝ as Prosets over Coq.Reals in Instance/Poset/Numeric.v; #967 builds the extended real line as a complete-lattice poset in Instance/ExtReal.v; #1022 builds ℚ and ℝ>0 as thin categories in Instance/Poset/Reals.v. All three make the same Coq.Reals-vs-constructive substrate decision and all three commit to extending docs/AXIOMS.md."}` |
| **H11** | Amend the existing `Theory/Algebra/Monoid/Free.v` entry ({296, 502}) | `"issues": [296, 471, 502]`, appending to `rationale`: `PATH NOTE: #471 (MacLane VI.4) builds the free-monoid MONAD W₀ under Monad/Instance/List.v — the same construction at (Sets, ×), invisible to the path-keyed check. #471 is now blocked_by 296; #502 is a Related pointer only.` |
| **H12** | Add (construction-keyed) | `{"module": "Powerset_Quantale (construction, not a path: Instance/Powerset/Quantale.v vs Instance/Quantale/Examples.v)", "issues": [780, 791], "creator": 780, "reason": "Ordered, not merely serialized: #791 is already blocked_by #780, which creates the symbol. Recorded because the two propose different paths for a literally identically-named artifact.", "rationale": "#780's Verification prints Powerset_MonoidalPreorder and Powerset_Quantale; #791's prints Powerset_Quantale and Bottleneck_Quantale. #791 Work item 1 itself attributes both halves to #780."}` |
| **H13** | Add (construction-keyed) | `{"module": "⊗-word / canonical-map apparatus (construction, not a path: Construction/FreeMonoidal.v + Structure/Monoidal/Coherence.v vs Structure/Monoidal/Symmetric/Coherence.v)", "issues": [496, 497, 520], "creator": 497, "reason": "Ordered after remediation: #520 gains blocked_by 497, which transitively supplies #496. Recorded because #520's own XI.1 append claims the apparatus is 'subsumed in this issue's scope' under a third path.", "rationale": "Precedent: #608's Work says 'Reusing the inductive type of ⊗-words from the free-monoidal-category development (issue #496)' and its blocked_by is exactly [496]."}` |
| **H14** | Add (construction-keyed) — **path must be reconciled with the #774 rewrite first (§5)** | `{"module": "numeric monoidal-preorder bases (Instance/Nat/Monoidal.v + Instance/Reals/Order.v vs Instance/Poset/Numeric/Monoidal.v)", "issues": [774, 775, 783], "creator": null, "reason": "Ordered, not merely serialized: #783 is already blocked_by both 774 and 775. Recorded because #783's Verification prints Nat_Add_MonoidalPreorder — the SAME symbol #775's Verification prints — under a third path.", "rationale": "#775 owns (ℕ,≤,0,+) in Instance/Nat/Monoidal.v; #774 owns (ℝ,≤,0,+) in Instance/Reals/Order.v; #783 must import both."}` |
| **H15** | Amend the existing `Instance/RQuiver.v` entry | #979's remediation drops that path from its Suggested-modules line and Verification block; update the issue list accordingly. Its rationale (`"NEITHER builds the notion: that is #906"`) stays correct. |
| **H16** | Add | `{"module": "Instance/Rng/MonoidRing.v", "issues": [310, 518], "creator": 518, "reason": "SERIALIZE_ONLY: same-file peers; no dependency edge is asserted, but they must not be dispatched in the same parallel wave.", "rationale": "#518 builds ℤ[M] and the adjunction ℤ ⊣ U; #310's monoid-ring clause is the universal-arrow presentation of the same object and is derivable from it. #518's own Riehl §2.3 checkbox (Ring(ℤ[G],R) ≅ Grp(G,Rˣ)) already discharges #310's group-ring-as-universal-arrow-to-units clause."}` |
| **H17** | Add (construction-keyed) | `{"module": "EssentialImage (construction, not a path: Construction/Subcategory/EssentialImage.v vs Theory/EssentialImage.v)", "issues": [918, 985], "creator": 918, "reason": "Ordered, not merely serialized: #985 is blocked_by #918, which creates the notion. Recorded because the two propose different paths, so check_collisions.py cannot see the pair.", "rationale": "#985's Suggested-modules line offers \`Theory/EssentialImage.v\` for the notion 'if the Riehl §1.5 issue has not already landed it' — a live invitation to define it twice."}` |
| **H18** | Add (construction-keyed) | `{"module": "monoid object in (Cat, ×, 1) (construction, not a path: Instance/StrictCat/Monoid.v vs Instance/StrictCat/MonoidObject.v)", "issues": [500, 851], "creator": 500, "reason": "SERIALIZE_ONLY: same identification under two proposed paths, invisible to the path-keyed collision check.", "rationale": "#500's Work says 'with the \`Cat\`-monoid identification perhaps in \`Instance/StrictCat/Monoid.v\`'; #851 proposes \`Instance/StrictCat/MonoidObject.v\`. One path must win; state it in the header of whichever lands first."}` |
| **H19** | Add (construction-keyed; arises from the #890 demotion) | `{"module": "site of opens of a space (construction, not a path: Theory/Sheaf/Space.v vs Instance/Top/Cover.v)", "issues": [890, 460], "creator": 890, "reason": "SERIALIZE_ONLY: both construct the site of opens/covers; no edge is asserted in this direction (precedence runs #890 -> #460).", "rationale": "#890 re-founds the Sheaf predicate and builds Theory/Sheaf/Space.v; #460 builds the concrete sheaf of continuous functions over Instance/Top/Cover.v and consumes #890's predicate."}` |
| **H20** | Amend the existing `Structure/Lattice.v` entry — **conditional** | `issues` → `[340, 389, 422, 1003]`, appending to the rationale: `EXTENDED (Riehl Ch5 audit): #1003 (Riehl 5.5, lattices/semilattices as categories over Set) proposes the SAME order-theoretic vocabulary under \`Instance/Lattice.v\`; the differing path made the collision invisible to check_collisions.py. No precedence exists, so no edge is asserted.` **Apply ONLY if the Group C coordination bullet for #1003 is applied** — that bullet makes #1003 name `Structure/Lattice.v` inside a scanned section, and without this amendment `check_graph` invariant 4 would newly report three CONFLICT-FREE violations. If #1003 stays wholly in `Instance/Lattice.v`, skip this. **J18.** |
| **H21** | Add | `{"module": "Structure/Abelian/Homology.v", "issues": [516, 557], "creator": 557, "reason": "SERIALIZE_ONLY: same construction under two paths (\`Structure/Homology.v\` vs \`Structure/Abelian/Homology.v\`)."}` Precedent: the file's own `Construction/Fractions.v` entry. Once the Group B edge 516→557 and the Group C rewrite land, this is belt-and-braces (only one issue then writes the file); keeping it documents the erased hazard. |
| **H22** | Add — **ONLY if J15 resolves to option (b)** | `{"module": "CospanCat FinSet (named term; #869 -> Construction/PROP/Frobenius.v, #879 -> Instance/FinSet/Cospan/Operad.v)", "issues": [869, 879], "creator": 879, "reason": "SERIALIZE_ONLY: same construction under two paths; invisible to check_collisions.py."}` If option (a) (edge 869←879) is taken, do not add it. |
| **H23** | Ledger multi-part split for `7sketches:6.3.1:ex6.57` (#868/#869) — four edits, verified well-formed against the header and the sanctioned precedent (`7sketches:6.2.1:ex6.3` at lines 1945/1988 with `part:` notes and issue-map key `…@756`) | (1) `ledger.tsv:1964` note column → `part: the positive/equality half of Exercise 6.57` (it currently holds the issue TITLE). (2) Append row `7sketches:6.3.1:ex6.57\tPARTIAL\t#869\t6\tpart: the separation/negative half of Exercise 6.57` (non-adjacent duplicate rows are house style). (3) `seven-sketches/issue-map.json` → add `"7sketches:6.3.1:ex6.57@869": 869`. (4) #869 trailer ids → `["7sketches:6.3.1:thm6.58","7sketches:6.3.1:ex6.57"]`. **Sequencing: apply (4) LAST or merge it with the #869 trailer edit** — if J15 takes option (a), deps must end as `["#827","7sketches:6.3.1:thm6.55","7sketches:6.5.2:example6.94"]`. |
| **H24** | Optional / not required after the fixes above | Record only if the coordinator wants the history visible: `Structure/WellPointed.v` vs `Structure/Separator.v` (#655/#638 — resolved by the Group B edge and #638 dropping its file); `Instance/CompHaus.v` vs `Instance/CHaus.v` (#413/#915 — resolved by #915 ceasing to create the path); `Structure/End/Preservation.v` vs `Structure/End/Parameter.v` (#578/#582 — resolved by the edge, files disjoint). |
| **H25** | **#683 / `Structure/Thin.v`** | **head-plan Group H item 1 — do not re-file.** |

---

## 4. Judgement calls — decide individually, do not script

| # | Call | Tradeoff |
|---|---|---|
| **J1** | **#649 → #648 is contingent on #648 remaining the creator of `Instance/Mon.v`.** A sibling finding proposes handing the concrete monoid category to #296. | **Do exactly one of:** (a) keep `Instance/Mon.v` as #648's deliverable and declare 649→648 as in Group B; or (b) reassign creation to #296, delete the 648 edge on #649, and record #648/#649 as a serialize group on `Instance/Mon.v`. **Never both.** Note the Group C #648 rewrite explicitly preserves `Instance/Mon.v`, which favours (a). |
| **J2** | **#530 → #255: hard edge vs softening the checkbox.** | The edge is convention-consistent (a DoD item genuinely consumes #255; #1021's precedent declines edges precisely when no DoD item consumes). Cost: #530 is a ~20-line core lemma and #542 waits behind it, and #255 is unstarted. **Alternative:** soften the Riehl §3.1 checkbox to an explicit deferral and assert no edge. **Do NOT blend** — asserting the #255 edge *and* deferring the Pos/Mon/Top clause (Group D) is the pairing the verdicts jointly recommend; asserting neither, or both, is coherent; asserting the second without the first is not. |
| **J3** | **Three new dependents on #417 (#542, #559, #563) — decide as one.** #417 is layer 2 (`blocked_by [335,326,416]`). | #559 is layer 0, so the edge moves it and its dependents #563/#564 down; #542 is layer 1 and reschedules #546, #552 and through them #551/#555. All three consumptions are genuine (#559's cocone lemma quantifies over finiteness; #542's step IS #417's Work item 3; #563's Theorem 1 hypothesis is "P finite"). **Alternatives:** descope the finite case from #559 (a larger edit to a live DoD ending "…the finite-cocone lemma proved."), or rename #542's Verification symbol `abelian_finitely_complete` → `abelian_has_equalizers` and record #417 as Related only (which weakens the issue below its own Mac Lane §VIII.3 remark). **Recommend all three edges; decide knowingly, in one pass, and re-run the layering afterwards.** |
| **J4** | **#829 step (1) is a paired edit or nothing.** | If you delete #829's first Work bullet and its `Construction/Cospan/Corelation/Monoidal.v` module, you must **simultaneously** edit #824's step 3 to read `…obtained from the cospan structure by the jointly-epic quotient rather than rebuilt — proved once at the level of an ARBITRARY base in \`Construction/Cospan/Corelation/Monoidal.v\` (the tensor of two jointly-epic cospans is jointly epic, so the wide-subcategory structure is inherited, stated as a transport along \`Corel_to_Cospan\`), then instantiated at FinSet` and add that path to #824's Verification. Otherwise leave the bullet and reword it to "the GENERAL restriction (arbitrary base); the FinSet instantiation is #824's". Step (5) — consolidating `Instance/FinSet/Corel.v` into #824's `Instance/FinSet/Corelation.v` — is safe either way (the 829→824 edge serialises the file). |
| **J5** | **The Awodey edition label is campaign-wide, not per-issue.** `pagemap.md` establishes the PDF is the 1st-ed CMU pre-print (OLG 49, September 2005); ~34 issues label it "2nd ed. (Oxford Logic Guides 52)", inherited from the `*-drafts.md` templates. | #658's remediation (49 → 52) makes #658 *consistent with the corpus* and *inconsistent with the calibration report*. Either (a) relabel the ~34 to match `pagemap.md` and leave #658 as-is, or (b) record an explicit convention that "2nd ed." is deliberate shorthand for the pre-print and apply #658's edit. **Do not apply #658 in isolation.** #679 gets no edit either way. Note the Group E "(2nd ed.)" additions on #497, #520, #590, #772 presuppose branch (b). |
| **J6** | **#1002 → #1004: delete the edge, or add a Work item.** The line reads `the notion "category of models for an algebraic theory", in which Corollary 5.6.4 is stated` — provenance, not consumption; #1002's Work item 3 and DoD name only in-tree artifacts (`Theory/Lawvere/Sets.v:83` `ev1`, `Theory/Lawvere/Monad.v:89` `Context (refl : ReflectsIsos (ev1 T))`, `Instance/Sets.v:400` `bijective_is_iso`), and neither `AlgebraicCategory` nor "finitary" appears anywhere in #1002. | Deleting unparks a two-lemma result from the whole finitary/filtered chain. If the corollary is genuinely meant to be stated *for* `AlgebraicCategory`, keep the edge and add an explicit Work item saying so. **Related hardening:** the #259 DoD box `- [ ] The \`Top\`/\`Poset\` contrapositive is either proved or explicitly scoped out in the header with its dependency named.` still leaves a "proved" branch that silently reintroduces the #259 prerequisite; tighten to `…is explicitly scoped out in the header with #259 named as the missing prerequisite` if you want the demotion durable. |
| **J7** | **#750 → #704 demotion.** LOW, no verdict. Merits are clean (#750 needs the *covariant* powerset; #704 is explicitly the contravariant one; `serialize-groups.json` already records the file with a rationale defending only the #466/#227 legs) — **but a prior Riehl-Ch.5 audit recorded a deliberate decision to keep this edge.** | Demoting buys one wave and removes a false consumption claim; leaving preserves a recorded decision. **Default: leave, and record the covariant/contravariant distinction in the line instead.** |
| **J8** | **#516's new homology module path.** The finding says `Instance/Simplicial/Homology.v`; the verdict prefers `Structure/Abelian/Homology/Simplicial.v` as more consistent with the repo's Structure/ vs Instance/ split (the alternating-sum construction is general over any abelian category). | Either resolves the finding, but **#517 line 17 and #519 line 17 must be retargeted to the same choice in the same edit**, and it must not collide with #557's `Structure/Abelian/Homology.v`. |
| **J9** | **#890 — add the reverse edge 460←890?** | Removing 890←460 is right (it inverts precedence and parks a seven-issue hub). Adding 460←890 is honest — #460's Work already presumes #890's strengthening — but lengthens #460's chain. |
| **J10** | **`Graph` name clash (#705 / #926).** #705 names its directed-graph category `Graph` in `Instance/Parallel/Graphs.v`; #926 names a *different* simple-graph category `Graph` in `Instance/Graph.v`. | Renaming #926's to `SimpleGraph` is the obvious call but touches #926's body, its DoD, and the H9 key. **Structurally identical to the head plan's `Rng` (#362/#257) and `Grp` (#255/#440) clashes — resolve it the same way**, in one edit touching both issues plus the serialize key. |
| **J11** | **#471 — is `blocked_by 296` the right instrument at all?** The verdict rejected #502 (no `icoprod` in-tree) and substituted #296. But by the campaign's own test #471 could discharge "lawful `W₀` on Sets + `Mon ≅ EM`" by building the list monad directly, making **any** edge fabricated. | The verdict's judgement is that a second free-monoid construction is the worse outcome. Accept or drop the edge — but if you drop it, keep the `- Related (NOT blocking)` line naming both #296 and #502. |
| **J12** | **#536 / #336: Related-only, or a real edge?** | "once #336's boxes are amended to 'consume' #536's artifacts, a real edge `336 blocked_by 536` would also be defensible and layer-compatible (#536 layer 0, #336 layer 1) — but Related-only is the safer edit." Related-only asserts nothing (`Related (NOT blocking)` is in `REVERSE_CUE`); a real edge guarantees the ordering the amended checkboxes assume. |
| **J13** | **#818 — quantale-scoped collage vs #587's Sets-based collage.** #818 Work item 4 asserts `"Record the relationship with #587's one-sided construction — that issue's category is this one applied to a profunctor out of the unit V-category."` That is **false as written**: #587's `C_K` has genuine hom-*setoids* and `Sets` is not thin. | **(a)** Generalize the base — DoD becomes `- [ ] The collage is a V-category over an arbitrary monoidal base with an initial object (the empty join when the base is a quantale), so that V = Sets is covered and #587's \`C_K\` is a genuine instance`, and the identification becomes a proved theorem; costs real generality work. **(b)** Keep the quantale scoping — Work item 4 becomes `Record in the header the ANALOGY with #587's one-sided construction at V = Sets — the same four-block shape over a different base; the quantale collage does not specialise to it, since \`Sets\` is not thin`, DoD becomes `- [ ] The header states the analogy with #587's Sets-based collage and why it is not an instance`, **and the #587 edge is demoted** to `- Related (NOT blocking): #587 (the one-sided Bénabou collage at V = Sets — the same shape over a base this issue does not cover).` with trailer `["7sketches:4.2.2:def4.8","#799","#787"]` and native 818←587 dropped. (a) buys a true theorem and keeps the edge; (b) is cheap but loses a stated deliverable. **Either way:** `"is recorded"` is unverifiable as a DoD item — say whether a proof or a header note is required. |
| **J14** | **#592 → #227: Related, or hard?** | Promote to `Depends on: #227` (plus trailer and native) **only if** the coordinator decides the `2^M ≅ P(M)` identification of Work item 1 is in scope rather than optional. #592's Current state confirms the gap: "there is no direct-image / power-set functor `2^M ≅ P(M)` in the tree." |
| **J15** | **#869/#879 `CospanCat FinSet`: edge (a) vs construction-keyed serialize entry (b).** | (a) ``Depends on: #879 (`7sketches:6.5.2:example6.94`) — the named `CospanCat FinSet` term.`` + trailer + native 869←879. (b) H22, keeping both unblocked. #879 has no blockers and #869 is blocked on [827,868], so the edge costs little scheduling; (b) preserves parallelism and matches the path-invisible precedent. **The #869 Work-item-5 and DoD deletions are unconditional either way.** The choice determines #869's final trailer deps, which must be reconciled in ONE edit with H23. |
| **J16** | **#873 — keep the #872 edge or parallelise.** | Keeping it (recommended) costs one wave but guarantees the six `DecCospan_*_Coherent` instances land once. Deleting it (`"deps":[]`, drop native 873←872, add `- Related (NOT blocking): #872 — derives the same coherences generically; whichever lands first, the other consumes rather than re-derives.`) buys parallelism at the risk of two competing instance sets. **Whichever is chosen, apply the same wording to #876 or the two drift.** |
| **J17** | **#900 ownership of `surjectivity_is_epic`.** The finding wanted the #245 edge deleted; the adversarial pass **reversed** this and ruled #245 the owner. | Both readings are defensible under campaign rules; the tie-breaker is whether #245's universe-scoped biconditional is the form #900 needs. **Apply the ADJUSTED version (keep the edge)** unless you decide otherwise — this is the one place in the tail where the "no edge if it can build it itself" rule was deliberately overridden. |
| **J18** | **#1003 — `Structure/Lattice.v` or `Instance/Lattice.v`.** | Landing the vocabulary in `Structure/Lattice.v` beside #340/#389/#422 requires H20 or `check_graph` invariant 4 fires three new violations. Staying in `Instance/Lattice.v` keeps the collision invisible but is architecturally defensible — the repo already ships both idioms (`Structure/Monoid.v` `MonoidObject` vs `Instance/CMon.v`). **Do not do half.** |
| **J19** | **#1017: descope (no couniversal dual) vs phrase the terminality clause over #347's class.** | Descoping keeps a flagship Kan issue unblocked and removes a duplicate, but means the couniversal dual reaches the tree only via #347/#989, whose timing is uncertain (#347 blocked by #302). Only if the coordinator positively WANTS #1017's terminality clause stated over `CouniversalArrow` should the edge + trailer + native relation be applied instead. |
| **J20** | **#1038: make the inherited ordering explicit?** After the Group C rewrite, #1038 explicitly consumes #257's `CRng` and #232's `Frac`, but its ordering is inherited by accident through 1038→971→{226,232}→257. | Adding explicit `Depends on: #257` / `Depends on: #232` lines makes it intentional. Verdict: "not required, and not adding them is not wrong." |
| **J21** | **#547 requires editing a SECOND live issue (#534).** | Leaving #534 alone leaves a false instruction live on the issue that authored it, so a #534 implementer will still look for a shared lemma; editing it widens the blast radius of a MEDIUM finding into an issue nothing else touches. **Recommend editing both.** |
| **J22** | **#647 bound refinement.** `n + e ≤ 6` is solid; the parenthetical `e ≤ 4` rests on "loops excluded by acyclicity" and is weaker. | Consider dropping it and keeping only `n + e ≤ 6` (hence `n ≤ 6`, `e ≤ 5`) in the DoD. |
| **J23** | **#570 mutates `ledger.tsv` (lines 790–791, `PARTIAL` → `ABSENT`)** on a LOW, unverified finding. | Head plan §6 already warns that ledger row numbers cited in findings were not confirmed. Verify the two rows before editing; the `schemas.md` rule quoted is right, the indices are inherited. |
| **J24** | **Post-trailer blocks on #1011 / #1014 / #1016.** All three deviate from the contract, all are marked "(added on audit)", all still parse. | Sweep them into F1 alongside #490/#526/#559/#589/#657/#809/#918/#972, or leave them and accept that `file_chapter.py` will silently skip the `ids` update on the next append to any of them. |
| **J25** | **#921: demote the #261 edge, or delete the alternative route.** Work item 1 says `either as the pointed restriction of issue #261's \`Set_*\` **or directly over \`Instance/FinSet.v\`**, whichever keeps the skeletal computability…` — the stated criterion points AWAY from #261 (whose deliverable is `Instance/Sets/Pointed.v`, unrestricted in size). | **A:** demote to `- Related (NOT blocking): #261 … — an optional donor if \`Fin_*\` is built as the pointed restriction of \`Set_*\`; Work item 1 equally sanctions building \`Fin_*\` directly over \`Instance/FinSet.v\`, which is the route that preserves skeletal computability.`, empty trailer deps, drop blocked_by 261. **B:** keep the edge and delete `or directly over \`Instance/FinSet.v\`, whichever keeps the skeletal computability that \`Instance/FinSet.v\` already enjoys` from item 1 and `(or imported)` from the DoD. **Pick exactly one, do not blend.** A is the campaign-precedent choice (`doc/wiggum-handoff.md:3260-3263`, the #541/#536 demotion). |
| **J26** | **F9's 19-issue `make todo` swap reaches outside the finding set.** Only #566 was audited; #567–#584 are inferred from a machine-checked substring sweep. | Pure one-line substring swap and the defect is real, but it edits 18 bodies no finding names. Either accept the sweep as evidence or re-run it before applying. |
| **J27** | **#529 path rename.** `Instance/Pointed/Sets.v` vs `Instance/Sets/Pointed.v` is the same near-mirror hazard as `Instance/Mod`/`Instance/Module`. LOW, no verdict; obligations genuinely distinct (#261 builds the category, #529 builds `(− ∧ S) ⊣ Set_*(S, −)` over the in-tree `Par`/`Coslice 1 Sets` model), so no edge is warranted either way. | Rename for legibility, or leave and add the disambiguating donor note only — **but treat it consistently with whatever is decided for `Mod`/`Module`.** |
| **J28** | **H10's `module` key is a literal placeholder.** One of `Instance/Poset/Numeric.v`, `Instance/ExtReal.v`, `Instance/Poset/Reals.v` must be nominated as the carrier path; whichever lands first owns the `docs/AXIOMS.md` entry (all three commit to extending it, all three make the same `Coq.Reals`-vs-constructive decision). | Decide the path before writing the entry. **Interacts with the #774/#783 path conflict in §5.** |
| **J29** | **#723 and #718.** #718 stands in the *same* Sets-vs-FinSet relation to #723 part (b) that justified demoting #404. | If #404 is demoted, #718's blocking status on #723 deserves the same scrutiny — deliberately deferred to a later pass rather than folded into this edit. |
| **J30** | **#518 vs #310 — who owns the monoid ring.** The finding said #518 consumes #310; the adversarial pass reversed it and made **#518 the creator**. | #518 is focused and nearly ready (blocked only on #257); #310 is a three-part bundle additionally requiring an `Instance/Vect` substrate that does not exist and has no builder issue, so blocking #518 behind it could stall it indefinitely. The reversal means the edge, if wanted, belongs on **#310** (`Depends on: #518 …`) — an out-of-batch edit. **Default: the ADJUSTED version (no edge, path unified, H16).** Decide whether to also touch #310. |
| **J31** | **#822 — demote (a) or home the reuse (b).** | (a) drops the #776 edge and records the link in prose (Group A). (b) keeps it by adding a real consumption: Work item `6. Instantiate the generalised interpreter at a thin symmetric monoidal base and prove it agrees with #776's validity predicate \`Structure/Monoidal/Preorder/Diagram.v\`, so the order-level diagram calculus is literally the thin case.` plus DoD `- [ ] The thin-base instantiation is proved to agree with #776's validity predicate (not merely noted in the header)`. (b) is more mathematics; (a) is the cheap correct-as-filed reading. |
| **J32** | **#704 — demote #227 fully, or keep it as an ordering edge?** | The verdict demotes it (Work item 1 builds the same carrier; #704's own callout denies precedence). It also allows keeping a creator-precedence edge as a *dispatch convenience* — but if you do, the body line **must** say `ordering only, NOT supply/consume` on its own line rather than asserting consumption. Pick one; do not leave the current text. |
| **J33** | **#465's edge strength.** The verdict notes #388 is arguably needed for #465's **main** Work item ("build the endofunctor `R ⊗ (−) : Ab ⟶ Ab`"), not merely the appended checkbox. | The edge is right either way; only the stated reason would change. |
| **J34** | **NEW duplicate pair surfaced, outside every chunk's issue set: #422 vs #737.** Both schedule the Proset colimit-is-a-join / limit-is-a-meet identification (#422 the general J-indexed statement; #737 the discrete-diagram case with `proset_colimit_is_join` in its Verification and its own DoD box). | Register it as its own duplicate pair — #737 should consume #422 rather than re-prove the discrete case. **Not actionable from this evidence alone.** Note the fan-in on #422 is now large: new dependents #444 and #1022, plus consumers #755, #1003, #1022 and (per this call) #737. |

---

## 5. Contradictions with the head plan — resolve before executing

1. **Head plan Group H item 3 (`Adjunction/Representability.v` [348, 437]) must NOT be applied.** `check_graph.py:233-240` discharges a co-creation conflict by `reach()` FIRST and consults `acknowledged` only when NO dependency path exists. **#437 IS blocked_by #348**, so the entry would never be read — dead data. Worse, every existing entry in that file declares "SERIALIZE_ONLY … No logical precedence exists, so NO dependency edge is asserted", the opposite of this pair. **The head plan's #437 body edit (its F6) is correct and should still be applied; only its "(+ Group H)" rider must be dropped.** If the coordinator wants the graph-level record anyway, change the entry's `reason` so it does not claim SERIALIZE_ONLY (the head plan's own wording, "Ordered, not merely serialized", already does this) — but then also fix the file's contract note, or the key's semantics stay ambiguous.

2. **Head plan Group C #440 is superseded.** Its wording makes "prove `GroupVariety ≅[Cat] Grp` once #255 has landed" an unconditional Work deliverable while declaring the relation non-blocking — a hidden blocker (an implementer scheduled before #255 cannot finish a Work item, and no edge warns the scheduler). Use the Group C text above, which makes the comparison conditional and homes it on whichever lands second.

3. **Head plan Group H item 4 is amended, not duplicated.** Its non-path key `"Grp (construction, not a path: Instance/Grp.v vs Instance/Variety.v)"` should become the path key `Instance/Grp.v` (H7). **Do not create both.** *(Note the internal tension: H7 argues path keys because every existing entry uses one, while H9/H10/H12/H13/H14/H17/H18/H19/H22 deliberately use construction keys — which the head plan's own §5 third-highest-leverage recommendation endorses. **Decide the keying convention once**, and apply it to all ten, rather than resolving each entry ad hoc.)*

4. **Head plan Group D #450's second checkbox is FALSE** (`⟨Ω,E⟩-Alg` has no objects exactly when the signature has no constants). `Instance/Comp.v:160` `Algs_Terminal` is the one-element algebra satisfying every equation. Use the corrected pair in Group D.

5. **Four head-plan lines carry unresolved or missing item ids and would fail invariant 3 as written:** #444's `Depends on: #422` (no id — supply `maclane:V.2:remark1`); #453's `Depends on: #353` (no id — supply `maclane:IV.2:construction2`); #455's `Depends on: #413` (no id — supply `maclane:V.1:ex2`); #458's optional Related line, where the head plan writes the ellipsis `maclane:V.2:...` (supply `maclane:V.2:thm1`).

6. **The three campaign-wide questions are NOT settled in the head plan as delivered.** `plan-head-216-471.md` §4 lists them as **judgement calls 4, 5 and 7** ("decide individually, do not script"), with recommendations but no decision:
   - **`deps` array convention (head §4.7)** — still open. This gates head-plan F3, this plan's F3 #953/#956/#963/#964/#968 normalisation, and every trailer edit in Groups A/B here (each written in whichever form its issue already uses, so no edit *increases* inconsistency — but normalisation must not run before the convention is fixed, or half of them get rewritten twice). This range makes the mix worse: item-id trailers (#691, #843, #777, #817), issue-number trailers (#566, #617, #712, #714, #991, #1009, #1017), and already-mixed ones (#437 ships `["#366","maclane:V.6:thm1","#348"]`; #461 gains `"#434"` beside `"maclane:V.6:thm2"`).
   - **`Instance/Mod` vs `Instance/Module` (head §4.4)** — still open; #454's Group B edge and J27's #529 rename both defer to it, and J27 must be resolved consistently with it.
   - **`Rng` name clash #362/#257 (head §4.5)** — still open. **#518 (Group C) uses `Rng` in #257's unital sense and proposes `Instance/Rng/MonoidRing.v` under it.** If the head plan's ruling ends up assigning `Rng` to the non-unital category, the #518 path and its H16 key must be revised in the same edit. J10's `Graph` clash is explicitly to be resolved "the same way", so all three must be decided together.

7. **Cross-section path conflict on ℝ (new, not in the head plan).** Section-1's #774 rewrite (Group C) **renames #774's deliverable away from `Instance/Reals/Order.v`** to `Structure/Monoidal/Preorder/OrderedMonoid.v` + a thin `Instance/Reals/Monoidal.v`, and has #774 consume #759's carrier. Section-2's #783 rewrite and **H14** both assert `(ℝ, ≤, 0, +)` **is** #774's deliverable *in* `Instance/Reals/Order.v`. **These must be reconciled in ONE edit** touching #774's Work/Verification, #783's Work item 1, H14's `module` key and rationale, and (per J28) H10's placeholder. Applying them independently leaves #783 importing a path #774 no longer creates.

8. **Cross-section path conflict on homology (managed, but must land together).** Section-4 makes `Structure/Abelian/Homology.v` #557's; Section-1 renames #516's remaining file per J8 and retargets #517/#519 line 17. Merged into one Group C row + H21 above — **do not apply the two sections' #516 rows separately.**

---

## 6. What remains unverified

- **64 LOW findings across the four chunks carry no adversarial verdict** and were not independently confirmed. By issue: **chunk 0** — #503-consume, #805, #987, #813, #463, #725, #740, #945, #658, #529, #432-F4, #533, #538, #756, #592, #750, and the F5 one-dependency-per-line split on #719. **chunk 1** — #446, #520-lit, #568, #570, #627, #631, #635, #639, #640–#647 (H1 block), #704-blockquote, #784, #786, #871, #874, #953/#956/#963/#964/#968, #955, #959-lit, #1005-lit. **chunk 2** — #435, #491, #497, #541-adjacent #545, #544, #552-lit, #561, #563-lit, #590, #602, #745, #779-lit, #807, #888, #889-adjacent, #930, #1021. **chunk 3** — #482, #526, #553, #606, #611, #637, #772, #832, #921, #982, #1017-F3.
- **The single highest-risk unverified item is #982's Group A demotion** — a graph mutation (edge delete + trailer + `blocked_by`) on evidence that was never adversarially checked. Re-read #982's body and `blocked_by` before the `DELETE` call.
- **Two unverified items mutate `ledger.tsv`**: #570 (rows 790–791, `PARTIAL` → `ABSENT`) and, indirectly, the H23 row-1964 note edit. **Verify row numbers before editing** — head plan §6 already flags that ledger indices cited in findings were never confirmed.
- **F9's #567–#584 (18 bodies) are collateral of a machine-checked substring sweep**, not of any finding. Only #566 was audited.
- **Verdict-scoping caveat (chunk 2):** verdicts are keyed to the *issue*, not the finding. Where an issue carried several findings and fewer verdicts, each verdict's `problem` string was matched to its finding; the unmatched ones (#552-lit, #563-lit, #717-format, #779-lit, #890→#416) are therefore unverified **even though their issue has a verdict**.
- **No issue body was opened.** Every verbatim quote, in-body line number, `blocked_by` set and trailer content is inherited from the sweep and the adversarial pass. Repository-side artifacts *were* checked directly for a subset: the `check_graph.py` `REVERSE_CUE`/`body_deps`/`unresolved_deps` live runs behind the four REFUTED cycle findings, `serialize-groups.json`'s entries, `Structure/Group.v:107/109/112`, `Instance/Comp.v:108/116/151/160/209/268/382`, `Functor/Hom/Yoneda.v:253`, `Instance/Sets.v:283`, `Construction/PROP/Presentation.v:180`, `Construction/PROP/Universal.v:603`, `Structure/Monoidal/Internal/Product.v` (directory contents), `Structure/Pullback.v:215`, `Instance/FinSet/Classifier.v:264`, `Structure/Limit/Product.v:128`, `Structure/Equalizer/Fork.v:68`, `ls Instance/Cat/`, and `make todo`'s 89 pre-existing hits. Two Rocq 9.1.1 probes were compiled (`Cat_Monoidal_probe`; the `GroupObject`/`CartesianMonoidal Cat` pair).
- **No cycle or layer analysis was run on the post-remediation graph.** Groups A and B together remove ≈49 edges and add ≈45; individual cycle checks are recorded per row, but the combined result — in particular the #417 fan-in (J3), the #422 fan-in (J34), the #255 fan-in (#530, #712, plus head-plan #298), and the interaction of the #1002/#1008 demotions with H6 — has not been checked. **Run `check_graph.py` after A and B, before dispatch.**