# QA remediation plan — TAIL (issues 432-1038), the four section plans

Recovered from the workflow journal because the MERGE agent output was truncated at the
top, losing its verdict, DO-NOT-ACT list and groups A-D. These four sections are the
complete, unmerged source and supersede the partial merge.


---

<!-- ===== section 1 ===== -->

# Remediation plan — tail chunk 1: issues **#433–#1022** (73 findings read, 56 distinct issues)

Source: `doc/plan/books/qa/findings-tail-1.json`. Severity mix: 18 HIGH / 37 MEDIUM / 18 LOW. **18 LOW findings carry no adversarial verdict** (issues 446, 520-lit, 568, 570, 627, 631, 635, 639, 640, 704-blockquote, 784, 786, 871, 874, 953, 955, 959-lit, 1005-lit) — they were not independently verified and are marked `⚠unverified` below. Two findings were REFUTED (§ DO NOT ACT). Group letters follow `plan-head-216-471.md`.

**Overlap with the head plan.** Six issues in my chunk are already fully covered there and must not be re-scripted: **#433** (F6), **#446** (F6), **#452** (Group E), **#454** (Group B), **#650** (Group A), and **#458** (F6). For #444 and #458 my chunk supplies a *correction* to the head plan's text (missing/ellipsised item id) — see the ⚑ rows. Head-plan judgement calls 4 (`Instance/Mod` vs `Instance/Module`) and 7 (`deps` array convention) are touched by my chunk and are **deferred to the head plan**, not re-litigated.

---

## GROUP A — Demote fabricated or over-asserted hard edges

Mechanical shape unchanged from the head plan: (i) delete the `Depends on: #N …` line from `## Dependencies`; (ii) add the reason on its own physical line beginning `- Related (NOT blocking):`; (iii) `gh api -X DELETE …/<ISSUE>/dependencies/blocked_by/<N>`; (iv) drop the entry from the trailer `deps`.

| Issue | Demote | Evidence (verbatim) | Replacement line + trailer |
|---|---|---|---|
| **#583** (HIGH, CONFIRMED) | #425 | Work: `"Suggested module: `Instance/Fun/NonPointwiseLimit.v`. In-tree donors: `Instance/Fun.v`, `Structure/Limit.v`, and the pointwise-limit development (#425) for contrast."` #425's two deliverables are both gated on the pointwise limit *existing*, which is exactly what #583 refutes. | Replace `Depends on: #425` with `None.` then `- Related (NOT blocking): #425 (the pointwise-limit theorem this counterexample delimits; its hypothesis fails here, so nothing is consumed).` Trailer → `{"ids": ["maclane:IX.7:ex1", "riehl:3.4:example10"], "deps": []}`. **Do not** substitute #424 — #583 can compare the two objects directly. |
| **#704** (HIGH, CONFIRMED) | #311 | #311's own Work: `"Define the contravariant power-set functor in the concrete setting where it exists at one universe level (FinSet: subsets as decidable predicates/monos; Ω = 2)"` + `"note in the header why Sets itself only supports the cross-universe reading"`. #704 Work 2 builds it: `"Define P : Sets^op ⟶ Sets with P(f) := f^{-1} and prove the functor laws."` | Delete the `Depends on: #311 (…)` line **and** its orphaned continuation `power-set functor)` (see F). Optional: `- Related (NOT blocking): #311 — the same functor at `FinSet`, where the universe obstruction does not bite; nothing here consumes it.` |
| **#704** (MED, CONFIRMED) | #428 | Work item 6: `"Prove P(A + B) ≅ P(A) × P(B) (Exercise 1, second clause), preferably by exhibiting P ≅ Hom(−, 2) and reusing Structure/BiCCC.v:134's exp_coprod; record in the header that the book's own route needs \"representables preserve limits\", which the tree does not yet have"`. `Structure/BiCCC.v:134` is `#[export] Program Instance exp_coprod {x y z : C} : x^(y + z) ≅ x^y × x^z`, already proved. | Delete `Depends on: #428 …`; add `- Related (NOT blocking): #428 (Hom-functors are continuous) — the book's own route to Exercise 1; this issue takes the `exp_coprod` route instead and only records the alternative in the header.` |
| **#704** (ADJUSTED) | #227 | Verdict: #704's Work item 1 is `"Define the powerset of a setoid (the setoid of ≈-closed predicates …)"` — it builds the carrier — and #704's own shared-module callout says `"Land #227's carrier first, OR land whichever comes first and have the other reuse it"`, i.e. it **denies** precedence. Same test that removed #1005's edges from that serialize-group. | Use the ADJUSTED text under Group F below (whole-block replacement). Result: `deps: []`, all six `ids` preserved verbatim, plus `gh api -X DELETE …/704/dependencies/blocked_by/227`. **Do NOT write "this issue's contravariant half reuses"** — Work item 1 contradicts it. |
| **#830** (MED, CONFIRMED) | #262 | Line: `"Depends on: #262 (Rel, converse relations, and the graph embedding — the / ambient relational development)."` #830 builds `Instance/FinRel.v` from scratch (`obj := nat`, `hom m n := Fin.t m -> Fin.t n -> Type`); #262's `R†` and `Faithful Relation_Functor` are consumed nowhere. Overlap is editorial co-location on `Instance/Rel.v` (`:33` comment, dead block `:96-157` — both verified in tree). | Replace the two-line entry with, on its own physical line: `- Related (NOT blocking): #262 (the dagger/converse on `Rel`). Both issues edit `Instance/Rel.v` — #262 adds `R†` and `Faithful Relation_Functor`, this issue fixes the composition-order comment at `:33` and resolves the commented-out block at `:96-157` — so they must not be dispatched in the same parallel wave, but neither consumes the other.` Trailer → `{"ids":["7sketches:5.2.1:example5.8","7sketches:5.2.1:ex5.10"],"deps":["7sketches:5.2.1:def5.2","7sketches:5.2.1:example5.3"]}`. **+ Group H entry.** |
| **#853** (HIGH, CONFIRMED) | #512 | Line (4 physical lines): `"Depends on: #512 (the simplicial category as the free strict monoidal category on a monoid — the plain-monoidal statement of the same correspondence; this issue is its symmetric/presented-prop counterpart and additionally requires the interpretation machinery to accept a non-prop target)."` Δ appears in zero Work items, zero DoD boxes, zero Verification commands, and is absent from the donor list. | Delete all four lines; add `- Related (NOT blocking): #512 — the plain-monoidal statement of the same correspondence (Δ as the free strict monoidal category on a monoid); this issue is its symmetric/presented-prop counterpart.` Trailer → `{"ids":["7sketches:5.4.2:remark5.74"],"deps":["7sketches:5.2.1:def5.11"]}`. Remove native 853←512; **keep 853←832**. |
| **#860** (HIGH, CONFIRMED) | #756 | Line: `"Depends on: #756 — the remaining case of Exercise 6.3, the two-element linear order `_2` with exactly one initial object, is that issue's `Initial` structure on `_2`."` #860's Source already scopes it out: `"Items 7sketches:6.2.1:ex6.3 (the zero- and two-initial-object cases only; the one-initial-object case is already filed)"`. No file overlap either. | Replace with `- Related (NOT blocking): #756 — the one-initial-object case of Exercise 6.3 (`Two_Initial` on `_2`) is filed there.` Trailer → `{"ids":["7sketches:6.2.1:ex6.3","7sketches:6.2.1:example6.5","7sketches:6.2.1:ex6.6"],"deps":[]}`. Remove native 860←756. **#861's edge on #860 is genuine and must stay.** |
| **#924** (MED, CONFIRMED) | #250 | Line: `"- Depends on: #250 (monic/epi cancellation and a non-invertible bimorphism)"`. #924's own Current state: `"The cancellation vocabulary is present: Theory/Morphisms.v:116 Class Monic … and :104 Class Epic …"` (both verified). Riehl's Lemma 1.6.22 is one application of the `monic` field. #924 creates `Theory/Morphisms/Rectangle.v`; #250 extends `Theory/Morphisms.v` — no conflict. | Replace with `- Related (NOT blocking): #250 (monic/epi cancellation and a non-invertible bimorphism) — a neighbouring issue in `Theory/Morphisms.v`; its `monic_cancel`/`epic_cancel` are not used here, since Riehl's Lemma 1.6.22 is one application of `Theory/Morphisms.v:116`'s `monic` field, which is already in tree.` Trailer `"deps": []`; drop native 924→250. |
| **#926** (HIGH, CONFIRMED) | #705 | Line: `"- Depends on: #705 (the category of directed graphs as a functor category — the nearest in-tree graph development; this issue must build the *simple*-graph variant and relate the two)"` — hedge inside a Depends-on line. Everything asked of #705 is prose: Work 1 `"State in the header how this relates to the directed-graph presentation of #705"`; DoD `"- [ ] `Graph` is defined for **simple** graphs, and the header states precisely how it differs from `Quiver`/`QuiverCategory` and from #705's directed graphs"`. | Replace with `- Related (NOT blocking): #705 (the category of directed graphs as the functor category Sets^Γ) — this issue builds the *simple*-graph variant itself and only contrasts the two in its header; no artifact of #705 is consumed. NOTE: #705's Work item 1 also names its category `Graph` (in `Instance/Parallel/Graphs.v`); this issue's `Instance/Graph.v` must pick a distinct name (e.g. `SimpleGraph`) or the two cannot be imported together.` Trailer → `{"ids":["riehl:2.0:construction-ncolor","riehl:2.4:example3"],"deps":["#345"]}`; drop native 926→705. **+ Group H + judgement call 5.** |
| **#959** (HIGH, CONFIRMED) | #720 | Only two mentions of #720 in the body: the donor list, and the DoD line `"- [ ] Preservation is proved for **all** limits, not only products (#720 covers products and exponentials)"` — an explicit statement that #720 covers *less*. `blocked_by(720) = [718]`, so the edge parks #959 behind the whole presheaf-exponentials development. Different files (`Functor/Hom/Yoneda/Limit.v` vs `…/Preservation.v`). | Delete `Depends on: #720`, drop from trailer, drop native edge, **and drop `#720` from the `In-tree donors:` line**. Add `- Related (NOT blocking): **#720** proves the product and exponential cases of Yoneda preservation; the general limit-preservation theorem here subsumes its product half. Do not duplicate — whichever lands second cites the other.` Leave the DoD line unchanged. |
| **#959** (HIGH, CONFIRMED) | #671 | Work item 4: `"Introduce the vocabulary — an X-shaped generalized element of a is a morphism X ~> a … Coordinate the definition with #671, which introduces local membership of generalized elements in a subobject, so the two do not diverge."` #671 Work 1 presupposes `z : Z ~> x` and never defines the standalone notion. | Delete `Depends on: #671`, drop from trailer and native set, drop `#671` from `In-tree donors:`. Add `- Related (NOT blocking): **#671** introduces local membership of a generalized element in a subobject (Theory/Subobject/Membership.v); keep the generalized-element definition here compatible with it. No dependency edge is asserted.` Leave Work item 4's coordination sentence. |
| **#1001** (MED, CONFIRMED w/ addition) | #997 | Line: `"Depends on: #997 (`riehl:5.1:exiii`) (the characterizations of an idempotent monad)"`. The words **"monic"** and **"whiskering"** do not occur in #1001 at all; it works from `Construction/Reflective/Idempotent.v:81`'s `idem_join_iso`. | Delete the line; trailer → `"deps":["#475","#985"]`; `gh api -X DELETE …/1001/dependencies/blocked_by/997`. Add `- Related (NOT blocking): **#997** adds two further characterizations of an idempotent monad; this issue works from the in-tree μ-invertible field `idem_join_iso` (`Construction/Reflective/Idempotent.v:81`) and consumes neither. No dependency edge is asserted. Both issues also EDIT `Construction/Reflective/Idempotent.v` (#997 to re-express the class, #1001 to add the uniqueness lemma), so do not work them concurrently.` **Verdict's explicit instruction: record the co-editing in this same sentence — do NOT add a serialize-group entry, since `check_graph.creates()` drops paths that already exist.** |
| **#1004** (HIGH, CONFIRMED) | #986 | Line: `"Depends on: #986 (locally presentable categories and accessible functors — for the cross-referenced LFP claim)"`. Work item 4: `"local finite presentability of such a category is the subject of #986 and should be cross-referenced, not restated"`; DoD: `"- [ ] The local-finite-presentability claim cross-referenced to #986 and the `cHaus` non-example flagged as pending, both in the file header, so nothing is asserted that is not proved."` #489 gets identical treatment in the same sentence and is correctly *not* a dependency. | Delete the line; trailer → `"deps":["#559"]`; `gh api -X DELETE …/1004/dependencies/blocked_by/986`. Add `- Related (NOT blocking): **#986** owns the local-finite-presentability claim; Work item 4 only cross-references it in the file header, exactly as it does #489 for the `cHaus` non-example. No dependency edge is asserted.` |
| **#1005** (HIGH, CONFIRMED) | #980 | Line: `"Depends on: #980 (the Beck–Chevalley transformations and their invertibility over a pullback square)"`. Work item 2: `"Prove it the book's way, by the pullback pasting criterion (Theory/Morphisms/Stability.v has the pasting toolkit), and note in the header that this is the Sets instance of the general Beck–Chevalley condition of #980."` #980 itself says neither transformation can currently even be *typed*. | Delete the line; trailer → `"deps":["#407","riehl:5.1:example4"]`; `gh api -X DELETE …/1005/dependencies/blocked_by/980`. Add `- Related (NOT blocking): **#980** builds the general Beck–Chevalley transformations over slices. Work item 2 proves the `Sets` instance directly from `Theory/Morphisms/Stability.v`'s pasting toolkit and only cross-references #980 in the header. No dependency edge is asserted.` |
| **#650** ⚑ | #649, #651 | **Already in head-plan Group A, row #650.** My chunk adds two further independent CONFIRMED verdicts, no text change. Apply the head plan's version verbatim (trailer already correct — do not touch). | — |

---

## GROUP B — Add missing hard edges

Shape: one `Depends on: #N (\`item-id\`) — <reason>` per physical line under `## Dependencies`; add the id to the trailer `deps` (**form per head-plan §4.7**); `gh api -X POST …/<ISSUE>/dependencies/blocked_by -f issue_id=<numeric REST id>` (`gh api repos/jwiegley/category-theory/issues/<N> --jq .id`, **not** the GraphQL node id).

| Issue | Edge | Exact line to insert |
|---|---|---|
| **#444** ⚑ (ADJUSTED) | #422 | Head plan's Group B row for #444 writes the line **without an item-id label**, which violates check_graph invariant 3. Corrected: `Depends on: #422 (\`maclane:V.2:remark1\`) — supplies the preorder-limit identification (products in a preorder are greatest lower bounds) used to prove \`Ord^op\` small-complete`. Insert after `Depends on: #437 (\`maclane:V.6:thm3\`)`. Also replace `"the §V.2 preorder issue"` (Work line 29) and `"the §V.2 preorder-limit issue"` (donors line 41) with `#422`. |
| **#454** ⚑ | #449 | **Already in head-plan Group B, identical text.** Caveat from my chunk's verdict: `#388` ("MacLane IV.6: The tensor-hom adjunction for modules") also creates `Instance/Module/Tensor.v`, and serialize-groups.json records that pair with `"creator": null`. #454's body names the §V.7 issue, so #449 is the faithful target; a Related pointer at #388 is optional. **Defer the directory name to head-plan judgement call 4.** |
| **#462** (ADJUSTED) | #353 | `Depends on: #353 (\`maclane:IV.2:construction2\`) — the colimit functor (Riehl's Proposition 3.3.1) from which the §3.3 checkbox derives functoriality of \`π₀\`` — the finding's `maclane:IV.2:construction-colim-adjoint` **does not exist**. Add `"#353"` to trailer deps + native edge. Cycle-safe: closure(353)={351}. **Do NOT put a second bare item id (e.g. `riehl:3.3:prop1`) on the line** — `RESOLVED_LABEL` strips only the first `#N (\`id\`)` group and the trailing id would be reported as unresolved. |
| **#471** (ADJUSTED) | #296, **not #502** | `Depends on: #296 (\`maclane:II.7:cor2\`) — builds the free monoid on a set (lists with concatenation) with its universal property / free–forgetful adjunction; \`W₀\` is its induced monad.` Trailer → `["maclane:VI.4:prop2", "#296"]`; native 471←296 (cycle-safe, closure(296)={}). **Plus** a non-blocking line: `- Related (NOT blocking): #502 builds the general free monoid \`∐ₙ aⁿ\` in a monoidal category (\`Theory/Algebra/Monoid/Free.v\`); at \`(Sets, ×)\` its induced monad is this issue's \`W₀\`. Reuse whichever construction has landed rather than building a second free-monoid monad.` **Reason the original #502 edge was rejected:** the tree has no indexed/countable coproducts (`Structure/Limit/Product.v` has `iprod`/`iprod_proj`/`iprod_ump` only, no `icoprod`), so #502 is the heaviest possible prerequisite and nothing in #471's DoD consumes a #502 deliverable. |
| **#514** (CONFIRMED) | #515 | `Depends on: #515 (\`maclane:VII.5:def-simplicialset\`) — the simplicial sets the coend formula is stated over`; add `"#515"` to trailer + native. Cycle-safe: `blocked_by(515)=[225,512]`, never reaches 514. Pairs with the Group D checkbox. |
| **#520** (HIGH, CONFIRMED) | #497 | Insert after `Depends on: #490 (\`maclane:VII.1:construction1\`)`: `Depends on: #497 (\`maclane:VII.2:cor1\`) — supplies the \`⊗\`-word datatype (via #496) and the canonical-map assignment \`can_B(v,w)\` that the permuted-word \`can_M(vσ, wτ)\` extends`. Trailer `["maclane:VII.1:construction1"]` → `["maclane:VII.1:construction1","maclane:VII.2:cor1"]`; native 520←497. One edge suffices (497 blocked_by [496]). No cycle. Precedent: **#608**'s Work says `"Reusing the inductive type of ⊗-words from the free-monoidal-category development (issue #496)"` and its blocked_by is exactly `[496]`. |
| **#542** (CONFIRMED) | #417 | `Depends on: #417 (\`FinitelyComplete\` — the predicate \`abelian_finitely_complete\` instantiates).` Trailer `["maclane:VIII.1:remark2"]` → add `"#417"`; native 542←417. Cycle-safe: closure(417)={335,326,416,427}. **Consumption is stronger than the finding states** — #417 Work item 3 is `"Prove the corollary: a terminal object, binary products and equalizers give finite completeness"`, which is exactly #542's `"hence (with biproducts + a terminal object) all finite limits"` step. **See judgement call 3 (scheduling cost).** |
| **#577** (CONFIRMED) | #254, #407 | Replace `## Dependencies` body `None.` with two physical lines: `Depends on: #254 (indexed products in \`Sets\` — gates the Riehl §3.2 equalizer checkbox only)` / `Depends on: #407 (equalizers/completeness of \`Sets\` — same checkbox)`. Trailer → `{"ids": ["maclane:IX.5:remark1", "riehl:3.2:exv"], "deps": ["#254", "#407"]}` + both native edges. Scoping parenthetical follows the #333→#259 precedent. Both cited lines verified: `Structure/Limit/Product.v:128` = `Class HasIndexedProducts (C : Category) := {`, `Structure/Equalizer/Fork.v:68` = `Class HasEqualizers (C : Category) := {`. |
| **#591** (CONFIRMED) | #334 | Add a second physical line: `Depends on: #334 (\`maclane:III.4:ex3\` — the limit-by-evaluation-at-an-initial-index-object theorem in \`Structure/Limit/Initial.v\`, the last step of Corollaries 3 and 4)`. Trailer → `["maclane:X.3:thm1", "#334"]`; native edge. In-batch precedent: **#585 is already natively blocked_by [334] for the same theorem.** |
| **#1014** (CONFIRMED) | #434 | Add `Depends on: #434 (a full reflective subcategory of a cocomplete category is cocomplete — the descent step Work item 1 consumes)`; trailer → `"deps":["riehl:E.4:def-grothendieck-topos","#715","#404","#590","#434"]`; native edge. Pairs with Group C + D below. |
| **#1022** (CONFIRMED) | #422 | Add `Depends on: #422 (products in a preorder are greatest lower bounds — the thin-category (co)limit/sup-inf dictionary)`; trailer → `{"ids":["riehl:6.2:example8","riehl:6.2:exiii"],"deps":["#589","#599","#422"]}`; add 422 to native blocked_by (currently `[589, 599]`). #422 has no dependencies → cannot cycle. Recognised-owner corroboration: **#967** Work item 2 says `"Using the poset dictionary of #422 (limits are greatest lower bounds, colimits least upper bounds)"`. |

---

## GROUP C — Rewrite Work item to CONSUME, not rebuild

| Issue | Rebuilt artifact | Owner | Exact edit |
|---|---|---|---|
| **#471** (ADJUSTED) | free-monoid monad `W₀` | #296 | Work bullet 1 → `- Obtain \`W₀\` on \`Sets\` as the monad induced by #296's free-monoid universal arrow / adjunction (\`Adjunction_Induced_Monad\`, or \`AdjunctionFromUniversalArrows\` if #296 stopped at the universal arrow) — do not re-derive a second free-monoid construction from \`Theory/Coq/List.v\`; discharge the categorical unit/associativity laws here.` **Do NOT write "verify its carrier computes to `list X`"** — a coproduct-of-tensor-powers carrier in Sets is not definitionally `list X`. |
| **#471** (CONFIRMED) | the category `Mon` | in-tree | Current-state parenthetical `(no ordinary category \`Mon\`, no algebra characterization)` → `(the ordinary category \`Mon\` DOES exist — \`Theory/Algebra/Monoid/Hom.v:83\`, with \`Mon_Forget\` at \`:93\` — but has no left adjoint, and the algebra characterization is absent)`. Work bullet 2 `- Define the category \`Mon\` of monoids (donor: \`MonoidObject\` in \`(Sets, ×)\`, \`Structure/Monoid.v\`).` → `- Reuse the existing category of monoids \`Mon\` (\`Theory/Algebra/Monoid/Hom.v:83\`) at \`C = Sets\`; do not define a new one (built over \`Theory/Algebra/Monoid.v:44\`'s \`Monoid\` class, not \`Structure/Monoid.v:124\`'s \`MonoidObject\`).` The issue's own Riehl append already calls the Current-state claim `**false**`. |
| **#520** (CONFIRMED) | ⊗-words + `can_B` | #496/#497 | Donor phrase `the free-monoidal/coherence development of §VII.2` → `#496's \`Construction/FreeMonoidal.v\` (words) and #497's \`Structure/Monoidal/Coherence.v\` (\`can_B\`), which this issue extends rather than re-derives`. (+ Group B edge, + Group H.) |
| **#650** ⚑ (CONFIRMED) | `surjectivity_is_epic` | #245 | Work bullet 1 → `- Consume #245's completed \`surjectivity_is_epic\` biconditional (\`Epic h → ∀ b, ∃ a, h a ≈ b\` for \`Sets\`), **as #245 states it** (its converse is scoped "at the honest universe placement"); do NOT re-prove it here — it is #245's deliverable, including the resolution of the size obstruction the current \`Abort\` at \`Instance/Sets.v:476\` records.` DoD → `- [ ] \`Epic → surjective\` is taken from #245 (not re-proved), and \`Sets_balanced\` adds no new global axiom.` (Head plan covers the *edge* half of #650; this Work-item half is **new**.) |
| **#783** (HIGH, CONFIRMED) | `(ℕ,≤,0,+)`, `(ℝ,≤,0,+)` | #775 / #774 | Work item 1 → `1. **CONSUME, DO NOT RE-CREATE** the two bases: \`(ℝ, ≤, 0, +)\` is #774's deliverable in \`Instance/Reals/Order.v\` and \`(ℕ, ≤, 0, +)\` is #775's in \`Instance/Nat/Monoidal.v\`; this issue is already blocked on both. Import them into \`Instance/Poset/Numeric/Monoidal.v\`; build no third copy and re-derive neither monotonicity lemma.` Verification: delete `Print Assumptions Nat_Add_MonoidalPreorder.` and `Print Assumptions R_Add_MonoidalPreorder.` (**#775's Verification prints the identical symbol `Nat_Add_MonoidalPreorder`**; #774's prints `Reals_Additive_MonoidalPreorder`). DoD box → `- [ ] The \`(ℕ, ≤, 0, +)\` and \`(ℝ, ≤, 0, +)\` instances used here are the ones from #775 and #774, imported rather than redefined`. **+ Group H.** |
| **#791** (HIGH, CONFIRMED) | `Powerset_Quantale` | #780 | Work item 1 → `1. **CONSUME, DO NOT RE-CREATE** the powerset quantale: \`Powerset_Quantale\` in \`Instance/Powerset/Quantale.v\` is #780's deliverable (its Exercise 2.35 monoidal preorder plus its Exercise 2.94 residual and arbitrary unions), and this issue is already blocked on #780. Import it; add nothing to \`Instance/Quantale/Examples.v\` for the powerset base.` Delete `Print Assumptions Powerset_Quantale.` from Verification (leaving `Bottleneck_Quantale`, `transport_enrichment`, `bottleneck_enrichment`). DoD → `- [ ] The bottleneck base is built as a quantale; the powerset quantale is consumed from #780, not rebuilt — so both can serve as bases of enrichment and as matrix bases`. **+ Group H.** |
| **#799** (CONFIRMED) | arbitrary joins | #684 | Replace Work item 1's last sentence — `"This is the join half of #684's completeness vocabulary; if that issue has landed, consume its definition instead of adding a second one."` — with `#684 is a hard dependency of this issue and delivers this vocabulary; CONSUME its join definition and add none of your own. This work item is a specification of what must be available, not a licence to define it a second time.` (`blocked_by(799) = [797,684,422]`, so the "if" branch is unreachable by construction.) |
| **#855** (HIGH, **ADJUSTED**) | `Rel` converse | #262 | Four edits (the fourth is the verdict's addition): (1) Work item 1 sentence 1 → `Consume #262's converse on \`Rel\` (\`Rel_converse : Rel ≅ Rel^op\`, with \`converse (converse R) ≈ R\` and \`converse (R ∘ S) ≈ converse S ∘ converse R\`); do NOT rebuild it.` (2) sentence 2 → `Add the rig-indexed analogue for the relations of §5.4.3, with the same involution and contravariance laws.` (3) DoD → `- [ ] The converse of the rig-indexed relations is built, with the involution and contravariance laws, reusing #262's \`Rel\` version rather than duplicating it.` (4) **Verification: `#   Print Assumptions Rel_converse.` → `#   Print Assumptions RelR_converse.`** Keep the #262 edge — it becomes genuine. |
| **#900** (**ADJUSTED** — reverses the finding) | `surjectivity_is_epic` | #245 | **Keep** the #245 edge, the trailer entry and the native relation. (1) Body line → `Depends on: #245 — it owns \`Instance/Sets.v:429\`'s aborted \`surjectivity_is_epic\` (epis in \`Sets\` are exactly the surjections, including the hard epic → surjective direction and its documented universe obstruction); Example 7.74's \`Set\` compilation consumes that theorem.` (2) Work item 4: replace `"Completing Instance/Sets.v:429's aborted surjectivity_is_epic is a prerequisite for the Set-level reading and should be done here or on #245."` with `Completing \`Instance/Sets.v:429\`'s aborted \`surjectivity_is_epic\` is #245's obligation; consume the completed lemma here rather than reproving it.` (3) DoD → `- [ ] The \`Set\` reading rests on #245's completed \`surjectivity_is_epic\`, not on an \`Abort\`ed statement.` (4) **Leave `Print Assumptions surjectivity_is_epic.` in Verification — it is the consumption check.** No trailer/blocked_by change. |
| **#909** (CONFIRMED) | split-mono preservation | #656 | Work item 1 (which currently orders *both* `"state and prove \"if r ∘ i ≈ id then fmap[F] r ∘ fmap[F] i ≈ id for any functor F …\""` **and** `"deferring the general statement to issue #656 and simply *consuming* it here"`) → `1. **Abstract half:** consume issue #656's lemma that a functor carries split monos to split monos and split epis to split epis. Do not restate or reprove it here; if #656 has not landed, cite it and take the statement as an explicit hypothesis in the header.` No dependency-surface change — the #656 edge is correct and agrees across all three surfaces. (Module-line fix under Group F.) |
| **#979** (CONFIRMED) | `rQuiver` | #906 | Work item 5 → `5. Exercise 4.5.iv: over #906's \`ReflexiveQuiverCategory\` (\`Construction/Free/ReflexiveQuiver.v\`) — do NOT redefine it — redo steps 2–4 for reflexive quivers and record where the answers differ from the plain-quiver case.` Drop `Instance/RQuiver.v` from the `Suggested modules` line **and** from the Verification `coqc` list. DoD `- [ ] \`rQuiver\` is defined and the whole computation redone there.` → `- [ ] The whole computation is redone over #906's \`ReflexiveQuiverCategory\`, with the differences from the plain-quiver case recorded.` serialize-groups.json already records `"NEITHER builds the notion: that is #906"`. **+ Group H amendment.** |
| **#979** (CONFIRMED) | `QuiverCategory ≃ [Parallel^op, Sets]` | #962 | Work item 1 → `1. Reuse the \`QuiverCategory ≃ [Parallel^op, Sets]\` equivalence built by #962 in \`Construction/Free/Quiver/Complete.v\`; do not rebuild it. (It is what makes the local cartesian closure of presheaf categories applicable here.)` Drop `Instance/Quiver/Presheaf.v` from `Suggested modules` and the Verification `coqc` list. DoD `- [ ] Quivers are identified with presheaves on the walking parallel pair, as a proved equivalence.` → `- [ ] #962's \`QuiverCategory ≃ [Parallel^op, Sets]\` equivalence is consumed (not re-proved) and is what carries local cartesian closure to quivers.` Extend the #962 rationale to `— supplies the presheaf identification AND the limits (in particular the pullbacks) the base-change functor needs.` |
| **#1014** (CONFIRMED) | cocompleteness-by-reflection | #434 | Work item 1 sentence 1 → `Prove that a Grothendieck topos is cocomplete by citing #434 (\`Cocomplete D → Cocomplete (the subcategory)\`) at the lex reflection, with presheaf cocompleteness from #715 — do not re-derive the descent argument.` (Post-trailer block already says `**Cite #434 rather than re-deriving the descent argument.**`) Apply the same edit to **#1016** if its Giraud-axioms record is to carry small colimits. (+ Group B edge, + Group D box.) |
| **#1022** (CONFIRMED) | thin-category (co)limit = sup/inf | #422 | Work item 2's second clause → `…and consume #422's poset dictionary (a limiting cone in the thin category induced by a preorder IS a greatest lower bound, and dually) to read bounded suprema and infima off as colimits and limits — do not restate that lemma here.` DoD `- [ ] The "in a thin category a (co)limit is a supremum/infimum" collapse proved as a reusable lemma` → `- [ ] The sup/inf reading is obtained by instantiating #422's preorder-(co)limit dictionary; no second collapse lemma is introduced`. (+ Group B edge.) |

---

## GROUP D — Add closure checkbox for prose-only increment

| Issue | Checkbox / scope line to append |
|---|---|
| **#514** (CONFIRMED) | Insert immediately after `- [ ] Cofaces realized as face inclusions.`: `- [ ] (from MacLane §IX.6) The coend formula for realization: \`\|S\| ≅ ∫^n Sn · Δn\` stated and proved over \`Structure/Coend.v\`, exhibiting the realization functor as a coend.` **Verdict correction:** the append's pointer to `Instance/Sets/Coend.v` is imprecise — the realization coend is valued in **Top**, not Sets; write `Structure/Coend.v` plus a Top-level copower. Ownership confirmed at `ledger.tsv:827` (`maclane:IX.6:remark2  ABSENT  #514  4`). Pairs with the #515 edge. |
| **#520** (CONFIRMED) | Insert two boxes after `- [ ] The symmetric coherence theorem (same-permutation canonical maps agree) proved.`: `- [ ] (from MacLane §XI.1) The permuted-word functors \`(wτ)_M : M^n ⟶ M\`, \`(wτ)_M(a_1,…,a_n) = w(a_{τ1},…,a_{τn})\`, defined for a \`⊗\`-word \`w\` and \`τ ∈ S_n\` — reusing \`tensor_list\`/\`tfold\` (\`Theory/Multicategory/Representable.v:55,67\`) and packaging them as functors.` / `- [ ] (from MacLane §XI.1) The canonical-map assignment presented as \`can_M(vσ, wτ)\` between permuted-word functors, characterized inductively (identity, \`α\`, \`γ\` canonical; closed under composite and \`⊗\`), with the unitor extension Mac Lane leaves to the reader discharged.` Ownership: `ledger.tsv:912-913`, both PARTIAL against #520. **Evidence correction:** #520 has five appends and **three** lack checkboxes, not two — the `riehl:E.2:thm2` append correctly has none because it states it adds no obligation. |
| **#645** (CONFIRMED) | Append one line immediately after the two Riehl checkboxes: `Clauses NOT homed here, and why: the groups/rings/fields/modules clause (the monoid case is part (b) of this issue; the group case rides #255's category of groups, and no category of rings, fields or modules exists in-tree), the topological-spaces clause (no category of spaces in-tree), and the homotopy-category clause (no homotopy category in-tree) — recorded as out of scope, not as omissions.` Pattern precedent verified in **#652**: `"Note (do not attempt here) that the Rings (ℤ initial, zero ring terminal) and Groups/Vect cases require categories the library does not yet provide."` |
| **#803** (CONFIRMED) | Insert after `- [ ] The reflection is proved to *truncate*…`: `- [ ] Exercise 3.22 is discharged: \`PreorderReflect\` of the one-object loop category is proved isomorphic to \`Instance/One.v:25\`'s \`_1\`` / `- [ ] Remark 3.23 is a two-sided containment lemma about \`HomRel\`s (bounded below by \`cong_incl\`, above by the total congruence of Work item 1), not prose`. **Also** add to the Verification `Print Assumptions` block: `Print Assumptions loop_reflection_is_terminal.` and `Print Assumptions congruence_interval.` (block currently lists only `PreorderReflect`, `preorder_reflection_adjunction`, `free_reflection_is_reachability`). |
| **#1014** (CONFIRMED) | Add immediately after the existing cocompleteness box: `- [ ] The descent step cites #434 rather than re-deriving it; whichever of #1014/#1016 lands first owns the proof.` Contrast precedent verified: **#1011**'s overlap block does carry checkboxes (`- [ ] **Do not build a second chain-complex category.**`). |

---

## GROUP E — Fix wrong or stale citations, clause maps, and one false claim

| Issue | Change | Verified? |
|---|---|---|
| **#916** | **The issue orders a false theorem.** Work item 2 `"Prove that \`Nat(Id, (−)₊)\` has exactly one element, the \"inject\" transformation \`x ↦ Some x\`…"` → `2. Prove that \`Nat(Id, (−)₊)\` has exactly TWO elements — the injection \`x ↦ Some x\` and the constant family \`x ↦ None\` — and prove both are natural. The count comes from probing with maps out of a singleton (\`Instance/Sets.v:248\` \`Sets_Terminal\`): naturality forces \`α_X (x) = fmap f (α_1 ∗)\` for \`x : 1 ⟶ X\`, and \`(1)₊\` has exactly two elements.` Work item 4 `(a singleton setoid, resp. the empty setoid)` → `(a two-element setoid, resp. the empty setoid)`. DoD → `- [ ] \`Nat(Id, (−)₊)\` is proved to be a two-element setoid, exhibiting both elements (the injection and the constant-\`None\` family)`. Verification: rename `nat_id_to_addpoint_unique` → `nat_id_to_addpoint_two`. **Leave the `Nat((−)₊, Id)` half untouched.** Riehl asserts no count: `"Characterize all natural transformations from the identity functor on Set to the functor (−)+ : Set → Set that adds a new disjoint point. Are there any natural transformations in the other direction?"` | HIGH, CONFIRMED (Yoneda: `Nat(Id,F) ≅ F(1) = option(1)`) |
| **#620** | Current state `Structure/Group.v:112` → `:109`. Work item 1 `"- Instantiate a group object in \`Cat\` using the in-tree \`GroupObject\` at \`Cat_Cartesian\`"` → `- Instantiate a group object in \`Cat\`: \`GroupObject\`'s context is \`@CartesianMonoidal C\` (\`Structure/Group.v:107\`), so first obtain \`@CartesianMonoidal Cat\` via \`Structure/Monoidal/Internal/Product.v:435\` \`CC_CartesianMonoidal\` from \`Cat_Cartesian\` (\`Instance/Cat/Cartesian.v:39\`) and \`Cat_Terminal\` (\`Instance/One.v:54\`), then instantiate \`GroupObject\` over it.` Add `Structure/Monoidal/Internal/Product.v` (`CC_CartesianMonoidal`) to In-tree donors. | CONFIRMED **by compilation** — the verifier compiled `Fail Check (fun g : Cat => @GroupObject Cat Cat_Cartesian g).` (succeeds as a `Fail`) and `Check (fun g : Cat => @GroupObject Cat Cat_CartesianMonoidal_probe g).` (elaborates). `@CartesianMonoidal Cat` is the correct fully-applied form; `@CartesianMonoidal Cat _` is an "Illegal application". |
| **#883** | **ADJUSTED — do not paste the auditor's text, it asserts a falsehood.** Replace `"(clause (iv) only — the colimits clause of that remark is recorded on #405)"` with: `(clause (iv) only). Full clause map for the five-property remark, whose authoritative record is #405's body: (i) all limits → #405, which carries the added checkbox 'An elementary topos is finitely complete: derive equalizers from products and pullbacks' — this is NOT already a theorem, since \`Structure/Topos.v:112\`'s \`ElementaryTopos\` carries terminal + products + pullbacks only as DATA and the file header discloses the pullback/equalizer reduction as unformalized; (ii) all colimits → #405; (iii) cartesian closure and (v) the subobject classifier → already axioms of the class (\`topos_closed\`, \`topos_classifier\`), so no issue is needed; (iv) epi-mono factorization → this issue. The remark's corollary 'since Set is a topos, all five hold for Set' is recorded on #404 and is blocked by the absence of any \`ElementaryTopos Sets\` instance.` | ADJUSTED |
| **#976** | Source: `§4.4 "Adjoint functors and duality":` → `§4.4 "Contravariant and multivariable adjoint functors":`. Nothing else in that paragraph changes — every page number (152/172, 155/175, 172/192) re-verified against `inventory-4.json`. | CONFIRMED |
| **#452** ⚑ | Already in head-plan Group E (`Theory/WeaklyInitial.v:119` → `Adjunction/SAFT.v:119`). No delta. | CONFIRMED |
| **#570** | `doc/plan/books/ledger.tsv` lines 790–791 `PARTIAL` → `ABSENT`; #570's Current state first word `Partial.` → `Absent.` Rule quoted from schemas.md: `"**PARTIAL requires a PROVED statement covering part of the item. A never-instantiated class does not qualify.**"` All four cites verified (`Structure/Wedge.v:38,61`, `Theory/Dinatural.v:51`, `Structure/Cartesian/Closed.v:75`). **Confirm the row numbers before editing** (head plan §6). | ⚠unverified (LOW) |
| **#520** | `Construction/ColouredPROP.v:108` → `:109` (`:108` blank; `:109` = `Class ColouredPROP : Type := {`). `Construction/PROP.v:68` is correct. | ⚠unverified (LOW) |
| **#568** | `PreservesColimit` (`Structure/Limit/Preservation.v:206`) → `:196`. | ⚠unverified (LOW) |
| **#631** | `Theory/Bicategory/OneObject.v:57` → `:56`. Sister issue #632 already cites `:56`. | ⚠unverified (LOW) |
| **#635** | `Structure/Group.v:112` → `:109` in the Current-state sentence (the In-tree-donors mention carries no line number). Same defect as #620. | ⚠unverified (LOW) |
| **#784** | Donor entry `Instance/Two.v:38,122,134` → `Instance/Two.v:122,134`; if the top-element lemma was intended, add `Construction/Enriched/Two.v:38` (`two_from_top`). | ⚠unverified (LOW) |
| **#786** | Both occurrences of `Instance/Proset.v:20` → `:19` (Current state + DoD). #803 already cites `:19`. | ⚠unverified (LOW) |
| **#874** | Replace `` `vertex_obj` occurs 27 times (cone apices, in `Theory/Equivalence/Limit.v`, `Instance/Cones*.v`, `Structure/Limit/*.v`, `Construction/Comma/Limit.v`) `` with `` `vertex_obj` occurs 104 times across 19 files (cone apices, throughout `Structure/Cone*.v`, `Structure/Limit/*.v`, `Instance/Cones*.v`, `Adjunction/Continuity.v`, `Structure/Pullback/Limit.v`, `Structure/UniversalProperty/Limit.v`, `Construction/Comma/Limit.v` and `Theory/Equivalence/Limit.v`) ``. The note's other two counts are correct. | ⚠unverified (LOW) |
| **#955** | `` `toggle` (`:492`) `` → `` `toggle` (`:493`) ``. The other three cites (`:449`, `:479`, `:485`) verify exactly. | ⚠unverified (LOW) |
| **#959** | `Theory/Equivalence/Limit.v:353` → `:350` (`Hlegs` is declared at `:350`; `:353` is a comment). The companion cite `:401` for `ff_reflects_limit` is correct. | ⚠unverified (LOW) |
| **#1005** | `(printed pp. 206–207, PDF pp. 226–228)` → `(printed pp. 206–208, PDF pp. 226–228)` **if** the lemma runs onto printed p. 208 (consistent with #1006/#1007 putting Exercises 5.5.v–vii on printed 208–209), otherwise `(printed pp. 206–207, PDF pp. 226–227)`. | ⚠unverified (LOW) |

---

## GROUP F — Contract / format

**F1. `## Dependencies` block physically mangled — #704 (ADJUSTED).** Current block, verbatim:
```
Depends on: #311 (MacLane III.1: A universal element for the contravariant
Depends on: #227 — creates `Instance/Sets/Powerset.v`; coordinate the module layout so the covariant and contravariant halves coexist.
power-set functor)
Depends on: #428 (MacLane V.4: Hom-functors are continuous) — the book's stated
route for Exercise 1
```
Replace the whole block with:
```
## Dependencies

None.

- Related (NOT blocking): #227 (MacLane I.3: the covariant power-set functor) — designated `creator` of `Instance/Sets/Powerset.v` in `doc/plan/books/graph/serialize-groups.json` (claimants 227, 466, 704, 750, 871, 1005). This issue's Work item 1 builds the same `≈`-closed-predicate carrier, so land whichever comes first and have the other reuse it; must not share a parallel wave.
```
plus the two `- Related (NOT blocking):` lines for #311 and #428 from Group A. Trailer → `"deps": []`, **`ids` preserved verbatim as all six**: `["awodey:7.5:construction-sets-double-dual", "awodey:7:ex9", "awodey:7:ex1", "awodey:9:ex5", "riehl:1.3:example7", "riehl:4.4:exiii"]`. Run the three `blocked_by` deletions (311, 428, 227).

**F2. Hedge occupying a `Depends on:` line, edge genuine — split, do not delete.**
- **#883** (CONFIRMED). Replace `Depends on: #405 — the sibling clause of the same topos-properties remark (colimits) is that issue's obligation; this issue deliberately does not restate it.` with two physical lines: `Depends on: #405 — \`Structure/Regular.v:66\`'s \`Class Regular\` requires \`regular_coeq\` (coequalizers of kernel pairs), so \`Topos_Regular : ElementaryTopos C → Regular C\` cannot be proved until finite colimits in a topos exist.` / `- Related (NOT blocking): #405 also owns the colimits clause of the same topos-properties remark; this issue deliberately does not restate that clause.` No trailer/native change.
- **#944** (CONFIRMED). Replace `- Depends on: #730 (the dependent product as the right adjoint to base change — related reading, distinguished in the header)` with `- Depends on: #730 (the dependent product as the right adjoint to base change — supplies \`Σ_!\` and \`Π_!\`, which the appended Riehl §4.5 item identifies with this issue's dependent sum and product along \`X → 1\`)`. Amend the contradicted DoD line `- [ ] The header distinguishes this dependent product from the base-change right adjoint of #730` → `- [ ] The header relates this dependent product to the base-change right adjoint of #730, and the appended §4.5 identification is proved (not merely a distinction)`. Append to Work item 5: `…and prove the identification the appended §4.5 item requires, rather than only recording the distinction.`

**F3. Unresolvable prose references → issue numbers.**
- **#591** (CONFIRMED). `That counterexample is filed as its own issue in the Riehl Chapter 6 batch.` → `That counterexample is filed as **#1019**.`; `(recorded against the group-theoretic induction issue) and Exercise 6.1.iv (in the counterexample issue)` → `(recorded against **#987**) and Exercise 6.1.iv (on **#1019**)`. Add as the last physical line of `## Dependencies`: `- Related (NOT blocking): #1019, #987. No edge is asserted — #1019 already depends on this issue, so the reverse would create a cycle.` (`blocked_by(1019) = [591,599,589]` verified.)
- **#458** ⚑ (CONFIRMED). Head-plan F6 already carries `the §V.2 issue` / `the §V.2 products-and-equalizers issue` → `#416` with **no edge**. **Delta from my chunk:** if the optional Related line is added, write the real id — `#416 (\`maclane:V.2:thm1\`)` — not the head plan's ellipsis `maclane:V.2:...`.
- **#446** ⚑, **#433** ⚑ — already in head-plan F6, no delta.

**F4. Missing `Suggested module` line.** **#909** (CONFIRMED): insert as the first line of `## Work to be done`, immediately after the paragraph beginning `"This issue is the *categorical spine*"`: `Suggested module: \`Theory/Topology/Brouwer.v\`.` (The Verification block already names `coqc -R . Category Theory/Topology/Brouwer.v` and `Print Assumptions brouwer_fixed_point.`, a path that appears nowhere else in the body — every batch sibling #905–#914 carries a module line.)

**F5. Body must not open with a redundant H1** — ⚠unverified (LOW), **#640, #641, #642, #643, #644, #645, #646, #647** (the whole Awodey Ch1 filing block; verified mechanically as the only 8 whose first line equals `# ` + title). Delete the first two lines (H1 + the blank line after it) from each body so each begins with `## Source`. **Note the collision with Group D: #645 also gets the clause-partition line — apply both in one edit.**

**F6. Section-internal format defects.**
- **#627** ⚠unverified (LOW). Replace the two lines `Depends on: #626 (\`maclane:XII.4:def3\`)` and `Depends on: #626 (\`maclane:XII.4:def4\`)` with the single line `Depends on: #626 (\`maclane:XII.4:def3\`, \`maclane:XII.4:def4\`)`. **Leave the trailer unchanged** (resolver already de-duplicates; `blocked_by` = 626,283).
- **#639** ⚠unverified (LOW). Delete the leading line `None (builds on the in-tree \`SubobjectClassifier\`, which is already present).`, leaving the `Depends on: #683 — **serialization / name-ownership only, NOT a content prerequisite.** …` line as the section's only content. Trailer `"deps": ["#683"]` and `blocked_by = 683` already agree. (The #639/#653 `Structure/BooleanAlgebra.v` collision is already registered with creator 653 — no change there.)
- **#871** ⚠unverified (LOW). Collapse the double blank line after `## Dependencies` to one; insert a blank line between `> \`doc/plan/books/graph/serialize-groups.json\`; do NOT convert it into blocked-by edges.` and the `<!-- catalog: … -->` trailer.
- **#704** ⚠unverified (LOW). Insert a blank line between ``> `P X`.`` and ``Suggested module: `Instance/Sets/Powerset.v`.`` (GFM lazy continuation currently swallows the module line into the blockquote); collapse the double blank line after `## Work to be done`.

**F7. Trailer `deps` convention — ⚠unverified (LOW), BLOCKED on head-plan judgement call 7.** #953's finding proposes normalising five trailers to uniform `#N`: `#953 → ["#259","#458","#951"]`; `#956 → ["#220","#954"]`; `#963 → ["#259","#220","#954"]`; `#964 → ["#259","#951"]`; `#968 → ["#563","#954"]` (both item ids resolve to 954, so the array de-duplicates). Resolutions taken from `doc/plan/books/riehl/issue-map.json`. **Do not run this until the head plan's convention decision is made** — it assumes the `#N` form and would have to be reversed if item ids win. `ids` arrays and body text untouched either way.

---

## GROUP H — `doc/plan/books/graph/serialize-groups.json`

1. **Add** `{"module": "Instance/Rel.v", "issues": [262, 830, 854], "creator": null, "reason": "SERIALIZE_ONLY: three issues edit one existing file with no supply/consume relation between them. No dependency edge is asserted.", "rationale": "#262 adds the converse R† and Faithful Relation_Functor; #830 fixes the composition-order comment at :33 and resolves the dead block at :96-157 while building a separate Instance/FinRel.v; #854 (Rel_R, the prop of relations over a rig) also edits this file's header."}` (from #830).
2. **Add**, keyed on the construction not a path: `{"module": "Graph (construction, not a path: Instance/Parallel/Graphs.v vs Instance/Graph.v)", "issues": [705, 926], "creator": 705, "reason": "SAME IDENTIFIER under two paths — invisible to check_collisions.py, which matches paths. #705 Work item 1 names its category `Graph` in Instance/Parallel/Graphs.v; #926 names a different category `Graph` in Instance/Graph.v. No dependency edge is asserted (#926 builds its simple-graph variant itself).", "rationale": "The two cannot be imported together; #926 must rename (e.g. SimpleGraph). See judgement call 5."}` (from #926).
3. **Add** `{"module": "<numeric substrate: Instance/Poset/Numeric.v | Instance/ExtReal.v | Instance/Poset/Reals.v>", "issues": [759, 967, 1022], "creator": 759, "reason": "SERIALIZE_ONLY: the SAME real-number carrier built under three different module paths. No precedence; no dependency edge asserted. NOTE this group is invisible to check_collisions.py, which matches paths.", "rationale": "#759 builds ℤ/ℝ as Prosets over Coq.Reals in Instance/Poset/Numeric.v; #967 builds the extended real line as a complete-lattice poset in Instance/ExtReal.v; #1022 builds ℚ and ℝ>0 as thin categories in Instance/Poset/Reals.v. All three make the same Coq.Reals-vs-constructive substrate decision and all three commit to extending docs/AXIOMS.md. Found by reading; no mechanical check can see it."}` — **the `module` key is a placeholder, see judgement call 6.** Pair with the #1022 body edits (Group A-adjacent, below).
4. **Amend** the existing `Theory/Algebra/Monoid/Free.v` entry (currently `{296, 502}`) to record **#471** as a third claimant of the same *construction* under `Monad/Instance/List.v`: `"issues": [296, 471, 502]`, and append to `rationale`: `PATH NOTE: #471 (MacLane VI.4) builds the free-monoid MONAD W₀ under Monad/Instance/List.v — the same construction at (Sets, ×), invisible to the path-keyed check. #471 is now blocked_by 296; #502 is a Related pointer only.`
5. **Add** `{"module": "Powerset_Quantale (construction, not a path: Instance/Powerset/Quantale.v vs Instance/Quantale/Examples.v)", "issues": [780, 791], "creator": 780, "reason": "Ordered, not merely serialized: #791 is already blocked_by #780, which creates the symbol. Recorded because the two propose different paths for a literally identically-named artifact.", "rationale": "#780's Verification prints Powerset_MonoidalPreorder and Powerset_Quantale; #791's Verification prints Powerset_Quantale and Bottleneck_Quantale. #791 Work item 1 itself attributes both halves to #780 (7sketches:2.2.4:ex35 + 7sketches:2.5.2:ex94, both in #780's trailer ids)."}` (from #791).
6. **Add** `{"module": "⊗-word / canonical-map apparatus (construction, not a path: Construction/FreeMonoidal.v + Structure/Monoidal/Coherence.v vs Structure/Monoidal/Symmetric/Coherence.v)", "issues": [496, 497, 520], "creator": 497, "reason": "Ordered after remediation: #520 gains blocked_by 497, which transitively supplies #496. Recorded because #520's own XI.1 append claims the apparatus is 'subsumed in this issue's scope' under a third path.", "rationale": "Precedent: #608's Work says 'Reusing the inductive type of ⊗-words from the free-monoidal-category development (issue #496)' and its blocked_by is exactly [496]."}` (from #520).
7. **Add** `{"module": "numeric monoidal-preorder bases (Instance/Nat/Monoidal.v + Instance/Reals/Order.v vs Instance/Poset/Numeric/Monoidal.v)", "issues": [774, 775, 783], "creator": null, "reason": "Ordered, not merely serialized: #783 is already blocked_by both 774 and 775. Recorded because #783's Verification prints Nat_Add_MonoidalPreorder — the SAME symbol #775's Verification prints — under a third path.", "rationale": "#775 owns (ℕ,≤,0,+) in Instance/Nat/Monoidal.v; #774 owns (ℝ,≤,0,+) in Instance/Reals/Order.v; #783 must import both."}` (from #783).
8. **Amend** the existing `Instance/RQuiver.v` entry: #979's remediation drops that path from its Suggested-modules line and Verification block, so the entry's issue list must be updated accordingly. Its rationale (`"NEITHER builds the notion: that is #906"`) stays correct.
9. **No change** to the `Instance/Sets/Powerset.v` entry (`[227, 466, 704, 750, 871, 1005]`, creator 227) — it already homes the #704/#227 constraint correctly, which is precisely why the #227 edge is demoted rather than kept.

---

## (i) REFUTED — DO NOT ACT

| Finding | Why not |
|---|---|
| **#542** `dependency` — "the `Related:` note glued onto the end of the Depends-on line carries `maclane:VIII.4:prop2`, which resolves to #552, so the next resolver run would assert the cycle 542→552→542" | **REFUTED.** The line and the ledger mapping are quoted correctly (`maclane:VIII.4:prop2` → #552, `ledger.tsv:734`; `blocked_by(552)=[542,545]`), but the harvest risk does not exist. `check_graph.py`'s `REVERSE_CUE` alternation includes `feeds?`, and both `body_deps()` and `unresolved_deps()` truncate at the first cue; **#542's line is named verbatim in that file's header comment as the motivating example** (`"(b) trailing DOWNSTREAM prose on the same line, which points the other way … Reading (b) as a dependency INVERTS the arrow, which is how the phantom cycles appeared."`). Executed against the live line: `body_deps → [530]`, `unresolved_deps → []`. And `resolve_chapter_deps.py` — the only tool that creates native relations — takes edges **exclusively** from the trailer `deps` array, which here is `["maclane:VIII.1:remark2"]`. Editing a live issue to fix a risk the tooling deliberately absorbs is churn on real tracking data. **(The *other* #542 finding, the missing #417 edge, is CONFIRMED and IS in Group B.)** |
| **#564** `dependency` — "an appended DoD checkbox names #353 as supplying the intermediate diagram, but #353 appears on none of the three dependency surfaces" | **REFUTED.** The auditor computed only the first level of the graph. `blocked_by(564) = [584, 575, 420]` is correct, but `blocked_by(420) = [353, 419]`, so closure(564) = {584, 575, 420, 582, 581, **353**, 419, 427, 351} — #353 cannot be scheduled after #564 under any valid topological order, which is the only thing an edge buys. #420's own Dependencies read `Depends on: #353` / `Depends on: #419 (\`maclane:V.2:ex4\`)`, trailer `["#353","maclane:V.2:ex4"]`, donors line ending `"…and #353's limit functor"`. The discoverability concern also fails: the checkbox names `#353` in its own text. **The remediation's alternative branch — rewriting the checkbox to say "constructed via #420" — would be an actively wrong edit**, since #420 builds the category of diagrams and the limit FUNCTOR while #353 supplies `Δ ⊣ lim`, the adjunction Riehl's cheap proof route depends on. |

---

## (ii) JUDGEMENT CALLS — decide individually, do not script

1. **#818 — quantale-scoped collage vs #587's Sets-based collage.** Work item 4 asserts `"Record the relationship with #587's one-sided construction — that issue's category is this one applied to a profunctor out of the unit V-category."` That is **false as written**: #587's `C_K` has genuine hom-*setoids* (`"hom-setoids are C(a,b) on the old objects, K c for ∞ → c, empty for c → ∞, and a singleton for ∞ → ∞"`, reusing `Instance/Sets.v`), and `Sets` is not thin, so it cannot be an instance of a quantale-parameterised collage. **(a)** Generalize the base — DoD becomes `- [ ] The collage is a V-category over an arbitrary monoidal base with an initial object (the empty join when the base is a quantale), so that V = Sets is covered and #587's \`C_K\` is a genuine instance`, and the identification becomes a proved theorem. Costs real generality work. **(b)** Keep the quantale scoping — Work item 4 becomes `Record in the header the ANALOGY with #587's one-sided construction at V = Sets — the same four-block shape over a different base; the quantale collage does not specialise to it, since \`Sets\` is not thin`, DoD becomes `- [ ] The header states the analogy with #587's Sets-based collage and why it is not an instance`, **and the #587 edge is demoted** to `- Related (NOT blocking): #587 (the one-sided Bénabou collage at V = Sets — the same shape over a base this issue does not cover).` with trailer `["7sketches:4.2.2:def4.8","#799","#787"]` and native 818←587 dropped. Tradeoff: (a) buys a true theorem and keeps the edge; (b) is cheap but loses a stated deliverable. Secondary defect either way: `"is recorded"` is unverifiable as a DoD item — say whether a proof or a header note is required.
2. **#542 → #417 scheduling cost.** The edge is real (see Group B), but #542 is layer 1 and #417 is layer 2, so adding it reschedules #542 *and its dependents* — #546, #552, and through them #551/#555. The alternative is a descope: rename the Verification symbol `abelian_finitely_complete` → `abelian_has_equalizers` and record #417 as Related only. That weakens the issue below its own stated Mac Lane §VIII.3 remark. **Recommend the edge; decide knowingly.**
3. **#900 ownership of `surjectivity_is_epic`.** The finding wanted the #245 edge deleted (issue can build it itself → no edge); the adversarial pass **reversed** this and ruled #245 the owner, keeping the edge. Both readings are defensible under the campaign rules — the tie-breaker is whether #245's universe-scoped biconditional is the form #900 needs. **Apply the ADJUSTED version (keep the edge)** unless you decide otherwise; note this is the one place in my chunk where the campaign's "no edge if it can build it itself" rule was overridden.
4. **#704 — demote #227 fully, or keep it as an ordering edge?** The verdict demotes it (Work item 1 builds the same carrier; #704's own callout denies precedence). The verdict also allows keeping a creator-precedence edge as a *dispatch convenience* — but if you do, the body line **must** say `ordering only, NOT supply/consume` on its own line rather than asserting consumption. Pick one; do not leave the current text.
5. **`Graph` name clash (#705 / #926).** #705 names its directed-graph category `Graph` in `Instance/Parallel/Graphs.v`; #926 names a *different* simple-graph category `Graph` in `Instance/Graph.v`. Renaming #926's to `SimpleGraph` is the obvious call but touches #926's body, its DoD, and the Group H key. Structurally identical to the head plan's `Rng` (#362/#257) and `Grp` (#255/#440) clashes — **resolve it the same way you resolve those**, and in one edit touching both issues plus the serialize-groups key.
6. **#1022's serialize-group `module` key is a literal placeholder** (`"<numeric substrate: Instance/Poset/Numeric.v | Instance/ExtReal.v | Instance/Poset/Reals.v>"`). One of the three must be nominated as the carrier path, and whichever lands first owns the `docs/AXIOMS.md` entry (all three commit to extending it, all three make the same `Coq.Reals`-vs-constructive decision). Decide the path before writing the entry.
7. **#471 — is `blocked_by 296` the right instrument at all?** The verdict rejected #502 (heaviest possible prerequisite: no `icoprod` in-tree) and substituted #296. But by the campaign's own test, #471 could also discharge "lawful `W₀` on Sets + `Mon ≅ EM`" by building the list monad directly, which would make **any** edge fabricated. The verdict's judgement is that a second free-monoid construction is the worse outcome. Accept or drop the edge — but if you drop it, keep the `- Related (NOT blocking)` line naming both #296 and #502.
8. **#570 mutates `ledger.tsv` (lines 790–791, `PARTIAL` → `ABSENT`)** on a LOW, unverified finding. Head plan §6 already warns that ledger row numbers cited in findings were not confirmed. Verify the two rows before editing; the schemas.md rule quoted (`"a never-instantiated class does not qualify"`) is the right rule, but the row indices are inherited.
9. **F7 (#953 + #956/#963/#964/#968 trailer normalisation) is gated on head-plan judgement call 7.** My chunk supplies the five concrete resolutions but presupposes the `#N` form throughout. **Do not run F7 before the convention is fixed.**

---

<!-- ===== section 2 ===== -->

# Remediation plan — tail chunk `findings-tail-0.json`

**Coverage: issues #432 – #1038 (70 distinct issues). 81 findings read; 63 carry an adversarial verdict, 18 do not (all LOW — flagged inline as UNVERIFIED).** Group letters follow `doc/plan/books/qa/plan-head-216-471.md`. Where a finding restates a head-plan row I say so and do not re-litigate; the deps-array convention, the `Instance/Mod` vs `Instance/Module` clash and the `Rng` name clash are the head plan's to decide.

---

## GROUP A — Demote fabricated or over-asserted hard edges

Mechanical shape per head plan §3: (i) delete the `Depends on:` line; (ii) add on its own physical line `- Related (NOT blocking): …`; (iii) `gh api -X DELETE repos/jwiegley/category-theory/issues/<N>/dependencies/blocked_by/<M>`; (iv) drop the entry from the trailer `deps`.

| Issue | Edge | Delete this line (verbatim) | Insert this line | Trailer after | Native after |
|---|---|---|---|---|---|
| **#652** | #247 | The whole `## Dependencies` body is ONE physical line: `Depends on: #653 (\`awodey:2.2:def-boolean-algebra\`) (the \`Bool\` witness needs the category of Boolean algebras). The uniqueness-up-to-iso half of Awodey's initial/terminal discussion is handled separately (Proposition 2.8 → #247).` | Split into two lines: L1 `Depends on: #653 (\`awodey:2.2:def-boolean-algebra\`) — the \`Bool\` witness needs the category of Boolean algebras.` L2 `- Related (NOT blocking): the uniqueness-up-to-iso half of Awodey's initial/terminal discussion (Proposition 2.8) is handled on #247.` | unchanged (`["awodey:2.2:def-boolean-algebra"]`) | `[653]` |
| **#698** | #231 **and** #503 | `Depends on: #503 (MacLane VII.3: Finite products in the category of monoids) —\nfor the category of monoids over \`Sets\`` and `Depends on: #231 (MacLane I.3: Full and faithful functors, subcategories, and\nreflection of monics)` | — (no Related line needed; #698's Work item 3 consumes only pre-existing `Theory/Functor.v:331`/`:342`) | `["#220","#219","#223","#255","#641"]` | `[220,219,223,255,641]` |
| **#854** | #262 | `Depends on: #262 (Rel, converse relations, and the graph embedding — the\nambient relational development).` | `- Related (NOT blocking): #262 — the converse and the faithfulness of \`Relation_Functor\` on the ambient \`Rel\`.` | `["7sketches:5.3.1:def5.36","7sketches:5.2.1:def5.2","7sketches:5.3.3:def5.50"]` | keep `[839,826,843]` |
| **#858** | #823 | `Depends on: #823 (consequences of compact closure — monoidal closedness,\nuniqueness of duals, and the double dual — the compact-closed vocabulary).` | `- Related (NOT blocking): #823 — consequences of compact closure (monoidal closedness, uniqueness of duals, double dual).` | `["7sketches:5.4.3:def5.79","7sketches:5.4.3:def-sfg-plus"]` | keep `[854,855]` |
| **#885** | #402 | `Depends on: #402 — the classifier of \`Sets\` with \`Ω = {true, false}\` and the classification rule the truth tables are read against.` | `- Related (NOT blocking): #402 — the \`Sets\` classifier; the truth tables here are checked at \`FinSet_Topos\` where \`Ω = 2\`, so nothing waits on it.` | `["#445","#639"]` | `[445,639]` |
| **#892** | #402 | `Depends on: #402 — the classifier of \`Sets\`, the other side of the reconciliation.` | `- Related (NOT blocking): #402 — if it ever delivers a genuine \`SubobjectClassifier Sets\`, the header disclosure this issue writes can be retired; the issue does not wait on it.` | `["7sketches:7.3.3:def7.35","7sketches:7.4.1:eq7.50","#259"]` | `[890,894,259]` |
| **#907** | #248 | `- Depends on: #248 (groupoids and the structure of connected groupoids — supplies the \`IsGroupoid\` predicate the maximality clause is best stated against)` | `- Related (NOT blocking): #248 — once its \`IsGroupoid\` predicate lands, restate the maximality clause as "the largest wide subcategory satisfying \`IsGroupoid\`"; the clause itself is provable without it.` **plus** set the Dependencies body to `None.` | `[]` | empty |
| **#925** | #276 | `- Depends on: #276 (functor categories over discrete shapes and their size)` | `- Related (NOT blocking): #276 (functor categories over discrete shapes and their size) — adjacent size material in \`Instance/Fun/Discrete.v\`; its Cantor-diagonal size remark and this issue's local-smallness closure theorems are independent, and neither consumes the other.` | `["#253"]` | keep `[253]` |
| **#971** | #232 (**backwards** — #232 CONSUMES a field category) | `Depends on: #232 (MacLane I.3: The field of quotients as a functor) — the first place a category of fields is required.` | `- Related (NOT blocking): **#232** and **#307** both need a category of fields; this issue builds it (\`Instance/Field.v\`) and they should consume it. No dependency edge is asserted.` | `["#226"]` (retain #226) | keep `[226]` |
| **#1002** | #259 | `Depends on: #259 (\`Top\`, needed only for the scoped-out contrapositive)` | `- Related (NOT blocking): **#259** (\`Top\`) is needed only for the contrapositive that \`Top\` and \`Poset\` are not monadic over \`Set\`, which the DoD already permits scoping out. No dependency edge is asserted.` | see combined below | — |
| **#1002** | #481 | `Depends on: #481 (reflection of coequalizers — the reflection vocabulary this issue extends)` | `- Related (NOT blocking): **#481** may create \`Structure/Limit/Reflection.v\`; if it lands first, put the two \`ReflectsIsos\` lemmas there instead of in \`Structure/Limit/Preservation.v\`. #1002 consumes no coequalizer-reflection result. Recorded as a same-file peer in \`doc/plan/books/graph/serialize-groups.json\`; no dependency edge is asserted.` | — | — |
| **#1002** | #1004 | `Depends on: #1004 (\`riehl:5.5:def5\`) (the notion "category of models for an algebraic theory", in which Corollary 5.6.4 is stated)` | `- Related (NOT blocking): **#1004** defines \`AlgebraicCategory\`; Corollary 5.6.4 is stated here over the in-tree \`Models T Sets\` / \`ev1\` vocabulary and does not consume that predicate.` — **judgement call, see §J6** | after all three: `"deps": []` | `[]` |

**#1002 A-edits are gated on GROUP H item 6.** The verdict is explicit: `Structure/Limit/Reflection.v` does not exist (`Structure/Limit/` holds only `Cartesian.v, Kan/, Preservation.v, Product.v, Terminal.v, Weighted.v`), `check_graph.py`'s `creates()` keeps non-existent paths, and both #481 and #1002 name it inside a scanned section — with the edge gone and no serialize-group entry, **invariant 4 starts failing**.

**#750 → #704** — demotion is defensible on the merits (#750 needs the *covariant* powerset; #704 is explicitly the contravariant one) but is LOW, UNVERIFIED, and overrides a recorded prior decision. See §J1.

---

## GROUP B — Add missing hard edges

Shape: `Depends on: #N (\`item-id\`) — <reason>` on its own physical line; add the id to the trailer `deps`; `gh api -X POST …/dependencies/blocked_by`.

| Issue | Add | Exact line | Verdict / note |
|---|---|---|---|
| **#453** | #438, #353 | `Depends on: #438 (\`maclane:V.6:lem1\`) — supplies the comma-projection creation result that Work item 1's monos lemma runs through` / `Depends on: #353 (\`maclane:IV.2:construction2\`) — supplies the Δ-adjunction characterization of colimits consumed by the Riehl §4.7 cocompleteness-corollary checkbox` | ADJUSTED. **Head-plan duplicate**, but the head plan's line for #353 omits the id; the correct id is `maclane:IV.2:construction2` — the finding's original text wrote a literal ellipsis `maclane:IV.2:...` into a live body. Cycle-safe: reach(438)={406}, reach(353)={351}. |
| **#459** | #458 | `Depends on: #458 (\`maclane:V.9:remark2\`) — supplies coequalizers in \`Top\` with the quotient topology, from which the collapse \`X/A\` is formed` | CONFIRMED. Head-plan duplicate. Donor line verbatim: `In-tree donors: #259's \`Top\`, the §V.9 Top-cocompleteness issue (for the quotient/coequalizer topology),`. The edge also buys #457 transitively. |
| **#466** | #227 | Replace `## Dependencies` body `None (self-contained over \`Instance/Sets.v\` and the Eilenberg–Moore machinery).` with `Depends on: #227 (\`maclane:I.3:construction1\`) — creates \`Instance/Sets/Powerset.v\` with the covariant power-set functor this monad is built on.`; delete the buried line `Depends on: #227 — creates \`Instance/Sets/Powerset.v\` with the covariant power-set functor that this monad is built on.` from inside the Awodey Ex. 10.6.5 block. | CONFIRMED. Head-plan duplicate (buried-edge hoist). Trailer + native already correct. |
| **#493** | #285 | Replace `## Dependencies` body `None.` with `Depends on: #285 (\`maclane:II.5:ex5\`) — builds \`Theory/EckmannHilton.v\`, the abstract two-unital-operations-with-interchange theorem this issue applies at \`End(e)\`.` Trailer → `"deps":["#285"]`; POST blocked_by 285. | CONFIRMED. #493's own Current state: "The Eckmann–Hilton argument itself is proved in `Structure/Semiadditive.v` … **but in the biproduct-convolution setting, on `hom(x, y)`, not on `End(e)`**". closure(285)=∅. |
| **#516** | #557 | Insert immediately BEFORE `Depends on: #515`: `Depends on: #557 (\`maclane:VIII.4:def-chain-complex\`)`. Trailer → `["maclane:VII.5:def-simplicialobject","maclane:VII.5:construction-geometric","#256","#259","#557"]`; POST blocked_by 557. | CONFIRMED. No cycle (557→550→544). Paired with the Group C rewrite below. |
| **#430** | #540 | Add `Depends on: #540` to **#430** (not #540). | CONFIRMED. #540 has empty closure so 430→540 is cycle-free; layers 540=0, 430=2. |
| **#559** | #417 | Replace `## Dependencies` body `None.` with `Depends on: #417 (the finiteness predicate on an index category — the cocone lemma and Riehl's cone-under-every-finite-diagram form both quantify over it).` Trailer `[]` → `["#417"]`; POST blocked_by 417. | CONFIRMED. closure(417)={335,326,416,427}, no cycle. **Scheduling cost is real — see §J3.** |
| **#579** | #321 | Replace `## Dependencies` `None.` with `Depends on: #321 (\`maclane:III.4:def4\` — supplies \`power\`/\`copower\` in \`Structure/Limit/Power.v\`)`. Trailer → `{"ids":["maclane:IX.6:def2","maclane:IX.6:ex5"],"deps":["#321"]}`; POST blocked_by 321. | CONFIRMED. #593/#594/#598 already declare #321 for exactly this artifact. |
| **#600** | #447 | Replace the whole `## Dependencies` body `None in-catalog. Related: the set-valued density theorem is filed as issue #346 (a special case).` with THREE physical lines: `Depends on: #447 (\`maclane:V.7:def4\` — defines the generating/separating family and the jointly-faithful-representables characterization in \`Structure/Generator.v\`)` / `- Related (NOT blocking): #346 (the set-valued density theorem — a special case of the density notion defined here).` / `- Related (NOT blocking): #712 (the full-image factorization of a functor — the construction Work item 5 consumes).` / `- Related (NOT blocking): #918 (the *essential* image, i.e. the replete closure of #712's full image — a distinct subcategory, recorded for cross-reference only).` Trailer `[]` → `["#447"]`; POST blocked_by 447. | CONFIRMED + ADJUSTED. **The #712/#918 Related lines must be SEPARATE** — the original blended line was adjusted because #918's essential image is a different subcategory. |
| **#643** | #220 | Add a second physical line: `Depends on: #220 (MacLane I.2 — the delooping \`B G\` in \`Construction/Deloop.v\` and the \`hom(a,a)\` monoid, both consumed by the Riehl §1.1 and §2.2 checkboxes).` Trailer `["#255"]` → `["#255","#220"]`; POST blocked_by 220. | CONFIRMED. #643's own checkboxes name #220: "(its monoid structure is the `hom(a,a)` monoid of #220)" and "the delooping `B G` (issue #220)". `grep -rni deloop` over `.v` returns only prose. |
| **#649** | #648 (declare an already-harvested edge) | Split the single physical line into: `Depends on: #296 (MacLane II.7 — the free monoid and its universal property).` / `Depends on: #648 (the category **Mon** at \`Instance/Mon.v\`, on which this issue's \`Instance/Mon/NatInt.v\` is built).` Trailer → `{"ids": ["awodey:2.1:example5", "riehl:1.6:exv"], "deps": ["#296", "#648"]}`. **Leave native `[296,648]` as-is.** | CONFIRMED with a **coordination constraint — see §J2**. The 648 was harvested from `Related: the Awodey 2.1 "monos in Mon are injective" issue (#648 …) shares the **Mon** infrastructure.` glued onto the Depends-on line. |
| **#667** | #641 | Add a second physical line: `Depends on: #641 (Awodey 1.4 — the category **Pos** of posets and monotone maps).` Trailer → `{"ids":["awodey:4.1:example3"],"deps":["#343","#641"]}`; POST blocked_by 641. | ADJUSTED — pair with the Group E Current-state rewrite (the ordered-group leg additionally needs a `@CartesianMonoidal` structure on **Pos** that no issue schedules). |
| **#815** | #812 | Add after the #813 entry: `Depends on: #812 (\`7sketches:4.2.2:example4.11\`) (the profunctor generated by a raw relation, which this instantiates at the cost base).` Trailer → `["7sketches:4.2.2:def4.8","7sketches:4.3.1:def4.21","7sketches:4.2.2:example4.11","#781","#787","#789","#790"]`; POST blocked_by 812. | CONFIRMED. No cycle (#812's deps: 811, 813, 789, 768). **Must be paired with the #812 generalisation in Group C/D** — as filed, #812 is Bool-only and #815's DoD demands "the general generated profunctor", which is homed nowhere. |
| **#1029** | #1028 | Insert immediately after the #1027 line: `Depends on: #1028 (\`riehl:6.4:def5\`) (the homotopy category and the localization functor — the construction Work item 2 instantiates at spaces)`. Trailer → `{"ids":["riehl:6.4:example7","riehl:6.4:example8"],"deps":["riehl:6.4:def3","riehl:6.4:def5","#557","#259","#260"]}`; native `[259,260,557,1027]` → `+1028`. | CONFIRMED. #1027 builds no localization; #1029's donor list names `Construction/Quotient.v` (#1028's donor) and NOT `Construction/Localization.v`. Cycle-free (#1028's blocked_by = [1027], which #1029 already has). |

---

## GROUP C — Rewrite Work item to CONSUME, not rebuild

| Issue | Rebuilt artifact | Owner | Exact edit |
|---|---|---|---|
| **#440** | the identifier `Grp` | #255 | **ADJUSTED — supersedes the head plan's #440 Group C row.** Replace the tail of Work bullet 1, `instantiate it at the group signature so \`Grp := ⟨GroupOp, GroupEq⟩-Alg\` is a genuine category (coordinate with #255).`, with: `instantiate it at the group signature to obtain \`GroupVariety := ⟨GroupOp, GroupEq⟩-Alg\` as a genuine category, under a name that does NOT collide with #255's \`Grp\` (\`Instance/Grp.v\`) — do not introduce a second identifier named \`Grp\`. If #255 has already landed, also prove \`GroupVariety ≅[Cat] Grp\`; otherwise record that comparison obligation in the file header for whichever of the two lands second.` Replace `None.` in Dependencies with `- Related (NOT blocking): **#255** (\`maclane:I.6:construction2\`) — owns the identifier \`Grp\` in \`Instance/Grp.v\`; this issue must not define a rival \`Grp\`, and the comparison equivalence belongs to whichever lands second.` **Rationale for the change from the head plan: the head-plan wording makes "prove `GroupVariety ≅[Cat] Grp` once #255 has landed" a Work deliverable while declaring the relation non-blocking — a hidden blocker.** |
| **#466** | the covariant power-set endofunctor | #227 | Replace Work bullet 1 `- Build the covariant power-set endofunctor on \`Sets\` (donor: \`Instance/Sets.v\`; over setoids, use the setoid of subsets/predicates up to \`iff\`) with \`ret\` = singleton and \`join\` = union; discharge the \`Monad\` laws.` with `- Reuse #227's covariant power-set endofunctor \`P\` on \`Sets\` (\`Instance/Sets/Powerset.v\`, predicate setoid compared by pointwise \`iff\`, direct-image action) and take \`ret\` from #227's singleton transformation \`Id ⟹ P\`; add \`join\` = union and discharge the \`Monad\` laws. Do not re-define the carrier, the functor action or the unit.` Leave the Suggested-module line as-is. |
| **#503** | the **Grp** half of the finite product | #343 | Change the append's trailing parenthetical `(the group case is the evident analogue)` to `(the group case is owned by #343, which builds \`Grp(C)\` and its finite products and already depends on this issue — do not duplicate it here)`. UNVERIFIED (LOW). |
| **#516** | chain complex + homology object | #557 | (a) Work clause `(a) Define a chain complex in an abelian category and the homology functor \`Hₙ = ker/im\`; over a simplicial object \`S\`` → `(a) Over #557's \`ChainComplex\` and \`homology_obj\` in an abelian category, and over a simplicial object \`S\``. (b) DoD: delete `- [ ] Chain complex and homology functor in an abelian category defined.`, insert `- [ ] The alternating-sum boundary exhibits a simplicial object as a \`ChainComplex\` in #557's sense; no second chain-complex or homology definition is introduced.` (c) Rename `Structure/Homology.v` → `Instance/Simplicial/Homology.v` in Work and Verification (singular stays at `Instance/Singular.v`). (d) **Propagate**: #517 line 17 and #519 line 17 both list `Structure/Homology.v` as a donor — retarget both. See §J8 for the path choice. |
| **#430** | free abelian group + UMP | #540 | Replace Work bullet 1 `Construct the free abelian group functor \`Sets ⟶ Ab\` (finitely supported integer-valued functions, the biproduct-style free construction over \`Instance/CMon/Biproduct.v\`), with its universal property.` with `Consume #540's free abelian group and its universal property (\`Construction/FreeAb.v\`), bridging to \`Instance/Ab.v\`'s carrier if the representations differ, and package it as the functor \`Sets ⟶ Ab\`; do not re-derive the universal property.` **The bridging clause is the verdict's addition** — #540 builds in `Theory/Algebra/` or over `Instance/Comp.v` `Group`, #430 needs an object of #256's `Ab`. Also add to #540's Dependencies: `- Related (NOT blocking): #430, which also constructs the free abelian group functor \`Sets ⟶ Ab\` with its universal property; build it ONCE here and have #430 consume it.` |
| **#536 / #336** | finite n-ary matrix calculus | #536 | On **#536**, after the existing Dependencies text (`None filed. Uses the binary biproduct (present); benefits from the biproduct bifunctor issue \`maclane:VIII.2:construction1\`.`), add on its own physical line: `- Related (NOT blocking): #336, which scopes the same n-ary identity-matrix comparison and the matrix-composition formula over the weaker bicartesian hypotheses; prove \`matrix_compose\` ONCE here and have #336 cite it.` On **#336**, append to the checkbox beginning `- [ ] (from Riehl §3.1) The general composition formula for matrices of components`: `— owned by #536 (\`matrix_compose\`); cite it rather than re-proving`; and to the checkbox beginning `- [ ] (from Riehl §3.1, direct sums) The identity-matrix comparison`: `— the n-ary biproduct object \`⊕_{i∈I} A_i\` is #536's first Work item; consume it.` **Related-only, not an edge** (the hypotheses genuinely differ; see §J9). |
| **#546** | `PreservesFiniteLimits` | #1013 | **ADJUSTED — prose only, NO graph edit.** In the third `### Also covered by` checkbox, replace `(new Riehl E.4 definition issue)` with `(#1013)` and replace `Two rival predicates must not be created; whichever issue lands first defines it and the other consumes it.` with `#1013 owns \`PreservesFiniteLimits\` in \`Structure/Limit/Finite.v\` (its DoD makes the named predicate a required deliverable); this issue CONSUMES it and must not define a rival.` In Dependencies, on a NEW physical line after the existing text: `- Related (NOT blocking): #1013 (owns the named \`PreservesFiniteLimits\` predicate — consume it); #417 (builds the finite-index-category predicate and \`FinitelyComplete\`, which #1013 deliberately does not use).` **Do NOT add `Depends on: #417`, and do NOT add 546 to the `Structure/Limit/Finite.v` serialize group** — see DO NOT ACT. |
| **#579** | copowers `S · A` | #321 | Work item 2 → `- Using the copower \`S · A\` supplied by #321 (do NOT redefine it), define \`∫^c Sc · Tc\` for \`S : C^op ⟶ Set\`, \`T : C ⟶ D\` with \`D\` cocomplete; prove it is a bifunctor \`Set^{C^op} ∏ D^C ⟶ D\`.` Also in Work item 3's donor list, move the annotation: `\`Structure/Limit.v\` (for copowers)` → `\`Structure/Limit.v\`` plus a new entry `\`Structure/Limit/Power.v\` (for copowers)`. |
| **#587** | — (peer cross-reference) | — | Add as the last physical line of Dependencies (currently `None.`): `- Related (NOT blocking): #818 (the collage of a V-profunctor, \`Construction/Enriched/Profunctor/Collage.v\`). This issue's \`C_K\` is that construction at \`V = Sets\` for a profunctor out of the unit; keep the two files distinct and cross-reference them in both headers. No edge is asserted — neither construction is a prerequisite of the other.` The link exists one-sidedly: #818 names #587 twice, #587 never names #818. |
| **#592** | `P(M)` | #227 | Add on its own physical line in Dependencies: `- Related (NOT blocking): #227 (designated creator of \`Instance/Sets/Powerset.v\` per \`serialize-groups.json\`) — consume \`P(M)\` from there for the \`2^M ≅ P(M)\` step rather than defining a seventh powerset.` UNVERIFIED (LOW); promotion to a hard edge is §J10. |
| **#600** | generator/separating family; image subcategory | #447; #712 | Work item 6 → `- Prove Exercise 4 over #447's \`Generator\`: the objects of \`M\` generate \`C\` iff \`c ↦ C(K−, c)\` is faithful — do NOT redefine the separating family here.` Work item 5 → `- Prove Exercise 3: density descends to the image subcategory \`K M\`. Consume the full-image subcategory from #712 (\`Construction/Functor/FullImage.v\`, Work item 2: the full subcategory of \`D\` spanned by the objects in the image of \`K\`) — do NOT introduce a third image-subcategory construction. Note that #918's *essential* image is the replete closure and is a different subcategory; use it only if the statement is deliberately restated up to isomorphism.` **ADJUSTED: do NOT write "from #712 or #918 (whichever lands first)".** |
| **#623** | `Monoidal Cat` | in-tree `Cartesian_Monoidal` | Work item 1 → `- Name the monoidal structure on \`Cat\`: \`Definition Cat_Monoidal : @Monoidal Cat := @Cartesian_Monoidal Cat Cat_Cartesian Cat_Terminal\` in \`Instance/Cat/Monoidal.v\`, with the universe placement checked.` DoD `- [ ] CLAUDE.md Key Files index updated (a \`Monoidal Cat\` instance is a reusable, flagship-level addition).` → `- [ ] CLAUDE.md Key Files index updated (the \`Cat\`-enriched = 2-category equivalence is the flagship result; the \`Monoidal Cat\` naming is a one-liner over \`Cartesian_Monoidal\`).` **The verdict compiled the probe** — `Definition Cat_Monoidal_probe : @Monoidal Cat := @Cartesian_Monoidal Cat Cat_Cartesian Cat_Terminal.` builds on Rocq 9.1.1 and prints "Closed under the global context". (Current-state edit in Group E.) |
| **#425** | `Cocomplete D → Cocomplete [C,D]` | #715 | Edit **#425** Work item 2 from `Corollary (Corollary 1): \`Complete X → Complete [P, X]\`, and the \`Cocomplete\` dual by the same construction over the opposite categories.` to `Corollary (Corollary 1): \`Complete X → Complete [P, X]\`. (The \`Cocomplete\` dual is #715's deliverable in \`Instance/Fun/Colimit.v\` — do not duplicate it here.)` Leave #715 and the #715←#425 edge unchanged; **add no serialize-group entry**. **Verdict extra:** while in #425, narrow its Riehl §3.4 checkbox `- [ ] (from Riehl §3.4) \`ev_a : [A, C] ⟶ C\` preserves limits and colimits` to the LIMIT side — it duplicates #715 Work item 3 / DoD (`PreservesAllColimits` for evaluation). |
| **#719** | the currying adjunction and both functors | #239 | Collapse Work items 1–2 into `1. Consume #239's \`(− × b) : C ⟶ C\`, \`(−)^b : C ⟶ C\` and the \`Adjunction (− × b) ((−)^b)\` record from \`Structure/Cartesian/Closed/Adjunction.v\` (whose unit half is #682's). Do not redefine any of the three; this issue's own increment starts at item 3.` Renumber 3–5 → 2–4. DoD `- [ ] \`(− × b) ⊣ (−)^b\` exists as a genuine \`Adjunction\` between functors` → `- [ ] #239's \`(− × b) ⊣ (−)^b\` \`Adjunction\` is consumed unchanged, so the general RAPL/LAPC machinery applies to it`. **ADJUSTED — two further surfaces must move or the defect survives:** (i) Suggested modules → `Suggested modules: \`Structure/Cartesian/Closed/Adjunction.v\` (created by #239) for the preservation theorem; instantiations may live beside the \`Sets\` and functor-category closed structures.`; (ii) drop `Print Assumptions prod_right_adjoint.` from Verification, keeping `Print Assumptions prod_PreservesAllColimits.` |
| **#727** | `O(X)`, the poset of opens | #685 | Amend the #268 dependency line to `Depends on: #268 (the presheaf of continuous functions over \`Open X\`; NOTE it builds \`Open X\` in \`Instance/Top/Presheaf.v\` while #685 builds the same opens in \`Instance/Top/Opens.v\` — consume whichever landed, do not build a third).` (+ Group H entry). |
| **#730** | `HasPullbacks Sets`; the base-change adjunction | #333; #387 | (a) Work bullet 1 → `- Consume #333's \`HasPullbacks Sets\` from \`Instance/Sets/Pullback.v\` to discharge \`Star_Functor\`'s \`pullbacks\` hypothesis; do not construct it here.` (b) DoD `- [ ] \`HasPullbacks Sets\` constructed` → `- [ ] #333's \`HasPullbacks Sets\` is consumed to discharge \`Star_Functor\`'s hypothesis`. (c) delete `coqc -R . Category Instance/Sets/Pullback.v` from Verification. (d) Work bullet → `- Consume #387's \`Bang_Functor f ⊣ Star_Functor f\`, and delete the commented-out wrong-direction stub at \`Construction/Slice/Pullback.v:114-127\` rather than leaving it in place.`; DoD → `- [ ] #387's \`Bang_Functor f ⊣ Star_Functor f\` is consumed, and the commented-out stub with its wrong-direction statement is removed from \`Construction/Slice/Pullback.v\``. (e) **Drop `Print Assumptions Bang_Star_Adjunction.`** — #387 exports the constant as `Base_Functor_Adjunction`; keep `Star_Pi_Adjunction`. |
| **#757** | `sub_reindex_monotone` | #670 | Work item 5 → `5. Cite, do not re-prove, the categorical companion: \`sub_le u v → sub_le (sub_reindex f u) (sub_reindex f v)\` is #670's Work item 3 and lands with it in \`Theory/Subobject/Functor.v\`; reference it from the header as the categorical form of step 3.` DoD → `- [ ] The header cites #670's \`sub_reindex_monotone\` as the categorical form of step 3 (the lemma itself is #670's deliverable, not duplicated here)`. Drop `Print Assumptions sub_reindex_monotone.` from Verification and `coqc -R . Category Theory/Subobject/Functor.v` from the first code block. Dependency line → `Depends on: #670 — monotonicity of subobject reindexing, the categorical companion this issue's step 3 mirrors at the order level.` **Keep the edge** — the header citation is still a real consumption. |
| **#770** | `bijective f ↔ IsIsomorphism f` in `Sets` | #645(a) | Work item 3 → `3. Package #645(a)'s \`Sets\` characterisation under a named \`bijective\` predicate — do NOT re-prove either direction; #645 part (a) already delivers \`IsIsomorphism h ↔ (injective h ∧ surjective h)\`. Disclose in the header the difference between the book's bare surjectivity and \`Lib/Setoid.v:121\`'s split, choice-carrying form, and state which one #645's characterisation uses.` DoD → `- [ ] \`bijective\` introduced as a named predicate wrapping #645(a)'s \`Sets\` characterisation, with the split-vs-bare surjectivity difference disclosed in the header (no re-proof of either direction)`. Dependency line → `Depends on: #645 — part (a) proves the \`Sets\` iso ↔ bijection characterisation this issue names.` |
| **#774** | the ordered ℝ carrier | #759 | Delete Work item 2 (`2. Introduce the ordered additive reals as an \`Instance/\` carrier (\`Coq.Reals.Rdefinitions\` + \`RIneq\`), with \`Rle\` as the preorder relation, and derive the thin category through \`Instance/Proset.v:33\`.`) and renumber; insert `2. Consume #759's ℝ carrier from \`Instance/Poset/Numeric.v\` — the \`SetoidObject\`, the \`Rle\` \`Proset\` and the docs/AXIOMS.md disclosure are #759's deliverables; this issue adds no new real-number carrier.` Suggested modules → `Structure/Monoidal/Preorder/OrderedMonoid.v` plus a thin `Instance/Reals/Monoidal.v`. Drop `coqc -R . Category Instance/Reals/Order.v`; keep `Print Assumptions Reals_Additive_MonoidalPreorder` against the new path. DoD → `- [ ] The instantiation reuses #759's ℝ \`Proset\` and inherits its already-recorded \`Coq.Reals\` axiom footprint (docs/AXIOMS.md is cited, not re-extended); the recipe itself is verified 'Closed under the global context'`. |
| **#778** | `Two_Initial`/`Two_Cocartesian`/`two_join`; the cocartesian-monoidal + symmetric bridge | #756; #490 | First clause of Work item 1: `1. Build \`Two_Initial\` and \`Two_Cocartesian\` on \`_2\` (bottom \`TwoX\`, join \`two_join\`), then supply the missing general bridge:` → `1. **CONSUME, DO NOT RE-CREATE** \`Two_Initial\`, \`Two_Cocartesian\` and \`two_join\` on \`_2\` — #756 builds all three (in \`Instance/Two/Monoidal.v\` or \`Instance/Two/Boolean.v\`) and this issue is already blocked on it. Import them; do not add a second copy in \`Instance/Two/Join.v\`. Then **CONSUME, DO NOT RE-CREATE** the generic bridge: #490 owns \`Cocartesian_Monoidal : @Monoidal C\` in \`Structure/Monoidal/Cocartesian.v\` AND, via its appended Seven Sketches §6.2.2 checkbox, the \`Cocartesian_BraidedMonoidal\`/\`Cocartesian_SymmetricMonoidal\` upgrades dual to \`CC_SymmetricMonoidal\`; this issue is already blocked on #490. Apply them at \`_2\`; add no generic bridge and no symmetry layer here.` DoD `- [ ] A generic "coproducts + initial ⇒ symmetric monoidal" bridge is added (dual of \`CC_Monoidal\`), not a bespoke \`_2\` construction.` → `- [ ] The join structure on \`_2\` is packaged monoidally by APPLYING #490's generic \`Cocartesian_Monoidal\`/\`Cocartesian_SymmetricMonoidal\`; no bespoke \`_2\` construction, and no second bridge or symmetry layer, is introduced here.` **ADJUSTED: the finding's original "add ONLY the missing symmetry layer" would have created a NEW duplicate** — #490 already owns `Cocartesian_SymmetricMonoidal` via its Seven Sketches §6.2.2 checkbox and its Riehl §E.2 clause (b). |
| **#801** | the order-theoretic AFT | #737 | Replace the final sentence of Work item 3 — `Route the argument through #737's order-theoretic adjoint functor theorem if that has landed, and otherwise prove it directly and note the overlap.` — with `Route the argument through #737's order-theoretic adjoint functor theorem, which is a hard dependency of this issue and already proves that a join-preserving monotone map has a right adjoint given by the explicit join formula; instantiate it at \`− ⊗ v\` rather than re-deriving it.` |
| **#812** | (generalisation required by #815) | — | Work item 1 `Define the profunctor generated by a raw relation between the carriers of two preorders, as the composite of the source's unit profunctor, the relation and the target's unit profunctor` → `Define, over an arbitrary quantale base, the V-profunctor generated by a raw V-matrix between two V-categories, as the composite of the source's unit profunctor, the matrix and the target's unit profunctor; specialise it to V = Bool as the bridge profunctor of Example 4.11.` (+ the DoD checkbox in Group D). |
| **#829** | `CorelComposable` + the monoidal transport | #824 | Apply steps (2)–(4) as written: (2) second Work bullet → `- In \`Instance/FinSet/Corel.v\`, consume #824's \`FinSet_CorelComposable\` instance and its transported symmetric monoidal structure on \`CorelCat FinSet\` — do NOT re-derive either — and assemble \`FinCorel_PROP : PROP\` together with the strict refinement of §5.2.1 (objects are \`nat\` definitionally).`; (3) delete DoD `- [ ] \`CorelCat\` inherits the symmetric (strict, on a skeletal base) monoidal structure from \`CospanCat\`, proved rather than assumed.` and rewrite the next to `- [ ] \`FinCorel_PROP : PROP\` with the strict refinement of §5.2.1, built on #824's composability instance and monoidal transport.`; (4) Verification → `coqc -R . Category Instance/FinSet/Corel.v`. **Step (1) — deleting the general lemma — only as a PAIRED edit: see §J4.** |
| **#865** | the indexed-coproduct file | #320 | Work item 3: delete everything from `; and, for parity with \`Structure/Limit/Product.v\`,` through `\`Instance/Discrete.v\`'s \`DiscreteCat\`.`, leaving `3. \`Cocartesian_Colimit (C : Category) : (∀ F : Two_Discrete ⟶ C, Colimit F) ↔ @Cocartesian C\`, dual to \`Cartesian_Limit\`.` DoD → `- [ ] \`Cocartesian_Colimit\` proved (the indexed-coproduct dual of \`Structure/Limit/Product.v\` is #320's scope, not this issue's).` Keep native `[320]`. |
| **#885** | a second internal-logic module | #639 | (1) `## Work to be done` opening line `New \`Structure/Topos/Logic.v\`, over \`Structure/Topos.v\`'s \`ElementaryTopos\`.` → `Extend \`Structure/Topos/InternalLogic.v\` (built by #639), over \`Structure/Topos.v\`'s \`ElementaryTopos\`. Do NOT create \`Structure/Topos/Logic.v\` — see the SCOPE note below.` (2) Verification `coqc -R . Category Structure/Topos/Logic.v` → `coqc -R . Category Structure/Topos/InternalLogic.v`. (3) DoD `- [ ] New files registered in \`_CoqProject\`.` → `- [ ] The connectives land in \`Structure/Topos/InternalLogic.v\` (#639's module); the only NEW file is \`Instance/FinSet/Logic.v\`, registered in \`_CoqProject\`.` Leave Work item 5 alone. |
| **#914** | torsion subgroup `TA` **and** the quotient `A/T(A)` | #371 | **ADJUSTED.** (1) Work item 2 opening `Define the torsion subgroup \`TA\`, the quotient \`A/TA\`, and prove` → `Reuse #371's torsion subgroup \`TA\` and its quotient \`A/T(A)\` — the reflector built in \`Instance/Ab/TorsionFree.v\`; do NOT define either a second time — and prove`. (2) **REPLACE** the DoD checkbox `- [ ] \`Ab_fg\`, \`TA\` and \`A/TA\` are constructed` with `- [ ] \`Ab_fg\` is constructed; \`TA\` and \`A/TA\` are #371's definitions imported from \`Instance/Ab/TorsionFree.v\`, not second copies` (do **not** add a second box alongside — "are constructed" and "are imported" would contradict). (3) Depends-on line → `- Depends on: #371 (it defines the torsion subgroup \`TA\` and the quotient \`A/T(A)\` in \`Instance/Ab/TorsionFree.v\`, which this issue imports)`. No trailer/native change (`[256,288,545,371]` all real). |
| **#947** | Riehl Lemma 1.6.16 | #247 | **ADJUSTED.** (a) Work item 2 → `2. CONSUME, do not re-prove: #247 owns Riehl Lemma 1.6.16 (its \`(from Riehl §1.6)\` checkbox homes item \`riehl:1.6:lem16\` — the full subcategory of terminal objects is empty or a contractible groupoid, with the initial dual, including the no-other-morphisms clause). Take that lemma as given; if #247 lands only the terminal case, add the initial dual here and say so in the header.` (b) DoD → `- [ ] The general empty-or-contractible lemma is consumed from #247 (\`riehl:1.6:lem16\`), not re-proved; only the initial dual, if #247 omits it, is added here`. (c) Depends-on parenthetical → `- Depends on: #247 (uniqueness of terminal, initial and zero objects — owns Riehl Lemma 1.6.16, the empty-or-contractible-groupoid lemma this issue instantiates)`. (d) **REPLACED fourth edit** — add to Work item 1: `This file is the home of the contractibility VOCABULARY only. #247 owns the lemma itself and can state its hom-set condition inline, so it does not depend on this file — whichever of the two lands second consumes the other's artifact rather than re-proving or redefining it.` **Do NOT write that #247 consumes this file** — with the retained hard edge 947→247 that is a circular obligation, and #247 is out of this batch. |
| **#963** | `Fin_iso` | #956 | Work item 1 → `1. Build the wide subcategory \`Fin_mono\` of skeletal \`FinSet\` on the injections (donor: \`Construction/Subcategory.v\`; keep the decidability of \`Instance/FinSet.v\` intact). If \`Fin_iso\` is needed, take it from #956's \`Instance/FinSet/Groupoid.v\` (item \`riehl:3.3:example5\`) — do not rebuild it here.` (+ Group H). No edge (the build is conditional and neither #963's DoD nor Verification mentions `Fin_iso`). |
| **#307** | category of fields + characteristic obstruction | #971 | On **#307**: first Work bullet → `… define the category \`Dom_m\` (domains, monomorphisms only) over #971's \`Instance/Field.v\` category of fields, and prove ⟨Q(D), j⟩ is a universal arrow …`; second bullet → `… reuse #971's characteristic obstruction rather than re-deriving it.` (+ Group H). |
| **#985** | the essential image | #918 | Suggested-modules line `Suggested modules: \`Theory/EssentialImage.v\` for the notion (if the Riehl §1.5 issue has not already landed it), and \`Construction/Reflective/EssentialImage.v\` for the three clauses.` → `Suggested module: \`Construction/Reflective/EssentialImage.v\` for the three clauses, over #918's \`EssentialImage\` in \`Construction/Subcategory/EssentialImage.v\`.` Work item 1 → `1. Use #918's \`EssentialImage (F : C ⟶ D) : Subcategory D\` (with its \`Full\` and \`Replete\` witnesses) from \`Construction/Subcategory/EssentialImage.v\`. Do not introduce a second \`Theory/EssentialImage.v\`.` DoD `- [ ] The essential image is defined and used, not worked around.` → `- [ ] #918's essential image is consumed (not redefined) and is what clause (ii) is stated with.` |
| **#992** | the category of pointed sets | #261 | Work item 3 opening `Construct the category of pointed sets (either directly or as the coslice \`Construction/Slice.v:169\` under the terminal setoid — the route already named in that file's prose at \`:82\`), and prove` → `Consume #261's \`Set_*\` (\`Instance/Sets/Pointed.v\`) — do NOT build a second pointed-set category; if #261 has not yet fixed the presentation, the coslice \`Construction/Slice.v:169\` under the terminal setoid is the route its file prose names at \`:82\`. Prove`. Normalise `Sets_*` → `Set_*` in Work item 3 and in the DoD box (#261 as file creator is the naming authority). |
| **#1038** | `CRng`; the field of fractions | #257; #232 | (1) In Current state, after `there is no ring theory of any kind.`, append `The category of commutative rings is already filed: #257 delivers \`CRng\` as a full subcategory inside \`Instance/Rng.v\`, and #226 lists it as residual roster work. The field of fractions is #232's obligation (\`Frac D\` as a setoid quotient of numerator/denominator pairs, over integral domains and monomorphisms). This issue must consume all three, not re-create them.` (2) Path line `Suggested modules: \`Instance/CRing.v\` and \`Monad/Instance/Codensity/Fields.v\` (new).` → `Suggested module: \`Monad/Instance/Codensity/Fields.v\` (new). The category of commutative rings is NOT created here — it is #257's \`CRng\` in \`Instance/Rng.v\`.` (3) Work item 1 → `1. Over #971's \`Instance/Field.v\` and #257's \`CRng\` (\`Instance/Rng.v\`), construct only the inclusion functor \`Field ⟶ CRng\` and prove its functor laws. Do not define a second commutative-ring category.`; Work item 2 → `2. Define the prime spectrum as the set of prime ideals and the quotient by a prime ideal; prove the quotient is an integral domain, and obtain its field of fractions from #232's \`Frac\` rather than re-constructing it.` (4) DoD: `- [ ] The category of commutative rings and the inclusion of fields constructed on the existing setoid-algebra pattern` → `- [ ] The inclusion \`Field ⟶ CRng\` constructed over #257's existing \`CRng\`; no second commutative-ring category is introduced`; `- [ ] The prime spectrum, the quotient by a prime, and the field of fractions constructed, with the integral-domain property proved` → `- [ ] The prime spectrum and the quotient by a prime constructed, the integral-domain property proved, and the field of fractions taken from #232's \`Frac\``. (5) Drop `Instance/CRing.v` from the Verification coqc line. Ordering is safe by accident (reach(1038) ⊇ {257,226,232} via 1038→971→{226,232}→257); see §J11. |

---

## GROUP D — Closure checkbox / DoD hardening for prose-only or escapable increments

| Issue | Edit |
|---|---|
| **#805** | Append after `- [ ] The automatic-satisfaction theorem is proved, not left implicit`: `- [ ] Exercise 3.3's counting observation is a stated lemma (non-identity columns of a table ↔ edges of the quiver out of the corresponding node), with the header recording that it is definitional given Work item 1`; add `Print Assumptions schema_columns_are_edges.` to the Verification block. UNVERIFIED (LOW). Trailer ids already carry `7sketches:3.1:ex3.3`, so closure is currently untracked. |
| **#987** | Append to the §6.1 Example 6.1.7 block: `- [ ] (from Riehl §6.1) The header records Riehl's caveat that the same discussion covers group-sets, group-spaces, based group-spaces and group-objects in any category, but that in a target with few limits or colimits the two adjoints need not exist — stated alongside the (co)completeness hypothesis this issue already carries.` UNVERIFIED (LOW). |
| **#812** | Add a DoD checkbox: `- [ ] The generated profunctor is defined over an arbitrary quantale, with the Boolean case obtained by instantiation (so the §4.2 cost issue can instantiate the same construction).` (paired with the Group B #815 edge and the Group C #812 rewrite). |
| **#920** | Replace the escape-clause DoD line `- [ ] All six bulleted claims are stated; each is either proved **or explicitly recorded as blocked on a named prerequisite issue**, with nothing left as prose` with `- [ ] All six bulleted claims are stated and proved — the groupoid clause against #248's \`IsGroupoid\` and the local-smallness clause against #253's predicate, both of which this issue's dependencies guarantee are available — with nothing left as prose`. Keep both edges. **Verdict extra:** also trim `blocked ones included` from the Reviewer checklist phrase `confirm each of the six claims appears explicitly, blocked ones included`, or it re-licenses what the hardened DoD forbids. |
| **#813** | Replace the clause `is either given a use site or removed while this file is being written` with `is either given a use site or SURFACED to the maintainer as a removal candidate in the PR description (do not delete it unilaterally)`, leaving the rest of the checkbox unchanged. UNVERIFIED (LOW) but it aligns the issue with the repo owner's standing rule against agent-initiated deletion of working code (the PR #212 objection). Factual base checks out: `Id_Profunctor` has exactly two hits, `Theory/Profunctor.v:71` (prose) and `:132`, and `Construction/Profunctor/Compose.v:342` is the identical `Definition prof_id … := Hom C.` |

---

## GROUP E — Fix wrong or stale citations, clause maps and factual claims

| Issue | Change | Verdict |
|---|---|---|
| **#929** | **The stated conclusion is false as written and the DoD is unprovable.** Work item 4: replace the clause after the enumeration with `…and conclude through the Yoneda embedding (\`Functor/Hom/Yoneda.v:253\` \`Covariant_Yoneda_Embedding\`) that \`Nat(ob, mor) ≅ Cat(_2, _1)\` has exactly ONE element (the identity-assigning transformation) and \`Nat(mor, ob) ≅ Cat(_1, _2)\` exactly TWO (domain and codomain) — three natural transformations between \`ob\` and \`mor\` in total, which is Riehl's count. State each as an isomorphism of setoids with a one-, resp. two-element setoid, so the counts are theorems rather than existence facts plus prose.` DoD → `- [ ] Exactly one natural transformation \`ob ⟹ mor\` and exactly two \`mor ⟹ ob\` are proved, as isomorphisms with a one- and a two-element setoid derived through the Yoneda embedding (three in total between \`ob\` and \`mor\`, which is Riehl's statement)`. Verification: `Print Assumptions nat_ob_mor_three.` → `Print Assumptions nat_ob_mor_one.` and `Print Assumptions nat_mor_ob_two.` Update the Reviewer-checklist phrase `the counting result is derived from the Yoneda embedding` to name both counts. | CONFIRMED. `Functor/Hom/Yoneda.v:253` verbatim: `Covariant_Yoneda_Embedding : ∀ A B : C, Copresheaves [Hom B,─] [Hom A,─] ≊ A ~> B`. The issue's own enumeration contradicts its own conclusion. |
| **#623** | Current-state parenthetical → `(\`Instance/Cat/Cartesian.v:39\` provides \`Cat_Cartesian\` and \`Instance/One.v:54\` provides \`Cat_Terminal\`, and the generic \`Structure/Monoidal/Cartesian.v:49\` \`Cartesian_Monoidal\` (over \`Structure/Monoidal/Internal/Product.v:54\` \`CC_Monoidal\`) turns those into \`@Monoidal Cat\` directly — the instance is simply never named, and \`Enriched\` is instantiated only at \`Sets\` and \`Two\`)` | CONFIRMED (probe compiled). |
| **#667** | Replace `There is no category of topological spaces in-tree, so the Top instance cannot yet be formed; \`Instance/Poset.v\` provides Pos but carries no ordered-group object.` with `There is no category of topological spaces in-tree, so the Top instance cannot yet be formed; and there is no category **Pos** either — \`Instance/Poset.v:116\` builds a *single* poset as a thin category (\`Definition Poset ... : Category := Proset P\`) and the \`[Pos]\` reference at \`:21\` is dangling — so the ordered-group case is blocked on #641, and additionally needs a \`@Cartesian\`/\`@CartesianMonoidal\` structure on **Pos** that no issue currently schedules; scope it as a follow-up rather than a required leg if that structure does not land.` | ADJUSTED — the final clause is the verdict's addition (`Structure/Group.v:107` opens `Context \`{@CartesianMonoidal C}` before `:109 Class GroupObject`). |
| **#698** | Current state `There is no category of monoids over \`Sets\` and no category of groups.` → `There is no *named* category of monoids over \`Sets\` — \`Theory/Algebra/Monoid/Hom.v:83\`'s \`Mon\` instantiates at \`Sets\` via \`Instance/Sets.v:283\`'s \`Sets_Product_Monoidal\`, but nothing in-tree names that instance — and there is no category of groups.` | CONFIRMED. |
| **#679** | Source sentence `Item \`awodey:5.5:def28\` (the contravariant-functor definition).` → `Item covered: \`awodey:5.5:def28-laws\` — the composition-reversal/identity clause of Definition 5.28. (The definition itself, \`awodey:5.5:def28\`, is PRESENT in-tree at \`Functor/Opposite.v:56\` and is not part of this issue.)` **Do not disturb the edition line** (see DO NOT ACT). | CONFIRMED. `ledger.tsv:1186` has `awodey:5.5:def28  PRESENT  -  -`; `:1190` has `awodey:5.5:def28-laws  ABSENT  #679`; `issue-map.json:138` maps only the residual. |
| **#978** | `§4.5 "Contravariant adjunctions" / locally cartesian closed material:` → `§4.5 "Locally cartesian closed categories":`. Nothing else in the paragraph changes (all three page numbers verify). | CONFIRMED. |
| **#463** | `(printed p. 189, PDF p. 210; item \`riehl:5.2:example6\`)` → `(printed p. 189, PDF p. 209; item \`riehl:5.2:example6\`)`. Riehl offset is printed = PDF − 20; #465 and #471 cite the same Example 5.2.6 correctly at PDF 209. | UNVERIFIED (LOW). |
| **#725** | Third Current-state bullet → `Lower sets, down-sets and downward-closed subsets appear only as header prose — \`Construction/Slice.v:81\` ('For a poset P, the slice P/p is the principal down-set of p'); there is no definition, lemma or instance. (\`Theory/Sheaf.v:117\`, \`Instance/Fun.v:63\` and \`Construction/Day.v:111\` are the three cocompletion-prose sites recorded in the next bullet, not lower-set sites.)` | UNVERIFIED (LOW); the substantive claim (no lower-set definition in tree) is independently true. |
| **#740** | Replace all six occurrences of `Exercise 10.6.7` with `§10.6 Exercise 7` (Source, Background, Current state, Work to be done, and the two DoD checkboxes). The issue's own id is `awodey:10:ex7`; pagemap's NUMBERING-CONVENTIONS fixes the scheme. | UNVERIFIED (LOW). |
| **#945** | `\`rg 'Slice_Proj|slice projection'\` returns 0 hits` → `\`rg 'Slice_Proj'\` returns 0 hits and the only \`slice projection\` hit is prose (\`Instance/Coq/Comonad/Env.v:79\`)` | UNVERIFIED (LOW). |
| **#658** | `(2nd ed., Oxford Logic Guides 49)` → `(2nd ed., Oxford Logic Guides 52)` in the `## Source` line. **Gated on §J5** — the #679 REFUTED verdict shows "2nd ed. (OLG 52)" is itself campaign-wide wrong. | UNVERIFIED (LOW). |

---

## GROUP F — Contract / format

**F1. Body must END with the trailer.** New cases beyond the head plan's list:
- **#490** — move the whole `### Clause (a) of Riehl's E.2 roster — the named instances (added on audit)` section (heading, three-paragraph rationale, the `- [ ]` checkbox, the closing "This mirrors the treatment the sibling leg #388 …" sentence) to sit immediately BEFORE the `<!-- catalog: … -->` line, and collapse the double blank line. ids/deps unchanged. *(The only issue in its 35-issue batch containing `\n\n\n`.)*
- **#559** — move the `### Dropped clauses from Riehl §3.8 Example 3.8.8 (added on audit)` block (heading through `**Related (NOT blocking): #957** … before #957's item 3 lands.`) verbatim to before the trailer; collapse the run of three blank lines to one. 1210 characters currently sit after the trailer, including two `- [ ]` DoD increments. Trailer arrays unchanged.
- **#657** — move the `### Same-file peer (NOT a dependency)` block (through `**They must not be worked in the same parallel wave.**`) to before the trailer; collapse the double blank line. Block content is correct and matches `serialize-groups.json` (`Instance/Sets/Choice.v`, [657, 981], creator 657).

**F3. Trailer `deps` out of sync.** Under the head plan's convention decision:
- **#732** — trailer-only fix: `"deps": ["#387", "#730"]` → `"deps": ["#387", "#730", "#731"]`. Body and native (`[387,731,730]`) are already right; #731 genuinely supplies `Slice_of_Slice`/`Slice_Over_Terminal`.
- **#649** — see Group B (trailer `["#296"]` → `["#296","#648"]`).
- All Group A demotions carry a trailer edit; they are listed inline in the Group A table.

**F4. Stray double blank line after `### Also covered by`** — my chunk's #432 finding reports 11 occurrences across #432(1), #435(1), #436(1), #437(2), #440(1), #441(2), #443(1), #444(1), #453(1). **This is exactly head-plan F4; no new action.** UNVERIFIED (LOW), detected mechanically with `awk 'prev=="### Also covered by" && $0=="" {getline nxt; if(nxt=="") print NR} {prev=$0}'`.

**F5. One dependency per physical line:**
- **#719** — split the first Dependencies line at the semicolon into `Depends on: #239 (the currying adjunction \`(− × B) ⊣ (−)^B\` itself — the two functors and the \`Adjunction\` record, in \`Structure/Cartesian/Closed/Adjunction.v\`)` and `Depends on: #682 (the UNIT of that adjunction and the global-points bijection only — #682 explicitly defers the adjunction assembly to #239)`. Both edges are genuine and already harvested; no trailer or native change. (UNVERIFIED as a standalone LOW, but it is the exact shape that mis-fired six times.)
- **#649**, **#652** — split, per Groups B and A.

**F6. Bare / unresolvable references → issue numbers:**
- **#453** — both `the §V.6 comma-creation issue` (Work item 1, and the In-tree-donors line) → `#438`. *(head-plan duplicate)*
- **#456** — `the §V.5 non-existence issue` and `the §V.5 non-existence\nidiom` → `#432's obstruction lemma`; add `- Related (NOT blocking): #432 (\`maclane:V.5:ex1\`) — packages the adjoint-obstruction lemma; \`Adjunction/Continuity.v:223\` already supplies the positive direction, so the contrapositive can be taken locally if #432 has not landed.` **No `blocked_by`.** *(head-plan duplicate)*
- **#459** — `the §V.9 Top-cocompleteness issue` → `#458`. *(head-plan duplicate)*
- **#546** — `(new Riehl E.4 definition issue)` → `(#1013)` (see Group C).
- **#756** — `they are filed as their own issue (Seven Sketches 6.2.1, "Deciding initiality in small concrete categories")` → `they are filed as #860 (Seven Sketches 6.2.1, "Deciding initiality in small concrete categories"), which depends on this issue`. UNVERIFIED (LOW); `filed-issues.tsv` and the ledger split both confirm #860, whose trailer already declares `deps ["#756"]`.

**F7. Unrunnable `<path>` placeholders in Verification** (both UNVERIFIED, LOW):
- **#533** — `\`coqc -R . Category <path>\` compiles standalone.` → `\`coqc -R . Category Structure/Preadditive/ZeroObject.v\` compiles standalone (or \`coqc -R . Category Structure/Preadditive.v\` if the results are added in place).`
- **#538** — `\`coqc -R . Category <path>\` compiles standalone.` → `\`coqc -R . Category Instance/AbGroup/InfiniteBiproduct.v\` compiles standalone.`

**F8. Stray `None.` inside a populated Dependencies block:**
- **#865** — delete the single line `None.` immediately following `Depends on: #320 (MacLane III.3: Indexed coproducts) — it owns the indexed-coproduct file this issue must not rebuild.` Leave the blank line and the SCOPE blockquote.

**F9. Confusable module path (#529, UNVERIFIED LOW, see §J7):** change `Instance/Pointed/Sets.v` → `Instance/Sets/Pointed/Smash.v` in both the Work line and the Verification line, and append to the donor list `(distinct from #261's \`Instance/Sets/Pointed.v\`, which builds the category itself; this issue uses the in-tree \`Par\`/\`Coslice 1 Sets\` model instead and asserts no edge)`.

---

## GROUP H — `doc/plan/books/graph/serialize-groups.json`

All entries below are keyed on a **real `.v` path**, per the `Construction/Fractions.v` (#972/#1028) precedent; `check_graph.py` reads only the `issues` key.

1. **Add** `{"module": "Structure/Biproduct/Finite.v", "issues": [336, 536], "creator": 536, "reason": "SERIALIZE_ONLY: the finite matrix calculus is scoped on both, under `Structure/Bicartesian/Matrix.v` (#336) and `Structure/Biproduct/Finite.v` (#536). Hypotheses differ (#336 bicartesian + ZeroObject; #536 HasBiproducts + zero object), so no precedence edge is asserted."}`
2. **Add** `{"module": "Construction/FreeAb.v", "issues": [430, 540], "creator": 540, "reason": "SERIALIZE_ONLY: the free abelian group on a set and its universal property are scoped on both, under `Construction/FreeAb.v` (#540) and `Instance/Ab/FreeNotContinuous.v` (#430)."}` *(paired with the Group B edge 430→540)*
3. **Add** `{"module": "Instance/Top/Opens.v", "issues": [268, 685], "creator": 685, "reason": "SERIALIZE_ONLY: the poset of opens of a space is built twice under two paths — #268 as the inclusion-ordered thin category `Open X` in `Instance/Top/Presheaf.v`, #685 as the lattice/frame of opens in `Instance/Top/Opens.v`. Invisible to check_collisions.py, which matches paths. Whichever lands first owns the carrier; #727 consumes it.", "rationale": "#727 declares both as blockers and never says which owns O(X)."}`
4. **Add** `{"module": "Instance/FinSet/Groupoid.v", "issues": [956, 963], "creator": 956, "reason": "SERIALIZE_ONLY: both build Fin_iso, the groupoid of finite sets and bijections, under different proposed paths (#956 Instance/FinSet/Groupoid.v; #963 offers to build it inside Instance/Top/Configuration.v). Invisible to check_collisions.py, which matches paths. #956 is the canonical builder; no precedence edge is asserted because #963 needs only Fin_mono for its own deliverables."}`
5. **Add** `{"module": "Instance/Field.v", "issues": [307, 971], "creator": 971, "reason": "SERIALIZE_ONLY: the SAME category of fields and the SAME characteristic-obstruction non-existence argument under two different module paths (#971 Instance/Field.v; #307 Instance/Dom/Fractions.v \"if not already present\"). Invisible to check_collisions.py, which matches paths. No precedence edge is asserted; #971 is the canonical builder (#1038 already cites it as such)."}`
6. **Add — REQUIRED companion to the #1002→#481 demotion, not optional** — `{"module": "Structure/Limit/Reflection.v", "issues": [481, 1002], "creator": 481, "reason": "SERIALIZE_ONLY: #481 creates the file for coequalizer reflection; #1002 would add two ReflectsIsos lemmas to it if it exists. No precedence; edge removed on audit."}` Without it, with the edge gone both `blocked_by` sets become empty on a path both bodies name inside a scanned section, and invariant 4 fires.
7. **Amend head-plan Group H item 4.** The head plan proposes a non-path key `"Grp (construction, not a path: Instance/Grp.v vs Instance/Variety.v)"`. The adversarial verdict on #440 says every existing entry's `module` holds a real path and the precedent keys on ONE of the two. Use instead: `{"module": "Instance/Grp.v", "issues": [255, 440], "creator": 255, "reason": "SERIALIZE_ONLY: the same category built two ways under two paths (Instance/Grp.v vs Instance/Variety.v) — invisible to check_collisions.py, which matches paths. #255 owns the name; #440 builds its variety presentation under a different name and the comparison equivalence falls to whichever lands second. No dependency edge is asserted."}` **This is a correction to the head plan, not a new entry — do not create both.**

**Do NOT add:** #546 to the `Structure/Limit/Finite.v` group (refuted, below); any entry for `Instance/Fun/Limit.v` / `Instance/Fun/Colimit.v` (#425/#715 — the existing dependency edge serialises them once the overlap is removed).

---

## (i) REFUTED — DO NOT ACT

| Finding | Why not |
|---|---|
| **#679** `literature-reference` — "wrong edition: change `(1st ed., Carnegie Mellon pre-print, September 2005)` to `(2nd ed.)`" | **The finding is backwards.** `doc/plan/books/awodey/pagemap.md:7` — the campaign's own Phase-A0 calibration for `/Users/johnw/dl/Awodey_Category_Theory.pdf` — records `First edition content — specifically the author's Carnegie Mellon pre-print/draft of the 1st edition (Oxford Logic Guides 49, OUP 2006), dated September 2005`, with five pieces of evidence, and `:175` says all inventory page references cite that PDF. The pre-print TOC puts §5.5 at pp. 112–115 and `inventory/5.json` gives def28 p.114 / PDF 123 — exactly #679's coordinates. #679 carries the **only accurate** edition line in its batch; applying the edit replaces a true statement with a false one and sends an implementer holding the real 2nd edition (renumbered Ch. 7, different pagination) to the wrong pages. The real defect is the ~34 issues labelled "2nd ed. (OLG 52)" — see §J5. |
| **#546** — the STRUCTURAL half only: "add `Depends on: #417`" and "add 546 to the `Structure/Limit/Finite.v` serialize group" | Both rejected by the adversarial pass. (a) #1013 — the owner of the predicate #546 consumes — already documents in its own Dependencies that #417 is `- Related (NOT blocking): **#417** … It is **not** a prerequisite here — Work item 1 above deliberately declines a rival finite-shape notion and assembles \`PreservesFiniteLimits\` from the existing \`TerminalFunctor\`, \`CartesianFunctor\` and cospan-\`PreservesLimit\` pieces, all of which exist today.` Asserting 546→417 imports a prerequisite the predicate's owner has documented it does not need, and parks layer-2 #546 behind #417 (blocked_by [335,326,416]). (b) #546's only declared module is `Functor/Structure/Exact.v`; it never names `Structure/Limit/Finite.v`. Serialize groups record write hazards among CREATORS; a declared consumer belongs in neither. **The prose/checkbox half of the #546 remediation IS kept — see Group C.** If a hard edge is ever judged warranted it must be 546→#1013, never 546→#417. |
| **#829 step (1)** — "delete the first Work bullet and the `Construction/Cospan/Corelation/Monoidal.v` module" **as an unpaired edit** | Rejected standalone: #824's step 3 is sited in `Instance/FinSet/Corelation.v` and phrased concretely ("the empty set as unit and disjoint union as tensor") — it is the FinSet-level instance, while #829's bullet is the base-generic statement over an arbitrary base. Deleting #829's bullet with no compensating edit loses the general restriction lemma between the two issues. See §J4 for the paired form. |
| **#778** finding-2's original wording — "add ONLY the missing symmetry layer (`Cocartesian_SymmetricMonoidal`) in that same file" | Would create a NEW duplicate: #490's appended Seven Sketches §6.2.2 checkbox already owns `Cocartesian_BraidedMonoidal`/`Cocartesian_SymmetricMonoidal` in `Structure/Monoidal/Cocartesian.v`, and its Riehl §E.2 clause (b) records the same. Use the corrected consume-both-layers text in Group C. |
| **#600** finding-2's original wording — "Consume the full/essential image from #712 or #918 (whichever lands first)" | #918 builds the ESSENTIAL image (`sobj d := ∃ c, F c ≅ d` plus a `Replete` witness) and its own Work item 1 says repleteness is what distinguishes it from #712's strict image. Mac Lane's `K M` is the strict full subcategory; substituting #918 silently changes the subcategory the exercise is about. Use the corrected text. |
| **#947** finding's fourth edit — "add to Work item 1 that `#247's checkbox needs this vocabulary, so this file is the home of the definition and #247 consumes it`" | Combined with the retained hard edge 947→247 this asserts a mutual dependency, and #247 is out of batch so cannot be edited to accept it. Riehl's condition is statable inline. Use the corrected replacement in Group C. |
| **#914** finding's remediation as written — "build the quotient `A/TA` on top of it" and "add a matching DoD checkbox" | Re-authorises the second duplicate (#371 item 3 builds `A / T(A)` too) and leaves the old checkbox `- [ ] \`Ab_fg\`, \`TA\` and \`A/TA\` are constructed` asserting the opposite of the new one. Use the corrected text. |
| **#440** finding's remediation as written — "prove `GroupVariety ≅[Cat] Grp` once #255 has landed" as an unconditional Work deliverable, plus a prose-blob serialize key | Hidden blocker (an implementer scheduled before #255 cannot finish a Work item, and no edge warns the scheduler) and a non-path serialize key against precedent. Use the corrected text and the `Instance/Grp.v` key. |
| **#546 / #679 / #829 / #778 / #600 / #947 / #914 / #440** | In every case above the *problem* is confirmed; only the named clause of the remediation is refuted. |

---

## (ii) JUDGEMENT CALLS — decide individually, do not script

**J1. #750 → #704 demotion.** LOW, **no adversarial verdict**. The merits are clean: #750 needs the *covariant* powerset ("the functor is the **covariant** powerset (action by direct image), matching Awodey §10.5, not the contravariant one or the topos power object"), #704 is titled "the contravariant powerset functor …", and `serialize-groups.json` already records `Instance/Sets/Powerset.v` [227,466,704,750,871,1005] with a rationale that defends only the #466/#227 legs. **But a prior Riehl-Ch.5 audit recorded a deliberate decision to keep this edge.** Trade-off: demoting buys one wave and removes a false consumption claim; leaving it costs one wave and preserves a recorded decision. Default: leave, and record the covariant/contravariant distinction in the line instead.

**J2. #649 → #648 is contingent on #648 remaining the creator of `Instance/Mon.v`.** A finding in a sibling tail chunk ("finding 6") proposes handing the concrete monoid category to #296. If that is applied, the edge declared here points at an issue that no longer builds the donor. **Do exactly one of:** (a) keep `Instance/Mon.v` as #648's deliverable and declare 649→648 as in Group B; or (b) reassign creation to #296, delete the 648 edge on #649, and record #648/#649 as a serialize group on `Instance/Mon.v`. **Never both.**

**J3. #559 → #417 costs scheduling depth.** #559 is layer 0 and #417 layer 2 (blocked_by [335,326,416]), so the edge moves #559 and its dependents #563/#564 down the schedule. The alternative is to descope the finite case from #559 — a larger edit to a live issue whose DoD box currently ends "…the finite-cocone lemma proved." Verdict's position: the cost is honest given the DoD as written. Decide before running Group B.

**J4. #829 step (1) is a paired edit or nothing.** If you delete #829's first Work bullet and its `Construction/Cospan/Corelation/Monoidal.v` module, you must **simultaneously** edit #824's step 3 to read `…obtained from the cospan structure by the jointly-epic quotient rather than rebuilt — proved once at the level of an ARBITRARY base in \`Construction/Cospan/Corelation/Monoidal.v\` (the tensor of two jointly-epic cospans is jointly epic, so the wide-subcategory structure is inherited, stated as a transport along \`Corel_to_Cospan\`), then instantiated at FinSet` and add that path to #824's Verification. Otherwise leave the bullet and reword it to "the GENERAL restriction (arbitrary base); the FinSet instantiation is #824's". Step (5) — consolidating `Instance/FinSet/Corel.v` into #824's `Instance/FinSet/Corelation.v` — is safe either way, since the 829→824 edge already serialises the file.

**J5. The Awodey edition label is a campaign-wide decision, not a per-issue edit.** `pagemap.md` establishes the PDF is the 1st-ed CMU pre-print (OLG 49, September 2005); ~34 issues label it "2nd ed. (Oxford Logic Guides 52)", a label inherited from the `*-drafts.md` templates. #658's remediation (49 → 52) makes #658 *consistent* with the corpus and *inconsistent* with the calibration report. Either (a) relabel the ~34 to match `pagemap.md` and leave #658 as-is, or (b) record an explicit convention that "2nd ed." is deliberate shorthand for the pre-print and apply #658's edit. **Do not apply #658 in isolation without picking one.** #679 gets no edit either way (see DO NOT ACT).

**J6. #1002 → #1004: delete the edge, or add a Work item.** The Depends-on line reads `the notion "category of models for an algebraic theory", in which Corollary 5.6.4 is stated` — a provenance note, not a consumption; #1002's Work item 3 and DoD name only in-tree artifacts (`Theory/Lawvere/Sets.v:83` `ev1`, `Theory/Lawvere/Monad.v:89` `Context (refl : ReflectsIsos (ev1 T))`, `Instance/Sets.v:400` `bijective_is_iso`), and neither `AlgebraicCategory` nor "finitary" appears anywhere in #1002. Deleting the edge unparks a two-lemma result from the whole finitary/filtered chain. If the corollary is genuinely meant to be stated *for* `AlgebraicCategory`, keep the edge and add an explicit Work item saying so — that is the only reading under which it stands. **Related optional hardening on the same issue:** the #259 DoD box `- [ ] The \`Top\`/\`Poset\` contrapositive is either proved or explicitly scoped out in the header with its dependency named.` still leaves a "proved" branch that would silently reintroduce the #259 prerequisite; tighten to `…is explicitly scoped out in the header with #259 named as the missing prerequisite` if you want the demotion to be durable.

**J7. #529 path rename.** `Instance/Pointed/Sets.v` vs `Instance/Sets/Pointed.v` is the same near-mirror hazard as the head plan's `Instance/Mod` / `Instance/Module` call (§4.4). LOW, no verdict; the obligations are genuinely distinct (#261 builds the category, #529 builds `(− ∧ S) ⊣ Set_*(S, −)` over the in-tree `Par`/`Coslice 1 Sets` model) so no edge is warranted either way. Rename for legibility, or leave and add the disambiguating donor note only — but treat it consistently with whatever the head plan decides for `Mod`/`Module`.

**J8. #516's new module path.** The finding says `Instance/Simplicial/Homology.v`; the verdict notes `Structure/Abelian/Homology/Simplicial.v` is more taxonomically consistent with the repo's Structure/ vs Instance/ split, since the alternating-sum construction is general over any abelian category. Either resolves the finding, but **#517 line 17 and #519 line 17 must be retargeted to the same choice** in the same edit.

**J9. #536 / #336: Related-only, or a real edge?** The verdict: "once #336's boxes are amended to 'consume' #536's artifacts, a real edge `336 blocked_by 536` would also be defensible and layer-compatible (#536 layer 0, #336 layer 1) — but Related-only is the safer edit." Related-only asserts nothing (`Related (NOT blocking)` is in `REVERSE_CUE`); a real edge would guarantee the ordering the amended checkboxes assume.

**J10. #592 → #227: Related, or hard?** Promote `- Related (NOT blocking): #227 …` to `Depends on: #227` (plus trailer and native) **only if** the coordinator decides the `2^M ≅ P(M)` identification of Work item 1 is in scope rather than optional. #592's Current state confirms the gap: "there is no direct-image / power-set functor `2^M ≅ P(M)` in the tree."

**J11. #1038: make the inherited ordering explicit?** After the Group C rewrite, #1038 explicitly consumes #257's `CRng` and #232's `Frac`, but its ordering is inherited by accident through 1038→971→{226,232}→257. Adding explicit `Depends on: #257` / `Depends on: #232` lines makes the ordering intentional. The verdict: "not required, and not adding them is not wrong."

---

<!-- ===== section 3 ===== -->

# Remediation plan — issues **#435 – #1033** (chunk `doc/plan/books/qa/findings-tail-2.json`)

**76 findings read, over 60 issues** (435, 439, 442, 450, 465, 479, 485, 491, 497, 500, 518, 541, 544, 545, 550, 552, 561, 563, 575, 582, 589, 590, 602, 604, 632, 647, 655, 671, 683, 717, 723, 726, 729, 745, 755, 760, 775, 779, 785, 807, 814, 822, 851, 873, 886, 888, 890, 915, 918, 972, 977, 986, 989, 998, 1003, 1008, 1016, 1021, 1025, 1033). 58 findings carry an adversarial verdict (37 CONFIRMED, 17 ADJUSTED, 4 REFUTED); **18 LOW findings have no verdict and were NOT independently verified** — each is marked *(unverified)* below.

**Overlap with the head plan.** #435, #439, #442, #450, #683 are already in `plan-head-216-471.md`. Do not double-file them; two deltas are flagged in-line (a **correction** to the head plan's #450 Group D row, and the exact trailer text for #683).

**Campaign-wide questions I do not re-litigate:** the `deps`-array convention (head plan §4.7 — every trailer edit below is written in whichever form the issue already uses; normalise after the convention is picked), `Instance/Mod` vs `Instance/Module` (head plan §4.4), the `Rng` name clash (head plan §4.5). #518 proposes `Instance/Rng/MonoidRing.v` and uses `Rng` in #257's unital sense — consistent with the head plan's ruling.

**Verdict-scoping caveat:** verdicts in this payload are keyed to the *issue*, not the finding. Where an issue carries several findings and fewer verdicts, I matched each verdict's `problem` string to its finding; the unmatched findings (#552-lit, #563-lit, #717-format, #779-lit, #890-#416) are therefore **unverified** even though their issue has a verdict.

---

## GROUP A — Demote fabricated or over-asserted hard edges

Mechanical shape (head plan): (i) delete the `Depends on: #N …` line from `## Dependencies`; (ii) add on its own physical line `- Related (NOT blocking): …`; (iii) `gh api -X DELETE repos/jwiegley/category-theory/issues/<ISSUE>/dependencies/blocked_by/<N>`; (iv) drop the entry from the trailer `deps` array.

| Issue | Edge to demote | Exact replacement line | Trailer / extra |
|---|---|---|---|
| **#485** | #484 | `- Related (NOT blocking): #484 completes Beck's theorem (absolute condition, general converse, strict form); none of that is needed for Exs. 7-8.` | trailer → `"deps":["maclane:VI.7:def2"]`; blocked_by → `[481]`. **Also** in the Work bullet change ``routing through the completed Beck theorem (the §VI.7 Theorem 1 issue) / `beck_monadicity` `` → ``routing through the in-tree `beck_monadicity` (`Monad/Monadicity/Beck.v:739`)``. CONFIRMED: the sufficiency direction is already proof-complete in-tree. |
| **#723** | #404 (keep #403, #718) | `- Related (NOT blocking): #404 (presheaf categories as elementary toposes) assembles the `Sets`-valued presheaf topos and is a useful assembly template, but part (b) is over `FinSet` and consumes nothing from it.` | drop `"#404"` from trailer deps; drop native 723←404. See Group C for the Work-item-4 rewrite. |
| **#785** | #223 | `- Related (NOT blocking): #223 (preorders as thin categories — background vocabulary; #785 consumes no #223 artifact).` | trailer → `"deps":[]`; delete native 785←223. CONFIRMED: #785's three Work items are round trips over the LOCAL `TwoPreorder` record (`Construction/Enriched/Two.v:60`), never over `Proset`/`hom_preorder`/`Thin`. **Unblocks the chain #785 → #786 → #792 → #793.** |
| **#814** | #262 | ``- Related (NOT blocking): #262 (the dagger/converse structure on `Rel`). This issue consumes only the bare category already built at `Instance/Rel.v:45`, not #262's deliverables.`` | trailer → `<!-- catalog: {"ids":["7sketches:4.3.2:def4.24"],"deps":["7sketches:4.3.2:thm4.23","7sketches:4.2.1:def4.2","#785"]} -->`; delete native 814←262. |
| **#822** | #776 | `- Related (NOT blocking): #776 (Seven Sketches 2.2: wiring diagrams over a monoidal preorder). This issue is the CATEGORIFICATION of that one and the book states the link (printed p.134 opens §4.4.2 with "We introduced a very simple sort of wiring diagram in Section 2.2.2."), but the diagram syntax this issue reuses is the already-in-tree `Construction/PROP/Term.v:39` / `Construction/ColouredPROP/Signature.v:139`, not anything #776 builds.` | trailer → `<!-- catalog: {"ids": ["7sketches:4.4.3:ex4.50"], "deps": []} -->`; delete native 822←776. See judgement call 4 for option (b). |
| **#886** | #402 | ``- Related (NOT blocking): #402 — only if the implementer takes the rejected `ZArith`/`Sets` route for Exercise 7.16; the FinSet route this issue specifies needs nothing from it.`` | trailer `"deps":["#402","7sketches:7.2.3:construction-and"]` → `"deps":["7sketches:7.2.3:construction-and"]`; blocked_by → `[885]`. |
| **#890** | #460 | `- Related (NOT blocking): #460 — the concrete sheaf of continuous functions on a space; it consumes the re-founded predicate this issue delivers, so the precedence runs #890 -> #460, not the reverse.` | trailer `"deps": ["#460","#259","#268"]` → `"deps": ["#259","#268"]`; `gh api -X DELETE …/890/dependencies/blocked_by/4954308248`. **MANDATORY FIFTH EDIT** (from the ADJUSTED verdict — without it #890 becomes unsatisfiable): rewrite the DoD box `- [ ] At least one **positive** instance of the new `Sheaf` predicate exists in the tree (today there is none at all), so the predicate is demonstrably non-vacuous in both directions.` → `- [ ] At least one **positive** instance of the new `Sheaf` predicate is constructed HERE (the terminal presheaf is the cheapest witness; do not wait on #460's sheaf of continuous functions), so the predicate is demonstrably non-vacuous in both directions.` Optionally add the reverse edge 460←890. **Hub cost of leaving it: #888, #891, #892, #893, #894, #902, #903 all block on #890.** |
| **#918** | #231 | ``- Related (NOT blocking): #231 (full and faithful functors, subcategories, and reflection of monics) — its `Faithful (Incl S)` instance would shorten the corestriction proof if it has landed, but this issue consumes only the in-tree `Construction/Subcategory.v` `Full`/`Replete` and `Theory/Equivalence/FullFaithful.v:160`.`` | drop `#231` from trailer deps and native blocked_by (leaving `[712]`). **Keep #712.** Apply BEFORE/together with the F1 trailer move — see Group F. |
| **#986** | #437 | ``- Related (NOT blocking): **#437** treats the representability face of the same adjoint-existence machinery. Work item 5 feeds the already in-tree `Adjunction/GAFT.v:241` `GAFT` directly and consumes nothing #437 builds. No dependency edge is asserted.`` | trailer → `"deps": ["#559"]`; `gh api -X DELETE …/986/dependencies/blocked_by/437`. |
| **#1008** | #481 | ``- Related (NOT blocking): **#481** develops the dual coequalizer-reflection vocabulary; the creation-along-an-equivalence plumbing of Work item 1 is built here and shares no artifact with it. No dependency edge is asserted.`` | trailer → `"deps": ["#467", "#407"]`; `gh api -X DELETE …/1008/dependencies/blocked_by/481`. |
| **#1025** | #935 (**replace**, see Group B) | delete `Depends on: #935 (the category of vector spaces)` | trailer → `{"ids":["riehl:6.3:example3","riehl:6.3:example4"],"deps":["riehl:6.2:example9","#598","#456","#258","#305","#987"]}`; blocked_by → `[258, 305, 456, 598, 987, 1023]`. |
| **#683** | #223 | **already head plan Group A + Group H item 1 — do not re-file.** Delta from this chunk: the exact trailer is `<!-- catalog: {"ids": ["awodey:6.3:def7", "awodey:6.3:remark-ha-ump-equivalence", "awodey:6.3:def-heyting-negation", "awodey:9.4:example13", "awodey:9:ex7"], "deps": []} -->`, and the hedged line is a dangling line *below* the last append (the `## Dependencies` section at lines 64-66 already reads `None.`). | — |
| **#545** *(unverified LOW)* | none (line hygiene only) | Split the one physical line in two: keep ``Depends on: #544 (`maclane:VIII.3:def2`) (coimage — for the `coker f = coim g` form of exactness).`` then a NEW line ``- Related (NOT blocking): this is the reusable exact-sequence vocabulary that the diagram lemmas (#554, #555, #556), exact functors (#546), `Ses A` (#553) and homology (#557) all build on.`` | No trailer or native change. **Optional**: the three sibling findings of exactly this shape (#541, #550, #552) were all REFUTED because `check_graph.py`'s `REVERSE_CUE` already truncates them; #545's trailing clause carries no `#N` at all, so the risk is prospective only. |

---

## GROUP B — Add missing hard edges

Shape: add ``Depends on: #N (`item-id`) — <reason>`` on its own physical line in `## Dependencies`; add the id to the trailer `deps`; `gh api -X POST …/<ISSUE>/dependencies/blocked_by`.

| Issue | Add edge to | Exact Depends-on line(s) | Notes |
|---|---|---|---|
| **#465** | #388 **and** #1008 | ``Depends on: #388 (`maclane:IV.6:construction1`) — the tensor product over ℤ and the tensor–hom adjunction that exhibits `R ⊗ (−)` as a left adjoint`` / ``Depends on: #1008 (`riehl:5.6:thm5`) — the monadic-creation theorem whose colimit clause the checkbox applies`` | **ADJUSTED**: the original label `maclane:IV.6:thm-tensor-hom` is FABRICATED — #388's trailer ids are `[maclane:IV.1:construction4, maclane:IV.6:construction1, riehl:4.4:example15, riehl:4.4:exv, riehl:4.6:cor4, riehl:E.2:example-symmetric-monoidal]`. Trailer += `"#388","#1008"`. Cycle-safe (closure(388)={255,256,257,258,264,265,303,306}, closure(1008)={254,351,353,406,407,467,481}). Also F6: in the append prose replace `(the Riehl §5.6 Theorem 5.6.5 issue)` with `(#1008)`. |
| **#479** | #471 | ``Depends on: #471 (`maclane:VI.4:ex1`) — the free-monoid monad and `Mon`, required by the Awodey §3.4 checkbox`` | Trailer += `"#471"`. Cycle-safe: closure(471)={470}. |
| **#500** | #259 | `Depends on: #259` | Trailer `["#256","#258","#265"]` → `["#256","#258","#265","#259"]`. #259 is NOT transitively present (closure is {255,256,257,258,264,265}). Precedent: #516 and #514 declare `Depends on: #259` for the same reason. |
| **#563** | #417 | `Depends on: #417 (the finiteness predicate on an index category — the hypothesis "P finite" of Theorem 1).` | Trailer `["maclane:IX.1:def2", "#425"]` += `"#417"`. Closure(563)={559,425,424} — #417 unreachable. Cycle-safe: closure(417)={335,326,416,427}. |
| **#575** | #345 **only** | ``Depends on: #345 (`maclane:III.7:construction1` — builds `Construction/Elements.v`'s `Elements`, which the Riehl §2.4 identification checkbox consumes)`` plus, on its own line, ``- Related (NOT blocking): #333 (builds `Sets_HasPullbacks` in `Instance/Sets/Pullback.v`; the Riehl §3.2 `Sq(f,g)` checkbox can be read off it instead of hand-building the square).`` | Replaces the body `None.`; trailer `[]` → `["#345"]`. **No #333 edge** — one square is provable without a `HasPullbacks Sets` instance (in-tree precedent `Instance/Sets/Classifier.v:224`). |
| **#582** | #578 | ``Depends on: #578 (`maclane:IX.5:def2` — defines `PreservesEnd`/`CreatesEnd` in `Structure/End/Preservation.v`, which Exercise 4 states)`` | Trailer → `<!-- catalog: {"ids":["maclane:IX.7:thm2","maclane:IX.7:thm3","maclane:IX.7:ex2","maclane:IX.7:ex4"],"deps":["maclane:IX.7:prop1","maclane:IX.5:def2"]} -->`. Also insert `Structure/End/Preservation.v` after `Structure/Coend.v` in the 4th Work bullet's donor list. Without this the predicate is defined twice under two paths (`Structure/End/Parameter.v` vs `Structure/End/Preservation.v`), invisible to any path-keyed check. |
| **#604** | #599 | ``Depends on: #599 (`maclane:X.5:def2`)`` inserted immediately after ``Depends on: #598 (`maclane:X.5:def1`)`` | Trailer → `"deps": ["maclane:X.5:def1", "maclane:X.5:def2", "#398"]`. Convention verified against sister issue #605. |
| **#632** | #497 | `Depends on: #497` (replacing the body `None.`) | Trailer → `<!-- catalog: {"ids":["maclane:XII.7:construction1"],"deps":["#497"]} -->`. No cycle (#497 depends on #496). See Group C for the Work-item-2 rewrite (the original's `Theory/Bicategory.v:113` clause is WRONG — that line is the bicategory *strictification* theorem). |
| **#638** | #655 | ``Depends on: #655 (`Structure/WellPointed.v` — the well-pointedness predicate).`` | Edge lands on **#638**, not #655. Trailer of #638 += `"#655"`. See Group C. |
| **#998** | #994 | ``Depends on: #994 (the continuation monad at `S = 2` — the double power set MONAD structure that Work item 3's morphism-of-monads statement requires; #704 supplies only the functor and unit)`` | Trailer → `["#700","#704","#994"]`. Cycle-safe: #994 blocked_by [239,704]. Precedent: #1005 already declares #994 for the same reason. Also rename in Work item 3: `the double power set monad of the continuation-monad issue for §5.1` → `the double power set monad of #994`. |
| **#1025** | #258 **and** #305 | `Depends on: #258 (module categories R-Mod and friends — supplies `Vct_F`/`Vect_k`)` / `Depends on: #305 (the free vector space as a universal arrow — the free ⊣ forgetful adjunction that makes the underlying-set functor a right adjoint)` | Paired with the Group A demotion of #935. 1025→258 is transitively redundant given 1025→305→258; `check_graph.py` has no invariant against redundant edges, so the explicitness is safe. |
| **#450** | #445 | **already head plan Group B** — ``Depends on: #445 (`maclane:V.7:def2`) — supplies the intersection (meet) of subobjects…`` | CONFIRMED by this chunk. |

---

## GROUP C — Rewrite Work item to CONSUME, not rebuild

| Issue | Rebuilt artifact | Owner | Exact edit |
|---|---|---|---|
| **#518** | monoid ring `ℤ[M]`/`R[M]` | **#518 itself** (ADJUSTED — the finding's direction was reversed) | **Do NOT add `Depends on: #310`.** (1) Change the suggested module and the Verification line from `Instance/GroupRing.v` to `Instance/Rng/MonoidRing.v`. (2) In `## Dependencies`, after `Depends on: #257`, add on its own line: `- Related (NOT blocking): #310 (maclane:III.1:ex1) also constructs the monoid ring ℤ[M]/R[M] at Instance/Rng/MonoidRing.v, as a universal arrow to the units functor. This issue is the CREATOR of that file — its adjunction ℤ ⊣ U subsumes that universal arrow — and #310 consumes it. Recorded as a same-file peer in doc/plan/books/graph/serialize-groups.json; no dependency edge is asserted here.` (3) Leave trailer deps and native blocked_by unchanged. (4) Record in #518 that its Riehl §2.3 checkbox already discharges #310's group-ring-universal-arrow clause. (5) H entry below. Rationale: an adjunction yields the universal arrow, not conversely; #310 is a bundle additionally requiring a nonexistent `Instance/Vect` substrate. |
| **#604** | (nothing) — clarity only | #398 | Rewrite Work item 4 to: ``- Prove Exercise 1: exhibit §X.7's hom-bijection as an instance of the adjoint-square bijection built by #398 (`AdjointSquare` / `palmquist_bijection` in `Adjunction/Square.v`), unfolding through the in-tree `mate_iso` (`Theory/Bicategory/Mates.v:515`) and `Cat_mate_unfold` (`Instance/Cat/Bicategory/Adjunction.v:260`).`` **Do NOT take the finding's second branch** (deleting the #398 edge) — see DO NOT ACT. |
| **#632** | monoidal coherence | #497 | Rewrite Work item 2 to: ``- Record the coherence payoff: derive bicategorical coherence for α, λ, ρ in the one-object case from #497's monoidal coherence theorem (Structure/Monoidal/Coherence.v, monoidal_coherence), generalising the single instance already transported by Monoidal_OneObject_unit_coincidence (Theory/Bicategory/OneObject.v:90).`` DoD → ``- [ ] Bicategorical coherence for α/λ/ρ in the one-object case proved from #497's monoidal_coherence.`` **Drop the `Theory/Bicategory.v:113` clause entirely** — those lines read "The coherence theorem (Mac Lane, Paré 1985) shows every bicategory biequivalent to a strict 2-category", a different and much larger theorem. |
| **#638** (host: #655's finding) | `WellPointed` | #655 | Work bullet 1 → ``- Reuse the `WellPointed` predicate built by #655 (`Structure/WellPointed.v`); define nothing new here. The separating-*family* notion is #447's (`Structure/Generator.v`) and this bundle does not need it.`` **Merged DoD line** (the two duplicate findings collide on the same physical line — use ONE): ``- [ ] `ExternalAC` defined (`Structure/Choice.v`); `CategoryOfSets` bundles `ElementaryTopos` + `WellPointed` (from #655) + `ExternalAC` + NNO.`` Verification: drop `coqc -R . Category Structure/Separator.v`; change `Print Assumptions CategoryOfSets.` (and `WellPointed`, `InternalAC`) to (and `ExternalAC`), noting `WellPointed` is inherited from #655. Plus the Group B edge. |
| **#723** | (scoping) | #403 / #718 | Rewrite Work item 4 to: `4. Part (a) instantiates #403's general sieve classifier — state in the header which general theorem each concrete classifier is transported from. Part (b) is over `FinSet`, where the Sets-valued presheaf results do not apply directly: build the finite-diagram structure here following #718's presheaf-exponential formula (`Instance/FinSet/Closed.v` is the finite template), and say so in the header.` (This removes the blanket "if not yet available, prove directly" escape for #403, a genuine blocker.) **Do NOT write "part (b) consumes #718's diagram exponentials"** — #718 delivers `@Closed (@Fun C^op Sets) _`, Sets-valued. |
| **#726** | unit-side determination | split with #347 | Replace #726's dependency line with: `Depends on: #347 (the counit/couniversal-arrow side of the determination theorem; its Work item 3 does the from-an-adjunction round trip on the COUNIT side and its item 4 touches the unit presentation — this issue owns the UNIT side, `adjunction_unit_universal`/`adjunction_comma_initial` and the two biconditionals; neither issue may re-derive the other's).` Narrow **#347** Work item 4 to: ``Record the first presentation separately from the third, on the counit side only; the unit-side statement (a unit whose every component is universal, with the transpose recovered as `fmap[U] g ∘ η`) is #726's deliverable — cross-reference it rather than proving it here.`` |
| **#729** | `Structure/Limit/Coproduct.v` | #320 | (a) Suggested-module-paths line → ``Suggested module paths: `Instance/Sets/IndexedFamilies.v` and `Instance/Sets/ChangeOfBase.v` (`Structure/Limit/Coproduct.v` is #320's).`` (b) first Work bullet → ``- Consume #320's `icoprod`, `icoprod_inj`, `icoprod_ump` and `HasIndexedCoproducts` from `Structure/Limit/Coproduct.v`; do not re-dualize `Structure/Limit/Product.v` here.`` (c) first DoD checkbox → ``- [ ] #320's `icoprod`/`icoprod_ump`/`HasIndexedCoproducts` are consumed unchanged`` (d) delete `coqc -R . Category Structure/Limit/Coproduct.v` from Verification. #320 already carries the reciprocal "Downstream consumer: #729 … Land this issue first; do not duplicate the construction there." |
| **#745** *(unverified LOW)* | `Instance/Powerset.v` | #382 | Suggested-modules first entry → ``  `Instance/Powerset.v` — CONSUMED from #382, which creates it; add nothing to the powerset order here``. DoD `- [ ] The subsets of a type as an inclusion-ordered thin category.` → ``- [ ] #382's inclusion-ordered powerset preorder (`Instance/Powerset.v`) consumed unchanged as the ambient order — no second subset order defined.`` Drop `coqc -R . Category Instance/Powerset.v` from Verification. |
| **#755** | Proset glb/lub dictionary | #422 | Work item 6 → ``6. Consume #422's identification lemma — a limiting cone in a `Proset` is a greatest lower bound and dually a colimiting cocone is a least upper bound — to state step 2's least-upper-bound property through the existing `Cocartesian` API. Do not re-prove the dictionary; #422 delivers both halves. While here, update `Instance/Poset.v:51`'s comment to cite #422's lemma instead of asserting it.`` DoD → ``- [ ] Step 2's least-upper-bound property stated through #422's coproduct-is-a-join identification, and `Instance/Poset.v:51`'s comment updated to cite that lemma instead of asserting it``. Delete the parenthetical `(If the poset adjoint-functor-theorem issue lands first, reuse its identification lemma instead of duplicating it.)` — **but on the ground that it is obsolete once item 6 consumes #422, NOT because #737 fails to build the lemma.** #737 DOES schedule it. |
| **#775** | ordered ℤ | #759 | Work item 4(iii) → ``(iii) using #759's ordered-ℤ `Proset` from `Instance/Poset/Numeric.v` (do not introduce a second integer carrier), refute monotonicity of `·` for `≤` on ℤ with the book's negative-multiplier witness.`` Suggested-modules third entry ``  `Instance/Int/Order.v` (the ℤ clause of Exercise 2.45)`` → ``  `Instance/Int/Monoidal.v` (the ℤ refutation of Exercise 2.45, over #759's carrier)``; update the Verification `coqc` path to match. |
| **#779** | three-element chain `Proset` | #765 | Replace the first sentence of Work item 1 with: ``1. **CONSUME, DO NOT RE-CREATE** the carrier: #765 registers the three-element chain explicitly as a `Proset` in `Instance/Poset/Chain.v`, and this issue is already blocked on it. `Instance/Three.v` should re-export that chain (and its thinness), not build a second three-element `Proset`. Relate it to the existing `Theory/Metacategory.v:413` `Three` by an isomorphism of categories, following the `n = 2` precedent, so the two presentations are not left disconnected.`` DoD → begin ``- [ ] #765's three-element chain `Proset` is re-exported (not redefined), proved thin, and connected…`` |
| **#851** | `Instance/Preord.v` | #786 | Work item 2 opening clause → ``Consume #786's `Instance/Preord.v` (`Preord`: objects preorders, morphisms `MonotoneMap`s) and add ONLY its cartesian monoidal structure, in a new `Instance/Preord/Monoidal.v`; do not rebuild the category.`` DoD → ``- [ ] #786's `Preord` is equipped with its cartesian monoidal structure in `Instance/Preord/Monoidal.v`.`` Keep the #786 edge. **Plus one sentence** in the same Work item: the `(Cat, ×, 1)` monoid identification is #500's clause (b), whose suggested path is `Instance/StrictCat/Monoid.v` while this issue proposes `Instance/StrictCat/MonoidObject.v` — state in the header which path wins (H entry below). |
| **#873** | six `DecCospan_*_Coherent` | #872 | **Recommended (keeps the edge):** Work item 2 → `Instantiate the decorated-cospan tower at it by CONSUMING #872's derived `DecCospan_*_Coherent` instances — do not re-derive them — so the library finally has one concrete decorated-cospan category.` and delete the parenthetical `(they should follow from the unit coherences alone)`. Apply the same wording decision to **#876** Work item 1's `(or consume them as theorems …)` so the two do not drift. **Do NOT repeat the finding's rationale** that "#872's own DoD wants a concrete `Print Assumptions` witness" — it is inaccurate. See judgement call 3. |
| **#915** | `Instance/CHaus.v` | #413 | (1) Verification `coqc -R . Category Instance/Ban.v Instance/CHaus.v Theory/Analysis/Riesz.v` → `coqc -R . Category Instance/Ban.v Theory/Analysis/Riesz.v`. (2) Replace Work item 1 in full with: ``1. Consume `Instance/CompHaus.v` — the category of compact Hausdorff spaces built by #413. Do NOT build a second one; this issue creates only `Instance/Ban.v` and `Theory/Analysis/Riesz.v`.`` (this also removes the now-redundant sentence "Issue #489 … and issue #413 already schedule adjacent material — align with whatever `cHaus` those deliver rather than building a second one."). (3) DoD ``- [ ] `cHaus` and `Ban` are constructed, or explicitly imported from the issues that build them`` → ``- [ ] `Ban` is constructed; `CompHaus` is imported from #413's `Instance/CompHaus.v`, not rebuilt``. |
| **#918** | `EssentialImage` ownership | #918 (owner) | Add at the end of Work-to-be-done: `6. This issue is the OWNER of the `EssentialImage` construction. #985 (Riehl 4.6) depends on it and currently offers an alternative home; the notion lands here, at `Construction/Subcategory/EssentialImage.v`, and #985 must consume it rather than define a second one.` **Omit the string `Theory/EssentialImage.v` from the note** — `creates()` scans the Work section for `.v` paths and would attribute it to #918. Companion one-liner on **#985** (out of batch, apply together): strike ``(if the Riehl §1.5 issue has not already landed it)`` and ``Theory/EssentialImage.v`` from its Suggested-modules line. |
| **#977** | closure operators / fixed points | #386 | Add to `## Dependencies`, each on its own physical line: `- Related (NOT blocking): **#386** derives the closure operators, their idempotence and the closed-elements/fixed-points correspondence generically (Construction/Reflective/FixedPoints.v plus a poset instantiation). Work item 2 here must CONSUME that result if #386 has landed, not re-derive it.` and `- Related (NOT blocking): **#385** (orthogonal complement) and **#381** (group acting on a set) are two further instances of the relation-induced antitone connection built by Work item 1; they should be re-expressed as instances of it rather than kept as bespoke constructions.` Amend Work item 2 to begin `Derive (or, if #386 has landed, import) the closure operators …`. No edge asserted. |
| **#989** | couniversal-arrow vocabulary | #347 | Work item 3's final clause ``; introduce the couniversal-arrow vocabulary the tree lacks, preferably as `UniversalArrow` in the opposite category so that `Adjunction/Opposite.v` supplies the duality for free.`` → ``; consume #347's couniversal-arrow vocabulary and its `Adjunction/Opposite.v` transport rather than introducing a second one.`` DoD ``- [ ] The couniversal/terminal-arrow presentation introduced, with duality obtained through `Adjunction/Opposite.v` rather than by a parallel development.`` → ``- [ ] The couniversal/terminal-arrow clause is stated over #347's couniversal arrows, with no second couniversal notion introduced.`` Edge preserved. |
| **#1003** | lattice vocabulary | #340/#389/#422 group | New FIRST Work bullet: ``**Module coordination:** the order-theoretic lattice vocabulary is already claimed by the `Structure/Lattice.v` serialize group — #340 (`LatticeObject` by internalization), #389 (Boolean/Heyting algebras as thin CCCs) and #422 (glb/lub in a preorder and their identification with (co)limits); see `doc/plan/books/graph/serialize-groups.json`. Land `MeetSemilattice`/`JoinSemilattice`/`Lattice` in `Structure/Lattice.v` beside them rather than opening a rival class, and keep `Instance/Lattice.v` for the three CATEGORIES and their forgetful functors to `Sets`. Work item 2's dictionary is the binary/nullary case of #422's glb-vs-limit identification — cite it, do not restate it. Cross-reference #340's `LatticeObject` in the header as the internal/algebraic presentation of the same notion; proving the bridge between the two presentations is explicitly NOT in scope here.`` Dependencies ``None (self-contained over `Instance/Poset.v` and the finite (co)limit classes).`` → `None blocking.` + on its own line ``- Related (NOT blocking): **#340**, **#389**, **#422** share the lattice vocabulary in `Structure/Lattice.v`; recorded as same-file peers in `doc/plan/books/graph/serialize-groups.json`.`` **Drop the original's "prove the dictionary to #340's `LatticeObject`"** — undeclared scope AND an undeclared #340 dependency. |
| **#1033** | `Lan_Id T ≅ T` / `Ran_Id T ≅ T` | #603 | Work item 1 → ``1. Consume #603's Lan_Id T ≅ T / Ran_Id T ≅ T in the LOCAL form its Riehl §6.1 append commits to (a single diagram, with identity 2-cell) — do not restate it here. The new content is only that such an extension is automatically pointwise, since restriction along the identity is the identity functor and #599's criterion applies trivially; prove that.`` Add a DoD checkbox immediately after the first: ``- [ ] The identity-Kan statement is #603's, consumed not re-proved; only the automatic-pointwiseness lemma is new here``. **DO NOT rename `Theory/Kan/SelfExtension.v`** — see DO NOT ACT. |
| **#439**, **#442** | — | — | **already head plan Group C** — this chunk CONFIRMS both. |

---

## GROUP D — Add closure checkbox for prose-only increment

| Issue | Checkbox / edit |
|---|---|
| **#450** ⚠️ **CORRECTS THE HEAD PLAN** | The head plan's Group D row for #450 carries the *pre-verdict* second box, which states a **falsehood** (`Instance/Comp.v:160` `Algs_Terminal` is the one-element algebra with carrier `unit`, satisfying every equation, so `⟨Ω,E⟩-Alg` is never empty). Append these two boxes at the end of the MacLane IX.1 Exercise 2 block instead: ``- [ ] (from MacLane IX.1 Ex. 2) Identify the initial object of the variety category as the free algebra on the empty set (donor: `Instance/Comp.v:209` `Algs_Initial`, which records this for the equation-free case), stated for `⟨Ω,E⟩-Alg``` and ``- [ ] (from MacLane IX.1 Ex. 2) Characterize when that initial algebra is EMPTY: its carrier (the closed terms) is empty exactly when the signature has no constants (no nullary operations). Note that the CATEGORY `⟨Ω,E⟩-Alg` is never empty — the one-element algebra `Instance/Comp.v:160` `Algs_Terminal` satisfies every equation — so the book's "when is `Alg_τ` empty" must be read as a statement about the initial algebra's carrier.`` |
| **#479** | Append to the Awodey §3.4 Prop. 3.22 block (which currently ends `New aspect: instantiating the canonical presentation for the free-monoid monad / **Mon** (MacLane VI.4 monoid monad = #471) to realize `M ≅ F(G)/(r₁=r₂)`.`): ``- [ ] (from Awodey §3.4 Prop. 3.22) Instantiate the canonical split presentation at the free-monoid monad / `Mon` (#471): exhibit every monoid as `M ≅ F(G)/(r₁ = r₂)` via the bar coequalizer `T²M ⇉ TM → M`, and show it is preserved by `Mon → Sets` (Awodey §3.5 Ex. 3) because the fork is split.`` **Fold in the verdict's factual note**: the append's parenthetical "(no category **Mon**, no free-monoid monad)" is half wrong — `Mon` and `Mon_Forget` DO exist at `Theory/Algebra/Monoid/Hom.v:83`/`:93`; only the free-monoid monad is missing, so the new checkbox must not imply `Mon → Sets` has to be built. |
| **#500** | Insert after the first DoD checkbox: ``- [ ] (from Riehl §1.6 Def. 1.6.3) Topological monoids identified as monoid objects in `(Top, ×, 1)` once #259 lands, with the unit map out of the one-point space.`` Extend Work clause (a) from ``(a) rings as monoids in `(Ab, ⊗, ℤ)` and K-algebras as monoids in `(K-Mod, ⊗_K)` — depend on those categories (`Ab` is #256, its tensor product is #265, module categories are #258);`` to ``… module categories are #258), and topological monoids as monoids in `(Top, ×, 1)` (`Top` is #259);`` (+ Group B edge). |
| **#647** | Insert before the enumeration bullet in Work: `Fix the statement first: (i) define quiver isomorphism (a bijection on nodes together with a family of setoid bijections on the edge sets, over `Construction/Free/Quiver.v:54`'s endpoint-indexed `edges : nodes → nodes → uedges`); (ii) prove the finiteness bound that makes the enumeration decidable — in the free category on a quiver with n nodes and e edges the n length-0 paths and the e length-1 paths are pairwise distinct arrows, so 'exactly six arrows' forces n + e ≤ 6 (hence n ≤ 6 and e ≤ 5; and since e ≥ 1 forces n ≥ 2 once loops are excluded by acyclicity, e ≤ 4) — leaving a finite, explicitly enumerated set of quivers to check.` DoD → ``- [ ] Quiver isomorphism defined; the `n + e ≤ 6` bound proved; the resulting finite list of generating quivers enumerated and proved to be exactly those whose free category has six arrows.`` **The finding's original "at most 6 nodes and at most 3 edges" bound is FALSE** (n=2, e=4 parallel arrows `a ⇉ b` is acyclic with exactly six arrows) and would make the checkbox provably unsatisfiable. |
| **#717** | Append to the `awodey:9.6:prop16` block, after its final paragraph: ``- [ ] (from Awodey §9.6) The adjunction form: `F_!` is LEFT ADJOINT to the restricted-hom functor `E(F−,−) : E ⟶ [C^op, Sets]` — an `Adjunction` record, not merely the extension-plus-uniqueness pair; `Theory/Profunctor.v:155` `Repr_left` is the right-hand functor in uncurried form.`` |
| **#729** | Append at the end of the `riehl:4.5:example7` block: ``- [ ] (from Riehl §4.5) Fibrewise cartesian closure: the category of `X`-indexed families of sets is cartesian closed with internal hom the fibrewise exponential `(B_x^{A_x})`, stated in the library's `Structure/Cartesian/Closed.v` `Closed` vocabulary — the clause the ledger assigns to this issue alongside the fibrewise adjoint triple.`` (ledger.tsv:2617 reads `riehl:4.5:example7\tABSENT\t#729\t10\tpart: fibrewise cartesian closure and the fibrewise adjoint triple`.) Alternative: re-home on #732/#734 and correct the line-2617 part label to `part: the fibrewise adjoint triple`. |
| **#760** | **Two edits, in order.** (1) In the `### Also covered by` block replace the closing clause `…Riehl's statement is the same file with the multiplier taken to be the identity, so no separate obligation arises.` with `…Riehl's statement is the same file with the multiplier taken to be the identity, so the increment is small — parameterising the multiplier — and is tracked by the checkbox below.` (2) Then append: ``- [ ] (from Riehl §4.1) Parameterise the three maps by a positive multiplier `k` — `k × − : ℤ → ℝ`, `⌈−/k⌉`, `⌊−/k⌋` — and instantiate at `k = 1`, recording the adjoint triple `⌈−⌉ ⊣ i ⊣ ⌊−⌋` for the inclusion `i : ℤ ↪ ℝ` as a named result alongside the `k = 3` case.`` (3) Work item 1's first clause ``define the three monotone maps: `3 × − : ℤ → ℝ`, `⌈−/3⌉ : ℝ → ℤ` and `⌊−/3⌋ : ℝ → ℤ``` → ``define, for a positive multiplier `k`, the three monotone maps `k × − : ℤ → ℝ`, `⌈−/k⌉ : ℝ → ℤ` and `⌊−/k⌋ : ℝ → ℤ` (the book's case is `k = 3`; Riehl's is `k = 1`, where `k × −` is the inclusion)``. Without edit (1) the block contradicts itself. |

---

## GROUP E — Fix wrong or stale citations and clause maps

| Issue | Change |
|---|---|
| **#435** *(unverified LOW; already head plan)* | `printed pp. 251–253, PDF pp. 260–261` → `printed pp. 251–253, PDF pp. 260–262` (uniform +9 Awodey offset; #436 cites the adjacent passage correctly as `printed pp. 250–253, PDF pp. 259–262`). |
| **#491** *(unverified LOW)* | `(Strict.v:43, Compose.v:55, Product.v:28, Braided.v:43)` → ``(`Functor/Structure/Monoidal/Strict.v:43`, `.../Compose.v:55`, `.../Braided.v:43`, and `Structure/Monoidal/Product.v:28`)`` — `Functor/Structure/Monoidal/Product.v` does not exist; the intended file is in a different directory. |
| **#497** *(unverified LOW)* | `Riehl, Epilogue §E.2, the statement of the Mac Lane–Kelly coherence theorem for` → `Riehl, *Category Theory in Context*, 2nd ed., Epilogue §E.2, the statement of the Mac Lane–Kelly coherence theorem for`. Same one-word fix applies to **#520**'s second Riehl append, which opens `Riehl, Epilogue §E.2, the corollary drawn from symmetric coherence:`. |
| **#552** *(unverified LOW)* | ``(`Structure/Regular.v:25`, `:81`)`` → ``(`Structure/Regular.v:79`–`:81`, the `regular_stable` field)`` — `:25` is a blank line; the field runs `:78`–`:81`. |
| **#563** *(unverified LOW)* | ``(the only `κ`/`kappa` hits are Freyd `κ`-categories in `Structure/Premonoidal/Freyd.v` and a bound cone variable in `Structure/Limit/Weighted.v`)`` → ``(the `κ`/`kappa` hits are all unrelated: Freyd `κ`-categories in `Structure/Premonoidal/Freyd.v`, a bound cone variable in `Structure/Limit/Weighted.v`, the Funny-tensor-to-product comparison functor `κ = FunnyToProduct` in `Construction/Funny/Comparison.v:14` and `Construction/Funny/Closed.v:33`, and continuation variables in `Instance/Lambda/Eval.v`; no morphism anywhere has a colimit as source and a limit as target)``. |
| **#590** *(unverified LOW)* | `Awodey, *Category Theory*, §9.6, Corollary 9.17 —` → `Awodey, *Category Theory* (2nd ed.), §9.6, Corollary 9.17 —` (+ the F4 blank-line fix below). |
| **#671** | In the `### Also covered by` §8.8 block, change the opening citation line to: ``Awodey, *Category Theory* (2nd ed.), §8.8 (Topoi), Remark 8.18, printed page 211 (PDF page 220), item `awodey:8.8:remark18` (multi-part — leg 1/2, the membership criterion; the topos-level adjoint-triple leg is recorded against #387)``. Leave the rest unchanged. (ledger.tsv:1340/:1341 record the two legs; #683/#686 are the batch's precedent for naming the counterpart on both sides.) **Out-of-scope note recorded, not proposed:** #387's copy of the same block cites `printed p. 210, PDF p. 219`, disagreeing with the inventory's 211/220. |
| **#779** *(unverified LOW)* | `with the arrow table at :395` → `with the arrow table at :396` (`396:Program Definition ThreeArrows : Metacategory := {|`; `:395` is blank; `:413` is correct). |
| **#785** | Replace **both** occurrences of `Instance/Two.v:38` with `Construction/Enriched/Two.v:38`. Current-state sentence becomes ``…takes reflexivity from `eid` through `Construction/Enriched/Two.v:38` (`two_from_top : TwoHom TwoY z → z = TwoY`)…``; in the In-tree-donors line, merge `38` into the existing `Construction/Enriched/Two.v:60,65,71,104,113,131,165` (→ `:38,60,65,71,104,113,131,165`) and delete the standalone `Instance/Two.v:38` entry. (`Instance/Two.v:38` is the essay line "   the base over which enrichment produces preorders.  Both readings turn".) |
| **#851** | **Label correction, NOT a demotion.** Replace the two-line justification ``Depends on: #520 (coherence for symmetric monoidal categories, and\nproduct/coproduct as symmetric tensor — the cartesian-tensor vocabulary).`` with: ``Depends on: #520 — the free-standing *symmetric strict monoidal* class (#520's Seven Sketches §4.4.3 checkbox), which is the notion Example 5.71's conclusion names; the tree has no such bundling outside `Construction/PROP.v:68` and `Construction/ColouredPROP.v:108`, both of which pin their objects. NOT consumed from #520: the symmetric coherence theorem, and the cocartesian symmetric tensor — the *cartesian* symmetric monoidal structure this issue needs is already in tree as `CC_SymmetricMonoidal` (`Structure/Monoidal/Internal/Product.v:314`).`` **Keep trailer `["#771","#786","#520","#500"]` and the native edge 851←520.** |
| **#888** *(unverified LOW)* | ``  `Construction/Arrow.v:110`'s `Arrow := Id[C] |> Id[C]` `` → ``  `Construction/Arrow.v:110`'s `Arrow {C : Category} : Category := (Id[C] ↓ Id[C])` `` — the comma-category notation is `↓`, not `|>`. |
| **#918** | `§2.2 Corollary 2.2.8, printed p. 65 (PDF p. 85)` → `§2.2 Corollary 2.2.8, printed p. 64 (PDF p. 84)`. (`riehl/inventory/inventory-2.json` records `riehl:2.2:cor8` at book_page 64 / pdf_pages [84]; 65/85 is where `riehl:2.2:example9` lives, as #929 cites it.) |
| **#1016** | `This obligation appears on **both #1014 and #1014**,` → `This obligation appears on **both #1014 and #1016**,` (the mirror block on #1014 reads `both #1014 and #1016`). |

---

## GROUP F — Contract/format

**F1. Body must END with the trailer.**
- **#589**: move the whole block from `### The reindexing functor this issue's functoriality step needs (added on audit)` through `…cross-reference the appends already imply.` to sit immediately **before** `<!-- catalog: {"ids": ["maclane:X.3:thm1", "maclane:X.3:def2", "riehl:6.2:thm1"], "deps": []} -->`, separated by one blank line. Collapse the double blank line at that boundary. **Also** prefix the note's final line with `- ` so it reads `- Related (NOT blocking): #1021 — no dependency edge is asserted here; the link is a cross-reference the appends already imply.` *(unverified LOW)*
- **#918**: move the trailer to the very end, after the `### Also covered by` block, and collapse the triple newline before that block to a single blank line. **Trailer text depends on the Group A demotion order** — write `<!-- catalog: {"ids":["riehl:1.5:example11","riehl:2.2:cor8"],"deps":["#712"]} -->` if the #231 demotion is applied (it should be); use `"deps":["#712","#231"]` only if that demotion is rejected. Applying the finding's literal text without this care silently re-adds the #231 edge.
- **#972**: move the whole `### SHARED CONSTRUCTION with #1028 (added on audit)` block (heading through ``Recorded in `doc/plan/books/graph/serialize-groups.json`.``) to sit immediately **before** `<!-- catalog: {"ids":["riehl:4.1:example15"],"deps":["#907","#248","#299"]} -->`; collapse the two stray double blank lines. **Preserve the block's text verbatim** — the `- [ ] Build the zig-zag localization **once**.` checkbox and the `Related (NOT blocking): **#1028**.` line are both correct. (This is where `file_chapter.py` itself inserts appended blocks, `body[:tm.start()]`; while the trailer is not last, `file_chapter.py:224` falls to the `else` at `:231` and appends **without updating `ids`**.)
- **#1011 / #1014 / #1016** also carry post-trailer content marked "(added on audit)"; rated LOW/no-action by the verdict. Judgement call 9.

**F2. Trailer ids.** #918 → add `"riehl:2.2:cor8"` (ledger.tsv has two rows for #918; `riehl/issue-map.json` maps both ids to 918).

**F3. Trailer `deps` out of sync.**
- **#807** → `"deps":["#705","7sketches:3.2.1:example3.13"]` becomes `"deps":["#705","7sketches:3.2.1:example3.13","#742"]`. Body and native blocked_by both carry 742; this was the only three-surface disagreement in its batch. No other change.
- **#1021** → `<!-- catalog: {"ids":["riehl:6.2:construction-comma-slice-projection"],"deps":["#345",]} -->` → `…"deps":["#345"]} -->` (delete the trailing comma). It is the only trailer in its batch that `json.loads` rejects: *"Illegal trailing comma before end of array: line 1 column 72 (char 71)"*.
- All Group A/B trailer edits above.

**F4. Stray double blank line after `### Also covered by`** — **#590** (delete one of the two blank lines between `### Also covered by` and the `Awodey,` line).

**F5. Verification placeholders / missing targets.**
- **#544** *(unverified LOW)*: ``- `coqc -R . Category <path>` compiles standalone.`` → ``- `coqc -R . Category Structure/Abelian/Image.v` compiles standalone (or `coqc -R . Category Structure/Abelian.v` if the coimage is added in place).``
- **#602** *(unverified LOW)*: ``- `coqc -R . Category` on the density-coend file compiles from a clean tree.`` → ``- `coqc -R . Category Structure/Coend/Density.v` compiles from a clean tree.`` and tighten the Work sentence to ``Suggested home: `Structure/Coend/Density.v` (new; alternatively fold into `Theory/Kan/Density.v` alongside the §X.6 density notion built by #600).``

**F6. Bare/unresolvable references and unlabelled deps.**
- **#465**: `(the Riehl §5.6 Theorem 5.6.5 issue)` → `(#1008)`.
- **#561** *(unverified LOW)*: `Depends on: #440` → ``Depends on: #440 (`maclane:V.6:construction-variety`) (the variety category `Alg_τ` — the general algebraic setting of `maclane:IX.1:remark2`, of which `Grp` is an instance).`` No trailer or native change; the edge is real, only its label was missing.
- **#590** *(unverified LOW)*: append to the Seven Sketches checkbox `— that witness is #407 (completeness of `Sets` by cone sets), which rests on #254 (indexed products/coproducts in `Sets`).` and add, on its own physical line in `## Dependencies`: ``- Related (NOT blocking): #407, #254 (the `Complete Sets` witness the Seven Sketches clause waits on; the Mac Lane statement itself is parametric in a complete `A`, so no edge is asserted).`` **No edge.**
- **#890** *(unverified LOW)*: add on its own physical line in `## Dependencies`, after the #268 line and before the first `### Also covered by`: ``- Related (NOT blocking): #416 (MacLane V.2, limits from products and equalizers) — the textbook route from the colimit-preservation form to the equalizer form; `Structure/Equalizer/Fork.v` already suffices in-tree, so this issue does not wait on it.`` Do not touch the trailer or native relations.
- **#998**: `the double power set monad of the continuation-monad issue for §5.1` → `the double power set monad of #994`.
- **#1025**: Work item 2 opening `2. Over #935's vector spaces:` → ``2. Over #258's `Vect_k` and #305's free ⊣ forgetful adjunction:``.

---

## GROUP H — `doc/plan/books/graph/serialize-groups.json`

1. **Add** `{"module": "Instance/Rng/MonoidRing.v", "issues": [310, 518], "creator": 518, "reason": "SERIALIZE_ONLY: same-file peers; no dependency edge is asserted, but they must not be dispatched in the same parallel wave.", "rationale": "#518 builds ℤ[M] and the adjunction ℤ ⊣ U; #310's monoid-ring clause is the universal-arrow presentation of the same object and is derivable from it. #518's own Riehl §2.3 checkbox (Ring(ℤ[G],R) ≅ Grp(G,Rˣ)) already discharges #310's group-ring-as-universal-arrow-to-units clause."}`
2. **Add**, keyed on the CONSTRUCTION (not a path — this is the invisible-pair case): `{"module": "EssentialImage (construction, not a path: Construction/Subcategory/EssentialImage.v vs Theory/EssentialImage.v)", "issues": [918, 985], "creator": 918, "reason": "Ordered, not merely serialized: #985 is blocked_by #918, which creates the notion. Recorded because the two propose different paths, so check_collisions.py cannot see the pair.", "rationale": "#985's Suggested-modules line offers `Theory/EssentialImage.v` for the notion 'if the Riehl §1.5 issue has not already landed it' — a live invitation to define it twice."}`
3. **Add**, keyed on the construction: `{"module": "monoid object in (Cat, ×, 1) (construction, not a path: Instance/StrictCat/Monoid.v vs Instance/StrictCat/MonoidObject.v)", "issues": [500, 851], "creator": 500, "reason": "SERIALIZE_ONLY: same identification under two proposed paths, invisible to the path-keyed collision check.", "rationale": "#500's Work says 'with the `Cat`-monoid identification perhaps in `Instance/StrictCat/Monoid.v`'; #851 proposes `Instance/StrictCat/MonoidObject.v`. One path must win; state it in the header of whichever lands first."}`
4. **Add** (arising from the #890 Group A demotion — the verdict rules the shared site-of-opens is a serialize matter, not an edge): `{"module": "site of opens of a space (construction, not a path: Theory/Sheaf/Space.v vs Instance/Top/Cover.v)", "issues": [890, 460], "creator": 890, "reason": "SERIALIZE_ONLY: both construct the site of opens/covers; no edge is asserted in this direction (precedence runs #890 -> #460).", "rationale": "#890 re-founds the Sheaf predicate and builds Theory/Sheaf/Space.v; #460 builds the concrete sheaf of continuous functions over Instance/Top/Cover.v and consumes #890's predicate."}`
5. **Amend** the existing `Structure/Lattice.v` entry: `issues` → `[340, 389, 422, 1003]`, appending to the rationale: `EXTENDED (Riehl Ch5 audit): #1003 (Riehl 5.5, lattices/semilattices as categories over Set) proposes the SAME order-theoretic vocabulary under `Instance/Lattice.v`; the differing path made the collision invisible to check_collisions.py. No precedence exists, so no edge is asserted.` **Apply only if the Group C coordination bullet for #1003 is applied** — that bullet makes #1003 name `Structure/Lattice.v` inside a scanned section, and without this amendment `check_graph` invariant 4 would newly report three CONFLICT-FREE violations. If #1003 stays wholly in `Instance/Lattice.v`, skip this and keep only the coordination bullet and the Related line.
6. **Optional / not required after the fixes above** (record only if the coordinator wants the history visible): `Structure/WellPointed.v` vs `Structure/Separator.v` (#655/#638 — resolved by the Group B edge and #638 dropping its file); `Instance/CompHaus.v` vs `Instance/CHaus.v` (#413/#915 — resolved by #915 ceasing to create the path); `Structure/End/Preservation.v` vs `Structure/End/Parameter.v` (#578/#582 — resolved by the edge, files disjoint).
7. **#683 / `Structure/Thin.v`** — **already head plan Group H item 1.** Do not re-file.

---

## (i) REFUTED — DO NOT ACT

| Finding | Verdict reason |
|---|---|
| **#541** `dependency` — "'Benefits from' glued onto the Depends-on line carries a harvestable id resolving to #536" | **REFUTED.** `check_graph.py`'s `REVERSE_CUE` comment cites **#541 by number** as the reason the SOFT cue family exists ("#541 says only 'Benefits from the finite-biproduct matrix calculus', and promoting that to a hard edge needlessly serialized it behind #536"). Running the module on the live line: `body_deps → [221, 264]`, `unresolved_deps → []`, matching native `blocked_by(541)=[221,264]`. The resolver reads the trailer, `["#221","#264"]`. **No edit.** |
| **#550** `dependency` — "'Provides … used by' would harvest #557 and assert the cycle 550→557→550" | **REFUTED.** `REVERSE_CUE` matches both `Provides?` and `used by`; live run gives `body_deps → [544]`, `unresolved_deps → []`. `resolve_chapter_deps.py` takes edges only from the trailer (`["maclane:VIII.3:def2"]`). No surface harvests the id. **No edit.** |
| **#552** `dependency` — "'Required by' would harvest #551 and assert 551→552→551" | **REFUTED.** `REVERSE_CUE` contains `Required by`, and `check_graph.py`'s header comment quotes **#552's line verbatim** as example (b) of prose that must NOT be read as a dependency. Live run: `body_deps → [542, 545]`, `unresolved_deps → []`. **No edit.** |
| **#518** `dependency` — "add `Depends on: #255`; the Riehl §2.3 units checkbox cannot start when #518 unblocks on #257 alone" | **REFUTED.** `blocked_by 518→[257]`, `257→[256]`, `256→[255]`: `Grp` is already a transitive prerequisite, so the ordering the finding wants is already enforced. A direct edge is a harmless no-op but fixes nothing and **must not be reported as a readiness defect**. (Note: the same reasoning does *not* rescue the analogous #530 finding — #530's `blocked_by` is empty.) |

**Partial refutations — apply the surrounding fix but NOT these clauses:**
- **#604**: do **not** take branch 2 (deleting `Depends on: #398` / the trailer entry / the native edge). #604's own Source item `maclane:X.7:ex1` scopes Exercise 1 as "a special case of the adjoint-square/mates bijection", and #398's `AdjointSquare` + `palmquist_bijection` do not exist in-tree.
- **#851**: do **not** delete the #520 edge. #520's Seven Sketches §4.4.3 checkbox delivers the free-standing *symmetric strict monoidal* class that Example 5.71's conclusion names; the tree has no such bundling (`Structure/Monoidal/Strict.v:52` is `StrictMonoidal` alone; the only strict+symmetric bundles, `Construction/PROP.v:68` and `Construction/ColouredPROP.v:108`, pin their objects). Apply the Group E label rewrite instead.
- **#450**: do **not** file the head plan's second checkbox as written ("`⟨Ω,E⟩-Alg` has no objects exactly when the signature has no constants") — false.
- **#647**: do **not** write "at most 6 nodes and at most 3 edges" — false (n=2, e=4).
- **#518**: do **not** add `Depends on: #310` — direction reversed.
- **#1003**: do **not** write "prove the dictionary to #340's `LatticeObject`" — undeclared scope plus an undeclared #340 dependency, contradicting the same edit's "no edge is asserted".
- **#1033**: do **not** rename `Theory/Kan/SelfExtension.v` → `Theory/Kan/IdentityPointwise.v` — the name correctly describes Work item 2 (a functor as a (co)limit over its own (co)slices).
- **#755**: do **not** write "#737 is not the builder" — #737 explicitly schedules "Prove the identification lemma … a colimit of a discrete diagram is a join and a limit is a meet" with `proset_colimit_is_join` in its Verification.
- **#873**: do **not** write "#872's own DoD wants a concrete `Print Assumptions` witness that #873 supplies" — #872's DoD asks that `DecoratedCospan_Hypergraph` become an unconditional consequence of lax symmetric monoidality, achieved generically.
- **#723**: do **not** write "part (b) consumes #718's diagram exponentials" — #718 is Sets-valued, the same mismatch used to demote #404.
- **#632**: do **not** instruct upgrading `Theory/Bicategory.v:113` — that is the Mac Lane–Paré strictification theorem, not #497's deliverable.
- **#655**: do **not** instruct #638 to consume #447's `Separator` — it would create a second undeclared consumption.
- **#890**: do **not** repeat "#890 consumes nothing #460 builds" — #890's positive-instance DoD box does consume one; re-home it in the same edit (mandatory 5th edit above).

---

## (ii) JUDGEMENT CALLS — decide individually, do not script

1. **#518 vs #310 — who owns the monoid ring.** The finding said #518 consumes #310; the adversarial pass reversed it and made **#518 the creator**. Tradeoff: #518 is focused and nearly ready (blocked only on #257) whereas #310 is a three-part bundle additionally requiring a `Instance/Vect` substrate that does not exist and has no builder issue — blocking #518 behind it could stall it indefinitely. But the reversal means the edge, if wanted, belongs on **#310** (`Depends on: #518 …`), an out-of-batch edit. **Default: the ADJUSTED version above (no edge, path unified, serialize group).** Decide whether to also touch #310.
2. **#723 and #718.** #718 stands in the *same* Sets-vs-FinSet relation to part (b) that justified demoting #404. If #404 is demoted, #718's blocking status deserves the same scrutiny — deliberately deferred to a later pass rather than folded into this edit.
3. **#873 — keep the edge or parallelise.** Keeping it (recommended) costs one scheduling wave but guarantees the six `DecCospan_*_Coherent` instances land once; deleting it (`"deps":[]`, drop native 873←872, add `- Related (NOT blocking): #872 — derives the same coherences generically; whichever lands first, the other consumes rather than re-derives.`) buys parallelism at the risk of two competing instance sets for the same six classes. Whichever is chosen, **apply the same wording to #876** or the two drift.
4. **#822 — demote (a) or home the reuse (b).** (a) drops the edge and records the link in prose. (b) keeps it by adding a real consumption: Work item `6. Instantiate the generalised interpreter at a thin symmetric monoidal base and prove it agrees with #776's validity predicate `Structure/Monoidal/Preorder/Diagram.v`, so the order-level diagram calculus is literally the thin case.` plus DoD `- [ ] The thin-base instantiation is proved to agree with #776's validity predicate (not merely noted in the header)`. (b) is more mathematics; (a) is the cheap correct-as-filed reading.
5. **#890 — reverse edge or not.** Removing 890←460 is right (it inverts precedence and parks a seven-issue hub). Whether to *add* 460←890 is optional: #460's Work already presumes #890's strengthening, so the edge is honest, but it lengthens #460's chain.
6. **#1003 — `Structure/Lattice.v` or `Instance/Lattice.v`.** Landing the vocabulary in `Structure/Lattice.v` beside #340/#389/#422 requires the Group H amendment or `check_graph` invariant 4 fires three new violations. Staying in `Instance/Lattice.v` keeps the collision invisible but is architecturally defensible — the repo already ships both idioms (`Structure/Monoid.v` `MonoidObject` vs `Instance/CMon.v`). **Do not do half.**
7. **NEW duplicate pair surfaced, outside this chunk's issue set: #422 vs #737.** Both schedule the Proset colimit-is-a-join / limit-is-a-meet identification (#422 the general J-indexed statement; #737 the discrete-diagram case with `proset_colimit_is_join` in its Verification and its own DoD box). Register it as its own duplicate pair — #737 should consume #422 rather than re-prove the discrete case. Not actionable from this chunk's evidence alone.
8. **#647 bound refinement.** `n + e ≤ 6` is solid. The parenthetical `e ≤ 4` rests on "loops excluded by acyclicity" and is a weaker claim; consider dropping it and keeping only `n + e ≤ 6` (hence `n ≤ 6`, `e ≤ 5`) in the DoD.
9. **Post-trailer blocks on #1011 / #1014 / #1016.** The contract says the trailer ends the body; all three deviate, all three are marked "(added on audit)", and all three still parse. Sweep them into F1 alongside #589/#918/#972, or leave them and accept that `file_chapter.py` will silently skip the `ids` update on the next append to any of them.
10. **#465's edge strength.** The verdict notes #388 is arguably needed for #465's **main** Work item ("build the endofunctor `R ⊗ (−) : Ab ⟶ Ab`"), not merely the appended checkbox — i.e. stronger than filed. The edge is right either way; only the stated reason would change.

---

<!-- ===== section 4 ===== -->

# QA remediation plan — tail chunk 3

**Covered: issues #437–#1017 (56 distinct issues). 73 findings read from `doc/plan/books/qa/findings-tail-3.json`, carrying 89 adversarial verdicts (62 CONFIRMED, 27 ADJUSTED, 0 REFUTED). 10 findings carry NO verdict (LOW severity, never independently verified) — flagged inline as `[UNVERIFIED]`.**

Group letters follow `doc/plan/books/qa/plan-head-216-471.md`. Two campaign-wide questions the head plan already settled recur here and are **not re-litigated**: the `deps` array convention (item ids vs `"#N"` — head plan §4.7; every trailer edit below is written in whichever form that issue already uses, and must be normalised only after the convention is picked), and the `Instance/Mod`/`Instance/Module` and `Rng` clashes (neither touches this range). **One direct conflict with the head plan is recorded in §(ii) judgement call 1 — head plan Group H item 3 must not be applied.**

---

## GROUP A — Demote fabricated or over-asserted hard edges

Mechanical shape unchanged from the head plan: (i) delete the `Depends on:` line from `## Dependencies`; (ii) add the reason on its own physical line starting `- Related (NOT blocking):`; (iii) `gh api -X DELETE repos/jwiegley/category-theory/issues/<ISSUE>/dependencies/blocked_by/<N>`; (iv) drop the entry from the trailer `deps`.

| Issue | Edge to demote | Evidence (verbatim) | Exact replacement line |
|---|---|---|---|
| **#678** | #669 | Body `## Dependencies` is exactly `Depends on: #669 (\`awodey:5.1:def1\`)`; trailer `"deps":["awodey:5.1:def1"]`; blocked_by = 669. But #678's In-tree donors line reads `In-tree donors: \`Theory/Subobject.v\`, \`Theory/Subobject/Functor.v\`, \`Theory/Morphisms/Stability.v\`, \`Structure/Pullback.v\`` — the library files, not #669. #669's four Work items (`SubCat x : Category`, the slice identification, the Remark 5.4 poset quotient, a header note) appear in no #678 Work item or DoD line. | Section body → `None.`; trailer → `<!-- catalog: {"ids":["awodey:5:ex4"],"deps":[]} -->`; then `- Related (NOT blocking): #669 (the subobject preorder as a category) — same \`Theory/Subobject.v\` vocabulary, but nothing here consumes it.` **Do NOT copy the finding's line numbers into any edit** — verdict: `SubObj_Setoid` is `:33` not `:32`, `sub_le_unique` `:78` not `:76`, `sub_equiv_iff_mutual` `:93` not `:91`. |
| **#691** | #689 | Work item 4 verbatim: `Prove cartesian closure with the function type as exponential and the book's explicit evaluation and transposition terms, verifying the two equations of the equational characterization (which is the companion §6.4 issue's deliverable — **use its smart constructor if it has landed**, which is exactly the route Awodey takes).` DoD is route-neutral; Verification asks only `Print Assumptions CL_Closed`, dischargeable against `Structure/Cartesian/Closed.v:43`'s `Class Closed` (`exp_iso` at `:51`, both verified). | Delete `- Depends on: #689 (\`awodey:6.4:prop12\`)`; trailer → `<!-- catalog: {"ids":["awodey:6.5:construction-category-of-types","awodey:6.5:prop-category-of-types-ccc"],"deps":["awodey:6.5:def-typed-lambda-calculus"]} -->`; add `- Related (NOT blocking): #689 (the equational characterization of CCCs) — if its smart constructor has landed, build \`CL_Closed\` through it; otherwise build directly against \`Structure/Cartesian/Closed.v\`'s \`Closed\` class.` Unblocks #691 → #693/#694/#695. |
| **#736** | #436 | #736's own Current state quotes the theorem as already proved: `\`Adjunction/GAFT.v:241\` — \`Theorem GAFT (U : C ⟶ D) (comp : @Complete C) (cont : @PreservesImageLimit C D U) (sols : forall d : D, SolutionSet U d) : { F : D ⟶ C & F ⊣ U }\`` (verdict re-verified the source line). Work item: `State and prove \`GAFT_small ...\` **as an immediate corollary of \`GAFT\`**`. #436's deliverables (`solution_set_of_adjunction`, `GAFT_iff`) appear in no Work item and none of #736's ten DoD checkboxes. | Delete `Depends on: #436`; add `- Related (NOT blocking): #436 packages \`GAFT_iff\`, the characterization form; \`GAFT_small\` is a corollary of the already-proved \`Adjunction/GAFT.v:241\` \`GAFT\` and consumes nothing from it.` Drop `"#436"` from trailer; `DELETE …/736/dependencies/blocked_by/436`. No serialize group needed (#436 extends `Adjunction/GAFT.v`, #736 creates `Adjunction/GAFT/Small.v`). |
| **#843** | #789 | The Depends-on entry is self-incriminating, verbatim across two physical lines: `Depends on: #789 (V-matrices over a quantale and the category Mat(V) — the` / `quantale-entry sibling; the two should share the Σ-fold infrastructure).` #789 builds `VMatrix V X Y := X → Y → V` with `(M ∗ N)(x,z) := ⋁_{y} M(x,y) ⊗ N(y,z)` over arbitrary SETS; #843 needs `hom m n := Fin.t m -> Fin.t n -> carrier R` with a Σ-fold over `Fin.t`. #843's In-tree donors credit `the rig class and the Σ-swap lemma of §5.3.1` (= #839). | Delete both physical lines; add `- Related (NOT blocking): #789 (V-matrices over a quantale). Once #840 lands (every quantale determines a rig), #789's \`Mat(V)\` is the quantale-rig instance of this construction at arbitrary index sets; if a bridge is wanted it belongs in a Work item on one of the two issues, not as an edge here.` Trailer → `<!-- catalog: {"ids":["7sketches:5.3.3:eq5.48","7sketches:5.3.3:remark5.49","7sketches:5.3.3:def5.50","7sketches:5.3.3:ex5.51"],"deps":["#221","7sketches:5.3.1:def5.36","7sketches:5.2.1:def5.2"]} -->`; `DELETE …/843/dependencies/blocked_by/789`. **Leave #221, #839, #826 alone — all three genuine.** |
| **#970** | #225 | `Depends on: #225 (MacLane I.2: The simplicial category Delta) — the cofaces and codegeneracies are \`Δ\`'s generating maps; build them once.` But #225's Work section ends `- Optional stretch (document if deferred): faces/degeneracies as generators and the simplicial identities, which Chapter VII will need.` — the cited content may never be delivered. #225's `Instance/Simplex.v` has `objects \`nat\`; \`hom m n :=\` monotone functions \`Fin.t m → Fin.t n\`` (cofaces are MORPHISMS); #970 item 2 builds `the coface \`d^i : Ord n ⟶ Ord (S n)\` and codegeneracy \`s^i : Ord (S n) ⟶ Ord n\`` (FUNCTORS). #225 blocked_by = []. | Delete the line; trailer deps `["#224","#225"]` → `["#224"]`; remove 225 from blocked_by; add `- Related (NOT blocking): **#225** builds the simplicial category Δ, whose morphisms include the coface/codegeneracy monotone maps; the functors \`d^i\`/\`s^i\` built here between ordinal CATEGORIES are the 2-categorical counterpart. Cross-link the two files, but no edge is asserted.` |
| **#975** | #283 | `Depends on: #283 (MacLane II.5: Strict 2-categories, double categories, and Cat) — the ambient double-category vocabulary.` Contradicted by #975's own Current state: `\`Theory/DoubleCategory.v\` supplies the pseudo double category class (strict vertical category, weak horizontal composition, squares \`dsq h u v k\` as setoids with the \`dsq_coerce\` boundary calculus, horizontal pasting with \`dinterchange\`, and the globular \`dassoc\`/\`dunit_left\`/\`dunit_right\`)`. #283 builds `Theory/TwoCategory.v` (STRICT). Also redundant: `blocked_by(399)` = 394,395,283. | Delete the line; trailer `["#398","#283","#399"]` → `["#398","#399"]`; remove 283 from blocked_by; add `- Related (NOT blocking): **#283** builds the STRICT double-category and 2-category vocabulary in Theory/TwoCategory.v; this issue instantiates the in-tree PSEUDO class of Theory/DoubleCategory.v instead. (#283 already reaches this issue transitively through #399.)` |
| **#991** | #414 | `Depends on: #414 (\`Cat\` is small-complete — the other half of Corollary 4.6.15)`. #991's own Current state scopes it out: `Gap: the adjunction, the reflectivity, and both halves of the corollary are absent; the completeness half is separately filed (#414).` Work item 5: `Record in the header how this relates to the direct route: \`Complete Cat\` is #414's products-and-equalizers construction, and this issue supplies the colimit half by reflection, as Riehl does.` Ledger split confirmed: `ledger.tsv:2567` `riehl:4.6:cor15 PARTIAL #991 … part: Cat is cocomplete`; `:2637` `… #414 … part: Cat is complete`. | Delete the line; add `- Related (NOT blocking): **#414** owns the OTHER half of Corollary 4.6.15 (\`Complete Cat\`, ledger.tsv:2637). Work item 5 only cross-references it in the header; the cocompleteness proof here consumes nothing it builds. No dependency edge is asserted.` Remove `"#414"` from trailer; `DELETE …/991/dependencies/blocked_by/414`. Module sets are disjoint (`Instance/Cat/Limit.v` vs `Construction/Nerve/Homotopy.v` + `Instance/Cat/Cocomplete.v`), so invariant 4 cannot trip. |
| **#1009** | #329 | `Depends on: #329 (chain unions as colimits and the cocompleteness of \`Sets\`)`. Verdict grepped the body: the ONLY hits for "329" or "Sets" are that line and the trailer — `Sets` does not occur even in the Background. Work item 2: `Prove Proposition 5.6.11: for \`C\` cocomplete and \`U : A ⟶ C\` monadic, \`A\` is cocomplete iff \`A\` has coequalizers.` | Delete the line; trailer `["#320","#329","#483"]` → `["#320","#483"]`; `DELETE …/1009/dependencies/blocked_by/329`. If a sanity witness is wanted instead, add a Work item `instantiate Proposition 5.6.11 at \`Sets\` once #329 lands` and keep the edge — do not leave the edge with no consumer. |
| **#982** `[UNVERIFIED — LOW, no adversarial verdict]` | #238 | `Depends on: #238 (MacLane I.4: The skeleton equivalence between finite sets and finite ordinals) — the finite model against which the cardinal identities are sanity-checked.` But Work item 3 says `\`Instance/FinSet.v\` is the finite model to sanity-check against` and item 5 `Sanity examples: check the finite instances compute by \`eq_refl\` in \`Instance/FinSet.v\`, in the spirit of \`Instance/FinSet/Topos.v\`'s \`Pow 2 = 4\`.` | Prefer (a): delete the line, trailer → `["#374"]`, remove 238 from blocked_by, add `- Related (NOT blocking): **#238** builds the finite skeleton equivalence and a finite cardinal functor \`# : Set_f ⟶ FinSet\` (Instance/FinSet/Skeleton.v) — a template for the \`Card\` layer here, but not a prerequisite.` |

**Edge SWAPS (demote + add in one edit — apply as a unit):**

- **#515**: replace `Depends on: #512 (\`maclane:VII.5:construction1\`)` with `Depends on: #513 (\`maclane:VII.5:construction2\`) — supplies the coface \`δᵢ\`/codegeneracy \`σⱼ\` generators and the cosimplicial identities that clause (c) dualizes`. Trailer `["#225","maclane:VII.5:construction1"]` → `["#225","maclane:VII.5:construction2"]`. Remove blocked_by 512, add 513. Also append to the donors line so it reads `In-tree donors: #225, \`Instance/Delta/Arrows.v\` (cofaces/codegeneracies), \`Instance/Fun.v\`, \`Instance/Sets.v\`, \`Construction/Subcategory.v\`, \`Structure/Complete.v\`.` Evidence: #512 builds only `(a) equip Δ with the strict monoidal structure of ordinal addition \`+ : Δ ∏ Δ ⟶ Δ\`, unit 0; (b) exhibit the universal monoid \`⟨1, μ : 2 → 1, η : 0 → 1⟩\`; (c) prove Proposition 1 …`, none of which appears in #515's five Work clauses; #515 clause (c) is `derive the equivalent face \`dᵢ\`/degeneracy \`sᵢ\` presentation and the dual simplicial identities`, the op-dual of #513's `(a) the epi-mono factorization of monotone maps and the coface \`δᵢ\`/codegeneracy \`σⱼ\` generators; (b) the (co)simplicial identities and the unique coface-then-codegeneracy normal form`. Ordering is preserved: #513 is itself blocked_by [225,512].
- **#472**: replace `Depends on: #360 (the forgetful \`R-Mod → Ab\`/\`Set\` and its adjoints)` with `Depends on: #309 (\`maclane:III.1:remark2\`) — the free R-module on a set as a universal arrow to the forgetful functor, which IS \`T_R\`.` Trailer `["#258","#360"]` → `["#258","#309"]`; remove blocked_by 360, add 309; in the Work bullet change `(cf. #360)` to `(cf. #309)`. **The verdict corrected the label** — the finding's `maclane:III.1:ex7` is #309's polynomial-algebra leg; the free-R-module leg is `maclane:III.1:remark2`. Evidence: #360 builds only `2. Construct the extension-of-scalars functor \`A ↦ R ⊗_Z A\` … 3. Construct the coextension functor \`A ↦ Hom_Z(R, A)\` …` (R-Mod ⇄ Ab), while #309 has `Over #258's module categories, construct the free R-module on a setoid and prove its insertion of generators a universal arrow to the forgetful functor`. Cycle-safe: closure(309) = {255,256,257,258}.

---

## GROUP B — Add missing hard edges

Shape: `Depends on: #N (\`item-id\`) — <reason>` on its own physical line in `## Dependencies`; add the id to the trailer `deps`; `gh api -X POST …/<ISSUE>/dependencies/blocked_by -F issue_id=$(gh api repos/jwiegley/category-theory/issues/<N> --jq .id)`. **Use the numeric database id, not the GraphQL node id** (verdict correction on #617; the same correction applies to every POST in this group).

| Issue | Add edge to | Exact `Depends on:` line | Cycle check / note |
|---|---|---|---|
| **#437** | #428 | `Depends on: #428 (\`maclane:V.4:thm1\`) — supplies the continuity of the covariant hom-functor, the converse half of the Theorem 3 biconditional` | **Already in head plan Group B — do not double-file.** My chunk adds only the prose fix: replace the bare `the §V.4 issue` in Work item 2 with `#428`. closure(428) = {254,331,351,353,407}. |
| **#455** | #413 | `Depends on: #413 (\`maclane:V.1:ex2\`) — supplies \`CompHaus\` and the creation of limits by its underlying-set functor, the subject of the discrete case` | **Head plan Group B has this edge but WITHOUT the label**; the verdict supplies it: `maclane:V.1:ex2`, verified in #413's Source line and trailer. Also replace all three `the §V.1 CompHaus issue` occurrences with `#413`. reach(413) = {259,406}. |
| **#461** | #434 | `Depends on: #434 (\`maclane:V.5:ex3\`) — transfer of cocompleteness along a reflection, used by the Riehl §4.7 checkbox` | The finding's `maclane:V.5:thm-reflective-cocomplete` **is fabricated** — #434's trailer ids are `[maclane:V.5:ex3, riehl:4.6:prop14, riehl:4.6:exxi]`. Checkbox that consumes it: `- [ ] (from Riehl §4.7) The reflector obtained BY the general adjoint functor theorem …, and the cocompleteness of \`Haus\` derived by transfer along the reflection (#434).` closure(434) = {373}. |
| **#469** | #641 | `Depends on: #641 (\`awodey:1.4:construction-pos\`) — the category \`Pos\` of posets, required by the Awodey §10.3 checkbox` | Label verified correct (verbatim in #641's trailer). Checkbox: `- [ ] (from Awodey §10.3) the poset instance: … (needs the category of posets from #641).` Verdict also confirmed `Pos` genuinely absent in-tree: `Instance/Poset.v:116` `Definition Poset … := Proset P` is a SINGLE poset as a thin category. closure(641) = {}. |
| **#487** | #1003 **and** #261 | `Depends on: #1003 (\`riehl:5.5:def-lattice\`) — the lattice / meet-semilattice / join-semilattice categories over Set, with their forgetful functors` / `Depends on: #261 (\`maclane:I.7:construction6\`) — the category \`Set_*\` of pointed sets` | The finding's `maclane:I.7:construction-pointed-sets` **is fabricated** — #261's trailer ids are `[maclane:I.7:construction6, maclane:I.7:prop2, awodey:1.6:example8, awodey:7.9:example26]`. Also in the checkbox replace `(see the Riehl §5.5 lattices issue)` with `(#1003)`. Checkbox verbatim: `- [ ] (from Riehl §5.5) Instantiate the theorem at the lattice, meet-semilattice and join-semilattice categories (see the Riehl §5.5 lattices issue) and at pointed sets (#261) …` closure(1003) = closure(261) = {}. |
| **#530** | #255 | `Depends on: #255 — the category \`Grp\`, needed only for the Riehl §3.1 concrete-instance checkbox; the core biconditional needs nothing beyond the in-tree \`Additive\`/\`Kernel\`/\`Equalizer\` donors.` | Then, on its own line, `- Related (NOT blocking): this lemma is a reusable prerequisite of the finite-(co)completeness assembly in §VIII.3.` (splitting the current one-line body `None (uses only in-tree \`Additive\`/\`Kernel\`/\`Equalizer\` donors, all present). This lemma is a reusable prerequisite of the finite-(co)completeness assembly in §VIII.3.`). Trailer `[]` → `["#255"]`. Consuming checkbox: `- [ ] (from Riehl §3.1) The concrete \`Grp\` instance once #255 lands: the equalizer of \`φ, ψ : G ⇉ H\` is the subgroup \`{g | φ g = ψ g}\` …`. **See judgement call 2 — this blocks a 20-line core lemma and #542 behind it.** |
| **#566** | #333 | `Depends on: #333 (\`maclane:III.4:ex1\` — builds \`Sets_HasPullbacks : HasPullbacks Sets\` in \`Instance/Sets/Pullback.v\`)` | Trailer → `<!-- catalog: {"ids":["maclane:IX.2:ex3","maclane:IX.2:ex4"],"deps":["maclane:IX.2:ex2","#333"]} -->`. Pairs with the Group C rewrite below. #333's own body already says `**Downstream consumers:** #730 and #402 both target the same new module \`Instance/Sets/Pullback.v\` … Land this issue first; do not duplicate the construction there.` — #566 is a third consumer absent from that list. |
| **#617** | #337 | Replace the `## Dependencies` body `None.` with `Depends on: #337` | Trailer `"deps": []` → `"deps": ["#337"]`. Consuming checkbox verbatim: `- [ ] (from Riehl §3.6) Pullbacks in \`Cat\` (needed to form \`C₁ ×_{C₀} C₁\`) taken from #337 rather than rebuilt`. #337 depends only on #290; `ls Instance/Cat/` re-verified as `Bicategory.v, Cartesian.v, Cocartesian.v` + two subdirs. |
| **#712** | #255 | `Depends on: #255 (MacLane I.6: Grp, the category of groups — the \`Groups ↣ Sets\` illustration checkbox in the §7.1 append below cannot be stated without it)` | Trailer gains `"#255"`. Self-declaring checkbox: `- [ ] (from Awodey §7.1) The two illustrations: … and there is no concrete \`Grp\` (blocked on #255).` blocked_by(255) = []. |
| **#714** | #382 | `Depends on: #382 (the powerset preorder of a setoid as a thin category, \`Instance/Powerset.v\` — three of the appended Seven Sketches checkboxes below compare \`Low(P)\`/\`Up(P)\` against it and cannot be stated without it)` | Then on its own line `- Related (NOT blocking): the general reflection statement is #428's and #316's neighbourhood, and the sieve generalisation of lower sets is scoped to #403.` (splitting the current `None blocking. Related filed work: …`). Trailer `[]` → `["#382"]`. Appends say it: `the power set as an ORDER is #382's`. Cycle-checked: 382 ← [380,227,311], none reaching 714. |
| **#777** | #778 | `Depends on: #778 (\`7sketches:2.5.1:example85\`) (the negated-closedness statement shape, which Example 2.86 reuses rather than re-inventing).` | Trailer → `"deps":["7sketches:2.2.1:def2","7sketches:2.5.1:def79","7sketches:2.5.1:example85"]`. #777 DoD: `- [ ] The chemistry preorder is proved *not* monoidal closed (Example 2.86; printed p. 70), reusing the negated-closedness statement shape rather than introducing a second one.` #778 item 4: `This requires the negated-closedness statement shape, which the library does not yet have — introduce it here.` blocked_by(778) = 771,797,799,756,490,389 — no cycle. |
| **#817** | #821 | `Depends on: #821 (\`7sketches:4.4.4:remark4.53\`) (the thin-base collapse of enrichment data to properties, which Work item 2 consumes rather than restates).` | Trailer → `<!-- catalog: {"ids":["7sketches:4.3.3:remark4.39"],"deps":["#799","#380","#771","7sketches:4.4.4:remark4.53"]} -->`. Work item 2: `… — consuming the §4.4.4 Remark 4.53 collapse rather than restating it.` #811 already declares the same edge correctly, so #817 is the outlier. blocked_by(821) = [771,785]. |
| **#991** | #715 **and** #329 | `Depends on: #715 (colimits in a functor category are pointwise, and presheaf categories are cocomplete — the cocompleteness of simplicial sets that Work item 4 transfers)` / `Depends on: #329 (cocompleteness of \`Sets\` — the base of that pointwise cocompleteness)` | Work item 4 verbatim: `with simplicial sets cocomplete pointwise (from cocompleteness of \`Sets\`, #329, via pointwise colimits in a functor category, #715), transfer along the reflection (#434) to obtain \`Cocomplete Cat\``. Final trailer after the Group A removal: `["#621","#515","#434","#715","#329"]`. **Do NOT add the #1034 edge** — see §(i). |
| **#516** ← **#557** | #557 | On **#516**: `Depends on: #557` | Direction is 516→557 (516 consumes). blocked_by(557)=[550] (closure {550,544}), blocked_by(516)=[515,514,256,259] — no path either way, no cycle. Pairs with the Group C rewrite. |
| **#657** ← **#638** | #638 | On **#657**: `Depends on: #638 (\`ExternalAC\` in \`Structure/Choice.v\`).` | Trailer gains `"#638"`. blocked_by(638)=[637], blocked_by(657)=[429] — no path, no cycle. Pairs with the Group C rename. |

---

## GROUP C — Rewrite Work item to CONSUME, not rebuild

| Issue | Rebuilt artifact | Owner | Exact edit |
|---|---|---|---|
| **#557 / #516** | chain complex + homology object/functor in an abelian category | **#557** | #557 Work: `Define a chain complex in an abelian category (an \`ℤ\`- or \`ℕ\`-indexed family of objects and boundary maps with \`∂ₙ ∘ ∂ₙ₊₁ ≈ 0\`, using \`zero_mor\`), and the dual cochain complex.` / `Suggested module: \`Structure/Abelian/Homology.v\`.` #516 Work (a): `Define a chain complex in an abelian category and the homology functor \`Hₙ = ker/im\`` / `Suggested modules: \`Structure/Homology.v\` (chain complexes and homology of simplicial objects)`, with DoD box `- [ ] Chain complex and homology functor in an abelian category defined.` **Edits:** on **#516** rewrite Work item (a)'s opening to `Consume the chain complex and homology object of #557 (\`Structure/Abelian/Homology.v\`); do not redefine them`, drop the chain-complex/homology role from its `Structure/Homology.v` description, DELETE the DoD box above, add `Depends on: #557`. On **#557** add `- Related (NOT blocking): #516, which restates the same chain-complex-and-homology definition under the path \`Structure/Homology.v\`; build it ONCE here.` Already propagated: #519 depends on #516 and names `Structure/Homology.v` as donor; #1011 depends on #557 and names "the chain complexes of #557". |
| **#566** | `HasPullbacks Sets` | #333 | Work item 1 currently: `- Supply (or assume, and discharge) pullbacks in \`Set\`; prove coproducts commute with pullback in \`Set\` (Exercise 3): the canonical map from the coproduct of the fibre pullbacks to the pullback of the coproduct is an isomorphism.` → `- Consume \`Sets_HasPullbacks\` from #333 (do NOT rebuild pullbacks in \`Set\`); prove coproducts commute with pullback in \`Set\` (Exercise 3): the canonical map from the coproduct of the fibre pullbacks to the pullback of the coproduct is an isomorphism.` (+ Group B edge). Re-verified in tree: `Structure/Pullback.v:215` is `Class HasPullbacks (C : Category) : Type := {` and `Instance/FinSet/Classifier.v:264` `FinSet_Pullbacks` is the ONLY instance tree-wide. |
| **#585** | `Structure/Limit/Initial.v` (file creation) | #334 | Work item 1: `Suggested home: \`Structure/Limit/Initial.v\` (new) or an addition to \`Structure/Initial.v\`, using \`Structure/Cone.v\` and \`Structure/Limit.v\`.` → `Suggested home: \`Structure/Limit/Initial.v\` — created by #334; ADD to it, do not recreate it (or place the lemma in \`Structure/Initial.v\`)`. #334's own Verification says `\`coqc -R . Category Structure/Limit/Initial.v\` compiles standalone after its dependencies`; #585 is blocked_by [334], so the file exists when #585 starts. |
| **#638 / #657** | `∀ x y (f : x ~> y), Epic f -> Retraction f` | **#638** | Byte-identical statements: #638 `InternalAC C := ∀ x y (f : x ~> y), Epic f -> Retraction f` (every epi splits); #657 `ExternalAC (C : Category) := forall x y (f : x ~> y), Epic f -> Retraction f`. (1) In **#638** rename `InternalAC` → `ExternalAC` throughout (Work bullet, DoD, both Verification lines, and the bundle `CategoryOfSets C := ElementaryTopos C + WellPointed C + InternalAC C + NaturalNumbersObject C`), keeping `Structure/Choice.v`. **The DoD line must be the MERGED form** (verdict rider — do not apply two conflicting rewrites to the same physical line): `- [ ] \`ExternalAC\` defined (\`Structure/Choice.v\`); \`CategoryOfSets\` bundles \`ElementaryTopos\` + \`WellPointed\` (from #655) + \`ExternalAC\` + NNO.` (2) In **#657** replace the checkbox with `- [ ] (from Riehl §1.2) Reuse the \`ExternalAC\` predicate built by #638 (\`Structure/Choice.v\`) — do not re-define it — and record that Riehl's name for it is the *external* axiom of choice`. **Evidence correction the verdict requires carrying into the edit:** #657 does NOT propose `Theory/Morphisms/Classes.v` as the home — its checkbox says the predicate is `stated over \`Theory/Morphisms/Classes.v\`'s class vocabulary`, and #657's own path is `Instance/Sets/Choice.v`. Leave #657's second Riehl checkbox untouched. (+ Group B edge.) |
| **#648** | `Mon` over `Sets`; free monoid | in-tree / #296 | **Use the ADJUSTED text, not the finding's.** (1) Current state → `The generic internal-monoid category \`Mon(C)\` exists (\`Theory/Algebra/Monoid/Hom.v:83\`, faithful \`Mon_Forget\` at \`:93\`) but is never instantiated at \`Sets\`, even though \`Instance/Sets.v:283\` already exports \`Sets_Product_Monoidal : @Monoidal Sets\` — so \`@Mon Sets Sets_Product_Monoidal\` is a one-liner. \`Instance/CMon.v\` covers only commutative monoids, and nothing anywhere characterizes the monos of a monoid category.` (2) Work bullet 1 → `- Obtain **Mon** by instantiating the existing generic \`Mon\` (\`Theory/Algebra/Monoid/Hom.v:83\`) at \`Sets\` through the already-exported \`Sets_Product_Monoidal\` (\`Instance/Sets.v:283\`), reusing \`Mon_Forget\` (\`:93\`) as \`U : Mon ⟶ Sets\`, and register the thin wrapper as \`Instance/Mon.v\`; do NOT build a second monoid category with \`Instance/CMon.v\` as a template. Coordinate with #296 Work item 1, which also fixes a concrete monoid category.` (3) Work bullet 2 → `- Reuse #296's free monoid and its \`UniversalArrow\` against \`U\`; this issue only specializes it to \`M(1)\`. If #296 fixes its base over Coq types (\`Instance/Coq/Monoid/Free.v\`) rather than \`Mon(Sets)\`, state only the \`M(1)\`-at-\`Mon(Sets)\` specialization here.` Drop `Instance/Mon/Free.v` from suggested paths. (4) **Do NOT route through `CC_CartesianMonoidal` over `Instance/Sets/Cartesian.v`** as the finding proposed — unnecessary detour. (5) **KEEP `Instance/Mon.v`** — it is the donor #649 names; removing it invalidates the #649→#648 edge. |
| **#718** | terminal presheaf `@Terminal (@Fun C D)` | #339 | The issue's own blockquote already says `**Shared obligation — the terminal presheaf belongs to #339.** … Do NOT rebuild it here: land #339's terminal instance first and consume it.` Three lines below, unamended: `Suggested modules: \`Instance/Fun/Terminal.v\` and \`Instance/Fun/Closed.v\` (new satellites of \`Instance/Fun.v\` …)` and `1. Terminal presheaf: \`@Terminal (@Fun C D)\` from \`@Terminal D\`, objectwise. With \`Instance/Fun/Cartesian.v:111\` this gives finite products in \`[C, D]\`.` **Edits:** (a) module line → `Suggested module: \`Instance/Fun/Closed.v\` (new satellite of \`Instance/Fun.v\`, alongside the existing \`Instance/Fun/Cartesian.v\`); \`Instance/Fun/Terminal.v\` is #339's.`; (b) item 1 → `1. Consume #339's \`@Terminal (@Fun C D)\` from \`Instance/Fun/Terminal.v\`; with \`Instance/Fun/Cartesian.v:111\` this gives finite products in \`[C, D]\`.`; (c) delete `coqc -R . Category Instance/Fun/Terminal.v` from Verification. **Leave the DoD checkbox** — it is a correct gating condition. |
| **#724** (two findings — **merge into ONE edit**, both touch the Suggested-module-paths line) | `Coproduct_Diagonal_Adjunction` (owner #351); `Functor/Coproduct/Internal.v` (owner #658) | #351, #658 | (a) Suggested-module-paths line → `Suggested module paths: extend \`Adjunction/Diagonal/Product.v\`; \`Functor/Coproduct/Internal.v\` is #658's and \`Adjunction/Diagonal/Coproduct.v\` is #351's — this issue adds only the converses.` (b) DELETE the bullet `- Define the internal binary-coproduct bifunctor \`+(C) : C ∏ C ⟶ C\` for \`{@Cocartesian C}\`, mirroring \`InternalProductFunctor\`.` (c) DELETE the bullet `- Prove \`Coproduct_Diagonal_Adjunction : +(C) ⊣ Diagonal_Product C\`. …`, retaining only, as a new bullet (verdict wording): `- Prove the dualization lemma \`(C^op ∏ C^op)^op ≅ C ∏ C\` as a named \`Isomorphism\` in \`Cat\` (it will be reused); if #351 already introduced it while deriving its adjunction by duality, consume that one.` (d) DoD `- [ ] \`+(C) : C ∏ C ⟶ C\` exists as a bona fide bifunctor with \`Proper\` instances` → `- [ ] #658's \`+(C) : C ∏ C ⟶ C\` is consumed unchanged (not redefined)`. (e) Strike `Coproduct_Diagonal_Adjunction` from the DoD `Print Assumptions` list, leaving only the two converses. (f) Delete `coqc -R . Category Functor/Coproduct/Internal.v` from Verification, and delete ONLY the `coqtop … 'Print Assumptions Coproduct_Diagonal_Adjunction.'` invocation — **KEEP `coqc -R . Category Adjunction/Diagonal/Coproduct.v`** (verdict: #724 still lands the coproduct-side converse in that file). (g) Dependency line → `Depends on: #351 (supplies \`Coproduct_Diagonal_Adjunction : +(C) ⊣ Diagonal_Product C\` in \`Adjunction/Diagonal/Coproduct.v\` — this issue's increment is the two CONVERSES, not the forward adjunction; do not re-prove it)`. |
| **#731** | `Slice_Terminal` | #652 | (a) Delete the Work bullet `- Prove \`Slice_Terminal : @Terminal (@Slice C c)\` with terminal object \`(c; id[c])\`.` (b) DoD `- [ ] \`Slice_Forget\`, \`Slice_Terminal\`, \`Slice_Cartesian\`, \`Slice_Over_Terminal\` and \`Slice_of_Slice\` are defined and proved` → `- [ ] \`Slice_Forget\`, \`Slice_Cartesian\`, \`Slice_Over_Terminal\` and \`Slice_of_Slice\` are defined and proved, matching Awodey §9.7 Remark 9.21 (setoid \`≈\` discipline; never \`=\` on morphisms)`. (c) Dependency line → `Depends on: #652 (supplies \`Slice_Terminal : @Terminal (@Slice C c)\` in \`Construction/Slice.v\` — consume it, do not redefine it; \`Slice_Over_Terminal\` below uses it)`. (d) **Verdict rider:** #731's DoD also says `\`Print Assumptions\` closed for each of the five principal artifacts` — change **five → four**. |
| **#736** | smallness predicate | #423 | Work bullet 1 currently: `Introduce a smallness discipline the library can actually use. The cheapest honest option is a \`SmallCategory\` record pinning the object and hom types to a fixed universe …` → `Consume the smallness witness introduced by #423 in \`Structure/Complete/Freyd.v\` (an indexing of a category's arrows by a type at a fixed level) — do not introduce a second smallness notion; EXTEND #423's witness in that file if it does not already pin what this corollary needs. Restate in this file's header what 'small' means there, in the style of \`Structure/Complete.v\`'s size note.` (**the "EXTEND" clause is a verdict rider** — #423's witness indexes ARROWS of C, whereas #736's tautological solution set needs `sol_index := { c : C & d ~> U c }` pinned to a fixed level; an absolute bar would leave the implementer stuck.) DoD box 1 → `- [ ] #423's smallness witness is consumed (not redefined), and its universe bookkeeping is restated in this file's header`. Dependency line → `Depends on: #423 (supplies the smallness witness for a category in \`Structure/Complete/Freyd.v\`, and the Freyd thinness caveat this issue's header must disclose)`. |
| **#765** | finite chains | #224 | Work item 1 verbatim: `1. Define the finite chains \`n\` as \`Proset\`s (either as an initial segment of the naturals under \`≤\`, or as a standalone inductive with a decidable order), and prove each is a \`TotalOrder\` in the sense of #223. Register the three-element chain explicitly, since Exercise 1.99 needs it and the tree's \`Roof\` is a span, not a chain.` #224 builds `In \`Instance/Ordinal.v\` (new): the family \`[n] :=\` thin category on \`Fin.t n\` with \`i ≤ j\` (donor: \`Instance/Proset.v\`), for all \`n\`.` plus `define/export \`_3 := [3]\` for downstream use`. Paths differ (`Instance/Poset/Chain.v` vs `Instance/Ordinal.v`), so no path-keyed check sees it. **Replace item 1 with:** `1. Consume #224's finite-ordinal family \`[n]\` from \`Instance/Ordinal.v\` (and its exported \`_3 := [3]\`) as the finite chains — do NOT redefine them. Add only what #224 does not supply: a proof that each \`[n]\` is a \`TotalOrder\` in the sense of #223.` DoD box → `- [ ] #224's \`[n]\` (in particular \`_3\`) proved total via #223's \`TotalOrder\`; no second finite-chain family is defined in \`Instance/Poset/Chain.v\``. Append to the bare dependency line: `Depends on: #224 — the finite-ordinal family \`[n]\` and the exported \`_3\`, which this issue consumes rather than rebuilding.` |
| **#780** | powerset preorder in `Instance/Powerset.v` | #382 | Work item 1 currently: `1. Introduce the powerset preorder \`(P(S), ⊆)\` as a thin category via \`Instance/Proset.v:33\` on \`S → Prop\` (or \`Ensemble S\`) with inclusion, proved reflexive and transitive. Keep it distinct from \`Instance/Ens.v\`, whose homs are functions; a header note should say so, since the near-miss is easy to mistake for the real thing.` → `1. **CONSUME, DO NOT RE-CREATE** the carrier: #382 builds the powerset preorder \`(P(S), ⊆)\` as a thin category in \`Instance/Powerset.v\`, and this issue is already blocked on it. Extend that file rather than adding a second carrier. If #382's header does not already distinguish it from \`Instance/Ens.v\` (whose homs are functions, not inclusions), add that note here.` DoD → `- [ ] #382's \`(P(S), ⊆)\` is extended (not duplicated), and its distinction from \`Instance/Ens.v\` is documented in the file header.` (The conditional phrasing is deliberate: #382's Verification already says `Reviewer checks: the order is inclusion (contrast \`Instance/Ens.v:64\`, whose homs are not inclusions)`.) |
| **#795** | extended `[0,∞]`-valued metric space | #308 | **Use the ADJUSTED remediation — the finding's option (b) is harmful.** #308 DOES own the extended variant, via its appended Seven Sketches §2.3.3 checkbox: `- [ ] (from Seven Sketches §2.3.3) the extended metric space: the same four axioms with the distance valued in \`[0, ∞]\`, together with a lemma identifying ordinary metric spaces among them as the ones whose distances are all finite`. KEEP the edge, the `"#308"` trailer entry and the native relation. (1) Current state — replace `The classical metric space, whose extended variant this exercise recovers, is the obligation of #308 (which builds the category of metric spaces); the \`[0,∞]\`-valued variant is not in that issue's scope and is added here.` with `The classical metric space AND its \`[0,∞]\`-valued extended variant are both the obligation of #308 — the extended record is scoped by that issue's appended Seven Sketches §2.3.3 checkbox … This issue consumes that structure and proves the enriched characterization of it.` (2) Dependency line → `Depends on: #308 (the metric-space structure INCLUDING the \`[0,∞]\`-valued extended metric space its §2.3.3 append scopes — consumed here, not rebuilt).` (3) Work item 4 — after `…is exactly an extended metric space in the sense of Definition 2.51 with codomain \`[0,∞]\`` insert `— importing #308's extended-metric record rather than defining a parallel one —`. |
| **#869 / #879** | `CospanCat FinSet` as a named in-tree term | **#879** | #869 item 5: `Form \`CospanCat FinSet\` explicitly as a named in-tree term while you are here — nothing in the tree currently does, and this issue needs it (see the note in the operad-Cospan issue for why that matters).` + DoD `- [ ] \`CospanCat FinSet\` exists as a named in-tree term.` #879 item 1: `Form \`CospanCat FinSet\` as a named in-tree term. **Nothing in the tree does this today** …` + DoD, plus `- [ ] **Library defect, closed here:** the tree currently never forms \`CospanCat FinSet\` in code`. #879 needs NO change (it carries the defect checkbox and the `docs/INHABITATION.md` rationale, and has no blockers). **In #869:** item 5 → `5. Consume #879's named \`CospanCat FinSet\` term (built in \`Instance/FinSet/Cospan/Operad.v\`); do not re-form it.`; DELETE the DoD box `- [ ] \`CospanCat FinSet\` exists as a named in-tree term.` Then choose option (a) edge or (b) serialize entry — **judgement call 3**. Corroborated independently at `doc/plan/books/seven-sketches/library-defects.md:386`: `#869 ↔ #879 \| Both own creating \`CospanCat FinSet\` in their DoD … \| OPEN`. |
| **#948** | discrete left/right fibration predicates + lifting-diagram formulation + covariant round trip | #809 | #809 already carries, in its `riehl:2.4:def13` append: `- [ ] (from Riehl §2.4) Discrete **left** and **right** fibrations are both defined (the fibration side as well as the opfibration side), with the header stating that cartesian lifts are strictly weaker than unique lifts` and `- [ ] (from Riehl §2.4) The equivalent lifting-diagram formulation is proved, as a unique-diagonal-filler condition against the endpoint inclusions \`_1 ⟶ _2\` (donors: \`Theory/Orthogonality.v:43\` \`Class Orthogonal\`, \`Instance/Two.v:134\`)` — same three obligations, same two donors as #948's Work item 1. **Edits:** item 1 → `1. CONSUME #809's discrete left/right fibration predicates and their lifting-diagram (unique-diagonal-filler) formulation — both are #809's \`(from Riehl §2.4)\` checkboxes for item \`riehl:2.4:def13\`, with the same donors (\`Theory/Orthogonality.v:43\`, \`Instance/Two.v:134\`). Do not redefine either. Also consume #809's covariant round trip (its Work item 2: elements projection is a discrete opfibration, plus the converse construction).` Delete the two duplicated DoD lines, replacing with `- [ ] The left/right predicates and the lifting-diagram formulation are imported from #809, not redefined`. **Verdict refinement: RETAIN a DoD line for the per-file cartesian-vs-unique header disclosure**, since #948's Work item 6 still requires it for its own new file. Narrow Work item 4 to the CONTRAVARIANT case, the essential-image packaging, and `E ≅ ∫F` over C. Amend the Depends-on parenthetical to `(discrete opfibrations and the elements/fibration round trips — supplies the discrete left/right predicates, the lifting-diagram formulation and the covariant round trip; this issue adds the contravariant case, the essential-image packaging and the fully faithful \`Set^C ⟶ CAT/C\` statement that #809 scopes out)`. |
| **#970** | `Ord n` in `Instance/Ordinal.v` | #224 | Work item 1 verbatim: `Define \`Ord (n : nat) : Category\` — the finite ordinal \`{0 < … < n-1}\` as a thin category. Reuse the \`Type\`-valued order idiom of \`Instance/Omega.v\` (\`le_t\`) rather than stdlib \`le\`` → `1. Use #224's finite-ordinal family \`[n] : Category\` from \`Instance/Ordinal.v\` (thin category on \`Fin.t n\` ordered by \`i ≤ j\`, over the \`Type\`-valued \`le_t\` idiom of \`Instance/Omega.v\`). Do NOT introduce a second name \`Ord n\`; add only what #224 does not supply.` **DO NOT drop `coqc -R . Category Instance/Ordinal.v` on its own** (verdict). Take verdict option (a), the smaller edit: keep both coqc lines and change the Suggested-module line to `\`Instance/Ordinal.v\` — #224's file, EXTENDED here with the cofaces and codegeneracies, not created — and \`Instance/Ordinal/Adjunctions.v\` for the chain`. |
| **#984** | the categories `2` and `3` | #666 / #224 | **Use the ADJUSTED text.** Work item 1 currently: `Build the ordinal categories \`2\` (the walking arrow) and \`3\` (the free composable pair) in the \`Instance/\` layer, and the functor \`d^1 : 2 ⟶ 3\` picking out the composite. If the ordinal family from the Riehl §4.1 chain-of-adjoints issue lands first, reuse it rather than hand-building; otherwise build just these two.` → `1. Take the category \`3\` (the free composable pair) from #666 and the walking arrow \`2\` from \`Instance/Two.v\` (\`_2\`, \`:134\`) — or, if #224's uniform finite-ordinal family \`[n]\` in \`Instance/Ordinal.v\` has landed, take both from there (#224 exports \`_3 := [3]\` and the agreement iso \`[2] ≅ _2\`). Build only the functor \`d^1 : 2 ⟶ 3\` picking out the composite, unless #970 has already contributed it as a coface.` DoD `- [ ] The categories \`2\`, \`3\` and the functor \`d^1\` are constructed.` → `- [ ] The functor \`d^1 : 2 ⟶ 3\` is constructed over #666's \`3\` (or #224's ordinal family); no rival \`2\`/\`3\` is introduced.` Add on its own line: `- Related (NOT blocking): **#224** builds the uniform finite-ordinal family \`[n]\` in Instance/Ordinal.v (with \`_3 := [3]\` and the agreement isos); prefer it over #666's presentation of \`3\` if it has landed, and note that **#970** adds the coface functors \`d^i\` to that same file, of which the \`d^1\` needed here is an instance. No edge is asserted.` **The finding's attribution of the ordinal family to #970 is WRONG — #224 builds it** (this is exactly what the #970 findings establish); writing #970 in would contradict them. |
| **#1017** | couniversal-arrow dual of `UniversalArrow` | #347 | **PREFERRED: descope, assert NO edge** (verdict). (1) Work item 2 — replace `Add the couniversal-arrow dual to \`Theory/Universal/Arrow.v\` while you are there — its absence is why no comma category in the tree currently carries a \`Terminal\` instance.` with `The couniversal-arrow dual of \`UniversalArrow\` is #347's Work item 1 (\`Theory/Universal/Arrow/Couniversal.v\`) — do NOT add a second one here. Its absence today is why no comma category in the tree carries a \`Terminal\` instance, but this issue's terminality proof goes directly through \`Construction/Comma.v\` and \`Structure/Terminal.v\` and does not need the class.` (2) DoD — replace `- [ ] The couniversal-arrow dual of \`Theory/Universal/Arrow.v\`'s \`UniversalArrow\` is added` with `- [ ] No second couniversal-arrow notion is introduced; the \`Terminal\` clause is proved directly over the candidate comma category, with #347 cross-referenced in the header.` (3) Add `- Related (NOT blocking): **#347** owns the couniversal-arrow dual of \`UniversalArrow\` (its Work item 1); this issue introduces none and consumes nothing it builds. No dependency edge is asserted.` **Leave trailer `"deps":[]` and blocked_by `[]` untouched.** Rationale: `Theory/Universal/Arrow.v:127` already exists in-tree, which is precisely why `check_graph.py`'s `creates()` drops the path and every mechanical check stays clean. |

---

## GROUP D — Add closure checkbox for prose-only increment

- **#597** (CONFIRMED). Insert a second checkbox immediately after the existing one in the `### Also covered by` block:
  `- [ ] (from Riehl §6.1) The ingredient identity \`Induced_{L ◯ K} ≅ Induced_K ◯ Induced_L\` — restriction along a composite is the composite of the restrictions — over \`Theory/Kan/Extension.v:127\`'s \`Induced := (− ◯ F)\`. Verified absent: the restriction functor is never composed with another restriction anywhere in tree, and it is needed by both directions of the composition law.`
  The prose that names the increment without homing it: `The Riehl verification pass recorded one concrete work item that this issue's plan does not name: the ingredient identity relating restriction along a composite to the composite of the restrictions is nowhere proved in tree — the restriction functor is never composed with another restriction — and \`Adjunction/Compose.v:173\` \`Definition Adjunction_Compose : (F' ◯ F) ⊣ (U ◯ U')\`, while relevant to the global form, makes no Kan claim.` The verdict independently re-verified both cited lines and the "never composed" claim, and confirmed the direction of the proposed iso.

- **#869** — Presented_factor prerequisites. **Use the ADJUSTED text; the finding presents genuinely hard work as routine assembly.** Insert between current items 1 and 2:
  `1a. Make \`CospanCat FinSet\` a legal target for \`Presented_factor\` (\`Construction/PROP/Presentation/Universal.v:340\`, whose section requires \`PROP\` + \`HomEqProp\` + \`ObjDecEq\` at \`:120-122\`). \`ObjDecEq\` is immediate (\`obj (CospanCat FinSet) = obj FinSet = nat\`, \`Construction/Cospan/Category.v:560\`, \`Instance/FinSet.v:117\`). The \`PROP\` instance needs \`Cospan_SymmetricMonoidal\` (\`Construction/Cospan/Symmetric.v:398\`) plus a \`StrictMonoidal\` assembly over nat addition — the same labour #827 performs for FinSet. **\`HomEqProp\` is the hard one and must not be assumed cheap:** the hom-setoid is \`cospan_equiv\` (\`Construction/Cospan/Category.v:78-81\`), a Σ-type carrying the apex isomorphism as DATA, and the legs do not determine it, so \`heq_elim : heq f g → f ≈ g\` is a Prop-to-Type elimination. Over FinSet it is discharged by a decidable finite search over \`Fin.t a -> Fin.t b\` (the enumeration codecs of \`Instance/FinSet/Closed.v\` are the donor). If that proves disproportionate, record in the header that Work item 3 routes around \`Presented_factor\` instead — e.g. via \`InterpF\`/\`interp_unique\` on the free prop plus an explicit quotient argument — and say which route was taken.`
  DoD box: `- [ ] \`CospanCat FinSet\` either carries \`PROP\` + \`HomEqProp\` + \`ObjDecEq\` so \`Presented_factor\` applies, or the header records the route taken around it.`

- **#530** (Awodey §3.4 Ex. 3.16) — **the ADJUSTED verdict says DO NOT add the proposed checkbox.** Apply only the parenthetical correction, rewriting `(Pos = #641, Top = MacLane I.7 = #259, and a category **Mon**)` to `(Pos = #641, Top = MacLane I.7 = #259, Mon = #648, which builds \`Instance/Mon.v\`)` — #648 is verified as the creator (`parallel-hazards.json` shared map has `"Instance/Mon.v": [648, 649]`; #648 Work bullet 1: `Assemble a category **Mon** of monoids (setoid carrier, associative operation, unit) and monoid homomorphisms, with the forgetful functor \`U : Mon ⟶ Sets\`. Suggested path: \`Instance/Mon.v\``). Then partition by explicit deferral on its own line in `## Dependencies`:
  `- Related (NOT blocking): the Awodey §3.4 Ex. 3.16 illustrations — equalizers in **Pos**, **Mon** and **Top** as the underlying-set equalizer subset carrying the induced order / submonoid / subspace structure — are deferred to #641, #648 and #259 respectively, which build those categories. They are illustration-level once those categories exist; no Definition-of-Done item here consumes them and no dependency edge is asserted.`
  Reason: the finding's checkbox would be conditioned on three unbuilt categories with no edges, directly contradicting the #255 edge the *other* #530 finding adds; and its escape hatch (`or an explicit note that each is deferred`) makes it satisfiable by writing a sentence.

- **#547** — reciprocal note, **ADJUSTED: the finding's "share the lemma" instruction is technically impossible and must not be propagated.** #547 item 1 (`Derive invertibility of the canonical comparison \`a ⊔ b → a × b\` from the abelian axioms (kernels/cokernels + normality), feeding \`bicartesian_preadditive\` without assuming it.`) and #534 item 2 prove the same conclusion under DISJOINT, mutually unavailable hypotheses. Apply instead:
  On **#547**, in `## Dependencies` on its own physical line: `- Related (NOT blocking): #534 proves \`IsIsomorphism (can_comparison x y)\` in a \`Preadditive\` category with the biproduct (\`can_comparison_iso\`, \`Structure/Semiadditive.v\`). This issue proves the SAME conclusion under disjoint hypotheses — the abelian axioms with NO assumed enrichment — so the lemmas cannot be shared: #534's needs the enrichment this issue is deriving. Give this one a distinct name (e.g. \`abelian_can_comparison_iso\` in \`Structure/Abelian/Economical.v\`), cite #534 in the header, and do not re-prove the Preadditive route.` Append to Work item 1: `(distinct from #534's \`can_comparison_iso\`, which assumes the \`Preadditive\` enrichment this item must not assume)`.
  **Also correct #534 itself** (a second live issue): replace `should share the resulting lemma rather than proving it twice` with `cover both routes under disjoint hypotheses; name the two lemmas distinctly and cross-cite, rather than attempting to share one`. Assert no edge in either direction. **See judgement call 6.**

---

## GROUP E — Fix wrong or stale citations and clause maps

| Issue | Change |
|---|---|
| **#487** | Replace `The syntactic variety category \`Algs\` (\`Instance/Comp.v:268\`, over \`Type\`) exists but is entirely disconnected from any monad, \`Set^T\`, or comparison functor` with `\`Instance/Comp.v:151\` defines \`Algs\`, the category of algebras of a signature WITHOUT equations, and \`Instance/Comp.v:268\` defines the record \`Algebra S E\` (a type, not a category) — so the variety category \`⟨Ω,E⟩-Alg\` is nowhere formed (this is #440's deliverable), and nothing connects any of it to a monad, \`Set^T\`, or a comparison functor.` Verified: `:151` is `Program Definition Algs : Category := {| obj := OpAlgebra S; hom := AlgHom; … |}`; `:268` is `Record Algebra (S : OpSignature) (E : EqSignature S) := { alg :> OpAlgebra S ; equations : … }`; `:382` is `Definition Group := Algebra GroupOp GroupEq`. Verdict adds: the sentence's trailing parenthetical was ALSO wrong (the free-algebra universal property IS formalized — `Instance/Comp.v:108` `Definition induced_hom : AlgHom Free A`, `:116` `Theorem from_free_unique`), so dropping it is correct, not a loss. |
| **#617** | `Structure/Group.v:112` → `Structure/Group.v:109` in `\`Structure/Group.v:112\` (\`GroupObject\`) is the internal-group notion`. `:112` is the field `groupobject_is_monoid : MonoidObject grp;`; `Class GroupObject (grp : C) := {` is at `:109`. Sister issue #612 already cites `:109`. |
| **#688** | `Construction/PROP/Presentation.v:48` → `:180`. `:48` is header prose `   presented by generators and relations is the free monoid quotiented`; `Definition PresentedCat : Category :=` is at `:180`. **Leave `Construction/PROP/Universal.v:603` alone** — verified as `Theorem interp_unique : ∀ m n (t : Term S m n),`. |
| **#690** | Same fix: `Construction/PROP/Presentation.v:48` → `:180`. **Leave `Instance/Comp.v:240` alone** — verified as `Record EqSignature (S : OpSignature) := {`. (#690's In-tree-donors line also names the file without a line number — no edit needed there.) |
| **#722** | `doc/plan/books/ledger.tsv` line 1323, note column: `MULTI-PART leg 2/2: **the higher-order-logic/quantifier reading** -> #722` → `MULTI-PART leg 2/2: every slice of a topos is again a topos -> #722`. The stray phrase belongs to `awodey:8.8:remark18` (`8-duplicates.json:85`), whose two legs are ledgered at 1340 (#671) and 1341 (#387). **Line 1339 (#405) is correct** — optionally sharpen `the topos-properties list` to `every topos has finite colimits` while in the file. |
| **#827** | In the In-tree-donors paragraph replace the fragment `` `Structure/Monoidal/Internal/Cocartesian.v`-style cocartesian-to-monoidal assembly `` with `` `Structure/Monoidal/Internal/Product.v:54` (`CC_Monoidal`) composed with `Construction/Opposite/Monoidal.v:92` (`Monoidal_op`) for the cocartesian-to-monoidal assembly, with `Instance/FinSet/Lawvere.v:41-42` as the in-tree precedent for exactly this pairing ``. **Do not create `Structure/Monoidal/Internal/Cocartesian.v`** — `Structure/Monoidal/Internal/` contains only `Product.v`. All three replacement citations verified at source. |
| **#931** | Replace the non-reproducing command with `\`rg -in 'PreservesMonos' -g '*.v'\` → 0 hits; \`rg -n 'Monic' Functor/\` → 0 hits, so there is no \`PreservesMonos\` predicate and \`Monic\` is never applied to a functor's action. The only nearby assertions are \`Theory/Adjunction.v:311\` \`adj_monic\` (a *faithful left adjoint* transposes a mono) and the passing remark at \`Construction/Reflective/Idempotent.v:137\`; neither concerns representable or hom-functors.` **Verdict rider: write the searches with `-g '*.v'`** — an unscoped `rg -in 'PreservesMonos'` now hits the untracked `doc/plan` campaign artifacts and would not literally return 0. Note `adj_monic` is at `:311`, not `:310` (`:310` is its comment). |
| **#606** `[UNVERIFIED]` | `Structure/Monoidal/Braided/Proofs.v:507` → `:506` (`:507` is blank; `:506` is ``Context `{S : @SymmetricMonoidal C}.``). `Structure/Monoidal/Braided.v:132` → `:131` (`:132` is the field `braid {x y} : x ⨂ y ~> y ⨂ x;`; `:131` is the misleading comment). Other citations in the issue are exact and must not be touched. |
| **#772** `[UNVERIFIED]` | Opening line → `Riehl, *Category Theory in Context*, 2nd ed., Epilogue §E.2, the roster of examples of symmetric monoidal categories,`. Clause map `(The roster's other clauses are recorded on #490,\n#388, and the new Riehl E.2 chain-complex issue.)` → `(The roster's other clauses: clause (a), finite products, is already in force in-tree as \`CC_SymmetricMonoidal\` and needs no issue; clause (b) is on #490, clause (c) on #388, clause (d) on #1011.)` |
| **#832** `[UNVERIFIED]` | `` (`Construction/PROP/Tietze.v:265`, `:288`) `` → `` (`Construction/PROP/Tietze.v:265`, `:301`) ``. `:288` is `RelabelFunctor_Braided`; `RelabelFunctor_Symmetric` is at `:301`. The In-tree-donors line cites only `:265` and needs no change. |
| **#930** `[UNVERIFIED]` | `returns **exactly two hits**, both prose in header comments` → `returns four hits, none of them a definition of the notion: two prose comments (\`Theory/Sheaf.v:80\`, \`Construction/Localization.v:101\`) and the section delimiters \`Section SubFunctor.\` / \`End SubFunctor.\` in \`Theory/Subobject/Functor.v\`, which house the SUBOBJECT functor \`Sub : C^op ⟶ Sets\` — a different construction whose name the new \`Subfunctor\` class must not be confused with.` |

---

## GROUP F — Contract/format

**F1. Body must END with the trailer** (move the block above the trailer; collapse the stray multi-newline gap):
- **#809** — cut the block from `### CONSUME, DO NOT RE-CREATE the category of elements (added on audit)` through `…it does not remove a duplicated instruction.` and re-insert immediately BEFORE `<!-- catalog: {"ids": ["7sketches:3.5.3:remark3.100", "riehl:2.4:def13"], "deps": ["#345", "#337"]} -->`, one blank line each side; collapse the three consecutive blank lines. **The block's substance is correct and must be kept** — `serialize-groups.json` lists `Construction/Elements.v` for `[345, 809, 1021]` with creator 345. Verdict swept all 35 batch issues: #809 is the only body with content after the trailer.
- **#526** `[UNVERIFIED]` — same move for the block `### Same-file peer (NOT a dependency)` … above `<!-- catalog: {"ids":["maclane:VII.9:def-pointed"],"deps":["#259"]} -->`; collapse the triple newline.

**F2. Missing trailer id.**
- **#1006** → `<!-- catalog: {"ids":["riehl:5.5:exv","riehl:5.5:exvi","riehl:5.5:example7"],"deps":["#320","#366","#484"]} -->`. `ledger.tsv:2782` reads `riehl:5.5:example7\tABSENT\t#1006\t10\tpart: clause (iii): ev : C^J -> C^{ob J} is monadic for C with coproducts and J small`, `riehl/issue-map.json` has `"riehl:5.5:example7@1006": 1006`, and the body already homes it (`- [ ] Statement fidelity to Riehl §5.5 Exercises 5.5.v (all four clauses) and 5.5.vi, and Example 5.5.7(iii)`). Siblings #465, #474, #993 all carry the id; #1006 is the only ledger-rowed coverer omitting it.

**F3. Depends-on lines carrying non-dependency prose** — split onto separate physical lines.
- **#585**: replace the whole `## Dependencies` body (currently `None in-catalog. Related in-tree: Freyd's adjoint functor theorem (issue #436) is the stronger-hypothesis route already filed; RAPL/LAPC are present (\`Adjunction/Continuity.v\`).` followed by a `Depends on: #334 — creates …` line carrying its whole justification) with:
  ```
  Depends on: #334
  - Related (NOT blocking): #436 (Freyd's adjoint functor theorem — the stronger-hypothesis route, already filed). RAPL/LAPC are present in-tree (`Adjunction/Continuity.v`).
  - Note on #334: it creates `Structure/Limit/Initial.v` and supplies `IsALimit F (F initial_obj)`, which discharges criterion (ii) in the forward half of Theorem 2 (the adjunction alone yields only an initial object of `x ↓ G`).
  ```
  Leave trailer (`"deps": ["#334"]`) and native relation unchanged.
- **#648**: single line `Depends on: #296 (MacLane II.7 — the free monoid and its universal property; provides the monoid/free-monoid infrastructure this issue builds on). Related: the group/ring/vector-space cases of Example 2.3 additionally require categories the library does not yet have.` → two lines: `Depends on: #296 (MacLane II.7 — the free monoid and its universal property).` / `- Related (NOT blocking): the group/ring/vector-space cases of Example 2.3 additionally require categories the library does not yet have.`
- **#780** — **corrupted section, highest-priority format fix.** Raw body has the #745 line spliced into the MIDDLE of the #382 line, stranding `adjunction — the inclusion-ordered carrier).` on its own physical line. Replace the affected lines with exactly:
  ```
  Depends on: #382 (the powerset preorder and the direct-image/inverse-image adjunction — the inclusion-ordered carrier).
  Depends on: #745 (the interior comonad and closure monad on the subsets of a space — it lands in the same `Instance/Powerset.v` and must be in place before this issue extends that file).
  ```
  and move the hedge to its own physical line after all Depends-on lines: `- Note: #382, #745 and this issue all write \`Instance/Powerset.v\`; the chain 382 → 745 → 780 serialises them, so extend the file rather than re-creating it.` **No change to the trailer or native relations** (all three surfaces already agree; the chain is genuine — `blocked_by(745)` = 727,463,382 and `blocked_by(780)` = 771,799,382,389,685,745).
- **#482** `[UNVERIFIED]` — split the one-line section into `Depends on: #393 (\`maclane:IV.7:def-map-of-adjunctions\`) — the general maps-of-adjunctions notion, of which this comparison is the identity-on-\`X\` specialization, supplying the derived \`M ∘ ε' ≈ ε ∘ M\`.` + a new line, no issue numbers: `Otherwise uses the in-tree Beck engine (\`CreatesUSplitCoequalizers\`/\`Beck_Inverse\`) and \`EM_Comparison\`.`
- **#553** `[UNVERIFIED]` — split into `Depends on: #545 (\`maclane:VIII.3:def4\`) (short exact sequences — the objects of \`Ses A\`).` + `- Related (NOT blocking): the morphism-of-SES notion is reused by the short five lemma (#554) and the snake lemma (#555).`
- **#1017** `[UNVERIFIED]` — replace the section body `None in catalog. Related: #590 supplies the global adjoint form of the same bijection, and this issue's local form should be stated so that #590's assembly consumes it.` with two lines: `None blocking.` / `- Related (NOT blocking): **#590** supplies the GLOBAL adjoint form of the same bijection. The arrow runs the other way — this issue's local form should be stated so that #590's assembly consumes it. No dependency edge is asserted.` (Merge with the Group C #1017 edit, which appends a second Related line for #347.)

**F4. #437 — the hedge that denies the edge it asserts.** Replace the `Depends on: #348 …` line (match on its text; the verdict places it at **body line 69, not 71 — edit by text match, not line number**), currently `Depends on: #348 — supplies the representability packaging this theorem is stated against (\`AUniversalArrow\` ⟷ \`UniversalArrow\`); note the adversarial check found the connection weaker than first claimed, so treat this as an ordering constraint on shared ground rather than a strict logical necessity.` with exactly:
`Depends on: #348 (\`maclane:IV.1:cor2\`) — owns and creates the new module \`Adjunction/Representability.v\`, which this issue extends; consume its \`AUniversalArrow\` ⟷ \`UniversalArrow\` packaging lemma rather than re-deriving it.`
Keep the native `blocked_by` 348 and the trailer `"#348"`. **Do NOT add the serialize-groups entry** — see §(i).

**F5. Blockquote inside `## Dependencies`.** Move the `> **SCOPE (added on audit).** …` blockquote out of `## Dependencies` in **#843** and place it at the END of `## Work to be done`, immediately after the In-tree-donors paragraph, unchanged. After the move, `## Dependencies` contains the three surviving `Depends on:` lines (#221, #839, #826) **PLUS the `- Related (NOT blocking): #789 …` line Group A adds — the Related-line convention is legitimate and must not be stripped** (the finding's "only the Depends-on lines and nothing else" wording contradicts its own sibling remediation). Apply the same relocation, cosmetically, at **#827** (`> **Verification note folded in on audit …**`), **#829** (`> **SCOPE (added on audit — two-thirds of the original Work section belonged to #824).**`) and **#832** (`> **Precision correction (added on audit).**`) — verified same placement, no stray issue numbers. **Severity note from the verdict: this is uniformity/readability, MEDIUM-minus, not a proven resolver bug** — the campaign's own convention already puts bare `#N` inside `## Dependencies` on `- Related (NOT blocking):` lines.

**F6. #921 `[UNVERIFIED]`** — collapse the double blank line after `## Dependencies`, and replace the blockquote `> **Related, NOT a dependency (corrected on audit):** #225 (the simplicial category Delta) is referenced for the header CONTRAST only. …` with the standard single line placed before the Depends-on line: `- Related (NOT blocking): #225 (the simplicial category Delta) — referenced for the header CONTRAST only; previously mis-filed as a \`Depends on:\` line, which made the resolver assert a blocker the prose denied.` (`Related (NOT blocking)` is the exact string `check_graph.py`'s `REVERSE_CUE` recognises; the blockquote form is invisible to it.)

**F7. #637 `[UNVERIFIED]`** — collapse both `### Also covered by\n\n\n` occurrences to `### Also covered by\n\n`.

**F8. Bare/unresolvable prose references → issue numbers.**
- **#437**: `the §V.4 issue` → `#428` (Group B).
- **#455**: all three `the §V.1 CompHaus issue` → `#413` (Group B) — already in head plan F6.
- **#487**: `(see the Riehl §5.5 lattices issue)` → `(#1003)` (Group B).
- **#611** `[UNVERIFIED]`: `see the §XI.3 strictification issue` → `see #609, which owns the primary (free-monoid-on-objects) route`; `the main strictification result of §XI.3` → `the main strictification result of §XI.3 (#609)`; append after `Depends on: #610 (\`maclane:XI.3:ex1\`)` the line `- Related (NOT blocking): #609 — same theorem statement by a deliberately independent route; do not merge.` Leave the trailer deps unchanged.
- **#984**: `the Riehl §4.1 chain-of-adjoints issue` resolves to **#970**, but the ordinal family is **#224's** — handled in Group C, do not write `#970` as the builder.

**F9. `make todo` DoD line — bulk substring swap across 19 issues.** In each of **#566, #567, #568, #569, #570, #571, #572, #573, #574, #575, #576, #577, #578, #579, #580, #581, #582, #583, #584** replace the substring `` `make todo` clean. `` with `` `make todo` adds no new hits. `` in the final DoD checkbox. No other text on that line changes. Verified: `make todo` on the current worktree prints 89 lines of pre-existing hits (`./Structure/Pullback.v:254:(* jww (2017-06-01): TODO *)`, `./Structure/Closed.v:154:(* jww (2018-10-05): TODO`, `./Structure/Discrete.v:29:  (* jww (2017-06-02): Equality is too much here. *)`, plus `Fail`/`Abort`/`admit` matches from `Test/`, `Theory/Metacategory.v`, `Theory/Kan/Extension.v:433-438`), so the box can never be ticked; the split is exactly 19 (#566–#584, `clean.`) / 16 (#585–#600, `adds no new hits`), machine-checked. **Only #566 is in this chunk's finding set; the other 18 are in-range collateral of the same defect.**

---

## GROUP H — `doc/plan/books/graph/serialize-groups.json`

1. **Add** `{"module": "Structure/Abelian/Homology.v", "issues": [516, 557], "creator": 557, "reason": "SERIALIZE_ONLY: same construction under two paths (`Structure/Homology.v` vs `Structure/Abelian/Homology.v`)."}`. Precedent is the file's own `Construction/Fractions.v` entry ([972,1028], "the SAME zig-zag localization built under two different module paths … NOTE this pair is invisible to check_collisions.py, which matches paths"). **Verdict note:** once the Group B edge 516→557 and the Group C rewrite land, this entry is belt-and-braces (only one issue then writes the file); keeping it is harmless and documents the erased hazard.
2. **Add, ONLY if judgement call 3 resolves to option (b)** — `{"module": "CospanCat FinSet (named term; #869 -> Construction/PROP/Frobenius.v, #879 -> Instance/FinSet/Cospan/Operad.v)", "issues": [869, 879], "creator": 879, "reason": "SERIALIZE_ONLY: same construction under two paths; invisible to check_collisions.py, which matches paths."}` — a construction-keyed (non-path) entry, matching head-plan Group H items 1 and 4. If option (a) (edge 869←879) is taken instead, do not add it.
3. **`Adjunction/Representability.v` [348, 437] — DO NOT ADD. This contradicts head plan Group H item 3.** See §(i).
4. **`Theory/Universal/Arrow/Couniversal.v` [347, 989, 1017] — DO NOT ADD** under the preferred #1017 resolution (descope: #1017 introduces no rival, so there is nothing to serialise).
5. **Ledger multi-part split for `7sketches:6.3.1:ex6.57` (#868/#869)** — four edits, all verified well-formed against the header `item_id\tclassification\tissue\tprojects\tevidence_or_note` and against the sanctioned precedent (`7sketches:6.2.1:ex6.3` at lines 1945/1988 with `part:` notes and the issue-map key `7sketches:6.2.1:ex6.3@756`):
   (1) `ledger.tsv:1964` note column → `part: the positive/equality half of Exercise 6.57` (it currently holds the issue TITLE: `Seven Sketches 6.3.1: The spider normal-form theorem — connected Frobenius diagrams are spiders`);
   (2) append row `7sketches:6.3.1:ex6.57\tPARTIAL\t#869\t6\tpart: the separation/negative half of Exercise 6.57` (non-adjacent duplicate rows are house style);
   (3) `seven-sketches/issue-map.json` → add `"7sketches:6.3.1:ex6.57@869": 869`;
   (4) #869 trailer ids → `["7sketches:6.3.1:thm6.58","7sketches:6.3.1:ex6.57"]`. **Sequencing: apply this trailer edit LAST, or merge it with the #869/#879 trailer edit** — if judgement call 3 takes option (a), deps must end as `["#827","7sketches:6.3.1:thm6.55","7sketches:6.5.2:example6.94"]`.

---

## (i) REFUTED / DO NOT ACT

**No finding in this chunk was REFUTED.** All 89 verdicts were CONFIRMED or ADJUSTED. What follows are the sub-parts of ADJUSTED verdicts that explicitly instruct *no action* — acting on the original remediation would cause harm.

| Item | Do not do | Reason (from the verdict) |
|---|---|---|
| **#437 serialize-groups entry** | Do NOT add `{"module": "Adjunction/Representability.v", "issues": [348, 437], "creator": 348, …}`. **This directly contradicts head plan Group H item 3, which proposes exactly that entry.** | `check_graph.py:233-240` discharges a co-creation conflict by `reach()` FIRST and consults `acknowledged` only when NO dependency path exists. #437 IS blocked_by #348, so the entry would never be read — dead data. Worse, every existing entry in that file declares "SERIALIZE_ONLY … No logical precedence exists, so NO dependency edge is asserted", the opposite of this pair. The body rewrite (F4) alone makes the co-creation visible. **The head plan's #437 body edit (its Group F6) is correct and should still be applied; only its "(+ Group H)" rider must be dropped.** |
| **#530 Awodey §3.4 checkbox** | Do NOT add `- [ ] (from Awodey §3.4 Ex. 3.16) The concrete equalizers in **Pos**, **Mon** and **Top** … or an explicit note that each is deferred to those issues.` | It would be conditioned on three unbuilt categories (#641, #648, #259) with no edges asserted — self-inconsistent with the #255 edge the sibling finding adds to the same issue. Adding three more edges would be worse: it strands a 20-line core lemma and #542 behind four category constructions the append itself calls "illustration-level". The escape hatch also makes the box satisfiable by writing a sentence. Use the Related-line deferral in Group D instead. |
| **#547 shared lemma** | Do NOT write `Share the lemma \`can_comparison_iso\` — do not prove it twice` into #547. | Technically impossible: #534 proves it *assuming* the `Preadditive` enrichment, #547 must prove it *before* any enrichment exists (`feeding \`bicartesian_preadditive\` without assuming it`, discharging the `Context (E : ∀ x y, IsIsomorphism (can_comparison x y))` hypothesis at `Structure/Semiadditive.v:324`). Neither can invoke the other. #534's own "should share" phrasing is the confused part and must not be propagated to a second live issue — correct #534 instead. |
| **#648 instantiation route** | Do NOT route through `CC_CartesianMonoidal` over `Instance/Sets/Cartesian.v`. Do NOT drop `Instance/Mon.v` from #648. | `Instance/Sets.v:283` already exports `#[export] Program Instance Sets_Product_Monoidal : @Monoidal Sets`, and `Mon` needs only `@Monoidal C` — the `CC_CartesianMonoidal` route works but is an unnecessary detour. Dropping `Instance/Mon.v` would silently invalidate the #649→#648 edge that another finding asks the coordinator to declare in the same pass. Also: the finding's claim that #648 "omits" the existence of `Mon(C)` is **false** — #648's Current state names it at line 83 explicitly; it *understates* the instantiation gap. |
| **#724 Verification deletions** | Do NOT delete `coqc -R . Category Adjunction/Diagonal/Coproduct.v`. | #724 still lands the coproduct-side converse (`Prove the two converses: \`Cartesian_of_diagonal_right_adjoint …\` and its dual`) in that file; deleting the line leaves the issue never compiling a file it writes to. Delete only the `Print Assumptions Coproduct_Diagonal_Adjunction` invocation, and strike that constant from the DoD's principal-artifacts list. |
| **#795 option (b)** | Do NOT delete the `Depends on: #308` line, the `"#308"` trailer entry or the native relation. | #308 DOES own the extended `[0,∞]` metric space, via its appended §2.3.3 DoD checkbox. Removing the edge would strip the only ordering keeping #795 from building a second copy. The real defect is the mirror image: #795's Current-state sentence is STALE. |
| **#843 Dependencies contents** | Do NOT leave `## Dependencies` with "only the four `Depends on:` lines (three, after the #789 removal) and nothing else". | That instruction contradicts the same issue's other remediation, which ADDS a `- Related (NOT blocking): #789 …` line there. The Related-line convention is legitimate repo-wide and must not be stripped. |
| **#869 Presented_factor framing** | Do NOT present the `PROP` + `HomEqProp` + `ObjDecEq` assembly as routine. | `HomEqProp` (`Construction/Quotient.v:206-210`) demands `heq_elim : heq f g → f ≈ g`; on `CospanCat FinSet` the hom-setoid `cospan_equiv` (`Construction/Cospan/Category.v:78-81`) is a Σ-TYPE carrying the apex isomorphism as data, and cospan legs are not jointly epic so that iso is genuinely not unique. This is a Prop-to-Type elimination requiring a decidable finite search — a real proof obligation. Use the honest Group D wording, which includes the route-around escape. |
| **#970 Verification line** | Do NOT drop `coqc -R . Category Instance/Ordinal.v` on its own. | Doing so while the Suggested-module line still reads `\`Instance/Ordinal.v\` for the family and its functors` leaves #970 writing the coface/codegeneracy functors into a file its own verification no longer compiles. Take option (a). |
| **#984 attribution** | Do NOT write "**#970** builds a uniform finite-ordinal family in Instance/Ordinal.v". | **#224** builds it — which the #970 findings in this same chunk establish. Writing #970 in would contradict them and violate the campaign's "name the issue that BUILDS the thing" rule. |
| **#991 → #1034 edge** | Do NOT add `Depends on: #1034`. | #991's own appended §6.5 paragraph says Riehl "notes explicitly that the direct construction given earlier is still needed, because the general theorem presupposes that the target is cocomplete — which is exactly the corollary this issue derives." #1034 applied at `Cat` CONSUMES #991's output; blocking all of #991 behind #1034 (itself seven deps deep, blocked_by [590,589,591,346,600,598,716]) inverts the natural order for one appended checkbox. Reword the checkbox instead: `- [ ] (from Riehl §6.5) Exhibit the nerve/homotopy-category adjunction as an instance of #1034's presheaf-adjunction theorem — … — OR, if #1034 has not landed, record in the header that the instance-of-#1034 reading is deferred to #1034`, plus `- Related (NOT blocking): **#1034** owns the presheaf-adjunction theorem the appended §6.5 checkbox asks this adjunction to instantiate; applying it at \`Cat\` presupposes the \`Cocomplete Cat\` proved here, so no dependency edge is asserted.` |
| **#1017 edge to #347** | Do NOT add `Depends on: #347`, do not touch the trailer or `blocked_by`, do not add a serialize-groups entry. | #1017 does not consume #347 today — it ADDS a rival. Its actual Work item 2 goal (prove `LocalRightKan X` terminal in `Induced ↓ =(X)`) goes directly through `Construction/Comma.v` + `Structure/Terminal.v` and needs no `CouniversalArrow` class. The proposed fix would rewrite the issue to consume #347 and then serialise an otherwise-unblocked flagship Kan-extension issue behind MacLane IV.1 (#347, itself blocked by #302) for a deliverable it can simply drop. `check_graph.py`'s own header records this failure mode (the #541 "Benefits from" case: promoting a soft mention to a hard edge "needlessly serialized it"). Descoping is the honest minimum. |
| **#678 line numbers** | Do NOT copy the finding's cited line numbers into any edit. | Verdict re-read the file: `SubObj_Setoid` is `:33` not `:32`, `sub_le_unique` `:78` not `:76`, `sub_equiv_iff_mutual` `:93` not `:91`. The verdict is unaffected; the numbers are. |

---

## (ii) Judgement calls — decide individually, do not script

1. **#437's serialize-groups entry contradicts head plan Group H item 3.** The head plan orders that entry added; this chunk's adversarial pass says it is inert (never consulted for a pair with a dependency path) and contradicts the file's own "no edge asserted" contract. **Tradeoff:** adding it costs nothing at runtime but pollutes a file whose every other entry means the opposite, and teaches future readers a wrong invariant; omitting it means the 348/437 co-creation of `Adjunction/Representability.v` is visible only in #437's body prose. **Recommendation: omit, and instead treat the F4 body rewrite as the record.** If the coordinator wants the graph-level record anyway, change the entry's `reason` so it does not claim SERIALIZE_ONLY (head-plan Group H item 3's own wording already does this: "Ordered, not merely serialized") — but then also fix the file's contract note, or the semantics of the key stay ambiguous.

2. **#530: hard edge to #255 vs. softening the checkbox.** The verdict endorses the edge as convention-consistent (a DoD item genuinely consumes #255, and #1021's precedent declines edges precisely when "no Definition-of-Done item here consumes #716"). But it also names the cost plainly: #530 is a ~20-line core lemma and #542 waits behind it, and #255 is unstarted. **Tradeoff:** convention-consistency and honest closure tracking vs. parking a leaf lemma and its dependent behind a whole category construction. **Alternative:** soften the Riehl §3.1 checkbox to an explicit deferral (the same instrument Group D uses for the Awodey §3.4 clause) and assert no edge. Do NOT blend — asserting the #255 edge while deferring the Pos/Mon/Top clause is the choice the verdicts jointly recommend; asserting neither, or both, is also coherent; asserting the second without the first is not.

3. **#869/#879 `CospanCat FinSet`: edge (a) vs construction-keyed serialize entry (b).** (a) `Depends on: #879 (\`7sketches:6.5.2:example6.94\`) — the named \`CospanCat FinSet\` term.` + trailer + native edge 869←879. (b) The non-path serialize entry in Group H item 2, keeping both unblocked. **Tradeoff:** #879 has no blockers and #869 is blocked on [827,868], so the edge costs little scheduling; but (b) preserves parallelism and matches the precedent for path-invisible pairs. **Either way the #869 Work-item-5 and DoD deletions in Group C are unconditional.** Whichever is chosen determines #869's final trailer deps, which must be reconciled in ONE edit with the Group H item 5 ids rewrite — apply the ids edit last or merge them.

4. **#921: demote the #261 edge, or delete the alternative route.** Work item 1: `either as the pointed restriction of issue #261's \`Set_*\` **or directly over \`Instance/FinSet.v\`**, whichever keeps the skeletal computability that \`Instance/FinSet.v\` already enjoys` — and the stated criterion actively points AWAY from #261, whose deliverable is `Instance/Sets/Pointed.v` (pointed setoids, unrestricted in size); even the import route needs a finite restriction #921 performs itself. **Option A:** demote to `- Related (NOT blocking): #261 … — an optional donor if \`Fin_*\` is built as the pointed restriction of \`Set_*\`; Work item 1 equally sanctions building \`Fin_*\` directly over \`Instance/FinSet.v\`, which is the route that preserves skeletal computability.`, empty the trailer deps, drop `blocked_by` 261. **Option B:** keep the edge and delete `or directly over \`Instance/FinSet.v\`, whichever keeps the skeletal computability that \`Instance/FinSet.v\` already enjoys` from item 1 and `(or imported)` from the DoD. **The verdict is explicit: pick exactly one, do not blend.** Option A is the campaign-precedent choice (`doc/wiggum-handoff.md:3260-3263`, the #541/#536 demotion).

5. **#1017: descope (no couniversal dual) vs phrase the terminality clause over #347's class.** Preferred is descope (§(i)). **Tradeoff:** descoping keeps a flagship Kan-extension issue unblocked and removes a duplicate, but it means the couniversal-arrow dual reaches the tree only via #347/#989, whose own timing is uncertain (#347 is blocked by #302). Only if the coordinator positively WANTS #1017's terminality clause stated over `CouniversalArrow` should the edge + trailer + native relation be applied instead.

6. **#547 requires editing a SECOND live issue (#534).** The corrected remediation changes #534's own append (`should share the resulting lemma rather than proving it twice` → `cover both routes under disjoint hypotheses; name the two lemmas distinctly and cross-cite, rather than attempting to share one`). **Tradeoff:** leaving #534 alone leaves a false instruction live on the issue that authored it, so a #534 implementer will still look for a shared lemma; editing it widens the blast radius of a MEDIUM finding into an issue nothing else in this chunk touches. **Recommendation: edit both** — the #547 note alone contradicts #534's text and a reader will not know which to believe.

7. **#772, #832, #606, #930, #482, #526, #553, #611, #637, #982 are UNVERIFIED** (LOW severity, excluded from the adversarial pass). All ten are citation- or format-level, so acting on them is low risk, but none was independently confirmed — in particular **#982's edge demotion is a graph mutation on unverified evidence** and should be re-checked against #982's body and `blocked_by` before the `DELETE` call.

8. **F9's 19-issue `make todo` swap reaches outside this chunk's finding set.** Only #566 was audited; #567–#584 are inferred from a machine-checked substring sweep (19 hits for `` `make todo` clean. ``, 0 for the correct phrase; #585–#600 the exact complement). **Tradeoff:** it is a pure substring swap on one line per issue and the defect is real (`make todo` prints 89 pre-existing hits), but it edits 18 bodies no finding names. Either accept the sweep as evidence or re-run it before applying.

9. **`deps` array convention (head plan §4.7) is unresolved and this chunk makes it worse.** My edits produce item-id trailers (#691, #843, #777, #817), issue-number trailers (#566, #617, #712, #714, #991, #1009, #1017), and mixed ones (#437 already ships `["#366","maclane:V.6:thm1","#348"]`; #461 gains `"#434"` beside `"maclane:V.6:thm2"`). Every trailer line above is written in the form that issue already uses, so no edit *increases* inconsistency — but F3-style normalisation must not be run before the convention is picked, or half these edits get rewritten twice.