# Library defects surfaced during the Awodey catalog campaign

Same convention as doc/plan/books/maclane/library-defects.md: in-tree
defects (dangling pointers, comment-vs-code mismatches) found by the
coverage/verify agents while classifying — NOT missing-theory gaps.
Recorded so they are not lost; filing as GitHub issues is a maintainer call.

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| A1 | Instance/Poset.v:21 | Header comment "(See also [Pos], the category of posets, whose objects are posets and whose morphisms are monotone maps.)" is a dangling in-tree `[Name]` cross-reference: NO `Pos` category exists (only the single-poset-as-thin-category `Poset` in the same file). Stale/aspirational pointer. (Also noticed in passing during MacLane Ch I.) | Naturally resolved by issue **#641** (Awodey 1.4: "Pos, the category of posets") — building `Pos` makes the pointer valid; #641's body references the Poset.v pointer. |
| A2 | Instance/Proset.v:19 | Header comment "See also [Ord], for the category of preordered sets (where the prosets are the objects, and morphisms are monotone mappings)" is a dangling `[Ord]` cross-reference: NO `Ord` category exists in the tree. Stale/aspirational pointer. | **No direct coverage-gap home** (the proset-roster item deduped to MacLane #226). Recorded here; trivial doc fix (drop or build). Would be resolved by an eventual `Ord`/`Proset`-category issue. |

| A3 | Theory/Isomorphism.v:83 (+ Structure/Terminal.v:18-19) | Header essay says the uniqueness theorem `univ_property_unique_up_to_unique_iso` has "Structure/Terminal.v the simplest instance", implying terminal objects are a formal instance of the universal-property uniqueness framework. They are NOT: Terminal/Initial are never instantiated into IsUniversalProperty (only UniversalArrow and Limit are), and Terminal.v proves no object-level uniqueness-up-to-iso (only morphism-level `one_unique`). Doc overstates what is formalized — this IS the substance of Awodey 2.2:prop8's gap. | Folded into issue **#247** (MacLane V-era terminal/initial uniqueness — prop8's cross-book dedup target; #247 body carries the Isomorphism.v:83 note). |
| A4 | Instance/Lambda/Eval.v | Self-disclosed operational pair-step defect in the evaluation `step` relation (the file discloses its own defect). Surfaced while confirming awodey:2.6:construction-lambda-types (PRESENT — Lambda G : Category is genuine; the UMP proof via SemExp is independent of this eval defect). | UNPLACED (the item is PRESENT → no drafted issue touches the file). Recorded here only; already self-disclosed in-file, low priority. |

## Chapter 10 haul (2026-07-29) — A44 is the campaign's LARGEST cluster: EIGHT files

All entries below were re-read from source by the main session, with
counter-searches run, before recording.

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| **A44** | **EIGHT files** (widened on fess-aw10 M1): `Monad/Eilenberg/Moore.v:33-35`, `Monad/Eilenberg/Moore/Adjunction.v:48-50`, `Theory/Monad.v:49-50`, `Monad/Kleisli.v:29-30`, `Monad/Adjunction.v:35-36`, `Comonad/Duality.v:75-82` (+ further instances at `:186-187`, `:277-278`), **`Theory/Adjunction.v:68-70`**, **`Monad/Comparison.v:25,28`** | **The "Kleisli is the INITIAL resolution, Eilenberg–Moore the TERMINAL one" claim is asserted declaratively across EIGHT files and is not merely unproven — it is UNSTATABLE in the current tree.** Verified verbatim at all sites (e.g. EM.v: "This is the terminal such resolution of T; the Kleisli category … is the initial one, **sitting inside C^T as the full subcategory of free algebras**"; Comonad/Duality.v goes furthest: "Every inducing adjunction therefore **receives a canonical comparison functor from the former and sends one into the latter**"). Counter-search, decisive: **`grep -c -i kleisli Monad/Comparison.v` = 0** and **0 hits across all of `Monad/Monadicity/*.v`** — there is NO comparison functor from the Kleisli resolution into `EilenbergMoore M` (the generic `EM_Comparison` lands in the algebras of `Adjunction_Induced_Monad`, and the transport along `Kleisli_Monad_agrees` is never done); and **`(Record\|Class\|Definition\|Instance).*Resolution` = 0 hits**, so there is no category of resolutions in which initiality or terminality could even be *stated*. Asymmetry worth keeping: **terminality is PARTLY backed** (`EM_Comparison_Forget:198`, `EM_Comparison_Free:260` give a mediating functor commuting with both resolutions — but uniqueness AND the ambient category of resolutions are both missing, so 'only uniqueness' would be too generous); **initiality has no in-tree content at all**, and neither does the "full subcategory of free algebras" claim. | Recorded — **the campaign's largest cluster** (8 files, vs. 3 for A14 and A20). A single true theorem would discharge all eight sites. **METHOD NOTE:** my original count said six because my counter-search proved the ARTIFACT absent (`kleisli` = 0 in `Monad/Comparison.v`) but never searched for further ASSERTION SITES — and `Monad/Comparison.v`, the very file I searched, asserts 'the terminal resolution' twice. Absence-of-artifact and absence-of-claim are different searches. |
| **A45** | Instance/Poset.v:46-54 | Header asserts "**`[Poset]` installs the resulting dictionary**" and lists four collapses — adjunction = Galois connection, **monad = closure operator**, product = meet, coproduct = join — each with a parenthetical file pointer. **Nothing of the sort is installed.** Verified: the entire file defines only `Lemma eq_equiv` (:111), `Definition Poset` (:116) and `Definition LessThanEqualTo_Category` (:120). The parentheticals name where the GENERAL categorical notion is defined, not where the poset collapse is proved — so a reader (or a coverage classifier) naturally reads them as formalization pointers. This is the exact trap: it was found while confirming `awodey:10.2:example4` **ABSENT**. | Recorded (HIGH — actively misleads classification). The monad=closure-operator obligation is **#463**. |
| **A46** | Theory/Lambek.v:19-21 vs :40-41 | Header states Lambek's lemma as "**the structure map** of an initial F-algebra **is an isomorphism** … `[α : F μ ~> μ]` **is invertible**". The theorem actually proved states only `F μ ≅ μ` — that *some* isomorphism exists, with no claim that **α itself** is the iso. **CORRECTED (fess-aw10 H1, machine-verified):** my original rationale here — that the weaker form "cannot be used to derive a non-existence result" — was **FALSE**, and I had propagated it into filed issue #750 in four places. `lambek`'s conclusion `F μ ≅ μ` is exactly what a Cantor argument contradicts; α being *the* iso is irrelevant. The auditor compiled a probe under Rocq 9.1 and I re-compiled it independently: `intro I; exact (cantor _ (lambek P I)).` closes the non-existence with the CURRENT opaque lemma. #750 has been corrected and its spurious `#748` blocker removed. What remains genuinely defective is only the header overstating the theorem: it promises an invertible structure map and delivers a bare isomorphism, which misleads a reader about what is available (and `Qed` at `:69` seals the witness even though the proof body sets `to := \`2 iobj` at `:67`). | Recorded (HIGH — an over-stated header that hides a real formalization gap). Tracked by **#748**/**#750**. |
| A47 | docs/INHABITATION.md:31-61 | `adamek` (`Theory/Adamek.v:285`) and `adamek_cocomplete` (`Theory/Adamek/Corollaries.v`) **CORRECTED (fess-aw10 L4):** they are not under-disclosed, they are **absent entirely** — `grep -i adamek docs/INHABITATION.md` = **0 hits**, so `adamek`/`adamek_cocomplete` appear in neither the Witnessed nor the Conditional table. An OMISSION, not a mis-description. `docs/INHABITATION.md` is the document CLAUDE.md directs readers to *precisely* to check this, which makes the omission worse than an ordinary doc gap. | Recorded (MEDIUM — the audit doc is the wrong place to under-disclose). |
| A48 | Monad/Kleisli.v:29-30 | "C_T is the full subcategory of free algebras inside the Eilenberg–Moore category" — the free-algebra embedding is not constructed anywhere. | Folded into **A44** (same missing artifact). |
| A49 | Instance/Poset.v:21-22, Instance/Proset.v:19-21 | Third independent re-flagging of **A1**/**A2** (dangling `[Pos]`/`[Ord]` pointers). | Duplicates of A1/A2 — `Pos` is resolved by **#641**. |

## Chapter 9 haul (2026-07-29) — 12 rows (A32-A43), 11 genuinely new (A40 duplicates A1); A32/A33 are a TWO-FILE NAMING CONTRADICTION on the tree's most important concept

Verification note: A32-A38 and A41-A43 were re-read from source by the main
session before recording (NOT transcribed from agent claims). A34-A36 were
additionally confirmed by counter-search: no limit/colimit functor exists
anywhere in the tree, "cocompletion" occurs in 3 COMMENT lines and 0
assertions, and `Instance/Fun/` contains only `Cartesian.v`. A41 was
UPGRADED over the agent's framing once the source was read. A37/A38 are
recorded at the severity the file actually warrants — `Construction/Slice/Pullback.v`
**discloses its own stub**, so the verifier's "asserts as fact" framing is
softened here to match what is really on the page.

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| **A32** | Adjunction/Natural/Transformation.v:23-24 | Calls `[Category.Theory.Adjunction]` **"the universal-morphism form"**. That file self-describes at `Theory/Adjunction.v:46-47` as "**takes Kan's hom-set form as primitive**", and at :53-54 assigns the universal-arrow form to `Theory/Universal/Arrow.v`. Verified verbatim both ends. A reader following the cross-reference for universal arrows lands on a hom-setoid isomorphism. | Recorded. Fix: call it the hom-set form. Pairs with A33. |
| **A33** | Adjunction/Hom.v:36-37 | The **same** mislabel ("recover the counit/unit form … and the universal-morphism form `[Category.Theory.Adjunction]`"), and this one **propagates into the API surface**: `Adjunction_Hom_to_Universal` (:223) and `Adjunction_Universal_to_Hom` (:259) are named for a "universal" form that is actually the hom-set form. Correct labelling is: `Theory/Adjunction.v` = hom-set, `Adjunction/Hom.v` = hom-**bifunctor**, `Theory/Universal/Arrow.v` = universal-arrow. | Recorded — worse than A32 because renaming touches exported identifiers. |
| A34 | Structure/Limit.v:79-82 | "When limits of every shape exist, they assemble into functors right adjoint to the constant-diagram functors Δ" sits in a paragraph ("Two reformulations knit the concept into the rest of the theory") whose other two sentences **do** name in-tree files. The claim is true mathematically (cited to Riehl 4.6.1) but **no limit/colimit functor exists in-tree** (`LimitFunctor|Limit_Functor|ColimitFunctor|Colimit_Functor` → 0 hits), so in that position it reads as an in-tree reformulation. | Recorded. Fix: an explicit "not formalized in-tree" marker, as `Theory/Adjunction.v:442-456` already does for RAPL/LAPC. Rides **#353**. |
| A35 | Theory/Sheaf.v:116-118 | Header lists presheaf categories as "the free cocompletion and target of the Yoneda embedding" — the free-cocompletion half is **not asserted anywhere in-tree**. | Recorded; the obligation is tracked by **#717**. |
| A36 | Instance/Fun.v:59-63 | "Presheaf categories `[C^op, Sets]` are the leading case: their limits, colimits, epis and monos are computed objectwise, **they are cartesian closed**, and the Yoneda embedding … its **free cocompletion**" — THREE unbacked claims in one sentence, not one (scope widened on fess-aw9 F12): (a) objectwise **colimits** — `Instance/Fun/` holds only `Cartesian.v`; (b) **cartesian closed** — `Instance/Fun/Cartesian.v:111` supplies `Cartesian` (products) only, never `Closed`; (c) **free cocompletion** — 0 assertions tree-wide. Note the SAME file hedges correctly at :104-105 ("when C is small and D is cartesian closed and complete, `[C, D]` is cartesian closed (nLab)"), so :61's flat claim contradicts its own file's later, careful statement. | Recorded; (a)/(c) tracked by **#715**/**#717**. |
| A37 | Construction/Slice/Pullback.v:29-40 | Header states "**These two functors are adjoint**, dependent sum on the left and base change on the right … Σ_f ⊣ f\*" — and the adjunction is **not formalized**. Mitigating (verified): the same header immediately says it "is sketched in the **commented** `Base_Functor_Adjunction` stub below", so the file does disclose its own status; the opening sentence is nonetheless assertoric. | Recorded (MEDIUM, self-disclosed). The obligation is **#387**; the Π_f half is the new **#730**. |
| A38 | Construction/Slice/Pullback.v:121-127 | The commented stub states `Star_Functor f ⊣ Bang_Functor f` — i.e. **f\* ⊣ Σ_f, backwards** (dependent sum is the LEFT adjoint). Verified verbatim. Mitigating: the header at :39-40 **already flags exactly this** ("note that the live statement should read `Bang_Functor f ⊣ Star_Functor f`"). | Recorded (LOW — a known-wrong stub, annotated as such). Resolved by whoever implements **#387**. |
| A39 | Structure/Pullback.v:129-130 | Background essay asserts "The base-change functor between slices, with its left adjoint, is built in `Construction/Slice/Pullback.v`" — the left adjoint's **adjointness** is not built there (only the two functors). Same underlying gap as A37, different file. | Recorded; rides **#387**. |
| A40 | Instance/Poset.v:21-22 | Recurrence of **A1** (dangling `[Pos]` pointer), re-flagged independently by the Ch9 verifier. | Duplicate of A1 — resolved by **#641**. |
| **A41** | Instance/Comp.v:16 **↔** :41-44 | **UPGRADED after re-reading source** (the agent's "arity is an arbitrary type" framing understates it). Header :16 claims the file "develops **finitary** (single-sorted) universal algebra" — and the file **refutes this itself 25 lines later**: :41-44 says "The main trick is that the arity of an operation is *not* a number but rather a type … (And **nothing prevents us from having an infinite type as arity**.)", with `arity : operation → Type` at :47. The development is deliberately infinitary and says so; only the header says otherwise. | Recorded — self-refuting within one file, the strongest form. |
| A42 | Instance/Comp.v:34 | Comment opens "In universal algebra, a (**many-sorted**) signature …", contradicting :16's "**single-sorted**" in the same file. (Verified both lines.) So the header's parenthetical is wrong on **both** counts — finitary and single-sorted. | Recorded (pairs with A41). |
| A43 | Instance/Comp.v:210-213 | The comment justifying `Algs_Initial` argues "The free functor is left adjoint to the forgetful functor **and so preserves colimits**, hence sends the initial set ∅ to the initial algebra" — appealing to machinery that **does not exist in-tree**: verified `⊣` occurs **0 times** in the file, and there is no free or forgetful *functor* (only `Free : OpAlgebra S` at :92 and `induced_hom` at :108, a pointwise UMP). | Recorded (LOW — the argument is explanatory and the concrete construction that follows is self-contained, but it justifies a proof by an unavailable theorem). |

## Chapter 8 haul (2026-07-29) — 6 distinct; A26 is the campaign's clearest RULE VIOLATION

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| **A26** | Construction/Cayley.v:179-201 | **Violates the project's own CLAUDE.md rule** ("Never use `=` for morphisms. Always use `≈`"). `Cayley_Right`/`Cayley_Left` state **Leibniz equality between morphisms** (`f ∘ g ∘ h = fmap[From_Cayley] (…)`) — verified verbatim at :179-182. Worse, both are **guarded by the hypothesis `∀ a b (k : a ~> b), id[b] ∘ k = k`** — strict Leibniz `id_left`, which a general setoid-enriched category **cannot supply** (`id_left` is only an `≈`-law). So as stated the lemmas are conditional, and the file's headline claim (normalisation to a left-associated chain, :40-45) is not unconditional. **CORRECTION + MACHINE VERIFICATION (fess-aw8, compiled under Rocq 9.1):** an earlier draft here said the hypothesis is one "most in-tree categories do not have" — too strong. It IS dischargeable for *definitional-identity* categories: for `Instance/Coq.v`'s `Coq` it holds by `reflexivity` (eta). But `Fail reflexivity` confirms it does NOT hold for `Sets`, the setoid model, so it is genuinely unavailable in general. The auditor also **machine-proved the remediation**: for an ARBITRARY `C`, with NO hypothesis, the `≈` restatements (`Cayley_Right_setoid`/`Cayley_Left_setoid`) close by `simpl; rewrite id_left; reflexivity`. Restating with `≈` really does make both lemmas unconditional. | Recorded — **the most actionable defect found so far**: a genuine usability/soundness issue, not a doc mismatch. Rides the Cayley dedup target **#643**. |
| A27 | Construction/Cayley.v:39-41 | Header asserts "`To_Cayley`/`From_Cayley` … witness that this embedding is (split) faithful: `From_Cayley (To_Cayley f) ≈ f`" — **no lemma of that form exists**. :158-172 only DEFINE the two functors; the round-trip is never stated, and is used only implicitly inside the defective A26 lemmas. | Recorded (companion to A26). |
| A28 | Functor/Hom/Yoneda.v:22 | Header displays the formalized statement as "Nat([Hom ─,A], F) ≅ F A, **natural in both A and F**", but `Yoneda_Lemma` (:133) is only a pointwise family at fixed `F`; **neither naturality clause is proved** here or anywhere (Theory/Coend/Yoneda.v:174/:297 are likewise pointwise). | Recorded; the naturality obligation is separately tracked by **#316**. |
| A29 | Functor/Hom.v:29-32 | Prose calls `Curried_Hom : C^op ⟶ [C, Sets]` "a full and faithful embedding … the Yoneda embedding" — overstates what is proved at that site. | Recorded. |
| A30 | Structure/Topos.v:95-98 **↔** Construction/Slice.v:114-115 | **UPGRADED (fess-aw8): a TWO-FILE SELF-CONTRADICTION**, not merely a missing theorem. `Structure/Topos.v:95-97` says "Construction/Slice.v **records** the fundamental theorem of topos theory — every slice of a topos is again a topos", while `Construction/Slice.v:114-115` says of that very theorem: "it remains the headline theorem about this construction **not yet formalized here**." Each file's header contradicts the other. Same class as A14 (the three-file free-CCC contradiction). | Recorded — strongest form of this defect class. |
| A31b | Construction/Cayley.v:174 | Vacuous `Context `{Cayley}.` — neither lemma's statement mentions it; its only effect is to shift the hypothesis's auto-name to `H0`, which both proofs then rewrite. (fess-aw8) | Recorded (minor). |
| A31 | Instance/Fun/Cartesian.v:35 | Recurrence of **A23** (terminal/nullary product of `[C,D]` claimed "supplied separately"; it is not supplied). | Duplicate of A23. |

Also this chapter: **two Phase-C records carried factually FALSE negative claims**, caught and corrected in place by the verifier (provenance preserved) — (i) the evaluation-functor gap claimed "nothing in the tree has `fobj := fun P => P C`", but `Theory/Lawvere/Sets.v:83 ev1` is literally that; (ii) the graphs example claimed no file relates `Parallel` to quivers, but `Instance/Parallel.v:152-166` states it and defines `Presheaf_Graph`. Both stayed PARTIAL (the general obligations remain absent), but the *reasons* were wrong. This is the failure mode the hardened evidence prompt is meant to reduce — a **negative** claim asserted without an exhaustive search.

## Chapter 7 haul (2026-07-29) — 8 distinct, incl. a THREE-FILE "generic faithful inclusion" cluster

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| A20 | Construction/Subcategory.v:25-26 **+ 2 callers** | **A three-file cluster.** The header asserts the inclusion functor "is always faithful, since on each hom-set it is the first projection out of a sigma type" — but **no generic `Faithful Incl` exists anywhere**. Two independent files then cite it as if it did: `Structure/Binoidal/Central.v:242` ("the generic faithful `[Incl C CentralSub]` from Construction/Subcategory.v") and `Structure/Monoidal/CopyDiscard/Deterministic.v:586` (same wording for `Det ⟶ C`). Meanwhile `Theory/Sheaf/Category.v:103` **re-proves the generic one-liner ad hoc** for a single subcategory (`Sheaves_Faithful`, literally `intros …; exact Hfg`). Three consumers, one missing three-line lemma. | Recorded. A three-line `Incl_Faithful` in Construction/Subcategory.v would make all three comments true and let Sheaf/Category.v:103 stop re-proving it. Natural rider on any subcategory issue. |
| A21 | Functor/Coproduct.v:18-20 | Header claims `CoproductFunctor` "is the unique functor `[Id, Id] : C ∐ C ⟶ C` with `[Id,Id] ∘ inl = Id` and `… ∘ inr = Id`, furnished by the universal property of the coproduct in Cat". Reality: it is defined by **direct case analysis** on the sum (`sum_obj`/`sum_map`, :39-57); neither triangle is stated or proved, no uniqueness lemma exists, and Construction/Coproduct.v defines no injection FUNCTORS for the claimed triangles. The comment describes a UMP-mediated derivation the code never performs. **CORRECTION (fess-aw7):** an earlier draft of this entry claimed "`inl`/`inr` are not in-tree symbols at all" — that is FALSE. `inl`/`inr` ARE defined, as MORPHISMS, at `Structure/Cocartesian.v:142-143` (`Definition inl {x y} : x ~> x + y := @exl _ O _ _`) and are used 27x in that file. What is missing is the functor-level triangles/uniqueness, not the symbols. The filed artifact (#697's DoD) states the defect correctly and never contained this error. Line cite corrected: the header claim spans `Functor/Coproduct.v:19-22`, not :18-20. | Recorded. |
| A22 | Instance/Fun.v:246-253 | Header above `Functor_Setoid_Nat_Iso` overstates the theorem — but **at :248-249, not :250-252** (fess-aw7 correction). The accurate-but-flagged sentence is "an iso in `[C,D]` yields a family of componentwise isomorphisms" (that IS what the theorem gives, plus the naturality-conjugation condition). The real overstatement is the preceding clause: "a natural transformation is invertible **iff each of its components is invertible in D**" — `Functor_Setoid_Nat_Iso` quantifies over the EXISTENCE of an iso, not over a GIVEN natural transformation θ. The `awodey:7.5:lem11` coverage record identifies this precisely. | Recorded. |
| A23 | Instance/Fun/Cartesian.v:35-37 | Header parenthetical says the terminal/nullary product of `[C,D]` — the constant functor at D's terminal object — "is supplied separately". Substance CONFIRMED (fess-aw7): no terminal object for `[C,D]` exists in-tree (the only relative is `Functor/Structure/Terminal.v`'s `TerminalFunctor`, a property OF a functor, not the object). Precision note: the comment names no location, so "not supplied where claimed" overstates — it is simply not supplied. | Recorded. |
| A24 | Instance/Fun.v:104-106 (wording) | "when C is small and D is cartesian closed and complete, `[C,D]` is cartesian closed" — cites nLab for a result the file does not deliver; only the cartesian case is instantiated. (Related to the MacLane-era note on the same lines.) | Recorded (lower severity, wording). |
| A25 | Instance/Poset.v:21 | Dangling `[Pos]` — **fourth recurrence** (= A1/A18). | Rides #641/#680. |

Non-defect recorded for the ledger: `Functor/Coproduct.v`'s unused `@Cocartesian C`
context is odd (the construction needs nothing of C) but is **explicitly disclosed**
in the file header at :27-30 — documented, not misleading.

## Chapter 6 haul (2026-07-29) — 6 new distinct defects (+2 recurrences)
Theme: **headers asserting results the tree does not build**, including a
three-file contradiction about the free CCC.

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| A14 | Instance/Lambda/Sem.v:41-42 **and** Instance/Lambda/Sound.v:38-39 | Both headers assert the in-tree STLC syntax **IS the free CCC** ("By Lambek's theorem the syntax is the free cartesian closed category…"), **directly contradicting `Instance/Lambda.v:92-96`**, which disclaims exactly that. Three files, two claims, one denial — a reader believes whichever it opens first. Same shape as the Ch5 `Structure/Pullback.v` self-contradiction. | Recorded; the substantive obligation is tracked by **#693** (universal property of C(L) — the free CCC on a λ-theory). |
| A15 | Instance/AST.v:14, :23-38, :35-36, :91-93 | File titled "The free bicartesian closed category, presented syntactically"; header asserts "because the free category is initial among bicartesian closed categories…" — but **no initiality is proved** anywhere in the file or the tree (declarations end without it). Advertises a universal property it does not deliver. | PLACED by the drafter in **#693**'s DoD. |
| A16 | Structure/Cartesian/Closed.v:34, :47-50 | The `exp_iso` field comment claims it is "natural in x, y and z" and that "naturality recovers the substitution law" — the field is a bare hom-setoid isomorphism; naturality is **not** a field and is not proved. (Related but distinct from the Ch5 A11 apex-only issue.) | PLACED by the drafter in **#682**'s DoD (the currying-adjunction issue). |
| A17 | Structure/Closed.v:121-123 | The `[Curry]` header comment claims it "is what makes the `[ X , ─ ]` / `[ ─ , X ]` notations below denote genuine functors" — but **both notations are defined INSIDE the commented-out `Class Closed` block**, so neither exists in the environment. (Consistent with the known-dead-stub status of this file; CLAUDE.md already flags `Structure/Closed.v` as "an incomplete Eilenberg–Kelly stub… not yet in force".) | Recorded only (the file is a disclosed stub). |
| A18 | Instance/Poset.v:21 | Dangling `[Pos]` — **third recurrence** (= A1, also re-flagged in Ch3). | Rides **#641**/#680 (Pos is cartesian closed). |
| A19 | Instance/Proset.v:19 | Dangling `[Ord]` — **third recurrence** (= A2). | Still no home; trivial doc fix. |

Verifier record-nits (no verdict impact, recorded for accuracy): `Coq_ClosedMonoidal`
is at Instance/Coq.v:179 not :180; the FinSet hom description is at :20-22 not :15;
and the claim that the library "has no generic fix-one-argument-of-a-bifunctor
combinator" is too strong — `Cat_Closed` (Instance/Cat/Cartesian/Closed.v:45-48)
has exactly bifunctor currying, so `(−)^A` is *derivable* though nowhere named.

## Chapter 5 haul (2026-07-23) — 10 distinct defects, the richest of the campaign
Concentrated in the pullback / subobject / limit-preservation spine.

| # | File:line | Defect | Disposition |
|---|-----------|--------|-------------|
| A5 | Structure/Pullback.v:178 | Docstring over `pullback_unique` claims "unique up to a unique isomorphism **respecting the projections**", but the lemma states only `Pull f g P ≅ Pull f g Q` — no uniqueness of the comparison arrow, no commutation with `pullback_fst/snd`, and it is `Qed`-opaque so the triangles are unrecoverable. **The file contradicts itself** at :139-144, which concedes the strengthened uniqueness is "re-derived there because [pullback_unique] is Qed-opaque". | PLACED in issue **#672**'s DoD (draft 4). |
| A6 | Structure/Pullback.v:80 | Header's Wikipedia quote closes with "(proven below as [pullback_unique])" — overclaims for the A5 reason. Honest pointer is `PullbackTransport`/`pullback_transport` (Theory/Morphisms/Stability.v:313-330), which actually carries `transport_fst/snd`. | Same cluster as A5 → **#672**. |
| A7 | Structure/Pullback.v:129-130 | "The base-change functor between slices, **with its left adjoint**, is built in Construction/Slice/Pullback.v as [Bang_Functor] ⊣ [Star_Functor]" — only the two functors exist (:50, :67); the adjunction is a COMMENTED-OUT stub (:121-126) whose own header admits it is "sketched" and mis-oriented. Reader following the ⊣ finds nothing. **(Same defect MacLane #3 found via Structure/Pullback.v — recurring.)** | Recorded; relates to MacLane defect ledger entry + issue #387. |
| A8 | Theory/Subobject.v:56 | Comment on `sub_le` asserts "The factorization is automatically monic-through" — no such lemma anywhere (`Monic (m'∘f) → Monic f` right-cancellation is absent tree-wide). This IS `awodey:5.1:prop-factor-through-mono-is-mono`, classified ABSENT. The neighbouring uniqueness claim DOES carry a pointer, making the unproven half read as equally established. | PLACED in issue **#669**'s DoD (draft 1). |
| A9 | Structure/Cone.v:39 (cosmetic) | `AConeEquiv` binds `c (F : C ⟶ J)`, **reversing** the diagram/target roles vs `ACone` (:24) and `Cone` (:51), both `F : J ⟶ C`; surrounding comments say "a diagram F : J ⟶ C". Typechecks only because both categories are universally quantified. | Recorded only (cosmetic). |
| A10 | Instance/Cones.v:45 | Comment says "Its **initial** object is the colimit of F", but for `Cocones F := Cones (F^op)` (:46) the colimit is the **TERMINAL** object (`Colimit F := Limit (F^op)`, Limit = terminal cone). Comment is wrong by a duality slip. | Recorded only (doc fix). |
| A11 | Structure/Limit/Preservation.v:20-24 | The file that OWNS the preservation vocabulary defines preservation as the **apex-only** condition and calls `PreservesAllLimits` "continuous" (nLab citation :15) **without disclosing** the class is too weak — the library itself discloses this elsewhere (the cone-level `PreservesImageLimit` exists because apex-only is insufficient; see MacLane Phase-14 history). | PLACED as a checkbox on the **#427** dedup append. |
| A12 | Adjunction/Continuity.v:198-203 | `right_adjoint_preserves_limit(s)` PROVE the cone-level statement (`rapl_acone` :102 with image legs :91-92, mediator+uniqueness :139/:156) but **export only the apex-only** `PreservesLimit`/`PreservesAllLimits` — the stronger proven content is discarded at the interface. | Recorded; natural companion to A11/#427. |
| A13 | Instance/Poset.v:21 | Dangling `[Pos]` cross-ref (already recorded as **A1**; re-confirmed this chapter with the note that the `[Name]` bracket convention is used for real in-tree identifiers throughout that same file, so it genuinely reads as a pointer). | Duplicate of A1 → rides **#641**. |

**Process note (my closeout grep was blind):** the Ch5 verifiers emitted their
LIBRARY-DEFECT entries at the **workflow/coverage level** (`coverage[].problems[]`),
NOT inside each verified record — the verified-*.json records have no
`problems[]` key at all. My reconcile grep over `verified-*.json` therefore
returned 0 and would have lost all ten. FIXED: the closeout reconcile must read
BOTH (a) the workflow result's `coverage[].problems[]` and (b) each record's
`problems[]`/`verifier.notes`. The drafter correctly flagged this schema gap.

Note: the Awodey Ch1 drafter's LIBRARY-DEFECT scan under-counted (reported
"no LIBRARY-DEFECT flags") although the batch-0 verifier flagged both A1/A2
in its problems[]. Same batch-scoped under-count seen at MacLane Ch V — the
closeout reconciliation grep over verified-*.json catches them (this file).
